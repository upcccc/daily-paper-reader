---
title: "RAIN: Secure and Robust Aggregation under Shuffle Model of Differential Privacy"
title_zh: RAIN：差分隐私洗牌模型下的安全鲁棒聚合
authors: "Yuhang Li, Yajie Wang, Xiangyun Tang, Peng Jiang, Yu-an Tan, Liehuang Zhu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03108v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 洗牌差分隐私模型下的安全鲁棒聚合
tldr: 本研究针对差分隐私洗牌模型（Shuffle-DP）在联邦学习中面临的投毒攻击和不可信洗牌器挑战，提出了RAIN框架。RAIN通过符号空间聚合增强鲁棒性，并设计了基于加性秘密共享的洗牌与聚合协议，在保护隐私的同时实现了可验证的安全性。实验表明，该框架在保持高精度的同时，显著降低了通信开销并提升了聚合速度，有效抵御了恶意攻击。
selection_source: fresh_fetch
motivation: 现有的Shuffle-DP模型因梯度失真和身份匿名化，难以抵御恶意投毒攻击且面临洗牌器被攻破的安全风险。
method: 提出RAIN框架，采用符号空间聚合衡量更新一致性，并结合秘密共享协议与实时完整性验证机制。
result: 实验显示RAIN在维持强隐私保护的同时，通信成本降低了90倍，聚合速度提升了10倍，且能完全检测篡改。
conclusion: RAIN成功统一了Shuffle-DP下的隐私性、鲁棒性与可验证性，为安全联邦学习提供了高效且可靠的解决方案。
---

## 摘要
安全聚合是隐私保护学习的基础构建模块，但在对抗行为下实现鲁棒性仍然具有挑战性。现代系统越来越多地采用差分隐私洗牌模型（Shuffle-DP），通过在本地扰动客户端更新并通过洗牌进行全局匿名化，以增强隐私保护。然而，这些扰动和匿名化扭曲了梯度几何结构并消除了身份关联，使系统容易受到对抗性投毒攻击。此外，洗牌器（通常是第三方）可能会被攻破，从而削弱了针对恶意对抗者的安全性。为了应对这些挑战，我们提出了 RAIN（噪声中的鲁棒聚合），这是一个在 Shuffle-DP 下协调隐私性、鲁棒性和可验证性的统一框架。其核心是，RAIN 采用符号空间聚合来鲁棒地衡量更新一致性，并在噪声和匿名化环境下限制恶意影响。具体而言，我们设计了两种新型的用于洗牌和聚合的秘密共享协议，它们直接在加法份额上运行，并保留了 Shuffle-DP 紧致的隐私保证。在每一轮中，聚合结果都会经过验证，以确保聚合的正确性并检测任何选择性丢弃，从而以极低的开销实现恶意安全性。在全面基准测试上的广泛实验表明，RAIN 在 Shuffle-DP 下保持了强大的隐私保证，并且对投毒攻击具有鲁棒性，准确率和收敛性的下降可以忽略不计。此外，它还提供具有完整篡改检测的实时完整性验证，与之前的工作相比，通信成本降低了高达 90 倍，聚合速度提高了 10 倍。

## Abstract
Secure aggregation is a foundational building block of privacy-preserving learning, yet achieving robustness under adversarial behavior remains challenging. Modern systems increasingly adopt the shuffle model of differential privacy (Shuffle-DP) to locally perturb client updates and globally anonymize them via shuffling for enhanced privacy protection. However, these perturbations and anonymization distort gradient geometry and remove identity linkage, leaving systems vulnerable to adversarial poisoning attacks. Moreover, the shuffler, typically a third party, can be compromised, undermining security against malicious adversaries. To address these challenges, we present Robust Aggregation in Noise (RAIN), a unified framework that reconciles privacy, robustness, and verifiability under Shuffle-DP. At its core, RAIN adopts sign-space aggregation to robustly measure update consistency and limit malicious influence under noise and anonymization. Specifically, we design two novel secret-shared protocols for shuffling and aggregation that operate directly on additive shares and preserve Shuffle-DP's tight privacy guarantee. In each round, the aggregated result is verified to ensure correct aggregation and detect any selective dropping, achieving malicious security with minimal overhead. Extensive experiments across comprehensive benchmarks show that RAIN maintains strong privacy guarantees under Shuffle-DP and remains robust to poisoning attacks with negligible degradation in accuracy and convergence. It further provides real-time integrity verification with complete tampering detection, while achieving up to 90x lower communication cost and 10x faster aggregation compared with prior work.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **RAIN (Robust Aggregation in Noise)** 的框架，旨在解决联邦学习中差分隐私洗牌模型（Shuffle-DP）与鲁棒聚合之间的冲突。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：联邦学习（FL）常采用差分隐私洗牌模型（Shuffle-DP）来平衡隐私与效用。它通过本地扰动（LDP）和匿名洗牌（Shuffling）实现隐私放大。
*   **核心问题**：
    1.  **鲁棒性失效**：Shuffle-DP 引入的噪声扭曲了梯度的几何结构，且洗牌过程消除了客户端的身份连续性，导致现有的基于几何（如距离过滤）或基于声誉（如历史记录）的鲁棒聚合方法完全失效。
    2.  **洗牌器不可信**：现有的 Shuffle-DP 框架通常假设洗牌器是诚实的，但如果洗牌器被攻破或与服务器合谋，它可以恶意篡改、丢弃或重新排序更新，而无需担心被检测。
*   **研究动机**：开发一个既能保持 Shuffle-DP 隐私优势，又能抵御恶意投毒攻击，且不依赖可信洗牌器的统一框架。

### 2. 方法论：核心思想与关键技术
RAIN 的核心思想是**“在符号空间进行鲁棒聚合”**，并结合**“基于秘密共享的加密计算”**。
*   **符号空间聚合（Sign-space Aggregation）**：
    *   **本地随机化**：客户端使用 Sign-Gaussian 机制，对梯度进行剪裁、加噪并提取符号（+1/-1）。
    *   **汉明距离度量**：利用一个小的公共参考数据集生成参考方向。服务器计算匿名更新与参考方向之间的汉明距离，而非欧氏距离。
    *   **ReLU 自适应权重**：根据汉明距离通过 ReLU 函数分配权重，距离越远（一致性越低）的更新权重越低甚至为零，从而限制恶意影响。
*   **秘密共享洗牌与聚合协议**：
    *   采用**二服务器架构（S0, S1）**和加性秘密共享。客户端将更新拆分为两个份额发送给非合谋的服务器。
    *   服务器在加密份额上直接执行洗牌和聚合，确保任何单一服务器都无法获知原始更新或洗牌置换关系。
*   **流式完整性验证（Streaming Integrity Verification）**：
    *   引入轻量级消息认证码（MAC）。在洗牌和聚合的每个步骤中实时验证消息，能够 100% 检测到服务器的选择性丢弃或篡改行为。

### 3. 实验设计
*   **数据集**：MNIST（IID 设定）、Fashion-MNIST（非 IID 设定）、CIFAR-10（非 IID 设定）。
*   **攻击场景（Benchmark）**：
    *   **传统攻击**：标签翻转（Label Flipping）、Krum 攻击、Trim 攻击、缩放攻击（Scaling Attack）。
    *   **自适应攻击**：针对 DP-FL 特别设计的 Attack-DPFL，该攻击通过调整恶意更新的范数来模拟加噪后的良性更新。
*   **对比方法**：
    *   **基准**：FedAvg, SignSGD。
    *   **鲁棒聚合类**：Krum, Trimmed-Mean, Median, FLTrust, FoolsGold, FLAME, FLOD, FLARE 等。
    *   **安全框架类**：Camel, FLGuard。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **效率指标**：论文重点展示了通信开销和运行时间的对比。例如，在服务器端，RAIN 的运行时间在 0.9 秒左右（MNIST/FMNIST），显著优于 FLGuard 的 5-7 秒。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括收敛性分析、隐私-效用-鲁棒性三者权衡实验、针对 5 种不同攻击的防御实验、以及不同客户端规模（K）下的通信/计算效率实验。
*   **充分性评价**：实验设计较为充分。它不仅对比了传统的鲁棒方法，还证明了这些方法在 Shuffle-DP 环境下的失效，从而突出了 RAIN 的必要性。通过将恶意客户端比例提升至 90% 进行极端测试，验证了算法的鲁棒边界。

### 6. 主要结论与发现
*   **鲁棒性突破**：在 20% 恶意客户端的 Attack-DPFL 攻击下，传统方法准确率普遍下降 40-50%，而 RAIN 保持了极高的稳定性。即使在 90% 恶意客户端的极端情况下，RAIN 仍能维持约 80% 的准确率。
*   **隐私放大**：RAIN 完美保留了 Shuffle-DP 的隐私放大效应，在 $\epsilon=2$ 时即可实现稳定的鲁棒聚合。
*   **高效性**：相比于之前的安全 Shuffle-DP 框架（如 Camel），RAIN 的通信成本降低了高达 90 倍，聚合速度提升了 10 倍。
*   **完整性**：流式 MAC 机制实现了 100% 的篡改检测率，且额外开销低于 1%。

### 7. 优点（亮点）
*   **几何无关性**：巧妙地将鲁棒性检测从易受噪声干扰的欧氏空间转移到统计特性更稳定的符号空间（汉明距离）。
*   **去中心化信任**：通过二服务器 MPC 协议消除了对“可信第三方洗牌器”的依赖。
*   **实时验证**：不同于事后审计，其流式验证机制可以在攻击发生时立即中断协议，防止隐私泄露。

### 8. 不足与局限
*   **依赖参考集**：RAIN 需要一个小的、审计过的公共“根数据集”来生成参考方向。在某些极端隐私敏感或数据分布极度偏移的场景下，获取此类数据可能存在挑战。
*   **非 IID 挑战**：虽然实验涉及了非 IID 设定，但讨论部分提到，在极度非 IID 且高噪声环境下，符号一致性可能会减弱，需进一步研究更精细的阈值自适应策略。
*   **算力细节缺失**：缺乏具体的硬件环境描述，不便于其他研究者精确复现其性能基准。

（完）
