---
title: Distributed Deep Variational Approach for Privacy-preserving Data Release
title_zh: 用于隐私保护数据发布的分布式深度变分方法
authors: "Zahir Alsulaimawi, Huaping Liu"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03069v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 联邦学习中的隐私保护数据发布
tldr: 针对联邦学习中梯度和表示可能泄露敏感信息的问题，本文提出高斯隐私保护器（GPP）。该框架利用变分下界最小化表示与敏感属性间的互信息，同时保留效用属性。通过在联邦设置下部署本地编码器，GPP在保证原始数据不离地的基础上，实现了实例级的隐私保护，在多个数据集上证明了其能在维持高效用的同时有效抵御属性推理攻击。
selection_source: fresh_fetch
motivation: 联邦学习虽能保持数据本地化，但模型更新和发布的表示仍可能泄露用户的敏感属性。
method: 提出GPP框架，通过随机编码器将原始数据映射为脱敏表示，并利用变分推断在最小化敏感信息泄露与最大化任务效用之间取得平衡。
result: 在MNIST、CelebA和HAPT数据集上的实验表明，GPP在保持与无约束模型相当的效用时，能将攻击者的识别准确率降至接近随机猜测。
conclusion: GPP为分布式环境下的隐私保护数据发布提供了一种有效方案，兼顾了数据效用与实例级的隐私安全性。
---

## 摘要
联邦学习（FL）允许分布式节点在不交换原始数据的情况下训练共享模型，但在医疗传感器、物联网设备、可穿戴设备等隐私敏感的部署场景中，仅靠数据本地化提供的保护是不完整的：梯度、模型更新以及发布的表示本身都可能泄露敏感属性。我们提出了高斯隐私保护器（Gaussian Privacy Protector, GPP），这是一种针对连续高维输入的数据发布框架，它学习一个随机编码器，将原始数据映射到低维的脱敏表示。该编码器针对发布表示与指定敏感属性之间互信息的变分下界进行训练，同时通过一个独立的交叉熵项来保留指定的效用属性，并使用拉格朗日乘子 β 来控制两者之间的权衡。随后，我们将 GPP 扩展到联邦设置中，其中每个客户端训练一个本地编码器，敏感标签从不离开客户端，聚合器仅接收脱敏后的表示，从而在联邦学习标准的“原始数据留在本地”保证之外，提供了实例级的隐私保护。我们在 MNIST（数字和效用，奇偶敏感）、CelebA（微笑 vs. 性别）和 HAPT-Recognition（活动 vs. 受试者身份）上对 GPP 进行了评估。在所有三个基准测试中，GPP 获得的效用与无约束自编码器基准相比差距在约一个百分点以内，同时将攻击者的 AUC 降低到接近随机猜测的水平。

## Abstract
Federated learning (FL) lets distributed nodes train a shared model without exchanging their raw data, but in privacy-sensitive deployments medical sensors, IoT devices, wearables the protection offered by keeping data local is incomplete: gradients, model updates, and the released representations themselves can leak sensitive attributes. We propose the \emph{Gaussian Privacy Protector} (GPP), a data-release framework for continuous, high-dimensional inputs that learns a stochastic encoder mapping raw data to a low-dimensional sanitized representation. The encoder is trained against a variational lower bound on the mutual information between the released representation and a designated sensitive attribute, while a separate cross-entropy term preserves a designated utility attribute, with a Lagrange multiplier $β$ controlling the trade-off. We then extend GPP to the federated setting, in which each client trains a local encoder, sensitive labels never leave the client, and the aggregator receives only sanitized representations giving instance-level privacy protection in addition to the standard ``raw data stays local'' guarantee of FL. We evaluate GPP on MNIST (digit-sum utility, parity sensitive), CelebA (smiling vs.\ gender), and HAPT-Recognition (activity vs.\ subject identity). Across all three benchmarks, GPP attains utility within roughly one percentage point of an unconstrained autoencoder baseline while reducing the adversary's AUC to near random guessing.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**高斯隐私保护器（Gaussian Privacy Protector, GPP）**的框架，旨在解决分布式学习（如联邦学习）中即便原始数据不离地，模型更新和数据表示仍可能泄露敏感属性的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在联邦学习（FL）等分布式场景中，虽然原始数据保留在本地，但传输的梯度、模型更新或中间表示（Representations）仍包含敏感信息（如性别、种族、身份指纹等）。
*   **研究动机**：现有的隐私保护技术（如差分隐私 DP）在处理图像、视频等高维连续数据时，往往会导致严重的效用（Utility）损失。论文旨在开发一种能够将高维数据映射到低维脱敏表示的方法，在最大化保留有用信息的同时，消除敏感属性的泄露。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：基于信息论中的**信息瓶颈（Information Bottleneck）**原理，通过变分推断构建一个端到端的优化目标。
*   **关键技术细节**：
    *   **随机编码器**：学习一个映射 $P(Z|X)$，将原始数据 $X$ 转换为低维潜在表示 $Z$。
    *   **变分目标函数**：目标是最小化互信息 $I(Z; S)$（$S$ 为敏感属性）并最大化 $I(Z; U)$（$U$ 为效用属性）。
    *   **鞍点优化（Saddle-point）**：引入拉格朗日乘子 $\beta$ 控制权衡。编码器与“对手网络（Adversary）”进行博弈（最小化对手推断敏感属性的能力），同时与“效用分类器”协作（最大化效用预测准确率）。
    *   **高斯潜在空间正则化**：借鉴 VAE 的重参数化技巧，强制潜在表示服从标准高斯分布，增加随机性以增强隐私。
    *   **分布式扩展（Distributed GPP）**：每个客户端运行本地编码器和本地对手网络，敏感标签不离开本地；聚合器仅接收脱敏后的表示 $Z$ 和效用标签 $U$。

### 3. 实验设计
*   **数据集**：
    1.  **MNIST**：效用任务为数字之和（0-18），敏感属性为奇偶性。
    2.  **CelebA**：效用任务为是否微笑，敏感属性为性别。
    3.  **HAPT-Recognition**：手机传感器数据，效用任务为活动识别（6类），敏感属性为受试者身份（30人）。
*   **Benchmark 与对比方法**：
    *   **No-Privacy**：无约束的自编码器（效用上限）。
    *   **Random Projection**：随机投影（仅降维，不学习）。
    *   **Noisy Encoder**：类似 DP 的加噪编码器。
*   **评估指标**：效用 AUC（越高越好）和对手 AUC（越接近 0.5 随机猜测越好）。

### 4. 资源与算力
*   **硬件环境**：论文明确提到实验运行在单台 **NVIDIA Tesla V100 GPU** 上。
*   **训练细节**：使用了 Adam 优化器，学习率为 $10^{-4}$，Batch Size 为 64。具体的总训练时长未详细说明，但提到每个配置在 MNIST 上训练了 50 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文设计了 6 组核心实验，包括基准对比、$\beta$ 参数扫描、瓶颈维度 $d_z$ 影响、对手更新频率 $k$ 的消融实验、效用-敏感属性相关性（$\rho$）的鲁棒性测试，以及 10 次随机运行的统计显著性分析。
*   **充分性评价**：实验设计非常充分且客观。它不仅验证了方法的有效性，还通过消融实验证明了“对手训练”是隐私保护的关键（而非仅仅靠降维），并探讨了属性高度相关时的极端情况，具有较强的说服力。

### 6. 主要结论与发现
*   **高效用与强隐私**：在三个数据集上，GPP 的效用损失与无约束模型相比通常在 **1%** 以内，而对手的预测能力被降至接近 **0.5（随机猜测）**。
*   **权衡可控**：参数 $\beta$ 能平滑地调节隐私与效用之间的天平。
*   **分布式有效性**：分布式 GPP 在 HAPT 数据集上的表现与中心化训练几乎一致（差距在 0.005 AUC 以内），证明了其在联邦场景下的可行性。

### 7. 优点
*   **理论严谨**：提供了完整的变分推断推导，将隐私保护问题转化为清晰的信息论优化目标。
*   **实例级保护**：相比于传统的联邦学习，GPP 提供了更细粒度的隐私保护，防止了通过表示或梯度进行的属性推理攻击。
*   **通信效率**：由于传输的是低维表示 $Z$ 而非高维梯度或原始数据，在特定条件下（Batch Size 较小时）能显著降低通信带宽需求。

### 8. 不足与局限
*   **威胁模型限制**：假设聚合器是“诚实但好奇”的。如果聚合器是恶意的并主动操纵效用分类器的梯度来探测客户端，该框架可能需要结合安全聚合或差分隐私噪声。
*   **SOTA 对比略显不足**：虽然对比了基础基准，但未在实验部分直接与最新的同类对抗性脱敏方法（如 Adversarial Censoring 的最新变体）进行大规模的数值对比。
*   **属性依赖性**：当效用属性与敏感属性在统计上完全重合时，任何隐私保护方法都无法在不损失效用的情况下保护隐私，GPP 也不例外。

（完）
