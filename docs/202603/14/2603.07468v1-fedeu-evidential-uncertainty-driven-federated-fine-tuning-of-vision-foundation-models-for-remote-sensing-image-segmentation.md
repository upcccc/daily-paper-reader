---
title: "FedEU: Evidential Uncertainty-Driven Federated Fine-Tuning of Vision Foundation Models for Remote Sensing Image Segmentation"
title_zh: FedEU：证据不确定性驱动的遥感图像分割视觉基础模型联邦微调
authors: "Xiaokang Zhang, Xuran Xiong, Jianzhong Huang, Lefei Zhang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07468v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 跨分布式数据集的联邦微调
tldr: 针对遥感图像分割在联邦学习中面临的数据异构性和模型更新不确定性挑战，本文提出FedEU框架。该框架结合参数高效微调技术，引入证据不确定性建模来量化局部模型风险，并通过客户端特定特征嵌入（CFE）和Top-k不确定性引导加权（TUW）策略优化全局聚合。实验证明，FedEU能有效缓解分布偏移，在提升分割性能的同时增强了联邦学习系统的鲁棒性与可靠性。
selection_source: fresh_fetch
motivation: 预训练基础模型在联邦异构数据下进行微调时，缺乏对局部模型更新不确定性的有效估计，导致协作优化过程不够可靠。
method: 提出一种由证据不确定性驱动的联邦优化框架，利用个性化不确定性建模、客户端特定特征嵌入以及Top-k不确定性引导的加权聚合策略。
result: 在三个大规模异构遥感数据集上的实验结果表明，FedEU在性能上显著优于现有方法，并能有效降低预测的不确定性。
conclusion: FedEU通过显式建模和利用不确定性信息，实现了在多样化客户端间的平衡模型适配，为遥感图像分割提供了更稳健的联邦学习方案。
---

## 摘要
联邦环境下的遥感图像分割（RSIS）因其能够在不共享原始图像或标注的情况下，跨分布式数据集实现协作模型训练而受到越来越多的关注。联邦 RSIS 与参数高效微调（PEFT）相结合，可以释放预训练基础模型在实际应用中的泛化能力，且参数聚合和通信开销极小。然而，由于缺乏对每个本地模型的不确定性估计，预训练模型对异构客户端数据的动态适应不可避免地增加了更新的不确定性，并损害了协作优化的可靠性。为了弥补这一差距，我们提出了 FedEU，这是一个由证据不确定性驱动的 RSIS 模型微调联邦优化框架。具体而言，引入了个性化证据不确定性建模来量化本地模型的认知差异，并识别本地数据分布下的高风险区域。此外，利用客户端特定特征嵌入（CFE）来增强通道感知特征表示，同时通过个性化注意力和元素感知参数更新方法保留客户端特定属性。这些不确定性估计被上传到服务器，通过 Top-k 不确定性引导加权（TUW）策略实现自适应全局聚合，从而减轻分布偏移和不可靠更新的影响。在三个大规模异构数据集上的广泛实验证明了 FedEU 的卓越性能。更重要的是，FedEU 通过显式降低预测不确定性，实现了跨不同客户端的平衡模型自适应，从而产生了更稳健、更可靠的联邦结果。源代码将在 https://github.com/zxk688/FedEU 提供。

## Abstract
Remote sensing image segmentation (RSIS) in federated environments has gained increasing attention because it enables collaborative model training across distributed datasets without sharing raw imagery or annotations. Federated RSIS combined with parameter-efficient fine-tuning (PEFT) can unleash the generalization power of pretrained foundation models for real-world applications, with minimal parameter aggregation and communication overhead. However, the dynamic adaptation of pretrained models to heterogeneous client data inevitably increases update uncertainty and compromises the reliability of collaborative optimization due to the lack of uncertainty estimation for each local model. To bridge this gap, we present FedEU, a federated optimization framework for fine-tuning RSIS models driven by evidential uncertainty. Specifically, personalized evidential uncertainty modeling is introduced to quantify epistemic variations of local models and identify high-risk areas under local data distributions. Furthermore, the client-specific feature embedding (CFE) is exploited to enhance channel-aware feature representation while preserving client-specific properties through personalized attention and an element-aware parameter update approach. These uncertainty estimates are uploaded to the server to enable adaptive global aggregation via a Top-k uncertainty-guided weighting (TUW) strategy, which mitigates the impact of distribution shifts and unreliable updates. Extensive experiments on three large-scale heterogeneous datasets demonstrate the superior performance of FedEU. More importantly, FedEU enables balanced model adaptation across diverse clients by explicitly reducing prediction uncertainty, resulting in more robust and reliable federated outcomes. The source codes will be available at https://github.com/zxk688/FedEU.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FedEU** 的联邦学习框架，旨在解决遥感图像分割（RSIS）在分布式环境下的模型微调问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景：** 遥感图像通常分散在不同机构，由于隐私和安全原因难以集中。联邦学习（FL）允许在不共享原始数据的情况下协作训练模型。
*   **核心痛点：** 
    1.  **数据异构性（Non-IID）：** 不同地区的遥感图像在光谱、空间结构上存在巨大差异。
    2.  **更新不确定性：** 在联邦微调视觉基础模型（如 SAM）时，局部模型对异构数据的适应会导致更新方向不一致，缺乏对局部模型“可靠性”的评估，容易导致全局模型漂移。
*   **研究目的：** 引入**证据不确定性（Evidential Uncertainty）**来量化局部模型的认知风险，并以此指导全局聚合，提升联邦微调的鲁棒性和准确性。

### 2. 方法论
FedEU 框架由以下三个核心组件构成：
*   **证据不确定性建模（EU Head）：** 
    *   在解码器后添加一个证据头，利用**证据深度学习（EDL）**将预测建模为狄利克雷分布。
    *   通过计算“证据量”来推导认知不确定性（Epistemic Uncertainty），从而识别模型在局部数据分布下的高风险预测区域。
*   **客户端特定特征嵌入（CFE）：** 
    *   引入轻量级的通道注意力机制，结合客户端 ID 的 Embedding。
    *   通过门控机制自适应地调整特征通道，使模型能够捕捉客户端特定的空间和光谱属性。
*   **Top-k 不确定性引导加权（TUW）聚合：** 
    *   客户端上传模型参数的同时上传局部预测的 Top-k 像素级平均不确定性。
    *   服务器根据不确定性反向分配权重：不确定性越高的客户端，其在全局聚合中的权重越低，从而抑制不可靠更新对全局模型的影响。

### 3. 实验设计
*   **数据集：**
    1.  **GF-7 Building Dataset：** 中国6个城市的建筑提取，体现城市间的空间异构性。
    2.  **GLH-Water Dataset：** 全球范围的地表水提取，体现全球尺度的光谱差异。
    3.  **GVLM Dataset：** 跨国滑坡制图，具有极高的类别不平衡和边界复杂性。
*   **Benchmark（对比方法）：**
    *   通用联邦学习：FedAvg, FedProx。
    *   分割专用联邦学习：FedSeg, FedTGP。
    *   个性化联邦学习：Per-FedAvg, FedBN。
*   **评价指标：** 交并比（IoU）和总体准确率（OA）。

### 4. 资源与算力
*   **基础模型：** 采用 SAM (Segment Anything Model) 的 ViT-B 版本作为编码器。
*   **微调策略：** 使用参数高效的 **Adapter-tuning**。
*   **算力说明：** 论文中**未明确提及具体的 GPU 型号和数量**。实验设置提到：通信轮数 $T=200$，局部训练 Epoch $E=5$，Batch size 为 8，使用 Adam 优化器。

### 5. 实验数量与充分性
*   **实验规模：** 在三个不同任务（建筑、水体、滑坡）的大规模数据集上进行了验证，每个数据集包含多个异构客户端。
*   **消融实验：** 针对 CFE 模块和 TUW 策略分别进行了消融，验证了各组件对缓解模型漂移的贡献。
*   **深度分析：** 包含了参数敏感性分析（针对损失权重 $\mu$）、收敛性分析以及不确定性与预测误差的相关性可视化。
*   **评价：** 实验设计较为全面且客观，通过跨任务、跨区域的对比，充分证明了方法在处理遥感 Non-IID 数据时的有效性。

### 6. 主要结论与发现
*   **性能领先：** FedEU 在所有三个数据集上的 IoU 均显著优于现有方法（例如在 GF-7 上 IoU 提升了约 3.5%~6.6%）。
*   **不确定性价值：** 显式建模不确定性能有效识别模型“不知道”的区域，通过降低这些区域的聚合权重，能显著加速收敛并提升泛化性能。
*   **个性化适配：** CFE 模块能有效校准特征空间，使基础模型在保持全局一致性的同时，更好地适应局部地理环境。

### 7. 优点
*   **创新性：** 首次将证据学习（Evidential Learning）引入联邦微调领域，为解决联邦学习中的“模型不可靠更新”提供了新视角。
*   **高效性：** 结合 PEFT 技术，在保证高性能的同时，极大地降低了分布式环境下的通信开销。
*   **鲁棒性：** Top-k 引导的加权策略比简单的平均聚合（FedAvg）更能抵抗极端异构数据带来的负面影响。

### 8. 不足与局限
*   **算力细节缺失：** 未提供具体的训练时长和硬件资源消耗，难以评估其实际部署的成本。
*   **通信频率：** 虽然参数量减少了，但 200 轮通信在某些带宽受限的遥感应用场景下可能仍然偏高。
*   **任务覆盖：** 目前主要集中在二值分割任务（建筑、水体、滑坡），对于多类别、更复杂的语义分割场景（如全类地物覆盖）尚待进一步验证。

（完）
