---
title: Amplified Patch-Level Differential Privacy for Free via Random Cropping
title_zh: 通过随机裁剪免费增强 Patch 级别的差分隐私
authors: "Kaan Durmaz, Jan Schuchardt, Sebastian Schmidt, Stephan Günnemann"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24695v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 通过机器学习训练中的随机裁剪增强差分隐私
tldr: 本研究探讨了计算机视觉中常用的随机裁剪对差分隐私（DP）的放大作用。研究发现，当敏感信息在空间上局部化时，随机裁剪能以一定概率排除这些内容，从而在梯度噪声和随机采样之外提供第三种随机性来源。作者提出了“补丁级”邻近关系，并为结合随机裁剪的 DP-SGD 推导了紧致的隐私界限。该方法无需修改模型架构或训练流程，即可在图像分割等任务中实现更优的隐私与效用权衡，证明了利用现有随机性提升隐私保护的潜力。
selection_source: fresh_fetch
motivation: 传统的差分隐私训练忽略了随机裁剪在排除局部敏感特征（如人脸、车牌）时所带来的天然隐私保护能力。
method: 引入补丁级邻近关系，通过量化补丁包含概率，推导出随机裁剪与小批量采样结合后的紧致隐私放大界限。
result: 在多种分割架构和数据集上的实验验证了补丁级放大能在不增加计算成本的情况下显著改善隐私与效用的权衡。
conclusion: 将隐私核算与领域特定的数据结构及现有随机性相结合，可以在不改变训练过程的前提下获得更强的隐私保证。
---

## 摘要
随机裁剪是计算机视觉中最常用的数据增强技术之一，然而其固有的随机性在训练差分隐私机器学习模型中的作用至今尚未被探索。我们观察到，当图像中的敏感内容在空间上是局部的（例如人脸或车牌）时，随机裁剪可以以一定的概率将该内容从模型的输入中排除。这在采用随机梯度下降的差分隐私训练中引入了除梯度噪声和微批次采样之外的第三种随机性来源。这种额外的随机性在无需更改模型架构或训练过程的情况下增强了差分隐私。我们通过为视觉数据引入 Patch 级别的相邻关系，并推导结合随机裁剪的差分隐私随机梯度下降（DP-SGD）的紧致隐私界限，从而将这一效应形式化。我们的分析量化了 Patch 的包含概率，并展示了它如何与微批次采样复合，从而产生更低的有效采样率。在实验上，我们验证了 Patch 级别的增强在多种分割架构和数据集上改善了隐私与效用之间的权衡。我们的结果表明，将隐私核算与领域结构以及额外的现有随机性来源相结合，可以在不增加额外成本的情况下提供更强的保证。

## Abstract
Random cropping is one of the most common data augmentation techniques in computer vision, yet the role of its inherent randomness in training differentially private machine learning models has thus far gone unexplored. We observe that when sensitive content in an image is spatially localized, such as a face or license plate, random cropping can probabilistically exclude that content from the model's input. This introduces a third source of stochasticity in differentially private training with stochastic gradient descent, in addition to gradient noise and minibatch sampling. This additional randomness amplifies differential privacy without requiring changes to model architecture or training procedure. We formalize this effect by introducing a patch-level neighboring relation for vision data and deriving tight privacy bounds for differentially private stochastic gradient descent (DP-SGD) when combined with random cropping. Our analysis quantifies the patch inclusion probability and shows how it composes with minibatch sampling to yield a lower effective sampling rate. Empirically, we validate that patch-level amplification improves the privacy-utility trade-off across multiple segmentation architectures and datasets. Our results demonstrate that aligning privacy accounting with domain structure and additional existing sources of randomness can yield stronger guarantees at no additional cost.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：通过随机裁剪免费增强 Patch 级别的差分隐私

#### 1. 核心问题与整体含义（研究动机和背景）
在差分隐私（DP）机器学习中，**DP-SGD** 是标准算法，其隐私保护能力主要源于梯度加噪和微批次采样（Minibatch Subsampling）带来的隐私放大效应。然而，传统的隐私核算（Accounting）通常将整个图像视为一个敏感记录，忽略了视觉数据的空间结构。
*   **研究动机**：在许多现实场景（如自动驾驶、医疗影像）中，敏感信息往往是局部的（如人脸、车牌），而非遍布全图。
*   **核心问题**：计算机视觉中通用的**随机裁剪（Random Cropping）**本质上是一种随机采样过程，它有概率完全排除图像中的敏感局部区域。论文探讨了如何利用这种现有的随机性，在不改变模型架构和训练流程的前提下，获得更强的隐私保证。

#### 2. 方法论：核心思想与技术细节
论文提出了一种**补丁级差分隐私（Patch-Level DP）**框架，核心步骤如下：
*   **补丁级相邻关系（Patch-Level Neighboring Relation）**：重新定义相邻数据集 $x \simeq_{\Delta,p} x'$，即两个数据集仅在其中一张图像的一个固定矩形区域 $R$（补丁）内存在差异，其余部分完全相同。
*   **随机裁剪的形式化**：将随机裁剪建模为从所有可能裁剪位置 $\Omega$ 中均匀采样。定义补丁包含概率 $\gamma'_{crop}(R)$，即随机裁剪窗口与敏感补丁 $R$ 相交的概率。
*   **最坏情况分析**：证明了当敏感补丁位于图像中心时，其被裁剪窗口包含的概率最大（即最坏情况 $\gamma_{crop}$）。
*   **隐私放大理论**：推导出**有效采样率 $\gamma_{eff} = \gamma_{wo} \cdot \gamma_{crop}$**，其中 $\gamma_{wo}$ 是微批次采样率。
*   **紧致核算（Tight Accounting）**：利用 **Dominating Pairs** 框架和隐私损失分布（PLD），证明了结合随机裁剪后的 DP-SGD 满足更紧致的 $(\epsilon, \delta)$-DP 界限。这种放大效应是“免费”的，因为它仅通过改进隐私核算实现，无需增加噪声或计算开销。

#### 3. 实验设计
*   **任务与模型**：
    *   **语义分割**：DeepLabV3+、PSPNet（核心评估任务）。
    *   **图像分类**：ResNet-18、VGG-11。
*   **数据集**：
    *   分割：**Cityscapes**（城市街景）、**A2D2**（奥迪自动驾驶数据集）。
    *   分类：**DTD**（纹理数据集）、**MNIST**（用于局限性分析）。
*   **Benchmark 与对比方法**：
    *   **Baseline**：标准 DP-SGD（仅考虑微批次采样放大）。
    *   **对比 1**：数据级加噪（直接在像素上加高斯噪声）。
    *   **对比 2**：高斯噪声数据增强（Gaussian Noise Augmentation）的隐私放大。
*   **变量控制**：分析了裁剪尺寸（Crop Size）、补丁尺寸（Patch Size）、填充（Padding）以及补丁几何形状（圆形、不规则形状）对隐私参数 $\epsilon$ 的影响。

#### 4. 资源与算力
*   论文提到使用了 `seml` (Slurm Experiment Management Library) 进行实验管理。
*   **具体算力信息**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **计算限制说明**：作者提到受限于计算预算（compute constraints），在 A2D2 数据集上仅训练了 25 个 epoch，且 DeepLabV3+ 在该数据集上出现了不收敛的情况。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 种架构 $\times$ 2 个数据集的分割实验，以及 2 种架构 $\times$ 2 个数据集的分类实验。
*   **重复性**：所有效用实验均使用了 **4 个随机种子**，并报告了均值和标准差（Error Bars）。
*   **充分性**：实验设计较为全面，不仅验证了效用提升，还通过大量的参数扫描（Sweep）验证了理论公式的正确性。
*   **客观性**：作者诚实地报告了失败案例（如 MNIST 上的不适用性、A2D2 上的收敛问题），并对比了多种潜在的替代方案，实验结果具有较高的客观信度。

#### 6. 主要结论与发现
*   **隐私增强显著**：在相同的隐私预算 $\epsilon$ 下，补丁级 DP 显著提升了模型的平均交并比（mIoU）。例如在 Cityscapes 上，PSPNet 的 mIoU 提升最高达 330%。
*   **参数敏感性**：$\epsilon$ 随裁剪尺寸增加而迅速增加，随补丁尺寸增加呈近线性增加。当补丁大到覆盖全图时，退化为标准 DP-SGD。
*   **优于加噪方案**：相比于直接在输入端加噪，利用随机裁剪的随机性在保护隐私的同时对模型效用的损害极小。
*   **任务普适性**：该效应在分割和分类任务中均存在，只要训练流程中包含随机裁剪。

#### 7. 优点
*   **零成本（Free）**：不需要修改模型、不需要改变训练算法、不增加计算时间，仅通过更科学的“记账”方式就提升了隐私保证。
*   **理论完备**：为视觉领域特有的数据增强操作提供了严谨的数学证明和紧致的隐私界限。
*   **符合直觉**：将隐私定义从“整图”细化到“局部补丁”，更贴合实际应用中对人脸、车牌等特定目标的保护需求。

#### 8. 不足与局限
*   **假设依赖**：该方法依赖于“敏感信息是局部化的”这一假设。如果敏感信息遍布全图（如风格迁移任务），该方法失效。
*   **补丁尺寸预验**：需要预先估计敏感区域的大小。如果估计过小，会导致隐私泄露风险；如果估计过大，则无法获得明显的放大效应。
*   **低分辨率限制**：在 MNIST 等低分辨率数据集上，随机裁剪会破坏语义信息导致模型失效，因此该方法主要适用于高分辨率图像任务。
*   **二元相交逻辑**：目前的分析认为只要裁剪窗口与补丁有任何重叠就视为“包含”，这在处理部分重叠时可能过于保守。

（完）
