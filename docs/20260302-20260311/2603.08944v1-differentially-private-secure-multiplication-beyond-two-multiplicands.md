---
title: "Differentially Private Secure Multiplication: Beyond Two Multiplicands"
title_zh: 差分隐私安全乘法：超越两个乘数
authors: "Haoyang Hu, Viveck R. Cadambe"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08944v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 分布式计算中的差分隐私安全乘法
tldr: 本文研究分布式计算系统中的差分隐私（DP）安全乘法问题，旨在解决多个私有输入乘积计算时隐私与准确性的权衡。研究将以往仅针对两个乘数的成果扩展至任意 M 个乘数，提出一种结合编码多项式与分层噪声注入的通用框架。该方法能系统性抵消低阶噪声，提升估计精度。针对不同的节点协作规模，本文分别给出了最优权衡特征及渐近紧致的性能界限，为多方安全计算提供了理论支撑。
selection_source: fresh_fetch
motivation: 现有的差分隐私安全乘法研究主要局限于两个乘数，难以直接应用于需要计算多个私有输入乘积的复杂分布式场景。
method: 提出一种基于编码多项式与分层噪声注入的安全乘法框架，通过设计特定的多项式结构实现低阶噪声项的系统性抵消。
result: 在不同节点配置下刻画了最优隐私-准确度权衡，并证明了在特定高隐私机制下所提方案的界限具有渐近紧致性。
conclusion: 该研究成功将差分隐私安全乘法理论推广至多乘数通用场景，为分布式隐私计算提供了更高效的算法框架和理论界限。
---

## 摘要
我们研究了分布式计算系统中的差分隐私（DP）安全乘法问题，重点关注无法同时实现完美隐私和完美准确性的情形。具体而言，N个节点协作计算M个隐私输入的乘积，同时保证针对多达T个节点的任何合谋具有epsilon-DP。先前的研究已经刻画了两个乘数相乘时基本的隐私-准确性权衡。在本文中，我们将这些结果扩展到计算任意数量M个乘数之积的更一般设置。我们提出了一种基于精心设计的编码多项式并结合分层噪声注入的安全乘法框架。所提出的构造泛化了现有方案，并能够系统地抵消低阶噪声项，从而提高了估计准确性。我们探讨了两种情形：(M-1)T+1 <= N <= MT 以及 N = T+1。对于 (M-1)T+1 <= N <= MT，我们刻画了最优的隐私-准确性权衡。当 N = T+1 时，我们推导出了在强隐私情形下渐近紧致的非平凡可实现性界限和逆界限。

## Abstract
We study the problem of differentially private (DP) secure multiplication in distributed computing systems, focusing on regimes where perfect privacy and perfect accuracy cannot be simultaneously achieved. Specifically, N nodes collaboratively compute the product of M private inputs while guaranteeing epsilon-DP against any collusion of up to T nodes. Prior work has characterized the fundamental privacy-accuracy trade-off for the multiplication of two multiplicands. In this paper, we extend these results to the more general setting of computing the product of an arbitrary number M of multiplicands. We propose a secure multiplication framework based on carefully designed encoding polynomials combined with layered noise injection. The proposed construction generalizes existing schemes and enables the systematic cancellation of lower-order noise terms, leading to improved estimation accuracy. We explore two regimes: (M-1)T+1 <= N <= MT and N = T+1. For (M-1)T+1 <= N <= MT, we characterize the optimal privacy--accuracy trade-off. When N = T+1, we derive nontrivial achievability and converse bounds that are asymptotically tight in the high-privacy regime.

---

## 论文详细总结（自动生成）

这篇论文《Differentially Private Secure Multiplication: Beyond Two Multiplicands》深入探讨了分布式计算中多方安全乘法的差分隐私（DP）保护问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在分布式系统中，在节点资源受限（节点数 $N$ 较少）的情况下，安全地计算 $M$ 个私有实数变量的乘积，并满足 $T$ 个节点合谋下的差分隐私要求。
*   **研究动机**：传统的安全多方计算（MPC）协议（如 BGW 协议）为了实现“完美隐私”和“完美准确性”，通常需要大量的通信轮数或极高的硬件冗余（单轮协议需 $N \ge MT+1$ 个节点）。在现代机器学习等高维非线性计算场景中，这种开销成为了瓶颈。
*   **整体含义**：本文通过引入差分隐私，放宽了对完美隐私的要求，允许受控的隐私泄露，从而在更少的节点上实现单轮安全乘法，并系统性地刻画了隐私泄露量（$\epsilon$）与计算准确度（均方误差 LMSE）之间的基本权衡关系。

### 2. 论文提出的方法论
论文提出了一种基于**编码多项式与分层噪声注入**的通用框架：
*   **核心思想**：将每个输入变量 $A_i$ 嵌入到一个精心构造的多项式 $p_i(x)$ 中。该多项式不仅包含原始数据，还包含用于满足 DP 要求的噪声项和用于模拟秘密共享的随机项。
*   **关键技术细节**：
    *   **分层噪声**：噪声由两部分组成：第一层是基于阶梯机制（Staircase Mechanism）的最优 DP 噪声 $R_i$，用于提供基础隐私保护；第二层是缩放后的随机噪声 $S_{i,t}$，用于在多节点间建立关联。
    *   **多项式构造**：构造 $p_i(x) = (A_i + R_i) + \zeta_2 \sum S_{i,t}x^t + \zeta_1 R_i x^T$。通过调整缩放因子 $\zeta_1, \zeta_2$ 的渐近行为，使得在解码时能够分离出目标乘积项。
    *   **线性解码**：解码器收集各节点的计算结果（多项式在特定点的取值乘积），利用范德蒙德矩阵的性质进行线性组合，系统性地抵消低阶噪声项，最终提取出接近真实乘积的估计值。

### 3. 实验设计
由于本文属于信息论与编码领域的理论研究，其实验主要体现为**数值仿真与数学证明**：
*   **场景设定**：设定 $N=5$ 个节点，$M=3$ 个乘数，$T=2$ 个合谋节点。
*   **Benchmark（基准）**：
    1.  **复数域 Shamir 秘密共享**：现有的基于复数嵌入的 DP 方案。
    2.  **独立加性噪声**：在每个节点简单添加独立噪声的朴素方案。
*   **对比指标**：隐私参数 $\epsilon$ 与线性均方误差（LMSE）的关系曲线。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**使用了特定的 GPU 或大规模算力集群。
*   **性质**：该研究侧重于数学推导和算法复杂度的理论分析。数值仿真部分通常在标准的个人电脑（如使用 MATLAB 或 Python）上即可完成，不涉及深度学习的大规模训练。

### 5. 实验数量与充分性
*   **实验规模**：论文主要通过数学定理（Theorem 1-4）证明了在不同节点区间（如 $(M-1)T+1 \le N \le MT$）下的性能界限。
*   **充分性与客观性**：
    *   **理论充分性**：通过逆定理（Converse）证明了所提方案在特定条件下的最优性，这在信息论研究中具有极高的说服力。
    *   **数值验证**：虽然数值仿真组数不多，但其目的是为了直观展示理论界限的优越性，对比基准选择合理，能够客观反映所提方案在降低误差方面的显著效果。

### 6. 论文的主要结论与发现
*   **最优权衡刻画**：在 $(M-1)T+1 \le N \le MT$ 范围内，证明了 LMSE 的下界为 $\eta^M / (1 + SNR^*(\epsilon))^M$，且该界限是渐近可实现的。
*   **误差缩放规律**：发现估计误差随乘数数量 $M$ 呈指数级增长，但通过协同噪声设计，可以将误差控制在单变量估计误差的 $M$ 次幂水平。
*   **最小冗余场景**：在 $N=T+1$ 的极端资源受限场景下，推导出了非平凡的可实现界限和逆界限，并在高隐私（$\epsilon \to 0$）极限下证明了这些界限是紧致的。

### 7. 优点
*   **通用性强**：将安全乘法从 $M=2$ 扩展到任意 $M$，填补了该领域的理论空白。
*   **几何直观**：为复杂的代数构造提供了几何解释（如矩形面积分割），增强了方法论的可理解性。
*   **资源高效**：相比传统 MPC，显著降低了实现安全乘法所需的节点数量。

### 8. 不足与局限
*   **渐近性限制**：最优性能是在参数 $\zeta \to 0$ 的极限条件下取得的，在实际有限数值计算中，过小的 $\zeta$ 可能导致数值稳定性问题（如浮点数溢出或精度丢失）。
*   **计算假设**：假设输入变量是独立同分布的，对于具有强相关性的输入数据，其隐私-准确度权衡可能需要重新评估。
*   **界限间隙**：在 $N=T+1$ 且 $N < M$ 的特定区间内，可实现界限与逆界限之间仍存在一定间隙，尚未完全闭合。

（完）
