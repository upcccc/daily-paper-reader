---
title: Structure Selection for Fairness-Constrained Differentially Private Data Synthesis
title_zh: 公平性约束下的差分隐私数据合成结构选择
authors: "Naeim Ghahramanpour, Mostafa Milani"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12112v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 差分隐私数据合成机制
tldr: 针对差分隐私（DP）合成数据中存在的偏见问题，本文提出了PrivCI框架。该框架通过在数据测量阶段引入条件独立性（CI）约束，利用改进的贪婪最小生成树（MST）算法和指数机制，在保证隐私的同时消除敏感属性与结果间的关联。实验证明，PrivCI在满足公平性约束的前提下，比现有方法具有更高的保真度和预测准确性，为公平性敏感的数据发布提供了有效方案。
selection_source: fresh_fetch
motivation: 传统的差分隐私数据合成方法会保留数据中的虚假关联，导致在公平性敏感场景下产生偏见。
method: 提出PrivCI算法，在最小生成树构建过程中集成条件独立性检查，通过指数机制选择满足约束的边。
result: 在标准公平性基准测试中，PrivCI在满足CI约束的同时，实现了比基线方法更强的保真度和预测精度。
conclusion: 通过在结构选择阶段显式引入公平性约束，可以有效提升差分隐私合成数据的公平性与实用性。
---

## 摘要
差分隐私 (DP) 实现了安全的数据发布，合成数据生成已成为近年来的常用方法。然而，标准的合成器保留了数据中的所有依赖关系，包括敏感属性与结果之间的伪相关。在对公平性要求较高的场景中，这会重现不必要的偏见。一种有原则的补救措施是强制执行条件独立性 (CI) 约束，这些约束编码了领域知识或法律要求，即在考虑了允许因素后，结果应独立于敏感属性。DP 合成通常分为两个阶段：(i) 测量步骤，对选定的边际分布进行隐私化处理，通常通过最小生成树 (MST) 进行结构化；(ii) 重建步骤，拟合一个与含噪边际分布一致的概率模型。我们提出了 PrivCI，它在测量步骤中通过一种感知 CI 的贪婪 MST 算法来强制执行 CI，该算法在指数机制下将可行性检查集成到 Kruskal 构造中，从而提高了相比竞争方法的准确性。在标准公平性基准上的实验表明，PrivCI 在满足指定 CI 约束的同时，实现了比先前基准更高的保真度和预测准确性。

## Abstract
Differential privacy (DP) enables safe data release, with synthetic data generation emerging as a common approach in recent years. Yet standard synthesizers preserve all dependencies in the data, including spurious correlations between sensitive attributes and outcomes. In fairness-critical settings, this reproduces unwanted bias. A principled remedy is to enforce conditional independence (CI) constraints, which encode domain knowledge or legal requirements that outcomes be independent of sensitive attributes once admissible factors are accounted for. DP synthesis typically proceeds in two phases: (i) a measure- ment step that privatizes selected marginals, often structured via minimum spanning trees (MSTs), and (ii) a reconstruction step that fits a probabilistic model consistent with the noisy marginals. We propose PrivCI, which enforces CI during the measurement step via a CI-aware greedy MST algorithm that integrates feasibility checks into Kruskal's construction under the exponential mechanism, improving accuracy over competing methods. Experiments on standard fairness benchmarks show that PrivCI achieves stronger fidelity and predictive accuracy than prior baselines while satisfying the specified CI constraints.

---

## 论文详细总结（自动生成）

这篇论文针对差分隐私（DP）合成数据中的公平性问题提出了创新性的解决方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：差分隐私合成数据旨在保留原始数据的统计结构，但这也意味着它会继承原始数据中的**偏见和虚假关联**（如性别与收入之间的不当关联）。
*   **研究动机**：标准的 DP 合成器（如 MST）仅优化数据依赖性的强度，而不考虑公平性约束。现有的公平性修复方法通常是事后处理，这会浪费隐私预算并降低数据效用。
*   **整体含义**：论文提出在 DP 合成的“测量阶段”直接嵌入**条件独立性（CI）约束**，确保合成数据在满足隐私保护的同时，从结构上消除不公平的依赖关系。

### 2. 方法论：核心思想与算法流程
*   **核心思想**：将公平性要求转化为图论中的**分离属性**。定义属性集为：受保护属性 $S$、结果属性 $O$、允许因素 $A$ 和其他属性 $I$。公平性约束 $O \perp S | A$ 要求在给定 $A$ 的情况下，$O$ 与 $S$ 独立。
*   **关键技术细节**：
    *   **PrivCI 框架**：基于“测量-重建”范式，改进了经典的 MST（最小生成树）算法。
    *   **感知 CI 的贪婪构造**：在利用 Kruskal 算法构建依赖树时，引入**可行性检查**。只有当加入某条边不会在 $V \setminus A$ 的诱导图中创建 $S$ 到 $O$ 的路径时，该边才被视为候选边。
    *   **指数机制（Exponential Mechanism）**：在满足上述可行性约束的候选边中，利用指数机制根据依赖得分（如互信息代理值）进行隐私化选择。
    *   **模型重建**：使用 PrivatePGM 根据选定的含噪边际分布拟合联合分布，并采样生成合成记录。

### 3. 实验设计
*   **数据集**：使用了 5 个标准公平性基准数据集：Adult（收入预测）、COMPAS（累犯预测）、Dutch Census（职业预测）、German Credit（信用风险）、Law School（法学院入学）。
*   **对比基准（Benchmark）**：
    *   **MST**：标准的、不考虑公平性的 DP 合成基准。
    *   **PreFair**：现有的唯一支持在 DP 合成中强制执行 CI 约束的方法（主要通过预先删除边或启发式搜索实现）。
*   **评估指标**：统计保真度（KL 散度、TV 距离）、下游预测性能（AUC）、公平性指标（条件互信息 CMI、均等化赔率 EO）。

### 4. 资源与算力
*   **说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。由于该方法处理的是经过离散化处理的表格数据，且基于图模型而非深度学习，其计算开销主要集中在边际分布测量和 PrivatePGM 的优化上，通常对算力要求适中。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个数据集上进行了测试，涵盖了从 0.1 到 10 的多个隐私预算 $\epsilon$ 级别。
*   **充分性与客观性**：
    *   采用了 **5 折交叉验证**来评估下游 AUC。
    *   使用了 **Wilcoxon 符号秩检验**进行统计显著性分析，证明了 PrivCI 在大多数指标上显著优于 PreFair。
    *   实验设计考虑了隐私、效用和公平性之间的三方权衡，对比实验较为全面且客观。

### 6. 主要结论与发现
*   **效用提升**：在满足相同的 CI 约束下，PrivCI 生成的数据在统计保真度（KL/TV）和下游预测准确性（AUC）上普遍优于 PreFair。
*   **公平性有效性**：PrivCI 能够显著降低 CMI，证明其在分布层面上成功强制执行了条件独立性约束。
*   **结构选择的重要性**：实验表明，在测量阶段进行动态的、受约束的结构选择，比预先排除特定边（如 PreFair 的做法）能保留更多有用的信息。
*   **局限性发现**：虽然消除了分布层面的依赖，但并不保证所有下游分类器的公平性指标（如 EO）都能得到同等程度的改善，这取决于具体的数据集特性。

### 7. 优点
*   **原则性强**：将公平性约束直接集成到 DP 测量流程中，避免了隐私预算的浪费。
*   **灵活性高**：通过修改 MST 的可行性检查，可以适应不同的 CI 约束定义。
*   **性能优越**：在保持相同隐私和公平性水平的前提下，提供了比现有技术更高的实用价值。

### 8. 不足与局限
*   **结构限制**：目前仅限于**树状结构**（Tree-structured models），对于具有复杂高阶依赖的数据集，表达能力可能受限。
*   **数据类型**：主要针对离散化后的表格数据，对于连续属性或混合类型数据的处理依赖于预先的离散化方案。
*   **约束复杂性**：论文主要讨论了单一的 CI 约束，对于多个重叠或冲突的 CI 约束的处理尚待进一步研究。

（完）
