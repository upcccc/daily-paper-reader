---
title: "FedLLM: A Privacy-Preserving Federated Large Language Model for Explainable Traffic Flow Prediction"
title_zh: FedLLM：一种用于可解释交通流预测的隐私保护联邦大语言模型
authors: "Seerat Kaur, Sukhjit Singh Sehra, Dariush Ebrahimi"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16612v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 隐私保护联邦大语言模型
tldr: 本研究针对智能交通系统中交通预测面临的数据隐私、中心化存储及缺乏可解释性等挑战，提出了FedLLM框架。该框架结合了联邦学习与大语言模型，通过轻量化LoRA适配器在异构客户端间协作训练，并引入复合选择评分（CSS）和结构化提示词。FedLLM不仅实现了15-60分钟的精准交通流预测，还提供了可解释的推理输出，在保护隐私的同时解决了非独立同分布数据的学习难题。
selection_source: fresh_fetch
motivation: 现有的交通预测模型多依赖中心化数据且缺乏可解释性，难以应对现实中分布式且异构的交通系统及隐私保护需求。
method: 提出FedLLM框架，利用包含时空信息的结构化提示词微调领域适配的LLM，并通过联邦学习交换轻量化LoRA参数实现跨区域协作。
result: 实验结果显示，FedLLM在预测性能上优于中心化基准模型，并能生成结构化且具有解释性的预测结果。
conclusion: 结合联邦学习与领域适配的大语言模型为构建可扩展、隐私保护且可解释的智能交通预测系统提供了有效路径。
---

## 摘要
交通预测在智能交通系统（ITS）中发挥着核心作用，支持实时决策、拥堵管理和长期规划。然而，许多现有方法面临实际局限性。大多数时空模型基于集中式数据进行训练，依赖数值表示，且可解释性有限。近期的大语言模型（LLM）方法提升了推理能力，但通常假设数据是集中可用的，且未能充分捕捉现实世界交通系统分布式和异构的特性。为应对这些挑战，本研究提出了 FedLLM（联邦 LLM），这是一个用于可解释多步短期交通流预测（15-60 分钟）的隐私保护分布式框架。该框架引入了四项核心贡献：1) 一种用于数据驱动型高速公路选择的综合选择评分（CSS），用于捕捉不同交通区域的结构多样性；2) 一种领域自适应的 LLM，在编码了空间、时间和统计上下文的结构化交通提示词上进行微调；3) FedLLM 框架，支持在异构客户端之间进行协作训练，且仅交换轻量级的 LoRA 适配器参数；4) 一种支持上下文推理和跨区域泛化的结构化提示词表示。FedLLM 的设计允许每个客户端学习本地交通模式，同时通过高效的参数交换为共享的全局模型做出贡献，从而降低了通信开销并保持了数据隐私。该设置支持在非独立同分布（non-IID）交通分布下的学习。实验结果表明，FedLLM 的预测性能优于集中式基准模型，同时能生成结构化且可解释的输出。这些发现强调了将联邦学习（FL）与领域自适应 LLM 相结合，在实现可扩展、隐私感知且可解释的交通预测方面的潜力。

## Abstract
Traffic prediction plays a central role in intelligent transportation systems (ITS) by supporting real-time decision-making, congestion management, and long-term planning. However, many existing approaches face practical limitations. Most spatio-temporal models are trained on centralized data, rely on numerical representations, and offer limited explainability. Recent Large Language Model (LLM) methods improve reasoning capabilities but typically assume centralized data availability and do not fully capture the distributed and heterogeneous nature of real-world traffic systems. To address these challenges, this study proposes FedLLM (Federated LLM), a privacy-preserving and distributed framework for explainable multi-horizon short-term traffic flow prediction (15-60 minutes). The framework introduces four key contributions: 1) a Composite Selection Score (CSS) for data-driven freeway selection that captures structural diversity across traffic regions 2) a domain-adapted LLM fine-tuned on structured traffic prompts encoding spatial, temporal, and statistical context 3) FedLLM framework, that enables collaborative training across heterogeneous clients while exchanging only lightweight LoRA adapter parameters, 4) a structured prompt representation that supports contextual reasoning and cross-region generalization. The FedLLM design allows each client to learn from local traffic patterns while contributing to a shared global model through efficient parameter exchange, reducing communication overhead and keeping data private. This setup supports learning under non-IID traffic distributions. Experimental results show that FedLLM achieves improved predictive performance over centralized baselines, while producing structured and explainable outputs. These findings highlight the potential of combining FL with domain-adapted LLMs for scalable, privacy-aware, and explainable traffic prediction.

---

## 论文详细总结（自动生成）

### 论文总结：FedLLM —— 隐私保护的联邦大语言模型用于可解释交通流预测

#### 1. 核心问题与研究动机
*   **核心问题**：传统的交通预测模型（如基于图神经网络的时空模型）通常依赖**中心化数据存储**，这在多机构协作中存在严重的隐私泄露风险。此外，现有模型多为“黑盒”数值预测，缺乏**可解释性**，且难以处理现实中分布式的、非独立同分布（Non-IID）的异构交通数据。
*   **研究背景**：智能交通系统（ITS）需要实时、准确且可解释的预测来支持决策。大语言模型（LLM）虽具备推理能力，但其中心化训练和高算力需求限制了在隐私敏感的交通领域的应用。

#### 2. 方法论
论文提出了 **FedLLM** 框架，其核心思想是将联邦学习（FL）的隐私保护特性与 LLM 的推理能力相结合。
*   **复合选择评分（CSS）**：提出一种多准则评分指标，综合考虑交通流量（Mean Flow）、时间变异性（Std）、传感器可靠性（Zero-rate）和空间覆盖范围（Sensor Count），通过 K-means 聚类选择具有结构代表性的高速公路路段作为联邦客户端。
*   **结构化提示词表示（Structured Prompting）**：将数值化的交通数据转化为自然语言提示词。包含：
    *   **静态上下文**：传感器 ID、坐标、车道数、图连通性指标。
    *   **统计上下文**：长期平均流量、拥堵率、24小时及7天需求概况。
    *   **动态时空上下文**：最近12个时段的流量序列、短期趋势斜率、邻近传感器流量。
*   **领域适配与 QLoRA 微调**：以 **Qwen2.5-1.5B-Instruct** 为基座模型，利用 QLoRA（量化低秩适配器）进行参数高效微调。
*   **联邦学习架构**：采用 **FedAvg** 聚合算法。各客户端（不同高速公路管理单位）在本地训练 LoRA 适配器，仅向中央服务器上传轻量级的适配器参数（约 70.5 MB），而非完整的模型权重（约 2.9 GB），从而保护原始数据隐私并降低通信开销。

#### 3. 实验设计
*   **数据集**：使用 **LargeST** 基准数据集（源自加州 PeMS 系统），涵盖 2017-2021 年的数据。
    *   **训练/测试区**：大洛杉矶地区（District 12）的 953 个传感器。
    *   **零样本泛化区**：旧金山湾区（District 4）的 107 个传感器。
*   **预测任务**：多步短期预测（未来 15、30、45、60 分钟的流量）。
*   **对比方法（Baselines）**：
    *   **中心化深度学习**：GRU, STGCN, DCRNN, AGCRN, FC-LSTM, ASTGNN。
    *   **联邦学习基准**：Fed-GDAN, FedASTGCN。
    *   **LLM 基准**：未经领域适配的中心化 Qwen。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A100-SXM4-80GB** GPU（通过 Google Colab 环境）。
*   **训练细节**：
    *   使用 **Unsloth** 框架进行内核级优化。
    *   4-bit 量化加载模型，LoRA 秩（Rank）设为 16。
    *   联邦学习进行 2 轮通信，每轮本地训练 200 步。
    *   单次推理采用贪婪解码（Greedy Decoding）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度测试，包括不同预测步长（15-60min）、不同样本规模（100 到 5000 个样本）的稳定性测试。
*   **消融与泛化实验**：
    *   **消融实验**：验证了训练数据规模对 FedLLM 性能的影响。
    *   **零样本（Zero-shot）实验**：在完全未见的 District 4 区域进行跨区域泛化测试。
*   **评价指标**：RMSE, MAE, MAPE, $R^2$。
*   **充分性评价**：实验设计较为全面，涵盖了从中心化到分布式的多种对比路径，且通过跨区域测试验证了模型的鲁棒性，实验结果具有客观性。

#### 6. 主要结论与发现
*   **性能卓越**：FedLLM 在所有预测步长上均优于中心化和联邦学习基准模型。其整体 $R^2$ 达到 **0.985**，RMSE 显著低于传统图神经网络模型。
*   **隐私与效率并存**：通过仅交换 LoRA 参数，通信量减少了约 97.5%，同时实现了 Non-IID 数据下的高效协作。
*   **强大的泛化能力**：在零样本跨区域测试中，$R^2$ 仍保持在 0.92 左右，证明结构化提示词能有效捕捉通用的交通规律。
*   **可解释性输出**：模型能生成包含“区间解释”、“简明分析”和“步骤推理”的 JSON 格式输出，为交通管理人员提供决策依据。

#### 7. 优点与亮点
*   **首创性**：据作者称，这是首个将联邦 LLM 应用于高速公路交通流预测的研究。
*   **CSS 评分机制**：引入了数据驱动的 CSS 评分，解决了以往研究中路段选择随意的问题，确保了训练样本的结构多样性。
*   **端到端可解释性**：不仅给出数值，还通过自然语言解释预测背后的逻辑（如：因早高峰趋势和邻近传感器压力导致流量上升）。
*   **轻量化部署**：证明了 1.5B 小参数模型经过适配后，在特定领域能超越大型通用模型或复杂的专用架构。

#### 8. 不足与局限
*   **聚合策略简单**：目前仅使用标准的 FedAvg 算法，未充分利用更先进的交通感知聚合策略（如根据路段相似度加权）。
*   **场景覆盖有限**：实验主要集中在高速公路（Freeway），尚未验证在复杂的城市干道、信号灯路口等更具随机性的场景下的表现。
*   **模型规模限制**：受限于算力，仅测试了 1.5B 参数模型，更大规模模型（如 Llama-3 8B 或 DeepSeek）的潜力尚待挖掘。
*   **长时预测挑战**：虽然 60 分钟预测表现良好，但更长周期（如数小时或数天）的预测稳定性仍需验证。

（完）
