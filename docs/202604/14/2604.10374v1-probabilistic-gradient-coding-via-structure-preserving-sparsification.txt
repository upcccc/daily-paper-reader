Title: Probabilistic Gradient Coding via Structure-Preserving Sparsification

URL Source: https://arxiv.org/pdf/2604.10374v1

Published Time: Tue, 14 Apr 2026 01:17:50 GMT

Number of Pages: 15

Markdown Content:
> 1

# Probabilistic Gradient Coding via Structure-Preserving Sparsification 

Yuxin Jiang, Wenqin Zhang and Lele Wang. 

Abstract —Gradient coding is a distributed computing tech-nique aiming to provide robustness against slow or non-responsive computing nodes, known as stragglers, while bal-ancing the computational load for responsive computing nodes. Among existing gradient codes, a construction based on combina-torial designs, called BIBD gradient code, achieves the best trade-off between robustness and computational load in the worst-case adversarial straggler setting. However, the range of system parameters for which BIBD gradient codes exist is limited. In this paper, we overcome these limitations by proposing two new probabilistic gradient codes, termed the Sparse Gaussian (SG) gradient code and the Expansion-Preserving (EP) gradient code. Through probabilistic constructions, the former preserves the combinatorial structure of BIBDs, while the latter preserves key spectral properties. Both codes are based on a common two-step framework: first generating a random matrix and then applying distinct sparsification procedures. The SG gradient code constructs its encoding matrix from a correlated multivariate Gaussian distribution masked by Bernoulli random variables, while the EP gradient code derives its encoding matrix from sparsified expander-like graph structures that preserve key spec-tral properties. Experimentally, both codes achieve worst-case error performance comparable to that of the BIBD gradient code (when such a code with the same parameters exists). Moreover, they substantially extend the feasible range of system parameters beyond BIBD and soft BIBD gradient codes, offering practical and theoretically grounded solutions for large-scale distributed computing tasks. 

Index Terms —Gradient codes, distributed computing, coding theory, graph theory, machine learning. 

I. I NTRODUCTION 

Due to the ever-increasing computing demands in various applications such as machine learning and cloud computing, it becomes essential for large systems to perform their computa-tions in a distributed manner. In theory, distributed algorithms can substantially speed up computations compared to central-ized algorithms. However, these speed-up gains may not be realized in practice due to the presence of underperforming or non-responsive computing nodes, termed as stragglers [1]. Thus, how to distribute computations that are robust against stragglers is a major challenge in the design of distributed computation systems. In pursuit of this objective, numerous strategies have been developed to mitigate the impact of 

Yuxin Jiang and Lele Wang are with Department of Electrical and Com-puter Engineering, University of British Columbia, Vancouver, BC, Canada, (email: {yjiang28, lelewang }@ece.ubc.ca). Wenqin Zhang is with School of Cyber Science and Technology, Hubei Univeristy, Wuhan, Hubei, China, (email: wenqin zhang@hubu.edu.cn). A preliminary version of this work was submitted to the 2024 IEEE International Symposium on Information Theory (ISIT 2024), Athens, Greece. 

stragglers. These include duplicating tasks across computing nodes [2] and, where feasible, excluding stragglers from the computation process [3]. The primary challenge in these approaches lies in how to balance communication efficiency against straggler tolerance [4]. To address this challenge, methodologies grounded in cod-ing theory have been explored. Gradient Coding (GC) has emerged as an important tool. It involves dividing each training data into partitions and distributing them to certain computing nodes known as workers. These workers compute and send a linear combination of gradients to the master node, aggregating these to recover the gradient sum accurately. The utmost goal of GC is to recover the gradient sum exactly. Several gradient codes have been proposed under the exact recovery crite-rion, such as Fractional Repetition gradient codes. However, a significant drawback of exact recovery is its potentially high computing workload as it increases with the number of stragglers. Though exact recovery is desired, approximate recovery may be sufficient in many practical applications, such as stochastic gradient descent algorithms [5]. Instead of exactly recovering the gradient sum, approximate recovery finds an approximation of the gradient sum [6]. The ℓ2-norm of the difference between the exact gradient sum and the approximate gradient sum (i.e. squared error) is a common performance metric. There are two common assumptions for the stragglers: (1) the stochastic stragglers, where computing nodes are strag-gled independently with some probability, and (2) adversarial stragglers, where stragglers can deliberately disrupt any set of S computing nodes. Throughout this paper, we assume the adversarial straggler setting, which is more realistic for practical systems where obtaining a statistical estimation of the stragglers in real time is difficult. Under this assumption, we maximize the squared error over all possible straggling patterns and refer to it as the worst-case squared error .In the literature, many approximate gradient codes with small worst-case squared errors are introduced, for exam-ple, [2], [6]–[9]. Among these, one research direction in approximate gradient codes involves the utilization of the biadjacency matrix from expander graphs to develop gradient codes [7]. Expander graphs are favourable for gradient coding due to their strong connectivity, which enhances the robustness against stragglers. It is shown that the upper bound of the squared error is proportional to the second largest eigenvalue of the normalized adjacency matrix for the expander graph [7]. This makes Ramanujan graphs a good candidate as they provide small second largest eigenvalues. As a special type of Ramanujan graphs, Balanced Incomplete Block Designs 

> arXiv:2604.10374v1 [cs.IT] 11 Apr 2026 2

(BIBDs) achieves the best error performance with a closed-form expression for the worst-case error. Moreover, the error depends on the straggling pattern only through the number of stragglers, not the specifics of the straggling pattern [8]. Unfortunately, most existing constructions for expander graphs require extensive computer search [10] and the set of system parameters for which expander graphs exist is limited [11]. In particular, the allowable parameters of BIBDs are notably limited [12]. This motivates the following question: by allowing a real-valued encoding matrix instead of a binary one, can one construct, in polynomial time, a gradient code that broadens the feasible range of system parameters while retaining error performance comparable to that of BIBD gradient codes? We answer this question affirmatively by proposing two proba-bilistic gradient codes: the Sparse Gaussian gradient code 

(SG-GC) and the Expansion-Preserving gradient code (EP-GC). Both constructions follow a common two-step princi-ple: a random matrix is first generated and then sparsified via a code-specific procedure, with each step admitting a polynomial-time algorithm. Moreover, both codes support a wide range of system parameters, as illustrated in Fig. 1 and Fig. 2. Along the construction process, SG-GC inherits key combinatorial properties of BIBD constructions and achieves error performance comparable to BIBD gradient codes with high probability, as established in Theorem 2. For EP-GC, the error can be bounded explicitly, as shown in Theorem 4. The performance of both codes is further validated experimentally, as shown by Fig. 3. II. P ROBLEM FORMULATION 

Consider a computing machine that includes a master node, 

N workers, and a training set D = {(xi, y i)}Mi=1 , where 

N, M ∈ N+, xi ∈ Rd, and yi ∈ R. The master node intends to train a model using gradient descent by distributing the gradient updates amongst the workers. Specifically, we wish to find the w ∈ Rd that minimizes the empirical risk, i.e., min w∈Rd 1

> M

PMi=1 ℓ(xi, y i; w), where loss function 

ℓ(xi, y i; w) measures the distortion of prediction made by w

on (xi, y i).The optimization problem can be solved using the gradient descent algorithm. Specifically, the gradient of the loss at the current model w(t) is computed as PMi=1 ∇ℓ(xi, y i; w(t)), and the model is updated iteratively based on this gradient. The gradient sum computation will be a bottleneck when dealing with large training data size M . To address this challenge, we distribute gradient computations across multiple workers, where each worker computes some subsets of the M gradients whose sum is returned to the master to update the model. In a distributed gradient computation setting, the training set D can be partitioned to K disjoint partial data set Di for 

i ∈ [K], where |D i| = M/K . Define a partial gradient vector corresponding to Di as di ≜ P 

> (xi,y i)∈D i

∇ℓ(xi, y i; w).In the context of S stragglers, a gradient coding scheme contains two steps: distributing data to workers based on an encoding matrix and approximating the gradient sum from the received N − S partial gradients. A gradient code can be characterized by an encoding matrix 

E ∈ RK×N , where row i ∈ [K] corresponds to data partition 

Di and column j ∈ [N ] corresponds to worker j. Worker j

will return PKi=1 Eij di where Eij is the linear combination coefficient of the partial gradient corresponding to (xi, y i).For a gradient code, each data piece is distributed to an equal number of R workers and each worker computes an equal number of L partial gradients . The goal of the master node is to recover the gradient sum exactly 

> K

X

> i=1

di = D1K ,

where D = [d1, d2, . . . , dK ]. Though an exact recovery of the full gradient sum is ideal, it is often unrealistic in practice. Fortunately, many practical applications only require an approximate recovery. This shifts the goal toward producing an estimate of the gradient sum that minimizes the least-squares error relative to the exact value. Let F ⊂ [N ] denote the set of non-straggling workers, with |F| = N − S. Rather than resizing the encoding matrix E ∈ RK×N for each F, we keep the full matrix E

and encode the non-straggler pattern in the decoding vector. Hence, the master node receives the partial gradient sums 

DEv F , where each nonzero coordinate corresponds to a non-straggling worker. To approximate the gradient sum, the master node finds a decoding vector vF ∈ RN supported on F that minimizes the squared ℓ2-norm of the reconstruction error: 

vopt (F) ≜ arg min   

> vF∈RN
> supp( vF)⊆F

∥Ev F − 1K ∥22.

The difference between the target gradient sum and the ap-proximate reconstruction can be expressed as 

DEv F − D1K = D(Ev F − 1K ).

Since D is unknown, we evaluate the worst-case decoding error over all possible non-straggler sets. 

Definition 1. For an encoding matrix E ∈ RK×N , the nor-malized worst-case squared error (the error ) with S stragglers is defined as 

err( E, S ) = 1

K max   

> F⊂ [N]
> |F| =N−S

∥Ev opt (F) − 1K ∥22. (1) When the encoding matrix E is random, the above error is a random variable. We will study the high probability upper bound on the random error. Throughout the paper, Jn×m denotes the n × m all-one matrix, 0n×m the n × m all-zero matrix, In denotes an 

n × n identity matrix and 1n the all-one column vector in Rn. The ℓp-norm is written as ∥·∥ p. For x > 0, ln( x)

denotes the natural logarithm and log( x) the base-2 logarithm. For a matrix A ∈ Rn×m, its singular values are denoted as σ1(A) ≥ · · · ≥ σmax {n,m }(A) and its matrix norm is defined as ∥A∥2 = σ1(A). If A ∈ Rn×n is non-singular, its eigenvalues are denoted as λ1(A) ≥ · · · ≥ λn(A) and thus its matrix norm is defined as ∥A∥2 = λ1(A).3

III. E XISTING GRADIENT CODES 

This section summarizes the constructions of four existing gradient codes. 

A. Fractional Repetition Code 

The Fractional Repetition Code (FRC) is the first gra-dient code proposed in [13]. The encoding matrix of an 

(N, K, L, R )-FRC gradient code is given by 

EFRC =



JL×R 0L×R · · · 0L×R

0L×R JL×R · · · 0L×R

... ... . . . ...

0L×R 0L×R · · · JL×R

 ,

The empirical analysis demonstrates that the average error of the FRC is commendable. Nevertheless, it is vulnerable to adversarial stragglers, which gives the exact error: 

err( EFRC , S ) = LK

 SR



.

B. Bernoulli Gradient Code and its regularized variant 

To address the drawback inherent in the FRC, a probabilistic gradient code called the Bernoulli Gradient Code (BGC) is proposed in [14]. Each entry in the encoding matrix EBGC of an (N, K, L )-BGC is generated i.i.d. according to a Bernoulli distribution EBGC  

> ij

∼ Bern ( LK ).To restrict the computation load per worker to a maximum of 2L, regularized Bernoulli Gradient Code (rBGC) is pro-posed as a variant of BGC. An (N, K, L )-rBGC begins with the same initial structure as an (N, K, L )-BGC and then sets entries to zero in columns that exceed 2L non-zero entries until only L non-zero entries remain. 

C. Bipartite-expander-based Gradient Code [7] 

The Bipartite-expander-graph-based Gradient Code (BEG-GC) leverages the adjacency matrix or biadjacency matrix of biregular bipartite expander graphs. In this paper, we will primarily focus on the one adopting the biadjacency matrix. An (N, d )-BEG-GC corresponds to a d-regular bipartite expander graph G = ( W ∪ D , E), where |W| = |D| = N .Let EBEG be the biadjacency matrix of G. The normalized biadjacency matrix ¯EBEG ≜ 1 

> d

EBEG is the encoding matrix for the BEG-GC. Fix a non-straggler set F ⊂ [N ] and 

|F| = N − S. Its decoding vector takes the form 

vBEG  

> F

= 1N + aF , where aF =

(

−1, i̸ ∈ F   

> SN−S

i ∈ F . (2) Its error is upper bounded as 

err( ¯EBEG , S ) ≤ 1

> N

 σ2( ¯EBEG )

> d

2 N S N −S .

D. BIBD Gradient Code 

A (v, b, k, r, λ )-BIBD is characterized by a set V of v

points and a collection B of b blocks, each containing exactly 

k points, such that each point appears in exactly r blocks and every pair of distinct points is included in exactly λ

blocks [15]. The incidence matrix M for this design is a v × b

binary matrix, where M ij = 1 if point i is in block j, and 

M ij = 0 otherwise. A BIBD is symmetric when M = M ⊤.Given a BIBD with incidence matrix M , its dual-design is defined by a design with incidence matrix M ⊤. Throughout the paper, we only consider symmetric BIBD design or dual BIBD design. Given the incidence matrix M of a BIBD design, a gradient code (N = b, K = v, L = k, R = r)-GC is constructed by setting the encoding matrix EB = M . A sufficient condition for the existence of such code are: for N, K, L, R, λ ∈ N+,

N L = KR and R(L − 1) = λ(K − 1) . This code is known to be robust against adversarial stragglers. The corresponding worst-case error when there are S stragglers is given by 

err( EB, S ) = 1 − 1

KL2(N − S)

L + λ(N − S − 1) . (3) Moreover, it is known that for any straggling pattern F with 

|F| = N − S, the corresponding optimal decoding vector 

vopt (EB 

> F

) is the same constant vector and the corresponding squared error is identical. 

E. Soft BIBD Gradient Code 

A Soft BIBD gradient code arises from the aim to extend the range of system parameters inherent in BIBD gradient codes, while preserving its superior error performance [9]. An (N, K, L, R )-Soft BIBD employs a probabilistic approach to generate its encoding matrix, denoted by Esoft , which approximates the encoding matrix of an (N, K, L, R, λ )-BIBD gradient code. It is shown in [9] that the average error of the soft BIBD gradient code can be as good as that of BIBD with the same parameters. However, the construction of soft BIBD requires the encoding matrix to have binary entries. This potentially limits the set of system parameters the gradient code can take because the encoding matrix can take any real-valued entries in the general gradient code framework. So, in the remaining part of this paper, we demonstrate how the set of system parameters can be enlarged by exploiting real-valued encoding matrices. IV. S PARSE GAUSSIAN GRADIENT CODE 

In this section, we propose the sparse Gaussian gradient code, which extends the parameter regime inherited from the BIBD gradient code while preserves comparably good theoretical performance. BIBD gradient codes offer strong guarantees but exist only for limited parameter regimes, while random constructions are more flexible yet difficult to control. A real-valued framework bridges this divide. By combining prescribed sparsity with Gaussian weights, the sparse Gaussian gradient code broadens the achievable parameter range while preserving analytical tractability and strong approximation performance. 4

A. Construction 

The encoding matrix ESG is constructed by the element-wise product of two K × N matrices X and B, i.e., 

ESG  

> ij

= Xij Bij . (4) To generate X, we choose a correlated multivariate normal distribution N (μ, Σ) as the joint distribution for each row, where μ ∈ RN and Σ ∈ RN ×N defined as 

μ = a1N (5) and 

Σ =



b c · · · cc b · · · c

... ... . . . ...

c c · · · b

 . (6) The K row vectors 

[Xi1, Xi2, . . . , XiN ] , i ∈ [K],

are generated i.i.d. according N (μ, Σ). Note that different rows of X are independent, while different columns of X

are correlated due to the correlation in Σ.Now, to generate matrix B, each entry Bij 

> i.i.d.

∼ Bern ( γ).This way, we ensure that a γ fraction of the encoding matrix is non-zero in expectation. For the construction of an (N, K, L, R, λ, γ )-SG gradient code which simulates an (N, K, L, R, λ )-BIBD gradient code, three criteria have to be satisfied. 1) To mimic the BIBD property that each column has L

ones, we require for any i ∈ [K] and j ∈ [N ],

E

h

ESG 

> ij

i

= L/K ; (7) 2) To mimic the BIBD property that the intersection be-tween any two distinct columns is λ, we require for any 

i ∈ [K] and j, k ∈ [N ], j̸ = k, 

E

h

ESG  

> ij

ESG 

> ik

i

= λ/K ; (8) 3) To ensure the intersections between any column and itself is L, we require for any i ∈ [K] and j ∈ [N ],

E

h

ESG  

> ij

ESG 

> ij

i

= L/K. (9) Subsequently, for i ∈ [K] and j, k ∈ [N ], j̸ = k:

E [Xij Bij ] = L/K , E [Xij Xik Bij Bik ] = λ/K and 

E X2 

> ij

B2

> ij

 = L/K can be obtained from Equations (7), (8) and (9), respectively. Since the entries of the encoding matrix are independent of the i.i.d. Bernoulli random variables, a system of equations is shown as follows: 



aγ (a)

= L/K 

(c + a2)γ2 (b)

= λ/K 

(b + a2)γ (c)

= L/K , (10) where (a), (b) and (c) follow from E [Xij ] = a, E [Xij Xik ] = Cov( Xij Xik ) + E [Xij ] E [Xik ] = c + a2 and E X2

> ij

 =Var( Xij ) + E [Xij ]2 = b + a2, respectively. Hence, we have 



a = L/ (Kγ )

b = ( Lγ K − L2 

> K2

)/γ 2

c = ( λK − L2 

> K2

)/γ 2

, (11) where γ is restricted to some range, which will be discussed in the following subsection. 

B. Feasible Parameter Regime 

In the following, we provide a sufficient condition for the existence of the above Sparse Gaussian gradient code. 

Theorem 1. An (N, K, L, R, λ, γ ) Sparse Gaussian gradient code exists if L ≤ K and 

max 

 λL , (N − 1)( L2 − Kλ ) + L2

KL 



≤ γ ≤ 1.

Guided by Theorem 1, Fig. 1 provides a visualization of a feasible parameter space for L and R in sparse Gaussian gradient codes with specified values of N, L/K and R/N . In comparison, we plot several BIBDs with similar density L/K 

and the parameter region for soft BIBD gradient codes with the same settings of N, L/K and R/N . As illustrated, the parameter region for the proposed Sparse Gaussian gradient code expands the choices of feasible parameters compared to those of the soft BIBD and the BIBD gradient codes.     

> Fig. 1. The region in pink is the parameter region for our proposed SG gradient codes. In contrast, the region in blue is the parameter region for soft-BIBD gradient codes with the probabilistic method. The red dots are combinatorial BIBDs with a similar density, i.e., 0.24 ±0.05 .

C. Theoretical Performance Evaluation 

In this section, we provide a high probability upper bound on the random worst-case error of the sparse Gaussian gradient code. In the regime of parameters specified in Theorem 2, the proposed sparse Gaussian gradient code can have an error performance comparable to that of the symmetric or dual BIBD gradient code with the same parameters. 5

Theorem 2. Consider (N, K, L, R, λ, γ ) Sparse Gaussian gradient codes with encoding matrix ESG and K =Θ( N ), L = Θ( N ), λ = Θ( N ), γ = Θ(1) . For 0 < δ < 1/2

and 0 < α < 2δ, let the number of stragglers S = O(N α)

and ρ = LL+λ(N −S−1) . If ρ2(N − S)( b + ( N − S − 1) c) < 2

and c ≥ 0, then 

P

h

err( ESG , S ) ≤ err( EB, S ) + 1

> K1/2−δ

i

≥ 1 − e−Θ( N 2δ ),

where err( EB, S ) is the worst-case error of symmetric or dual 

(N, K, L, R, λ )-BIBD gradient code given in Equation (3) .

Remark 1. Note that 1 

> K1/2−δ

= o(1) and e−Θ( N 2δ ) = o(1) .This means in the specified parameter regime, with high probability, the error associated with the sparse Gaussian gradient code concentrates at or below the error of the BIBD Gradient Code with the same parameters. 

The proof of Theorem 2 is provided in Appendix A-A. V. E XPANSION -P RESERVING GRADIENT CODE 

CONSTRUCTION 

In this section, we introduce a new gradient coding scheme called expansion-preserving gradient code (EP-GC). We notice that the error of an BEG-GC can be upper bounded by a function of the second largest eigenvalue of its encoding matrix when all other parameters are fixed. The EP-GC builds on this insight by first generating an initial encoding matrix with a small second-largest eigenvalue (e.g., via repeated sampling), and then appending a row and column to enforce fixed row and column sums. This structure allows the use of D EGREE PRESERVING SPARSIFY , which both sparsifies the matrix and tightly controls changes to the second largest eigen-value. As a result, we can track and bound the corresponding change in error throughout the sparsification process. 

A. Construction 

Formally, the encoding matrix of an (N, c, d, ε )-EP-GC is constructed in the following three steps: 1) Initial Matrix Generation: Construct a symmetric 

(N − 1) × (N − 1) random matrix EEP 0 whose upper-triangular entries are i.i.d. standard half-normal random variables. Specifically, for 1 ≤ i ≤ j ≤ N − 1,

(EEP 0 )ij = |Xij | + c, Xij 

> i.i.d.

∼ N (0 , 1) .

2) Row and Column Sum Adjustment: Extend EEP 0 to an N × N symmetric matrix EEP by appending a final row and column as 

EEP =

EEP 0 mm⊤ α



, (12) where m ∈ RN −1 and α ∈ R are chosen such that row and column sums equal to d. Specifically, let 

mi = d − P 

> j∈[N−1]

(EEP 0 )ij , ∀i (13) and 

α = (2 − N )d + P 

> i,j ∈[N−1]

(EEP 0 )ij . (14)         

> Fig. 2. Comparison of parameter regimes for (37 , d )-BEG-GC (red dots) and
> (37 ,12 , d, ε )-EP-GC (green region). For EP-GC to be valid, dmust satisfy Eq.(17). Based on 2,000 independent samples of E0, the green band spans the median feasible interval; the shaded bands indicate the 5th–95th percentile range of each bound ( dlin blue, duin orange).

3) Degree-Preserving Sparsification: Consider the matrix 

EEP as the biadjacency matrix of the graph G. Then, the adjacency matrix of the bipartite graph G is 

A =

 0 EEP 

(EEP )⊤ 0



.

Note that the degree matrix of G is D = dI2N . To reduce density while preserving expansion properties, we apply D EGREE PRESERVING SPARSIFY [16] to ob-tain Gε = DEGREE PRESERVING SPARSIFY (G, ε ). The corresponding biadjacency matrix EEP  

> ε

is used as the final encoding matrix. We note that Gε is degree-preserving ε-sparsifier of G with 

ε ∈ (0 , 1] satisfying the following properties: 1) Gε is a subgraph of G, and thus is also a bipartite graph. For Gε, we denote its adjacency matrix as Aε

and biadjacency matrix as EEP  

> ε

.2) The weighted degree of each vertex remains unchanged (degree-preserving), i.e. for the degree matrix of Gε:

Dε = D. (15) 3) For the Laplacians of G and Gε, denoted as L ≜ D −A

and Lε ≜ Dε − Aε, respectively, we have 

∥L − Lε∥2 ≤ (eε − 1) ∥L∥2 . (16) 

B. Feasible Parameter Regime 

Both (N, d )-BEG-GC and (N, c, d, ε )-EP-GC can be viewed as undirected weighted graphs: BEG-GC uses binary weights, while EP-GC uses real-valued weights. Although d

denotes the node degree in both, it plays different roles. In BEG-GC, d determines sparsity and thus fixes the workload and replication levels. In EP-GC, d specifies only the graph degree, while sparsity is independently controlled by ε.Consequently, BEG-GC tightly couples degree, redundancy, and computational load, whereas EP-GC decouples structural regularity from sparsity. This decoupling enables a continuous tradeoff between accuracy and efficiency through ε, without al-tering d, and leads to substantially greater parameter flexibility. 6

In particular, BEG-GC and BIBD-GC constructions impose rigid combinatorial constraints on (N, d, L, R ), while EP-GC admits a significantly broader feasible parameter regime, as characterized below. 

Theorem 3. An (N, c, d, ε ) EP-GC exists, if and only if 

max  

> i∈[N−1]

P 

> j∈[N−1]

(EEP 0 )ij 

| {z }

> dl

< d <    

> P
> i,j ∈[N−1] (EEP 0)ij
> N−2

| {z }

> du

. (17) 

Proof. Positivity of all entries in EEP requires Eqs. (13) and (14) to be greater than zero, yielding Eq.(17). 

Corollary 1. The necessary conditions for Eq. (17) to hold are 

c > 

h        

> (N−2) max
> i∈[N−1]
> P
> j∈[N−1]
> |Xij |− P
> i,j ∈[N−1]
> |Xij |

i  

> N−1

and N ≥ 3.

Proof. Substituting (EEP 0 )ij = c + |Xij | into the expressions for dl and du from Theorem 1 gives 

dl = ( N − 1) c + max  

> i∈[N−1]

P 

> j∈[N−1]

|Xij |,du = (N −1) 2cN −2 +     

> P
> i,j ∈[N−1] |Xij |
> N−2

.

Since du contains the factor (N − 2) −1, we must have N̸ = 2 .Moreover, N = 1 is impossible, since in that case Eq. (17) is not well defined. Hence, as N is a positive integer, it follows that N ≥ 3. We then take the difference, 

du − dl

=    

> P
> i,j ∈[N−1]
> |Xij |
> N−2

+ max  

> i∈[N−1]

P 

> j∈[N−1]

|Xij |

+ c

h (N −1) 2  

> N−2

− (N − 1) 

i

=         

> P
> i,j ∈[N−1]
> |Xij |− (N−2) max
> i∈[N−1]
> P
> j∈[N−1]
> |Xij |
> !
> N−2

+ c(N −1)   

> N−2

.

(18) Setting Eq.(18) to be greater than zero and solving for c

complete the proof. These conditions are mild and impose no combinatorial structure. Moreover, ε ∈ (0 , 1] can be chosen to continuously tune sparsity. As a result, for fixed N , EP-GC admits a significantly broader feasible parameter regime than BEG-GC, as illustrated in Fig. 2. 

C. Theoretical Performance Evaluation 

We evaluate the performance of the proposed expansion-preserving gradient code by comparing it to BIBD gradient code and the sparse Gaussian gradient code. Through theoret-ical analysis, we derive the error performance as demonstrated in the accompanying proof (see supplementary material). The main results are summarized as follows: 

Theorem 4. Given a weighted bipartite graph G, let Gε =

DEGREE PRESERVING SPARSIFY (G, ε ) and EEP  

> ε

be the bi-adjacency matrix of Gε, we have 

err( EEP  

> ε

, S ) ≤ 1

> N



2ε√N − S

 NN −S



+ λ2(EEP )

> d

q N S N −S

2

.

(19) Our proof of Theorem 4 is as follows. 

Proof. We first choose the decoding vector defined in Eq.(2). Therefore, we can bound err( EEP , S ) by 

err( EEP  

> ε

, S ) ≤ 1 

> N

max   

> F⊂ [N]
> |F| =N−S

EEP  

> ε

vF − 1K

> 22

= 1 

> N

max   

> F⊂ [N]
> |F| =N−S

EEP  

> ε

vF − EEP vF

| {z }

> ∆1

+ EEP vF − 1N

| {z }

> ∆2
> 22(a)

≤ 1 

> N

max   

> F⊂ [N]
> |F| =N−S

(∥∆1∥2 + ∥∆2∥2)2 , (20) where (a) follows by applying the submultiplicativity of the spectral norm and the triangle inequality. We now state the following lemma, with its proof deferred to Appendix B-A. 

Lemma 1. Given a weighted bipartite graph G, let Gε =

DEGREE PRESERVING SPARSIFY (G, ε ). For any nonempty set 

F ⊂ [n] of size N − S, with the choice of vF being defined as Eq. (2) , we have 

∥∆1∥2 ≤ 2ε√N − S

 NN −S



and ∥∆2∥2 ≤ λ2(EEP )

> d

q N S N −S .

According to Lemma 1, the bounds of ∆1 and ∆2 are irrelevant to the non-straggling pattern F and thus right-hand-side term of Eq.(20) is upper bounded by 

> 1
> N



2ε√N − S

 NN −S



+ λ2(EEP )

> d

q N S N −S

2

,

which completes the proof of Eq.(19). Since EEP is random, λ2(EEP ) is random. We will now upper bound λ2(EEP ) by using design parameters (N, c, d, ε ).We start analyzing ∥EEP 0 ∥2 ≜ σ1(EEP 0 ), which can be written as 

∥EEP 0 ∥2(b)

=

q

λ1(EEP 0

> ⊤

EEP 0 ) (c)

=

q

λ1

 (EEP 0 )2

=

q λ1(EEP 0 )2 = |λ1(EEP 0 )| (21) where (b) is because the square of each singular value of square matrix EEP 0 equals a non-negative eigenvalues of 

EEP 0

> ⊤

EEP 0 ; (c) is because EEP 0 is symmetric. Following the construction given in Sec.V-A, EEP is random, and thus 

λ2(EEP ) in (19) is a random variable. We therefore derive a high-probability upper bound on λ2(EEP ) in terms of the design parameters (N, c, d, ε ).

Theorem 5. For the matrix EEP constructed in Sec. V-A, there exists C1 > 0 such that for any t > 0, we have 

λ2(EEP ) ≤ | μ + c|(N − 1) + C1(√N − 1 + t)7  

> Fig. 3. A comparison of worst-case normalized errors between our proposed sparse Gaussian gradient code (SG-GC), expansion-preserving gradient code (EP-GC), BIBD gradient code (BIBD-GC) and other exisiting gradient codes, with a specific focus on similar column densities, being 0.25 for EP-GC, and
> 0.24 for SG-GC, BIBD-GC and the other gradient codes.

with probability at least 1 − 4e−t2

.Proof. We leverage the following Lemma to bound λ2(EEP ).

Lemma 2 (Cauchy’s eigenvalue interlacing theorem [17, Theorem 4.3.15]) . Suppose A ∈ Rn×n is symmetric. Let 

B ∈ R(n−1) ×(n−1) be a principal submatrix (obtained by deleting both i-th row and i-th column for some values of 

i). Suppose A has eigenvalues λ1 ≥ · · · ≥ λn and B has eigenvalues β1 ≥ · · · ≥ βm. Then 

λ1 ≥ β1 ≥ λ2 ≥ β2 ≥ · · · ≥ βn−1 ≥ λn.

Recall our construction of EEP as of Eq.(12), we can rewrite EEP 0 and apply Lemma 2 and conclude λ2(EEP ) ≥

λ2(EEP 0 ). Therefore, combined with Eq.(21), we obtain 

λ2(EEP ) ≤ λ2(EEP 0 ) ≤ | λ1(EEP 0 )| = ∥EEP 0 ∥2 (22) Then, we turn to bound ∥EEP 0 ∥2 by using the following Lemma, whose proof is deferred to Appendix B-B. 

Lemma 3. For the matrix EEP 0 constructed in Sec. V-A, there exist absolute constants C1 > 0 such that for any t > 0,

∥EEP 0 ∥2 ≤ | μ + c|(N − 1) + C1(√N − 1 + t)

with probability at least 1 − 4e−t2

.

Combining Lemma 3 with Eq.(22) completes the proof. VI. E XPERIMENTAL PERFORMANCE EVALUATION 

In this section, we complement our theoretical analyses with a simulation comparing the worst-case error among six gradi-ent codes with similar workload densities. We select a valid BIBD-GC and choose parameters for other gradient codes to match its column density as closely as possible. To obtain the EP-GC and SG-GC configurations whose performance is closest to that of BIBD-GC, we evaluated thousands of random seeds and selected the best-performing instances for plotting. As shown in Fig. 3, the BIBD gradient code serves as the baseline due to its near-optimal worst-case reconstruction error across the entire range of straggler fractions. It consistently achieves the lowest error and provides a reference for evalu-ating other constructions. The proposed SG-GC and EP-GC closely track the perfor-mance of BIBD-GC over a wide range of straggler fractions. In the low-straggler regime, all three codes exhibit very small errors, indicating that the redundancy structure is sufficient to ensure accurate recovery. As the straggler fraction increases to the mid regime, a modest gap emerges between the proposed methods and BIBD-GC. However, this gap remains small, and both SG-GC and EP-GC preserve a similar growth trend as BIBD-GC. In the high-straggler regime, the performance of all codes degrades rapidly due to the severe loss of information. Notably, EP-GC becomes nearly indistinguishable from BIBD-GC in this region, suggesting that the expansion-preserving structure effectively captures the dominant spectral properties governing worst-case error. SG-GC also remains competitive, though with slightly larger deviations. In contrast, other existing gradient codes such as BGC and rBGC exhibit consistently higher errors across all regimes, indicating weaker robustness to stragglers. The FRC construc-tion shows the poorest performance, with pronounced stepwise behavior due to its coarse combinatorial structure, leading to significant error jumps as the number of stragglers increases. Overall, while BIBD-GC remains the optimal benchmark, the proposed SG-GC and EP-GC achieve near-optimal per-formance with substantially greater flexibility in parameter selection. This makes them practical surrogates in settings where BIBD constructions are unavailable or infeasible. VII. C ONCLUSION 

In this paper, we proposed two gradient codes, termed sparse Gaussian gradient code and expansion-preserving gra-dient code. Both codes are formulated as probabilistic mod-els that employ distinct sparsification schemes. Their error performance is analyzed theoretically and validated through numerical experiments. From both perspectives, they achieve performance comparable to the BIBD gradient code with an appropriate choice of system parameters. Future work may explore refined sparsification mechanisms that account for intrinsic worker characteristics, such as com-putation speed and recovery behavior following failures. Ex-tending these constructions to adaptive or dynamically evolv-ing graph structures could further illuminate their effectiveness and scalability in large-scale distributed learning systems. 8

APPENDIX APROOF FOR SPARSE GAUSSIAN GRADIENT CODE 

In this section, we prove Theorem 2. We begin with a definition and the required lemmas. 

Definition 2 ( [18, Definition 2.7.5]) . The sub-exponential norm of a random variable Z is defined as 

∥Z∥ψ1 = inf {t > 0 : E [exp( |Z|/t ) ≤ 2] }.

If ∥Z∥ψ1 is finite, we say that Z is sub-exponential. 

Lemma 4. Let σmax = ρ(N − S)1/2(b + ( N − S − 1) c)1/2

and βmax = ρ(N − S)a − 1. If σmax < √2, c ≥ 0 and ρ ≥ 0,then for any i ∈ [K], Z2 is a sub-exponential random variable with sub-exponential norm 

∥Z2∥ψ1 ≤ max 

n

2σ2max + β2max  

> ln ( √2/σ max )

, 4σ2max 

o

.

Lemma 5. If Z is a sub-exponential random variable then 

Z − E [Z] is a sub-exponential with 

∥Z − E [Z] ∥ψ1 ≤ C∥Z∥ψ1 ,

where C > 0 is an absolute constant. 

Lemma 6 (Bernstein’s inequality [18, Theorem 2.8.1]) . Let 

Z1, . . . , Z K be independent, mean zero, sub-exponential ran-dom variables. Then, for every t > 0, we have 

P

h PKi=1 Zi ≥ t

i

≤ 2 exp 

h

− C min  t2  

> PKi=1 q2

, t 

> max iq

oi 

,

where q = ∥Z∥ψ1 and C > 0 is an absolute constant. 

Lemma 7. Let ESG be the encoding matrix for an 

(N, K, L, R, γ )-SG-GC and ρ = LL+λ(N −S−1) . For any non-straggling set F with |F| = N − S, the expected error using constant decoding vector ρ1F is given by 

E∥ρESG 1F − 1K ∥22

 = K · err( EB, S ),

where err( EB, S ) is the error of an (N, K, L, R, λ )-BIBD-GC. 

The proofs of Lemmas 4, 5, and 7 are provided in Appen-dices B-C, B-D, and B-E, respectively. 

A. Proof of Theorem 2 Proof. Suppose an (N, K, L, R, λ, γ )-SG gradient code exists and denote its encoding matrix as ESG . For any non-straggling set F ⊂ [N ] with |F| = N − S, i ∈ [K], we define 

Xi = ρ P 

> j∈F

ESG  

> ij

− 1, Yi = X2 

> i

− E X2

> i

 . (23) Since rows in ESG are sampled from the same multivari-ate Gaussian distribution, i.e. N (μ, Σ), independently (cf. Sec.IV-A), then Y1, . . . , Y K is i.i.d. with mean zero. Under the assumptions on ρ = LL+λ(N −S−1) > 0, ρ2(N − S)( b +(N − S − 1) c) < 2 and c ≥ 0, Lemma 4 gives 

∥X2 

> i

∥ψ1 ≤ max 



2σ2max + β2max  

> ln (
> √2
> σmax )

, 4σ2max 



,

where 2σ2max +β2max 1ln ( √2  

> σmax )

= Θ(1) and 4σ2max = Θ(1) . So, 

X2 

> i

is a sub-exponential random variable by Definition 2. From Lemma 5, ∥Yi∥ψ1 ≤ C1∥X2 

> i

∥ψ1 for a constant 

C1 > 0, and thus Yi is also a sub-exponential random variable according to Lemma 4. Similarly, according to Lemma 4, Yi

is also a sub-exponential random variable and thus 

∥Yi∥ψ1 ≤ C1∥X2 

> i

∥ψ1 ≤ C1 max 



2σ2max + β2max  

> ln (
> √2
> σmax )

, 4σ2max 



.

Since rows of ESG are i.i.d. Gaussian (cf. Sec.IV-A), {Yi}Ki=1 

are i.i.d. mean-zero. We apply Lemma 6 on {Yi}Ki=1 and choose t = K1/2+ δ , where 0 < δ < 1/2. So, for any F ⊂ [N ]

with |F| = N − S, we have 

P

h PKi=1 Yi ≥ K1/2+ δ i

≤ 2 exp 



− C2 min 

 K1+2 δ 

> K·C1·∥ X2
> i∥2
> ψ1

, K1/2+ δ 

> ∥X2
> i∥ψ1



, (24) where C2 > 0. Thus, for any F ⊂ [N ] with |F| = N − S,

P

h

err( ESG , S ) − E[err( ESG , S )] ≥ K1/2+ δ i

> (a)

= P

h PKi=1 Xi − PKi=1 E [Xi] ≥ K1/2+ δ i

= P

h PKi=1 (Xi − E [Xi]) ≥ K1/2+ δ i

= P

h PKi=1 Yi ≥ K1/2+ δ i

> (b)

≤ 2 exp 



− C2 min 

 K2δ 

> C1·∥ X2
> i∥2
> ψ1

, K1/2+ δ 

> ∥X2
> i∥ψ1



(25) 

= 2 exp 



− C2 min 

 

> K2δ
> C1∥X2
> i∥2
> ψ1

, K1/2+ δ 

> ∥X2
> i∥ψ1

  

, (26) where an absolute constant C2 > 0. Above, (a) and (b) follow from Lemma 7 and Eq. (24), respectively. Therefore, we have 

P

h

err( ESG , S ) ≤ err( EB, S ) + Kδ−1/2i

= P

h

K · err( ESG , S ) ≤ Kerr( EB, S ) + K1/2+ δ i

> (c)

≥ P



max   

> F⊂ [N]
> |F| =N−S

err( ESG , S ) ≤ K · err( EB, S ) + K1/2+ δ



> (d)

= P

h ∀F⊂ [N ],|F| =N −S, 

> err( ESG ,S )≤E

[err( ESG ,S )]+K1/2+ δ

i

≥ P

 ∀F⊂ [N ],|F| =N −S, 

> err( ESG ,S )−E

[err( ESG ,S )] <K 1/2+ δ



= 1 − P

 ∃F ,|F| =N −S, s.t. 

> err( ESG ,S )−E

[err( ESG ,S )] ≥K1/2+ δ



> (e)

≥ 1 − 2 NS

P

h

err( ESG , S ) − E

h

err( ESG , S )

i

≥ K1/2+ δ i

(27) Above, (c) follows since 

K · err( ESG , S ) ≜ max   

> F⊂ [N]
> |F| =N−S

∥ESG  

> F

vopt − 1K ∥22

≤ max   

> F⊂ [N]
> |F| =N−S

∥ρESG  

> F

1N −S − 1K ∥22 = max   

> F⊂ [N]
> |F| =N−S

err ρ, F (ESG ).

(d), (e) follow from Lemma 7, the union bound, respectively. Recall system parameters satisfy Eq.(11). With the as-sumptions of γ = Θ(1) , K = Θ( N ), L = Θ( N ) and 9

λ = Θ( N ), we have a = Θ(1) , b = Θ(1) , c = Θ(1) .

Moreover, since S = O(N α) for 0 < α < 2δ, we obtain ρ = Θ(1 /N ), σ 2max = Θ(1) , β max = Θ(1) . Thus, 

2σ2max + β2max    

> ln ( √2
> σmax )

= Θ(1) and 4σ2max = Θ(1) . Furthermore, 

∥X2 

> i

∥ψ1 = Θ(1) . It follows that K2δ /∥X2 

> i

∥2 

> ψ1

= Θ( N 2δ ) and 

K1/2+ δ /∥X2 

> i

∥ψ1 = Θ( N 1/2+ δ ). Since when 0 < δ < 1/2,

N 1/2+ δ grows faster than N 2δ , we obtain 

min 

 K2δ 

> ∥X2
> i∥2
> ψ1

, K1/2+ δ 

> ∥X2
> i∥ψ1



= Θ( N 2δ ). (28) Thus, combined with Eqs.(24), (26), (27) and (28), we have 

Perr( ESG , S ) ≤ err( EB, S ) + Kδ−1/2

≥ 1 − 2 NS

 exp[ −Θ( N 2δ )] .

Note that  NS

 ≤ N S = exp( S ln N ). When S = O(N α) and 

0 < α < 2δ, we have 

 NS

 exp( −Θ( N 2δ )) = exp( −Θ( N 2δ ) + O(N α ln N )) = exp( −Θ( N 2δ )) = o(1) ,

which completes the proof of Theorem 2. APPENDIX BPROOF FOR LEMMAS 

A. Proof of Lemma 1 Proof. With the choice of decoding vector being defined by Eq.(2), we first bound ∥∆1∥2 in Eq.(20) by arranging it as 

∥∆1∥2 = ∥EEP  

> ε

vF − EEP vF ∥2

= 1

d ∥(EEP  

> ε

− EEP )( 1N + aF )∥2(a)

≤ 1

d ∥EEP  

> ε

− EEP ∥2 ∥1N + aF ∥2 , (29) where (a) follows from the sub-multiplicative property of a matrix norm. To complete the bounding for Eq.(29), we first finish the calculation for ∥1N + aF ∥2 first as follows: 

∥1N + aF ∥2 =

qP 

> i̸∈F

(1N + aF )2 

> i

+ P 

> i∈F

(1N + aF )2

> i

=

rP 

> i̸∈F

(1 − 1) 2 + P

> i∈F



1 + SN −S

2

= √N − S



1 + SN −S



. (30) Then, we bound EEP  

> ε

− EEP 

> 2

. Let A and Aε be the adjacency matrices of G and Gε respectively. We have 

A − Aε =

" 0 EEP 

EEP ⊤ 0

#

−

" 0 EEP 

> ε

EEP  

> ε
> ⊤

0

#

=

" 0 EEP − EEP 

> ε

EEP ⊤ − EEP  

> ε
> ⊤

0

#

=

 0 EEP − EEP 

> ε

(EEP − EEP  

> ε

)⊤ 0



.

To bound EEP  

> ε

− EEP 

> 2

, it suffices to bound ∥A − Aε∥2.By the following lemma, the singular values of EEP coincide with the absolute eigenvalues of the associated bipartite ad-jacency matrix, so any spectral-norm bound for EEP  

> ε

− EEP 

transfers directly to ∥A − Aε∥2.

Lemma 8 (Modified [17, Theorem 7.3.3]) . Let B be a real square matrix with singular values σ1(B) ≥ σ2(B) ≥ · · · ≥ 

σn(B) ≥ 0. For a block matrix P ≜

 0 BB⊤ 0



with eigenvalues λ1(P ) ≥ λ2(P ) ≥ . . . ≥ λ2n(P ), we have 

{λi(P )}2ni=1 = {σi(B)}ni=1 ∪ {− σi(B)}ni=1 .

We then obtain that 

EEP − EEP  

> ε2(a)

= σ1(EEP − EEP  

> ε

) (b)

= λ1(A − Aε)

> (c)

= σ1(A − Aε) (a)

= ∥A − Aε∥2 , (31) where (a) is because of the definition of matrix norm; (b) is because of Lemma 8; (c) is because of A − Aε is symmetric. Now, we try to bound ∥A − Aε∥2. We will need to leverage the definition of Laplacian matrix. 

Definition 3 (Laplacian matrix) . For a weighted graph G with 

n nodes, the Laplacian matrix Ln×n is defined as L = D−A,

where D is the degree matrix, and A is the graph’s weighted adjacency matrix. 

We use the Definition 3 and triangle inequality, and obtain 

∥A − Aε∥2 = ∥(D − L) − (Dε − Lε)∥2

= ∥(D − Dε) − (L − Lε)∥2

≤ ∥ D − Dε∥2 + ∥L − Lε∥2 . (32) According to the construction of EEP (cf. Sec.V-A), we apply the following Lemma on the right-hand-side terms of Eq.(32). 

Lemma 9. Let EEP be the encoding matrix of an (N, d, ε )

expansion-preserving gradient code. If the degree parameter obeys |d| ≤ N α for some fixed constant α > 0, then all entries of EEP satisfy EEP  

> ij

≤ N O(1) . Let G be the graph whose biadjacency matrix is EEP , and let Gε =

DEGREE PRESERVING SPARSIFY (G, ε ) for ε ∈ (0 , 1] . Denote by L and Lε the Laplacians of G and Gε, respectively. Since 

Gε is a degree-preserving ε-sparsifier of G, their Laplacians satisfy ∥L − Lε∥2 ≤ (eε − 1) ∥L∥2 .

The proof of 9 is provided in Sec.B-F. By Lemma 9, Gε is a degree-preserving ε-sparsifier of G,thus Eq.(15) and ∥L − Lε∥2 ≤ (eε − 1) ∥L∥2 hold. So, We have ∥A − Aε∥2 ≤ ∥ L − Lε∥2 ≤ (eε − 1) ∥L∥2 . Since 

∥L∥2 = ∥D − A∥2 = d − λn, then ∥L∥2 ≤ d − (−d) = 2 d.Therefore, Eq.(31) can be bounded as 

EEP − EEP  

> ε2

≤ 2( eε − 1) d. (33) Combining this bound with Eqs.(29) and (30), it follows that 

∥∆1∥2 = ∥EEP  

> ε

vF − EEP vF ∥2

≤ 2( eε − 1) √N − S



1 + SN −S



,10 

which gives the bound for ∆1.Now, we turn to bound ∆2 in Eq.(20). Given the follow-ing lemma, we may assume without loss of generality that 

u1(EEP ) = v1(EEP ) = 1N and 

∥ui(EEP )∥2 = ∥vi(EEP )∥2 = 1 , i ∈ { 2, . . . , n }. (34) 

Lemma 10. For the encoding matrix EEP  

> ε

of an (N, c, d, ε )

expansion-preserving gradient code, we conclude that 

i) (1N , 1N , d ) ∈ { (ui(EEP  

> ε

), vi(EEP  

> ε

), σ i(EEP  

> ε

)}ni=1 .

ii) σ2(EEP  

> ε

) < d .

The proof of Lemma 10 is deferred to Appendix B-G. We replace vF with Eq.(2) and obtain 

∥∆2∥2 = ∥EEP vF − 1N ∥2

= ∥ 1 

> d

EEP (1N + aF ) − 1N ∥2 = ∥ 1 

> d

EEP aF ∥2. (35) Due to the particular choice of aF (see Eq.(2)), we have the following lemma. 

Lemma 11. For any non-empty set F ⊂ [N ], there exists 

α2, . . . , α N ∈ R such that aF = α2v2(EEP ) + · · · +

αN vN (EEP ), and ∥aF ∥2 =

qPNi=2 α2 

> i

=

q N S N −S .

The proof of Lemma 11 is provided in Appendix B-H. Therefore, Eq.(35) can be rewritten as 

∥ 1 

> d

EEP aF ∥2 = ∥ 1 

> d

EEP PNi=2 αivi(EEP )



∥2(d)

= ∥ 1 

> d

EEP PNi=2 αiσi(EEP )vi(EEP )∥2

= 1

> d

qPNi=2 α2 

> i

σ2 

> i

(EEP ) (e)

≤ σ2(EEP )

> d

q N S N −S ,

where (d) is because of Eq.(34) and (e) follows from Lemma 11, which finishes the bound for ∥∆2∥2.

B. Proof of Lemma 3 

We begin with introducing sub-Gaussian random variables and sub-Gaussian norm. 

Definition 4 ( [18, Definition 2.5.6 & Proposition 2.5.2]) . The sub-Gaussian norm of a random variable X is defined as 

∥X∥ψ2 = inf {t > 0 : E exp( X2/t 2) ≤ 2}.

If ∥X∥ψ2 is finite, we say X is sub-Gaussian. 

Then, we prove Lemma 3 as follows. 

Proof. Let μ ≜ E|G| =

q 2 

> π

, where G ∼ N (0 , 1) . Define a symmetric random matrix W by 

EEP 0 = ( μ + c)11 ⊤ + W ,

where 1 ∈ RN −1. Equivalently, for 1 ≤ i ≤ j ≤ N − 1,

W ij = |Gij | − μ, and W ji = W ij for i > j .Here are properties of W . Since the random variables {Gij :1 ≤ i ≤ j ≤ N − 1} are i.i.d., the collection {W ij : 1 ≤ i ≤

j ≤ N − 1} is independent. Moreover, E[W ij ] = E|Gij | − 

μ = 0 . Note that |G| is sub-Gaussian because Pr( |G| > t ) ≤

2e−t2/2 for t ≥ 0.

Lemma 12 ( [18, Lemma 2.6.8]) . If X is a sub-Gaussian random variable then X − E [X] is a sub-gaussian, too, and 

X − E [X] ≤ C∥X∥ψ2 ,

where C is an absolute constant. 

Hence, by Lemma 12, W ij is i.i.d. sub-Gaussian with mean zero and there exists an absolute constant K > 0 such that 

∥Wij ∥ψ2 ≤ K, for all 1 ≤ i ≤ j ≤ N − 1. Now, we apply the following Lemma on W .

Lemma 13 (Modified [18, Corollary 4.4.8]) . Let A be an n×n

symmetric random matrix whose entries Aij on and above the diagonal are i.i.d., mean zero, sub-gaussian random variables. Then, for any t > 0 we have 

P ∥A∥ ≥ CK (√n + t) < 4 exp( −t2),

where C is an absolute constant and K = ∥Aij ∥ψ2 .

So, there exist absolute constants C1 > 0 such that for any 

t ≥ 0, P ∥W ∥2 ≤ C1(√N − 1 + t) ≥ 1 − 4 exp( −t2).

On the other hand, since 11 ⊤ is rank one, ∥11 ⊤∥2 =

∥1∥22 = N − 1. Therefore, ∥(μ + c)11 ⊤∥2 = |μ + c|(N − 1) .

Hence, by the triangle inequality, 

∥EEP 0 ∥2 ≤ ∥ (μ+c)11 ⊤∥2 +∥W ∥2 = |μ+c|(N −1)+ ∥W ∥2.

Combining this with the above tail bound for ∥W ∥2, we obtain 

P[∥EEP 0 ∥2 ≥ | μ + c|(N − 1) + C1(√N − 1 + t)] < 4e−t2

.

C. Proof of Lemma 4 Proof. By Definition 4, in order to obtain ∥Xi∥ψ2 , we need to calculate Eexp  X2 

> i

/C 2  first. Plug Xi =

ρ PN −Sj=1 Gij Bij − 1, where Gij 

> i.i.d.

∼ N (a, b ) and Bij 

> i.i.d.

∼

Bern (γ), into Eexp  X2 

> i

/C 2  , we have 

E

h

exp 

 X2

> i
> C2

i 

= E



exp 



ρ PN −Sj=1 Gij Bij − 1

2

/C 2

 

.

We use the law of total expectation and obtain 

E

"

exp 



ρ N −SP

> j=1

Gij Bij − 1

2

/C 2

#

= E

h

E

h

e(ρ PN −Sj=1 Gij Bij −1)2/C 2

{Bij : j ∈ [N − S]}

ii 

> (a)

= N −SP

> p=0

 N −Sp

γp(1 − γ)N −S−pE

h

e

 ρ Ppj=1 Gij −1

2

> C2

i

= N −SP

> p=0

 N −Sp

γp(1 − γ)N −S−pE

h

e

 ρ Ppj=1 Gij −ρpa +ρpa −1

2

> C2

i

= N −SP

> p=0

 N −Sp

γp(1 − γ)N −S−pE

h

exp 

  Y +β

2

> C2

i 

,

where (a) comes from Bij 

> i.i.d.

∼ Bern (γ). Let Y ≜ ρ pP

> j=1

Gij −

ρpa and β ≜ ρpa − 1. Therefore, Y ∼ N (0 , σ 2) where σ2 =

ρ2(pb + ( p2 − p)c).11 

Lemma 14. If a random variable G ∼ N (μ, σ 2), then for 

θ ∈ R and α ≤ 12σ2 ,

E exp ( αG 2 + θG ) = 1q 1  

> σ2−2α

exp 



− σ2(θ+2 μ)2 

> 4ασ 2−2

− μ2

> 2σ2



.

The proof of Lemma 14 is deferred to Appendix B-I. We apply Lemma 14 and have 

E



exp 

 1

> C2

 ρ Ppj=1 Gij − ρpa + ρpa − 12

= E

h

exp   Y 2+2 βY +β2

> C2

i

= 1r  

> 1
> σ2−2
> C2

exp 



− 4σ2( βC2 )2 

> 4σ2
> C2−2

+ β2

> C2



= 1r  

> 1
> σ2−2
> C2

exp 

 β2

> C2

 1 − 2σ2

> 2σ2−C2



. (36) Note that one essential condition for C is 2σ2 < C 2, which follows from 1 

> σ2

− 2 

> C2

> 0. Let Cψ2 be an upper bound of ∥Xi∥ψ2 , i.e., Cψ2 ≥ ∥ Xi∥ψ2 . By Definition 4, it follows that 2σ2 < C 2 

> ψ2

. We assume that C2 = qσ 2 where q ∈ R.Therefore, the right-hand-side of Eq. (36) can be simplified as   

> σr
> (1 −2
> q)

exp 

 β2

> σ2

  1

> q−2



. (37) Since 1 − 2 

> q

> 0 is required, we must have q > 2. To avoid this constraint, we can assume q ≥ 4 which implies that p1 − 2/q ≥ p1/2. Then, Eq. (37) can be upper bounded by √2σ exp 

 β2

> σ2

 1

> q−2

 

. Now, we further assume that √2σ exp 

 β2

> σ2

 1

> q−2

 set 

≤ 2 and obtain 

> β2
> σ2

 1

> q−2



≤ ln ( √2 

> σ

) (38) We must have σ < √2 to be satisfied since β2 

> σ2

( 1 

> q−2

) > 0

when q ≥ 4. Therefore, Inequality (38) can be rewritten as 

2 + β2 

> σ2
> 1ln ( √2/σ )

≤ q, (39) Therefore, conditioned on C2 = qσ 2 with q ≥ 4, σ < √2,and Eq. (39), C2 can be bounded as follows: 

C2 ≥ max 

n 

2 + β2

> σ2
> 1ln ( √2/σ )



σ2, 4σ2o

= max 

n

2σ2 + β2 

> ln ( √2/σ )

, 4σ2o

.

Recall that σ2 = ρ2(pb + ( p2 − p)c) and β = ρpa − 1. Let 

f (p) = ρ2(p2c + p(b − c)) = ρ2p(pc + ( b − c)) and g(p) = 

ρpa − 1, for p = 0 , 1, . . . , N − S. Since the covariance matrix 

Σ is a positive semi-definite matrix, b − c ≥ 0 is required. So, if c > 0, f (p) increases with the increment of p; if c = 0 ,then f (p) = 0 for p = 0 , 1, . . . , N − S. Furthermore, g(p) = 

ρpa − 1 doesn’t decrease with the increment of p if ρa ≥ 0.So, we claim that if c ≥ 0 and ρa ≥ 0, then 2σ2 + β2

> ln ( √2/σ )

doesn’t decrease with the increment of p. Denote f (N − S)

and g(N − S) as σmax and βmax respectively, then 

Cψ2 

> 2

= max 



2σ2max + β2max    

> ln ( √2
> σmax )

, 4σ2max 



, (40) where σmax 2 = ρ2(( N − S)b + (( N − S)2 − (N − S)) c) and 

βmax = ρ(N − S)a − 1. By Definition 4 and the assumption of Cψ2 being an upper bound of the sub-Gaussian norm of 

Xi, ∥Xi∥ψ2 < ∞ can be obtained. So, Lemma 4 follows. 

D. Proof of Lemma 5 Proof. By the triangle inequality of a norm, we have 

∥X − E [X] ∥ψ1 ≤ ∥ X∥ψ1 − ∥ E [X] ∥ψ1 . (41) Thus it suffices to bound the second term. According to definition 2, for a random variable Z ≜ E [X], we trivially have ∥E [X] ∥ψ1 ≲ |E [X] |. By Jensen’s inequality, we then have |E [X] | ≤ E [|X|] = ∥X∥1 .

Lemma 15 (Modified [18, proposition 2.7.1]) . Let X be a sub-exponential random variable, the moments of X satisfy that for K > 0,

∥X∥Lp = E [|X|p]1/p ≤ Kp, for all p ≥ 1.

Apply this lemma and let p = 1 , we obtain ∥X∥1 ≲ ∥X∥ψ1 .

Substitute this into Eq.(41), we complete the proof. 

E. Proof of Lemma 7 Proof. Assume an (N, K, L, R, λ, γ )-SG gradient code exists, and let ESG ∈ RK×N denote its encoding matrix. Let F ⊂ 

[N ] denote the non-straggler set, with |F| = N −S. Define the decoding vector vopt (F) ∈ RN such that, for each i ∈ [N ],the i-th entry satisfies vi(F) = ρ if i ∈ F , and vi(F) = 0 

otherwise, where ρ is the scalar decoding coefficient. Then for each row i ∈ [K], define 

Xi = ( ESG v(F)) i − 1 = ρ P 

> j∈F

ESG  

> ij

− 1.

The expectation of the sum of squared deviations is 

E

hPKi=1 X2

> i

i

= E[∥ESG v(F) − 1K ∥22]= E[1⊤ 

> K

1K − 2ρ1⊤ 

> N

1⊤F ESG ⊤1K ρ2 1⊤F ESG ⊤ESG 1F ]= 1⊤ 

> K

1K − 2ρ 1⊤ 

> N

E[ESG ⊤1K ]1F + ρ2 1⊤F E[ESG ⊤ESG ]1F ,

where 1F ∈ { 0, 1}N is the indicator vector of F (i.e., [1F ]i =1 if i ∈ F , 0 otherwise). Because ESG is row-wise i.i.d. Gaussian with expected value satisfying Eq. (7), we obtain 

E

h

ESG ⊤1K

i

= L1N . Hence, 

1⊤ 

> N

E

h

ESG ⊤1K

i

1F = L(N − S).

Next, note that E

h

ESG ⊤ESG i

is an N × N matrix with diagonal entries E e⊤ 

> i

ei

 = L and off-diagonal entries 

E e⊤ 

> i

ej

 = λ for i̸ = j, where ei denotes the i-th column of ESG . Thus, E

h

ESG ⊤ESG i

= ( L − λ)IN + λJN ×N .

Therefore, 

1⊤F E

h

ESG ⊤ESG i

1F = ( L − λ)1⊤F 1F + λ(1⊤F 1N )( 1⊤ 

> N

1F )= ( L − λ)( N − S) + λ(N − S)2.

Hence, E

hPKi=1 X2

> i

i

equals the following expression 

K − 2ρL (N − S) + ρ2 (L − λ)( N − S) + λ(N − S)2.12 

Setting ρ = LL+λ(N −S−1) , this expectation yields 

E

hPKi=1 X2

> i

i

= K



1 − 1  

> KL2(N−S)
> L+λ(N−S−1)



By Eq.(3), we have E[PKi=1 X2 

> i

] = K ·err( EB, S ), where EB

is the encoding matrix of an (N, K, L, R, λ ) BIBD-GC. 

F. Proof of Lemma 9 

In this proof, we retain the notation introduced in Sec. V-A. We begin by recalling a key guarantee of the algorithm DEGREE PRESERVING SPARSIFY .

Lemma 16 (modified from [16, Theorem 3.3]) . For any ε ∈

(0 , 1] , every undirected graph G whose edge weights are (i) strictly positive and (ii) polynomially bounded, meaning that each weight satisfies wij ≤ N c for some absolute constant 

c > 0, admits a degree-preserving ε-sparsifier. The algorithm 

DEGREE PRESERVING SPARSIFY takes G as input and returns such a degree-preserving ε-sparsifier Gε.

Before applying the algorithm, we verify that the weighted adjacency matrix EEP of the resulting sparsifier satisfies the assumptions required for our analysis: 1) each entry EEP  

> ij

is positive; 2) each entry is polynomially bounded, i.e., EEP  

> ij

≤ N C

for some constant C > 0.Once Conditions 1–2 hold, the returned graph Gε is indeed a degree-preserving ε-sparsifier of G, enabling us to bound the operator norm of the difference between their Laplacian matrices. 

Proof. We first validate the condition 1. Since the entries of 

EEP 0 are i.i.d. drawn from standard half-normal distribution, the entries are positive. According to the constraints of d

summarized in Sec.V-B, d > max  

> 1≤i≤N−1

PN −1 

> j

(EEP 0 )ij , which implies that mi = d − PN −1 

> j

(EEP 0 )ij > 0. Recall that 

α = (2 − N )d + PN −1

> i=1

PN −1 

> j=1

(EEP 0 )ij ,

where every term is positive, and thus α > 0. Therefore, the entries of EEP  

> ij

> 0 for all 1 ≤ i, j ≤ N .We then validate the condition 2. According to the con-struction described in Sec.V-A, (EEP 0 )ij follows i.i.d. standard half-normal distribution, that is, for 1 ≤ i, j ≤ N − 1, EEP 

> ij

follows i.i.d. standard half-normal distribution. Therefore, we obtain that, for 1 ≤ i, j ≤ N − 1 and t1 ≥ 0,

P

h

EEP  

> ij

≥ t1

i

= P [( |N (0 , 1) | ≥ t1)] = P [N (0 , 1) ≥ 1] + P [N (0 , 1) ≤ − 1] 

> (a)

= 2 P [N (0 , 1) ≥ 1] 

> (b)

≤ 2 exp( −t21/2) ,

where (a) is because of the symmetry of standard Gaussian distribution and (b) follows by applying Chernoff bound on a standard Gaussian random variable. Let M0 = max 1≤i,j ≤N −1 EEP  

> ij

. By union bound, we have 

P [M0 ≥ t1] = P

h

∪(N −1)  

> i,j =1

{EEP  

> ij

≥ t2}

i

≤ PN −1

> i

PN −1 

> j

P

h

EEP  

> ij

≥ t2

i

≤ 2( N − 1) 2 exp( −t22/2) . (42) Choose t2 = p2(1 + δ) ln( N − 1) 2, the right-hand-side of Eq.(42) can be simplified to 2( N − 1) 2δ . Therefore, with probability 1 − 2( N − 1) 2δ ,

M0 ≤ p2(1 + δ) ln( N − 1) 2 = p4(1 + δ) ln( N − 1) .

Since pln( N ) = o(N C1 ) for some fixed C1 > 0 and 1 ≤

i, j ≤ N − 1, we have 

EEP  

> ij

≤ O(pln( N )) = o(N C1 ) ≤ O(N C1 ). (43) Then we analyze entries of m and α, i.e. {EEP  

> ij

}i=N ∪{EEP  

> ij

}j=N . Let Si := PN −1 

> j=1

(EEP 0 )ij for 1 ≤ i ≤ N − 1.So, mi = d − Si for 1 ≤ i ≤ N − 1. Because (EEP 0 )ij follow standard half-normal distribution i.i.d. for 1 ≤ i ≤ j ≤ N − 1.Let μ = E [|N (0 , 1) |] = p2/π and thus E [Si] = μ(N − 1) 

by linearity of expectations. Since a standard half-normal distribution has the same tail as a standard Gaussian distribution, then a standard half-normal distribution is also a sub-Gaussian random variable. According to Definition 4, for some constant C2 > 0, we have 

P [|Si − μ(N − 1) | ≥ t3] ≤ 2 exp( −C2t23/N ).

Choose t3 = C3

√N ln N with C3 > 0, then 

P

h

|Si − μ(N − 1) | ≥ C3

√N ln N

i

≤ 2N −C2C23 .

Take union bound over all i ≤ N − 1, then we have 

P

h

max   

> 1≤i≤N−1

|Si − μ(N − 1) | ≥ C3

√N ln N

i

≤ 2N 1−C2C23 .

By triangle inequality, we have 

max   

> 1≤i≤N−1

|Si| + |μ(N − 1) | ≥ max   

> 1≤i≤N−1

|Si − μ(N − 1) |.

Thus, for constant C2, C 3 > 0, we have 

max   

> 1≤i≤N−1

|Si| ≤ C3

√N ln N − μ(N − 1) = O(N ),

with probability of 1 − 2N 1−C2C23 . When C2C23 is large enough, 1 − 2N 1−C2C23 → 0. With assumption of |d| ≤ N C

with some fixed C > 0, we obtain that, for 1 ≤ i ≤ N − 1,

|mi| = |d − Si| ≤ | d| + |Si| (c)

≤ N C + O(N ) ≤ C4N max( C, 1) ,

where (c) follows from triangle inequality. So every entry in 

m is bounded by N C4 for some constant C4 = max( C, 1) .We now turn to analyze α as follows: 

α = (2 − N )d + PN −1

> i=1

PN −1 

> j=1

(EEP 0 )ij 

> (d)

= (2 − N )N C + PN −1 

> i=1

O(N C1 )= (2 − N )N C + O(N 2C1 ) ≤ C5N max(2 C1,C +1) ,

where (d) comes from Eq.(43). Similarly, α is also bounded by N C6 , for some constant C6 = max(2 C1, C +1) . Therefore, we complete the validation that the entries of EEP are polynomially bounded. 13 

Assume that Gε is a degree-preserving ε-sparsifier of G in the multiplicative sense, namely, for all x ∈ R2N ,

e−ε x⊤Lx ≤ x⊤Lεx ≤ eε x⊤Lx . (44) Subtracting x⊤Lx from all sides of Eq. (44) yields 

 e−ε − 1 x⊤Lx ≤ x⊤(Lε − L)x ≤  eε − 1 x⊤Lx .

Since L ⪰ 0, we have x⊤Lx ≥ 0. Taking absolute values, 

x⊤(Lε − L)x ≤ max { eε − 1, 1 − e−ε } x⊤Lx .

For ε ≥ 0, note that eε − 1 ≥ 1 − e−ε, since eε + e−ε ≥ 2.Therefore, x⊤(Lε − L)x ≤  eε − 1 x⊤Lx . For any unit vector ∥x∥2 = 1 , we further have 

x⊤(Lε − L)x ≤  eε − 1 λ1(L) ∥x∥22 =  eε − 1 ∥L∥2.

Taking the supremum over all ∥x∥2 = 1 and using the variational characterization of the spectral norm for symmetric matrices, it follows that 

∥L − Lε∥2 = sup 

> ∥x∥2=1

x⊤(Lε − L)x ≤  eε − 1 ∥L∥2.

G. Proof of Lemma 10 Proof. By the construction method described in Sec.V-A, the adjacency matrix of Gε is 

Aε =

" 0 EEP 

> ε

EEP  

> ε
> ⊤

0

#

∈ R2N ×2N .

In Step 2, every row and every column of the pre–sparsified matrix (i.e. EEP ) sums to d, and Step 3 preserves weighted degrees at every vertex. So, we have EEP  

> ε

1N = d 1N , which implies (1N , 1N ) is an eigenvector of Aε with eigenvalue d.It follows that (1N , 1N , d ) is a singular triplet of EEP  

> ε

, since 

EEP 

> ε
> ⊤

EEP  

> ε

1N = EEP 

> ε
> ⊤

(d1N ) = d EEP 

> ε
> ⊤

1N = d21N , i.e., 

1N is a right singular vector of EEP  

> ε

with singular value d.This proves item (i). For (ii), let (ui(Aε), vi(Aε), λ i(Aε)) be any eigenpair of 

Aε with s, t ∈ RN . Therefore, 

A

ui(Aε)

vi(Aε)



= λ

ui(Aε)

vi(Aε)



⇐⇒ 

(EEP  

> ε

vi(Aε) = λui(Aε),

EEP 

> ε
> ⊤

ui(Aε) = λvi(Aε).

Thus, we have EEP 

> ε
> ⊤

EEP  

> ε

vi(Aε) = λi(Aε)2vi(Aε) and 

EEP  

> ε

EEP 

> ε
> ⊤

ui(Aε) = λi(Aε)2ui(Aε), so the eigenvalues of A are {± σi(EEP  

> ε

)}Ni=1 . Since the underlying bipartite graph is connected (the dense construction in Step 2 yields a connected graph and the degree-preserving sparsifier keeps it connected), the Perron–Frobenius theorem gives λ1(Aε) = d

and λ2(Aε) < d . Hence, σ1(EEP  

> ε

) = d and σ2(EEP  

> ε

) < d, 

which is item (ii). 

H. Proof of Lemma 11 Proof. By construction, 

1⊤ 

> N

aF = ( N − S)



− SN −S



+ S · 1 = −S + S = 0 .

Hence aF ⊥ 1N . By Lemma 10, we have aF ⊥ v1(EEP ).Because {v1(EEP ), . . . , vN (EEP )} is an orthonormal basis, this implies 

aF ∈ span {v2(EEP ), . . . , vN (EEP )},

so there exist α2, . . . , α N with aF = PNi=2 αivi(EEP ).From the definition of aF (see Eq.(2)), we have 

∥aF ∥22 = ( N − S)

 SN −S

2

+ S · 12 = S2  

> N−S

+ S = N S N −S .

Since {vi(EEP )}Ni=1 is orthonormal and aF ⊥ v1(EEP ),we have ∥aF ∥22 = PNi=2 αi

> 2

. Therefore, it follows that 

PNi=2 α2 

> i

= N S N −S . Thus, ∥aF ∥2 =

q N S N −S .

I. Proof of Lemma 14 Proof. Let G ∼ N (μ, σ 2). The corresponding PDF is 

> 1
> σ√2π

R ∞−∞ exp ( − 12 ( x−μσ )2). Therefore, 

E exp ( αG 2 + θG )

= 1

> σ√2π

Z ∞−∞ 

exp ( αx 2 + θx ) exp ( − 12 ( x−μσ )2)dx 

= 1

> σ√2π

Z ∞−∞ 

exp  αx 2 + θx − 12 ( x−μσ )2dx. 

Noting the identity 

αx 2 + θx − 12 ( x−μσ )2 = αx 2 + θx − 12σ2 (x2 − 2μx + μ2)= 2ασ 2−12σ2 (x + σ2(θ+2 μ)2 

> 2ασ 2−1

)2 − σ2(θ+2 μ)2 

> 4ασ 2−2

− μ2

> 2σ2

and applying a change of variable, we obtain 

E exp ( αG 2 + θG )

= exp   − σ2(θ+2 μ)2 

> 4ασ 2−2

− μ2

> 2σ2

 1 

> σ√2π

·

Z ∞−∞ 

exp   − 1/σ 2−2α 

> 2

x2dx 

= exp   − σ2(θ+2 μ)2 

> 4ασ 2−2

− μ2

> 2σ2

 1√1/σ 2−2α .

If G is a standard normal random variable, i.e., G ∼ N (0 , 1) ,

E exp ( αG 2 + θG ) = 1√1−2α exp ( θ2 

> 2−4α

).

APPENDIX CALGORITHMS FOR DEGREE PRESERVING SPARSIFIER 

We cite D EGREE PRESERVING SPARSIFY and related Algo-rithms from [16] in this appendix. It is worth noting that our construction permits positive real-valued edge weights. For practical implementation, we approximate the positive real edge weights using a fixed-point representation. Specifically, for a chosen precision parameter κ = 2 k, where k ∈ N, we define 

˜we = ⌊κw e⌉ ∈ N,

where ⌊·⌉ denotes rounding to the nearest integer. We then apply D EGREE PRESERVING SPARSIFY to the integer weights 

˜we, and rescale the output by 1/κ .14 

This procedure introduces a quantization error of at most 

1/κ per edge, which can be made arbitrarily small by choosing 

k sufficiently large. In practice, this provides a tractable way to obtain integer weights while preserving the structure of the problem up to a controlled approximation. 

Algorithm 1: DEGREE PRESERVING SPARSIFY (G, ε )

Input: Weighted graph G

Choose κ = 2 k such that κw e ∈ N for all edges e.Scale weights: we ← κw e for all e.Decompose edges so that all weights are powers of 2.Compute r, a 1.5-approximation of effective resistances in G.

while |E(G)| ≥ Ω( ˆ m log n + nLε −2 log n) do 

G ←

SPARSIFY ONCE (G, r, SHORT CYCLE DECOMP )

Rescale weights: we ← we/κ for all e. return G

Algorithm 2: NAIVE CYCLE DECOMP (G)

Input: Graph G(V, E )

Output: Cycle decomposition C

while ∃ u ∈ V with D(u) ≤ 2 do 

Remove u and its incident edges from G

return C

Algorithm 3: EXTRACT BDDEG GRAPH (G, ∆) 

Input: Graph G(V, E ), threshold ∆

Output: Graph H with degree O(∆) 

Initialize H ← ∅ .

foreach vertex u ∈ V do 

Add up to ∆ incident edges of u to H.

foreach vertex v in H with D(v) > 2∆ do 

Split v into 

j D(v)∆

k

vertices with balanced degrees. 

return H

Algorithm 4: SHORT CYCLE DECOMP (G, l, k )

Input: Graph G(V, E ), recursion depth l, parameter k

Output: Cycles C and extra edges Eextra 

if l = 0 or |V | < k then return NAIVE CYCLE DECOMP (G)

Set ∆ ←  64 · 10 6 γNS (n)4 log 2 nl k.Initialize C ← ∅ , Eextra ← ∅ .

while G is non-empty do 

Remove vertices of degree < ∆ and add their incident edges to Eextra .

H ← EXTRACT BDDEG GRAPH (G, ∆) (S, Cpartial ) ← MOVE EDGES (H, k )

Construct HS from (S, Cpartial )

CHS ← SHORT CYCLE DECOMP (HS , l − 1, k )

Lift CHS to cycles in G, add to C, remove from G

return (C, E extra )

Algorithm 5: MOVE EDGES (G, k )

Input: Graph G = ( V, E ), parameter k

Output: Vertex set S and a partial cycle decomposition onto S

Set α ← dmin /(4 γNS (n)) .

(Es, E d) ← NSE XPANDER DECOMPOSE (G, α ).Initialize S ← ∅ .

foreach connected component H of Es do if |V (H)| ≤ k then 

Apply N AIVE CYCLE DECOMP (H) and record the resulting cycles. 

else 

Apply M OVE EDGES EXPANDER (H, α/d max , k )

and record the output. 

return S and the union of the recorded partial decompositions 

Algorithm 6: MOVE EDGES EXPANDER (G, ϕ, k )

Input: Graph G = ( V, E ), conductance lower bound 

ϕ, parameter k

Output: Vertex set S and a partial cycle decomposition onto S

Pair parallel edges to form cycles. Set S as the ⌈n/k ⌉ highest-degree vertices. 

foreach edge e = ( u, v ) ∈ E do 

Generate 4k lazy random walks 1 from each of u

and v of length O(ϕ−2 log n).Discard walks traversing e.If both endpoints yield walks terminating in S,form a cycle via the induced paths in G/S .Select a maximal set of edge-disjoint cycles. 

if fewer than Ω

 ϕ4mk log 2 n



cycles are obtained then 

repeat the process 

return S and the selected cycles 15 

REFERENCES [1] J. Dean, G. Corrado, R. Monga, K. Chen, M. Devin, M. Mao, M. Ran-zato, A. Senior, P. Tucker, K. Yang et al. , “Large scale distributed deep networks,” NeurIPS , vol. 25, 2012. [2] M. F. Aktas, P. Peng, and E. Soljanin, “Effective straggler mitigation: Which clones should attack and when?” ACM SIGMETRICS Perfor-mance Evaluation Review , vol. 45, no. 2, pp. 12–14, 2017. [3] G. Ananthanarayanan, A. Ghodsi, S. Shenker, and I. Stoica, “Effective straggler mitigation: Attack of the clones,” in 10th USENIX Symposium on Networked Systems Design and Implementation , 2013, pp. 185–198. [4] M. Zaharia, M. Chowdhury, M. J. Franklin, S. Shenker, and I. Stoica, “Spark: Cluster computing with working sets,” in 2nd USENIX Workshop on Hot Topics in Cloud Computing (HotCloud 10) , 2010. [5] H. Mania, X. Pan, D. Papailiopoulos, B. Recht, K. Ramchandran, and M. I. Jordan, “Perturbed iterate analysis for asynchronous stochastic optimization,” SIAM Journal on Optimization , vol. 27, no. 4, pp. 2202– 2229, 2017. [6] M. Glasgow and M. Wootters, “Approximate gradient coding with optimal decoding,” IEEE journal on selected areas in information theory , vol. 2, no. 3, pp. 855–866, 2021. [7] N. Raviv, I. Tamo, R. Tandon, and A. G. Dimakis, “Gradient coding from cyclic mds codes and expander graphs,” IEEE Transactions on Information Theory , vol. 66, no. 12, pp. 7475–7489, 2020. [8] S. Kadhe, O. O. Koyluoglu, and K. Ramchandran, “Gradient coding based on block designs for mitigating adversarial stragglers,” in IEEE Internat. Symp. Inf. Theory (ISIT) , 2019, pp. 2813–2817. [9] A. Sakorikar and L. Wang, “Soft bibd and product gradient codes,” 

IEEE Journal on Selected Areas in Information Theory , vol. 3, no. 2, pp. 229–240, 2022. [10] A. Marcus, D. A. Spielman, and N. Srivastava, “Interlacing families i: Bipartite ramanujan graphs of all degrees,” in IEEE 54th Annual Symposium on Foundations of computer science , 2013, pp. 529–537. [11] H. Janwa and A. Lal, “On expanders graphs: Parameters and applica-tions,” CoRR , vol. cs.IT/0406048, 01 2004. [12] V. N. Sachkov, Combinatorial methods in discrete mathematics . Cam-bridge University Press, 1996, no. 55. [13] R. Tandon, Q. Lei, A. G. Dimakis, and N. Karampatziakis, “Gradient coding: Avoiding stragglers in distributed learning,” in International Conference on Machine Learning . PMLR, 2017, pp. 3368–3376. [14] Z. Charles, D. Papailiopoulos, and J. Ellenberg, “Approximate gradient coding via sparse random graphs,” arXiv:1711.06771 , 2017. [15] C. J. Colbourn and J. H. Dinitz, Handbook of Combinatorial Designs .Chapman and Hall/CRC, 2006. [16] T. Chu, Y. Gao, R. Peng, S. Sachdeva, S. Sawlani, and J. Wang, “Graph sparsification, spectral sketches, and faster resistance computation via short cycle decompositions,” SIAM Journal on Computing , vol. 52, no. 6, pp. FOCS18–85–FOCS18–157, 2023. [Online]. Available: https://doi.org/10.1137/19M1247632 [17] R. A. Horn and C. R. Johnson, Matrix Analysis . Cambridge University Press, 1985. [18] R. Vershynin, “High-dimensional probability,” University of California, Irvine , vol. 10, p. 11, 2020.