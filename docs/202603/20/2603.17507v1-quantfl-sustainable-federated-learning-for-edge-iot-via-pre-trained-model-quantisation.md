---
title: "QuantFL: Sustainable Federated Learning for Edge IoT via Pre-Trained Model Quantisation"
title_zh: QuantFL：通过预训练模型量化实现边缘物联网的可持续联邦学习
authors: "Charuka Herath, Yogachandran Rahulamathavan, Varuna De Silva, Sangarapillai Lambotharan"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17507v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 面向边缘物联网的可持续联邦学习
tldr: 针对物联网设备在联邦学习中因频繁上行传输导致的高能耗和碳足迹问题，本文提出 QuantFL 框架。该框架利用预训练模型初始化来支持激进且轻量级的量化方案，通过桶量化技术减少通信开销，无需复杂的误差反馈。实验表明，QuantFL 在保持高精度的同时，显著降低了通信比特数，为受限的物联网网络提供了一种可持续的绿色训练方案。
selection_source: fresh_fetch
motivation: 联邦学习在物联网应用中面临高昂的通信能耗和碳排放挑战，而预训练模型在降低微调能耗方面的潜力尚未被充分挖掘。
method: 提出 QuantFL 框架，利用预训练初始化带来的参数更新集中性，采用内存高效的桶量化实现激进压缩，且无需复杂的误差反馈机制。
result: "在 MNIST 和 CIFAR-100 上，QuantFL 减少了约 40% 的总通信量（上行链路减少可达 80% 以上），且在严格带宽限制下精度优于未压缩基准。"
conclusion: QuantFL 为电池受限的物联网设备提供了一种实用且环保的联邦学习方案，实现了通信效率与模型性能的平衡。
---

## 摘要
联邦学习（FL）在物联网（IoT）设备上实现了隐私保护的智能化，但由于频繁上行传输的高能耗，产生了显著的碳足迹。尽管预训练模型在边缘设备上已日益普及，但其在降低微调能耗方面的潜力尚未得到充分探索。在这项工作中，我们提出了 QuantFL，这是一个可持续的联邦学习框架，它利用预训练初始化来实现激进且计算轻量级的量化。我们证明了预训练能自然地集中更新统计信息，使我们能够使用内存高效的分桶量化（bucket quantisation），而无需复杂误差反馈机制带来的高能耗开销。在 MNIST 和 CIFAR-100 数据集上，QuantFL 在严格的带宽预算下，将总通信量减少了 40%（全精度下行链路时总比特数减少约 40%；上行链路或下行链路量化时减少 $\geq 80\%$），同时达到或超过了未压缩的基准模型；BU 在比特数减少几个数量级的情况下，分别实现了 89.00%（MNIST）和 66.89%（CIFAR-100）的测试准确率。我们还考虑了上行和下行链路的成本，并对量化水平和初始化进行了消融实验。QuantFL 为受电池限制的物联网网络上的可扩展训练提供了一种实用的“绿色”方案。

## Abstract
Federated Learning (FL) enables privacy-preserving intelligence on Internet of Things (IoT) devices but incurs a significant carbon footprint due to the high energy cost of frequent uplink transmission. While pre-trained models are increasingly available on edge devices, their potential to reduce the energy overhead of fine-tuning remains underexplored. In this work, we propose QuantFL, a sustainable FL framework that leverages pre-trained initialisation to enable aggressive, computationally lightweight quantisation. We demonstrate that pre-training naturally concentrates update statistics, allowing us to use memory-efficient bucket quantisation without the energy-intensive overhead of complex error-feedback mechanisms. On MNIST and CIFAR-100, QuantFL reduces total communication by 40\% ($\simeq40\%$ total-bit reduction with full-precision downlink; $\geq80\%$ on uplink or when downlink is quantised) while matching or exceeding uncompressed baselines under strict bandwidth budgets; BU attains 89.00\% (MNIST) and 66.89\% (CIFAR-100) test accuracy with orders of magnitude fewer bits. We also account for uplink and downlink costs and provide ablations on quantisation levels and initialisation. QuantFL delivers a practical, "green" recipe for scalable training on battery-constrained IoT networks.

---

## 论文详细总结（自动生成）

这是一份关于论文《QuantFL: Sustainable Federated Learning for Edge IoT via Pre-Trained Model Quantisation》的结构化技术总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对**边缘物联网（IoT）环境下的联邦学习（FL）可持续性问题**展开研究。
*   **核心挑战**：传统的联邦学习在训练过程中需要频繁上传大型模型更新，这导致了极高的通信开销和能源消耗，对于电池受限的物联网设备而言，会产生显著的碳足迹。
*   **研究动机**：作者发现，虽然预训练模型在边缘设备上越来越普及，但利用预训练初始化来降低微调阶段通信开销的潜力尚未被充分挖掘。
*   **核心发现**：预训练模型能使模型参数的更新量（$\Delta w$）在统计上更加集中（方差和动态范围更小），这为采用更激进、更简单的量化方法提供了可能。

### 2. 方法论：核心思想与关键技术
论文提出了 **QuantFL** 框架，其核心思想是结合**预训练初始化**与**分层桶量化（Bucketed Scalar Quantisation）**。
*   **核心流程**：
    1.  **初始化**：使用部分预训练的模型初始化全局模型 $w_0$。
    2.  **本地训练**：客户端在本地数据上训练得到 $w_{local}$，计算更新量 $\Delta w = w_{local} - w_{global}$。
    3.  **分层量化**：对每一层更新量进行量化。QuantFL 提供了两种方案：
        *   **BU (Bucket-Uniform)**：将更新值范围等分为 $L$ 个桶，简单且对异构数据鲁棒。
        *   **BQ (Bucket-Quantile)**：根据经验分布将更新值划分为等质量（等数量坐标点）的桶。
    4.  **通信优化**：客户端仅发送量化索引（Index），而码本（边界信息）则以较低频率（每 $T$ 轮）更新，从而分摊通信成本。
    5.  **解码聚合**：服务器采用中点解码（Mid-point decoding）重建更新量并进行聚合。
*   **关键创新**：QuantFL 摒弃了复杂的**误差反馈（Error Feedback, EF）**机制。作者认为预训练自然地最小化了更新方差，因此无需 EF 即可实现高保真压缩，从而降低了边缘设备的内存和计算负担。

### 3. 实验设计
*   **数据集**：MNIST（简单任务）和 CIFAR-100（复杂任务）。
*   **模型架构**：MNIST 使用浅层 CNN；CIFAR-100 使用 ResNet-18。
*   **对比方法（Benchmarks）**：
    *   **FedAvg (NQ)**：非量化、非预训练的基准。
    *   **QSGD**：经典的随机量化方法。
    *   **NP (Not Pre-trained)**：不使用预训练初始化的对比组，用于验证预训练的作用。
*   **场景设置**：模拟 100 个用户，每轮随机选择 10 个客户端，考虑了非独立同分布（Non-IID）数据分布（通过 Dirichlet-$\alpha$ 系数控制）。

### 4. 资源与算力
*   **硬件环境**：使用了一台高性能计算服务器，配置为 **NVIDIA RTX 6000 GPU (48GB VRAM)** 和 **Intel Core i9-10980 处理器**。
*   **训练细节**：文中提到了本地训练为 2 个 Epoch，但未明确说明总训练时长（小时/天）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同难度的标准数据集，并针对量化级别（$L=64, 128$）和数据异构性（$\alpha=1.0, 0.5, 0.1$）进行了多组实验。
*   **消融实验**：专门对比了“预训练+量化”与“从零训练+量化”的效果，充分证明了预训练对量化性能的提升作用。
*   **客观性**：实验考虑了上行和下行链路的完整比特成本，并使用了 Pareto 前沿曲线来展示精度与通信成本的权衡，评价维度较为全面。

### 6. 主要结论与发现
*   **通信大幅减少**：在保持或超过基准精度的情况下，QuantFL 将总通信量减少了约 **40%**（若仅计算上行链路，比特数减少可达 **80% 以上**）。
*   **BU 表现优异**：在 CIFAR-100 等复杂且非 IID 的任务中，**Bucket-Uniform (BU)** 量化比 BQ 和 QSGD 更具鲁棒性。
*   **预训练的协同效应**：预训练不仅加速收敛，还通过缩小更新量的动态范围，使得简单的标量量化也能达到极低的失真度。
*   **精度表现**：在 MNIST 上达到 89.00% 精度，在 CIFAR-100 上达到 66.89% 精度，均优于同等比特预算下的其他压缩方法。

### 7. 优点
*   **绿色可持续**：直接针对 IoT 设备的能耗痛点，通过减少通信比特数延长设备电池寿命。
*   **算法简洁**：无需存储残差误差（无 EF），降低了边缘设备的内存占用和逻辑复杂度。
*   **实用性强**：利用了现成的预训练模型趋势，易于在实际生产环境中部署。

### 8. 不足与局限
*   **数据集覆盖有限**：仅在 MNIST 和 CIFAR-100 上进行了验证，缺乏在大规模数据集（如 ImageNet）或自然语言处理任务（如 Transformer 模型）上的实验。
*   **下行链路假设**：部分实验结果基于“全精度下行链路”的假设，虽然文中讨论了下行链路量化，但实际部署中下行链路的带宽压力也可能成为瓶颈。
*   **预训练依赖**：该方法的效果高度依赖于预训练模型的质量和可用性，对于完全新颖、无预训练模型可用的领域不适用。

（完）
