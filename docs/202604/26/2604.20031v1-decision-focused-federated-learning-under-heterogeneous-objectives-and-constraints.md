---
title: Decision-Focused Federated Learning Under Heterogeneous Objectives and Constraints
title_zh: 异构目标与约束下的决策聚焦联邦学习
authors: "Konstantinos Ziliaskopoulos, Alexander Vinel"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.20031v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 决策聚焦的联邦学习框架
tldr: 本研究提出了决策聚焦联邦学习（DFFL）框架，旨在解决多个代理在不共享原始数据的情况下，通过预测模型优化下游线性决策的问题。针对客户端目标函数和约束条件的异构性，研究基于SPO+损失函数建立了异构性界限，并提出了衡量联邦学习收益的决策准则。实验证明，该方法在强凸约束下表现稳健，而在多面体约束下受约束异构性影响较大，为异构环境下的决策优化提供了理论支持。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中各客户端下游优化问题的目标函数和可行域约束存在异构性时，如何有效提升决策质量的问题。
method: 基于SPO+代理损失函数，利用支撑函数表示法推导了异构性界限，并设计了一种权衡本地与联邦风险的启发式决策规则。
result: 实验表明，FedAvg风格的DFFL在强凸约束下具有广泛的鲁棒性，而多面体约束下的性能主要受约束异构性的影响。
conclusion: 即使下游优化问题存在显著差异，直接结合FedAvg与SPO+的方法在特定条件下（尤其是强凸情形）仍能取得良好的决策效果。
---

## 摘要
我们考虑一种被称为“决策聚焦联邦学习（DFFL）”的框架，即由一组代理采用的“先预测后优化”方法。在这种方法中，每个代理的预测模型作为下游线性优化问题的输入，且不允许直接交换原始数据。重要的是，客户端在目标函数和可行性约束方面可能存在差异。我们基于著名的 SPO+ 方法，并针对这种情况开发了 SPO+ 代理损失的异构性界限。这是通过采用可行域的支撑函数表示来实现的，从而将（i）通过代价向量之间的范数距离表示的目标偏移，与（ii）通过约束集之间的形状距离表示的可行集偏移分离开来。在强凸可行域的情况下，由于优化器的稳定性，可以推导出更紧致的界限。基于这些结果，我们定义了一个启发式的“本地对比联邦”超额风险决策规则。在 SPO+ 风险下，该规则给出了联邦预期能提高决策质量的条件：异构性惩罚必须小于汇集数据的统计优势。我们在多面体和强凸问题上实现了一组 FedAvg 风格的 DFFL 实验，结果表明联邦在强凸设置下具有广泛的鲁棒性，而在多面体设置下，性能主要随约束异构性而下降，特别是对于样本量较多的客户端。换句话说，特别是在强凸情况下，即使下游优化问题存在显著差异，直接实现 FedAvg 和 SPO+ 的方法仍然可以产生有前景的性能。

## Abstract
We consider what we refer to as {Decision-Focused Federated Learning (DFFL)} framework, i.e., a predict-then-optimize approach employed by a collection of agents, where each agent's predictive model is an input to a downstream linear optimization problem, and no direct exchange of raw data is allowed. Importantly, clients can differ both in objective functions and in feasibility constraints. We build on the well-known SPO+ approach and develop heterogeneity bounds for the SPO+ surrogate loss in this case. This is accomplished by employing a support function representation of the feasible region, separating (i) objective shift via norm distances between the cost vectors and (ii) feasible-set shift via shape distances between the constraint sets. In the case of strongly convex feasible regions, sharper bounds are derived due to the optimizer stability. Building on these results, we define a heuristic local-versus-federated excess risk decision rule which, under SPO+ risk, gives a condition for when federation can be expected to improve decision quality: the heterogeneity penalty must be smaller than the statistical advantage of pooling data. We implement a FedAvg-style DFFL set of experiments on both polyhedral and strongly convex problems and show that federation is broadly robust in the strongly convex setting, while performance in the polyhedral setting degrades primarily with constraint heterogeneity, especially for clients with many samples. In other words, especially for the strongly convex case, an approach following a direct implementation of FedAvg and SPO+ can still yield promising performance even when the downstream optimization problems are noticeably different.

---

## 论文详细总结（自动生成）

这篇论文探讨了在联邦学习环境下，当各参与方（客户端）不仅数据分布不同，且其下游的**优化目标和约束条件也存在异构性**时，如何进行有效的决策聚焦学习（Decision-Focused Learning, DFL）。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究动机**：传统的联邦学习（FL）主要关注预测任务（如最小化均方误差 MSE），但在实际运营管理中，预测往往是为了后续的决策优化（Predict-then-Optimize）。
*   **核心挑战**：在跨组织协作中，不同客户端面临的决策环境不同。例如，不同地区的电力公司虽然都在预测电价，但其成本结构（目标函数）和电网容量（可行域约束）各异。
*   **核心问题**：当客户端的优化问题（目标和约束）不一致时，通过联邦学习汇集数据带来的“统计增益”是否能抵消由于模型不匹配带来的“异构性惩罚”？

### 2. 论文提出的方法论
*   **核心框架（DFFL）**：提出了决策聚焦联邦学习框架，采用 **SPO+（Smart Predict-then-Optimize）** 代理损失函数来训练预测模型，使其直接优化最终的决策质量。
*   **异构性界限推导**：
    *   利用**支撑函数（Support Function）**表示可行域，将异构性拆解为：
        1.  **目标偏移（Objective Shift）**：通过代价向量之间的范数距离衡量。
        2.  **可行集偏移（Feasible-set Shift）**：通过形状距离（Shape Distance，一种平移无关的豪斯多夫距离）衡量。
    *   **强凸性优化**：证明了如果可行域是强凸的（而非多面体），由于优化器具有更好的稳定性，其异构性界限会更紧致。
*   **联邦收益准则（Federation Gain）**：定义了一个启发式规则：当“统计复杂度的降低（数据量增加带来的好处）”大于“客户端与混合分布之间的差异（异构性惩罚）”时，客户端应选择参加联邦学习。

### 3. 实验设计
*   **实验场景**：
    1.  **合成数据实验**：
        *   **分数背包问题（多面体约束）**：代表一般的线性规划场景。
        *   **熵约束投资组合问题（强凸约束）**：用于验证强凸性带来的鲁棒性。
    2.  **真实案例研究**：使用 **PJM 电力市场** 的历史电价和天气数据，模拟 19 个区域代理在不同电价和需求约束下的购电调度决策。
*   **Benchmark 与对比方法**：
    *   **Local 模型**：每个客户端仅使用本地数据训练。
    *   **Federated 模型**：使用 FedAvg 算法在所有客户端数据上训练全局模型。
*   **变量控制**：调节目标异构性水平、约束异构性水平以及数据不平衡程度（数据量多 vs. 少）。

### 4. 资源与算力
*   **硬件环境**：使用了 Auburn 大学 Easley 集群的单 CPU 计算节点。
*   **配置细节**：分配了 **40 个 CPU 核心**和 **60 GB 内存**。
*   **训练时长**：总运行时间约为 **20 小时**（折合 800 核小时）。
*   **说明**：论文明确指出实验是基于 CPU 运行的，未使用 GPU，这可能与所采用的浅层神经网络和特定优化问题的规模有关。

### 5. 实验数量与充分性
*   **实验规模**：
    *   设置了 20 个客户端。
    *   涵盖了 4 种异构性水平（0 到 1.0）、4 种多项式阶数（控制非线性）、2 种噪声水平。
    *   每个配置重复 5 个随机种子。
*   **充分性评价**：实验设计非常系统，不仅涵盖了理论推导中的两种几何性质（多面体 vs. 强凸），还考虑了现实中常见的数据不平衡问题。通过真实电力市场数据的案例研究，进一步增强了结论的说服力。

### 6. 主要结论与发现
*   **几何形状的影响**：在**强凸约束**下，联邦学习表现出极强的鲁棒性，即使目标和约束有显著差异，大多数客户端仍能获益。而在**多面体约束**下，性能对约束异构性非常敏感。
*   **数据量的权衡**：数据量较少的客户端几乎总是能从联邦中获益；而数据量充足的客户端在异构性较高时，使用本地模型反而更好。
*   **决策准则的有效性**：提出的“联邦收益准则”在强凸场景下预测准确率较高（约 76%），但在多面体场景下预测较难（约 50%）。

### 7. 优点
*   **理论与实践结合**：首次将决策聚焦学习（DFL）扩展到联邦学习领域，并给出了严谨的数学界限。
*   **可解释性强**：提出的异构性度量（范数距离和形状距离）具有直观的物理意义。
*   **鲁棒性发现**：揭示了强凸性在缓解联邦学习异构性冲突中的关键作用，为实际应用提供了指导。

### 8. 不足与局限
*   **模型限制**：主要针对线性目标函数，对于非线性目标函数的泛化能力尚未讨论。
*   **算法单一**：实验主要基于基础的 FedAvg，未对比更先进的个性化联邦学习（Personalized FL）算法。
*   **Rademacher 复杂度估计**：在计算联邦收益准则时，对 Rademacher 复杂度的估计采用了启发式方法，可能存在一定偏差。
*   **应用范围**：虽然在电力市场表现良好，但在约束条件极端复杂或非凸的场景下，该理论界限可能过于保守。

（完）
