---
title: Prototype-Regularized Federated Learning for Cross-Domain Aspect Sentiment Triplet Extraction
title_zh: 面向跨域方面情感三元组抽取的原型正则化联邦学习
authors: "Zongming Cai, Jianhang Tang, Zhenyong Zhang, Jinghui Qin, Kebing Jin, Hankz Hankui Zhuo"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09123v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 具有隐私约束的分布式客户端联邦学习框架
tldr: 针对跨域方面情感三元组抽取（ASTE）中数据孤岛和隐私保护问题，本文提出了一种基于原型的正则化联邦学习框架PCD-SpanProto。该框架通过交换类级别原型而非模型参数，结合加权性能感知聚合策略和对比正则化模块，有效捕捉跨域共有特征并提升类间区分度，在降低通信成本的同时显著提升了模型性能。
selection_source: fresh_fetch
motivation: 现有的ASTE方法通常在孤立的数据集上训练，难以在保护隐私的前提下捕捉跨领域共享的特征表示。
method: 提出PCD-SpanProto框架，通过交换类级别原型并引入加权性能感知聚合与对比正则化来优化全局原型。
result: 在四个ASTE数据集上的实验证明，该方法在提升抽取准确率的同时，有效降低了联邦学习的通信成本。
conclusion: 基于原型的联邦学习框架能有效实现跨域知识迁移，解决了异构领域下的情感三元组抽取难题。
---

## 摘要
方面情感三元组抽取 (ASTE) 旨在从句子中提取出由方面项、观点项和情感极性组成的所有情感三元组。现有方法通常在孤立的单个数据集上进行训练，无法共同捕捉跨领域共享的通用特征表示。此外，数据隐私限制阻碍了数据的集中聚合。为了应对这些挑战，我们提出了基于原型的跨域跨度原型抽取 (PCD-SpanProto)，这是一种原型正则化的联邦学习框架，允许分布式客户端交换类级原型而非完整的模型参数。具体而言，我们设计了一种加权性能感知聚合策略和对比正则化模块，以在领域异构性下改进全局原型，并提升各客户端类内紧凑性和类间可分性。在四个 ASTE 数据集上的广泛实验表明，我们的方法优于基准模型并降低了通信成本，验证了基于原型的跨域知识迁移的有效性。

## Abstract
Aspect Sentiment Triplet Extraction (ASTE) aims to extract all sentiment triplets of aspect terms, opinion terms, and sentiment polarities from a sentence. Existing methods are typically trained on individual datasets in isolation, failing to jointly capture the common feature representations shared across domains. Moreover, data privacy constraints prevent centralized data aggregation. To address these challenges, we propose Prototype-based Cross-Domain Span Prototype extraction (PCD-SpanProto), a prototype-regularized federated learning framework to enable distributed clients to exchange class-level prototypes instead of full model parameters. Specifically, we design a weighted performance-aware aggregation strategy and a contrastive regularization module to improve the global prototype under domain heterogeneity and the promotion between intra-class compactness and inter-class separability across clients. Extensive experiments on four ASTE datasets demonstrate that our method outperforms baselines and reduces communication costs, validating the effectiveness of prototype-based cross-domain knowledge transfer.

---

## 论文详细总结（自动生成）

这篇论文针对跨域方面情感三元组抽取（ASTE）任务，提出了一种结合联邦学习与原型学习的新框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保护数据隐私的前提下，利用分布在不同领域（如餐饮、电子产品）的异构数据来提升方面情感三元组（方面项、观点项、情感极性）的抽取性能。
*   **研究背景**：
    *   **数据孤岛与隐私**：不同领域的数据通常由不同组织持有，由于隐私政策无法直接合并。
    *   **领域异构性**：不同领域的情感表达模式不同（非独立同分布，Non-IID），直接合并数据可能引入噪声。
    *   **通信瓶颈**：传统的联邦学习（如 FedAvg）需要频繁传输庞大的模型参数（如 BERT），在带宽受限场景下难以应用。

### 2. 方法论（PCD-SpanProto）
核心思想是**“交换原型而非参数”**，通过类级别的特征表示（原型）来实现跨域知识迁移。
*   **基础模型**：采用基于跨度（Span）的 STAGE 模型作为客户端本地抽取器。
*   **跨度标记方案**：设计了一个三维标记方案 $(t_a, t_o, t_s)$，将每个候选跨度分类为方面、观点或情感角色，并据此构建 8 类语义原型。
*   **本地原型构建**：客户端计算每类跨度表示的平均值，并使用动量更新机制（Momentum Update）来平滑训练过程中的特征波动。
*   **对比正则化（Contrastive Regularization）**：
    *   **对齐损失（Alignment Loss）**：拉近本地跨度表示与其对应的全局原型之间的距离。
    *   **分离损失（Separation Loss）**：推开跨度表示与其他无关类别的全局原型。
*   **性能感知聚合策略**：服务器不再简单平均各客户端原型，而是根据客户端在验证集上的 F1 分数进行加权聚合，性能越好的客户端对全局原型的贡献越大。

### 3. 实验设计
*   **数据集**：使用了 ASTE-Data-V2 标准基准，包含 14Lap（笔记本领域）以及 14Res、15Res、16Res（餐饮领域）。
*   **对比方法**：
    *   **流水线方法**：Peng-two-stage, BMRC, ASTE-RL 等。
    *   **端到端方法**：GTS-BERT, Span-ASTE, STAGE, S&T, MGEGCN 等。
*   **评估指标**：三元组级别的精确率（Precision）、召回率（Recall）和 F1 值。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 4090 GPU** 的服务器上运行。
*   **训练设置**：
    *   使用 BERT-base 作为编码器。
    *   联邦学习共进行 50 轮，每轮客户端本地训练 5 个 Epoch。
    *   学习率：BERT 部分为 2e-5，分类器部分为 1e-3。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括主性能对比、跨域泛化性测试、通信效率分析、消融实验（聚合策略）、参数敏感性分析以及可视化分析（t-SNE）。
*   **充分性评价**：实验设计非常充分且客观。作者特别进行了**数据去重**处理，防止了跨数据集训练时的标签泄露；同时对比了“数据合并训练”与“独立训练”等多种策略，有力证明了联邦原型学习的优越性。

### 6. 主要结论与发现
*   **性能提升**：PCD-SpanProto 在四个数据集上均优于基础模型 STAGE，且在多数指标上达到了 SOTA 水平。
*   **通信效率**：相比传输全模型参数（约 1.1 亿参数），传输原型仅需 3,200 个参数，**通信开销降低了约 34,000 倍**。
*   **跨域有效性**：联邦学习后的客户端不仅在本地任务上表现更好，在未见过的其他领域数据上也表现出更强的泛化能力。
*   **异构性处理**：F1 加权聚合策略能有效抑制性能较差客户端带来的噪声，保护全局原型的质量。

### 7. 优点与亮点
*   **极低通信成本**：通过原型交换解决了大模型在联邦学习中的传输难题。
*   **隐私与性能兼顾**：在不暴露原始数据的情况下，实现了比“数据直接合并”更好的效果（避免了跨域噪声干扰）。
*   **结构化建模**：将原型学习成功引入到复杂的 ASTE 结构化抽取任务中，而非简单的分类任务。

### 8. 不足与局限
*   **领域覆盖有限**：实验主要集中在餐饮和笔记本两个大类，对于更广泛、差异更大的领域（如医疗、法律）的有效性尚待验证。
*   **原型粒度**：目前的类级别原型可能过于粗犷，无法捕捉同一语义类别下更细微的语言学变化。
*   **模型依赖**：虽然减少了通信开销，但客户端本地仍需运行完整的 BERT 模型，对客户端本地算力仍有一定要求。

（完）
