---
title: Communication-Efficient and Robust Multi-Modal Federated Learning via Latent-Space Consensus
title_zh: 基于潜在空间共识的通信高效且鲁棒的多模态联邦学习
authors: "Mohamed Badi, Chaouki Ben Issaid, Mehdi Bennis"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19067v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 通过潜空间共识保护隐私的多模态联邦学习
tldr: 针对多模态联邦学习中客户端模态异构、架构差异及通信开销大的挑战，本文提出CoMFed框架。该框架利用可学习投影矩阵生成压缩的潜在表示，并通过潜在空间正则化器实现跨客户端的特征对齐，增强了模型对异常值的鲁棒性。在人体活动识别基准上的实验证明，CoMFed在保持极低通信开销的同时，实现了具有竞争力的分类准确率。
selection_source: fresh_fetch
motivation: 解决多模态联邦学习中由于客户端模态异构和模型架构差异导致的特征对齐困难及高通信成本问题。
method: 提出CoMFed框架，通过可学习投影矩阵压缩潜在表示，并引入潜在空间正则化器促进跨模态一致性。
result: 在人体活动识别任务中，该方法以极低的通信开销达到了与现有技术相当的准确率。
conclusion: CoMFed为资源受限环境下的鲁棒、高效多模态联邦学习提供了一种有效的潜在空间对齐方案。
---

## 摘要
联邦学习 (FL) 能够在不共享原始数据的情况下，在分布式设备之间实现协作模型训练，但将联邦学习应用于多模态场景会带来重大挑战。客户端通常拥有异构的模态和模型架构，这使得在保护隐私和最小化通信成本的同时，难以高效地对齐特征空间。为了解决这一问题，我们引入了 CoMFed，这是一个通信高效的多模态联邦学习框架，它使用可学习的投影矩阵来生成压缩的潜在表示。潜在空间正则化项在客户端之间对齐这些表示，从而提高了跨模态一致性和对离群值的鲁棒性。在人类活动识别基准测试上的实验表明，CoMFed 以极小的开销实现了具有竞争力的准确率。

## Abstract
Federated learning (FL) enables collaborative model training across distributed devices without sharing raw data, but applying FL to multi-modal settings introduces significant challenges. Clients typically possess heterogeneous modalities and model architectures, making it difficult to align feature spaces efficiently while preserving privacy and minimizing communication costs. To address this, we introduce CoMFed, a Communication-Efficient Multi-Modal Federated Learning framework that uses learnable projection matrices to generate compressed latent representations. A latent-space regularizer aligns these representations across clients, improving cross-modal consistency and robustness to outliers. Experiments on human activity recognition benchmarks show that CoMFed achieves competitive accuracy with minimal overhead.

---

## 论文详细总结（自动生成）

### 论文总结：基于潜在空间共识的通信高效且鲁棒的多模态联邦学习 (CoMFed)

#### 1. 研究动机与核心问题
*   **核心问题**：传统的联邦学习（FL）假设客户端拥有相同的模型架构和数据模态，但在现实的多模态场景（如物联网、边缘计算）中，客户端往往存在**模态异构**（有的有图像，有的只有传感器）和**架构异构**（模型参数不兼容）。
*   **研究动机**：
    *   **通信瓶颈**：交换大型多模态模型的梯度或参数成本极高。
    *   **对齐困难**：不同架构的模型参数无法直接聚合。
    *   **现有方案局限**：现有的蒸馏方法（如 FedMD）依赖公共数据集，而这在隐私敏感场景下难以获取；分区训练（如 Harmony）则限制了跨模态的知识迁移。

#### 2. 方法论：CoMFed 框架
*   **核心思想**：不交换模型参数，而是交换**类级别的低维潜在空间统计量**。通过学习一个客户端特有的投影矩阵，将异构的中间特征映射到一个统一的、低维的“共享语言”空间。
*   **关键技术细节**：
    *   **投影矩阵 ($P_i$)**：每个客户端学习一个线性投影矩阵，将本地特征 $v_i$ 映射到 $d$ 维潜在空间 $R^d$（$d$ 远小于特征维度）。
    *   **潜在空间正则化器**：在本地损失函数中加入正则项，惩罚本地投影特征与邻居（或全局）共识表示之间的差异。
    *   **鲁棒共识机制**：
        *   使用**算术平均**（L2范数平方）处理常规对齐。
        *   引入**几何中位数（Geometric Median）**（L2范数）作为距离度量，以增强对离群值或拜占庭（Byzantine）恶意攻击的鲁棒性。
    *   **交替优化算法**：客户端在每一轮中交替更新本地模型权重 $w_i$ 和投影矩阵 $P_i$。
    *   **通信效率**：每轮仅传输每个类别的一个 $d$ 维向量，通信量与模型大小无关。

#### 3. 实验设计
*   **数据集**：
    1.  **USC-HAD**：人体活动识别，包含加速度计（ACC）和陀螺仪（GYR）模态。
    2.  **DeepSense**：毫米波（mmWave）和激光雷达（LiDAR）数据，用于预测信号遮挡持续时间。
*   **实验场景**：设置了模态缺失的情况（如部分客户端仅有单模态，部分有双模态），并采用了异构的编码器架构（CNN、GRU等）。
*   **对比基准（Benchmarks）**：
    *   **Harmony (UniFL)**：基于模态子集的分区联邦学习。
    *   **FedMD**：基于公共数据集蒸馏的异构联邦学习。
    *   **FedIoT**：利用自编码器和公共数据进行分类。
    *   **Centralized**：全数据汇总训练的理想上限。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：提到学习率为 $10^{-3}$，进行了 250 次迭代，CoMFed 的投影维度 $d$ 在实验中分别测试了 8 到 64 维。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同领域（活动识别、无线通信）的真实多模态数据集上进行了验证。
    *   进行了**鲁棒性实验**，模拟了不同数量的拜占庭攻击者。
    *   进行了**消融/参数敏感性实验**，分析了投影维度 $d$ 对准确率和通信成本的影响。
*   **充分性评价**：实验设计较为全面，涵盖了准确率、通信开销、鲁棒性和超参数影响。对比了多种不同技术路线的基准方法，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **高性能与高效率**：CoMFed 在准确率上优于 Harmony 和 FedIoT，且在通信开销上比 Harmony 降低了几个数量级（从 GB 级降至 KB 级）。
*   **无需公共数据**：与 FedMD 不同，CoMFed 在不需要任何公共数据集的情况下实现了更好的平均性能。
*   **强鲁棒性**：采用几何中位数的 CoMFed 变体在存在恶意客户端的情况下，性能下降远小于传统平均方法。
*   **维度权衡**：增加投影维度 $d$ 可以提升准确率，但边际收益会递减，且通信成本线性增加。

#### 7. 优点
*   **架构无关性**：支持完全不同的模型架构和模态组合，具有极强的通用性。
*   **极低通信开销**：通过压缩潜在表示，解决了多模态模型在大规模部署时的带宽瓶颈。
*   **隐私友好**：仅交换类级别的统计量而非原始数据或模型参数，且低维空间天然适合集成差分隐私。
*   **单阶段训练**：无需像蒸馏方法那样进行复杂的多阶段预训练。

#### 8. 不足与局限
*   **投影简单性**：目前仅使用线性投影矩阵，对于极度复杂的特征空间对齐，线性映射可能存在表达能力不足的问题。
*   **类别依赖**：通信开销随类别数量 $|M|$ 增加，在超大规模分类任务（如成千上万类）中，优势可能会减弱。
*   **隐私证明**：虽然讨论了隐私优势，但未提供形式化的隐私保证（如差分隐私的 $\epsilon$ 分析）。
*   **硬件细节缺失**：缺乏实际运行时间的对比，难以评估投影矩阵更新带来的额外计算负担。

（完）
