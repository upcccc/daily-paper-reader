---
title: On the Optimal Number of Grids for Differentially Private Non-Interactive $K$-Means Clustering
title_zh: 关于差分隐私非交互式 K-均值聚类的最优网格数研究
authors: "Gokularam Muthukrishnan, Anshoo Tandon"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26963v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: K-means聚类的差分隐私机制
tldr: 本研究探讨了差分隐私非交互式K-means聚类中网格数量的最优选择问题。针对现有方法依赖经验调参且忽略聚类数量K的影响，本文提出了一种基于最小化K-means目标函数期望偏差上界的网格选择规则。该规则综合考虑了聚类数、数据集规模和隐私预算，为非交互式隐私聚类提供了更具理论依据的离散化策略。实验表明，该方法在严格隐私预算下仍能显著提升聚类精度。
selection_source: fresh_fetch
motivation: 现有的非交互式差分隐私聚类方法在网格划分时缺乏理论指导，且未考虑聚类数量对量化误差与隐私噪声平衡的影响。
method: 通过最小化K-means目标函数期望偏差的上界，推导出一种与聚类数、数据规模和隐私预算相关的精细化网格选择规则。
result: 实验结果显示，该策略在多种隐私预算下均优于现有最先进技术，尤其在隐私保护要求较高时表现出更强的准确性。
conclusion: 科学的网格分辨率选择对提升非交互式隐私聚类性能至关重要，本文提出的规则为该领域提供了更优的离散化标准。
---

## 摘要
差分隐私 K-均值聚类能够在保护个人隐私的同时，发布从数据集中提取的聚类中心。基于隐私化直方图的非交互式聚类技术具有吸引力，因为发布的摘要数据可以重复用于其他下游任务，而不会产生额外的隐私损失。用于离散化数据点的网格数量选择至关重要，因为它直接控制了量化偏差以及为保护隐私而注入的噪声量。广泛采用的策略选择的网格大小与聚类数量无关，且依赖于经验调优。在本研究中，我们重新审视了这一选择，并提出了一种改进的网格大小选择规则，该规则通过最小化 K-均值目标函数期望偏差的上界推导而来，从而为非交互式隐私聚类提供了一种更具原则性的离散化策略。与之前的工作相比，我们的网格分辨率在对聚类数量的依赖性以及随数据集大小和隐私预算的缩放比例方面均有所不同。广泛的数值结果表明，与现有最先进技术相比，即使在严格的隐私预算下，所提出的策略也能实现准确的聚类。

## Abstract
Differentially private $K$-means clustering enables releasing cluster centers derived from a dataset while protecting the privacy of the individuals. Non-interactive clustering techniques based on privatized histograms are attractive because the released data synopsis can be reused for other downstream tasks without additional privacy loss. The choice of the number of grids for discretizing the data points is crucial, as it directly controls the quantization bias and the amount of noise injected to preserve privacy. The widely adopted strategy selects a grid size that is independent of the number of clusters and also relies on empirical tuning. In this work, we revisit this choice and propose a refined grid-size selection rule derived by minimizing an upper bound on the expected deviation in the K-means objective function, leading to a more principled discretization strategy for non-interactive private clustering. Compared to prior work, our grid resolution differs both in its dependence on the number of clusters and in the scaling with dataset size and privacy budget. Extensive numerical results elucidate that the proposed strategy results in accurate clustering compared to the state-of-the-art techniques, even under tight privacy budgets.

---

## 论文详细总结（自动生成）

### 论文总结：关于差分隐私非交互式 $K$-均值聚类的最优网格数研究

#### 1. 核心问题与整体含义（研究动机和背景）
在差分隐私（DP）框架下，$K$-means 聚类主要分为**交互式**（如 DPLloyd，多次迭代消耗隐私预算）和**非交互式**（发布数据摘要，可重复利用）。非交互式方法通常采用“均匀网格”技术：将空间划分为 $M$ 个网格，发布加噪后的直方图，再进行聚类。
*   **核心痛点**：网格数量 $M$ 的选择至关重要。若 $M$ 太小，量化误差（离散化偏差）大；若 $M$ 太大，注入的隐私噪声总量增加。
*   **研究动机**：现有的主流方法（如 EUGkM）选择的 $M$ 与聚类数 $K$ 无关，且依赖经验调参。在严格隐私预算下，EUGkM 选取的网格数甚至可能少于聚类数（$M < K$），导致算法失效。

#### 2. 论文提出的方法论：RUGNIK
论文提出了一种名为 **RUGNIK**（Refined Uniform Gridding for Non-Interactive $K$-means）的精细化网格选择规则。
*   **核心思想**：通过数学推导，最小化 $K$-means 目标函数（组内平方和 WCSS）期望偏差的上界，从而在量化误差和隐私噪声之间取得理论最优平衡。
*   **关键技术细节**：
    *   将总偏差分解为三部分：噪声引起的偏差（$T_1$）、量化引起的交叉项偏差（$T_2$）以及纯量化偏差（$T_3$）。
    *   假设数据在网格内均匀分布，推导出偏差上界函数 $h(m)$。
    *   通过求解 $h'(m)=0$，证明了最优网格数 $M$ 的存在性与唯一性。
*   **核心公式**：推导得出最优网格数 $M$ 的缩放规律为：
    $$M = \Theta \left( (N \epsilon)^{\frac{2d}{4+d}} K^{\frac{4}{4+d}} \right)$$
    其中 $N$ 是样本量，$\epsilon$ 是隐私预算，$d$ 是维度，$K$ 是聚类数。这与以往方法显著不同，明确引入了 $K$ 的影响。

#### 3. 实验设计
*   **数据集/场景**：使用合成数据集，模拟不同维度（$d=2, 3$）、不同样本量（$N=100$ 到 $1600$）以及不同聚类数（$K=2, 4, 8$）的场景。
*   **对比方法（Benchmark）**：
    1.  **IBM diffprivlib (DPLloyd)**：交互式 DP 聚类的行业标准实现。
    2.  **Google Coreset-based Clustering (GCC)**：基于核心集构造的先进非交互式方法。
    3.  **EUGkM**：现有的基于均匀网格的非交互式代表性算法。
    4.  **无隐私版本**：作为性能上限参考。
*   **评估指标**：WCSS（越小表示聚类越准确）。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU/CPU 型号）、集群规模或具体的训练/运行耗时。由于该算法基于直方图和加权 Lloyd 算法，计算复杂度通常较低，预计在普通工作站即可完成。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 $d=2$ 和 $d=3$ 两个维度下，针对 3 种不同的 $K$ 值和 3 种不同的 $N$ 值，分别在 6 个隐私预算等级（$\epsilon \in [0.1, 1.0]$）上进行了详尽测试。
*   **充分性评价**：实验设计较为充分，涵盖了从极高隐私需求到中等隐私需求的区间。通过对比不同类型的算法（交互式 vs 非交互式，网格化 vs 核心集），验证了 RUGNIK 在各种配置下的稳健性。对比实验客观地展示了在小 $\epsilon$ 场景下传统方法的失效问题。

#### 6. 主要结论与发现
*   **性能优越性**：RUGNIK 在所有测试配置下均优于 IBM、Google 的方案以及 EUGkM。
*   **解决退化问题**：在强隐私保护（小 $\epsilon$）下，EUGkM 常因网格太粗（$M < K$）而失效，而 RUGNIK 通过考虑 $K$ 的依赖性，确保了网格分辨率足以区分各个簇。
*   **参数敏感性**：最优网格数 $M$ 与数据范围 $r$ 无关，这简化了实际应用中的参数设置。

#### 7. 优点
*   **理论完备性**：不再依赖经验调参，而是基于目标函数偏差最小化的原则推导参数，具有更强的数学支撑。
*   **非交互式优势**：生成的隐私直方图摘要可以多次复用，且计算效率高，适合大规模数据集。
*   **高隐私表现**：在隐私预算非常有限的情况下，依然能保持较高的聚类精度。

#### 8. 不足与局限
*   **数据分布假设**：推导过程中假设数据在网格内接近均匀分布，对于极度偏斜或具有复杂流形结构的数据，该规则的有效性可能受限。
*   **维度灾难**：虽然讨论了 $d=2, 3$，但均匀网格方法在超高维空间下会面临网格数量爆炸的问题，论文未探讨高维情况下的应对策略。
*   **噪声机制单一**：目前仅使用了 Laplace 机制，未探讨离散 Laplace 或高斯机制在特定场景下的潜在增益。

（完）
