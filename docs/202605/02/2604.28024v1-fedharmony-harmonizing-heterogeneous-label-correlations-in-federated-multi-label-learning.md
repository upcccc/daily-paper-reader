---
title: "FedHarmony: Harmonizing Heterogeneous Label Correlations in Federated Multi-Label Learning"
title_zh: FedHarmony：协调联邦多标签学习中的异构标签相关性
authors: "Zhiqiang Kou, Junxiang Wu, Wenke Huang, Wenwen He, Ming-Kun Xie, Changwei Wang, Yuheng Jia, Di Jiang, Yang Liu, Xin Geng, Qiang Yang"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28024v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 隐私约束下的联邦多标签学习
tldr: 本研究针对联邦多标签学习中由于数据异构导致的“标签相关性漂移”问题，提出了FedHarmony框架。该框架通过引入共识相关性作为全局教师来纠正本地偏差，并在聚合阶段根据数据量和相关性质量动态调整权重。此外，研究还开发了加速优化算法，在理论上保证了更快的收敛速度。实验证明，FedHarmony在多个真实数据集上均优于现有最先进方法，有效解决了异构分布下的标签建模难题。
selection_source: fresh_fetch
motivation: 旨在解决联邦多标签学习中因客户端标签分布不均导致的本地标签相关性偏离全局结构（即标签相关性漂移）的问题。
method: 提出FedHarmony框架，利用共识相关性纠正本地偏差，并根据数据规模和相关性质量进行加权聚合，同时配合加速优化算法。
result: 在真实世界的联邦多标签数据集上，FedHarmony在保持高精度的同时实现了更快的收敛，性能显著优于现有最先进方法。
conclusion: FedHarmony通过有效协调异构标签相关性，为联邦多标签学习提供了一种高效且鲁棒的协同训练方案。
---

## 摘要
联邦多标签学习是一种分布式范式，其中多个客户端拥有异构的多标签数据，并在不共享原始数据的情况下，在隐私约束下进行协作学习。然而，在异构分布下建模标签相关性仍然具有挑战性。由于客户端特定的标签空间和变化的共现模式，单个客户端学习到的相关性不可避免地偏离全局结构，我们将这一现象称为标签相关性漂移。为了解决这个问题，我们提出了 FedHarmony，这是一个协调跨客户端异构标签相关性的框架。它引入了共识相关性，捕捉其他客户端之间的一致性，并作为全局教师来纠正有偏差的本地估计。在聚合过程中，FedHarmony 根据数据量和相关性质量对每个客户端进行评估，并相应地分配权重。此外，我们为 FedHarmony 开发了一种加速优化算法，并在理论上证明了其在不牺牲准确性的情况下具有更快的收敛速度。在真实世界联邦多标签数据集上的实验表明，FedHarmony 一贯优于现有最先进的方法。

## Abstract
Federated Multi-Label Learning is a distributed paradigm where multiple clients possess heterogeneous multi-label data and perform collaborative learning under privacy constraints without sharing raw data. However, modeling label correlations under heterogeneous distributions remains challenging. Due to client-specific label spaces and varying co-occurrence patterns, correlations learned by individual clients inevitably deviate from the global structure, a phenomenon we term label correlation drift. To address this, we propose FedHarmony, a framework that harmonizes heterogeneous label correlations across clients. It introduces consensus correlation, capturing agreement among other clients and serving as a global teacher to correct biased local estimates. During aggregation, FedHarmony evaluates each client by both data size and correlation quality, assigning weights accordingly. Moreover, we develop an accelerated optimization algorithm for FedHarmony and theoretically establish faster convergence without sacrificing accuracy. Experiments on real-world federated multi-label datasets show that FedHarmony consistently outperforms state-of-the-art methods.

---

## 论文详细总结（自动生成）

这篇论文提出了 **FedHarmony** 框架，旨在解决联邦多标签学习（FedMLL）中由于数据异构性导致的“标签相关性漂移”问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在联邦学习环境下，不同客户端拥有的多标签数据分布高度异构（Non-IID）。这导致每个客户端学习到的标签共现模式（相关性）与全局真实分布存在偏差，即**标签相关性漂移（Label Correlation Drift）**。
*   **研究动机**：现有的联邦学习方法（如 FedAvg）主要通过参数平均来聚合模型，忽略了标签之间复杂的语义关联。如果某个客户端数据量大但标签相关性建模质量差，简单的加权聚合会损害全局模型的性能。因此，需要一种能协调异构相关性并识别高质量客户端的机制。

### 2. 方法论：FedHarmony 框架
FedHarmony 的核心思想是利用“群体共识”来指导本地学习，并根据相关性建模质量进行智能聚合。
*   **共识相关性教师（Consensus Correlation Teacher）**：
    *   每个客户端计算本地标签的边际概率和联合概率，生成本地相关性矩阵。
    *   服务器收集这些矩阵，为每个客户端生成一个“留一法”共识矩阵（即除该客户端外所有其他客户端的平均相关性），作为全局教师信号。
*   **共识引导纠正（Consensus-Guided Correction）**：
    *   在本地训练中，引入相关性对齐损失函数，强制本地学习到的标签关联向全局共识对齐，纠正本地偏差。
*   **分块优化算法（Block-wise Optimization）**：
    *   利用谱聚类将标签空间划分为多个子块（簇），仅对高相关的标签对进行对齐。
    *   **理论证明**：论文证明了这种分块对齐在保持精度的同时，能显著提高收敛速度（线性收敛率更高）。
*   **相关性感知聚合（Correlation-Aware Aggregation）**：
    *   聚合权重不再仅取决于数据量，还取决于客户端的“结构差异得分”（即本地相关性与共识的匹配度）。
    *   训练初期侧重数据量，后期逐渐转向侧重相关性质量。

### 3. 实验设计
*   **数据集**：
    *   **FLAIR**：包含 51,414 个用户、42.9 万张图像的真实联邦数据集。
    *   **MS-COCO**：80 个对象类别，通过 Dirichlet 分布人工构造异构划分。
    *   **PASCAL VOC 2007/2012**：20 个类别，同样构造为 Non-IID 场景。
*   **基准方法（Benchmarks）**：
    *   通用联邦优化方法：FedAvg, FedProx, FedNova。
    *   几何/曲率感知方法：FedCurv, SphereFed。
    *   多标签/特征增强方法：FedLGT, FedRDN。
*   **评价指标**：采用 mAP、O-mAP、CP、CR、CF1 等 8 项多标签学习主流指标。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **8 台 NVIDIA RTX 4090 GPU**。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：主干网络采用 ViT-B/16，本地训练 5 个 epoch，通信轮数 T=50。
*   **效率提升**：文中明确提到，分块优化（B-OPT）相比不分块的方法，在 FLAIR 数据集上减少了约 **28.3%** 的累计训练时间，在 VOC 上减少了 **31.7%**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1,000 到 2,500 个客户端的大规模扩展性测试。
*   **消融实验**：详细验证了专家引导损失（ECL）、相关性感知聚合（CAA）和分块聚类（C）各自的贡献。
*   **鲁棒性测试**：通过改变 Dirichlet 参数 $\gamma$（控制 Non-IID 程度）和客户端参与率，验证了模型在极端数据偏斜下的稳定性。
*   **客观性**：使用了 Wilcoxon 符号秩检验进行统计显著性分析，证明了分块优化在不损失精度的情况下提升了效率，实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能领先**：FedHarmony 在所有数据集上均显著优于 SOTA 方法。在 FLAIR 数据集上，mAP 提升超过 **10%**。
*   **纠偏能力**：定性分析显示，FedHarmony 能有效抑制伪标签（Spurious labels）并恢复缺失的语义关联。
*   **收敛效率**：提出的分块优化策略在理论和实验上均证明了其在处理大规模标签空间时的计算优势。
*   **稳定性**：在高度碎片化（客户端多、本地数据极少）的场景下，该方法依然能保持语义一致性。

### 7. 优点
*   **理论支撑强**：不仅提出了启发式算法，还为分块优化提供了收敛性证明。
*   **解决痛点**：直接针对联邦多标签学习中最核心的“相关性建模”难题，而非简单的参数对齐。
*   **实用性高**：通过分块策略降低了计算开销，使其更易于在资源受限的客户端部署。

### 8. 不足与局限
*   **隐私风险**：虽然不共享原始数据，但上传标签相关性矩阵（C×C 维度）可能在极端情况下泄露某些标签分布统计信息（尽管文中未深入讨论差分隐私保护）。
*   **超参数敏感性**：聚合权重中的转换周期 $T_0$ 和聚类数量 $G$ 可能需要针对不同数据集进行精细调优。
*   **标签空间假设**：假设所有客户端共享相同的全局标签空间定义，未讨论标签空间部分重叠或完全不同的极端开放场景。

（完）
