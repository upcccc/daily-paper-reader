---
title: "Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition"
title_zh: 通过联邦学习优化多语言大语言模型：客户端语言构成的研究
authors: "Aleix Sant, Jordi Luque, Carlos Escolano"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24242v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 具有客户端特定机制的大语言模型联邦学习
tldr: 本研究探讨了在联邦学习环境下优化多语言大语言模型的策略。针对客户端语言分布不均和资源差异挑战，扩展了FederatedScope-LLM框架，并提出局部动态早停机制（LDES-FL）以提升训练效率。通过实验分析了客户端语言构成对模型质量、公平性和成本的影响，发现增加客户端内的语言多样性能显著增强全局模型性能，尤其对低资源语言增益最大。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中多语言大语言模型面临的客户端语言分布异构及资源不平衡导致的性能与效率难题。
method: 扩展了FederatedScope-LLM框架并引入局部动态早停机制（LDES-FL），允许客户端根据本地验证表现自主暂停或恢复训练。
result: 增加客户端内部的语言多样性有助于构建更强大且公平的全局模型，并能显著缩小与集中式微调的差距，尽管这需要更多的优化步骤。
conclusion: 客户端语言构成是多语言联邦学习中的核心设计变量，决定了模型在性能、公平性和训练效率之间的权衡。
---

## 摘要
多语言环境下大语言模型（LLMs）的联邦学习（FL）面临着重大挑战，这些挑战源于客户端之间异构的语言分布以及语言资源可用性的差异。为了应对这些挑战，我们扩展了 FederatedScope-LLM 框架，以支持 LLMs 的多语言指令微调实验。我们还引入了一种新型的客户端特定早停机制——局部动态早停（LDES-FL），该机制允许客户端根据客户端侧的验证性能暂停和恢复本地训练，从而提高训练效率和可持续性。通过一系列实验，我们研究了客户端语言构成（从完全单语言到日益多样化的多语言客户端）如何影响多语言质量、公平性和训练成本。单语言本地微调对于单语言专业化仍然最为有效，而联邦训练则更适合学习单一的平衡多语言模型。在联邦学习中，增加客户端内部的多语言性可以产生更强大、更公平的全局模型，缩小与集中式多语言微调的差距，并为低资源语言带来最大的收益，尽管这以更多的优化步骤为代价。总之，我们的研究结果表明，客户端语言构成是多语言联邦学习中的一个关键设计变量，它塑造了性能、公平性和效率。

## Abstract
Federated Learning (FL) of Large Language Models (LLMs) in multilingual environments presents significant challenges stemming from heterogeneous language distributions across clients and disparities in language resource availability. To address these challenges, we extended the FederatedScope-LLM framework to support multilingual instruction-tuning experiments with LLMs. We also introduced a novel client-specific early stopping mechanism, Local Dynamic Early Stopping (LDES-FL), which allows clients to pause and resume local training based on client-side validation performance, enhancing training efficiency and sustainability. Through a series of experiments, we studied how client language composition - from fully monolingual to increasingly multilingual clients - affects multilingual quality, fairness and training cost. Monolingual local fine-tuning remains the most effective for single-language specialization, whereas federated training is better suited to learning a single balanced multilingual model. In FL, increasing within-client multilinguality leads to stronger and fairer global models, narrows the gap to centralized multilingual fine-tuning, and yields the largest gains for lower-resource languages, albeit at the cost of more optimization steps. Overall, our results identify client language composition as a key design variable in multilingual FL, shaping performance, fairness and efficiency

---

## 论文详细总结（自动生成）

这篇论文《Optimizing Multilingual LLMs via Federated Learning: A Study of Client Language Composition》深入探讨了在联邦学习（FL）框架下，客户端数据的语言构成如何影响多语言大语言模型（LLM）的微调效果。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在联邦学习中，客户端之间的数据通常是高度异构的（非独立同分布，Non-IID）。在多语言场景下，这种异构性表现为每个客户端拥有的语言种类和比例不同。论文旨在研究**客户端内部的语言多样性（从纯单语言到混合多语言）如何影响全局模型的性能、公平性以及训练效率**。
*   **研究背景**：多语言 LLM 的联邦微调面临通信开销大、计算资源受限以及由于“客户端漂移（Client Drift）”导致的收敛困难等挑战。

### 2. 方法论
*   **核心思想**：通过系统性地改变客户端本地数据集的语言组成比例，观察其对联邦优化过程的影响。
*   **关键技术细节**：
    *   **框架扩展**：基于 `FederatedScope-LLM` 框架进行了多语言适配，支持语言感知的样本处理和多语言数据流水线。
    *   **局部动态早停机制 (LDES-FL)**：这是本文提出的创新算法。不同于传统的全局早停，LDES-FL 允许客户端根据其私有验证集的损失函数自主决定暂停或恢复训练。如果全局模型在某轮更新后改善了客户端的本地验证表现，已停止的客户端可以重新加入训练。
    *   **参数高效微调 (PEFT)**：采用 LoRA（低秩适配）技术，仅更新极少量的参数（秩 r=16），以降低通信成本和计算压力。
    *   **聚合算法**：使用标准的 FedAvg 算法进行模型聚合。

### 3. 实验设计
*   **数据集**：使用了经过清洗的多语言 Alpaca 数据集，涵盖 8 种语言：英语 (en)、西班牙语 (es)、德语 (de)、加泰罗尼亚语 (ca)、丹麦语 (da)、塞尔维亚语 (sr)、克罗地亚语 (hr) 和巴斯克语 (eu)。
*   **实验场景（Benchmark）**：
    *   **100% Mono**：每个客户端仅含一种语言（极端 Non-IID）。
    *   **混合比例**：设置了 85%、70%、50%、30%、15% 等不同单语言占比的场景，占比越低，客户端内部多语言性越强（越趋向 IID）。
*   **对比方法（Baselines）**：
    *   **Base Model**：原始预训练模型（salamandra-2b-instruct）。
    *   **Local FT (Monolingual)**：在单个语言上进行本地集中式微调。
    *   **Local FT (Multilingual)**：在所有语言的并集上进行集中式微调（作为性能上限）。
*   **评估指标**：ROUGE-L 和 BERTScore ($F_{bert}$)，并使用标准差 ($\sigma$) 衡量多语言公平性。

### 4. 资源与算力
*   **模型规模**：使用 `salamandra-2b-instruct`（20亿参数）。
*   **训练配置**：微批次大小为 2，梯度累积 16 步，每轮进行 10 次优化器更新。
*   **算力说明**：论文**未明确说明具体的 GPU 型号和数量**，但详细记录了“归一化优化步骤（Normalized Optim. Steps）”，用于对比不同配置下的计算成本。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 种不同的联邦语言构成配置、8 组单语言本地微调实验以及 1 组集中式多语言微调实验。
*   **充分性与客观性**：
    *   实验设计了从极端 Non-IID 到接近 IID 的完整频谱，具有较强的系统性。
    *   引入了自助法（Bootstrap test）进行显著性检验（p-value < 0.05），确保结果的统计可靠性。
    *   控制了每个客户端的总样本量一致，排除了数据量差异带来的干扰。

### 6. 主要结论与发现
*   **语言构成的权衡**：增加客户端内部的多语言比例能显著提升全局模型的平均性能，并提高多语言公平性（降低不同语言间的表现差异）。
*   **低资源语言获益最大**：随着客户端多语言性的增加，低资源语言（如巴斯克语、塞尔维亚语）的性能提升幅度远高于高资源语言。
*   **训练成本**：更趋向 IID（多语言混合）的设置虽然效果更好，但需要更多的优化步骤才能收敛，而单语言设置虽然收敛快，但容易陷入局部最优。
*   **联邦 vs 本地**：单语言本地微调在特定语言上表现最强，但联邦学习在构建通用的平衡多语言模型方面更具优势，且总计算成本远低于为每种语言单独训练模型。

### 7. 优点
*   **LDES-FL 机制**：提出的动态早停算法不仅提高了训练效率，还通过“暂停-恢复”逻辑很好地适应了联邦学习中的异构动态。
*   **系统性研究**：填补了多语言环境下客户端数据分布对联邦 LLM 微调影响的研究空白。
*   **实用性**：证明了即使在隐私受限的联邦环境下，通过合理设计客户端数据构成，也能接近集中式训练的效果。

### 8. 不足与局限
*   **数据集偏差**：实验使用的多语言 Alpaca 是通过翻译生成的，并非严格的平行语料，可能存在翻译质量导致的偏差或模板重叠。
*   **语言覆盖**：虽然涵盖了 8 种语言，但主要集中在欧洲语系，对于语系差异更大的全球语言（如亚洲语系）的泛化性有待验证。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于其他研究者评估实际部署的物理成本。

（完）
