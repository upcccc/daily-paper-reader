Title: Semantic-based Distributed Learning for Diverse and Discriminative Representations

URL Source: https://arxiv.org/pdf/2604.18237v1

Published Time: Tue, 21 Apr 2026 02:56:31 GMT

Number of Pages: 16

Markdown Content:
> 1

# Semantic-based Distributed Learning for Diverse and Discriminative Representations 

Zhuojun Tian, Chaouki Ben Issaid, and Mehdi Bennis 

Abstract —In large-scale distributed scenarios, increasingly complex tasks demand more intelligent collaboration across networks, requiring the joint extraction of structural represen-tations from data samples. However, conventional task-specific approaches often result in nonstructural embeddings, leading to collapsed variability among data samples within the same class, particularly in classification tasks. To address this issue and fully leverage the intrinsic structure of data for downstream applications, we propose a novel distributed learning framework that ensures both diverse and discriminative representations. For independent and identically distributed (i.i.d.) data, we reformu-late and decouple the global optimization function by introducing constraints on representation variance. The update rules are then derived and simplified using a primal-dual approach. For non-i.i.d. data distributions, we tackle the problem by clustering and virtually replicating nodes, allowing model updates within each cluster using block coordinate descent. In both cases, the resulting optimal solutions are theoretically proven to maintain discriminative and diverse properties, with a guaranteed con-vergence for i.i.d. conditions. Additionally, semantic information from representations is shared among nodes, reducing the need for common neural network architectures. Finally, extensive simulations on MNIST, CIFAR-10 and CIFAR-100 confirm the effectiveness of the proposed algorithms in capturing global structural representations. 

pdf 

Index Terms —Distributed learning, representation learning, semantic communication, maximal coding rate reduction. 

I. I NTRODUCTION 

The rapid increase in devices with expanding storage and hardware capabilities has facilitated the efficient collection and processing of large-scale datasets. With the remarkable success of deep neural networks, distributed learning has gained signif-icant attention for handling large-scale collaborative scenarios [1]. As the collected data samples embed more complicated structures, the application scenarios and the user needs become various, there arise more challenging processing tasks. These intricate tasks require devices to collaboratively extract infor-mation from distinct datasets across multiple sources while preserving the intrinsic structures of the data to best facilitate subsequent tasks [2]. In conventional training algorithms for over-parameterized deep networks, particularly in classification tasks, the learned 

Z. Tian (zhuojun@kth.se) was with the Center for Wireless Commu-nications, University of Oulu, Oulu, Finland and now is with the Divi-sion of Information Science and Engineering, KTH Royal of Technology, Stockholm, Sweden. C. B. Issaid and M. Bennis are with the Center for Wireless Communications, University of Oulu, Oulu 90014, Finland. Email: 

{chaouki.benissaid, mehdi.bennis }@oulu.fi. The code for the paper will be available on https://github.com/ZhuoJTian/Distributed-MCR2. 

intermediate features often exhibit the ”neural collapse” phe-nomenon [3, 4], as illustrated in Fig. 1: The within-class features converge to their respective class means, while the means of different classes form a tightly structured frame. Empirical studies have observed this effect across various loss functions, including the widely used cross-entropy (CE) and mean squared error (MSE). Such feature representations compromise the intrinsic structure of individual data samples, reducing interpretability and limiting their effectiveness for more complex downstream tasks. Furthermore, the composi-tional capability [5], which is crucial for enabling connected intelligence in multi-agent systems, may be lost if the diversity in data structure is not maintained. To address this challenge, the original manifold of distributed datasets must be mapped onto global low-dimensional subspaces that maintain diversity within each class while ensuring clear discrimination between classes. 

Fig. 1: Illustration of the neural collapse phenomenon. 

Meanwhile, traditional distributed learning algorithms, such as federated learning (FL) and decentralized stochastic gradi-ent descent (SGD), rely on parameter transmission between nodes, imposing strict constraints on network architectures. However, in real-world scenarios, neural networks across different nodes may differ due to variations in computational power and storage capacity. To develop a more practical net-worked learning system, this work also aims to address system heterogeneity among nodes by enabling the transmission of semantic information instead of raw parameters. Here the semantic information refers to the extracted representation information in the latent space. 

A. Related Works 

Distributed Learning. Recently, distributed learning has made remarkable advances, including federated learning (FL) [6], which relies on a central processing node, and decen-tralized learning over networks [7]. Numerous procedures and frameworks have been developed to address key chal-lenges such as statistical heterogeneity [8–13], communication efficiency [14–17], etc. However, most existing algorithms 

> arXiv:2604.18237v1 [cs.LG] 20 Apr 2026 2

are task-specific, focusing on achieving consensus among model parameters, which often leads to the neural collapse phenomenon. As a result, clients struggle to extract expressive features necessary for more complex downstream processing, highlighting the need for distributed learning algorithms that generate meaningful representations. Moreover, in all these methods, model parameters or gradients must be transmitted and processed, necessitating a shared neural network archi-tecture among agents. Some recent approaches have explored heterogeneous system settings through split-learning [18, 19], sub-model training [20–24], and factorization [25, 26], where a central server has a large global model and conducts model partitioning, compression, or factorization. However, all these approaches depend on aggregating model parameters and re-quire a globally shared model structure to exploit correlations among varying small models. 

Distributed Representation Learning. The effectiveness of deep learning heavily depends on the quality of data repre-sentations. As a result, the rapid advancement of deep learning has been closely linked to significant progress in representa-tion learning [27–29]. This approach has been successfully applied across various domains, including image processing, natural language processing (NLP), speech recognition, and signal processing. In multi-agent scenarios [30], additional challenges arise due to the distributed nature of datasets and the potential multi-view perspectives of the global data space. Specifically, representations learned by different agents may become misaligned due to randomness in the training process. Additionally, the limited data available to each agent can lead to variations in data distribution, causing inconsistencies in the representation spaces encoded by different local models. Considering these factors, distributed representation learning primarily aims to address alignment and composition chal-lenges. The authors in [31] approach this problem from an information-theoretic perspective by extending Tishby’s cen-tralized Information Bottleneck (IB) method to a distributed setting, focusing on extracting the most relevant information for a given task. More recently, some other works [32–35] explore unsu-pervised or self-supervised federated representation learning using contrastive learning [36, 37] or data augmentation tech-niques. Among these works, FedU proposed in [32] deals with heterogeneity in federated self-supervised learning by divergence-aware predictor update rule, while Orchestra [33] utilizes local-global clustering. FedU 2 in [34] consists of flexible uniform regularizer and efficient unified aggregator to avoid the representation collapse and promote unified rep-resentations among clients. However, these methods require a central processor to coordinate the global information. Moreover, they rely on model aggregation through weighted averaging to construct a global model, which necessitates a shared neural network architecture among devices. 

Learning diverse and discriminative representation. The Maximal Coding Rate Reduction (MCR 2) principle, intro-duced in [38], optimizes a coding reduction objective to ex-pand the overall feature set while simultaneously compressing and linearizing features within each class. Leveraging MCR 2,the authors in [38] aimed to learn representations that are both  

> Fig. 2: Illustration of the learned subspaces of MCR 2principle.

diverse and discriminative for multi-class data. This approach maps real data distributions onto a feature space composed of multiple independent multi-dimensional linear subspaces, making it well-suited for both discriminative and generative tasks. Experimental results demonstrated that the learned representations effectively capture the structural information of the original datasets. Building on the MCR 2 principle, the authors in [39] incorporated an autoencoder architecture and introduced a closed-loop transcription mechanism by formulating a two-player min-max game between the encoder and decoder. However, this approach heavily relies on a large amount of training datasets and is therefore impractical for distributed devices with limited data availability. The method proposed in [40] utilizes the MCR 2 principle and subspace fusion to implement distributed multi-view perception. 

B. Contribution 

Inspired by the MCR 2 principle, this work proposes two distributed representation learning algorithms grounded in rigorous theoretical derivations and guarantees. By extending MCR 2 to distributed scenarios, our approach allows agents to collaboratively capture global structural representations using only their limited local data, while cooperating with other agents. This framework jointly addresses the challenges of neural collapse, alignment, and composition in distributed representation learning. Through cooperation, the representa-tion spaces within the same class are aligned across different agents, as illustrated on the right side of Fig. 3, ensuring that the global representations across the network reside within a unified space. Unlike the approach in [31], which employs the Information Bottleneck (IB) method, MCR 2 treats data labels as side information, offering a distinct perspective. Ad-ditionally, MCR 2 provides a metric for measuring “distance”, making it conceptually similar to contrastive learning methods [32, 35]. However, unlike contrastive learning, which focuses on sample-wise distance, MCR 2 considers the relationships between subsets of samples while incorporating label informa-tion as side information. Furthermore, our approach establishes consensus at the representation space level rather than relying on model parameter aggregation, enhancing its adaptability to diverse scenarios. Our contribution can be summarized as follows.  

> •

To achieve global representations that maintain diver-sity within classes and discriminability between classes, we apply the MCR 2 principle and extend it to dis-tributed settings. Specifically, we reformulate its original optimization problem into decentralized sub-problems by incorporating consensus constraints on the statistical 3

variance of the representation subspaces. For i.i.d. data distributions, the update rules are derived and simplified using augmented Lagrangian techniques.  

> •

We also address the case of non-i.i.d. data distributions among agents by reformulating the optimization problem into a cluster-based form. Specifically, we propose clus-tering the agents and virtually replicating nodes locally so that data distributions become i.i.d. within each cluster. This allows the optimization problem to be decoupled into multiple parallel clusters, where within each cluster, the sub-problems are solved sequentially by different agents using the block coordinate descent method.  

> •

For both i.i.d. and non-i.i.d. data distributions, we provide theoretical guarantees on the diverse and discriminative properties of the optimal solutions for the reformulated optimization problems. Additionally, we offer a conver-gence guarantee for the algorithm. Furthermore, in our proposed framework, devices share semantic information about their representations instead of model parameters, which is typically done in conventional distributed rep-resentation learning. This approach allows for the use of different neural network architectures among agents. The remainder of this paper is structured as follows. Section II introduces the system model and the MCR 2 principle. In Section III, we address the i.i.d. data distribution and reformulate the decomposable optimization problem, from which we develop the distributed representation learning al-gorithm for i.i.d. condition. Section IV focuses on non-i.i.d. data distribution, where we formulate the cluster-based global optimization problem and design the corresponding distributed learning algorithm. We give the theoretical analysis on the properties of the learned representation and convergence of the algorithm in Section V. The simulation results are presented in Section VI, with the conclusion provided in Section VII. II. P RELIMINARY 

A. System Model 

Consider a decentralized multi-agent communication net-work as shown in Fig. 3, which can be represented by an undirected graph G = ( V, E), where V = {1, . . . , N } denotes the set of N distributed agents/nodes and E = {εij }i,j ∈V 

represents the set of communication links between any two adjacent nodes. Let Ni denote the set of all neighboring nodes connected to node i and we denote the number of nodes in 

Ni by di = |N i|. The adjacency matrix of G is denoted by A,where A(i, j ) = 1 if εij ∈ E and A(i, j ) = 0 , otherwise. Each node has its own dataset from the global data distribution, which is denoted by Di = {X i, Yi}, and we define mi as the number of training data samples in node i.The output of the representation learning neural network in agent i is denoted by Zi ∈ Rd×mi , where d is the dimension of the output feature and mi is the number of data samples in node i. The global representation can be defined as Z =[Z1, · · · , ZN ] ∈ Rd×m, where m = PNi=1 mi denotes the total number of samples. The representation learning neural network (encoder) in node i is denoted by fi parameterized by θi and the output can be represented as Zi = fi(Xi, θi).

B. Maximal Coding Rate Reduction 

The compactness of the learned features as a whole can be measured by the average coding length per sample when the sample size is large enough, i.e., the coding rate subject to the distortion [38, 41], is given by. 

R(Z, ϵ ) = 12 log det( I + dmϵ 2 ZZ T ). (1) Considering that the generated features Z have multiple classes which may belong to different low-dimensional sub-spaces, the averaged number of bits per sample (the coding rate) is given in (2) [41], where Πk is a diagonal matrix whose diagonal entries indicate the membership of the samples in the multiple classes. In this sense, the label serves as side information. 

Rc(Z, ϵ |Π) = 

> K

X

> k=1

tr (Πk)2m log det( I + dtr (Πk)ϵ2 ZΠkZT ).

(2) To maximize the discrimination of the features among different classes, the whole space of Z must be as large as possible. On the other hand, within each class, the subspace should be of small volume to make the representation compact. Then, the loss function on the principle of Maximal Coding Rate Reduction can be expressed as follows [38]: 

max △R(Z, Π, ϵ ) = R(Z, ϵ ) − Rc(Z, ϵ |Π). (3) Given this objective function, our task is to decouple it into sub-problems for multiple distributed nodes, in order to jointly optimize their latent variables (feature space) Z =

{Z0, . . . , ZN }. In the following, we aim to develop a dis-tributed representation learning algorithm that can learn both diverse and discriminative representations across agents. This approach enhances the transparency of the learned feature representations, making them more interpretable and control-lable for downstream tasks. We consider two different data distribution conditions among agents: i.i.d. and non-i.i.d., and we develop the corresponding algorithms for each scenario. III. G LOBAL REPRESENTATION LEARNING FOR I .I.D.DATA DISTRIBUTION 

In this section, we consider an i.i.d. data distribution. Firstly, the original optimization problem (3) is equivalently reformulated so that the it can be decoupled among distributed agents. Building on augmented Lagrangian and primal–dual techniques, the original constrained optimization problem is reformulated into an unconstrained one, from which dis-tributed update rules can be systematically derived. For the first term on the right side of (3), based on the concave property of the log det function, we can write: 

R(Z, ϵ ) = 12 log det( I + dmϵ 2 ZZ T ) (4) 

= 12 log det 

h NX

> i=1

( mi

m (I + dmiϵ2 ZiZTi )) 

i

≥

> N

X

> i=1

mi

2m log det( I + dmiϵ2 ZiZTi ),4

Fig. 3: System model: The distributed nodes collaboratively learn the global representations by transmitting the semantic information. The right side shows the diverse and discriminative properties of the learned representations. 

where the first equality comes from the definition of Z and the last inequality comes from Jensen’s inequality given the concavity of log det function. The equality holds if and only if ZiZTi /m i = Zj ZTj /m j for any two nodes i, j ∈ V .We define Zi,k ∈ Rd×mi,k as the features in node i

corresponding to the k-th class, i.e., Zi,k is ZiΠi,k with zero columns removed. Drawing inspiration from the equality condition of Jensen’s inequality, we propose a procedure for decentralized representation learning. Specifically, we intro-duce a variance constraint (variance consensus) between the feature spaces of different nodes with respect to each class, i.e., Zi,k ZTi,k /m i,k = Zj,k ZTj,k /m j,k for any class k and arbitrary two nodes i and j. Then, we derive the following decomposable result for the second term in (3): 

Rc(Z, ϵ |Π) = 

> K

X

> k=1

tr (Πk)2m log det( I + dtr (Πk)ϵ2 ZΠkZT )=

> K

X

> k=1

mk

2m log det( I + dmkϵ2

> N

X

> i=1

Zi,k ZTi,k ) (5) 

=

> K

X

> k=1
> N

X

> i=1

mi,k 

2m log det( I + dmi,k ϵ2 Zi,k ZTi,k ).

Given (4) and (5), we formulate our optimization problem, using the lower bound of (4), as follows: 

max 

> Z
> N

X

> i=1

h

Ri(Zi) − Rci (Zi)

i

(6) s.t. ∥Zi,k ∥2 

> F

= mi,k , ∀i ∈ V , 1 ≤ k ≤ K, 

Zi,k ZTi,k 

mi,k 

= Zj,k ZTj,k 

mj,k 

, ∀i ∈ V , j ∈ N i, 1 ≤ k ≤ K, 

where 

Rci (Zi) = 

> K

X

> k=1

mi,k 

2m log det( I + dmi,k ϵ2 Zi,k ZTi,k ), (7) 

Ri(Zi) = mi

2m log det( I + dmiϵ2 ZiZTi ). (8) In (6), the first constraint aims to ensure that the reduction magnitude is comparable across different representations. This is achieved by normalizing each feature to lie on the unit sphere [38], which can be easily implemented by applying a normalization function to the output layer. Furthermore, with the connected network topology, the second constraint between connected nodes is sufficient to ensure Zi,k ZTi,k /m i,k =

Zj,k ZTj,k /m j,k for any class k and arbitrary two nodes i, j .By introducing the intermediate variable Ti,j,k , we equiva-lently reformulate problem (6). 

min 

> Z
> N

X

> i=1

Rci (Zi) − Ri(Zi) (9) s.t. ∥Zi,k ∥2 

> F

= mi,k , ∀i ∈ V , 1 ≤ k ≤ K, 

Zi,k ZTi,k 

mi,k 

= Ti,j,k , ∀i ∈ V , ∀j ∈ N i, 1 ≤ k ≤ K, 

Zj,k ZTj,k 

mj,k 

= Ti,j,k , ∀i ∈ V , ∀j ∈ N i, 1 ≤ k ≤ K, 

In order to solve this optimization problem with consensus constraints, we utilize the augmented Lagrangian techniques, which can transform the optimization problem into an uncon-strained one. By introducing the dual variables Ui,j,k , Vi,j,k ,the problem can be transferred into an unconstrained one. Specifically, we utilize the similar dual-decomposition tech-niques as in the distributed alternating direction method of multipliers (ADMM) algorithm [42], and the updating rules can be derived as in Proposition 1. 

Proposition 1. (Updating rules for i.i.d. data distribution.) Introduce Yi,j,k ≜ (Ui,j,k + Vj,i,k ) = 2 Ui,j,k , and U (0)  

> i,j,k

=

V (0)  

> j,i,k

= 0. Define the step size for dual variables as ρ and the penalty parameter as γ. Then the updating rules can be simplified into the following two steps, which can be conducted in parallel among the nodes: 

Y (t) 

> i,j,k

= Y (t−1)  

> i,j,k

+ ρ[ Z(t−1)  

> i,k

Z(t−1) 

> i,k T

mi,k 

− Z(t−1)  

> j,k

Z(t−1) 

> j,k T

mj,k 

],

(10a) 

Z(t) 

> i

= arg min L′(Y (t) 

> i

, Zi), (10b) 

where 

L′(Y (t) 

> i

, Zi) = Rci (Zi) − Ri(Zi)+ (11) 

X

> j∈N i
> K

X

> k=1

n

tr [Y (t)

> i,j,k T

( Zi,k ZTi,k 

mi,k 

− Z(t−1)  

> j,k

Z(t−1) 

> j,k T

mj,k 

)] + γ Zi,k ZTi,k 

mi,k 

−  

> Z(t−1)
> i,k Z(t−1)
> i,k T
> mi,k

+ Z(t−1)  

> j,k Z(t−1)
> j,k T
> mj,k

2

> 2
> F

o

.

Proof. See Appendix A. 5

Considering the fact that the optimization of Zi is imple-mented by optimizing the neural network parameters θi, the updating rule (10b) is expressed as follows: 

θ(t) 

> i

= arg min 

> θi

L′(Y (t) 

> i

, f i(θi, Xi)) . (12) Due to the non-convexity of (11) and the inherent non-convexity of the neural network, a closed-form solution for (12) cannot be obtained. However, it can be approximated through multiple steps of stochastic gradient descent, as is commonly done in neural networks. Therefore, the global structural representation learning algorithm for i.i.d. data dis-tribution is summarized in Algorithm 1. 

Algorithm 1: Distributed Representation Learning for i.i.d. Data Distribution  

> 1

for node i = 1 , 2, . . . , N in parallel do  

> 2

Initialize the local parameters of encoder θi and the dimension of the output feature d.

t = 0 , Y (0)  

> i,j,k

= 0 for all classes.  

> 3

Obtain V (0)  

> i,k

= Z(0)  

> i,k

Z(0) 

> i,k T

/m i,k and transmit 

V (0)  

> i,k

to all neighboring nodes.  

> 4

while not converge do  

> 5

t = t + 1  

> 6

for node i = 1 , 2, . . . , N in parallel do  

> 7

for class k = 1 , 2, . . . , K in parallel do  

> 8

Update Y (t) 

> i,j,k

according to (10a).  

> 9

for inner step t′ = 1 , . . . , T ′ do  

> 10

Randomly select a batch of data from local training data set Xs ∈ D i. 

> 11

Forward and Compute the loss (11) with 

Y (t) 

> i

, V (t−1)  

> i

and V (t−1)  

> j

, ∀j ∈ N i. 

> 12

Update θi with gradient descent.  

> 13

for node i = 1 , 2, . . . , N in parallel do  

> 14

Obtain Z(t) 

> i,k

∈ Rd×mi,k for all classes with all of the training data samples.  

> 15

Obtain V (t) 

> i,k

= Z(t) 

> i,k

Z(t)

> i,k T

/m i,k and transmit 

V (t) 

> i,k

to all neighboring nodes.  

> 16

Output the trained local encoders and the resultant representations Z.

Remark 1. (Computational cost analysis.) In Algorithm 1, in one iteration, the computation cost consists of three parts: the updating of Y (t)

> i,j,k

, the forward computation and backward propagation, the computation of V (t) 

> i,k

. We define the number of model parameters in node i as Pi = |θi|. Then the computational cost of the three steps are O(|N i| · K · d2),

O(T ′P ), O(d2mi) respectively. Take sum of them and we can obtain the computational cost in each iteration for node i as 

O(|N i|Kd 2 + T ′P + d2mi). Typically we have Pi ≫ d2, so the neural network training plays the main role, among which the computation of log det( ·) function costs the most. Compared to conventional algorithms such as Decentral-ized SGD (D-SGD) and its variants, the computational cost increases mainly with the computation of log det( ·) function, and additional updating of Y (t) 

> i,j,k

and V (t) 

> i,k

.

Remark 2. (Communication cost analysis.) In each iteration, each agent transmits V (t) 

> i,k

∈ Rd×d, ∀k = 1 , ..., K to all its neighboring nodes. Thus the total communication cost is O(T ·|N i| · K · d2).Compared with other algorithms requiring the exchange of model parameters of size O(P ), the proposed Algorithm 1 only communicates second-order representation statistics with complexity O(Kd 2), which is independent of both the model size and the local data volume. In deep neural network ar-chitectures, this significantly reduces communication overhead with Kd 2 ≪ P and makes the proposed method more scalable to large-scale and heterogeneous models. 

Theorem 1 in Section V guarantees the diverse and dis-criminative properties of the learned representations. However, in non-i.i.d. data distribution, we do not have ZiZTi /m i =

Zj ZTj /m j between different agents due to their different data distributions. As a result, the equality in (4) cannot be satisfied, and the resulting optimal solution does not possess the properties outlined in Theorem 1. In the following section, we will develop an algorithm to address this issue. IV. GLOBAL REPRESENTATION LEARNING FOR NON -I.I.D. DATA DISTRIBUTION In non-i.i.d. conditions, the first term in (3) cannot be decomposed due to the fact that ZiZTi /m i̸ = Zj ZTj /m j .To address this issue, we propose clustering the agents over the network so that the data distributions between clusters are i.i.d.. These clusters and their respective agents can then be decomposed for parallel updating. The procedure is il-lustrated in Fig. 4. In the first stage, we assume that the global information about the agents and their labels is known. To ensure i.i.d. data distributions among clusters, one node may belong to more than one cluster, as shown in Fig. 4, making it difficult to conduct inter-cluster parallelization. To address this issue, we propose the virtual node replication, whereby a heuristic clustering with Local Replication method as described in Algorithm 2. For ease of representation, we define the set of labels in agent i as Ki and assume that for any label k ∈ K , there exists at least one agent i ∈ V such that 

k ∈ K i. Note that Algorithm 2 prioritizes unclustered agents when constructing each cluster. Node replication is activated only when the remaining unclustered agents cannot cover all labels, which effectively limits redundant node usage. Specifically, if agent i exists in 1 < S i ≤ S clusters, we add Si virtual nodes in agent i and these virtual nodes are initialized the same as agent i with the same datasets and the same neural network model. Then, these Si virtual nodes, denoted by ˜Vi, are assigned to the clusters for parallelized im-plementation. The set of the agents together with the P 

> i∈V

Si

virtual nodes is denoted by ˜V. Correspondingly, the assigned (virtual) nodes’ set for cluster s is defined as ˜Vs. To ensure the local consensus of the virtual nodes within one agent i,we add the consensus constraints of Zi′ ZTi′ the local virtual 6

Fig. 4: Illustration of the global representation learning algorithm under non-i.i.d. conditions: First, 5 agents are clustered into 2 clusters. Through local replication in agent 5, the data distributions within each cluster become i.i.d. Then, in Algorithm 3, the (virtual) nodes across different clusters can be updated in parallel, while the agents within each cluster update their models sequentially. 

Algorithm 2: Clustering with Local Replication  

> 1

Input the set of the labels Ki for all i ∈ V . 

> 2

Initialize s = 1 , set of unclustered agents ¯V ← V . 

> 3

while ¯V̸ = ∅ do  

> 4

Initialize Vs = ∅, Us = K. 

> 5

while Us̸ = ∅ do  

> 6

if ¯V̸ = ∅ then  

> 7

Choose the agent 

i⋆ = arg max i∈ ¯V |K i ∩ U s| and update 

¯V ← ¯V \ { i⋆}. 

> 8

else  

> 9

Choose the agent 

i⋆ = arg max i∈V |K i ∩ U s|;

// Replication is allowed  

> 10

Update Vs ← V s ∪ { i⋆}, Us ← U s \ K i⋆ 

> 11

s = s + 1  

> 12

Output S = s − 1 clusters with node sets 

{V 1, . . . , VS }.nodes ˜Vi. With the same datasets in the node, this constraint can be easily satisfied by taking local average of the model parameters among i′ ∈ ˜Vi.After clustering and virtual replication, we could obtain the sets of nodes denoted by Vs, corresponding to the s-th cluster. Within s-th cluster, the set of nodes can be denoted by Vs. The number of nodes in Vs is denoted by N s and the total number of data samples in the cluster is denoted by ms. We have 

V1 ∪· · ·∪V S = V and ∪{K i, ∀i ∈ V s} = K, ∀s ∈ { 1, . . . , S },where ∪{·} denotes the union of all entries in the sets. Then, (4) can be reformulated as: 

R(Z, ϵ ) = 12 log det( I + dmϵ 2 ZZ T ) (13) 

= 12 log det 

h SX

> s=1

ms

m (I + dmsϵ2

X 

> i∈˜Vs

1

Si

ZiZTi

i

≥

> S

X

> s=1

ms

2m log det( I + dmsϵ2

X 

> i∈˜Vs

1

Si

ZiZTi ),

where the equality holds if 1

> ms

P   

> i∈˜Vs1
> Si

ZiZTi is equal among different clusters. ms is defined as ms = P  

> i∈˜Vs
> mi
> Si

,making sure PSs=1 ms = m. The first equality follows from the consensus constraints of Zi′ ZTi′ , i ′ ∈ ˜Vi, which can be ensured by taking the average of model parameters among the virtual nodes within each agent. In the non-i.i.d. condition, we still can make consensus con-straints on the variance of the subspaces w.r.t. each class. We define Vk as the set of original agents which have the label k,and ˜Vk as the set of nodes with the virtual nodes included. We add the constraints Zi,k ZTi,k /m i,k = Zj,k ZTj,k /m j,k , i, j ∈

˜Vk, ∀k ∈ { 1, . . . , K }. Then, the second term in (3) can be reformulated as: 

Rc(Z, ϵ |Π) = 

> K

X

> k=1

tr (Πk)2m log det( I + dtr (Πk)ϵ2 ZΠkZT )=

> K

X

> k=1

mk

2m log det( I + dmkϵ2

> S

X

> s=1

X 

> i∈˜Vsk

1

Si

Zi,k ZTi,k ) (14) 

=

> S

X

> s=1
> K

X

> k=1

msk

2mS i

log det( I + dmskϵ2

X 

> i∈˜Vsk

1

Si

Zi,k ZTi,k ),

where the last equality follows from the consensus constraints. By defining: 

Rcs(Zs) = 

> K

X

> k=1

msk

2mS i

log det( I + dmskϵ2

X 

> i∈˜Vsk

1

Si

Zi,k ZTi,k ),

the global optimization problem can be reformulated as: 

max 

> Z
> S

X

> s=1

h ms

2m log det( I + dmsϵ2

X 

> i∈˜Vs

1

Si

ZiZTi ) − Rcs(Zs)

i

s.t. ∥Zi,k ∥2 

> F

= mi,k , ∀i ∈ ˜V, 1 ≤ k ≤ K, (15) 

Zi,k ZTi,k 

mi,k 

= Zj,k ZTj,k 

mj,k 

, ∀i, j ∈ ˜Vk, 1 ≤ k ≤ K, 

Zi′ ZTi′ = ZiZTi , ∀i′ ∈ ˜Vi, i ∈ V ,

In (15), the first constraint can be satisfied by adding the normalization function on the output layer, as illustrated in the previous section. The third constraint can be satisfied by model parameter averaging among the local virtual nodes in each agent. As discussed above, this is due to the fact that these local virtual nodes share the same dataset and neural network architecture. By performing local averaging, consensus on the representations can be easily reached. To solve the formulated 7

optimization problem (15), the updating rules can be derived as in Proposition 2. 

Proposition 2. (Updating rules for non-i.i.d. data distribu-tion.) Introduce Yi,j,k and initialize it as Y (0)  

> i,j,k

= 0. Define the step size for dual variables as ρ and the penalty parameter as 

γ. Then the updating rules can be simplified into the following two steps, which can be conducted in parallel among the clusters. 

Y (t) 

> i,j,k

= Y (t−1)  

> i,j,k

+ ρ[ Z(t−1)  

> i,k

Z(t−1) 

> i,k T

mi,k 

− Z(t−1)  

> j,k

Z(t−1) 

> j,k T

mj,k 

],

(16a) 

(Zi)(t) = arg min Ls(Y (t), (Zi1 )(t), · · · , (Zi), · · · ,

(ZN s )(t−1) ), (16b) 

Within each cluster, the update of (16b) is conducted sequen-tially and 

Ls = − ms

2m log det( I + dmsϵ2

X 

> i∈˜Vs

1

Si

ZiZTi ) + Rcs(Zs)+ X 

> i∈˜Vs

X  

> j∈˜Vk/{i}

X

> k∈K i

n

tr [Y (t)

> i,j,k T

( Zi,k ZTi,k 

mi,k 

− Z(t−1)  

> j,k

Z(t−1) 

> j,k T

mj,k 

)] + γ Zi,k ZTi,k 

mi,k 

−  

> Z(t−1)
> i,k Z(t−1)
> i,k T
> mi,k

+ Z(t−1)  

> j,k Z(t−1)
> j,k T
> mj,k

2

> 2
> F

o

. (17) 

Proof. See Appendix B. In (16b), the updating of primal variables are decoupled into distributed nodes through the blocked coordinate descent procedure (BCD) [46, 47]. Specifically, in each coordinate descent step, the node receives the updated results from previous ones and then updates its local variables (model parameters). Under this sequential way, the problem can be solved as shown in the right side of Fig. 4. However, experiments with the local loss function (17) reveal that it is challenging for two nodes within the same cluster to achieve the same subspace for class k, if they both have the label k. This phenomenon results from the iterative nature of the process. In the experiments, the iterative updating process clearly shows that the expansion term (13) decreases quickly at the beginning, while the compression term (14) decreases much more slowly and later, as depicted in Fig. 5 in Section VI. This suggests that the expansion term takes priority during the update. In the local loss function (17), when two nodes share the same class k, the initial expansion term expands the space first, thus making it difficult for the two nodes to converge to the same subspace. To address this issue, we propose replacing the information on the common class k from other nodes with local one, using the constraints Zi,k ZTi,k /m i,k = Zj,k ZTj,k /m j,k , i, j ∈ ˜Vk for all k = 1 , . . . , K . Specifically, if node i and j have the same label k in cluster s, then in both (13) and (14), 1 

> Sj

Zj,k ZTj,k is replaced by mj,k   

> Sjmi,k

Zi,k ZTi,k in the updating of node i.Thus, the global structural representation learning algorithm for non-i.i.d. data distributions can be summarized in Algo-rithm 3. 

Algorithm 3: Distributed Representation Learning for non-i.i.d. Data Distribution  

> 1

for node i ∈ ˜V in parallel do  

> 2

Initialize the local parameters of encoder θi and the dimension of the output feature d.

t = 0 , Y (0)  

> i,j,k

= 0 for k ∈ K i. 

> 3

Obtain Z(0)  

> i,k

∈ Rd×mi,k for k ∈ K i with all of the training data samples.  

> 4

Obtain V (0)  

> i,k

= Z(0)  

> i,k

Z(0) 

> i,k T

/m i,k for k ∈ K i and 

transmit V (0)  

> i,k

to the nodes with label k. 

> 5

Transmit Z(0)  

> i

Z(0) 

> iT

/S i and corresponding mi/S i

to the nodes in the same cluster.  

> 6

while not converge do  

> 7

t = t + 1  

> 8

for node i ∈ ˜V in parallel do  

> 9

for class k = 1 , 2, . . . , K in parallel do  

> 10

Update Y (t) 

> i,j,k

according to (16a).  

> 11

for clusters s = 1 , . . . , S in parallel do  

> 12

for node i ∈ ˜Vs in sequential do  

> 13

for inner step t′ = 1 , . . . , T ′ do  

> 14

Update θi with gradient descent.  

> 15

Obtain Z(t) 

> i,k

∈ Rd×mi,k for k ∈ K i with all of the training data samples.  

> 16

Obtain Z(t) 

> i

Z(t)

> iT

/S i and transmit to the latter nodes within the cluster.  

> 17

for node i ∈ V in parallel do  

> 18

if Si > 1 then  

> 19

Take average of the model parameters θi′

among the virtual nodes i′ ∈ ˜Vi. 

> 20

Assign the averaged model parameters θi′

into the virtual nodes i′ ∈ ˜Vi. 

> 21

for node i ∈ ˜V in parallel do  

> 22

Obtain V (t) 

> i,k

= Z(t) 

> i,k

Z(t)

> i,k T

/m i,k for k ∈ K i

and transmit V (t) 

> i,k

to the nodes with label k. 

> 23

Output the trained local encoders. 

Remark 3. (Computational cost analysis.) In Algorithm 3, in one iteration, the computation cost is consist of four parts: the updating of Y (t)

> i,j,k

, the forward computation and backward propagation, the computation of V (t) 

> i,k

, the local averaging when Si > 1. We define the number of model parameters in node i as Pi = |θi|. Then the computational cost of the fours steps are O(|N (k) 

> i

| · Ki · d2), O(T ′P ),

O(d2mi), O(SiPi) respectively. Take sum of them and we can obtain the computational cost in each iteration for node i as 

O(|N i|Kd 2 +T ′P +d2mi +SiPi). Typically we have Pi ≫ d2

and T ′ ≫ Si, so the training of the neural network takes the main role, among which the computation of log det( ·) function costs the most. Compared to conventional algorithms such as D-SGD and 8

its variants, the computational cost increases with the com-putation of log det( ·) function, additional updating of Y (t)

> i,j,k

,

V (t) 

> i,k

and the local averaging. 

Remark 4. (Communication cost analysis.) In each iteration, there are two different kinds of communication. The first one is the communication within each cluster as in Line 16 of Algorithm 3, which is O(| ˜Vs| · d2). The second one is the label-wise communication as in Line 22 of Algorithm 3, which is O(|K i| · d2). The local model averaging does not introduce any communication cost. Then for each (virtual) node, the communication cost is O(d2 + |K i| · d2) = O(|K i| · d2).For non-i.i.d. data distributions, the proposed algorithm further reduces communication overhead by only exchanging class-conditional second-order representation statistics. The per-round communication complexity scales as O(|K i| · d2),which is significantly smaller than parameter-based methods with complexity O(P ), especially when each node contains only a small subset of classes. 

Ideally, to achieve the most efficient algorithm, the value of 

S should be as large as possible to maximize parallelization, while the number of agents, N s, should be minimized to reduce the sequential updating time in each cluster. However, increasing the value of S may require more local replication, which leads to higher local computation costs and greater demands on local capacities. Therefore, there is a trade-off between the number of clusters and the capabilities of the local agents. Additionally, considering potential multi-hop communications, the pre-training cluster and replication algorithm should also take into account the network topology to minimize communication costs. However, optimizing this aspect is not the primary focus of this work and will be addressed in future research. 

Remark 5. While the proposed framework demonstrates strong scalability and deployment potential, several practical considerations warrant further investigation. For example, the quadratic dependence on the representation dimension 

d may become non-negligible if extremely high-dimensional embeddings are used. Exploring low-rank approximations or adaptive dimension selection could further enhance scalability. Additionally, as discussed in Remarks 2 and 4, the communi-cation cost increases with the dimension of embeddings and the number of classes. In real-world large-scale distributed systems, some compression techniques can be applied to further reduce the communication cost. 

V. T HEORETICAL ANALYSIS 

For the i.i.d. conditions, we consider Assumption 1 on the number of data samples as follows. 

Assumption 1. For the number of data samples in each node 

i, we have mi,k  

> mi

= mj,k  

> mj

for any k ∈ K and j ∈ N i.

Note that in practical implementations, Assumption 1 can be satisfied easily by data augmentation techniques in each agent, to ensure the same proportion. Given Assumption 1, we could have the following Theorem 1 for the optimal solution of the optimization problem (6) in i.i.d. conditions. 

Theorem 1. (Property Guarantees) Under the large am-bient dimension d ≥ PKk=1 dk, high coding precision 

ϵ4 < min k∈K { mk 

> m

· d2

> d2
> k

} and Assumption 1, if we constrain rank (Zk) ≤ dk, then the optimal solution Z∗ of (6) among all nodes has the following properties. 1) (Between-class discriminative within node) 

(Z∗

> i,k 1

)T Z∗ 

> i,k 2

= 0 for different classes k1 and k2

in each node i ∈ V, i.e., Z∗ 

> i,k 1

and Z∗ 

> i,k 2

lie in orthogonal subspaces. 2) (Between-class discriminative among nodes) 

(Z∗

> i,k 1

)T Z∗ 

> j,k 2

= 0 for different classes k1 and k2

between any two nodes i and j, i.e., Z∗ 

> i,k 1

and Z∗ 

> j,k 2

lie in orthogonal subspaces. 3) (Within-class diverse among nodes) For the output fea-tures in the same class among all nodes, each subspace achieves its maximal dimension where rank (Z∗ 

> k

) = dk.And the largest dk − 1 singular values of Z∗ 

> k

are equal. Proof. See Appendix C. Theorem 1 guarantees the properties of the optimal solution of (6). After the collaborative training, the embeddings of the distributed data are promoted into multiple independent subspaces, where the features are distributed isotropically within the subspaces. To provide the convergence guarantee of Algorithm 1, we additionally make the following assumptions for the local learning steps. 

Assumption 2. (Smoothness) In the first iteration, given the dual variable Y (0)  

> i

, the local objective functions Li(Y (0)  

> i

, θi)

are L0-smooth for θi of each node i ∈ V .

Assumption 3. (Local Gradient) The local gradient estimator is unbiased in the t′-th inner gradient descent step. Further-more, there exists scalar κ > 0 such that the variance of the local gradient estimator is bounded by κ.

Consequently, the convergence guarantees of Algorithm 1 can be provided under Theorem 2. Theorem 2 characterizes the convergence of the model parameters given a small penalty parameter γ and updating step size ρ of the dual variable. However, it should be noted that Theorem 2 does not necessar-ily guarantee the algorithm converges to the optimal solution satisfying K.K.T. conditions. This is mainly due to the non-convexity of the neural network and the SGD-based updating rule of the primal variable. 

Theorem 2. (Convergence Guarantees) Define L =

max t∈[T ]Lt. Provided that the learning rate η < 14T ′L and 

(4 ρ + γ) is small enough such that there exist some scalar c′

where (4 ρ+γ) ≤ c′η2, under Assumption 2 and 3, the iterates of Algorithm 1 satisfy the following inequality: 

min   

> t∈[T]

E[∥∇L (Y (t+1) , θ (t))∥22] ≤ L0 − L ∗

cT T ′η + ηN E c ,

where 0 < c < 12 − 8η2T ′2L2, E = T ′L 

> 2

(1 + 4 ηT ′L)κ2 +

c′K|N i|. If we properly choose η = c′′   

> √T T ′

such that η <   

> 14T′L

, then L is a limited scalar and the convergence rate for Algorithm 1 is O( 1√T T ′ ).Proof. See Appendix D. 9

Remark 6. The conditions in Theorem 2 are standard in distributed learning, which can be satisfied in practice through appropriate hyperparameter selection. In our implementation, mini-batch training with Adam provides unbiased stochastic gradients at the sampling level with bounded variance due to the finite dataset, while the objective function is locally smooth for neural network training. The hyperparameters are chosen within stable ranges ensuring that the required bounds on η and (4 ρ + η) are met. The observed stable convergence behavior in our experiments further confirms that these theoretical conditions can be satisfied in practice. 

For the non-i.i.d. conditions, we make the following As-sumption 4 on the number of data samples among different clusters as follows. In the s-th cluster, the number of samples corresponding to class k is denoted by msk. Note that msk may consist of data samples from multiple nodes. 

Assumption 4. Among any different clusters s and s′, the pro-portion corresponding to the k-th class satisfies msk 

> ms

= ms′ 

> k
> ms′

for any k ∈ K , where msk = P  

> i∈˜Vsk
> mi,k
> Si

and ms = P 

> k∈K

msk.

Note that Assumption 4 can also be satisfied easily by data augmentation techniques in each node. Given Assumption 4, we could have the following Theorem 3 for the optimization problem (15) in non-i.i.d. conditions. 

Theorem 3. Under the large ambient dimension d ≥PKk=1 dk, high coding precision ϵ4 < min k∈K { mk 

> m

· d2

> d2
> k

} and Assumption 2, if we further constrain rank (Zk) ≤ dk, then the optimal solution Z∗ of (15) among all nodes has the same properties as those in Theorem 1. Proof. Under Assumption 2 and given the constraints in (15), it is easily to derive that 1

> ms

PKk=1 Zisk ,k ZTisk ,k is equal among different clusters for any k. Then the equality in (13) holds and the optimization problem (15) is equivalent to the original one. Then Theorem 2 can be proved. VI. N UMERICAL EXPERIMENTS 

In this section, we evaluate the performance of the proposed algorithms under i.i.d. and non-i.i.d. settings. Specifically, in each setting, we consider two datasets MNIST and CIFAR-10, which are typical 10-classification problems. For the MNIST dataset, the model architectures among nodes are identical. On the other hand, in the CIFAR-10 dataset, the models among nodes are different, to better show the proposed representation learning algorithms do not require the same architecture. In each experiment, we present the loss curves to illustrate the convergence behavior of the proposed algorithms, providing complementary empirical support for Theorem 2. To further validate the properties established in Theorems 1 and 3, we analyze the learned representations at convergence and compare them in terms of cosine similarity and singular value spectra. Finally, in the last subsection, we evaluate several performance metrics of the proposed algorithms against pre-trained representation learning models. We consider 7 different baseline methods for comparison: three conventional methods including Centralized MCR 2 with centralized datasets, D-SGD, Independent MCR 2 where each node independently updates its local model with MCR 2 prin-ciple; four state-of-the-art federated representation learning algorithms including FedU 2 [34], FedSimCLR [36], FedU [32] and Orchestra [33]. In these SOTA federated representation learning algorithms, we assume there is one central server in order to show the learned representations’ properties. If not specified, the experimental settings of these baseline methods are aligned with the original papers. 

A. Experiments with i.i.d. Setting 

In this subsection, we show the convergence curves of the proposed algorithm and properties of the learned representa-tions in i.i.d. data distributions. The datasets are evenly split among the nodes. The dimension of the representation is set to 

128 . The communication network topology is randomly gen-erated using the Erdos Renyi graph model, with the number of nodes N = 10 and a connectivity probability p = 0 .5.

> Fig. 5: Convergence curves of the averaged loss.

MNIST . We first consider the same model architecture among nodes and the MNIST dataset. The neural network in each node consists of a 4 convolutional layers, followed by one flattened layer and one linear layer to map the output to the size of 128 . The normalization operation is added after the output to satisfy the first constraint in (9). We set the initial learning rate to 0.1 with a weight decay of 10 −5 and use the Adam optimizer. The mini-batch size is set to 1000 

and the precision parameter ϵ2 = 0 .5. In each iteration, to approximate the optimal solution of (12), each node conducts 

5 local epochs. The step size for Y ’s update is set to ρ = 0 .1

and the penalty parameter γ is set to 1.0. The convergence curves of the averaged training loss for 1000 iterations are shown in Fig. 5(a), where the orange and blue curves represent the first and second term in (9). The green curve shows the reduction of the total loss in each node. It can be observed in Fig. 5(a) that after an initial stage, 

−Ri and Rci both decrease, indicating that in each node, the learned features Zi are expanding as a whole while each class 

Zi,k is being compressed. Meanwhile, the total loss of the augmented Lagrangian function decreases with the additional two terms in (11), which describes the correlation among the representations from different nodes. Then we plot the cosine similarity of the learned represen-tations and compare the results with the baseline methods. For D-SGD, we take the outputs of the second last layer as the learned representations. For the SOTA methods based on 10       

> (a) Proposed Algorithm 1 (b) Centralized MCR 2(c) D-SGD (d) Independent MCR 2
> (e) FedU 2(f) FedSimCLR (g) FedU (h) Orchestra
> Fig. 6: Cosine similarity between learned representations for MNIST under i.i.d. data distribution by using different algorithms.

federated learning framework, we use the global model in the server as the encoder to output the representations. The experimental settings are same as those in the original papers. The results can be shown in Fig. 6, visualizing the pairwise cosine similarity matrix of the learned representations, where samples are sorted by class label. Block structures along the diagonal indicate intra-class similarity, and black lines denote class boundaries. As shown in Fig. 6(a), the representation of different classes are nearly orthogonal when using the proposed distributed Algorithm 1. In comparison with Fig. 6(b), the equivalence of the optimization problems—between our formulation and the original one-can be verified by the similar resulting structure. In Fig. 6(c), D-SGD demonstrates performance comparable to centralized conventional methods with a cross-entropy loss function. Here, it is evident that the representations within each class are strongly correlated, as indicated by the dark blue color blocks, reflecting the neural collapse phenomenon. This shows that features within each class converge to the means with zero variability, and the features across different classes remain correlated. In Fig. 6(d), independent local updating with a limited number of training data samples results in poor performance in the learned representations, highlighting the need for communication and message sharing between nodes, thus validating the effectiveness of our proposed framework. In Fig. 6(e), the learned representations of FedU 2 show that the neural collapse problem is mitigated, while the cross-label correlation is still severe from those colored non-diagonal blocks. Finally, Figs. 6(f-h) show that those self-supervised methods still suffer from neural collapse problem and the resultant representations lack the discriminative properties. Finally, we compare the singular values of global represen-tations, for both overall data and each individual class. The results are shown in Fig. 7. We find that the representations learned by our proposed algorithm are much more diverse than those learned by D-SGD, with much higher dimensional spaces/subspaces, both for the overall data space and the subspaces for each class.  

> (a) Proposed Algorithm 1 (b) D-SGD
> Fig. 7: Singular value comparison for i.i.d. MNIST dataset: (red) overall data; (blue) individual class

CIFAR10. Secondly, we show the experiments on CIFAR10 datasets. To verify the superiority of our algorithm in the heterogeneous model conditions, we consider nodes with different neural network architectures. Specifically, we use the same communication network topology. The 10 nodes have the following neural network architecture respectively: ResNet18, ResNet34, VGG11, VGG16, ResNet18, ResNet34, VGG11, VGG16, ResNet18, ResNet34. The last linear layer of each model is replaced by a two-layer fully connected network with ELU activation function, such that the output dimension is 128 .Moreover, the normalization function is added to the outputs to satisfy the first constraint in the optimization problem (6). We set the initial learning rate to 0.01 with a weight decay of 

10 −5 and use the Adam optimizer. The mini-batch size is set to 

1000 and the precision parameter ϵ = 0 .5. In each iteration, to approximate the optimal solution of (12), each node conducts 11       

> (a) Proposed Algorithm 1 (b) Centralized MCR 2(c) D-SGD (d) Independent MCR 2
> (e) FedU 2(f) FedSimCLR (g) FedU (h) Orchestra
> Fig. 8: Cosine similarity between learned representations for CIFAR10 under i.i.d. data distribution using different algorithms.

5 local epochs. ρ is set to 0.1 and γ = 2 .0.The convergence curves of the averaged training loss over 

1000 iterations are presented in Fig. 5(b), demonstrating the convergence property of the proposed Algorithm 1. Addition-ally, we show the cosine similarity of the learned represen-tations and compare the results with other methods. Regard-ing the heterogeneous model architectures among nodes, D-SGD is implemented by sharing and averaging the model parameters between agents that have the same architecture. For those federated representation learning algorithms relying on model aggregation, we fix their neural network architec-tures as ResNet18 among agents. The results are shown in Fig. 8. The similarity between Fig. 8(a) and (b) confirms the effectiveness of our proposed method and the properties outlined in Theorem 1 under model heterogeneity conditions. Furthermore, in Fig. 8(c), the representations within each class no longer exhibit correlations due to the heterogeneous model architecture and partial averaging among agents. The result in Fig. 8(d) further highlights the effectiveness of nodes’ collaboration. The comparison between Fig. 8(a) and Figs. 8(e-h) exhibits the discriminative and diverse properties of the proposed algorithm and the MCR 2 principle, compared with those self-supervised federated representation learning algorithms. 

B. Experiments with non-i.i.d. Setting 

In this part, we simulate the performance of Algorithm 3 in the non-i.i.d. data distribution conditions. Specifically, we first simulate the conditions where the nodes can be fully clustered without local replication, to show the effectiveness of the parallel cluster and inner BCD. Then we consider more complicated scenarios where the nodes need to be locally replicated for inter-cluster i.i.d. data distribution. 

> Fig. 9: Convergence curves of the averaged loss.

In the first scenario, we consider 4 nodes in the network with local labels [1 , 2, 3, 4, 5] , [6 , 7,

8, 9, 0] , [0 , 3, 5, 7, 9] , [1 , 2, 4, 6, 8] respectively. Based on clustering, we could obtain two clusters in the network, i.e., cluster 1 with nodes 0, 1 and cluster 2 with nodes 2, 3.Similar to those in the previous subsection, we first simulate the results in the MNIST dataset, where the nodes have the common neural network architecture. We set γ = 2 .0, the initial learning rate 0.01 and other settings are the same as those in Section V-A. The convergence curves of Algorithm 3 are shown in Fig. 9(a) and the cosine similarities of the global representation are shown in Fig. 10. It can be observed in Fig. 10, that through the cooperation among nodes, the within-class correlation is built while the between-class orthogonality is enhanced. For the CIFAR-10 dataset and four nodes with ResNet18, VGG11, VGG16, and ResNet34 model architectures, the con-vergence curves are shown in Fig. 9(b) and the comparison of the learned representations are shown in Fig. 11. Then, we consider more complicated conditions with 5

nodes in the network, each with 4 random labels, which 12   

> (a) Proposed Algorithm 3 (b) Independent MCR 2
> Fig. 10: Cosine similarity between learned representations for MNIST under non-i.i.d. data distribution.
> (a) Proposed Algorithm 3 (b) Independent MCR 2
> Fig. 11: Cosine similarity between learned representations for CI-FAR10 under non-i.i.d. data distribution.

are [1 , 3, 5, 6] , [0 , 5, 7, 8] , [1 , 3, 8, 9] , [2 , 4, 6, 7] , [0 , 2, 4, 9] re-spectively. Then, based on Algorithm 2, we could have two clusters: cluster 1 with nodes {0, 1, 4} and cluster 2 with nodes {1, 2, 3}, with the local virtual replication in node 1.The cosine similarity of learned representations can be shown in Fig. 12 for MNIST and CIFAR-10 datasets. In CIFAR-10 datasets, the nodes have ResNet18, VGG11, VGG16, ResNet34, VGG11 model architectures, respectively.  

> (a) MNIST (b) CIFAR10
> Fig. 12: Cosine similarity of learned representations for MNIST and CIFAR-10 in non-i.d.d. condition under virtual node replication.

C. Metrics Comparison 

In this subsection, we evaluate and compare different met-rics of the learned representation neural networks between our proposed algorithms and those baseline methods. It is important to note that the proposed methods train each local model to map its local datasets into a shared global rep-resentation space. This allows each local model to capture a partial view of the global datasets, making the trained representation model personalized for each local perspective. For classification tasks, we propose averaging the output rep-resentations across nodes for each test data sample, which can be considered the global representation of the test data. Based on this global representation, classification is performed using a natural nearest subspace classifier, similar to the approach in [38], which uses the subspaces of global representations from the training data samples. Specifically, for the representations of training data samples corresponding to class k, denoted by 

Zk, let μk ∈ Rd be its mean and Φk ∈ Rd×rk be the first rk

principle components for Zk. Define z′ = 1

> N

P 

> i∈V

fi(x′, θi),then the predicted label of a test data sample x′ is given by 

k′ = arg min k∈K ∥(I − ΦkΦTk )( z′ − μk)∥22, (18) In addition to testing accuracy, to comprehensively evaluate the learned representations, we design a set of geometry-aware metrics that characterize neural collapse behavior, rep-resentation properties, and cross-node semantic alignment in distributed settings. Firstly, to assess class-level geometry and neural collapse, we compute the Class Mean Cosine Similarity Matrix, whose off-diagonal statistics quantify the angular relationships between class prototypes (mean/std). In addition, we measure the Within-Class Covariance Collapse Ratio (WCCR), which captures the proportion of variance retained within classes relative to the total variance, serving as a direct indicator of whether representations exhibit within-class collapse. To further evaluate discriminative structure without relying on classifier parameters, we report the Inter-Class to Intra-Class Distance Ratio (IIDR), which jointly re-flects class separation and intra-class compactness under non-collapsed representations. Beyond single-model geometry, we investigate representation alignment across clients. We employ Centered Kernel Alignment (CKA) to measure subspace-level similarity between embeddings learned by different clients, revealing whether distributed training enforces excessive rep-resentational collapse. These metrics provide a holistic view of representation quality, allowing us to disentangle neural collapse mitigation, semantic alignment, and representational diversity in distributed representation learning. The comparisons of these metrics between the proposed algorithms and baseline methods in i.i.d. condition are shown in Table I, where testing accuracy is the averaged results over 5 trails along with 95% confidence intervals. We additionally compare the results under CIFAR100 dataset. From Table I, considering MNIST, Algorithm 1 achieves competitive ac-curacy while significantly improving structural metrics. On CIFAR10, Algorithm 1 achieves the best classification per-formance and maintains strong representation compactness. Across both datasets, the off-diagonal statistics (mean/std) exhibit a relatively small mean off-diagonal similarity with low variance, indicating that class prototypes are nearly orthogonal and evenly distributed in the embedding space, which is consistent with Theorem 1. Moreover, a consistent trend can be observed that Algorithm 1 achieves the highest WCCR and the lowest IIDR. This suggests that the proposed method explicitly encourages representation compactness and reduces cross-13 

client feature divergence. In contrast, several baseline methods show higher IIDR and reduced WCCR, indicating potential representation misalignment or instability. Last but not least, although some methods including FedU 2 and FedSimCLR ex-hibit competitive CKA values, they suffer from higher IIDR or degraded ACC, implying a trade-off between feature similarity and discriminative power. In contrast, Algorithm 1 maintains both discriminative accuracy and structural coherence. On the other hand, Table 1 indicates that both the proposed algorithm and the centralized MCR 2 principle exhibit limited performance on the CIFAR100 dataset. This can be attributed to the large number of classes, which results in fewer samples per class. In the loss function (3) and those derived in Section IV, the accuracy of coding rate estimation strongly depends on the sample size. Furthermore, the analysis of computational and communication costs in Section IV shows that these costs increase significantly with larger K. Therefore, the MCR 2

principle is better suited to scenarios with a smaller number of classes. 

TABLE I: Comparison Among Different Algorithms. Method ACC(%) mean/std WCCR IIDR CKA 

MNIST 

Algorithm 1 0.9762 ±0.10% 0.07/0.07 0.9941 0.1159 0.0245 CenMCR 0.9838 ±0.12% 0.09/0.03 0.9235 0.4292 -D-SGD 0.9851 ±0.02% -0.08/0.25 0.2100 3.0249 0.0013 FedU 2 0.9560 ±0.21% 0.29/0.13 0.8224 0.6939 0.0060 FedSimCLR 0.9743 ±0.09% 0.81/0.06 0.6556 1.0855 0.0028 FedU 0.9773 ±0.12% 0.60/0.13 0.6189 1.1770 0.0029 Orchestra 0.9813 ±0.21% 0.91/0.05 0.5556 1.3454 0.0013 

CIFAR10 

Algorithm 1 71.76 ±0.06% 0.00/0.02 0.9914 0.1399 0.0208 CenMCR 71.08 ±0.02% 0.00/0.01 0.9153 0.4541 -D-SGD 68.26 ±0.10% -0.11/0.09 0.9044 0.4848 0.0018 FedU 2 41.18 ±0.20% 0.92/0.05 0.9028 0.4900 0.0027 FedSimCLR 49.03 ±0.41% 0.71/0.15 0.8179 0.6901 0.0022 FedU 68.08 ±0.21% 0.53/0.13 0.8689 0.5747 0.0068 Orchestra 64.02 ±0.19% 0.64/0.23 0.5539 1.3275 0.0011 

CIFAR100 

Algorithm 1 27.61 ±0.16% 0.00/0.02 0.9967 0.3834 0.0300 CenMCR 27.84 ±0.22% 0.01/0.03 0.5915 0.7728 -D-SGD 30.20 ±0.10% 0.75/0.10 0.9010 0.1442 0.0087 FedU 2 30.13 ±0.18% 0.16/0.21 0.8833 0.5186 0.0081 FedSimCLR 28.90 ±0.21% 0.49/0.20 0.7857 0.7371 0.0037 FedU 31.82 ±0.20% 0.24/0.23 0.8103 0.6836 0.0057 Orchestra 30.36 ±0.15% 0.95/0.03 0.5035 0.8093 0.0009 

VII. C ONCLUSION 

In this work, we proposed a novel distributed semantic-based representation learning framework, which can jointly capture the global structural representations in a multi-node system. Both i.i.d. and non-i.i.d data distributions were studied, where we reformulated the global optimization problem into equivalent decomposable ones. With the derivation of updating rules based on augmented Lagrangian function and dual-decomposition procedure, the sub-problems can be solved iteratively among distributed nodes. The properties of the optimal solutions and convergence of the proposed algorithm were theoretically given. Finally, we conducted extensive sim-ulations to validate the superiority of the proposed algorithms in terms of structural representations. The diverse and discriminative properties of the global representations are promising for solving more complicated downstream tasks, such as distributed multi-view problems and semantic communication. Its performance in dynamic net-work topologies and generalization to other non-vision tasks need to be further investigated. Another topic exploiting the representations is to apply it to generative tasks. Thirdly, the proposed distributed representation learning algorithm without transmission of model parameters may face the generalization challenge with limited local datasets, calling for a more comprehensive analysis of its generalization capability. APPENDIX 

A. Derivation of Proposition 1 

The augmented Lagrangian function for (9) can be formu-lated as follows: 

L(Z, T , U , V ) = 

> N

X

> i=1

{Rci (Zi) − Ri(Zi)+ (19) 

X

> j∈N i
> K

X

> k=1

n

tr [U Ti,j,k ( Zi,k ZTi,k 

mi,k 

− Ti,j,k )]+ 

tr [V Ti,j,k ( Zj,k ZTj,k 

mj,k 

− Ti,j,k )] 

o

+

γ

2

X

> j∈N i
> K

X

> k=1

h

∥ Zi,k ZTi,k 

mi,k 

− Ti,j,k ∥2 

> F

+ ∥ Zj,k ZTj,k 

mj,k 

− Ti,j,k ∥2

> F

i

},

where γ is a penalty parameter. To solve the problem, we use the dual gradient ascent and Gauss-Seidel strategy [43–45]. Then, the update rules of the dual variables {Ui,j,k , Vi,j,k },the primal variables Zi and the intermediate variable Ti,j,k can be derived alternatively, for all i ∈ V , j ∈ N i and 1 ≤ k ≤ K,as follows: 

U (t) 

> i,j,k

= U (t−1)  

> i,j,k

+ ρ[ Z(t−1)  

> i,k

Z(t−1) 

> i,k T

mi,k 

− T (t−1)  

> i,j,k

)] , (20a) 

V (t) 

> i,j,k

= V (t−1)  

> i,j,k

+ ρ[ Z(t−1)  

> j,k

Z(t−1) 

> j,k T

mj,k 

− T (t−1)  

> i,j,k

)] , (20b) 

Z(t) = arg min L(U (t), V (t), Z, T (t−1) ), (20c) 

T (t) 

> i,j,k

= arg min 

n

− tr [( U (t) 

> i,j,k

+ V (t)

> i,j,k

)T

Ti,j,k ]+ + γ

2

h

∥ Z(t) 

> i,k

Z(t)

> i,k T

mi,k 

− Ti,j,k ∥2 

> F

+ ∥ Z(t)

> j,k

Z(t)

> j,k T

mj,k 

− Ti,j,k ∥2

> F

io 

.

(20d) Here, ρ is the step size for the gradient ascent of the dual variables. From (20d), the closed form solution of T (t) 

> i,j,k

is: 

T (t) 

> i,j,k

= 12γ

U (t) 

> i,j,k

+ V (t)

> i,j,k

 + 12

 Z(t) 

> i,k

Z(t)

> i,k T

mi,k 

+ Z(t)

> j,k

Z(t)

> j,k T

mj,k 

.

(21) Under the initialization U (0)  

> i,j,k

+ V (0)  

> i,j,k

= 0, the update rules (20a) and (20b) and the closed form of T (t)

> i,j,k

, it can be easily deduced that 

U (t) 

> i,j,k

+ V (t) 

> i,j,k

= 0, ∀t ≥ 1, (22) 14 

which means the updating steps (20a) and (20b) can be simplified into one step (20a). Then T (t) 

> i,j,k

in (21) can be readily expressed by: 

T (t) 

> i,j,k

= 12

 Z(t) 

> i,k

Z(t)

> i,k T

mi,k 

+ Z(t)

> j,k

Z(t)

> j,k T

mj,k 

. (23) From (23), it can be observed T (t) 

> i,j,k

= T (t) 

> j,i,k

We additionally initialized U (0)  

> i,j,k

+ U (0)  

> j,i,k

= 0. Then according to (20a) and (23), we could have U (t) 

> i,j,k

+ U (t) 

> j,i,k

= 0 for all iterations t.And we have U (t) 

> i,j,k

= V (t)

> j,i,k

. We define 

Yi,j,k ≜ (Ui,j,k + Vj,i,k ) = 2 Ui,j,k ,

and initialize U (0)  

> i,j,k

= V (0)  

> j,i,k

= 0. Then, Yi,j,k is initialized as 

Y (0)  

> i,j,k

≜ (U (0)  

> i,j,k

+ V (0) 

> j,i,k

) = 0. Considering the closed form of 

T (t) 

> i,j,k

in (23), the update rules can be simplified into the two steps in (10a) and (10b) as in Proposition 1. 

B. Derivation of Proposition 2 

The global augmented Lagrangian function can then be formulated as follows: 

L =

> S

X

> s=1

h

Rcs(Zs) − ms

2m log det( I + dmsϵ2

X 

> i∈˜Vs

1

Si

ZiZTi )

i

+ X 

> i∈˜V

X  

> j∈˜Vk/{i}

X

> k∈K i

n

tr [U Ti,j,k ( Zi,k ZTi,k 

mi,k 

− Ti,j,k )]+ 

tr [V Ti,j,k ( Zj,k ZTj,k 

mj,k 

− Ti,j,k )]+ (24) 

γ

2

h

∥ Zi,k ZTi,k 

mi,k 

− Ti,j,k ∥2 

> F

+ ∥ Zj,k ZTj,k 

mj,k 

− Ti,j,k ∥2

> F

io 

.

Similar to the definition in Section III, we define Yi,j,k ≜

(Ui,j,k + Vj,i,k ) = 2 Ui,j,k , and U (0)  

> i,j,k

= V (0)  

> j,i,k

= 0. Then, 

Yi,j,k is initialized as Y (0)  

> i,j,k

= 0. Following similar derivations in Section III, we could get the global updating rules for all 

i ∈ ˜V and k = 1 , . . . , K as follows: 

Y (t) 

> i,j,k

= Y (t−1)  

> i,j,k

+ ρ[ Z(t−1)  

> i,k

Z(t−1) 

> i,k T

mi,k 

− Z(t−1)  

> j,k

Z(t−1) 

> j,k T

mj,k 

],

(25a) 

Z(t) = arg min L(Y (t), Z). (25b) The updating of Yi,k can be implemented by each node in parallel. However, Zi is coupled in the Lagrangian function (25b). Following we will derive how to solve the problem (25b) among distributed nodes. Note that P 

> i∈˜V

P 

> k∈K i

f (Zi,k ) = PSs=1 

P 

> i∈˜Vs

P 

> k∈K i

f (Zi,k ), both of which sum up the terms in all (virtual) nodes and all of their classes. Given this equivalence, problem in (25b) can be reformulated as: 

L =

> S

X

> s=1

n

− ms

2m log det( I + dmsϵ2

X 

> i∈˜Vs

1

Si

ZiZTi ) (26) 

+ Rcs(Zs) + X 

> i∈˜Vs

X  

> j∈˜Vk/{i}

X

> k∈K i

h

tr [Y (t)

> i,j,k T

( Zi,k ZTi,k 

mi,k 

− Z(t−1)  

> j,k

Z(t−1) 

> j,k T

mj,k 

)] + γ Zi,k ZTi,k 

mi,k 

−  

> Z(t−1)
> i,k Z(t−1)
> i,k T
> mi,k

+ Z(t−1)  

> j,k Z(t−1)
> j,k T
> mj,k

2

> 2
> F

io 

.

We define L = PSs=1 Ls, then the variables among different clusters can be decomposed in (26), where different clusters can be updated in parallel based on the clustered augmented Lagrangian function. 

(Zs)(t) = arg min Ls(Y (t), Zs). (27) Within each cluster, the variables in (27) can be decoupled into the distributed nodes using the blocked coordinate descent procedure (BCD) [46, 47]. Specifically, consider node i in the s-th cluster, the concatenation of Zi,k for all k ∈ K i is denoted by Zi. Then based on the block coordinate descent procedure, the solution in node i can be obtained through iteratively updating the following step for all i ∈ V s.

(Zi)(t) = arg min Ls(Y (t), (Zi1 )(t), · · · , (Zi), · · · ,

(ZN s )(t−1) ), (28) where 

Ls = − ms

2m log det( I + dmsϵ2

X 

> i∈˜Vs

1

Si

ZiZTi ) + Rcs(Zs)+ X 

> i∈˜Vs

X  

> j∈˜Vk/{i}

X

> k∈K i

n

tr [Y (t)

> i,j,k T

( Zi,k ZTi,k 

mi,k 

− Z(t−1)  

> j,k

Z(t−1) 

> j,k T

mj,k 

)] + γ Zi,k ZTi,k 

mi,k 

−  

> Z(t−1)
> i,k Z(t−1)
> i,k T
> mi,k

+ Z(t−1)  

> j,k Z(t−1)
> j,k T
> mj,k

2

> 2
> F

o

. (29) 

C. Proof of Theorem 1 Proof. We prove Theorem 1 by proving the equivalence of the reformulated optimization problem and the original problem in MCR 2 [38]. Under the constraints: 

Zi,k ZTi,k 

mi,k 

= Zj,k ZTj,k 

mj,k 

, ∀i ∈ V , j ∈ N i, 1 ≤ k ≤ K, 

in the connected communication network, this equality holds between any two nodes in the network topology ∀i ∈ V , j ∈V. Moreover, according to the definition of Zi and based on Assumption 1, we further have 

ZiZTi =

> K

X

> k=1

mi,k 

Zj,k ZTj,k 

mj,k 

=

> K

X

> k=1

mi

Zj,k ZTj,k 

mj

= mi

Zj ZTj

mj

.

Then the equality in (4) holds. Thus, the reformulated global optimization problem (6) is equivalent to the original one in (3) and the optimal solution of the decoupled sub-problems in the nodes is equivalent to the solution of (3). Then according to Theorem 2.1 in [38], we could have that (Z∗

> k1

)T Z∗ 

> k2

= 0

for different classes 1 ≤ k1 < k 2 ≤ K, based on which we 15 

obtain the property 1) and 2). Moreover, the optimal solutions have the diversity property 3) within a class. 

D. Proof of Theorem 2 Proof. According to the updating rule in (12), in the t-th iteration, we define the local loss function of node i as 

Li(Y (t) 

> i

, θi) = L′(Y (t) 

> i

, Zi), with Zi = fi(θi, Xi). The value of the global loss function in the t-th iteration is defined as 

L(Y (t), θ ) = X

> i∈V

Li(Y (t) 

> i

, θi).

For each local node, without loss of generality, we have 

E[Li(Y (t+1)  

> i

, θ(t+1)  

> i

) − L i(Y (t) 

> i

, θ(t) 

> i

)] (30) 

= E[Li(Y (t+1)  

> i

, θ(t+1)  

> i

) − L i(Y (t+1)  

> i

, θ(t) 

> i

)]+ 

E[Li(Y (t+1)  

> i

, θ(t) 

> i

) − L i(Y (t) 

> i

, θ(t) 

> i

)] ,

where the expectations are taken over the randomness in stochastic gradients. We analyze the two terms respectively on the right side of (30). According to our updating rule (12), the first term on the right side of (30) can be seen as the reduction of the local loss function in the (t + 1) -th iteration after several steps of SGD. Under Assumption 2, we have 

∥∇ Li(Y (0)  

> i

, θi) − ∇ Li(Y (0)  

> i

, θ′

> i

)∥ ≤ L0∥θi − θ′

> i

∥.

In the next iteration, it can be derived that 

∥∇ Li(Y (1)  

> i

, θi) − ∇ Li(Y (1)  

> i

, θ′

> i

)∥ (31) 

≤ 2∥∇ Li(Y (0)  

> i

, θi) − ∇ Li(Y (0)  

> i

, θ′

> i

)∥ + 2 ∥∇ Li(Y (1)  

> i

, θi)−∇Li(Y (0)  

> i

, θi) + ∇Li(Y (0)  

> i

, θ′

> i

) − ∇ Li(Y (1)  

> i

, θ′

> i

)∥

The second term on the right side of (35) can be bounded with the calculated gradients on θ and the normalization property of Z, which we omit here due to limited space. Following this relationship between the present and previous iterations, together with the smoothness of the neural network function fi(θi), it is easy to prove for any t-th iteration, 

Li(Y (t) 

> i

, θi) is Lt-smooth, with a limited scalar Lt.Then under Assumption 2 and 3, based on the Lt-smoothness of Li(Y (t) 

> i

, θi), we could obtain the following results according to the properties of T ′ steps of local SGD. Due to space limitation, we refer readers to Lemma 3 in [10] for details. 

E[Li(Y (t+1)  

> i

, θ(t+1)  

> i

) − L i(Y (t+1)  

> i

, θ(t) 

> i

)] ≤ (32) 

− cT ′η∥∇L i(Y (t+1)  

> i

, θ(t) 

> i

)∥22 + η2T ′Lt+1 

2 (1 + 4 ηT ′Lt+1 )κ2.

For the second term on the right side of (30), based on the definition of Li, we have 

E[Li(Y (t+1)  

> i

, θ(t) 

> i

) − L i(Y (t) 

> i

, θ(t) 

> i

)] ≤ (33) 

X

> j∈N i
> K

X

> k=1

tr [( Y (t+1)  

> i,j,k

− Y (t)

> i,j,k

)T

( Z(t) 

> i,k

Z(t)

> i,k T

mi,k 

− Z(t)

> j,k

Z(t)

> j,k T

mj,k 

)] + γ

4

X

> j∈N i
> K

X

> k=1

Z(t) 

> i,k

Z(t)

> i,k T

mi,k 

− Z(t)

> j,k

Z(t)

> j,k T

mj,k 

> 2
> F

≤ (2 ρ + γ

2 ) X

> j∈N i
> K

X

> k=1

[ Z(t) 

> i,k

Z(t)

> i,k T

mi,k  

> 2
> F

+ Z(t)

> j,k

Z(t)

> j,k T

mj,k  

> 2
> F

]

≤ (4 ρ + γ)K|N i|,

where the first inequality follows from the updating rule in (10a) and the last inequality follows from the first constraint in the optimization problem, i.e., 

∥Zi,k ∥2 

> F

= mi,k , ∀i ∈ V , 1 ≤ k ≤ K

and it is satisfied in each iteration/step with the normalization function on the output layer. Take sum of (32) and (33), we could obtain that 

E[Li(Y (t+1)  

> i

, θ(t+1)  

> i

) − L i(Y (t) 

> i

, θ(t) 

> i

)] ≤ (4 ρ + γ)K|N i|− cT ′η∥∇L i(Y (t+1)  

> i

, θ(t) 

> i

)∥22 + η2T ′Lt+1 

2 (1 + 4 ηT ′Lt+1 )κ2

≤ − cT ′η∥∇L i(Y (t+1)  

> i

, θ(t) 

> i

)∥22 + η2E, (34) where E = T ′L 

> 2

(1 + 4 ηT ′L)κ2 + c′K|N i|, L = max t∈[T ]Lt,and the last inequality comes from (4 ρ + γ) ≤ c′η2. Take sum of t = 0 , . . . , T over all node i, and rearrange the terms, we could derive that 

min   

> t∈[T]

E[∥∇L (Y (t+1) , θ (t))∥22] ≤ L0 − L ∗

cT T ′η + ηN E c . (35) If we choose (4 ρ + γ) ≤ c′η2 and properly choose η = c′′  

> √T T ′

such that η < 14T ′L , then it is easy to prove that L is a limited scalar. This completes the proof. REFERENCES 

[1] M. Chen, D. G¨ und¨ uz, K. Huang, W. Saad, M. Bennis, A. V. Fel-jan, and H. V. Poor, “Distributed learning in wireless networks: Recent progress and future challenges,” IEEE Journal on Selected Areas in Commun. , vol. 39, no. 12, pp. 3579–3605, 2021. [2] Y. Ma, D. Tsao, and H.-Y. Shum, “On the principles of parsimony and self-consistency for the emergence of intelligence,” Frontiers of Information Technology & Electronic Engineering , vol. 23, no. 9, pp. 1298–1323, 2022. [3] Z. Zhu, T. Ding, J. Zhou, X. Li, C. You, J. Sulam, and Q. Qu, “A geometric analysis of neural collapse with unconstrained features,” Proc. Adv. Neural Inf. Process. Syst. (NIPS) , vol. 34, pp. 29 820–29 834, 2021. [4] C. Yaras, P. Wang, Z. Zhu, L. Balzano, and Q. Qu, “Neural collapse with normalized features: A geometric analysis over the riemannian manifold,” Proc. Adv. Neural Inf. Process. Syst. (NIPS) , vol. 35, pp. 11 547–11 560, 2022. [5] E. Elmoznino, T. Jiralerspong, Y. Bengio, and G. La-joie, “A complexity-based theory of compositionality,” arXiv: 2410.14817 , 2024. [6] T. Li, A. K. Sahu, A. Talwalkar, and V. Smith, “Federated learn-ing: Challenges, methods, and future directions,” IEEE signal process. magazine , vol. 37, no. 3, pp. 50–60, 2020. [7] X. Lian, C. Zhang, et. al., “Can decentralized algorithms out-perform centralized algorithms? a case study for decentralized parallel stochastic gradient descent,” Proc. Adv. Neural Inf. Process. Syst. (NIPS) , vol. 30, 2017. [8] V. Smith, C.-K. Chiang, M. Sanjabi, and A. S. Talwalkar, “Federated multi-task learning,” Proc. Adv. Neural Inf. Process. Syst. (NIPS) , vol. 30, 2017. [9] S. Pu and A. Nedi´ c, “Distributed stochastic gradient tracking methods,” Mathematical Programming , vol. 187, no. 1, pp. 409– 457, 2021. 16 

[10] Z. Tian, Z. Zhang, Z. Yang, R. Jin, and H. Dai, “Distributed learning over networks with graph-attention-based personaliza-tion,” IEEE Trans. Signal Process. , vol. 71, pp. 2071–2086, 2023. [11] F. Sattler, S. Wiedemann, K.-R. M¨ uller, and W. Samek, “Robust and communication-efficient federated learning from non-iid data,” IEEE Trans. Neural Net. Learn. Syst. , vol. 31, no. 9, pp. 3400–3413, 2019. [12] L. Collins, H. Hassani, A. Mokhtari, and S. Shakkottai, “Ex-ploiting shared representations for personalized federated learn-ing,” in Int. Conf. Mach. Learning , 2021, pp. 2089–2099. [13] Z. Tian, Z. Zhang, R. Jin, L. Liu, and Z. Yang, “Distributed compressed sensing with personalized variational auto-encoders,” in IEEE 33rd Inter. Workshop on Machine Learning for Signal Processing (MLSP) , 2023, pp. 1–6. [14] G. Zhu, Y. Du, D. G¨ und¨ uz, and K. Huang, “One-bit over-the-air aggregation for communication-efficient federated edge learning: Design and convergence analysis,” IEEE Trans. Wireless Com-mun. , vol. 20, no. 3, pp. 2120–2135, 2020. [15] B. McMahan, E. Moore, D. Ramage, S. Hampson, and B. A. y Arcas, “Communication-efficient learning of deep networks from decentralized data,” in Artificial Intell. and Statis. . PMLR, 2017, pp. 1273–1282. [16] C. Li, G. Li, and P. K. Varshney, “Communication-efficient federated learning based on compressed sensing,” IEEE Internet of Things Journal , vol. 8, no. 20, pp. 15 531–15 541, 2021. [17] A. Li, J. Sun, X. Zeng, M. Zhang, H. Li, and Y. Chen, “Fed-mask: Joint computation and communication-efficient personal-ized federated learning via heterogeneous masking,” in Proc. 19th ACM Conf. on Embed. Networked Sensor Syst. , 2021, pp. 42–55. [18] C. He, M. Annavaram, and S. Avestimehr, “Group knowledge transfer: Federated learning of large cnns at the edge,” Proc. Adv. Neural Inf. Process. Syst. (NIPS) , vol. 33, pp. 14 068–14 080, 2020. [19] O. Gupta and R. Raskar, “Distributed learning of deep neural network over multiple nodes,” Journal of Network and Computer Applications , vol. 116, pp. 1–8, 2018. [20] E. Diao, J. Ding, and V. Tarokh, “Heterofl: Computation and communication efficient federated learning for heterogeneous clients,” arXiv: 2010.01264 , 2020. [21] S. Horvath, S. Laskaridis, M. Almeida, I. Leontiadis, S. Ve-nieris, and N. Lane, “Fjord: Fair and accurate federated learning under heterogeneous targets with ordered dropout,” Proc. Adv. Neural Inf. Process. Syst. (NIPS) , vol. 34, 2021. [22] Y. Deng, W. Chen, J. Ren, F. Lyu, Y. Liu, Y. Liu, and Y. Zhang, “Tailorfl: Dual-personalized federated learning under system and data heterogeneity,” in Proc. 20th ACM Conf. Embedded Net-worked Sensor Systems , 2022, pp. 592–606. [23] Y. Jiang, S. Wang, V. Valls, B. J. Ko, W.-H. Lee, K. K. Leung, and L. Tassiulas, “Model pruning enables efficient federated learning on edge devices,” IEEE Trans. Neural Net. Learn. Syst. ,vol. 34, no. 12, pp. 10 374–10 386, 2022. [24] Z. Tian, Z. Zhang, Y. Li, and M. Bennis, “Communication-Efficient Personalized Distributed Learning with Data and Node Heterogeneity,” IEEE Transactions on Cognitive Communica-tions and Networking , 2025. [25] D. Yao, W. Pan, M. J. O’Neill, Y. Dai, Y. Wan, H. Jin, and L. Sun, “Fedhm: Efficient federated learning for heterogeneous models via low-rank factorization,” arXiv: 2111.14655 , 2021. [26] Y. Mei, P. Guo, M. Zhou, and V. Patel, “Resource-adaptive federated learning with all-in-one neural composition,” Proc. Adv. Neural Inf. Process. Syst. (NIPS) , vol. 35, pp. 4270–4284, 2022. [27] A. Payandeh, K. T. Baghaei, P. Fayyazsanavi, S. B. Ramezani, Z. Chen, and S. Rahimi, “Deep representation learning: Funda-mentals, technologies, applications, and open challenges,” IEEE Access , vol. 11, pp. 137 621–137 659, 2023. [28] Y. Li, M. Yang, and Z. Zhang, “A survey of multi-view represen-tation learning,” IEEE Trans. Knowledge and Data Engineering ,vol. 31, no. 10, pp. 1863–1883, 2018. [29] Y. Bengio, A. Courville, and P. Vincent, “Representation learn-ing: A review and new perspectives,” IEEE Trans. Pattern Analysis Machine Intell. , vol. 35, no. 8, pp. 1798–1828, 2013. [30] Y. Liu, Z. Tian, J. Sun, Y. Jiang, and X. Zhang, “Distributed representation learning via node2vec for implicit feedback rec-ommendation,” Neural Computing and Applications , vol. 32, no. 9, pp. 4335–4345, 2020. [31] I. E. Aguerri and A. Zaidi, “Distributed variational represen-tation learning,” IEEE Trans. Pattern Analysis Machine Intell. ,vol. 43, no. 1, pp. 120–138, 2019. [32] W. Zhuang, X. Gan, Y. Wen, S. Zhang, and S. Yi, “Collaborative unsupervised visual representation learning from decentralized data,” in Proc. IEEE/CVF Int. Conf. Computer Vision , 2021, pp. 4912–4921. [33] E. S. Lubana, C. I. Tang, F. Kawsar, R. P. Dick, and A. Mathur, “Orchestra: Unsupervised federated learning via globally consis-tent clustering,” arXiv: 2205.11506 , 2022. [34] X. Liao, W. Liu, C. Chen, et al. , “Rethinking the representation in federated unsupervised learning with non-iid data,” in Proc. IEEE/CVF Conf. Computer Vision Pattern Recognition , 2024, pp. 22841–22850. [35] F. Zhang, K. Kuang, L. Chen, et al. , “Federated unsupervised representation learning,” Frontiers of Information Technology & Electronic Engineering , vol. 24, no. 8, pp. 1181–1193, 2023. [36] T. Chen, S. Kornblith, M. Norouzi, and G. Hinton, “Simclr: A simple framework for contrastive learning of visual representa-tions,” in Int. Conf. Learn. Represen. , vol. 2, no. 4, 2020. [37] A. Ghalkha, Z. Tian, C. B. Issaid, and M. Bennis, “SheafAlign: A Sheaf-theoretic Framework for Decentralized Multimodal Alignment,” arXiv: 2510.20540 , 2025. [38] Y. Yu, K. H. R. Chan, C. You, C. Song, and Y. Ma, “Learning diverse and discriminative representations via the principle of maximal coding rate reduction,” Proc. Adv. Neural Inf. Process. Syst. (NIPS) , vol. 33, pp. 9422–9434, 2020. [39] X. Dai, S. Tong, M. Li, Z. Wu, M. Psenka, K. H. R. Chan, P. Zhai, Y. Yu, X. Yuan, H. Y. Shum et al. , “Closed-loop data transcription to an ldr via minimaxing rate reduction,” arXiv: 2111.06636 , 2021. [40] Z. Tian and B. Mehdi, “Compositional Distributed Learning for Multi-View Perception: A Maximal Coding Rate Reduction Perspective,” IEEE Signal Process. Letters , vol. 32, pp. 4409– 4413, 2025. [41] Y. Ma, H. Derksen, W. Hong, and J. Wright, “Segmentation of multivariate mixed data via lossy data coding and compression,” 

IEEE Trans. Pattern Analysis and Machine Intelligence , vol. 29, no. 9, pp. 1546–1562, 2007. [42] Z. Tian, Z. Zhang, J. Wang, X. Chen, W. Wang, and H. Dai, “Distributed admm with synergetic communication and compu-tation,” IEEE Trans. Commun. , vol. 69, no. 1, pp. 501–517, 2020. [43] J. Matamoros, S. M. Fosson, E. Magli, and C. Ant´ on-Haro, “Distributed admm for in-network reconstruction of sparse sig-nals with innovations,” IEEE Trans. Signal and Information Process. over Networks , vol. 1, no. 4, pp. 225–234, 2015. [44] M. Hong and Z.-Q. Luo, “On the linear convergence of the alternating direction method of multipliers,” Mathematical Pro-gramming , vol. 162, no. 1, pp. 165–199, 2017. [45] Z. Tian, Z. Zhang, and L. Hanzo, “Distributed multi-view sparse vector recovery,” IEEE Trans. Signal Process. , vol. 71, pp. 1448– 1463, 2023. [46] A. Beck and L. Tetruashvili, “On the convergence of block coordinate descent type methods,” SIAM journal on Optimization ,vol. 23, no. 4, pp. 2037–2060, 2013. [47] M. Hong, X. Wang, M. Razaviyayn, and Z.-Q. Luo, “Iteration complexity analysis of block coordinate descent methods,” Math-ematical Programming , vol.163, pp. 85–114, 2017.