---
title: A Multi-Prototype-Guided Federated Knowledge Distillation Approach in AI-RAN Enabled Multi-Access Edge Computing System
title_zh: 支持 AI-RAN 的多接入边缘计算系统中的一种多原型引导联邦知识蒸馏方法
authors: "Luyao Zou, Hayoung Oh, Chu Myaet Thwal, Apurba Adhikary, Seohyeon Hong, Zhu Han"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09727v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 边缘计算系统的联邦学习框架
tldr: 本文针对AI-RAN赋能的多接入边缘计算系统，提出了一种多原型引导的联邦知识蒸馏（MP-FedKD）方法。为解决联邦学习中非独立同分布（non-IID）数据导致的性能下降及单原型策略的信息丢失问题，该方法结合了自知识蒸馏、条件层次聚类（CHAC）及原型对齐方案，并设计了LEMGP损失函数。实验证明，该方法在准确率和误差指标上均优于现有主流方案。
selection_source: fresh_fetch
motivation: 传统联邦学习在处理AI-RAN边缘计算系统中的非独立同分布数据时面临挑战，且现有的单原型策略容易造成关键特征信息的丢失。
method: 提出MP-FedKD方法，利用条件层次聚类生成多原型，并结合原型对齐方案与LEMGP损失函数进行本地训练优化。
result: 在多种数据集和非独立同分布设置下的实验结果显示，该方法在准确率、RMSE和MAE等关键指标上均优于现有的先进基准算法。
conclusion: 通过引入多原型策略和知识蒸馏，该研究有效提升了异构数据环境下联邦学习在边缘计算系统中的模型表现和鲁棒性。
---

## 摘要
随着无线网络的发展，多接入边缘计算（MEC）和人工智能（AI）原生无线接入网（RAN）引起了广泛关注。特别是，AI-RAN 与 MEC 的融合有望改变网络效率和响应能力。因此，研究支持 AI-RAN 的 MEC 系统具有重要价值。联邦学习（FL）目前正成为支持 AI-RAN 的 MEC 系统的一种极具前景的方法，它使边缘设备能够在不泄露原始数据的情况下协作训练全局模型。然而，传统的联邦学习在处理非独立同分布（non-IID）数据时面临挑战。在联邦学习中，可以通过对每个类别的嵌入向量取平均值来获得单一原型，以处理数据异构性问题。然而，由于平均操作，这可能会导致有用信息的丢失。因此，本文提出了一种多原型引导的联邦知识蒸馏（MP-FedKD）方法。特别地，将自知识蒸馏集成到联邦学习中以解决 non-IID 问题。为了应对单一原型策略导致的信息丢失问题，本文采用了多原型策略，并提出了一种条件层次聚类（CHAC）方法和一种原型对齐方案。此外，我们为每个本地客户端设计了一种新型损失函数（称为 LEMGP 损失），重点关注全局原型与本地嵌入之间的关系。在具有各种 non-IID 设置的多个数据集上进行的广泛实验表明，所提出的 MP-FedKD 方法在准确率、平均准确率和误差（RMSE 和 MAE）方面均优于现有的最先进基准方法。

## Abstract
With the development of wireless network, Multi-Access Edge Computing (MEC) and Artificial Intelligence (AI)-native Radio Access Network (RAN) have attracted significant attention. Particularly, the integration of AI-RAN and MEC is envisioned to transform network efficiency and responsiveness. Therefore, it is valuable to investigate AI-RAN enabled MEC system. Federated learning (FL) nowadays is emerging as a promising approach for AI-RAN enabled MEC system, in which edge devices are enabled to train a global model cooperatively without revealing their raw data. However, conventional FL encounters the challenge in processing the non-independent and identically distributed (non-IID) data. Single prototype obtained by averaging the embedding vectors per class can be employed in FL to handle the data heterogeneity issue. Nevertheless, this may result in the loss of useful information owing to the average operation. Therefore, in this paper, a multi-prototype-guided federated knowledge distillation (MP-FedKD) approach is proposed. Particularly, self-knowledge distillation is integrated into FL to deal with the non-IID issue. To cope with the problem of information loss caused by single prototype-based strategy, multi-prototype strategy is adopted, where we present a conditional hierarchical agglomerative clustering (CHAC) approach and a prototype alignment scheme. Additionally, we design a novel loss function (called LEMGP loss) for each local client, where the relationship between global prototypes and local embedding will be focused. Extensive experiments over multiple datasets with various non-IID settings showcase that the proposed MP-FedKD approach outperforms the considered state-of-the-art baselines regarding accuracy, average accuracy and errors (RMSE and MAE).

---

## 论文详细总结（自动生成）

以下是对论文《A Multi-Prototype-Guided Federated Knowledge Distillation Approach in AI-RAN Enabled Multi-Access Edge Computing System》的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：在 6G 演进中，AI 原生无线接入网（AI-RAN）与多接入边缘计算（MEC）的融合是提升网络效率的关键。联邦学习（FL）因其隐私保护特性成为该架构下的主流训练范式。
*   **核心问题**：
    *   **数据异构性（Non-IID）**：边缘设备采集的数据分布不均，导致联邦学习模型收敛缓慢且准确率下降。
    *   **单原型信息丢失**：现有基于原型的 FL 方法（如 FedProto）通常对每一类特征向量取平均值生成“单原型”，这种平均化操作会抹除类内的多样性信息，导致特征表示能力不足。
*   **研究动机**：提出一种能够捕捉类内多特征分布、同时利用知识蒸馏缓解模型漂移的联邦学习新框架。

### 2. 方法论
论文提出了 **MP-FedKD** 框架，其核心思想是通过“多原型”引导和“自蒸馏”机制来增强本地训练。
*   **关键技术细节**：
    1.  **条件层次聚类 (CHAC)**：在本地客户端，不直接对特征取平均，而是使用改进的层次聚类算法。引入“条件”机制：若某类样本数充足，则聚类为预设的 $b\zeta$ 个原型；若样本不足，则每个样本自成一个原型。这保证了特征提取的精细度。
    2.  **自知识蒸馏 (SKD)**：将上一轮的本地模型作为“教师”，当前模型作为“学生”，通过 KL 散度约束本地更新，防止模型在异构数据上过度偏移。
    3.  **原型对齐 (PA)**：设计 PA 损失函数，使当前的全局原型与上一轮的本地嵌入向量对齐，补偿因聚合操作带来的信息损失。
    4.  **LEMGP 损失函数**：这是一种新型的对比损失，包含**吸引项**（拉近本地嵌入与同类全局原型的距离）和**排斥项**（推开本地嵌入与异类全局原型的距离）。
*   **算法流程**：服务端下发全局模型和全局多原型 $\rightarrow$ 客户端执行 CHAC 生成本地多原型 $\rightarrow$ 结合 SKD、PA 和 LEMGP 损失进行本地训练 $\rightarrow$ 服务端聚合模型参数和多原型。

### 3. 实验设计
*   **数据集**：
    *   标准数据集：MNIST, Fashion-MNIST, CIFAR-10, EuroSAT。
    *   跨域/混合数据集：M+F (MNIST + Fashion-MNIST), C+E (CIFAR-10 + EuroSAT)，用于模拟更复杂的 Non-IID 场景。
*   **Non-IID 设置**：采用 Dirichlet 分布划分数据，设置不同的参数 $Dir \in \{0.3, 0.5, 0.7, 0.9\}$，数值越小异构性越强。
*   **Benchmark（对比方法）**：
    *   经典 FL：FedProx, MOON。
    *   原型/蒸馏相关：FedProto, FedAS, E-FPKD, FedALA。
    *   聚类对比：将 CHAC 替换为 K-Means 作为消融对比。

### 4. 资源与算力
*   **模型架构**：使用了 Simple CNN (S-CNN)、ResNet-8 和 ResNet-10 作为骨干网络。
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU 类型、数量）以及总训练时长。文中仅提到了超参数设置：Batch Size 为 32，学习率为 0.001，本地 Epoch 为 5，总通信轮数为 50。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多达 6 个数据集的性能测试。
    *   针对客户端数量（10, 20, 50）进行了可扩展性分析。
    *   针对 Non-IID 程度、学习率、Batch Size 进行了参数敏感性分析。
*   **消融实验**：专门验证了 PA 机制和 LEMGP 损失的贡献，并对比了不同聚类算法（CHAC vs K-Means）。
*   **评价**：实验设计非常充分，不仅涵盖了准确率指标，还引入了 RMSE、MAE 和 Macro F1 分数，从多个维度客观评估了模型的鲁棒性和误差控制能力。

### 6. 主要结论与发现
*   **性能领先**：MP-FedKD 在所有测试数据集上的准确率均显著优于基准方法。例如在 EuroSAT 数据集上，相比其他方法提升了 1.98% 至 28.70%。
*   **多原型的优越性**：实验证明，设置多个原型（如 $\zeta=3$）比单原型能更有效地表示复杂数据分布。
*   **聚类算法影响**：基于层次聚类的 CHAC 优于 K-Means，因为它能通过树状图结构保留更多层级特征信息。
*   **鲁棒性**：在极高异构性（Dir=0.3）下，该方法依然能保持较高的平均准确率，显示出极强的抗 Non-IID 能力。

### 7. 优点（亮点）
*   **精细化特征表示**：通过 CHAC 解决了传统原型学习中“特征平均化”导致的信息丢失问题。
*   **损失函数创新**：LEMGP 损失结合了吸引与排斥机制，有效地在特征空间内拉开了类别间距。
*   **通信与隐私平衡**：在不增加原始数据传输的前提下，通过传输少量的多原型显著提升了模型性能。

### 8. 不足与局限
*   **计算复杂度**：层次聚类（HAC）的时间复杂度通常为 $O(N^3)$，虽然论文进行了优化，但在本地样本量极大时，边缘设备的计算压力会显著增加。
*   **通信开销略增**：相比单原型方法，传输“多原型”会略微增加每轮通信的载荷（尽管相比模型参数仍很小）。
*   **硬件描述缺失**：缺乏对实际运行环境（算力消耗）的量化描述，难以评估其在资源受限的真实 MEC 设备上的部署成本。

（完）
