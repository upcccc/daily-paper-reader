---
title: Integrating Feature Correlation in Differential Privacy with Applications in DP-ERM
title_zh: 在差分隐私中整合特征相关性及其在 DP-ERM 中的应用
authors: "Tianyu Wang, Luhao Zhang, Rachel Cummings"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03945v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 差分隐私经验风险最小化
tldr: 本研究针对标准差分隐私（DP）对所有特征施加统一约束而忽略特征敏感性差异的问题，提出了 CorrDP 框架。该框架允许对非敏感特征放宽隐私要求，并利用全变分距离量化其与敏感特征的相关性。研究者设计了适用于 DP-ERM 的算法，通过引入与相关性距离挂钩的噪声来提升模型效用。实验证明，在存在非敏感特征的情况下，CorrDP 在保持隐私的同时，其模型性能显著优于传统 DP 方法。
selection_source: fresh_fetch
motivation: 标准差分隐私忽略了特征间敏感性的异质性，导致在处理非敏感特征时过度保护，从而损害了模型效用。
method: 提出一种考虑特征相关性的 CorrDP 框架，通过全变分距离衡量特征关联，并设计了注入距离依赖噪声的 DP-ERM 算法。
result: 实验结果显示，CorrDP 算法在合成和真实数据集上均取得了比标准 DP 更好的隐私与效用平衡。
conclusion: 整合特征相关性的隐私保护机制能更精细地分配隐私预算，是提升差分隐私模型实用性的有效途径。
---

## 摘要
标准差分隐私对所有特征施加统一的隐私约束，忽略了实践中敏感特征与非敏感特征之间的内在区别。在本文中，我们引入了一种考虑这种异质性的松弛差分隐私定义，允许某些特征即使在与敏感特征相关时也被视为非敏感特征。我们提出了一个相关性感知框架 CorrDP，该框架在放宽非敏感特征隐私要求的同时，考虑了它们与敏感特征的相关性，并使用全变差距离（total variation distance）对相关性进行量化。我们在 CorrDP 框架下设计了差分隐私经验风险最小化（DP-ERM）算法，通过在梯度中加入与距离相关的噪声，以提高理论效用保证。当相关性距离未知时，我们从数据集中对其进行估计，并证明其能够实现相当的隐私-效用权衡。我们在合成数据集和真实数据集上进行了实验，结果表明，在存在非敏感特征的情况下，基于 CorrDP 的 DP-ERM 算法始终优于标准差分隐私框架。

## Abstract
Standard differential privacy imposes uniform privacy constraints across all features, overlooking the inherent distinction between sensitive and insensitive features in practice. In this paper, we introduce a relaxed definition of differential privacy that accounts for such heterogeneity, allowing certain features to be treated as insensitive even when correlated with sensitive ones. We propose a correlation-aware framework, $\textsf{CorrDP}$, which relaxes privacy for insensitive features while accounting for their correlations with sensitive features, with the correlations quantified using total variation distance. We design algorithms for differentially private empirical risk minimization (DP-ERM) under the $\textsf{CorrDP}$ framework, incorporating distance-dependent noise into gradients for improved theoretical utility guarantees. When the correlation distance is unknown, we estimate it from the dataset and show that it achieves a comparable privacy-utility guarantee. We perform experiments on synthetic and real-world datasets and show that $\textsf{CorrDP}$-based DP-ERM algorithms consistently outperform the standard DP framework in the presence of insensitive features.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：在差分隐私中整合特征相关性

#### 1. 核心问题与整体含义（研究动机和背景）
传统的差分隐私（DP）在处理多维数据时，通常假设所有特征具有相同的敏感性，并施加统一的隐私约束。然而，在实际应用中，特征的敏感性往往存在异质性（例如：病历中的“健康状况”高度敏感，而“年龄组”敏感性较低）。
*   **研究动机**：如果对所有特征添加等量噪声，会导致严重的效用损失（Utility Loss）；如果简单地将某些特征视为“公开”而不加保护，由于特征间存在相关性（Correlation），攻击者可能通过非敏感特征推断出敏感特征的信息。
*   **核心目标**：建立一个既能识别特征敏感性差异，又能量化并应对特征间相关性的隐私保护框架，从而在保证严谨隐私证明的同时，提升机器学习模型（尤其是经验风险最小化 ERM）的效用。

#### 2. 方法论：核心思想与技术细节
论文提出了 **$\textsf{CorrDP}$** 框架，其核心在于根据特征间的相关程度动态调整噪声水平。
*   **核心思想**：引入一个依赖于数据库距离的松弛 DP 定义。当相邻数据库在非敏感特征上存在差异时，隐私约束的强度由该特征与敏感特征之间的相关性距离 $d(D, D')$ 决定。
*   **关键技术细节**：
    *   **相关性量化**：使用**全变分距离（Total Variation Distance, TV Distance）**来衡量。具体而言，计算在给定非敏感特征取值的情况下，敏感特征条件分布的差异。
    *   **$\textsf{CorrDP-SGD}$ 算法**：在随机梯度下降（SGD）过程中，对梯度向量的不同维度注入非各向同性的噪声。敏感特征维度维持标准噪声，而非敏感特征维度的噪声规模根据其 TV 距离进行缩减（缩放因子通常为 $\max\{TV(i), m_s^2/m^2\}$）。
    *   **距离估计**：针对实际中相关性距离未知的场景，提出了基于样本的经验估计方法，并引入上置信界（UCB）来补偿估计误差，确保在估计过程中不泄露隐私。
    *   **理论保证**：证明了该方法在凸损失函数下具有更好的效用上界，并给出了接近匹配的理论下界。

#### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据集**：多元高斯分布数据，用于验证线性回归（OLS）性能。
    *   **真实数据集**：Adult（收入预测）、Sepsis（败血症生存预测）、Credit Card（信用卡违约预测）、Medical Cost（医疗费用预测）。
*   **模型类型**：线性回归、逻辑回归（Logistic Regression）、两层全连接神经网络（2-NN）。
*   **Benchmark（对比方法）**：
    1.  **Standard**：标准 DP-SGD，对所有特征添加统一噪声。
    2.  **Semi**：半敏感 DP，仅在敏感特征上加噪（隐私保证较弱，作为效用上限参考）。
    3.  **Partial**：仅使用非敏感特征进行非私密训练（丢弃敏感信息）。
    4.  **$\textsf{CorrDP}$（本文方法）**：考虑相关性的加噪策略。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Apple M1 芯片（8 核 CPU）**和 **16GB RAM** 的笔记本电脑上完成。
*   **软件框架**：使用 PyTorch 和 scikit-learn 实现。
*   **训练时长**：完成论文中所有实验总计耗时约 **40 小时**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 1 个合成数据集和 4 个不同领域的真实数据集，任务涉及分类和回归。
*   **充分性**：实验针对不同的隐私预算 $\epsilon$ 进行了详尽的扫描，并对比了多种模型架构（从线性模型到神经网络）。
*   **公平性**：所有方法在相同的隐私参数（$\epsilon, \delta$）下进行比较，并对神经网络的梯度裁剪等超参数进行了标准化处理。实验结果包含了标准差误差带，体现了结果的统计客观性。

#### 6. 主要结论与发现
*   **效用提升**：在相同的隐私预算下，$\textsf{CorrDP}$ 的效用显著优于标准 DP，尤其是在敏感特征较少而总维度较高的场景下。
*   **接近上限**：$\textsf{CorrDP}$ 的表现非常接近于不考虑相关性风险的 Semi-sensitive 方法，说明其在提供更强隐私保护的同时，几乎没有损失额外的效用。
*   **估计有效性**：即使在相关性距离需要从数据中估计的情况下，该框架依然稳健，证明了其在实际应用中的可行性。

#### 7. 优点
*   **精细化建模**：打破了 DP 中“一刀切”的假设，利用特征相关性实现了更精细的噪声校准。
*   **理论完备**：不仅提出了算法，还提供了严谨的隐私证明、效用上界分析以及下界分析。
*   **扩展性强**：方法不仅适用于广义线性模型（GLM），还成功扩展到了深度神经网络的第一层特征保护。

#### 8. 不足与局限
*   **高维估计挑战**：在特征维度极高时，准确估计全变分（TV）距离可能面临“维度灾难”，计算开销会显著增加。
*   **特征划分依赖**：算法效果高度依赖于预先对“敏感”与“非敏感”特征的准确划分，若划分不当可能影响效用。
*   **假设限制**：部分理论保证（如 Assumption 3.4）要求损失函数对特征变化具有平滑性，这在某些极端非线性或不连续的模型中可能不成立。

（完）
