---
title: Adversarial Attacks on Locally Private Graph Neural Networks
title_zh: 针对本地差分隐私图神经网络的对抗攻击
authors: "Matta Varun, Ajay Kumar Dhakar, Yuan Hong, Shamik Sural"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20746v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 针对本地差分隐私（LDP）保护的图神经网络的对抗攻击
tldr: 本文深入探讨了本地差分隐私（LDP）保护下的图神经网络（GNN）在对抗攻击下的表现。研究分析了LDP的隐私保证如何影响对抗扰动的有效性，评估了现有攻击方法在隐私约束下的局限性，并探讨了在LDP框架下构建对抗样本的挑战。该工作揭示了隐私保护与系统安全之间的复杂关系，为开发兼具鲁棒性和隐私性的GNN架构提供了重要见解和防御方向。
selection_source: fresh_fetch
motivation: 旨在填补本地差分隐私（LDP）在保护GNN隐私时，对其对抗攻击鲁棒性影响研究的空白。
method: 通过分析现有攻击手段在LDP保护的GNN上的表现，探讨隐私约束与对抗扰动之间的相互作用。
result: 分析了LDP对攻击有效性的影响，并识别了在隐私保护环境下实施对抗攻击的具体挑战。
conclusion: 强调了图学习中隐私与安全的交织关系，指出未来需开发能同时兼顾鲁棒性与隐私保护的GNN架构。
---

## 摘要
图神经网络 (GNN) 是分析图结构数据的强大工具。然而，它们对对抗攻击的脆弱性引发了严重关注，尤其是在处理敏感信息时。本地差分隐私 (LDP) 为训练 GNN 提供了一个隐私保护框架，但其对对抗鲁棒性的影响仍有待深入研究。本文研究了针对受 LDP 保护的 GNN 的对抗攻击。我们探讨了 LDP 的隐私保证如何被对抗扰动所利用或阻碍。本文分析了现有攻击方法在受 LDP 保护的 GNN 上的有效性，并讨论了在 LDP 约束下构建对抗样本的潜在挑战。此外，我们提出了防御受 LDP 保护的 GNN 免受对抗攻击的研究方向。这项工作研究了图学习中隐私与安全之间的相互作用，强调了对鲁棒且具有隐私保护能力的 GNN 架构的需求。

## Abstract
Graph neural network (GNN) is a powerful tool for analyzing graph-structured data. However, their vulnerability to adversarial attacks raises serious concerns, especially when dealing with sensitive information. Local Differential Privacy (LDP) offers a privacy-preserving framework for training GNNs, but its impact on adversarial robustness remains underexplored. This paper investigates adversarial attacks on LDP-protected GNNs. We explore how the privacy guarantees of LDP can be leveraged or hindered by adversarial perturbations. The effectiveness of existing attack methods on LDP-protected GNNs are analyzed and potential challenges in crafting adversarial examples under LDP constraints are discussed. Additionally, we suggest directions for defending LDP-protected GNNs against adversarial attacks. This work investigates the interplay between privacy and security in graph learning, highlighting the need for robust and privacy-preserving GNN architectures.

---

## 论文详细总结（自动生成）

这篇论文《针对本地差分隐私图神经网络的对抗攻击》（Adversarial Attacks on Locally Private Graph Neural Networks）深入探讨了在隐私保护机制下，图神经网络（GNN）的安全漏洞。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：研究本地差分隐私（LDP）在保护图数据隐私的同时，如何影响图神经网络（GNN）对对抗攻击的鲁棒性。
*   **研究动机**：虽然 LDP 通过在客户端注入噪声来保护节点特征和标签，但这种噪声是否会引入新的安全漏洞，或者是否能抵御传统的对抗攻击，目前尚不明确。
*   **背景**：GNN 在处理敏感数据（如金融、社交网络）时面临隐私泄露和对抗攻击的双重威胁。现有的 LPGNN（本地隐私 GNN）框架虽然提供了隐私保证，但其在恶意攻击下的表现缺乏系统性评估。

### 2. 论文提出的方法论
论文针对 LPGNN 框架设计并评估了四种不同类型的攻击：
*   **节点注入攻击 (Node Injection Attack)**：黑盒攻击。攻击者向图中注入带有随机特征和标签的恶意节点，并将其连接到高度数节点，试图通过消息传递机制传播噪声，降低模型准确率。
*   **标签翻转攻击 (Label-Flipping Attack)**：白盒攻击。攻击者识别关键节点（如高度数节点）并随机篡改其标签。此攻击发生在 LDP 处理之前。
*   **推理攻击 (Inference Attack)**：黑盒攻击。利用图数据的局部相似性假设，在服务器端对接收到的脱敏（加噪）特征进行邻域均值聚合，试图推断原始特征值。
*   **投毒攻击 (Poisoning Attack)**：灰盒攻击。这是本文的核心贡献。攻击者通过向特征注入特定的“毒素”值（Poison），操纵 LDP 机制中的 Multi-Bit 编码器的伯努利试验概率。
    *   **关键技术**：通过注入特定偏移量 $p$，使得当原始特征为特定值（如 $\alpha$）时，编码输出为 1 的概率变为 0。这样，服务器一旦收到特定输出，就能以 100% 的确定性推断出原始特征，从而彻底瓦解 LDP 的隐私保证。

### 3. 实验设计
*   **数据集**：使用了四个基准图数据集：**Cora、PubMed、Facebook、LastFM**。
*   **GNN 架构**：在 **GCN、GAT 和 GraphSAGE** 三种主流架构上进行了验证。
*   **基准 (Benchmark)**：以未受攻击的 LPGNN 性能作为基准。
*   **对比设置**：
    *   改变注入节点的比例（10% 到 90%）。
    *   改变标签翻转的比例（1% 到 50%）。
    *   调整隐私预算 $\epsilon$（1 到 100）以观察隐私与鲁棒性的权衡。
    *   评估推理攻击的余弦相似度和平均特征差异。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。实验主要侧重于攻击成功率和准确率的变化分析。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个数据集和 3 种模型上进行了交叉实验，涵盖了从攻击强度到隐私参数的多维度分析。
*   **充分性**：实验设计较为全面，不仅测试了传统的结构攻击，还深入分析了针对隐私机制本身的投毒攻击。
*   **客观性**：通过对比不同 $\epsilon$ 值下的表现，客观展示了 LDP 噪声在某些情况下（如节点注入）反而可能掩盖攻击效果的现象。

### 6. 论文的主要结论与发现
*   **节点注入攻击效果有限**：在 LPGNN 中，由于 LDP 本身引入了大量噪声，随机注入节点的干扰往往被隐私噪声掩盖，导致准确率下降不明显。
*   **标签翻转攻击依然有效**：该攻击能显著且线性地降低模型准确率，说明 LPGNN 对标签层面的恶意篡改缺乏抵抗力。
*   **简单推理攻击失效**：由于 Multi-Bit 机制将特征映射到了极大的输出域，简单的邻域聚合推理难以恢复原始特征。
*   **投毒攻击极具威胁**：本文提出的投毒攻击在所有数据集上达到了 **97% 至 100% 的推理成功率**，证明了现有的 LPGNN 隐私编码机制存在严重的逻辑漏洞，攻击者可以绕过 $\epsilon$ 的限制获取真实数据。

### 7. 优点
*   **视角独特**：首次系统性地研究了 LDP 与 GNN 对抗安全性之间的交集。
*   **攻击设计精巧**：提出的投毒攻击利用了 LDP 算法底层的数学特性（伯努利分布的边界条件），具有很强的理论说服力和实战破坏力。
*   **防御启示**：论文在附录中提出了简单的防御建议（如特征值范围校验），具有实际指导意义。

### 8. 不足与局限
*   **算力细节缺失**：缺乏实验环境的详细描述，不利于复现性评估。
*   **攻击场景假设**：投毒攻击假设攻击者已知 LDP 的编码参数（如 $\alpha, \beta, m$），在完全黑盒的极端环境下可能难以实施。
*   **防御研究较薄弱**：论文重点在于“破”，对于如何“立”（构建更鲁棒的隐私保护架构）仅做了初步探讨，未提出完整的防御算法框架。

（完）
