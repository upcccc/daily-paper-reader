---
title: Compressed Distributed Stochastic Nonconvex Optimization with Differential Privacy
title_zh: 具有差分隐私的压缩分布式随机非凸优化
authors: "Antai Xie, Xiaoqiang Ren, Xinlei Yi, Tao Yang, Xiaofan Wang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21640v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 具有差分隐私的分布式随机非凸优化
tldr: "本文研究了具有通信压缩和差分隐私的分布式随机非凸优化问题，提出了一种新型分布式随机梯度下降算法。该算法支持包含相对和绝对误差的通用压缩算子，在平滑非凸函数下实现了线性加速收敛率，并在满足P-L条件时表现出更快的收敛速度。研究证明该算法能在不牺牲收敛精度的前提下实现(0,δ)-差分隐私，有效解决了分布式学习中的通信效率与隐私保护难题。"
selection_source: fresh_fetch
motivation: 针对分布式优化中高昂的通信成本和潜在的数据隐私泄露风险，寻求一种兼顾效率、安全与收敛性能的解决方案。
method: 提出一种压缩分布式随机梯度下降算法，通过集成通用压缩算子和差分隐私机制来处理非凸优化任务。
result: 算法在非凸及P-L条件下均达到线性加速收敛率，且在实现隐私保护的同时不损失优化精度。
conclusion: 该算法成功实现了通信压缩、差分隐私与高效非凸优化的有机结合，为分布式机器学习提供了理论保障。
---

## 摘要
本文研究了具有通信压缩和差分隐私的分布式随机非凸优化问题，其中每个代理旨在通过局部压缩信息交换来最小化所有代理成本函数的总和。为此，我们提出了一种压缩分布式随机梯度下降算法，该算法在一类允许相对和绝对压缩误差的通用压缩算子下具有鲁棒性。随后，我们证明了所提算法能够以 $\mathcal{O}(1/\sqrt{nT})$ 的线性加速收敛率找到平滑非凸函数的一阶驻点；如果全局成本函数额外满足 Polyak--Łojasiewicz (P--Ł) 条件，则算法以 $\mathcal{O}(1/(nT^θ)),θ\in(0,1)$ 的收敛率收敛至最优解，其中 $T$ 是总迭代次数，$n$ 是代理数量。此外，如果预先知道 P--Ł 常数，我们证明所提算法可以达到 $\mathcal{O}(1/(nT))$ 的收敛率。最后，我们证明了所提算法能够在不牺牲收敛精度的情况下实现 $(0,δ)$-差分隐私。进行了数值实验以...

## Abstract
This paper studies distributed stochastic nonconvex optimization problems with compressed communication and differential privacy, in which each agent aims to minimize the sum of all agents' cost functions by using local compressed information exchange. To this end, we propose a compressed distributed stochastic gradient descent algorithm, which is robust under a general class of compression operators that allow both relative and absolute compression errors. We then show that the proposed algorithm finds the first-order stationary point for smooth nonconvex functions with the linear speedup convergence rate $\mathcal{O}(1/\sqrt{nT})$ and converges to the optimum if the global cost function additionally satisfies the Polyak--Łojasiewicz (P--Ł) condition with the convergence rate $\mathcal{O}(1/(nT^θ)),θ\in(0,1)$, where $T$ is the total number of iterations and $n$ is the number of agents. Furthermore, if the P--Ł~constant is known in advance, we show that the proposed algorithm achieves a convergence rate $\mathcal{O}(1/(nT))$. Finally, we show that the proposed algorithm is able to achieve $(0,δ)$-differential privacy without sacrificing convergence accuracy. Numerical experiments are carried out to

---

## 论文详细总结（自动生成）

这篇论文题目为《具有差分隐私的压缩分布式随机非凸优化》（Compressed Distributed Stochastic Nonconvex Optimization with Differential Privacy），由上海大学、同济大学和东北大学的研究人员合作完成。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在分布式机器学习和多智能体系统中，各代理（Agent）通过协作解决全局优化问题。然而，实际应用面临两大挑战：
*   **通信瓶颈**：代理间频繁交换高维梯度数据，受限于网络带宽。
*   **隐私泄露**：直接交换原始数据或梯度可能导致本地敏感信息泄露。
*   **研究动机**：现有的压缩分布式算法大多针对凸优化，且实现差分隐私（DP）通常需要添加外部噪声（如高斯噪声），这往往会损害算法的收敛精度。本文旨在设计一种既能压缩通信、又能保护隐私，且在非凸条件下保持高精度收敛的鲁棒算法。

### 2. 方法论：核心思想与关键技术
论文提出了一种名为 **RCP-SGD（鲁棒压缩原对偶随机梯度下降）** 的算法：
*   **核心思想**：结合原对偶（Primal-Dual）框架与梯度追踪技术，利用压缩后的信息进行状态更新，以抵消压缩带来的偏差。
*   **通用压缩算子**：算法支持一类通用的随机压缩算子，允许同时存在相对压缩误差和绝对压缩误差，涵盖了大多数现有的有偏和无偏压缩方法。
*   **隐私增强变换（核心创新）**：提出了一种不需要添加额外噪声的差分隐私实现方法。通过对压缩器进行变换：以概率 $q$ 输出 0，以概率 $1-q$ 输出正常压缩值。这种“混淆”机制在减少通信频率的同时，实现了 $(0, \delta)$-差分隐私。
*   **收敛性保证**：
    *   对于平滑非凸函数，达到 $O(1/\sqrt{nT})$ 的线性加速收敛率。
    *   对于满足 P-Ł 条件的函数，若已知常数，可达到 $O(1/(nT))$ 的收敛率。

### 3. 实验设计
*   **实验场景**：分布式二分类问题（非凸逻辑回归）。
*   **数据集**：使用乳腺癌（Breast Cancer）数据集进行模型训练。
*   **Benchmark 与对比方法**：
    *   **DSGD**（分布式随机梯度下降）：标准基准。
    *   **Choco-SGD**：经典的通信压缩分布式算法。
    *   **unRCP-SGD**：未压缩版本的本算法。
*   **压缩器测试**：对比了偏置 b-bits 量化器、Sign-norm 压缩器及其改进版本。
*   **拓扑结构**：在环形图（Ring）、环面图（Torus）和全连接图（Fully connected）上验证了鲁棒性。

### 4. 资源与算力
*   论文中**未明确说明**具体的硬件型号（如 GPU/CPU 数量）或具体的训练时长。
*   实验侧重于算法逻辑的验证，主要通过迭代次数（Iterations）和传输比特数（Transmitted bits）来衡量效率，而非物理计算时间。

### 5. 实验数量与充分性
*   **实验数量**：论文展示了约 4 组主要实验图表，涵盖了残差随通信量变化的曲线、不同拓扑结构的对比、以及针对隐私保护效果的攻击模拟。
*   **充分性与公平性**：
    *   实验对比了多种压缩器，证明了算法的广泛适用性。
    *   引入了 **DLG（深度梯度泄露）攻击** 模拟，直观展示了算法在面对恶意攻击时的隐私保护能力。
    *   实验设计较为客观，但数据集规模相对较小，主要用于验证理论结果的正确性。

### 6. 主要结论与发现
*   **线性加速**：RCP-SGD 能够随代理数量 $n$ 的增加而线性提升收敛速度，证明了分布式协作的有效性。
*   **隐私与精度兼得**：通过压缩器变换实现的差分隐私不会像传统加噪方法那样导致收敛精度的永久性损失（即依然能收敛到最优解附近）。
*   **通信效率**：改进后的压缩器（如 C3, C4）在保持精度的同时，显著降低了传输比特数。

### 7. 优点与亮点
*   **鲁棒性强**：对压缩算子的要求极低，适应性广。
*   **无噪隐私机制**：巧妙地利用压缩过程中的“不确定性”来实现隐私保护，避免了精度与隐私之间的剧烈权衡。
*   **理论完备**：在非凸和 P-Ł 条件下均给出了严谨的收敛率证明，且参数设计不依赖于具体的网络拓扑。

### 8. 不足与局限
*   **网络限制**：目前的研究仅限于无向图，未扩展到有向图或时变网络。
*   **隐私权衡**：虽然不牺牲最终精度，但隐私保护水平 $q$ 的提高会降低收敛速度（需要更多迭代次数）。
*   **实验规模**：实验仅在较小的数据集上进行，缺乏在大规模深度学习任务（如 ImageNet 训练）上的性能验证。
*   **参数依赖**：算法性能对超参数（如步长、压缩概率 $q$）的选取较为敏感，实际部署时可能需要精细调优。

（完）
