---
title: "FedSLoP: Memory-Efficient Federated Learning with Low-Rank Gradient Projection"
title_zh: FedSLoP：基于低秩梯度投影的内存高效联邦学习
authors: "Yutong He, Zhengyang Huang, Jiahe Geng"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24012v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 资源受限环境下分布式机器学习的联邦优化算法
tldr: 针对联邦学习在资源受限环境下面临的通信和内存开销大、收敛慢等挑战，本文提出了一种名为 FedSLoP 的优化算法。该算法通过梯度的随机低秩子空间投影，在保留优化进度的同时显著降低了更新数据的维度。理论证明其在非凸条件下具有收敛性，实验表明该方法在减少通信量和内存占用的同时，保持了优于或等同于 FedAvg 的准确率，为高效联邦学习提供了新方案。
selection_source: fresh_fetch
motivation: 传统的联邦学习算法在异构和资源受限的环境中存在通信成本高、内存消耗大以及收敛速度慢的问题。
method: 提出 FedSLoP 算法，利用梯度的随机低秩子空间投影来减少通信和存储的更新维度。
result: 实验证明 FedSLoP 在显著降低通信量和客户端内存需求的同时，达到了与 FedAvg 相当或更好的分类准确率。
conclusion: 随机子空间动量方法为实现通信和内存高效的联邦学习提供了一种原则性且有效的途径。
---

## 摘要
联邦学习使大量客户端能够在不交换原始数据的情况下协作训练机器学习模型，但标准算法（如 FedAvg）在异构、资源受限的环境中面临收敛缓慢以及高昂的通信和内存成本。我们提出了 FedSLoP，这是一种联邦优化算法，它结合了梯度的随机低秩子空间投影，从而在保持优化进度的同时，降低了通信和存储更新的维度。在理论方面，我们在标准的平滑性和有界方差假设下，进行了详细的非凸收敛性分析，证明了 FedSLoP 能够以 $O(1/\sqrt{NT})$ 的速率收敛到一阶驻点。在实证方面，我们在具有异构数据划分的联邦 MNIST 分类任务上进行了广泛的实验，结果表明，与 FedAvg 以及代表性的稀疏或低秩基准方法相比，FedSLoP 在显著减少通信量和客户端内存的同时，实现了具有竞争力或更好的准确率。总之，我们的结果表明，像 FedSLoP 这样的随机子空间动量方法为实现通信和内存高效的联邦学习提供了一种有原则且有效的方法。代码可在以下网址获取：https://github.com/pkumelon/FedSLoP.git。

## Abstract
Federated learning enables a population of clients to collaboratively train machine learning models without exchanging their raw data, but standard algorithms such as FedAvg suffer from slow convergence and high communication and memory costs in heterogeneous, resource-constrained environments. We introduce FedSLoP, a federated optimization algorithm that combines stochastic low-rank subspace projections of gradients, thereby reducing the dimension of communicated and stored updates while preserving optimization progress. On the theoretical side, we develop a detailed nonconvex convergence analysis under standard smoothness and bounded-variance assumptions, showing that FedSLoP is guaranteed to converge to a first-order stationary point at a rate of $O(1/\sqrt{NT})$. On the empirical side, we conduct extensive experiments on federated MNIST classification with heterogeneous data partitions, showing that FedSLoP substantially reduces communication volume and client-side memory while achieving competitive or better accuracy compared with FedAvg and representative sparse or low-rank baselines. Together, our results demonstrate that random subspace momentum methods such as FedSLoP provide a principled and effective approach to communication- and memory-efficient federated learning. Codes are available at: https://github.com/pkumelon/FedSLoP.git.

---

## 论文详细总结（自动生成）

这是一份关于论文《FedSLoP: Memory-Efficient Federated Learning with Low-Rank Gradient Projection》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在资源受限的边缘设备上，标准的联邦学习算法（如 FedAvg）面临三大挑战：**通信开销大**（需传输全量参数）、**内存占用高**（需存储全量模型及动量缓冲区）以及**数据异构性（Non-IID）导致的收敛缓慢**。
*   **研究背景**：现有的通信压缩技术（如量化、稀疏化）虽能减少传输比特，但通常仍需在客户端维护全维度的优化器状态。而低秩适配（如 LoRA）虽减少了参数量，但限制了模型的表达能力。本文旨在寻找一种既能显著降低客户端内存和通信成本，又能保持模型全维度优化能力的联邦优化方案。

### 2. 论文提出的方法论
*   **核心思想**：提出 **FedSLoP** 算法，通过在每一轮通信中随机采样一个低秩子空间，并将梯度和动量均投影至该子空间内进行更新。
*   **关键技术细节**：
    *   **随机投影**：服务器在每轮 $t$ 开始时，从 Stiefel 流形 $St(d, r)$ 中均匀采样一个正交基矩阵 $P_t$，并构建投影矩阵 $\Pi_t = P_t P_t^\top$。
    *   **投影动量更新**：客户端在本地训练时，不直接使用原始随机梯度 $g_{i,s}$，而是更新投影后的动量：$v_{i,s+1} = \mu v_{i,s} + \Pi_t g_{i,s}$。
    *   **模型更新**：利用投影后的动量更新全维度模型：$\theta_{i,s+1} = \theta_{i,s} - \eta v_{i,s+1}$。
    *   **内存优化**：客户端无需存储 $d$ 维动量，仅需存储 $r$ 维的投影状态（$v = P_t w$），当 $r \ll d$ 时，内存需求大幅下降。
*   **理论保证**：在非凸目标函数下，证明了该算法具有 $O(1/\sqrt{NT})$ 的收敛速率，且在客户端数量 $N$ 上实现了线性加速。

### 3. 实验设计
*   **数据集与场景**：使用 **MNIST** 手写数字识别数据集。通过 Dirichlet 分布（参数 $\alpha$）模拟非独立同分布（Non-IID）场景，$\alpha$ 越小表示异构性越强。
*   **Benchmark（基准）**：以带服务器端动量的全参数 **FedAvg-M** 作为性能上限。
*   **对比方法**：
    *   **稀疏/部分更新类**：FedMef（动态剪枝）、NeuLite（弹性训练）、FederatedSelect（坐标选择）。
    *   **低秩模型类**：FedLoRA-M（模型级低秩适配）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
*   **复现性**：作者提供了 GitHub 代码仓库链接，并给出了超参数配置（如学习率 $\eta=0.018$、动量 $\mu=0.8$、Batch Size=32 等）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：对比了 6 种算法在 $\alpha=0.1$ 异构条件下的收敛曲线和最终精度。
    *   **敏感性分析**：针对异构程度 $\alpha$（0.05 到 1.0）进行了 4 组对比。
    *   **消融实验**：针对投影秩 $r$（16 到 192）和客户端数量 $p$（10 到 100）进行了多组实验。
*   **充分性评价**：实验设计较为全面，覆盖了联邦学习的核心挑战（异构性、压缩率、扩展性）。对比方法涵盖了当前主流的内存/通信高效方案，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能优异**：在 1/7 的压缩率下，FedSLoP 的精度（95.11%）极度接近全参数 FedAvg-M（95.51%），且显著优于其他稀疏化或低秩模型方法。
*   **异构鲁棒性**：在极端异构（$\alpha=0.05$）环境下，FedSLoP 的表现甚至超过了 FedAvg-M（约高出 2%），表明随机子空间投影可能具有某种降噪或正则化作用，能抑制 Non-IID 带来的负面影响。
*   **边际效应**：投影秩 $r$ 存在饱和点，当 $r$ 达到一定规模后，继续增加秩对精度的提升非常微小，但通信成本会线性增加。

### 7. 优点
*   **内存效率极高**：通过在子空间内维护动量，直接解决了深度学习模型在边缘设备上最严重的内存瓶颈（优化器状态）。
*   **理论与实证结合**：不仅有严谨的非凸收敛性证明，还在极具挑战性的 Non-IID 场景下验证了实用性。
*   **无缝集成**：该方法不改变模型架构，仅改变优化器逻辑，易于集成到现有的联邦学习框架中。

### 8. 不足与局限
*   **任务复杂度有限**：实验仅限于 MNIST 数据集和简单的 MLP 模型，尚未在更复杂的视觉任务（如 ImageNet）或大语言模型（LLM）上验证其有效性。
*   **算力开销未量化**：虽然减少了内存和通信，但频繁的投影操作（矩阵乘法）在极低功耗设备上的计算开销未做详细评估。
*   **理论假设限制**：收敛性分析主要基于全参与（Full Participation）假设，对于实际中常见的客户端部分参与（Partial Participation）情况，理论分析尚不完善。

（完）
