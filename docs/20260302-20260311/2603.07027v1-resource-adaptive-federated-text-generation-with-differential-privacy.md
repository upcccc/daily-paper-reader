---
title: Resource-Adaptive Federated Text Generation with Differential Privacy
title_zh: 具有差分隐私的资源自适应联邦文本生成
authors: "Jiayi Wang, John Gounley, Heidi Hanson"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07027v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 具有差分隐私的联邦文本生成
tldr: 针对跨机构联邦学习中计算资源异构和隐私保护的挑战，本文提出一种资源自适应的差分隐私（DP）文本生成框架。该框架允许资源充足的客户端进行DP微调，而资源受限的客户端通过轻量级DP投票机制优化合成数据。通过引入控制代码确保数据分布一致性，该方法在保护隐私的同时，有效整合了所有参与者的贡献，提升了合成数据的质量和下游任务的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的联邦学习在处理文本数据时面临计算资源不均、隐私泄露风险以及预训练模型领域偏移的问题。
method: 提出一种结合强客户端DP微调与弱客户端轻量级DP投票的两阶段框架，并利用控制代码引导合成文本生成。
result: 实验证明该框架在差分隐私和异构环境下显著提升了合成数据的分布对齐度及下游任务的性能。
conclusion: 该研究为异构联邦环境下的隐私保护文本生成提供了一种高效且灵活的解决方案。
---

## 摘要
在跨机构联邦学习（FL）中，由于隐私法规的限制，敏感文本数据集仍局限于本地组织，这使得针对每个下游任务的重复训练既耗费通信资源又对隐私保护有极高要求。一个极具前景的替代方案是生成近似全局分布且可跨任务重用的差分隐私（DP）合成数据集。然而，预训练大语言模型（LLMs）在领域偏移下往往表现不佳，且联邦微调受到计算异构性的阻碍：只有资源丰富的客户端才能更新模型，而性能较弱的客户端则被排除在外，这放大了数据偏斜和差分隐私噪声的负面影响。我们提出了一种能够适应客户端能力的灵活参与框架。强客户端执行差分隐私联邦微调，而弱客户端则通过一种轻量级的差分隐私投票机制来优化合成文本。为了确保合成数据能够反映全局数据集，我们应用了代表每个客户端数据比例的控制代码（如标签、主题、元数据），并将投票限制在语义一致的子集中。这种两阶段方法对弱客户端仅需一轮通信，并整合了所有参与者的贡献。实验表明，我们的框架在差分隐私和异构性条件下提高了分布对齐度和下游鲁棒性。

## Abstract
In cross-silo federated learning (FL), sensitive text datasets remain confined to local organizations due to privacy regulations, making repeated training for each downstream task both communication-intensive and privacy-demanding. A promising alternative is to generate differentially private (DP) synthetic datasets that approximate the global distribution and can be reused across tasks. However, pretrained large language models (LLMs) often fail under domain shift, and federated finetuning is hindered by computational heterogeneity: only resource-rich clients can update the model, while weaker clients are excluded, amplifying data skew and the adverse effects of DP noise. We propose a flexible participation framework that adapts to client capacities. Strong clients perform DP federated finetuning, while weak clients contribute through a lightweight DP voting mechanism that refines synthetic text. To ensure the synthetic data mirrors the global dataset, we apply control codes (e.g., labels, topics, metadata) that represent each client's data proportions and constrain voting to semantically coherent subsets. This two-phase approach requires only a single round of communication for weak clients and integrates contributions from all participants. Experiments show that our framework improves distribution alignment and downstream robustness under DP and heterogeneity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Resource-Adaptive Federated Text Generation with Differential Privacy》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在跨机构联邦学习（Cross-silo FL）中，保护敏感文本隐私的同时生成高质量合成数据是核心挑战。
*   **研究动机**：直接在联邦环境下为每个下游任务训练模型通信开销巨大且隐私风险高。生成差分隐私（DP）合成数据集作为替代方案更具吸引力。
*   **核心问题**：
    1.  **领域偏移**：预训练大模型（LLM）直接生成的文本往往无法匹配特定领域的全局分布。
    2.  **计算异构性**：并非所有客户端都有能力微调 LLM。若只让资源丰富的“强客户端”参与，会导致模型偏向强客户端的数据分布，产生严重的“数据偏斜”。
    3.  **DP 噪声放大**：在参与者有限的情况下，DP-SGD 引入的噪声会严重破坏生成文本的质量。

### 2. 方法论：核心思想与技术细节
论文提出了一种**资源自适应的灵活参与框架**，将过程分为两个阶段：

*   **核心思想**：利用强客户端进行生成能力的“粗调”，利用弱客户端通过轻量级投票进行分布的“精炼”。
*   **关键技术细节**：
    1.  **控制代码（Control Codes）**：使用标签、主题或元数据作为控制代码。它们有两个作用：一是代表客户端的数据比例（Profiling），指导合成样本的分配；二是约束投票范围，确保语义一致性。
    2.  **阶段一：DP 联邦微调**：资源充足的客户端（$C_s$）使用 DP-SGD 算法对全局生成模型（如 GPT-2）进行微调，捕捉领域语言模式。
    3.  **阶段二：DP 投票精炼**：
        *   **Profiling**：所有客户端发送受 DP 保护的控制代码计数，服务器据此决定各类别生成的样本量。
        *   **Voting**：资源受限的客户端（$C_r$）不进行反向传播，仅对服务器生成的候选文本进行 K-最近邻（KNN）投票（基于句子嵌入相似度）。
        *   **重采样**：服务器聚合带噪声的投票结果，对合成数据进行重加权和重采样，以对齐全局分布。

### 3. 实验设计
*   **数据集**：
    *   **Yelp Reviews**：用于通用的商业评论生成，控制代码为类别和评分。
    *   **PubMed Abstracts**：用于医学领域的专业文本生成，控制代码为 MeSH 医学主题词。
*   **场景设置**：涵盖了 IID（独立同分布）和 Non-IID（非独立同分布，模拟数据偏斜）设置。
*   **Benchmark 与对比方法**：
    *   **Pretrained**：直接使用预训练模型进行零样本生成。
    *   **$\epsilon = \infty$**：无隐私保护的联邦微调（性能上限）。
    *   **DP Finetuning (No Refinement)**：仅进行 DP 微调，不进行弱客户端投票。
    *   **对比维度**：改变强客户端占比（1% 到 40%）、隐私预算（$\epsilon=4, 8$）。

### 4. 资源与算力
*   **硬件环境**：使用了 NVIDIA DGX 系统，配备 **8 块 H100 GPU (80GB)**，2 个 AMD EPYC 9654 CPU，以及 2TB 系统内存。
*   **软件框架**：基于 PyTorch、Hugging Face Transformers 和 Opacus（用于 DP 训练）。
*   **训练细节**：针对 GPT-2 和 GPT-2-Large 进行了不同轮数的微调（R=50 到 200 轮不等），并测试了 LLaMA-7B 结合 LoRA 的性能。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的实验，包括两个不同领域的文本数据集、多种强/弱客户端比例、IID 与多种 Non-IID 偏斜策略。
*   **消融实验**：对采样率（$r$）、投票数（$K$）、生成温度（Temperature）等超参数进行了详细的消融研究。
*   **客观性与公平性**：实验对比了不同模型规模（GPT-2 到 LLaMA），并使用了下游任务准确率（Accuracy）、F1 分数、MAUVE 分数（分布对齐度）和 NER 任务表现等多种指标，评估较为全面且客观。

### 6. 主要结论与发现
1.  **微调的必要性**：即使只有 1% 的强客户端参与，DP 微调后的生成效果也显著优于纯预训练模型的零样本生成。
2.  **投票机制的有效性**：精炼阶段（Refinement）能显著提升性能，尤其是在强客户端占比极低或数据高度偏斜的情况下，它能有效纠正微调阶段产生的分布偏差。
3.  **抵消 DP 噪声**：投票机制能缓解 DP-SGD 引入的噪声负面影响。在某些 Non-IID 场景下，带精炼的 DP 模型表现甚至接近非隐私模型。
4.  **资源友好**：弱客户端仅需一轮通信且无需计算梯度，极大地提高了联邦学习的包容性。

### 7. 优点：亮点与创新
*   **包容性设计**：首次明确针对跨机构 FL 中的“计算异构性”提出解决方案，不抛弃低算力节点。
*   **两阶段解耦**：将“学习生成能力”与“对齐数据分布”解耦，降低了对单一微调过程的依赖。
*   **隐私与效用的平衡**：通过控制代码和 KNN 投票，在严格的 DP 保证下实现了较高的合成数据质量。

### 8. 不足与局限
*   **控制代码的假设**：假设控制代码（如标签）是预先存在且非私有的，但在某些极端隐私场景下，标签分布本身可能也属于敏感信息。
*   **Embedding 依赖**：投票机制的效果高度依赖于句子编码器（Sentence Transformer）的质量，若编码器在特定领域表现不佳，投票可能失效。
*   **通信负载**：虽然弱客户端计算负担轻，但服务器需要向其广播大量的候选合成文本以供投票，这可能在带宽受限的环境下产生通信瓶颈。
*   **任务局限**：目前主要验证了分类和 NER 等下游任务，对于更复杂的长文本逻辑生成任务验证尚不充分。

（完）
