Title: Efficient Proposal-Test-Release for Minimax Optimal Estimation

URL Source: https://arxiv.org/pdf/2605.03264v1

Published Time: Wed, 06 May 2026 00:29:21 GMT

Number of Pages: 32

Markdown Content:
# Efficient Proposal-Test-Release for Minimax Optimal Estimation 

## Tao Shen, Xin T. Tong and Wanjie Wang ∗

## May 6, 2026 

Abstract 

Differential privacy (DP) is a rigorous framework that protects the par-ticipation of individuals in a dataset by limiting information leakage from released estimators. This creates a challenging setting for statisticians: DP must hold uniformly over all possible datasets, whereas statistical practice often downweights atypical or rare outcomes. The conceptual challenge is especially pronounced in sensitivity analysis—the key quan-tity governing the magnitude of DP noise and, consequently, estimator accuracy—because many estimators, including ordinary least squares for linear regression, exhibit markedly higher sensitivity on atypical datasets. Propose–Test–Release (PTR) is designed to address such cases, but its classical implementation requires computing the exact insensitive set and the dataset’s Hellinger distance to that set, both of which are typically intractable. We introduce efficient PTR (ePTR), which replaces the exact insensitive set with a simpler subset and the exact Hellinger distance with a Lipschitz-based lower bound. This flexibility enables substantially sim-pler DP mechanisms that achieve rate-optimal accuracy in many settings. To illustrate, we study basic estimators for Bayes classification, linear regression, and nonparametric regression. We show that each can have high sensitivity on atypical datasets, yet admits intuitive ePTR-based de-signs. Empirically and theoretically, we compare ePTR against popular DP baselines and demonstrate improved accuracy while maintaining pri-vacy guarantees. 

# 1 Introduction 

In the modern era, data privacy is foundational to trust in digital society. As institutions collect and process vast amounts of personal information—from lo-cation traces to health records—individuals face heightened risks of identity theft and discrimination. Data privacy protects autonomy and dignity by lim-iting misuse and ensuring that data is handled transparently and securely. It 

> ∗

wanjie.wang@nus.edu.sg 

1

> arXiv:2605.03264v1 [stat.ME] 5 May 2026

can also enable innovation and economic growth: when people trust that their data is safeguarded, they are more willing to adopt new services. The importance of data privacy has been recognized for decades. Early approaches often relied on anonymization—removing or masking personal in-formation to prevent identification. Although many ad hoc schemes were pro-posed, most proved unsafe: a determined adversary can frequently re-identify individuals from “anonymized” datasets, see e.g. [1, 2]. Differential privacy (DP), introduced by Dwork et al. [3], provides a rigorous privacy framework. Its core idea is to inject carefully calibrated randomness into queries or model training so that the presence or absence of any single person’s data does not substantially change the output distribution. Formally, an algorithm or estimator ˆθ is ( ϵ, δ )-DP if the following holds 

P (ˆθ(X ) ∈ B ) ≤ P (ˆθ(X ′) ∈ B )eϵ + δ. (1) for set B and neighboring datasets X and X ′. Here a dataset X = {z1, . . . , z n}

is a considered to be a neighbor of X = {z′

> 1

, . . . , z ′

> n

} if their Hellinger distance, 

DH (X , X ′) = 1. In other words, zi = z′ 

> i

for all i ∈ [n] except one index j.Because of its principled formulation, DP is future-proof and has become widely adopted. Numerous noise-injection schemes have been developed to guar-antee DP, and they now appear in real-world deployments such as real-world deployments such as government statistics [4], data aggregation [5], and privacy-preserving deep learning [6]. However, these mechanisms are often conservative in their noise calibration, which can lead to large effective privacy budgets (in-flated ϵ) and degraded utility. For example, typical choices of range from 1 to 10 in stochastic gradient descent [7] and large language models [8]; by (1), this implies that an adversary could potentially reject the null hypothesis that the dataset is X against X ′ at a p-value as small as 4 .54 × 10 −5—a level of privacy many statisticians would consider weak. Statisticians are natural candidates to address this tension. The field has a long tradition of analyzing the performance of estimators (now often called “al-gorithms”) and is theoretically driven, seeking accuracy, efficiency, and power. Accordingly, there is a growing literature on minimax-optimal rates for DP esti-mation and matching procedures [9, 10]. Yet many statisticians we have spoken with find DP “hard”, and there have been attempts to replace it with relaxed versions [7, 11]. We argue this perception partly stems from a mindset gap between statistical practice and DP’s deterministic requirements. Since Jacob Bernoulli’s formulation of the Law of Large Numbers in [12], statisticians—both frequentist and Bayesian—have viewed data as random out-comes from a probabilistic model. In this worldview, it is reasonable to focus on high-probability events and downweight the rest. Graduate training reinforces this approach through tools like the law of large numbers, the central limit theo-rem, and concentration inequalities, all aimed at studying estimators on typical events. Unfortunately, this mindset is not appropriate in the DP setting. DP does not assume a probabilistic data-generating process; it requires (1) to hold for all neighboring datasets X and X ′, including low-probability, atypical out-comes where estimators may behave irregularly. This deterministic view of data 2clashes with our default probabilistic perspective. Our statistical training leaves a blind spot for handling such atypical sets, leading many to conclude that “DP is hard.” 

## 1.1 Global sensitivity and Ordinary least square {sec:OLSsen }

To illustrate our point, we will investigate a standard way to construct an DP estimator–the Gaussian mechanism [13]. Given an estimator ˆθ ∈ Rp, the global 

sensitivity is defined as ∆ˆθ = max      

> X,X′:D(X,X′)=1

∥ˆθ(X ) − ˆθ(X ′)∥. (2) When ∆ ˆθ is given, Gaussian mechanism indicates we can use ˜θ(X ) = ˆθ(X ) + 2

ϵ

plog(1 .25 /δ )∆ ˆθ ζ, ζ ∼ N (0 , I p) (3) to achieve ( ϵ, δ )-DP. We will refer the noise injection 2

> ϵ

plog(1 .25 /δ )∆ ˆθ ζ as DP noise. It blurs the deterministic output ˆθ(X ), so an adversary cannot tell it from ˆθ(X ′). On the other hand, it also reduces the accuracy of ˜θ(X ) as an estimator. As a pedagogical example, let us consider a simple univariate linear regres-sion, where we try to estimate θ in a model with n data points 

yi = θx i + ϵi.

For simplicity, we assume xi and ϵi are both i.i.d. samples from Unif[ −1, 1]. In statistics 101, we all learned the ordinary least square (OLS) estimator, ˆθ(X ) = 

P 

> j∈[n]

xj yj

P 

> j∈[n]

x2

> j

.

Suppose X and X ′ differs only over ( xi, y i), then we can easily obtain that 

∥ˆθ(X ) − ˆθ(X ′)∥ = ∥(xiyi − x′

> i

y′

> i

) P  

> j̸=i

x2 

> j

+ (( x′

> i

)2 − x2 

> i

) P  

> j̸=i

xj yj ∥

(P 

> j∈[n]

x2 

> j

)( P

> j∈[n]

(x′ 

> j

)2) . (4) If a statistician is asked to estimate the scale of (4), it is very natural to think that 1) the nominator is can be bounded as an O(n) constant; 2) the typical scale of the denominator is n2. As consequence, the statistician will think 

∥ˆθ(X ) − ˆθ(X ′)∥ = O( 1 

> n

). If we can use ∆ ˆθ = O( 1 

> n

), the DP injection noise is weak and o(1), assuming ϵ ≫ 1/n . In other words, ˜θ can still be a consistent estimator. However, ∥ˆθ(X ) − ˆθ(X ′)∥ = O( 1 

> n

) is only the typical estimate. The global sensitivity ∆ ˆθ (2) needs to hold for all possible X , including the atypical ones. Let us think of an atypical possible data outcomes Xa = {(x1 = u, y 1 = 1) , (xi ≡

0, y i ≡ 0) , i ≥ 2}, and X ′ 

> a

= {(x1 = −u, y 1 = 1) , (xi ≡ 0, y i ≡ 0) , i ≥ 2} we can easily find that |ˆθ(X ) − ˆθ(X ′)| = 2 

> u

. In other words, ∆ ˆθ = ∞ and there is no way to use Gaussian mechanism on the OLS estimator! And if standard DP technique like Gaussian mechanism cannot be applied to basic estimators like OLS, it is natural to have the impression that “DP is hard”. 31.2 Propose-Test-Release: an expensive savior {sec:PTRintro }

From the OLS example, we see that an estimator may have insensitive or robust output for typical datasets. Statisticians are familiar with such properties due to their statistical training in probability theory. However, the estimator can have singular behavior for atypical datasets. This is often overlooked in standard statistical analysis. Unfortunately, DP does not allow such ignorance. Propose-Test-Release (PTR) is a technique proposed in in [14] for robust data scale estimation, and later generalised in chapter 7 of [13] to handle atypical data outcomes. Instead of focusing on the restrictive global sensitivity (2), PTR considers the local sensitivity of an estimator ˆθ at X :∆ˆθ (X ) = max     

> X′:D(X,X′)=1

∥ˆθ(X ) − ˆθ(X ′)∥. (5) It serves a role similar to (4) in our earlier discussion. For many estimators ˆθ,we can build a tighter o(1) upper bound for ∆ ˆθ (X ) ≤ α over typical X . This part should be familiar for statisticians. As its name suggests, PTR involves a three-step procedure: 1. Propose: find a level α on the local sensitivity by studying the typical value of ∆ θ (X ). 2. Test: conduct a DP test on whether X ’s sensitivity fits the propose level. 3. Release: if X passes the DP test, release ˆθ(X ) + p2 log(1 .25 /δ ) αϵ ζ. Oth-erwise, produce “ ⊥”: Here “ ⊥” represents a “no reply” message. In scenarios where one would prefer the output is also in Rp, it can be a random sample from a prefixed distribution like N (0 , I p) or δ0. It is shown in [14] that the final output is DP. The most tricky step of PTR is constructing a DP test. Define all the dataset fitting the local sensitivity level α as sublevel set: 

sub α(∆ ˆθ ) := {X : ∆ ˆθ (X ) ≤ α} (6) We will often write it as sub α and refer it a sub-sensitivity set for simplicity. Naively, one may want to pass X if X ∈ sub α. But this result is not DP. It is possible that there are neighboring X , X ′ so that X ∈ sub α, X ′ ∈ sub cα. Then releasing this naive test result, where X passes but X ′ does not, will allow an adversary to differentiate X from X ′.A DP safe augmentation of this naive test is to release X with higher proba-bility if it is further away from sub cα. Following this idea, it is natural to consider the Hellinger distance of a given X to the sensitive set sub cα:

Dα(X ) := min {DH (X , X ′) : X ′ ∈ sub cα}. (7) A DP test can be establish by passing X with a probability p(X ) monotonically increases with Dα(X ). The exact formulation of p(X ) can be found in algorithm 1. (For later development, we present an equivalent version of [15].) 4Algorithm 1 PTR [15] {alg:PTR }

Require: Data X , tolerance ϵ, δ , Dα(X )

Ensure: An ( ϵ, δ ) output ˜lP T R  

> 1:

Find 

p(A) = 

( 12 δ exp( ϵD α(A)/2) , log(1 /δ ) > 12 ϵD α(A); 1 − 12δ exp( −ϵD α(A)/2) , log(1 /δ ) < 12 ϵD α(A). 

> 2:

Output the following with ζ ∼ N (0 , I ). ˜θP T R =

(

⊥ with prob. 1 − p(A)˜θα = ˆθ + αϵ

p2 log(1 .25 /δ )ζ with prob. p(A).

But with a closer look, implementing algorithm 1 can be computationally challenging. This has been acknowledged in existing PTR works like [16]. The main reason is the test stage: first it is often difficult to have a simple description of the sub cα. To be more specific, in order to check if ∆ ˆθ (X ) ≥ α, one needs to consider the maximal changes of ˆθ(X ′) among all neighbors of X . This is an optimization problem by itself. Looking back at the OLS estimator, the local sensitivity is given by ∆ˆθ (X ) = max ∥(xiyi − x′

> i

y′

> i

) P  

> j̸=i

x2 

> j

+ (( x′

> i

)2 − x2 

> i

) P  

> j̸=i

xj yj ∥

(P 

> j∈[n]

x2 

> j

)( P

> j∈[n]

(x′ 

> j

)2) , (8) where the maximization is taken over all {i ∈ [n], x ′ 

> i

∈ [−1, 1] , y ′ 

> i

∈ [x′ 

> i

− 1, x ′ 

> i

+1] }. It is clear ∆ ˆθ (X ) does not admit any simple analytic solution nor is it easy for us to characterize the sub-senstivity set sub α.To make the matter worse, when using PTR, we need to find Dα(X ), the Hellinger distance to sub cα. In other words, from a dataset X , we needs to find the shortest path connecting it with sub cα, where each step one data point is replaced. This is another layer of optimization. Moreover, finding shortest path in the abstract data graph is not within a typical statistician’s training. 

## 1.3 Main objective and related literature 

This paper proposes a novel efficient PTR (ePTR) procedure that can make adapt classical estimator to DP settings. The new procedure is designed to be friendly to statisticians both conceptually and computationally. We will then demonstrate its effectiveness on three basic statistical problems, Bayes classifier, linear regression and kernel regression. More specifically, this work contributes in the following three directions: 

• By looking at some basic statistic problems, we illustrate that it is actually quite typical for classical statistical estimators to have bad global sensi-5tivity. Meanwhile, we want to inform the community that PTR and our proposed ePTR can be applied on these estimators for a DP adaptation. 

• We propose a novel efficient PTR (ePTR) procedure. It replaces com-putationally challenging terms in the origianl PTR procedures like local sub-sensitivity set sub (∆ ˆθ ) and Hellinger distance Dα with proper “lower bounds”, namely some high probability set G and a sub-distance statistics 

γ. These concepts are more flexible and in many context can be obtained intuitively and computed efficiently. 

• We study the accuracy loss introduces by ePTR. We investigate three on three different statistical problem and their classical estimators. We show that ePTR can adapt them to DP settings while incur minal computational cost. We also demonstrate that ePTR can produce rate optimal accuracy loss. 

• We implement ePTR on real data and test it against popular alternatives and show its efficient practical performances. Since its inception [3], DP has become immensely popular. It is unlikely we can compare our contribution with all existing DP works. We will focus on the ones that are most relevant. In the later development, we will investigate ePTR’s application on more specific problems, where we will compare with some related works for those problems. Our work is an computationally efficient version of PTR. PTR was pro-posed in [14] for robust estimation of data scale and later proposed as general method to handle atypical data outcomes in [13]. Later works have discussed its application in mean and median estimation [15], linear regression and en-semble aggregation [16], high dimensional problems [16] and related Renyi di-vergence analysis [17]. However, as pointed out in Section 1.2, implementation of PTR requires computing the Hellinger distance Dα(X ), which is often highly intractable. This is admitted in the algorithm Section 1.2 of [17] that the algo-rithm “is not computationally efficient, as the Test step requires enumerating over a certain neighborhood of the input dataset and the Release step requires enumerating over all directions in high dimension”. A key contribution of this work is showing that Dα can be replaced by easily assessable and intuitive lower bound while maintaining the same minimax rate. Moreover, none of the exist-ing works view PTR as a generic DP adapter for classical statistical estimators, but rather a way to achieve optimal accuracy. This is an important perspective we try to illustrate because we think it is more important for the statistical community. As demonstrated earlier, our work focuses on the disparity between local and global sensitivity in statistical problems. PTR is not the only way to handle this issue nor is it popular. It is more common to consider applying stochastic gradient descent with additional DP noise [18] to obtain a DP estimator. For example, [9] shows that this approach can provide the rate optimal solution for linear regression with the correct tuning parameter. Alternatively, one can also 6consider the local DP framework, where noise is added to the data before pro-cessing [19], or using regularization [20] to improve the global sensitivity. While these methods can be effective or even rate optimal, their implementation re-quires tuning some hyperparameter that work globally, which can be practically difficult. In contrast, ePTR (or PTR) separates the typical and atypical data outcomes, so it is easier to achieve better accuracy. Moreover, the prior DP approaches often need to significantly augment clas-sical estimators. In comparison, ePTR seeks to make minimal changes. This will be more friendly for most statisticians. For example, for most statistician, OLS is still the most direct and elegant estimator when it comes to linear regres-sion. Learning that one cannot use OLS but need an online procedure or proper regularization tuning to achieve DP adaptation can be discouraging knowledge itself. Overall, it is not our intention to obtain the DP estimator with the best per-formance in the most general settings. For example, to simplify our discussion, we assume the knowledge of the range of data and parameter, which some may argue is not practical. There are other DP methods that can infer those range. Actually the original PTR in [14] is designed to estimate the scale of data, and this knowledge free theme has also been the main selling point of research in PTR [15, 16]. One can consider integrating ePTR with them like what is done in [9, 16]. And in the nonparameteric regression problem, we assume the under-lying function to be Cs, but works like [11] discussing the accuracy loss for more general function class. While it is possible to generalize our work to broader settings, we think such generalization will necessarily involve more technicality, and defies our main intention. Our main intention is to build a statistician friendly tool, ePTR, that can adapt existing estimators to DP requirement, and show there is not much accu-racy and computational toll for its implementation. For that purpose, we picked some of the most basic estimators in statistics and illustrate that ePTR can be applied in a simple and intuitive manner. We hope through this demonstration that more statisticians will be convinced that “DP is not hard”. 

# 2 Efficient PTR 

When designing an estimator ˆθ, a statistician’s main concern is its accuracy, which is often is measured under some probabilistic data generation process. But, differential privacy (DP) demands (1) to hold for all datasets, even if the dataset is atypical. This is a deterministic perspective. Therefore, we will split the discussion of efficient PTR (ePTR) into two halves. In the first deterministic half, we verify that ePTR is DP. In the second probabilistic half, we assess the accuracy of ePTR. The assessement will give us guidance on how to implement ePTR. 72.1 The determinstic half: ePTR is DP 

From our earlier discussion, we find the test step in PTR to be difficult. This is mainly because 1) the sub-sensitive set sub α lacks a simple formulation; 2) finding the Hellinger distance Dα to its complement sub cα is even more compli-cated. We propose to replace these ingredients with other . First, instead of using the exact sub α, we propose to think of a subset of sub α

G ⊆ sub α.

At this stage, we do not have further requirement for G. We will provide some high level guidelines in the end of this section, when we have a more complete perspective of ePTR. Next, we need to have a “distance” like function to indicate how far is X from the sensitive set Gc. While we can use something like DH (X , Gc), the Hellinger distance can be difficult to compute in some problems. But as it turns out, a “lower bound” of DH (X , Gc) will suffice this role. We will call it a sub-distance: 

> {defn:subdist }

Definition 2.1. Given a set G, a function γ(X ) ∈ [0 , ∞) is a sub-distance from 

X to the complement Gc if 

∥γ(X ) − γ(X ′)∥ ≤ DH (X , X ′), {γ(A) > 0} ⊆ G .

The efficient PTR can be obtained once we replace Dα with γ. We provide the formulation in Algorithm 2. Note that we modify the release probability 

p(A) slightly so it is easier to present. 

Algorithm 2 Efficient PTR {alg:ePTR }

Require: Data X , sub-distance γ, tolerance ϵ, δ , local sensitivity α

Ensure: An ( ϵ, δ ) output ˜θγ (X ) 

> 1:

Compute M = 1 + 2 

> ϵ

log(max { 1 

> δ

, 1 

> ϵ

}) 

> 2:

Compute γ(X ) 

> 3:

Set 

p(X ) = exp( 12 ϵ(γ(X ) − M )) exp( 12 ϵ(γ(X ) − M )) + 1  

> 4:

Let ˜θγ =

(

⊥ with prob. 1 − p(A)˜θα = ˆθ + 2αϵ

p2 log(1 .25 /δ )ζ with prob. p(A).

Here “ ⊥” represents a “no reply” message. In scenarios where one would prefer the output is also in Rp, it can be a random sample from a prefixed distribution π⊥ like Unif[ −1, 1] p or δ0.The first order of business is verifying ePTR is ( ϵ, δ )-DP: 8{thm:ePTRissafe }

Theorem 2.2. Given ϵ, δ > 0 and ˆθ. If G ⊂ sub α(∆ ˆθ ) and γ is a sub-distance to Gc, then the ePTR ˜θγ is (ϵ, δ )-DP. Proof. Let the test result I(X ) = 1 ˜θγ =˜θ . Our first step is showing that I(X ) is ( 12 ϵ, 0)-DP. We note that the following holds for all γ, ϵ ≥ 0. 

e 12 ϵγ 

e 12 ϵγ + 1 ≤ e 12 ϵ(γ−1) e 12 ϵ

e 12 ϵ(γ−1) + 1 , 1

e 12 ϵγ + 1 ≤ e 12 ϵ

e 12 ϵ(γ+1) + 1 .

Recall that |γ(X ) − γ(X ′)| ≤ 1. so for any i ∈ { 0, 1}

P (I(X ) = i)e− 12 ϵ ≤ P (I(X ) = i) ≤ P (I(X ′) = i)e 12 ϵ.

Meanwhile, for any set B, we decompose 

P (˜θγ (X ) ∈ B ) = P (˜θγ (X ) ∈ B , I (X ) = 1) + P (˜θγ (X ) ∈ B , I (X ) = 0) = P (˜θ(X ) ∈ B )P (I(X ) = 1) + π⊥(B)P (I(X ) = 0) ,

where π⊥ describes the distribution of ⊥.When γ(X ) > 0, since X ∈ G ⊂ sub α, so ∥ˆθ(X ) − ˆθ(X ′)∥ ≤ α. Then by Gausssian DP mechanism we have 

P (˜θα(X ) ∈ B ) ≤ P (˜θα(X ′) ∈ B )e 12 ϵ + δ, 

so we have 

P (˜θγ (X ) ∈ B ) = P (˜θα(X ) ∈ B )P (I(X ) = 1) + π⊥(B)P (I(X ) = 0) 

≤ e 12 ϵP (˜θα(X ′) ∈ B )e 12 ϵP (I(X ′) = 1) + P (I(X ) = 1) δ + π⊥(B)P (I(X ′) = 0) e 12 ϵ

≤ eϵ 

P (˜θα(X ′) ∈ B )P (I(X ′) = 1) + π⊥(B)P (I(X ′) = 0) 



+ P (I(X ) = 1) δ

≤ eϵP (˜θγ (X ′) ∈ B ) + δ. 

Suppose γ(X ) = 0, then γ(X ′) ≤ 1, and 

P (I(X ) = 1) = e− 12 M ϵ 

e− 12 M ϵ + 1 ≤ δe − 12 ϵ, P (I(X ′) = 1) ≤ e− 12 (M −1) ϵ

e− 12 (M −1) ϵ + 1 ≤ δ. 

We have 

P (˜θγ (X ) ∈ B ) = P (˜θ(X ) ∈ B )P (I(X ) = 1) + π⊥(B)P (I(X ) = 0) ≤ δ + π⊥(B).

Likewise 

P (˜θγ (X ′) ∈ B ) = P (˜θ(X ′) ∈ B )P (I(X ′) = 1) + π⊥(B)P (I(X ′) = 0) ≥ e 12 (M −1) ϵ

e 12 (M −1) ϵ + 1 π⊥(B)9Then because eϵ ≥ 1 + ϵ while e 12 (M −1) ϵ ≥ 1 

> ϵ

,

eϵ e 12 (M −1) ϵ

e 12 (M −1) ϵ + 1 = eϵ 1

e− 12 (M −1) ϵ + 1 ≥ (1 + ϵ) 1

eϵ + 1 ≥ 1.P (˜θγ (A) ∈ B ) ≤ δ + π⊥(B) ≤ eϵ e 12 (M −1) ϵ

e 12 (M −1) ϵ + 1 π⊥(B) + δ ≤ eϵP (˜θγ (A) ∈ B ) + δ. 

In a sense, we are replacing the original PTR’s sensitivity set sub cα and Hellinger distance DH with some lower bounds. This provides a large room of flexibility. In particular, one can still take G = sub α and γ = Dα, then ePTR is essentially the same as the original. But we can choose computationally efficient ones. At a high level, this is similar to the usage of evidence lower bound (ELBO) instead of the exact Kullback Leiber divergence, the former is sometimes much easier to obtain than the latter. Noticeably, Theorem 2.2 have strong requirement on G or γ. There seems no connection between them and the data generation process. This is because we are still discussing deterministic half of DP algorithm design, where our concern is ensure the algorithm is DP, a concept independent of data generation process. Our next step is concerning the statistical performance of ePTR. There we will have more guidance on how to pick G and γ.

## 2.2 The probabilistic half: ePTR performance 

As a statistician, we are more interested in the performance of an estimator ˜θ, or more precisely, how far is the difference ∥˜θ(X ) − θ∥. This will naturally assuming X coming from a probabilistic model Pθ governed by the true θ, which further leads to the mean square error: 

Lθ (˜θ) = Eθ [∥˜θ(X ) − θ∥2].

The ePTR formulation is simple enough that we can easily decompose it: {thm:ePTRperformance }

Theorem 2.3. Given any probabilistic model parametrized by θ ∈ Rp, let M =1 + 2 

> ϵ

log(max { 1 

> δ

, 1 

> ϵ

}). Suppose for some constant C⊥, ∥⊥ − θ∥2 ≤ C⊥ a.s., then we have 

Lθ (˜θ) ≤ Eθ [∥ˆθ − θ∥2]

| {z }

> (I)

+ 8α2pϵ2 log( 1.25 

δ )

| {z }

> (II)

+ C⊥(Pθ (γ(A) < 2M ) + δ)

| {z }

> (III)

. (9) 

Proof. Let the DP test result I(X ) = 1 ˜θγ =˜θα . We simply decompose 

Eθ [∥˜θγ (X ) − θ∥2] = Eθ [∥˜θγ (X ) − θ∥2I(X )] + Eθ [∥˜θγ (X ) − θ∥2(1 − I(X ))] = Eθ [∥˜θα − θ∥2I(X )] + Eθ [∥⊥ − θ∥2(1 − I(X ))] 

≤ Eθ [∥˜θα − θ∥2] + C⊥Pθ (I(X ) = 0) = (I) + (II) + C⊥Pθ (I(X ) = 0) .

10 Finally we note that Pθ (I(X ) = 0 |γ(X ) ≥ 2M ) ≤ e− 12 M ϵ ≤ δ. So 

Pθ (I(X ) = 0) ≤ Pθ (I(X ) = 0 |γ(X ) ≥ 2M )Pθ (γ(X ) ≥ 2M ) + Pθ (γ(X ) < 2M )

≤ δ + Pθ (γ(X ) < 2M ).

This leads to (III). A similar error decomposition for the original PTR can be found in [15]. The decomposition (9) is easy to understand: (I): The privacy-less error. This comes from our choice of ˆθ, it has no thing to do with DP. It can often be reduced by choosing a good ˆθ based on existing statistical knowledge. (II): The local DP loss. This part comes from the Gaussian perturbation ˜θα

we used when releasing the outcome. This part can be reduced by finding a tight typical local sensitivity. (III): The PTR failure loss. This part comes from the scenario where the null response ⊥ is released. In many cases, when n and 1 /δ are sufficiently large, this part is neglible compared with the first two parts. When adapting an estimator ˆθ to DP setting, terms (II) and (III) in the decom-position (9) provide us some guidelines how to find G and γ:1. We find an upper bound ∆ ˆθ (X ) < α for typical X and find some intuitive criteria that can enable the upper bound. These criteria formulate G,which is a high probability set. 2. We aim to find sub-distance γ so that P (γ(A) > 2M = eO( 1 

> ϵ

)) = 1 − o(1). In many instances, γ can be intuitively obtained by the same criteria that formulates G.In the next sections, with more concrete problems, we will show how to imple-ment these guidelines. 

# 3 Bayes Classifier 

Classification is a basic statistical problem. The (simple) Baye classifier is one of the most basic classification tools. In the simplest setups, one may model the class labels comeing from a prior P (yi = k) = μk, and the likelihood as simple Gaussian P (x|y) ∼ N (mk, I ). When presented with a new query point x, the Bayes classifier will simply produce the posterior likelihood 

P (y = k|x) = exp( − 12 ∥x − mk∥2)μk

P 

> j∈[K]

μj exp( − 12 ∥x − mj ∥2) ,

or the maximum posterior estimator. 11 In order to construct this classifier, one needs to estimate the prior μ and mean m[K] from some existing data points X = {(xi, y i), i ∈ [n]}. It’s natural to consider using the sample frequency and sample mean ˆμk(X ) = |Ik|

n , ˆmk(X ) = 

P 

> j∈Ik

xj

|Ik| , where Ik := {j : yj = k}. (10) We will illustrate that for this simple estimator, the aforementioned local-global sensitivity disparity exists. Standard PTR solves the issue but is computation-ally annoying, while the ePTR provides an efficient solution and reaches the minimax rate. 

## 3.1 Formulating ePTR 

We proceed to investigate the sensitivity of these estimators. For simplicity of exposition, we assume here the data points ∥xi∥ ≤ Rx for some known range 

Rx. This can be relaxed in practice by projection preprocessing. We consider a dataset X ′ 

> i

which shares the same entries with X , except the 

i-th entry is replaced by ( x′

> i

, y ′

> i

). It is straight forward to verify that ˆ μk have an 

o(1) global sensitivity bound: 

|ˆμk(X ) − ˆμk(X ′)| ≤ 1

n .

The global sensitivity of the mean ˆ mk on the other hand will not. If we consider the case with K = 2 classes, and two neighboring datasets 

X = {(Rx, 1) , (0 , 2) , (0 , 2) , . . . , (0 , 2) }, X ′ = {(−Rx, 1) , (0 , 2) , (0 , 2) , . . . , (0 , 2) }.

One can easily check that ˆ m1(X ) = Rx 

> |I1|

= Rx while ˆ m1(X ′) = −Rx 

> |I1|

= −Rx,which suggests that ∆ ˆm ≥ 2Rx and cannot be o(1). On the other hand, it is easy to notice that the reason that ∆ ˆm is large is because we have to take into account that |Ik| can be small. But this is a rare scenario, so PTR may address it. Next, we consider how to resolve this issue using standard PTR. By consid-ering replacing a data point ∆ ˆmk (X ) = max 

 Rx + max j∈Ik ∥xj ∥|Ik| , max j∈Ik ∥xj − ˆmk∥|Ik| − 1 , max x:∥x∥≤ Rx ∥x − ˆmk∥|Ik| + 1 



.

(11) While this is computable, the formula is not simple. As a consequence, the sub-level set sub α = {∆k(X ) ≤ α} does not admit any easy mathematical formula-tion. When implementing PTR 1, we need to compute the Hellinger distance 

Dα(X ). This is equivalent to ask what is the smallest number of data points we need to replace from X , so that ∆ ˆmk (X ) ≤ α. It is unclear to us whether there is algorithm that can solve optimization problem in polynomial time, but it is clear it will be challenging to write down even if it exist. 12 Now let’s consider how to implement ePTR. From (11), it is straight forward to have an upper bound ∆ ˆmk (X ) ≤ 2Rx

|Ik| − 1 .

To propose a typical scale of this local sensitivity upper bound, we just need a typical lower bound for |Ik| − 1. Given that yi are generated from the prior 

μ, one natural guess is that c0n, where c0 is a constant that depends on some prior assumption of μ. This further leads to a high probability set 

G =



X : min  

> k∈[K]

|Ik(X )| > c 0n + 1 



,

Using (11), it is easy to verify that when X ∈ G , ∆ ˆmk (X ) ≤ 2Rx 

> c0n

. In other words, G ⊂ sub α( ˆ mk) with α = 2Rx 

> c0

.Compared with the exact sub-sensitivity set sub α, G is very simple in for-mulation. In particular, given a data set X , its Hellinger distance to Gc is straightforward to obtain, we can set 

γ(X ) := DH (X , Gc) = 



min  

> k

|Ik| − c0n − 1



> +

(12) Evidently, γ(X ) is very simple and easy to compute. Following this simple formulation, we can derive an ePTR adaptation for the sample averaged Bayes classifier in (10). This involves projecting the data point or output onto feasible domains: ΠR(x) = R

∥x∥ ∨ R .

This is a standard step in many DP algorithm, see e.g. [9]. Also note that we need to release ˜ μk and ˜ mk for all k, so the local sensitivity needs a slightly more detailed investigation. The final algorithm is provided as Algorithm (3) 

Algorithm 3 Bayes Classifier with ePTR {alg:ePTRBC }

Require: Data X , tolerance ϵ, δ , prefixed bounds Rx, c 0.

Ensure: An ( ϵ, δ ) output ˜θγ (X ) 

> 1:

Let xi = Π Rx (xi).  

> 2:

Find ˆ μk, ˆmk 

> 3:

Apply ePTR on (ˆ μ[K], ˆm[K]) with γ being (12) and α = 2

> n

q 2R2

> x
> c20

+ 2  

> 4:

Augment μγ to be probability vector ˜μγ,k = ˜μγ,k ∨ c0

PKk=1 ˜μγ,k ∨ c0

13 {thm:BayePTRDP }

Theorem 3.1. Algorithm 3 output is (ϵ, δ )-DP. Proof. First, we verify (11). Note that if a ( xi, y i) is replaced with ( x′

> i

, y ′

> i

). There are four possibilities 1. yi = k = y′

> i

. The resulting change for ˆmk will be x′  

> i−xi
> |Ik|

. Then the maximum change for ˆ mk is caused by changing xi to −sgn( xi)Rx, ∥xi∥ is the maximum among all data points. 2. yi = k̸ = y′ 

> i

= j. The resulting change will be ˆ mk − ˆmk |Ik |− xi  

> |Ik|− 1

= xi− ˆmk  

> |Ik|− 1

.3. yi = j̸ = k = y′

> i

. The resulting change will be ˆ mk − ˆmk |Ik |+x′  

> i
> |Ik|+1

= ˆmk −x′  

> i
> |Ik|+1

.4. yi̸ = k̸ = y′

> i

. There will be no changes to ˆ mk.Maximizing over these gives us (11). Then since we are using Π Rx (xi) in place of xi, and X ∈ G , when n is sufficiently large, ∆k(X ) ≤ max { 2Rx

c0n , Rx

c0n − 1 , Rx

c0n + 1 } ≤ 2Rx

c0n .

Note also that in any of these four situations, only at most two ˆ mi and ˆ μi will be perturbed. The overall perturbation is ( ˆ m1, . . . , ˆmK , ˆμ1, . . . , ˆμK )∥(X ) − (X ′)∥ ≤ 2

n

s

2R2

> x

c20

+ 2 =: α. 

In other words, G ⊂ sub α. Finally, because || Ik(X )| − | Ik(X ′)|| ≤ 1, so 

γ(X ) is a 1-Lip w.r.t. DH . Theorem 2.2 indicates that ePTR-Bayes Classifier is ( ϵ, δ )-DP. 

## 3.2 Performance analysis 

Next, we check if ePTR provide good accuracy for Bayes classifier. This requires some assumption on the data generating process. With the Bayesian setup, it is natural to assume the data is generated from the prior, i.e. {aspt:Bayes }

Assumption 3.2. The data points (xi, y i) are obtained as i.i.d. samples fol-lowing 

P (yi = k) = μk, xi|yi=k ∼ N (mk, I ).

Suppose the true prior satisfies min μk > 2c0, then a Bernstein inequality indicates γ(X ) > 12 c0n with high probability. Applying this in Theorem 2.3, we see that if ϵ ≫ 1 

> n

, δ < 1 

> n

then Pθ (γ(X ) >

2M ) = 1 − o(1). So the PTR loss is negligible. 14 {thm:BayePTR }

Theorem 3.3. The γ adapted estimator (˜ μk, ˜mk) are (ϵ, δ )-DP. Moreover, under Assumption 3.2, if min k μk > 2c0, ∥mk∥ ≤ Cm

√p, δ < n −3, Rx =4Cm

pp| log n| then if n ≥ 4 

> c0

+ 8 

> c0ϵ

log( 1 

> δ

). then 

E[

> K

X

> k=1

(∥ˆμk − μk∥2 + ∥mk − ˜mk∥2)] ≤ O

 pn + p2 log n log(1 /δ )

ϵ2n2



Note that even when there is one class K = 1, the optimal mean estimation loss is O( pn + p2 log(1 /δ ) 

> ϵ2n2

) [9]. Our method matches the lower bound up to a log n

inflation. As for the requirement ϵ ≫ 1 

> n

, it is well known that for sufficiently small δ, a 0, there is no consistent ( ϵ, δ )-DP algorithm if 

Proof. As for the performance, by Bernstein 

P (|Ik| < c 0n) ≤ exp 



− 

> 12

c20n2

c0n + c0n/ 3



= exp( − 38 c0n).

Moreover, 

P (|| Ik| − μkn| > 2pn log n) ≤ exp 



− 

> 12

n log npkn + √n log n/ 3



≤ n−4.

E[|ˆμk − μk|2] = μk(1 − μk)

n

Meanwhile, P (max i ∥xi∥ ≥ Rx) ≤ n−4. Denote 

A = {max  

> i

∥xi∥ ≤ Rx, |Ik| ≥ c0n}.

Then P (Ac) = O( 1 

> n3

). So with high probability, we can decompose xj = mk +ξj

with ξj ∼ N (0 , I ), ˆmk = 1

|Ik|

X 

> yj=k

xj = mk + 1

|Ik|

X 

> j:yj=k

ξj .

Note that condition on y[n]

E

 1

|Ik|

X

> j∈Ik

ξj

> 2

y[n]

 = p

|Ik|

And in A, 1  

> |Ik|

= O(1 /n ). Therefore 

E[∥ˆθ − θ∥21A] = O( pn ), E[∥ˆθ − θ∥21Ac ] = O( R2

> x

n3 )o( pn ).

Put all these estimates in Theorem 2.3 gives us the desired bound. 15 3.3 Numerical simulation 

We evaluate the proposed ePTR classifier under a Gaussian naive Bayes model with K = 3 and p = 10. Specifically, the class label satisfies P(Y = k) = μk,and conditional on Y = k, the feature vector follows X | Y = k ∼ N (mk, I p). The class means are taken as coordinate-separated signals, 

m1 = (∆ , 0, . . . , 0) ⊤, m2 = (0 , ∆, 0, . . . , 0) ⊤, m3 = (0 , 0, ∆, 0, . . . , 0) ⊤,

with ∆ = 3. Throughout, the observed covariates are clipped to the Euclidean ball of radius Rx = 8 before estimation. We consider three simulation regimes: varying the privacy budget ε, varying the training sample size n, and varying the minimum class proportion πmin 

to study class imbalance. For the first two regimes, we set ( μ1, μ 2, μ 3) = (0 .75 , 0.15 , 0.10), and for the last one, we set ( μ1, μ 2, μ 3) = (0 .7−πmin , 0.3, π min ). Performance is measured by the balanced test error, namely the average misclas-sification rate across classes. We use this metric because it is more informative under class imbalance, as it prevents the majority class from dominating the overall error. The error is estimated on an independent test set of size 10 5, and all results are averaged over 500 Monte Carlo replications. We compare four methods: the nonprivate Gaussian naive Bayes classifier, the proposed ePTR procedure, a DP naive Bayes baseline (DPNB) [21], and a variant considering smooth sensitivities (DPNBSS) [22]. Unless otherwise stated, we fix δ = 0 .01. Figure 1 summarizes the main findings. As expected, the nonprivate classi-fier achieves the lowest balanced error throughout. Among the private methods, ePTR performs most favorably: its error decreases as ε or n increases, and it consistently attains lower balanced error than DPNB and DPNBSS across all settings. The advantage of ePTR is especially clear under class imbalance, where rare classes make private estimation more challenging for all methods, but ePTR still tracks the nonprivate benchmark much more closely than the competing private alternatives. 

Figure 1: Simulation results for Bayes classification under varying privacy bud-get, sample size, and class imbalance condition. {fig:bayes_sim_all }

# 4 Application to linear regression 

In this section we continue our discussion for linear regression in Section 1.1. But we consider the general multivariate version where we need estimate θ ∈ Rp

16 in 

yi = x⊺ 

> i

θ + ϵi

The well known OLS solution is given by ˆθ = ( XX ⊺)−1XY =

 X

> i∈[n]

xix⊺

> i

 

> −1

 X

> i∈[n]

yixi

 .

We will use ePTR to adapt it for DP settings. 

## 4.1 Formulating ePTR 

Consider a neighbor X ′ of X , where we change ( xi, y i) → (x′

> i

, y ′

> i

), we have 

∥ˆθ(X ) − ˆθ(X ′)∥ = ∥(XX ⊺)−1XY − (X′(X′)⊺)−1XY + ( X′(X′)⊺)−1(XY − X′Y ′)∥.

From our discussion in Section 1.2, finding the exact local sensitivity function is hard even in the univariate case. In comparison, the first step of ePTR seeks only high probability upper bound for ∆ ˆθ (X ). Assuming all data points satisfy ∥xi∥ ≤ Rx, ∥ˆθ∥ ≤ Rθ , |yi| ≤ Ry = Rθ Rx, where Rx, R θ are obtained either through expert knowledge or another DP estimator of scale. Then we can bound ∆ˆθ (X ) ≤ ∥ (X′(X′)⊺)−1(x′

> i

(x′

> i

)⊺ − xix⊺ 

> i

)∥∥ ˆθ∥ + ∥(X′(X′)⊺)−1∥y′

> i

x′ 

> i

− yixi∥≤ 4R2

> x

Rθ ∥(X′(X′)⊺)−1∥. (13) Using random matrix theory [23], we know with high probability ∥(XX ⊺)−1∥ =

O(1 /n ) and so is ∥(X′X⊺)−1∥ = O(1 /n ). This gives us a typical estimate of ∆ˆθ (X ). Note that by Weyl’s inequality, 

|λp(XX ⊺) − λp(X′(X′)⊺)| ≤ ∥ xix⊺ 

> i

− x′

> i

(x′

> i

)⊺∥ ≤ 2R2

> x

, (14) If we set the high probability set 

G = {λp(XX ⊺) ≥ c0n + 2 R2

> x

}.

when X ∈ G , ∥(X′(X′)⊺)−1∥ ≤ 1 

> c0n

. This leads to G ⊂ sub α, α = 4R2 

> xRθ
> c0n

. Notice that G is much easier to understand and express than sub α.The next step of ePTR design, we need to find a sub-distance γ. One may consider DH (X , Gc), which translates to how many xi do we need to modify (or remove) so λmin (XX ⊺) is below c0n. While one may solve this through some matrix optimization, we may avoid it completely by consider an alternative γ.Since G is characterized by λmin , so it is natural to consider using λmin as a candidate. Recall definition 2.1, Weyl inequality (14), we can come to using So it is natural to consider 

γ(X ) = 12R2

> x

(λmin (XX ⊺) − c0n − 2R2

> x

)+. (15) 17 Once again, this γ is very efficient to compute. To formulate the algorithm, we need to enforce ∥xi∥ ≤ Rx, ∥ˆθ∥ ≤ Rθ , |yi| ≤ 

Ry = Rθ Rx by proper projection. The final details are provided in Algorithm 4. We delay the verification that it is DP in Theorem 4.3. {thm:LRDPcheck }

Theorem 4.1. Algorithm 4 is (ϵ, δ )-DP. Proof. We start by verification of the local sensitivity. Because all ∥xi∥ ≤ 

Rx, |yi| ≤ RxRθ through the projection, suppose X ′ replaces ( xi, y i) with ( x′

> i

, y ′

> i

), we have 

∥ˆθ(X ) − ˆθ(X ′)∥

= ∥(X′(X′)⊺)−1X′Y ′ − (XX ⊺)−1XY ∥≤ ∥ (X′(X′)⊺)−1(X′Y ′ − XY )∥ + ∥(( X′(X′)⊺)−1 − (XX ⊺)−1)XY ∥≤ ∥ (X′(X′)⊺)−1(y′

> i

x′ 

> i

− yixi)∥ + ∥(X′(X′)⊺)−1 − (XX ⊺)−1XY ∥

= ∥(X′(X′)⊺)−1(y′

> i

x′ 

> i

− yixi)∥ + ∥(X′(X′)⊺)−1(X′(X′)⊺ − XX ⊺)( XX ⊺)−1XY ∥

= ∥(X′(X′)⊺)−1(x′

> i

(x′

> i

)⊺ − xix⊺ 

> i

)ˆθ∥ + ∥(X′(X′)⊺)−1∥∥ y′

> i

x′ 

> i

− yixi∥≤ 2R2

> x

∥(X′(X′)⊺)−1∥(∥ˆθ∥ + Rθ )Then by Lemma 4.4 

∥ΠRθ (ˆθ(X )) −ΠRθ (ˆθ(X ′)) ∥ ≤ 4R2

> x

∥(X′(X′)⊺)−1∥ ∥ˆθ∥ + Rθ

(∥ˆθ∥/R θ ) ∨ 1 ≤ 8R2

> x

Rθ ∥(X′(X′)⊺)−1∥

Next, by eigenvalue perturbation, 

|λp(XX ⊺) − λp(X′(X′)⊺)| ≤ ∥ xix⊺ 

> i

− x′

> i

(x′

> i

)⊺∥ ≤ 2R2

> x

.

So if X ∈ G , then λp(X′(X′)⊺) ≥ c0n, and ∆ ˆθ (X ) ≤ 8R2 

> xR0
> c0n

. Consequentially, 

G ⊂ sub α.From above, we also see 

∥γ(X ) − γ(X ′)∥ ≤ 12R2

> x

|λp(XX ⊺) − λp(X′(X′)⊺)| ≤ 1.

This verifies γ is a sub-distance to Gc. Theorem 2.2 indicates ePTR is ( ϵ, δ )-DP. 

## 4.2 Performance analysis 

In order to discuss the performance of ePTR-OLS, we need to set up the prob-abilistic data generation process Pθ . For simplicity, we make the following sim-plified assumption. 18 Algorithm 4 Linear regression with ePTR {alg:ePTROLS }

Require: Data X , tolerance ϵ, δ , prefixed bounds Rx, R θ , c 0.

Ensure: An ( ϵ, δ ) output ˜θγ (X ) 

> 1:

Let xi = Π Rx (xi), y i = Π RxRθ (yi).  

> 2:

Compute projected OLS ˆθ = Π Rθ (( XX ⊺)−1XY ).  

> 3:

Apply ePTR on ˆθ with γ as in (15) and α = 8R2

> xRθ
> nc 0

{aspt:LR }

Assumption 4.2. x[n] are i.i.d. samples from N (0 , Σ) , and yi is generated from 

yi = θ⊺xi + ϵi with ϵi being independent samples from N (0 , σ 2 

> ϵ

). In addition, we assume the covariance matrix is homogeneous, so that there is a constant Cx

C−2 

> x

≤ λp(Σ) ≤ · · · ≤ λ1(Σ) ≤ C2

> x

.

There is an Rθ so that ∥θ∥ ≤ Rθ and σϵ ≤ CxRθ

√p.

In applications, the constants Cx, R θ can be obtained known either through expert knowledge or other DP estimator. Under this setting, by high dimension probability theory, we have ∥xi∥ ≤ 

2Cx

√p log n, which we can use as Rx. Then by random matrix theory, when 

p < n   

> 4C4
> xlog n

, λp(XX ⊺) ≥ 34 nC −2 

> x

with high probability. So if we set c0 ≤ 14C2

> x

we find that 

γ(X ) = 12R2

> x

(λmin (XX ⊺) − c0n − 2R2

> x

)+ ≥ n

4pC 4 

> x

log n .

with high probability. According to Theorem 2.3, if ϵ ≫ p log nn , then the PTR loss is neglible. In summary we have the following theorem. {thm:LRDP }

Theorem 4.3. The ePTR-OLS is (ϵ, δ )-DP. If we assume the data generating process as Assumption 4.2, Rx = 2 Cx

√p log n and δ ≤ 1 

> n3

, n  

> log n

≥ 8pC 4

> x

(1 +  

> 2
> ϵ

log( 1 

> δ

)) the mean square error is bounded by 

Lθ (˜θγ ) ≤ O( pR 2

> θ

n + p3R2 

> θ

log n log(1 /δ )

ϵ2n2 )In [9], it is shown that if λi(Σ) = O(1 /p ) then for any ( ϵ, δ )-DP estimator ˜θ,

E[( ˜θ − θ)⊺Σ( ˜θ − θ)] ≳ pn + p2 

> ϵ2n2

in [9]. We note that ePTR reaches the optimal DP-linear regression error rate . In the same paper, [9] achieves this rate using an online learning method, but requires that n ≫ p3/2 log n, which is stronger than ours n ≫ p log n. Using the standard PTR, [16] has a similar rate to us and does not require knowledge of the parameters, but it is not computationally friendly. 

Proof. As for the performance, we note that by [23] P (max i ∥xi∥ ≥ 12 Rx) ≤

n−4, P (|ϵi| ≥ 12 RxRθ ) ≤ n−4. Therefore P (|yi| ≥ RxRθ ) ≤ n−4. Moreover, by 19 random matrix theory, if we denote E = Σ −1/2X, its columns are distributed as N (0 , I p). So by random matrix theory, because n ≫ p, with high probability 1

n λp(EE ⊺) ≥ 34 .

So we have 

λp(XX ⊺) ≥ λp(Σ) λp(EE ⊺) ≥ 34 nC −2 

> x

,

with high probability 1 − n−3.Consider the set 

A = {λp(XX ⊺) ≥ 34 nC −2 

> x

, ∥xi∥ ≤ Rx, |yi| ≤ RxRθ , ∀i ∈ [n]},

It takes place with probability 1 − n−4. Then we have 

E[∥ΠRθ (ˆθ) − θ∥2] = E[∥ΠRθ (ˆθ) − θ∥21A] + E[∥ΠRθ (ˆθ) − θ∥21Ac ]

≤ E[∥(XX ⊺)−1XY − θ∥21A] + 4 n−4R2

> θ

≤ E[tr(( XX ⊺)−1)1 A] + 4 n−4R2 

> θ

≤ 4pR 2 

> θ

C2

> x

n .

Also note that P (γ(X ) > 2M ) ≥ P (A) ≥ 1 − n−3 and δ < 1/n 3, it suffices to apply Theorem 2.3 to obtain our error bound. 

{lem:rperturb }

Lemma 4.4. For two vectors v, v ′

∥Pa(v) − Pa(v′)∥ ≤ ( 2(∥v∥/a ) ∨ 1 ) ∥v − v′∥

Proof. Simply check that 

v

∥v∥ ∨ a − v′

∥v′∥ ∨ a ≤ v

∥v∥ ∨ a − v′

∥v∥ ∨ a + v′

∥v∥ ∨ a − v′

∥v′∥ ∨ a

≤ ∥v − v′∥

a ∨ ∥ v∥ + ∥v′∥ |∥ v′∥ ∨ a − ∥ v∥ ∨ a|

(∥v′∥ ∨ a)( ∥v∥ ∨ a)

≤ ∥v − v′∥

a ∨ ∥ v∥ + ∥v′∥ ∥v′ − v∥

(∥v∥ ∨ a)( ∥v′∥ ∨ a)

≤ ∥v − v′∥

a ∨ ∥ v∥ (1 + ∥v′∥∥v′∥ ∨ a ) ≤ ( 2

∥v∥ ∨ a ) ∥v − v′∥ .

## 4.3 Numerical simulation 

We evaluate the proposed ePTR method under the mentioned linear regression model. In particular, the covariates are generated from Xi ∼ N (0 , I p), the 20 response follows Yi = X⊤ 

> i

θ + ϵi, ϵ i ∼ N (0 , 1) , and the underlying regression coefficient is taken as 

θ = (1 , 1/2, . . . , 1/p )⊤

∥(1 , 1/2, . . . , 1/p )⊤∥2

,

so that ∥θ∥2 = 1. Throughout, we set p = 5. We consider two simulation regimes: varying the privacy budget ε and vary-ing the training sample size n. Likewise, for each Monte Carlo replication, all methods are trained on the same sample and evaluated on the same independent test sample of size 10 5. We report both the parameter estimation error ∥ˆθ − θ∥22

and the mean squared error on the test set. All results are averaged over 500 replications, with δ = 0 .01. We compare five methods: the non-private esti-mator, the proposed ePTR estimator, the private Johnson–Lindenstrauss pro-jection baseline (DPJL) [24], the differentially private gradient-descent baseline (DPGD) [9], and the functional mechanism (FM) as seen in [25]. Figure 2 summarizes the results. As expected, the non-private estimator performs best across all settings. Among the private procedures, ePTR delivers the best overall performance: both the parameter estimation error and test MSE decrease more rapidly toward the nonprivate benchmark than those of the competing private methods as ε or n increases. In particular, for moderate to large values of ε or n, ePTR consistently achieves the lowest error among the private estimators under both criteria. 

Figure 2: Simulation results for linear regression under varying privacy budget and sample size. The top row varies ε with n fixed, and the bottom row varies 

n with ε fixed. {fig:linear_all }

21 5 Kernelized regression 

Nonparametric regression is a fundamental problem. The formulation is given training data pairs zi = ( xi, y i) where yi = f (xi) + ϵi, where ϵi ∼ N (0 , σ 2 

> ϵ

) and we want to estimate function value f (x0) at a point of interest x0. We also assume xi to lives compact set M with intrinsic dimension p, and are generated from some density pM(x) bounded from below and above. For first time reader, one can think M = [0 , 1] p, but M can also be some manifold structures. There has been a long lines of work on nonparametric estimation. Methods include spline methods and the more recent neural network powered problems. Kernelized regression is a simple and classical tool for non parametric estima-tion. The simplest estimator is the Nadaraya–Watson (NW) estimator [26], which is given by ˆf (x0) = 1

dσ (x0, X )

X

> i∈[n]

Kσ (x0, x i)yi, where dσ (x0, X ) := X

> i∈[n]

Kσ (x0, x i),

is often called the degree function. The choice of kernel depends on the smoothness of the function f and density 

μ. When f and μ are up to second order smooth, it is a common choice to use a Gaussian kernel 

Kσ (x, x ′) = 1

√2πσ 2d exp( − ∥x − x′∥2

2σ2 ). (16) The standard deviation σ is often known as the bandwidth. To understand why does NW estimator work, it suffices to know that if μ, f 

are C2 functions, then with high probability 1

n

X

> i∈[n]

Kσ (x0, x i)f (xi) ≈ f (x0)μ(x0),

and 1

n dσ (x0, X ) = 1

n

X

> i∈[n]

Kσ (x0, x i) ≈ μ(x0),

therefore their ratio will approximately be f (x0), the quantity of interest. What is left in the NW estimator ˆf (x0) are noises coming from ϵi. But they can be managed by standard concentration inequalities. When μ and f are of higher order Cs smoothness, the approximation above can be made more accurate. But this would require one to use some other kernels, of which the introduction is slightly technical. As it turns out, deriving the ePTR procedure for the NW estimator has little dependence on the choice of Kσ , and the readers will not miss much by assuming the kernel is Gaussian. So we delayed our discussion of kernel choice in Section. 22 5.1 Formulating ePTR 

Much similar to the linear regression problem, finding the exact ∆ ˆf (x0)(X ) in the standard PTR is not an easy problem. We will discuss only the ePTR approach. To start, we assume |Kσ | ≤ σ−dCK for a constant CK . This holds for Gaussian kernel with CK = (2 π)− d 

> 2

and most other kernels used in pratice. We also assume | ˆf (x0)|, |yi| ≤ Rf for some known Rf . This can be enforced by projection in algorithm. The perturbation can be bounded: 

| ˆf (x0, X ) − ˆf (x0, X ′)|≤ |Kσ (x0, x i)yi − Kσ (x0, x ′

> i

)y′

> i

|

dσ (x0, X ′) + |Kσ (x0, x i) − Kσ (x0, x ′

> i

)|

dσ (x0, X ′) | ˆf (x0)|≤ 4CK Rf

σddσ (x0, X ′) . (17) It is clear that dσ (x, X ′) control the local sensitivity and we need to estimate its typical scale to propose an α. Notice that 

|dσ (x, X ′) − dσ (x, X )| ≤ | Kσ (x′

> i

, x ) − Kσ (xi, x )| ≤ 2σ−dCK , (18) Meanwhile, the typical scale of dσ (x, X ) is Ω( n) by (21). With these in mind, it is natural to consider a high probability set with c0 to be tuning parameter 

G = {dσ (x, X ) ≥ c0n + 2 σ−dCK }.

Using (17), this leads to a local sensitivity level α = 4Rf CK 

> σdc0n

, and G ⊂ sub α.It is also quite intuitive to design a sub-distance γ given G’s formulation: since dσ is involved in its formulation, we should build γ based on it. Further-more, dσ is 2 σ−dCK Lipschitz w.r.t. DH . Since we need {γ(X ) > 0} ⊆ G , we consider 

γ(X ) = 12σ−dCK

(dσ (x, X ) − c0n − 2σ−dCK )+ (19) This is again an easily computable statistics. We summarize the procedure into {thm:KRDP }

Theorem 5.1. Suppose |Kσ | ≤ σ−dCK , Algorithm 5 is (ϵ, δ )-DP. Proof. We note that 

|ˆθ(X ) − ˆθ(X ′)| =

P 

> j∈[n]

Kσ (x, x j )yj

dσ (x, X ) −

P 

> j∈[n]

Kσ (x, x ′ 

> j

)y′

> j

dσ (x, X ′)

≤ |Kσ (x, x i)yi − Kσ (x, x ′

> i

)y′

> i

|

dσ (x, X ′) + |Kσ (x, x i) − Kσ (x, x ′

> i

)|

dσ (x, X ′) |ˆθ(X )|≤ 2Rf CK + 2 CK |ˆθ(X )|

σddσ (x, X ′) .

23 Algorithm 5 Nadaraya–Watson with ePTR {alg:ePTRKR }

Require: Data X , tolerance ϵ, δ , prefixed bounds Rf , c 0 and kernel upper bound CK .

Ensure: An ( ϵ, δ ) output ˜θγ (X ) 

> 1:

Let xi = Π M(xi), y i = Π Rf (yi).  

> 2:

Compute projected Kernel regression ˆf (x0) = Π Rf

 X

> i∈[n]

Kσ (x, x i)yi/d σ (x, X )

 . 

> 3:

Apply ePTR on ˆf (x0) with γ as in (19) and α = 4Rf CK 

> σdc0n

, return the output as ˆfγ (x0). Therefore, Lemma 4.4 leads to 

|ΠRf

ˆθ(X ) − ΠRf

ˆθ(X ′)| ≤ 4Rf CK

σddσ (x, X ′) .

So if γ(X ) > 0, we have dσ (x, X ′) > c 0n. This leads to G ⊂ sub α, with 

α = 4Rθ 

> c0nσ p

. Finally we apply Theorem 2.2. 

## 5.2 Kernel estimation for higher order smoothness 

Before we move on to discuss ePTR’s accuracy, we need to discuss the choice of kernel, since NW estimator’s accuracy depends on it. For readers who are interested in C2 functions, this part can be skipped since the Gaussian kernel (16) suffices. Also, most content of this part is not new and can be found in nonparametric estimation textbooks like [27]. We provide a compact version of them here for self-completeness. 

Definition 5.2. We say f ∈ C s at x, if ∂αf (x) exists for any multi-index 

α such that |α|1 ≤ s. Moreover for some r0 and Lf , the multivariate Taylor expansion hold for all y : ∥y − x∥ ≤ r0

f (y) − f (x) −

> s−1

X

> j=1

1

j! ∇j f (x)[ y − x, . . . , y − x] ≤ Lf ∥y − x∥s.

Intuitively, a smoother function will be easier to estimate. To leverage the order s smoothness, one need to use an order s kernel, which is defined as below (Note that in [27], it is defined as order s − 1 instead). 

Definition 5.3. Kσ (x, x ′) is a regular radial basis kernel with bandwidth σ and order s if Kσ (x, x ′) = 1 

> σd

K( ∥x−x′∥ 

> σ

) for some symmetric function K such that 

Z

> Rd

K(∥x∥)dx = 1 ,

Z

> R

uj K(u)du = 0 , j = 1 , . . . , s − 1. (20) 24 And there are some constant CK so that 

|K(u)| ≤ CK exp( −| u|2/C K ).

It is easy to verify that the Gaussian kernel (16) is a regular radial basis kernel with bandwidth σ of order 2. Another popular order 2 kernel uses K(v) =  

> 1
> Vd

1|v|≤ 1 where Vd is the volume of the radius 1 ball. Then the corresponding NW estimator ˆf (x0) will be averaging yi within the radius σ ball around x0.For order s ≥ 3 kernels, K need to take negative values but they exist for any fixed s. Proposition 1.3 in [27] shows how to build K when d = 1 using Legendre polynomials. In general, K can be constructed using as a linear combination of some basic functions such as polynomial or wavelet and find the linear coefficients by fitting the constraints (20). We provide an example on how to do this in Lemma (A.4) for Gaussian densities. When x0 is an interior point of M, f and μ are Cs at x0, one classical result of Kernel approximation is the following (We provide a proof in Proposition A.3): 

1

n

X

> i∈[n]

Kσ (x0, x i)f (xi) − f (x0)μ(x0) = Op



σs + log n1+ d/ 2

√nσ d



.

One special case is f ≡ 1, then 

|dσ (x0, X ) − nμ (x0)| = o(1) (21) This will be useful when we estimate the scale of γ(X ). 

## 5.3 Performance analysis 

In order to evaluate the performance of ePTR, we need to make assumptions on the DGP. We have discussed mostly of them during our derivation. We gather them below formally: {aspt:DGPKR }

Assumption 5.4. For some constant Cf , L f , L μ, r 0, the following hold 

• M is a compact domain of Rd and xi are i.i.d. samples on M generated from a distribution with density function μ(x) (w.r.t. Lebesgue measure). 

• yi = f (xi) + ϵi where ϵi are i.i.d. samples from N (0 , σ 2 

> ϵ

)

• f (x) ≤ Cf , σ ϵ ≤ Cf .

• x0 is an interior point of M, so Br (x0) ⊂ M .

• μ(x) and f (x) are Cs at x0 with regularity Lf and Lμ.

Using Theorem 2.2, we can easily predict the performance of ePTR and by checking the typical scale of γ(X ). Using (21), we can find γ(X ) is of scale Ω( nσ d) if μ(x0) > 2c0, nσ d > 1. This suggests that ϵ ≫ 1 

> nσ d

will guarantee that the PTR loss is negligible. More formally, we have 25 {thm:KR }

Theorem 5.5. Under Assumption 5.4, suppose Kσ (x, x ′) is a regular radial basis kernel with bandwidth σ and order s, μ(x0) > 2c0 + 4 

> nσ d

CK , δ ≤ n−3, then there is a C, when n ≥ 1+8 log(1 /δ ) 

> μ(x0)ϵ

, the following estimate holds 

E[L( ˆfγ (x0))] = O



σ2s + (log n)2+ d

nσ d + 1

ϵ2(nσ d)2



.

For kernelized estimation, the well known optimal scaling of bandwidth vs sample size is σ = n− 1 

> d+2 s

. Using it we have the following error estimate vs sample size: 

Corollary 5.6. Let σ = n− 1 

> d+2 s

. Under the same assumption as Theorem 5.5, we have 

E[L( ˆfγ (x0))] = eO(n− 2s 

> 2s+d

+ ϵ−2n− 4s 

> 2s+d

).

The error bound naturally decomposes into two parts, the first part, n− 2s

> 2s+d

is the classical nonparametric estimation rate for Cs functions[28], the second part is caused by DP constraint. Notably, this matches the lower bound (up to a polynomial of logn ) established in [29] for nonparametric regression. 

Proof. With our choice of Rf , it is easy to see that with high probability, the projection will not change the data points. Using Proposition A.3, we find that with high probability 1 − n−2

1

n

X

> i∈[n]

Kσ (x0, x i)f (xi) − f (x0)μ(x0) = O(σs + log n1+ d/ 2

√nσ d )

1

n

X

> i∈[n]

Kσ (x0, x i) − μ(x0) = O(σs + log n1+ d/ 2

√nσ d )Meanwhile, conditioned on x[n], it is easy to observe that the noise part 

> 1
> n

P 

> i∈[n]

Kσ (x0, x i)ϵi

> 1
> n

P 

> i∈[n]

Kσ (x0, x i) ∼ N (0 , σ 2 

> e

Σ) with Σ = 

P 

> i∈[n]

Kσ (x0, x i)2

dσ (x0, X )2 .

Note further that by Lemma A.1 

X

> i∈[n]

Kσ (x0, x i)2 ≤ X

> i∈[n]

1xi∈Br (x0)Kσ (x0, x i)2 + C2 

> K

n−5σ−2d

≤ ( X

> i∈[n]

1xi∈Br (x0) + n−5)C2 

> K

σ−2d

≤ (2 nμ (x0)Vdrd + n−5C2 

> K

)σ−2d

26 with high probability. Therefore, with high probability Σ ≤ rdσ−2d

nμ (x0) .

Therefore 

P

> 1
> n

P 

> i∈[n]

Kσ (x0, x i)ϵi

> 1
> n

P 

> i∈[n]

Kσ (x0, x i) >

s

log n rdσ−2d

nμ (x0)

!

≤ n−4.

Putting these together, and the fact Π Rf will confine ˆf into a bounded set, we have 

E[∥ ˆf (x0) − f (x0)∥2] = O



σ2s + (log n)2+ d

nσ d



.

The last step is checking that P (γ > 2M ) is negligible. This is true because by Proposition A.3, with high probability 1 − n−3

X

> i∈[n]

Kσ (x0, x i) − nμ (x0) = O(nσ s + n log n1+ d/ 2

√nσ d ).

So if μ(x0) > 2c0 + 4 

> nσ d

CK , then 

γ(X ) ≥ X

> i∈[n]

Kσ (x0, x i) − c0n − 2σ−dCK ≥ 12 nμ (x0).

With our condition, 12 nμ (x0) ≥ 2M = 2 + 4 

> ϵ

log(1 /δ ) and leads to our final estimate. 

## 5.4 Numerical simulation 

We evaluate the proposed ePTR method under the given nonparametric regres-sion model and we take 

f (x) = sin(2 πx ) + 0 .5 cos(4 πx ), ϵi ∼ N (0 , σ 2 

> ϵ

),

with σϵ = 0 .2 and target point x0 = 0 .5 for which f (x0) = 0 .5. We also consider three simulation regimes: varying the privacy budget ε,varying the training sample size n, and varying the concentration parameter a

to study the influence of local density. For the first two regimes, X ∼ Unif[0 , 1], and for the last one, we use X ∼ Beta( a, a ) to vary the local concentration of observations around x0. Performance is evaluated by the pointwise squared error [ ˆf (x0) − f (x0)] 2, averaged over 500 Monte Carlo replications. We compare three methods: the non-private Nadaraya–Watson estimator, a Wavelet-based private estimator (WPE) for nonparametric regression as seen in [29], and the proposed ePTR estimator for which we use the Nadaraya–Watson estimator with Gaussian kernel and the bandwidth is set as 0 .2 n−1/5.

27 Figure 3 summarizes the simulation results. As expected, the non-private estimator achieves the smallest error throughout. Among the private methods, ePTR performs best across all three settings: its error decreases as either ε or 

n increases, and it consistently improves over WPE. The advantage of ePTR is especially clear in the last regime, where larger values of a place more observa-tions near x0, making the local kernel estimate more stable and increasing the probability of a successful private release. 

Figure 3: Simulation results for kernelized regression under varying privacy budget, sample size, and design local density. {fig:kernel_sim_all }

# A Technical details of Kernel regression {lem:outlier }

Lemma A.1. If Kσ (x, y ) is a regular radial basis kernel with bandwidth σ,then for any bounded function g(x) and density μ(x) the following holds for 

r = 2 CK σ√log n Z 

> ∥u∥>rσ

|K(u)f (x + σu )μ(x + σu )|du ≤ n−3.

Z 

> M∩ Bcr(x)

Kσ (x, y )g(y)μ(y)dy = O(n−3σ−d)

X

> i∈[n]

1xi∈Bcr (x)Kσ (x, x i)g(xi) = O(n−3σ−d)

Proof. Simply note that by regularity of K, we have that 

K(u)f (x + σu )μ(x + σu ) ≤ n−4CK Cf Cμ

Kσ (x, y ) ≤ σ−dCK exp( − r2

σ2 ) ≤ σ−dn−4.

{lem:ind }

Lemma A.2. Suppose Br (x0) ⊂ M . Let Fi = Kσ (x0, x i)f (xi), then |Fi| ≤ 

Cf CK σ−d a.s., while 

|E[Fi] − f (y)μ(y)| = O(n−3σ−d + rs)

var [Fi] ≤ O(Vdrdσ−2d + n−3σ−2d)28 Proof. The upper bound is straight foward. For the mean, using Lemma A.1 we have 

E[Fi] −

Z 

> Br(x0)

Kσ (x, y )f (y)μ(y)dy = O(n−3σ−d)Meanwhile, if we write g(y) = f (y)μ(y), we can write 

Z 

> Br(x0)

Kσ (x0, y )f (y)μ(y)dy 

=

Z 

> ∥u∥≤ rσ

K(u)g(x0 + σu )du 

=

Z 

> ∥u∥≤ rσ

K(u)

g(x0) + 

> s−1

X

> j=1

σj

j! ∇j g(x0)[ u, . . . , u ]

 du + O(rs)=

Z

> u∈Rd

K(u)

g(x0) + 

> s−1

X

> j=1

σj

j! ∇j g(x0)[ u, . . . , u ]

 du + O(rs)= g(x0) + O(rs).

Meanwhile, var[ Fi] ≤ E[F 2 

> i

] = 

Z

> M

Kσ (x0, y )( Kσ (x0, y )f (y)2)μ(y)dy 

=

Z 

> Br(x0)

Kσ (x0, y )( Kσ (x0, y )f (y)2)μ(y)dy + O(n−3σ−2d)= O(Vdrdσ−2d + n−3σ−2d).

{pro:NW }

Proposition A.3. Suppose Br (x0) ⊂ M , f (x)μ(x) is Cs at x0, then the fol-lowing estimate holds with high probability 1 − n−2:

1

n

X

> i∈[n]

Kσ (x0, x i)f (xi) − f (x0)μ(x0) = O



σs + (log n)1+ d/ 2

√nσ d



.

Proof. Note that by Lemma A.2, if we write Ui = Fi − E[Fi], then by Bernstein inequality, 

P

| X

> i∈[n]

Ui| > t 

 ≤ 2 exp 



− t2

Cnr dσ−2d + Ctσ −d



So if we pick t = C1 log n√nr dσ−d for some sufficiently large C1, we have 

P (| P 

> i∈[n]

Ui| > t ) ≤ n−5. Meanwhile, Lemma A.2 indicates 

|E[Fi] − f (x0)μ(x0)| = O(rs).

Putting both parts together we have our claim. 29 {lem:buildK }

Lemma A.4. Let ϕσ (∥v∥) = 1 

> √2σπ d

exp( − ∥v∥2 

> 2σ2

) be the Gaussian density with mean zero and variance σ2Id. Then given any even positive 2s, a regular kernel of order 2s can be written as 

K(∥v∥) = 

> s

X

> i=1

aiϕi(∥v∥)

for some ai ∈ R.Proof. Note that R 

> Rd

K(∥x∥)dx = 1 if P ai = 1. The requirement R 

> R

uj K(u)du =0 holds for all odd j by symmetry. And if j is even, note that R uj ϕσ (u)du =

σj R uj ϕ1(u)du . So asking R 

> R

uj K(u)du = 0 is equivalent to asking Psi=1 (i)j ai =0. So it suffices for us to solve s linear equations 

> s

X

> i=1

ai = 1 ,

> s

X

> i=1

(i)j ai = 0 , j = 1 , . . . , s − 1.

Note that the coefficient of these equations form a Vandermonde matrix, which will not be singular. Therefore there will always be a unique solution to our problem. Finally, because K is a linear combination of Gaussian density, so 

|K(u)| ≤ CK exp( −| u|2/C K ) holds. 

# References 

[1] Michael Barbaro, Tom Zeller, and Saul Hansell. A face is exposed for aol searcher no. 4417749. New York Times , 9(2008):8, 2006. [2] Arvind Narayanan and Vitaly Shmatikov. Robust de-anonymization of large sparse datasets. In 2008 IEEE Symposium on Security and Privacy (sp 2008) , pages 111–125. IEEE, 2008. [3] Cynthia Dwork. Differential privacy. In International colloquium on au-tomata, languages, and programming , pages 1–12. Springer, 2006. [4] John M Abowd. The us census bureau adopts differential privacy. In Pro-ceedings of the 24th ACM SIGKDD international conference on knowledge discovery & data mining , pages 2867–2867, 2018. [5] ´Ulfar Erlingsson, Vasyl Pihur, and Aleksandra Korolova. Rappor: Ran-domized aggregatable privacy-preserving ordinal response. In Proceedings of the 2014 ACM SIGSAC conference on computer and communications security , pages 1054–1067, 2014. [6] Martin Abadi, Andy Chu, Ian Goodfellow, H Brendan McMahan, Ilya Mironov, Kunal Talwar, and Li Zhang. Deep learning with differential privacy. In Proceedings of the 2016 ACM SIGSAC conference on computer and communications security , pages 308–318, 2016. 30 [7] Jinshuo Dong, Aaron Roth, and Weijie J Su. Gaussian differential privacy. 

Journal of the Royal Statistical Society Series B: Statistical Methodology ,84(1):3–37, 2022. [8] H Brendan McMahan, Daniel Ramage, Kunal Talwar, and Li Zhang. Learn-ing differentially private recurrent language models. In Proceedings of the 2018 International Conference on Learning Representations , 2018. [9] T Tony Cai, Yichen Wang, and Linjun Zhang. The cost of privacy: Optimal rates of convergence for parameter estimation with differential privacy. The Annals of Statistics , 49(5):2825–2850, 2021. [10] Arnab Auddy, T Tony Cai, and Abhinav Chakraborty. Minimax and adap-tive transfer learning for nonparametric classification under distributed dif-ferential privacy constraints. Journal of the Royal Statistical Society Series B: Statistical Methodology , page qkaf070, 2025. [11] T Tony Cai, Dong Xia, and Mengyue Zha. Optimal differentially pri-vate pca and estimation for spiked covariance matrices. arXiv preprint arXiv:2401.03820 , 2024. [12] Jakob Bernoulli. Ars coniectandi . Impensis Thurnisiorum, fratrum, 1713. [13] Cynthia Dwork, Aaron Roth, et al. The algorithmic foundations of differ-ential privacy. Foundations and trends ® in theoretical computer science ,9(3–4):211–407, 2014. [14] Cynthia Dwork and Jing Lei. Differential privacy and robust statistics. In 

Proceedings of the forty-first annual ACM symposium on Theory of com-puting , pages 371–380, 2009. [15] Victor-Emmanuel Brunel and Marco Avella-Medina. Propose, test, re-lease: Differentially private estimation with high probability. arXiv preprint arXiv:2002.08774 , 2020. [16] Xiyang Liu, Weihao Kong, and Sewoong Oh. Differential privacy and robust statistics in high dimensions. In Conference on Learning Theory , pages 1167–1246. PMLR, 2022. [17] Jiachen T Wang, Saeed Mahloujifar, Shouda Wang, Ruoxi Jia, and Prateek Mittal. Renyi differential privacy of propose-test-release and applications to private and robust machine learning. Advances in Neural Information Processing Systems , 35:38719–38732, 2022. [18] Shuang Song, Kamalika Chaudhuri, and Anand D Sarwate. Stochastic gra-dient descent with differentially private updates. In 2013 IEEE global con-ference on signal and information processing , pages 245–248. IEEE, 2013. [19] Shiva Prasad Kasiviswanathan, Homin K Lee, Kobbi Nissim, Sofya Raskhodnikova, and Adam Smith. What can we learn privately? SIAM Journal on Computing , 40(3):793–826, 2011. 31 [20] Yu-Xiang Wang, Jing Lei, and Stephen E Fienberg. Learning with differ-ential privacy: Stability, learnability and the sufficiency and necessity of erm principle. Journal of Machine Learning Research , 17(183):1–40, 2016. [21] Jaideep Vaidya, Basit Shafiq, Anirban Basu, and Yuan Hong. Differentially private naive bayes classification. In 2013 IEEE/WIC/ACM International Joint Conferences on Web Intelligence (WI) and Intelligent Agent Tech-nologies (IAT) , volume 1, pages 571–576. IEEE, 2013. [22] Farzad Zafarani and Chris Clifton. Differentially private naive bayes clas-sifier using smooth sensitivity. arXiv preprint arXiv:2003.13955 , 2020. [23] Roman Vershynin. Introduction to the non-asymptotic analysis of random matrices. arXiv preprint arXiv:1011.3027 , 2010. [24] Or Sheffet. Differentially private ordinary least squares. In International Conference on Machine Learning , pages 3105–3114. PMLR, 2017. [25] Jun Zhang, Zhenjie Zhang, Xiaokui Xiao, Yin Yang, and Marianne Winslett. Functional mechanism: Regression analysis under differential privacy. Proceedings of the VLDB Endowment , 5(11), 2012. [26] Larry Wasserman. All of nonparametric statistics . Springer, 2006. [27] Alexandre B Tsybakov. Nonparametric estimators. In Introduction to Nonparametric Estimation , pages 1–76. Springer, 2008. [28] David L Donoho and Iain M Johnstone. Minimax estimation via wavelet shrinkage. The annals of Statistics , 26(3):879–921, 1998. [29] T Tony Cai, Abhinav Chakraborty, and Lasse Vuursteen. Optimal feder-ated learning for nonparametric regression with heterogeneous distributed differential privacy constraints. arXiv preprint arXiv:2406.06755 , 2024. 32