---
title: "PTOPOFL: Privacy-Preserving Personalised Federated Learning via Persistent Homology"
title_zh: PTOPOFL：基于持续同调的隐私保护个性化联邦学习
authors: "Kelly L Vomo-Donfack, Adryel Hoszu, Grégory Ginot, Ian Morilla"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04323v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 使用拓扑描述符的隐私保护个性化联邦学习
tldr: 针对联邦学习中梯度共享带来的隐私泄露和非独立同分布（non-IID）导致性能下降的问题，本文提出PTOPOFL框架。该框架利用持续同调（PH）生成的拓扑描述符替代梯度进行传输，通过48维紧凑向量实现隐私保护。服务器基于Wasserstein相似度进行拓扑引导的个性化聚合。实验证明，该方法在医疗等非IID场景下取得了最高的AUC，且重构风险降低了4.5倍。
selection_source: fresh_fetch
motivation: 解决联邦学习中梯度共享易受重构攻击以及数据非独立同分布导致模型聚合效果差的双重挑战。
method: 采用持续同调提取拓扑特征向量代替梯度传输，并结合Wasserstein相似度聚类与拓扑加权实现个性化聚合。
result: 在医疗和病理基准测试中分别达到0.841和0.910的最高AUC，且数据重构风险较传统梯度共享降低了4.5倍。
conclusion: PTOPOFL通过拓扑描述符在保证模型收敛性的同时，显著提升了联邦学习的隐私安全性和对非IID数据的适应性。
---

## 摘要
联邦学习 (FL) 面临两个结构性矛盾：梯度共享使得数据重构攻击成为可能，而非独立同分布 (non-IID) 的客户端分布则降低了聚合质量。我们提出了 PTOPOFL，这是一个通过将梯度通信替换为源自持续同调 (PH) 的拓扑描述符来同时解决这两个挑战的框架。客户端仅传输 48 维 PH 特征向量（一种紧凑的形状摘要，其多对一结构使得反演在证明上是病态的），而非模型梯度。服务器执行拓扑引导的个性化聚合：根据客户端 PH 图之间的 Wasserstein 相似性对其进行聚类，簇内模型进行拓扑加权，并将各簇与全局共识进行融合。我们证明了一个信息收缩定理，表明在强凸损失函数下，PH 描述符每样本泄露的互信息严格少于梯度，并且我们确立了 Wasserstein 加权聚合方案的线性收敛性，其误差下限严格小于 FedAvg。在非 IID 医疗场景（8 家医院，2 家对抗性医院）和病理基准测试（10 个客户端）中，与 FedAvg、FedProx、SCAFFOLD 和 pFedMe 相比，PTOPOFL 分别实现了 0.841 和 0.910 的 AUC（在两种设置中均为最高），同时相对于梯度共享，将重构风险降低了 4.5 倍。代码公开于 https://github.com/MorillaLab/TopoFederatedL，数据公开于 https://doi.org/10.5281/zenodo.18827595。

## Abstract
Federated learning (FL) faces two structural tensions: gradient sharing enables data-reconstruction attacks, while non-IID client distributions degrade aggregation quality. We introduce PTOPOFL, a framework that addresses both challenges simultaneously by replacing gradient communication with topological descriptors derived from persistent homology (PH). Clients transmit only 48-dimensional PH feature vectors-compact shape summaries whose many-to-one structure makes inversion provably ill-posed-rather than model gradients. The server performs topology-guided personalised aggregation: clients are clustered by Wasserstein similarity between their PH diagrams, intra-cluster models are topology-weighted,and clusters are blended with a global consensus. We prove an information-contraction theorem showing that PH descriptors leak strictly less mutual information per sample than gradients under strongly convex loss functions, and we establish linear convergence of the Wasserstein-weighted aggregation scheme with an error floor strictly smaller than FedAvg. Evaluated against FedAvg, FedProx, SCAFFOLD, and pFedMe on a non-IID healthcare scenario (8 hospitals, 2 adversarial) and a pathological benchmark (10 clients), PTOPOFL achieves AUC 0.841 and 0.910 respectively-the highest in both settings-while reducing reconstruction risk by a factor of 4.5 relative to gradient sharing. Code is publicly available at https://github.com/MorillaLab/TopoFederatedL and data at https://doi.org/10.5281/zenodo.18827595.

---

## 论文详细总结（自动生成）

# PTOPOFL：基于持续同调的隐私保护个性化联邦学习论文总结

## 1. 核心问题与整体含义（研究动机和背景）
联邦学习（FL）在实际应用中面临两大核心挑战：
*   **隐私泄露风险**：标准 FL 传输的模型梯度包含大量原始数据信息。研究表明，攻击者可以通过“梯度反演”高保真地重构客户端的训练样本。
*   **数据异构性（Non-IID）**：不同客户端的数据分布不一致会导致“客户端漂移”，使得全局模型难以收敛，或在特定客户端上表现不佳。

本文提出了 **PTOPOFL** 框架，旨在通过**拓扑数据分析（TDA）**的视角同时解决这两个问题。其核心动机是利用数据的几何形状（拓扑特征）来代替梯度进行通信，因为拓扑特征既能反映分布特性（用于个性化聚合），又具有天然的“多对一”特性（难以反演，保护隐私）。

## 2. 方法论：核心思想与技术细节
PTOPOFL 弃用了传统的梯度共享，转而采用基于**持续同调（Persistent Homology, PH）**的拓扑描述符。

*   **拓扑描述符提取**：客户端计算其本地数据的持续同调，生成 48 维的紧凑向量（包含 Betti 数、持续熵、Betti 曲线等）。该映射是多对一的，从数学上保证了反演过程是病态的（即无法从 48 维向量还原高维原始数据）。
*   **拓扑引导的个性化聚合**：
    1.  **聚类**：服务器根据描述符之间的 Wasserstein 距离对客户端进行层次聚类，将分布相似的客户端归为一簇。
    2.  **簇内加权**：在簇内部，根据客户端拓扑特征与簇中心的接近程度进行指数加权聚合。
    3.  **簇间融合**：将簇模型与全局共识模型进行加权融合（混合系数 $\beta_{blend}$），平衡个性化与泛化性。
*   **异常检测**：通过计算拓扑描述符的 z-score，识别并降低潜在中毒攻击（如标签翻转）客户端的权重。
*   **理论保证**：论文证明了**信息收缩定理**（PH 描述符泄露的互信息远少于梯度）和**线性收敛性**（在强凸条件下，其误差下限优于 FedAvg）。

## 3. 实验设计
论文设计了四个主要实验场景：
*   **场景 A（医疗 Non-IID）**：模拟 8 家医院（其中 2 家为对抗性），执行肺移植后死亡风险预测。
*   **场景 B（病理基准）**：10 个客户端，具有极端的类别分布倾斜（Class Imbalance）。
*   **场景 C/D（深度学习扩展）**：在 CIFAR-10 和 FEMNIST 数据集上使用 ResNet-18 和 ConvNet 进行验证。
*   **对比方法（Benchmark）**：FedAvg（标准聚合）、FedProx（近端正则化）、SCAFFOLD（控制变量法）、pFedMe（Moreau 包络个性化）。

## 4. 资源与算力
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **计算复杂度分析**：文中提到持续同调计算（Vietoris-Rips 过滤）的复杂度为 $O(n^3)$。为了降低开销，客户端采用了子采样技术（如每客户端采样 80-200 个点），且拓扑描述符仅在第 0 轮或检测到漂移时计算，通信开销极低（仅 48 维）。

## 5. 实验数量与充分性
实验设计较为全面且客观：
*   **多维度评估**：涵盖了分类性能（AUC/准确率）、收敛速度、对抗鲁棒性（不同攻击比例）、隐私风险（重构风险评估）以及拓扑特征的稳定性。
*   **消融实验**：专门分析了聚类、加权和融合三个组件对最终性能的贡献，证明了拓扑引导聚类是提升性能的关键。
*   **公平性**：对比了多种主流的个性化联邦学习算法，并在相同的 Non-IID 设置下运行。

## 6. 主要结论与发现
*   **性能领先**：在医疗场景下 AUC 达到 0.841，在病理基准下达到 0.910，均优于所有对比方法。
*   **隐私增强**：相比于传输梯度，PTOPOFL 将数据重构风险降低了 **4.5 倍**。
*   **鲁棒性强**：在 30% 的客户端为攻击者的情况下，通过拓扑异常检测仍能保持稳定的性能，而 FedAvg 性能显著下降。
*   **收敛快**：由于拓扑特征在训练初期即可确定分布结构，模型往往从第 1 轮通信开始就表现出极高的 AUC。

## 7. 优点（亮点）
*   **跨学科创新**：首次将持续同调（PH）引入联邦学习的通信机制，用几何抽象代替数值梯度。
*   **双重效用**：一个 48 维的描述符同时解决了隐私保护和异构数据聚类两个难题。
*   **理论扎实**：不仅有实验，还提供了关于信息论隐私界限和优化收敛性的严格数学证明。
*   **通信极简**：相比于传输数百万参数的梯度，传输 48 维向量极大地节省了带宽。

## 8. 不足与局限
*   **理论假设限制**：收敛性证明基于强凸假设，虽然在逻辑回归上表现良好，但在非凸的深度神经网络上缺乏严格的理论保证。
*   **计算瓶颈**：尽管有子采样，但对于超大规模数据集，计算 PH 仍可能比计算梯度更耗时。
*   **隐私定义的差异**：论文提供的是“信息收缩”保证，而非严格的 $(\epsilon, \delta)$-微分隐私（DP）。虽然重构更难，但尚未与标准的 DP 框架完全对齐。
*   **超参数敏感性**：聚类数量 $M$ 和融合系数 $\beta_{blend}$ 需要预先设定或通过验证集调整。

（完）
