---
title: "RELIEF: Turning Missing Modalities into Training Acceleration for Federated Learning on Heterogeneous IoT Edge"
title_zh: RELIEF：将缺失模态转化为异构物联网边缘联邦学习的训练加速
authors: "Beining Wu, Zihao Ding, Jun Huang"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04243v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 面向异构物联网边缘设备的联邦学习框架
tldr: 针对异构IoT边缘设备在联邦学习中面临的系统、模态和数据耦合异构性问题，本文提出RELIEF框架。该框架通过将LoRA投影矩阵划分为模态对齐的列块，实现按模态队列聚合以消除梯度干扰，并根据散度引导资源受限设备优先训练高影响参数。实验表明，RELIEF在提升稀有模态性能的同时，显著加快了训练速度并降低了能耗。
selection_source: fresh_fetch
motivation: 现有联邦学习方法难以处理低算力设备因传感器缺失导致的梯度不完整及跨模态干扰问题。
method: 提出将LoRA矩阵划分为模态对齐块，实施队列聚合与基于散度的个性化训练预算分配。
result: "在PAMAP2等数据集上实现高达9.41倍的加速、37%的能耗降低，并提升稀有模态F1分数达15.3个百分点。"
conclusion: RELIEF通过解耦模态干扰并优化资源分配，有效解决了异构IoT环境下的联邦学习效率与精度瓶颈。
---

## 摘要
在异构物联网（IoT）边缘设备上进行的联邦学习（FL）面临着系统-模态-数据耦合的异构性：低成本设备通常携带较少的传感器且计算能力较低，因此最慢的设备（掉队者）会产生最不完整的梯度信号。简单地对它们的更新进行平均会稀释稀有模态的信息，并在缺失传感器的参数上浪费计算资源，而现有方法通常孤立地处理三重异构性（系统、模态、数据），且没有一种方法能解决它们的耦合问题。为了解决这一问题，我们提出了 RELIEF 框架，该框架将融合层低秩自适应（LoRA）投影矩阵划分为模态对齐的列块，并将此划分作为聚合、弹性训练和通信的统一接口。每个块仅在拥有该模态的设备群组内进行聚合，从而消除了跨模态梯度干扰；随后，服务器通过优先处理群组内部差异最大的块来分配个性化训练预算，使资源受限的设备能够训练更少但更具影响力的参数。我们证明了群组聚合从收敛界中消除了干扰，并且基于差异引导的分配实现了次线性遗憾。在两个物联网传感器数据集（PAMAP2、MHEALTH）上进行的实验（包括全参数 CNN 和参数高效 LoRA 训练）表明，与 FedAvg 相比，RELIEF 实现了高达 9.41 倍的加速和 37% 的能耗降低，稀有模态的 F1 分数提升高达 15.3 个百分点，在两台 Jetson AGX Orin 测试台上的真实设备验证也证实了这些结果。

## Abstract
Federated learning (FL) over heterogeneous IoT edge devices faces coupled system-modality-data heterogeneity: the lower-cost device carries both fewer sensors and less computational power, so the slowest device (straggler) produces the most incomplete gradient signals. Naively averaging their updates dilutes rare-modality information and wastes computation on absent-sensor parameters, whereas existing methods handle the triple heterogeneity (system, modality, data) in isolation and none addresses their coupling. To resolve this issue, we propose RELIEF, a framework that partitions the fusion-layer Low-Rank Adaptation (LoRA) projection matrix into modality-aligned column blocks and uses this partition as a unified interface for aggregation, elastic training, and communication. Each block is aggregated only within the cohort of devices possessing that modality, which eliminates cross-modal gradient interference; the server then allocates personalized training budgets by prioritizing blocks with the highest cohort-internal divergence, so that resource-constrained devices train fewer but more impactful parameters. We prove that cohort-wise aggregation removes interference from the convergence bound and that the divergence-guided allocation achieves sublinear regret. Experiments on two IoT sensor datasets (PAMAP2, MHEALTH) under both full-parameter (CNN) and parameter-efficient (LoRA) training show that RELIEF achieves up to 9.41x speedup and 37% energy reduction over FedAvg with up to 15.3 pp rare-modality F1 gains, and real-device validation on a two-Jetson AGX Orin testbed confirms these results.

---

## 论文详细总结（自动生成）

### 论文总结：RELIEF 框架

#### 1. 核心问题与研究动机
论文关注的是**异构物联网（IoT）边缘环境下的多模态联邦学习（FL）**。其核心挑战在于“耦合的系统-模态-数据异构性”：
*   **耦合异构性**：低成本设备通常传感器少（模态缺失）且算力弱（掉队者）。
*   **现有问题**：
    1.  **梯度干扰**：标准 FedAvg 简单平均所有更新，导致拥有全模态的设备产生的丰富梯度被仅有单模态设备的“零填充”噪声稀释。
    2.  **计算浪费**：掉队设备在同步 FL 中被迫训练其并不具备的传感器参数，浪费了宝贵的计算资源。
    3.  **接口不匹配**：现有的多模态 FL 方法（关注聚合）与弹性训练方法（关注速度）在参数选择逻辑上存在冲突。

#### 2. 方法论：RELIEF 框架
RELIEF 提出了一种统一的结构化接口，将融合层的 **LoRA（低秩自适应）投影矩阵 $A$ 划分为与模态对齐的列块**。
*   **核心思想**：利用模态对齐的列块作为聚合、训练分配和通信的共同边界。
*   **关键技术细节**：
    *   **模态分解 LoRA (MDLoRA)**：将投影矩阵 $A$ 拆分为 $M$ 个块，每个块对应一个传感器模态。
    *   **队列聚合 (Cohort-wise Aggregation)**：每个列块仅在拥有该模态的设备子集（队列）内进行聚合，彻底消除跨模态梯度干扰。
    *   **散度引导的弹性训练**：服务器计算每个参数块在队列内的更新散度（利用 EMA 平滑），优先让资源受限的设备训练那些“分歧大、影响大”且“自身具备传感器”的参数块。
    *   **按需通信**：设备仅上传其训练过的参数块，减少了通信开销。

#### 3. 实验设计
*   **数据集**：使用了两个真实世界的物联网传感器数据集：**PAMAP2** 和 **MHEALTH**（人体活动识别）。
*   **模型架构 (Backbones)**：
    1.  **轻量级 CNN**：用于验证全参数训练场景。
    2.  **MOMENT (Transformer) + LoRA**：用于验证参数高效微调场景。
*   **对比方法 (Baselines)**：共对比了 10 种方法，包括经典 FL (FedAvg, FedProx)、系统异构 FL (FedEL, DarkDistill)、多模态 FL (Harmony, Pilot) 以及联邦 LoRA 变体 (HeLoRA, FedLEASE)。

#### 4. 资源与算力
*   **仿真环境**：服务器端使用 NVIDIA RTX A2000 GPU。
*   **真实设备测试台**：使用了两台 **NVIDIA Jetson AGX Orin (64GB)** 模块。
*   **算力模拟**：通过调整 Jetson 的功率模式（MAXN 60W, 30W, 15W）来模拟不同层级的边缘设备（从高性能网关到低功耗贴片）。
*   **训练时长**：实验运行 200 个通信轮次，每轮包含 5 个本地 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个数据集 × 2 种模型架构 × 11 种对比方法。
*   **消融实验**：验证了弹性训练、队列聚合和散度引导分配各自的贡献。
*   **敏感性分析**：测试了不同异构程度（10x 到 100x 算力差距）和不同设备规模（8 到 100 个客户端）下的表现。
*   **客观性**：实验采用了 3 个随机种子取平均值，并在真实硬件上进行了功耗和时间的物理测量，实验设计较为充分且客观。

#### 6 主要结论与发现
*   **加速效果**：在 LoRA 场景下，RELIEF 相比 FedAvg 实现了高达 **9.41 倍** 的墙钟时间加速。
*   **能效提升**：实现了约 **37%** 的整体能耗降低。
*   **精度增益**：在稀有模态（如磁力计、心率）上的 F1 分数提升高达 **15.3 个百分点**，且在 MHEALTH 数据集上总精度提升了 20.2%。
*   **理论验证**：证明了队列聚合能消除收敛界中的干扰项，且散度引导分配具有次线性遗憾（Sublinear Regret）。

#### 7. 优点（亮点）
*   **统一性**：巧妙地利用 LoRA 的矩阵结构作为多维异构性处理的统一接口，解决了聚合与加速的冲突。
*   **稀有模态保护**：通过队列聚合有效解决了多模态学习中“强模态主导、弱模态被稀释”的顽疾。
*   **软硬结合**：不仅有严谨的收敛性证明，还有详尽的真实边缘设备（Jetson）部署验证。

#### 8. 不足与局限
*   **应用领域限制**：实验主要集中在人体活动识别（HAR）的时间序列数据上，尚未在多模态视觉-语言模型等更大规模任务上验证。
*   **模型依赖**：该方法高度依赖于 LoRA 结构或具有明确模态对应关系的线性融合层，对于非线性或高度融合的架构（如 Early Fusion）可能难以直接应用。
*   **预训练偏差**：在使用 MOMENT 基础模型时，由于领域差异，绝对精度在某些任务上仍有提升空间。

（完）
