---
title: "Three Birds, One Stone: Solving the Communication-Memory-Privacy Trilemma in LLM Fine-tuning Over Wireless Networks with Zeroth-Order Optimization"
title_zh: 一石三鸟：利用零阶优化解决无线网络中大语言模型微调的通信-内存-隐私三难困境
authors: "Zhijie Cai, Yuhao Zheng, Haolong Chen, Dongzhu Liu, Bin Wang, Guangxu Zhu"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12401v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 使用零阶优化的LLM隐私保护联邦学习
tldr: 针对无线网络中大语言模型（LLM）联邦微调面临的通信、内存和隐私三大挑战，本文提出 pAirZero 框架。该框架创新性地结合了零阶（ZO）优化与空中计算（OTA），使资源受限设备能以推理级的内存消耗和极低的比特级通信开销参与训练。通过自适应功率与噪声控制，在保障隐私的同时显著提升了微调效率，为边缘侧 LLM 协作微调提供了高效安全的解决方案。
selection_source: fresh_fetch
motivation: 联邦学习微调 LLM 时存在高维梯度导致的通信开销巨大、内存需求过高以及梯度泄露引发的隐私风险。
method: 提出 pAirZero 框架，通过零阶优化降低内存需求，并结合空中计算实现极低带宽的梯度聚合与自适应隐私保护。
result: "实验表明，该方法在 OPT-125M 模型上仅需 25% 的峰值内存，且通信负载较传统方法降低了数个数量级。"
conclusion: pAirZero 成功解决了 LLM 边缘微调中的通信-内存-隐私三难困境，实现了高效且安全的无线联邦学习。
---

## 摘要
联邦学习 (FL) 为在边缘侧协同微调大语言模型 (LLM) 提供了一条极具前景的途径；然而，这一范式面临着一个关键瓶颈：交换高维梯度所带来的极高通信和内存开销。此外，最近的研究表明，用户的训练数据仍可从这些本地梯度中恢复，从而损害了联邦学习的核心隐私承诺。在本文中，我们通过提出 pAirZero 来解决通信、内存和隐私这一三难困境，这是一个将零阶 (ZO) 优化与空中计算 (OTA) 相结合的新型框架。独特的是，pAirZero 使资源受限的设备能够以仅比特级的通信负载提交本地梯度，同时以推理级的内存成本参与大语言模型的联邦微调。该方法不仅消除了大语言模型微调所需的高内存需求，还缓解了困扰传统空中计算方法的严格同步要求。我们进一步构建了一个严谨的优化模型，以自适应地确定最佳发射功率和噪声水平，从而确保无论信道条件如何都能提供一致的隐私保护。数值实验证明了 pAirZero 在无线网络上实现安全、高效的大语言模型微调方面的优越性，在 OPT-125M 模型上仅需 25% 的峰值内存成本，且通信负载比传统方法低几个数量级。

## Abstract
Federated Learning (FL) offers a promising pathway for collaboratively fine-tuning Large Language Models (LLMs) at the edge; however, this paradigm faces a critical bottleneck: the prohibitive communication and memory overheads incurred by exchanging high-dimensional gradients. Furthermore, recent studies reveal that user training data can still be recovered from these local gradients, undermining the core privacy promise of FL. In this paper, we address this trilemma of communication, memory, and privacy by proposing pAirZero, a novel framework that synergizes Zeroth-Order (ZO) optimization with Over-the-Air (OTA) computation. Uniquely, pAirZero enables resource-constrained devices to submit their local gradient with only bit-level communication loads while participating in federated fine-tuning of LLMs with inference-level memory costs. This approach not only eliminates the high memory requirements needed for LLM fine-tuning but also alleviates the strict synchronization requirements that plague conventional OTA methods. We further formulate a rigorous optimization model to adaptively determine the optimal transmit power and noise levels, ensuring consistent privacy protection regardless of channel conditions. Numerical experiments demonstrate the superiority of pAirZero in enabling secure, efficient LLM fine-tuning over wireless networks, with only 25% peak memory cost on OPT-125M and communication load orders of magnitude lower than conventional methods.

---

## 论文详细总结（自动生成）

### 论文结构化总结：一石三鸟（pAirZero 框架）

#### 1. 核心问题与整体含义
本文针对在无线边缘网络（如智能手机、IoT设备）中微调大语言模型（LLM）时面临的**“通信-内存-隐私”三难困境（Trilemma）**提出了解决方案。
*   **研究动机**：LLM 参数量巨大，传统的联邦学习（FL）在边缘侧微调时存在三大瓶颈：
    1.  **通信瓶颈**：交换高维梯度向量会耗尽无线带宽。
    2.  **内存墙**：反向传播（BP）需存储中间激活态，内存需求远超边缘设备容量。
    3.  **隐私风险**：梯度反推攻击（Gradient Inversion）可能从梯度中恢复原始训练数据。
*   **核心目标**：开发一个既能节省内存和通信开销，又能提供数学级隐私保证的统一框架。

#### 2. 方法论
论文提出了 **pAirZero** 框架，其核心思想是**将零阶（ZO）优化与空中计算（OTA）深度融合**。
*   **零阶优化（ZO）**：不通过反向传播计算梯度，而是通过两次前向传播（对模型参数施加随机扰动）来估计梯度在随机方向上的投影（一个标量）。这使得内存需求降至“推理级”。
*   **空中计算（OTA）**：利用无线信道的电磁叠加特性，让多个客户端同时发送信号，在接入点直接实现梯度的物理聚合，无需分时频传输。
*   **关键技术细节**：
    *   **标量传输**：由于 ZO 仅需传输梯度投影标量，pAirZero 将每轮通信量从“模型维度 $d$”降至“常数 $O(1)$”（比特级）。
    *   **隐私内生设计**：利用无线信道固有的高斯噪声，并辅以人工注入的补偿噪声，构建差分隐私（DP）机制。
    *   **功率控制优化**：推导了收敛误差的上界，并建立优化模型，通过闭式解（Closed-form solution）动态调整每轮的发射功率和噪声水平，以在隐私预算内实现最优收敛。
    *   **变体设计**：提出了支持模拟调制的 **pAirZero** 和支持数字调制（1-bit 压缩）的 **Sign-pAirZero**。

#### 3. 实验设计
*   **模型与任务**：使用 **OPT-125M** 模型；测试任务包括 **SST-2**（情感分类）和 **SQuAD**（阅读理解/问答）。
*   **对比基准（Benchmarks）**：
    *   **Perfect**：假设无噪声、无隐私约束的理想聚合情况。
    *   **Static**：隐私预算在迭代中平均分配的静态功率方案。
    *   **Reversed**：与优化方案相反的功率分配趋势。
    *   **传统方法对比**：与一阶优化（FO SGD/Adam）在内存和通信量上进行理论与数值对比。
*   **场景设置**：模拟不同信噪比（SNR）下的无线信道环境。

#### 4. 资源与算力
*   **硬件环境**：使用了 1 台配备 **AMD EPYC 7742 64核处理器** 和 **4 张 NVIDIA A100-SXM4-80GB GPU** 的服务器。
*   **训练细节**：迭代次数 $T=8000$，扰动尺度 $\mu=0.001$。文中未明确给出总训练时长，但强调了 ZO 优化仅需前向传播，计算开销较低。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个代表性 NLP 任务，并在不同 SNR（从低到高）下进行了多次测试。
*   **客观性保证**：每组实验均运行 **4 次独立试验**，并报告了均值和标准差（图中阴影部分），以排除随机性干扰。
*   **充分性**：除了主实验，还进行了消融实验（验证功率分配方案）、参数敏感性分析（如梯度剪切阈值 $\gamma$ 的选取、符号反转概率 $e_0$ 的实测），实验设计较为严谨、客观。

#### 6. 主要结论与发现
*   **内存大幅节省**：微调 OPT-125M 仅需约 **250MB** 内存，相比 Adam 优化器（约 955MB）节省了约 **75%** 的峰值内存。
*   **通信效率极高**：每轮仅需传输 1 个标量（1-bit 或 16-bit），相比传统方法传输 238MB 的梯度向量，通信负载降低了数个数量级。
*   **隐私与性能平衡**：在 $\epsilon=5$ 的隐私保护下，性能接近无损基准。
*   **调制特性**：数字化的 Sign-pAirZero 在低 SNR 环境下比模拟版本更稳健，而模拟版本在复杂任务（如 SQuAD）中上限更高。

#### 7. 优点
*   **协同效应**：巧妙地利用了 ZO 优化的“标量特性”解决了 OTA 计算中难以实现大规模高维向量同步的痛点。
*   **隐私成本低**：将信道噪声转化为隐私保护的“工具”，实现了“Privacy for free”的理念。
*   **理论支撑强**：不仅有算法设计，还提供了严谨的收敛性证明和最优功率分配的数学推导。

#### 8. 不足与局限
*   **模型规模限制**：实验仅在 125M 参数的模型上完成，对于百亿（10B+）甚至千亿参数的模型，ZO 优化的收敛速度可能会显著变慢。
*   **收敛速度**：虽然每轮开销小，但 ZO 优化通常比一阶方法需要更多的迭代次数才能达到相同精度。
*   **任务多样性**：实验主要集中在分类和抽取式问答，未验证在长文本生成、逻辑推理等更复杂 LLM 任务上的表现。

（完）
