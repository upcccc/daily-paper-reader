---
title: Population Risk Bounds for Kolmogorov-Arnold Networks Trained by DP-SGD with Correlated Noise
title_zh: 使用相关噪声 DP-SGD 训练的 Kolmogorov-Arnold 网络（KANs）的总体风险界
authors: "Puyu Wang, Jan Schuchardt, Nikita Kalinin, Junyu Zhou, Sophie Fellenz, Christoph Lampert, Marius Kloft"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12648v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 带有相关噪声的DP-SGD及其隐私-效用权衡
tldr: 本研究首次建立了Kolmogorov-Arnold网络（KANs）在小批量SGD及差分隐私SGD（DP-SGD）训练下的泛化风险界限，涵盖了独立及时间相关的高斯噪声。相比以往研究，本文更贴近实际应用，探讨了非凸机制下相关噪声对隐私与效用权衡的影响。通过引入辅助未投影动力学和移位迭代等技术手段，解决了时间依赖性和投影步骤带来的分析难题，为非凸深度学习中的相关噪声DP训练提供了理论支撑。
selection_source: fresh_fetch
motivation: 现有的KAN理论研究主要集中在全批量梯度下降，缺乏对实际应用中更有效的小批量SGD及相关噪声差分隐私机制的理论分析。
method: 通过引入辅助未投影动力学、移位迭代吸收噪声扰动以及高概率引导技术，克服了非凸优化中时间依赖性和投影步骤带来的分析障碍。
result: 建立了KANs在小批量DP-SGD下的首个泛化风险界限，并证明了该界限在特定情况下比现有全批量研究结果更为紧致。
conclusion: 该工作为非凸神经网络在相关噪声差分隐私训练下的优化与泛化提供了首个理论框架，具有重要的理论与实践意义。
---

## 摘要
我们为通过带梯度裁剪的小批量 SGD 训练的 Kolmogorov-Arnold 网络 (KANs) 建立了首个总体风险界，涵盖了非私有 SGD 以及在独立噪声和时间相关噪声之间插值的带高斯扰动的差分隐私 SGD (DP-SGD)。这一设定在两个维度上比之前的 KAN 理论更接近实践：一是采用现代网络的标准方案——小批量 SGD 进行训练，而非全批量梯度下降 (GD)；二是相关噪声机制在经验上表现出比独立噪声机制更优的隐私-效用权衡。我们的结果涵盖了 Wang 等人 (2026) 关于 KANs 的相应全批量 GD 和独立噪声 DP-GD 的结果，同时在固定第二层的特殊情况下得到了更紧致的界。技术核心是针对非凸机制下相关噪声 DP 训练的一种新分析路径。时间依赖性破坏了标准单步 SGD 论证背后的条件中心化结构，而投影步骤阻碍了相关扰动的精确抵消结构。我们通过辅助的非投影动力学、吸收当前噪声扰动的偏移迭代项，以及证明投影不活跃的高概率自助法来解决这些困难。将这种优化分析与基于稳定性的泛化论证相结合，得出了所述的总体风险界。据我们所知，这是首个针对凸学习之外（特别是针对神经网络）的 DP 训练相关噪声机制的优化和总体风险分析。

## Abstract
We establish the first population risk bounds for Kolmogorov-Arnold Networks (KANs) trained by mini-batch SGD with gradient clipping, covering non-private SGD as well as differentially private SGD (DP-SGD) with Gaussian perturbations that interpolate between independent and temporally correlated noise. This setting is substantially closer to practice than prior KAN theory along two axes: training is by mini-batch SGD, the standard recipe for modern networks, rather than full-batch gradient descent (GD); and correlated-noise mechanisms have empirically shown a more favorable privacy-utility tradeoff than independent-noise mechanisms. Our results cover the corresponding full-batch GD and independent-noise DP-GD results for KANs by Wang et al. (2026), while yielding sharper fixed-second-layer specializations. The technical core is a new analysis route for correlated-noise DP training in the non-convex regime. Temporal dependence breaks the conditional-centering structure underlying standard one-step SGD arguments, and the projection step obstructs the exact cancellation structure of correlated perturbations. We address these difficulties through an auxiliary unprojected dynamics, a shifted iterate that absorbs the current noise perturbation, and a high-probability bootstrap certifying projection inactivity. Combining this optimization analysis with a stability-based generalization argument yields the stated population risk bounds. To the best of our knowledge, this is the first optimization and population risk analysis of a correlated-noise mechanism for DP training beyond convex learning, in particular for neural networks.

---

## 论文详细总结（自动生成）

这篇论文对使用带相关噪声的差分隐私随机梯度下降（DP-SGD）训练的 Kolmogorov-Arnold 网络（KANs）进行了深入的理论分析。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 KANs 理论研究大多局限于全批量（Full-batch）梯度下降，而实际应用中通常使用带梯度裁剪的小批量（Mini-batch）SGD。此外，在差分隐私（DP）领域，虽然“相关噪声”机制在实践中表现出比传统“独立噪声”更好的隐私-效用权衡，但其在非凸模型（如神经网络）中的理论保证仍然缺失。
*   **研究动机**：KANs 作为多层感知机（MLP）的替代方案，在科学计算和生物建模等敏感数据领域应用广泛。论文旨在填补 KANs 在实际训练机制（小批量、裁剪、相关噪声 DP）下的理论空白，建立首个总体风险界（Population Risk Bounds）。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：通过结合优化误差分析和基于算法稳定性的泛化分析，推导 KANs 在非凸设定下的风险界限。
*   **关键技术细节**：
    *   **噪声模型**：采用 DP-$\lambda$CGD 机制，噪声项为 $\xi_t = \kappa(Z_t - \lambda Z_{t-1})$，其中 $\lambda$ 控制时间相关性。当 $\lambda=0$ 时退化为独立噪声。
    *   **辅助未投影动力学（Auxiliary Unprojected Dynamics）**：为了绕过投影操作对噪声抵消结构的破坏，先分析一个不带投影的辅助轨迹。
    *   **移位迭代（Shifted Iterate）**：引入变量 $U_t = \tilde{W}_t + \eta c_{priv} Z_t$。通过这种变换，将具有时间依赖性的噪声转化为一种更易处理的抵消结构，从而吸收当前的噪声扰动。
    *   **高概率引导（High-probability Bootstrap）**：证明在一定宽度条件下，训练轨迹会以高概率保持在初始化附近的局部区域内，从而使投影操作实际上处于“不活跃”状态。
*   **算法流程**：在每一步迭代中，采样小批量数据，计算并裁剪梯度，添加相关高斯噪声，最后进行参数更新和局部投影。

### 3. 实验设计
*   **性质说明**：本论文是一篇**纯理论证明论文**，其核心贡献在于数学定理的推导。
*   **引用实验**：论文引用了前人（Kalinin et al., 2026）在 **CIFAR-10** 数据集上使用 CNN 进行的实验结果作为动机支撑。该实验展示了相关噪声（$\lambda > 0$）在不同隐私预算 $\epsilon$ 下相比独立噪声能显著提升验证准确率。
*   **对比基准**：理论分析的对比基准是 Wang et al. (2026) 关于 KANs 的全批量 GD 和独立噪声 DP-GD 的研究结果。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的算力消耗（如 GPU 型号或训练时长）。由于其主要工作是数学证明和复杂度的理论推导，不涉及大规模的数值实验。

### 5. 实验数量与充分性
*   **理论覆盖面**：论文通过定理和推论覆盖了多种场景：
    1.  非私有小批量 SGD（$\kappa=0$）。
    2.  独立噪声 DP-SGD（$\lambda=0$）。
    3.  相关噪声 DP-SGD（$\lambda \in (0,1)$）。
    4.  全批量训练（$B=n$）作为特例。
*   **充分性评价**：从理论分析的角度看，实验（即定理的应用场景）是非常充分的。它不仅涵盖了现有研究，还通过更细致的分析（如对 NTK 边际参数 $\gamma$ 的依赖）改进了前人的界限。

### 6. 主要结论与发现
*   **风险界限**：在隐私保护（$\epsilon, \delta$）-DP 下，KANs 达到了 $O(\frac{1}{\sqrt{n}} + \frac{\sqrt{d}}{n\epsilon})$ 的总体风险率，这在对数因子意义下匹配了凸学习的下界。
*   **相关噪声优势**：证明了相关噪声可以减缓累积噪声的增长，从而在优化项中引入 $(1-\lambda)$ 的缩减因子。
*   **宽度要求**：确定了 KANs 训练所需的隐藏层宽度 $m$。在多项式对数宽度下，非私有 SGD 的风险可达到 $O(1/n)$。
*   **理论改进**：相比之前的全批量研究，本论文在固定第二层参数的设定下，减少了对宽度的要求，并得到了更紧致的泛化界。

### 7. 优点：亮点与创新
*   **技术突破**：首次解决了非凸优化中相关噪声与投影操作冲突的分析难题，提出了一套通用的分析框架（移位迭代+辅助动力学）。
*   **贴近实践**：将 KAN 理论从理想化的全批量 GD 推进到了工业界常用的带裁剪的小批量 DP-SGD。
*   **统一性**：一个框架同时涵盖了私有/非私有、独立/相关噪声、小批量/全批量等多种情况。

### 8. 不足与局限
*   **隐私校准保守**：论文提到其使用的闭式隐私校准（Privacy Calibration）较为保守，导致在最终的渐进速率中，相关噪声带来的方差减小效果被噪声倍增器的增加所抵消，未能从理论速率上直接体现出比独立噪声的绝对优势（尽管经验上有效）。
*   **架构限制**：目前仅限于两层 KANs，尚未扩展到更深层的架构。
*   **平滑性假设**：分析依赖于激活函数和 B-样条基函数的平滑性假设，对于某些不平滑的变体可能不适用。
*   **理论与实践的差距**：虽然证明了存在性，但对于如何选择最优的相关参数 $\lambda$ 仍缺乏直接的理论指导。

（完）
