---
title: "Replacing Parameters with Preferences: Federated Alignment of Heterogeneous Vision-Language Models"
title_zh: 以偏好取代参数：异构视觉语言模型的联邦对齐
authors: "Shule Lu, Yujing Wang, Hainan Zhang, Xiaoshan Yang, Hongwei Zheng, Yongxin Tong, Changsheng Xu, Zhiming Zheng"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03426v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 隐私敏感领域的联邦对齐框架
tldr: 针对隐私敏感领域中视觉语言模型（VLM）的联邦学习难题，本文提出MoR框架。该框架通过偏好协作取代传统的参数聚合，解决了客户端在计算资源和模型架构上的异构性。MoR结合了GRPO算法与混合奖励机制（Mixture-of-Rewards），利用本地偏好标注训练奖励模型，并在服务端通过学习型路由自适应融合这些信号，实现了异构环境下高效且隐私保护的模型对齐。
selection_source: fresh_fetch
motivation: 传统的联邦学习难以应对客户端在模型架构和计算资源上的极端异构性，且直接交换参数存在隐私风险。
method: 提出MoR框架，利用本地偏好数据训练奖励模型，并通过混合奖励机制与GRPO算法在服务端对齐基础模型。
result: 在多个视觉语言基准测试上的实验表明，MoR在泛化能力和跨客户端适应性方面均优于现有的联邦对齐基准。
conclusion: MoR为异构视觉语言模型在联邦设置下的隐私保护对齐提供了一种可扩展且高效的解决方案。
---

## 摘要
视觉语言模型 (VLMs) 在医疗和金融等隐私敏感领域具有广泛潜力，但严格的数据共享限制使得集中式训练变得不可行。联邦学习通过实现去中心化训练缓解了这一问题，但在实际部署中，客户端在计算资源、应用需求和模型架构方面的异构性带来了挑战。在极端的模型和数据异构情况下，用基于偏好的协作取代参数聚合提供了一个更合适的接口，因为它消除了直接进行参数或数据交换的需求。受此启发，我们提出了 MoR，这是一个将 GRPO 与奖励混合机制 (Mixture-of-Rewards) 相结合的异构 VLMs 联邦对齐框架。在 MoR 中，每个客户端利用本地偏好标注训练本地奖励模型，在不泄露原始数据的情况下捕获特定的评估信号。为了整合这些异构监督信号，MoR 引入了带有学习路由的奖励混合机制，根据输入和对齐目标自适应地融合客户端奖励模型。随后，服务器使用带有 KL 惩罚（相对于参考模型）的 GRPO 算法优化基础 VLM，从而在无需客户端模型共享架构或参数的情况下实现偏好对齐。在多种公开视觉语言基准测试上的实验表明，MoR 在泛化能力和跨客户端适应性方面始终优于联邦对齐基准方法。我们的方法为联邦设置下异构 VLMs 的隐私保护对齐提供了一个可扩展的解决方案。

## Abstract
Vision-Language Models (VLMs) have broad potential in privacy-sensitive domains such as healthcare and finance, yet strict data-sharing constraints render centralized training infeasible. Federated Learning mitigates this issue by enabling decentralized training, but practical deployments face challenges due to client heterogeneity in computational resources, application requirements, and model architectures. Under extreme model and data heterogeneity, replacing parameter aggregation with preference-based collaboration offers a more suitable interface, as it eliminates the need for direct parameter or data exchange. Motivated by this, we propose MoR, a federated alignment framework that combines GRPO with Mixture-of-Rewards for heterogeneous VLMs. In MoR, each client locally trains a reward model from local preference annotations, capturing specific evaluation signals without exposing raw data. To combine these heterogeneous supervision signals, MoR introduces a Mixture-of-Rewards mechanism with learned routing, which adaptively fuses client reward models according to the input and alignment objective. The server then optimizes a base VLM using GRPO with a KL penalty to a reference model, enabling preference alignment without requiring client models to share architectures or parameters. Experiments on diverse public vision-language benchmarks demonstrate that MoR consistently outperforms federated alignment baselines in generalization and cross-client adaptability. Our approach provides a scalable solution for privacy-preserving alignment of heterogeneous VLMs under federated settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Replacing Parameters with Preferences: Federated Alignment of Heterogeneous Vision-Language Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保护隐私的前提下，对分布在不同客户端、且具有**高度异构性**（包括数据分布、计算资源、模型架构和评价标准不同）的视觉语言模型（VLM）进行有效的偏好对齐。
*   **研究动机**：
    *   VLMs 在医疗、金融等领域应用广泛，但数据隐私限制了集中式训练。
    *   传统的联邦学习（如 FedAvg）依赖参数聚合，要求模型架构一致，且难以处理不同客户端之间相互冲突的偏好信号（例如：有的客户端偏好详细描述，有的偏好简洁警告）。
    *   现有的去中心化对齐方法通常假设奖励模型结构相同，无法应对现实中极端的异构环境。

### 2. 方法论（MoR 框架）
论文提出了 **MoR (Mixture-of-Rewards)** 框架，核心思想是“**以偏好协作取代参数交换**”，分为三个阶段：
1.  **本地奖励模型初始化**：每个客户端利用其私有的偏好数据（Pairwise Preference）独立训练一个本地奖励模型（RM）。通过本地归一化（Normalization）处理，确保不同客户端返回的奖励分数具有可比性。
2.  **联邦路由网络学习**：服务器广播一个轻量级的路由网络 $g_\phi$。客户端利用本地数据训练该路由，学习如何根据输入（图像+文本）将任务分配给最合适的本地奖励模型。服务器仅聚合路由器的参数，而不触及奖励模型或原始数据。
3.  **GRPO 策略对齐**：
    *   服务器使用 **GRPO（群体相对策略优化）** 算法优化全局 VLM 策略。
    *   **混合奖励机制**：对于生成的每个响应，路由器选择最匹配的客户端 RM 提供奖励信号。
    *   **在线路由更新**：引入 **Neural Thompson Sampling（神经汤普森采样）**，在对齐过程中动态更新路由器，以解决策略进化导致的分布偏移（Distribution Shift）问题。

### 3. 实验设计
*   **数据集**：整合了 POVID、VLFeedback 和 MME-RealWorld。涵盖了详细描述、医疗理解、OCR 推理等多个领域，并构建了非独立同分布（non-IID）的客户端环境。
*   **Benchmark 与对比方法**：
    *   **基准**：Qwen2.5-VL-7B-Instruct。
    *   **对比方法**：Single RM（独立训练）、Avg RM（奖励平均）、Random Selection（随机选择）、**FedAvg**（参数聚合）、**PluralLLM-alpha**（先进的联邦偏好聚合方法）。
*   **评估指标**：LLM-as-a-Judge（评估有用性、视觉忠实度、伦理）、MME-RealWorld 准确率（Acc.）、胜率（Win-rate）。

### 4. 资源与算力
*   **硬件**：实验在一台配备 **NVIDIA RTX A6000 PRO (96GB)** GPU 的服务器上运行。
*   **软件框架**：基于修改后的 **EasyRL** 框架实现。
*   **模型规模**：基础模型使用 Qwen2.5-VL-7B，异构实验中还涉及了 Qwen2-VL-2B 和 LLaVA-OneVision-0.5B。
*   **训练细节**：文中未明确给出总训练时长，但提到路由网络仅有 4 层 Transformer，非常轻量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 3 客户端和 4 客户端的扩展性实验。
    *   涵盖了**模型异构**（不同架构的 RM）和**任务异构**（多选 vs 开放式生成）场景。
    *   包含消融实验（验证在线更新和归一化的必要性）。
    *   包含**隐私攻击测试**（成员推理攻击和梯度反演攻击）以及**通信效率分析**。
*   **充分性评价**：实验设计非常全面，不仅验证了性能提升，还深入探讨了在极端异构（如不同 Backbone）下的鲁棒性，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能优越**：MoR 在 MME-RealWorld 等基准上显著优于 FedAvg 和 PluralLLM，ID 平均准确率提升了约 10-26 个百分点。
*   **泛化能力强**：学习到的路由器能够将经验迁移到未见过的任务类别中。
*   **鲁棒性高**：在模型架构完全不同的情况下，MoR 依然能通过路由机制协调各方偏好，而传统的参数聚合方法在此时失效。
*   **隐私与效率**：MoR 极大地减少了通信开销（相比全参数聚合），且能有效抵御针对梯度和成员信息的隐私攻击。

### 7. 优点
*   **架构解耦**：通过“偏好路由”彻底解决了联邦学习中模型必须同构的限制。
*   **动态适应**：在线路由更新机制解决了强化学习中常见的分布偏移问题。
*   **通信高效**：稀疏路由（Sparse Routing）使得每次仅需与一个客户端通信，适合带宽受限的场景。
*   **隐私保护**：不交换 RM 参数，仅交换轻量路由参数和标量奖励值，安全性更高。

### 8. 不足与局限
*   **视觉负载**：虽然参数交换减少了，但在在线评估阶段仍需传输图像数据（Payload），这在视觉任务中仍占据较大带宽。
*   **公共数据依赖**：服务器端对齐仍需要一定规模的公共查询数据集（Public Query Set）。
*   **复杂度**：三阶段的训练流程（本地 RM -> 联邦路由 -> 全局对齐）相比传统方法更为复杂，增加了部署难度。
*   **极端冲突处理**：如果两个客户端对同一输入的偏好完全相反，路由器的决策逻辑可能会面临震荡（尽管文中通过混合奖励有所缓解）。

（完）
