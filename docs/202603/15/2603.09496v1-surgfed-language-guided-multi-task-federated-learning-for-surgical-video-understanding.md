---
title: "SurgFed: Language-guided Multi-Task Federated Learning for Surgical Video Understanding"
title_zh: SurgFed：面向手术视频理解的语言引导多任务联邦学习
authors: "Zheng Fang, Ziwei Niu, Ziyue Wang, Zhu Zhuo, Haofeng Liu, Shuyang Qian, Jun Xia, Yueming Jin"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09496v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 多任务联邦学习框架
tldr: SurgFed是一个针对手术视频理解的多任务联邦学习框架，旨在解决手术场景中的组织多样性和任务多样性挑战。该框架通过引入语言引导的通道选择（LCS）和超聚合（LHA）机制，利用文本输入增强局部模型的站点适应性，并优化跨站点的任务交互。在五个公开数据集上的实验证明，SurgFed在手术场景分割和深度估计任务中均优于现有先进方法。
selection_source: fresh_fetch
motivation: 旨在解决手术视频理解中由于组织多样性导致的局部模型适应难，以及任务多样性导致的服务器端聚合不准确的问题。
method: 提出了SurgFed框架，利用语言引导的通道选择（LCS）实现站点自适应，并通过语言引导的超聚合（LHA）优化跨站点任务交互。
result: 在涵盖四种手术类型的五个公开数据集上，SurgFed在手术场景分割和深度估计任务中均显著优于现有的最先进方法。
conclusion: 通过引入语言先验知识引导联邦学习过程，SurgFed有效提升了多任务模型在异构临床环境下的泛化性能和预测精度。
---

## 摘要
手术场景多任务联邦学习（MTFL）对于机器人辅助微创手术（RAS）至关重要，但由于两个关键挑战，其在手术视频理解领域仍未得到充分探索：(1) 组织多样性：本地模型难以适应特定场所的组织特征，限制了其在异构临床环境中的有效性，并导致本地预测效果不佳。(2) 任务多样性：由于站点间的任务异构性，仅依赖基于梯度的聚类的服务器端聚合往往会产生次优或错误的参数更新，从而导致定位不准确。针对这两个问题，我们提出了 SurgFed，这是一个多任务联邦学习框架，能够跨多种手术类型实现手术场景分割和深度估计的联邦学习。SurgFed 由两个核心设计驱动，即语言引导通道选择（LCS）和语言引导超聚合（LHA），旨在解决跨站点和跨任务充分探索的挑战。在技术上，LCS 首先设计了一个轻量级的个性化通道选择网络，利用预定义的文本输入增强特定站点的适应性，从而优化本地模型对特定嵌入的学习。我们进一步引入了 LHA，它采用带有预定义文本输入的逐层交叉注意力机制，以建模跨站点的任务交互，并引导超网络进行个性化参数更新。大量的实验证据表明，SurgFed 在涵盖四种手术类型的五个公开数据集上，相较于现有最先进的方法均有所提升。代码可在 https://anonymous.4open.science/r/SurgFed-070E/ 获取。

## Abstract
Surgical scene Multi-Task Federated Learning (MTFL) is essential for robot-assisted minimally invasive surgery (RAS) but remains underexplored in surgical video understanding due to two key challenges: (1) Tissue Diversity: Local models struggle to adapt to site-specific tissue features, limiting their effectiveness in heterogeneous clinical environments and leading to poor local predictions. (2) Task Diversity: Server-side aggregation, relying solely on gradient-based clustering, often produces suboptimal or incorrect parameter updates due to inter-site task heterogeneity, resulting in inaccurate localization. In light of these two issues, we propose SurgFed, a multi-task federated learning framework, enabling federated learning for surgical scene segmentation and depth estimation across diverse surgical types. SurgFed is powered by two appealing designs, i.e., Language-guided Channel Selection (LCS) and Language-guided Hyper Aggregation (LHA), to address the challenge of fully exploration on corss-site and cross-task. Technically, the LCS is first designed a lightweight personalized channel selection network that enhances site-specific adaptation using pre-defined text inputs, which optimally the local model learn the specific embeddings. We further introduce the LHA that employs a layer-wise cross-attention mechanism with pre-defined text inputs to model task interactions across sites and guide a hypernetwork for personalized parameter updates. Extensive empirical evidence shows that SurgFed yields improvements over the state-of-the-art methods in five public datasets across four surgical types. The code is available at https://anonymous.4open.science/r/SurgFed-070E/.

---

## 论文详细总结（自动生成）

这是一份关于论文《SurgFed: Language-guided Multi-Task Federated Learning for Surgical Video Understanding》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：机器人辅助微创手术（RAS）依赖于对手术场景的深度理解（如器械分割、深度估计）。为了提升模型性能，需要多中心协作训练，但患者隐私和数据传输限制使得直接共享数据不可行。
*   **核心挑战**：
    1.  **组织多样性（Tissue Diversity）**：不同临床站点的解剖背景、手术类型和器械差异巨大，导致本地模型难以适应特定站点的特征。
    2.  **任务多样性（Task Diversity）**：不同站点可能执行不同的任务（分割 vs. 深度估计），且标签空间不重叠。传统的联邦学习（FL）聚合机制（如基于梯度的聚类）在处理这种异构任务时容易产生次优或错误的参数更新。
*   **研究目的**：提出一种多任务联邦学习框架 **SurgFed**，通过引入语言先验知识来引导模型在异构环境下的自适应与协作。

### 2. 方法论
SurgFed 的核心思想是利用**预定义文本（Language-guided）**作为桥梁，连接视觉特征与手术领域知识。
*   **语言引导通道选择（LCS, Local-side）**：
    *   在本地端，利用预训练的 CLIP 模型提取文本嵌入（描述数据集、任务类型及具体标签，如“器械分割：轴、腕部”）。
    *   设计轻量级通道选择网络，将文本嵌入与视觉特征融合，通过全局平均池化和全连接层生成指示器，对特征通道进行像素级加权增强，实现站点特异性的特征表达。
*   **语言引导超聚合（LHA, Server-side）**：
    *   在服务器端，利用逐层交叉注意力机制（Layer-wise Cross-Attention）建模各站点梯度更新之间的交互。
    *   结合文本提示引导一个**超网络（Hypernetwork）**。该超网络根据任务相似性和语义关联，动态计算每个站点的个性化聚合权重，从而优化全局参数向本地模型的更新过程。
*   **骨干网络**：采用 **SAM2 (Segment Anything Model 2)** 作为基础架构，冻结编码器，仅微调解码器（Decoder）和存储模块（Memory）。

### 3. 实验设计
*   **数据集**：使用了 5 个公开的手术视频数据集，涵盖 4 种手术类型：
    1.  **EndoVis2017**（器械零件分割）
    2.  **EndoVis2018**（手术场景分割）
    3.  **AutoLaparo**（腹腔镜子宫切除场景分割）
    4.  **SCARED**（猪腹部深度估计）
    5.  **StereoMIS**（活体深度估计）
*   **对比方法（Baselines）**：
    *   **单机训练**：Local Train, Inference Only (Zero-shot)。
    *   **传统/个性化 FL**：FedAvg, FedProx, FedRep。
    *   **多任务 FL**：MaT-FL, FedHCA 2。
*   **评价指标**：Dice、IoU（分割任务）；RMSE（深度估计）；$\Delta m$（综合性能提升率）。

### 4. 资源与算力
*   **硬件环境**：论文提到在 **NVIDIA RTX A5000** GPU 上进行了推理效率测试。
*   **训练细节**：
    *   本地更新轮数 $E=3$，总通信轮数 $T=100$。
    *   优化器：Adam，学习率分别为 1e-4（本地）和 1e-3（服务器超网络）。
    *   参数量：LCS 模块引入约 0.25MB 的额外参数，服务器端超网络同样极轻量。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个具有显著领域差异（Domain Shift）的数据集上进行了验证，涵盖了从器械到解剖组织、从分割到回归（深度）的多种任务组合。
*   **消融实验**：
    *   验证了 LCS 和 LHA 模块的各自贡献（LCS 对分割提升大，LHA 对深度估计提升显著）。
    *   对比了不同指示器设计（随机初始化 vs. One-hot vs. 文本嵌入）。
    *   分析了微调 SAM2 不同层（Decoder vs. Memory）对结果的影响。
*   **客观性**：实验对比了多种最新的联邦学习变体，并使用了统一的 SAM2 骨干网络，确保了比较的公平性。

### 6. 主要结论与发现
*   **性能领先**：SurgFed 在所有 5 个站点上均优于 SOTA 方法，平均性能提升率 $\Delta m$ 达到 **+5.92%**。
*   **语言引导的价值**：文本提示能有效弥补通用预训练模型（如 CLIP）在手术专业领域的知识鸿沟，帮助模型在异构站点间找到共性特征。
*   **任务协同**：LHA 机制证明了分割任务和深度估计任务之间存在可利用的互补信息，通过语义引导的聚合可以避免负迁移。

### 7. 优点
*   **创新性**：首次将语言引导的手术先验知识引入多任务联邦学习框架。
*   **高效性**：通过轻量级适配器（Adapter）和超网络实现个性化，通信开销极低，且无需共享任何隐私数据。
*   **鲁棒性**：在跨手术类型、跨物种（猪 vs. 人）的极端异构场景下表现稳定。

### 8. 不足与局限
*   **实时性挑战**：虽然 LCS 模块增加的延迟极小，但受限于 SAM2 骨干网络，整体推理速度仅为 **0.36 FPS**，远未达到实时手术（通常需 >20 FPS）的要求。
*   **文本依赖性**：模型性能可能受到预定义文本描述准确性的影响，如何自动生成或优化这些 Prompt 尚未讨论。
*   **任务广度**：目前仅验证了分割和深度估计，对于手术阶段识别、动作识别等时序性更强的多任务场景尚待验证。

（完）
