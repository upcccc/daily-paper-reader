---
title: Sherpa.ai Privacy-Preserving Multi-Party Entity Alignment without Intersection Disclosure for Noisy Identifiers
title_zh: Sherpa.ai：针对含噪声标识符且无交集泄露的隐私保护多方实体对齐
authors: "Daniel M. Jimenez-Gutierrez, Enrique Zuazua, Georgios Kellaris, Joaquin Del Rio, Oleksii Sliusarenko, Xabi Uribe-Etxebarria"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19219v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 纵向联邦学习中的隐私保护实体对齐
tldr: 本研究针对纵向联邦学习中的隐私保护实体对齐（PPEA）问题，提出了Sherpa.ai多方私有集合并（PSU）协议。该协议解决了传统PSI泄露交集成员身份的问题，支持多方参与且具备容错匹配能力。通过引入保序和无序两种变体，实现了在不暴露样本交集的前提下，对包含拼写错误的标识符进行高效对齐，为医疗、金融等领域的跨机构协作提供了安全、可扩展的对齐方案。
selection_source: fresh_fetch
motivation: 传统的实体对齐方法（如PSI）会泄露参与方之间的交集信息，且现有PSU方案多局限于两方或不支持模糊匹配。
method: 提出一种支持多方的PSU协议，包含用于精确对齐的保序版本和支持容错匹配的无序版本，并建立了通用的索引映射机制。
result: 该协议在保证正确性和隐私性的同时，实现了较低的通信和计算开销，并能有效处理标识符中的拼写和格式差异。
conclusion: Sherpa.ai PSU协议为多方纵向联邦学习提供了一个数学严谨、可扩展且保护交集隐私的实体对齐解决方案。
---

## 摘要
联邦学习 (FL) 能够在不集中原始数据的情况下，实现多方之间的协作模型训练。联邦学习主要有两种范式：横向联邦学习 (HFL)，即所有参与方共享相同的特征空间但持有不同的样本；以及纵向联邦学习 (VFL)，即各方针对同一组样本持有互补的特征。纵向联邦学习训练的前提是隐私保护实体对齐 (PPEA)，它在不泄露各方之间共享了哪些样本的情况下，建立跨参与方的通用样本索引（对齐）。传统的隐私集合求交 (PSI) 虽然可以实现对齐，但会泄露交集成员身份，从而暴露数据集之间的敏感关系。标准的隐私集合求并 (PSU) 通过在标识符的并集而非交集上进行对齐来降低这一风险。然而，现有方法通常局限于两方，或者缺乏对容错匹配（如拼写错误）的支持。在本文中，我们介绍了用于纵向联邦学习的 Sherpa.ai 多方 PSU 协议，这是一种能够隐藏交集成员身份并支持精确匹配和模糊匹配的 PPEA 方法。该协议以较低的通信开销将两方方法推广到多方，并提供两个变体：用于精确对齐的保序版本，以及能够容忍拼写和格式差异的无序版本。我们证明了其正确性和隐私性，分析了通信和计算（指数运算）复杂度，并形式化了从本地记录到共享索引空间的通用索引映射。这种多方 PSU 为现实世界的纵向联邦学习部署提供了一种可扩展且具有数学基础的 PPEA 协议，例如多机构医疗疾病检测、银行与保险公司之间的协作风险建模，以及电信与金融机构之间的跨域欺诈检测，同时保护了交集隐私。

## Abstract
Federated Learning (FL) enables collaborative model training among multiple parties without centralizing raw data. There are two main paradigms in FL: Horizontal FL (HFL), where all participants share the same feature space but hold different samples, and Vertical FL (VFL), where parties possess complementary features for the same set of samples. A prerequisite for VFL training is privacy-preserving entity alignment (PPEA), which establishes a common index of samples across parties (alignment) without revealing which samples are shared between them. Conventional private set intersection (PSI) achieves alignment but leaks intersection membership, exposing sensitive relationships between datasets. The standard private set union (PSU) mitigates this risk by aligning on the union of identifiers rather than the intersection. However, existing approaches are often limited to two parties or lack support for typo-tolerant matching.   In this paper, we introduce the Sherpa.ai multi-party PSU protocol for VFL, a PPEA method that hides intersection membership and enables both exact and noisy matching. The protocol generalizes two-party approaches to multiple parties with low communication overhead and offers two variants: an order-preserving version for exact alignment and an unordered version tolerant to typographical and formatting discrepancies. We prove correctness and privacy, analyze communication and computational (exponentiation) complexity, and formalize a universal index mapping from local records to a shared index space. This multi-party PSU offers a scalable, mathematically grounded protocol for PPEA in real-world VFL deployments, such as multi-institutional healthcare disease detection, collaborative risk modeling between banks and insurers, and cross-domain fraud detection between telecommunications and financial institutions, while preserving intersection privacy.

---

## 论文详细总结（自动生成）

### Sherpa.ai 隐私保护多方实体对齐协议总结

#### 1. 核心问题与整体含义（研究动机和背景）
在纵向联邦学习（VFL）中，各参与方持有同一组样本的不同特征。训练前的首要任务是**实体对齐（Entity Alignment）**。
*   **研究动机**：传统的隐私集合求交（PSI）虽然能对齐样本，但会泄露“交集成员身份”。例如，银行与癌症诊所对齐数据时，若发现某客户在交集中，银行即可推断该客户患有癌症，造成隐私泄露。
*   **核心挑战**：现有的隐私集合求并（PSU）方案虽然能隐藏交集信息，但大多局限于两方参与，且对现实中常见的“噪声标识符”（如拼写错误、格式不一）缺乏容错能力。
*   **整体含义**：本文提出了 Sherpa.ai 多方 PSU 协议，旨在实现一个既能隐藏交集成员身份、又支持多方参与、且能处理噪声数据的通用实体对齐框架。

#### 2. 论文提出的方法论
该协议基于**交换加密（Commutative Encryption）**和 **n-gram 分词**技术，主要流程如下：
*   **n-gram 令牌化**：将原始标识符（如姓名、地址）切分为重叠的 n-gram 字符串，以增强对拼写错误和格式差异的鲁棒性。
*   **哈希与投影**：使用 SHA3-256 将令牌哈希并投影到二次剩余群 $QR(Z_p^*)$ 中，利用离散对数难题保证安全性。
*   **多方交换加密协议**：
    *   **保序版本（精确匹配）**：各方使用私钥对哈希值进行循环加密和洗牌。由于加密具有交换性（先 A 后 B 加密等于先 B 后 A），最终所有参与方能得到一致的加密标识符，从而在不暴露原始值的情况下计算并集。
    *   **无序版本（模糊匹配）**：不考虑 n-gram 的顺序，通过计算两个加密标识符之间匹配令牌的数量是否超过阈值 $\lambda$ 来判定是否为同一实体。
*   **通用索引映射**：建立从本地记录到全局统一索引空间的映射。
*   **合成数据填充**：对齐后，对于并集中缺失特征的样本，使用 SDV（如 CTGAN 或高斯 Copula）生成隐私保护的合成数据进行填充，以便后续 VFL 训练。

#### 3. 实验设计
*   **数据集/场景**：论文主要从理论和算法流程角度进行论述，提及了医疗疾病检测、银行与保险协作风险建模、电信与金融欺诈检测等应用场景。
*   **Benchmark 与对比方法**：论文在理论层面对比了标准 PSI、标准 PSU 以及 Sherpa.ai PSU。对比维度包括：是否交换原始标识符、是否泄露交集、是否支持多方、是否支持噪声对齐（见文中 Table 1）。
*   **注意**：该 PDF 文本主要侧重于协议设计、数学证明和复杂度分析，未展示具体的数值实验结果（如在特定公开数据集上的准确率或耗时曲线）。

#### 4. 资源与算力
*   **算力说明**：文中**未明确说明**使用了何种具体的 GPU/CPU 型号或具体的训练时长。
*   **复杂度分析**：论文分析了计算复杂度，指出主要开销在于大素数模下的指数运算（Modular Exponentiation），并强调这些运算可以并行化处理。

#### 5. 实验数量与充分性
*   **实验充分性**：作为一篇偏向**协议设计与形式化证明**的论文，其“实验”主要体现在对算法逻辑的严密推导和对不同对齐范式的对比分析上。
*   **局限性**：由于缺乏大规模真实数据集的实测性能数据（如通信量随参与方数量增加的具体增长曲线），其在超大规模工业场景下的实际表现尚需进一步验证。

#### 6. 论文的主要结论与发现
*   **多方扩展性**：成功将两方 PSU 协议推广至 P 方，且通信轮数保持在较低水平。
*   **隐私保护**：证明了在半诚实模型下，该协议能有效隐藏交集成员身份，防止敏感属性推断。
*   **鲁棒性**：通过无序 n-gram 匹配机制，解决了现实数据中因拼写错误导致对齐失败的问题。
*   **VFL 兼容性**：该协议作为预处理步骤，可无缝集成到 SBVFL（盲纵向联邦学习）等后续训练框架中。

#### 7. 优点
*   **隐私等级高**：相比 PSI，PSU 提供了更强的成员身份保护。
*   **容错性强**：引入 n-gram 和相似度阈值，使其在处理非标准化的现实数据时比传统精确匹配方法更实用。
*   **数学严谨**：基于 Diffie-Hellman 原理和交换加密，提供了清晰的数学定义和算法流程。
*   **灵活性**：提供了保序和无序两种变体，平衡了精确度与鲁棒性。

#### 8. 不足与局限
*   **计算开销**：模指数运算在大规模数据集（如千万级）上可能存在计算瓶颈，虽然文中提到可用 Bloom 过滤器优化，但未给出实测加速效果。
*   **误报风险**：无序匹配模式下，若阈值 $\lambda$ 设置不当，可能引入假阳性（False Positive），即将不同实体误判为同一实体。
*   **实证缺失**：论文缺乏在标准 Benchmark 数据集上的实测性能对比，难以直观评估其相对于其他高性能 PSU 变体（如基于 OPRF 的方案）的运行效率。

（完）
