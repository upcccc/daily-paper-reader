---
title: Towards Differentially Private Reinforcement Learning with General Function Approximation
title_zh: 迈向具有通用函数逼近的差分隐私强化学习
authors: "Yi He, Xingyu Zhou"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.07049v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 结合指数机制的差分隐私强化学习
tldr: "本研究首次为具有通用函数逼近的差分隐私（DP）在线强化学习提供了理论保证，突破了以往仅限于表格和线性设置的局限。通过结合批处理策略更新方案与指数机制，并引入创新的遗憾分析方法，证明了在无模型设置下，其遗憾界与线性设置的最优结果一致，达到 O(K^{3/5})。此外，研究还建立了基于覆盖性复杂度的遗憾界，并纠正了以往线性隐私强化学习研究中的关键性错误。"
selection_source: fresh_fetch
motivation: 旨在解决现有差分隐私强化学习理论仅局限于简单表格或线性模型，无法适应复杂通用函数逼近的问题。
method: 提出了一种结合批处理策略更新方案与指数机制的新算法，并配套了全新的遗憾分析框架。
result: "在通用函数逼近下实现了与线性设置相当的 O(K^{3/5}) 遗憾界，并首次建立了基于覆盖性的批处理在线强化学习遗憾界。"
conclusion: 该研究填补了通用函数逼近下隐私强化学习的理论空白，并澄清了该领域以往研究中的关键性错误。
---

## 摘要
我们为具有通用函数逼近的差分隐私在线强化学习（RL）提供了首个理论保证，将其从以往局限于表格和线性设置的研究扩展到了更广泛的领域。我们的方法结合了批量策略更新方案与指数机制，并配合一种新颖的遗憾分析。我们证明，即使在通用函数逼近下，差分隐私下无模型设置中的遗憾值也达到了线性情况下的最优水平，其规模为 $\widetilde{O}(K^{3/5})$，其中 $K$ 表示回合数。作为一个重要的副产品，我们还为基于批量更新的在线强化学习建立了首个依赖于标准复杂度度量“可覆盖性”（coverability）的遗憾界，补充了基于新引入的 Eluder-Condition 类的现有结果。此外，我们揭示了近期关于线性函数逼近隐私强化学习结果中的根本性缺陷，从而澄清了该领域的研究现状。

## Abstract
We present the first theoretical guarantees for differentially private online reinforcement learning (RL) with general function approximation, extending beyond prior work restricted to tabular and linear settings. Our approach combines a batched policy update scheme with the exponential mechanism, together with a novel regret analysis. We show that, even under general function approximation, the regret in the model-free setting under differential privacy matches the state of the art for the linear case, scaling as $\widetilde{O}(K^{3/5})$, where $K$ denotes the number of episodes. As an important by-product, we also establish the first regret bound for online RL with batch update that depends on the standard complexity measure of coverability, complementing existing results based on a newly introduced Eluder-Condition class. In addition, we uncover fundamental gaps in recent results for private RL with linear function approximation, thereby clarifying its landscape.

---

## 论文详细总结（自动生成）

### 论文总结：具有通用函数逼近的差分隐私强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
在个性化推荐、医疗决策和大型语言模型（LLM）优化等现实场景中，强化学习（RL）代理需要处理敏感的用户数据。虽然差分隐私（DP）为保护隐私提供了数学框架，但现有的隐私强化学习理论研究大多局限于**表格（Tabular）**或**线性（Linear）**马尔可夫决策过程（MDP）。
*   **研究动机**：现代 RL 系统通常使用深度神经网络等复杂的非线性函数逼近器。现有的隐私保护技术（如基于低维统计量的置信集）难以直接扩展到通用函数类。
*   **核心问题**：在满足联合差分隐私（JDP）约束的前提下，具有通用函数逼近的在线 RL 是否仍能实现次线性遗憾（Sublinear Regret）？

#### 2. 论文提出的方法论
论文提出了一种通用的算法框架，主要包含以下核心思想和技术细节：
*   **无约束优化目标**：不同于非隐私 RL 中常用的基于置信集的受限优化，本文采用了一种显式平衡探索与利用的无约束目标函数。这使得算法可以直接应用标准的隐私机制。
*   **批量策略更新（Batch Update）**：为了控制重复更新带来的累积隐私损耗，算法将 $K$ 个回合分为固定大小的批次（Batch Size 为 $B$），仅在每个批次开始时更新策略。这有效地将隐私组合（Composition）次数从 $O(K)$ 降低到 $O(K/B)$。
*   **指数机制（Exponential Mechanism）**：在更新策略时，算法不直接选取最优函数，而是根据得分函数（结合了贝尔曼损失和乐观项）通过指数机制进行采样，从而实现隐私化。
*   **针对不同场景的算法**：
    *   **Algorithm 1 (通用 MDP)**：要求“贝尔曼完备性”，使用贝尔曼误差损失。
    *   **Algorithm 2 (确定性 MDP + 结果奖励)**：针对 LLM 等应用建模，仅要求“可实现性”，使用结果贝尔曼残差损失。

#### 3. 实验设计
由于本文侧重于理论突破，实验部分被设计为**概念验证（Proof-of-concept）**：
*   **场景**：一个具有确定性转移和结果奖励（Outcome Rewards）的合成 MDP 环境，模拟上下文相关的决策任务。
*   **函数类**：一个包含 243 个非线性假设的有限假设类，涉及位运算（XOR）和历史依赖规则。
*   **Benchmark 与对比方法**：
    *   **非隐私无批量（Non-private, no batch）**：基准上限。
    *   **非隐私批量（Non-private, batched）**：评估批量处理带来的性能损失。
    *   **隐私批量（Private, $\epsilon=8$ 和 $\epsilon=5$）**：评估不同隐私强度对遗憾值的影响。
*   **评估指标**：累积遗憾曲线和“平台回合数”（Plateau Episode，即遗憾趋于稳定所需的时间）。

#### 4. 资源与算力
*   **算力消耗**：论文明确指出实验是在**标准 CPU 机器**上运行的，运行时间仅需**几分钟**。
*   **硬件需求**：未使用 GPU。这反映了实验规模较小，主要用于验证理论趋势而非测试大规模计算性能。

#### 5. 实验数量与充分性
*   **实验规模**：仅在“简单”和“困难”两种合成环境配置下进行了测试。
*   **充分性评价**：从实证角度看，实验**并不充分**。它没有在标准 RL 基准数据集（如 Atari 或 MuJoCo）上运行，也没有处理连续状态空间。
*   **客观性**：实验设计是客观的，清晰地展示了隐私保护、批量大小与学习速度之间的权衡，符合其作为理论论文补充实验的定位。

#### 6. 主要结论与发现
*   **遗憾界保证**：在 JDP 约束下，通用函数逼近的 RL 可以实现 $O(K^{3/5})$ 的遗憾界。这与目前线性模型在无模型（Model-free）设置下的最优隐私遗憾界一致。
*   **复杂度度量**：首次建立了依赖于“可覆盖性”（Coverability）的批量更新 RL 遗憾分析，证明了批量更新在通用函数类中的可行性。
*   **理论纠偏**：论文指出近期两篇声称在线性 MDP 下实现 $\sqrt{K}$ 隐私遗憾界的论文（Ngo et al. 2022; Sahu 2025）存在根本性分析漏洞（关于私有设计矩阵的单调性失效问题），澄清了该领域的理论边界。
*   **确定性 MDP 的优势**：在确定性转移下，只需“可实现性”假设即可获得类似的隐私遗憾保证，且无需复杂的极大极小优化。

#### 7. 优点
*   **理论开创性**：填补了通用函数逼近下隐私强化学习的理论空白。
*   **分析框架通用**：提出的批量更新+指数机制框架具有模块化特征，易于扩展到其他隐私机制。
*   **严谨性**：通过揭示前人研究的漏洞，展现了深厚的理论功底，确保了结论的可靠性。

#### 8. 不足与局限
*   **遗憾界差距**：$K^{3/5}$ 的遗憾界虽然匹配了线性设置的最优水平，但仍高于非隐私设置下的 $\sqrt{K}$，隐私代价依然显著。
*   **计算效率**：指数机制在处理无限或极大规模函数类时，需要构建 $\beta$-覆盖集，这在计算上可能是不可行的（NP-hard）。
*   **实验覆盖面窄**：缺乏真实世界大规模数据集的验证，算法在深度神经网络上的实际表现尚不明确。
*   **假设限制**：Algorithm 1 仍依赖于较强的“贝尔曼完备性”假设。

（完）
