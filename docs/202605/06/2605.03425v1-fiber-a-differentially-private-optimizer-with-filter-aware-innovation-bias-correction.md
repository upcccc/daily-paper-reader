---
title: "FIBER: A Differentially Private Optimizer with Filter-Aware Innovation Bias Correction"
title_zh: FiBeR：一种具有滤波器感知创新偏差修正的差分隐私优化器
authors: "Duc Dm, Thao Do, Minh Son Hoang, Anh Le Duc Tran, Daeyoung Kim, Huy Nguyen"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03425v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 在梯度中注入噪声的差分隐私训练
tldr: 差分隐私（DP）训练通过加噪保护隐私，但噪声会干扰AdamW等自适应优化器。现有方法通过时间滤波降低方差，却导致二阶矩偏差修正失效。本文提出FiBeR优化器，通过在创新空间进行去噪、解耦观测几何与增益，并引入针对滤波器的二阶矩校准机制，有效解决了滤波后的噪声统计失调问题。在视觉和语言任务中，FiBeR在相同隐私预算下显著超越了现有SOTA结果。
selection_source: fresh_fetch
motivation: 现有的差分隐私优化器在引入时间滤波降低方差时，会导致二阶矩的偏差修正因噪声统计特性改变而失效。
method: 提出FiBeR优化器，在创新空间进行去噪，并引入一种能够根据滤波器特性自动调整的二阶矩校准机制。
result: 在多个视觉和语言基准测试中，FiBeR在相同的隐私约束下均取得了优于现有最先进方法的性能。
conclusion: FiBeR通过精确校准滤波后的噪声偏差，显著提升了差分隐私自适应优化器的训练效率和模型精度。
---

## 摘要
差分隐私（DP）训练通过在梯度中添加噪声来保护个体样本，但注入的噪声与自适应优化器之间存在非平凡的相互作用。最近的 DP 方法对私有化梯度进行时间滤波以降低方差；然而，滤波也改变了 AdamW 二阶矩累加器所观察到的 DP 噪声统计特性。因此，针对未滤波 DP 噪声推导的偏差修正（例如减去 sigma_w 的平方）在存在滤波时可能会变得失准。我们提出了 FiBeR，这是一种专为时间滤波私有化梯度设计的 DP 优化器。FiBeR (i) 通过对残差流进行滤波并将其积分以形成滤波后的梯度估计，在创新空间中执行去噪；(ii) 将两点观测几何与创新增益解耦，以实现独立调优；(iii) 引入了滤波器感知的二阶矩校准，减去衰减后的 DP 噪声贡献 A(omega) sigma_w 平方，其中 A(omega) 是针对创新滤波器以闭式形式推导的，并且可以针对一般的稳定线性滤波器进行计算。在视觉和语言基准测试中，FiBeR 一致地展示了 DP 优化器性能的显著提升，在多个任务的等效隐私约束下超越了现有最先进的结果。

## Abstract
Differentially private (DP) training protects individual examples by adding noise to gradients, but the injected noise interacts nontrivially with adaptive optimizers. Recent DP methods temporally filter privatized gradients to reduce variance; however, filtering also changes the DP noise statistics seen by AdamW's second-moment accumulator. As a result, bias corrections derived for unfiltered DP noise, such as subtracting sigma_w squared, can become miscalibrated when filtering is present.   We propose FiBeR, a DP optimizer designed for temporally filtered privatized gradients. FiBeR (i) performs denoising in innovation space by filtering the residual stream and integrating it to form the filtered gradient estimate, (ii) decouples the two-point observation geometry from the innovation gain to enable independent tuning, and (iii) introduces a filter-aware second-moment calibration that subtracts the attenuated DP noise contribution A(omega) sigma_w squared, where A(omega) is derived in closed form for the innovation filter and can be computed for general stable linear filters.   Across vision and language benchmarks, FiBeR consistently demonstrates substantial improvements in the performance of DP optimizers, surpassing state-of-the-art results under equivalent privacy constraints on multiple tasks.

---

## 论文详细总结（自动生成）

### 论文总结：FIBER —— 具有滤波器感知创新偏差修正的差分隐私优化器

#### 1. 核心问题与整体含义
在差分隐私（DP）机器学习中，为了保护隐私，通常会在梯度中注入高斯噪声。然而，这些噪声会与 AdamW 等自适应优化器的状态（一阶矩和二阶矩）产生复杂的相互作用。特别是二阶矩累加器会将均值为零的噪声转化为持久的正向偏差，导致步长缩减和优化器失效。
**研究动机：** 虽然最近的研究通过时间滤波（Temporal Filtering）来降低噪声方差，但滤波会改变噪声的统计特性。如果直接套用针对未滤波噪声设计的偏差修正方法，会导致二阶矩估计失准。本文旨在解决滤波后的私有化梯度与自适应优化器之间的统计失调问题。

#### 2. 方法论
FIBER（Filter-aware Innovation Bias-corrected optimizER）的核心思想是通过精确建模滤波器对噪声的衰减作用，来校准优化器的预处理器。
*   **创新空间滤波（Innovation-space Filtering）：** 不同于直接平滑梯度状态，FIBER 维护一个残差（创新）流，通过平滑残差并积分来估计梯度。这是一种二阶递归形式，相比一阶 EMA 平滑，能更好地追踪梯度的非平稳漂移。
*   **参数解耦：** 将两点观测几何参数（$\kappa, \gamma$，用于构建梯度观测）与创新增益参数（$\omega$，用于时间去噪）解耦，实现了观测几何与平滑强度的独立调优。
*   **滤波器感知偏差修正：** 论文推导了稳定线性滤波器对噪声方差的衰减因子 $A(\omega)$。对于 FIBER 的创新滤波器，其衰减因子为闭式解 $A(\omega) = (2-\omega)/(4-3\omega)$。在更新 AdamW 二阶矩时，减去该衰减后的噪声贡献，从而获得更准确的预处理器。

#### 3. 实验设计
*   **数据集与场景：**
    *   **计算机视觉（CV）：** MNIST、CIFAR-10/100（从头训练）、ImageNet-1k（从头训练）、ViT-small 在 CIFAR-100 上的微调。
    *   **自然语言处理（NLP）：** RoBERTa 在 GLUE 基准上的微调。
    *   **文本生成（NLG）：** GPT-2 在 E2E 数据集上的微调。
*   **Benchmark 与对比方法：**
    *   基线包括：DP-AdamW（标准无滤波）、DiSK（状态滤波）、DOPPLER（低通滤波）、MF-DP-FTRL（相关噪声机制）。
    *   消融基线：DiSK-CORR（为 DiSK 增加本文提出的滤波器感知修正）。

#### 4. 资源与算力
*   **硬件设备：** 使用了 NVIDIA RTX 4090 (24GB)、RTX 5090 (32GB) 和 RTX Pro 6000 (96GB) GPU。
*   **训练时长：** 论文提到最耗时的任务（ImageNet-1k 上的 ViT-small）在 15 天内完成。
*   **计算开销：** 由于采用两点梯度观测，FIBER 的单步计算量约为 DP-AdamW 的 1.79 倍（理论上限为 2 倍），内存开销仅增加约 7.6%。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从小型 CNN 到大型 Transformer（ViT, RoBERTa, GPT-2）的多种架构，跨越了视觉、分类、理解和生成等多个领域。
*   **调优充分性：** 每个优化器在每个任务上都进行了 100 次独立的网格搜索调优，确保了对比的公平性。
*   **验证深度：** 进行了合成漂移诊断实验、方差衰减因子的经验验证以及针对二阶矩修正假设的审计实验。整体实验设计非常详尽且客观。

#### 6. 主要结论与发现
*   **性能领先：** FIBER 在所有视觉和语言基准测试中均显著优于 DP-AdamW 和 DiSK，尤其在严苛的隐私预算（低 $\epsilon$）和长周期训练中优势更明显。
*   **校准的重要性：** 实验证明，如果不考虑滤波器的衰减作用而盲目减去原始噪声方差，会导致优化器性能下降；而 FIBER 的精确校准能有效防止预处理器膨胀。
*   **创新滤波的优势：** 在处理具有持续漂移特征的梯度信号时，创新空间滤波比传统的梯度平滑更具鲁棒性。

#### 7. 优点
*   **理论与实践结合：** 论文不仅提出了算法，还为线性滤波器下的噪声传播提供了严谨的理论分析和闭式解。
*   **即插即用：** FIBER 作为一种优化器，可以方便地集成到现有的 DP 训练框架中。
*   **参数解耦：** 解决了以往方法中观测几何与平滑增益耦合导致的调参难题。

#### 8. 不足与局限
*   **计算成本：** 相比单点梯度的 DP-AdamW，FIBER 需要两次反向传播，虽然精度提升显著，但在算力受限场景下存在 1.8 倍左右的时间开销。
*   **假设依赖：** 偏差修正依赖于“信号与噪声不相关”的假设，虽然实验证明在闭环训练中这种偏差较小，但在某些极端非线性动态下可能失效。
*   **超参数增加：** 尽管进行了解耦，但相比标准优化器，仍引入了 $\kappa, \gamma, \omega$ 等额外超参数，增加了初始配置的复杂度。

（完）
