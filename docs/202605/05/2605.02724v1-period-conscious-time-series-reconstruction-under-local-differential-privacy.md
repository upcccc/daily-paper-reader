---
title: Period-conscious Time-series Reconstruction under Local Differential Privacy
title_zh: 本地差分隐私下周期感知的时间序列重构
authors: "Yaxuan Wang, Tianxin Li, Enji Liang, Yue Fu, Yanran Wang"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02724v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 本地差分隐私下的时间序列重构
tldr: 针对多媒体信号中周期性模式在本地差分隐私（LDP）保护下易受噪声干扰导致相位漂移和周期估计失效的问题，本文提出CPR（周期与相位恢复）框架。该框架通过多尺度周期探测、多共识选择及相位对齐聚合，结合EM去噪和核密度估计，有效恢复了受扰动时间序列的周期结构。实验证明CPR在低隐私预算下显著优于现有LDP基准，提升了重构精度并保持了良好的周期特性。
selection_source: fresh_fetch
motivation: 本地差分隐私引入的噪声会破坏时间序列的频谱峰值并导致相位漂移，使得周期估计和数据重构在隐私保护环境下变得极不可靠。
method: 提出CPR框架，利用多尺度周期探测和多共识选择抑制噪声干扰，并通过相位对齐聚合与EM去噪技术恢复底层相位值。
result: 在两个真实周期性数据集上的实验表明，CPR在保持周期结构和降低重构误差方面均一致优于代表性LDP基准，尤其在低隐私预算下表现突出。
conclusion: CPR框架有效解决了LDP环境下周期性时间序列的重构难题，通过感知周期特性显著提升了隐私保护数据的实用性。
---

## 摘要
周期性模式是多媒体信号和系统中的基本线索，包括视频中的重复动作（如步态周期）、音频中的节奏和音高相关结构，以及图像序列中的循环纹理。当从边缘设备收集此类用户生成的流数据时，本地差分隐私（LDP）因其在上传前对数据进行扰动而具有吸引力；然而，注入的噪声可能会破坏频谱峰值并引起相位漂移，导致周期估计不可靠并降低重构质量。我们提出了 CPR（Cycle and Phase Recovery，周期与相位恢复），这是一种针对 LDP 下周期性时间序列的周期感知重构框架。CPR 执行多尺度周期探测和多共识选择，以抑制噪声引起的频谱干扰，然后在匹配的周期内相位位置聚合扰动样本，以稳定跨周期的相位对齐。为了恢复潜在的各相位值，CPR 将基于 EM 的去噪与核密度估计相结合，提高了在严格隐私预算下的鲁棒性。在两个真实世界周期性数据集上的实验表明，与代表性的 LDP 基准方法相比，CPR 能更好地保留周期结构，并始终实现更低的重构误差，尤其是在低 ε 机制下。

## Abstract
Periodic patterns are fundamental cues in multimedia signals and systems, including repetitive motion in video (e.g., gait cycles), rhythmic and pitch-related structure in audio, and recurring textures in image sequences. When such user-generated streams are collected from edge devices, local differential privacy (LDP) is appealing because it perturbs data before upload; however, the injected noise can corrupt spectral peaks and induce phase drift, making period estimation unreliable and degrading reconstruction quality. We propose \textbf{CPR} (\textit{Cycle and Phase Recovery}), a period-aware reconstruction framework for periodic time series under LDP. CPR performs multi-scale period probing and multi-consensus selection to suppress noise-induced spectral interference, then aggregates perturbed samples at matched within-cycle phase positions to stabilize phase alignment across cycles. To recover the underlying per-phase values, CPR combines EM-based denoising with kernel density estimation, improving robustness under tight privacy budgets. Experiments on two real-world periodic datasets demonstrate that CPR better preserves periodic structure and consistently achieves lower reconstruction error than representative LDP baselines, especially in the low-$ε$ regime.

---

## 论文详细总结（自动生成）

这篇论文针对本地差分隐私（LDP）环境下周期性时间序列的重构问题提出了创新方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多媒体和传感器数据（如步态分析、音频节奏、生理监测）中，周期性模式是关键特征。然而，本地差分隐私（LDP）在数据上传前注入的噪声会严重破坏频谱峰值并导致“相位漂移”（Phase Drift），使得传统的重构方法无法准确识别周期，导致重构信号失真。
*   **研究背景**：现有的LDP流数据发布方案（如LBD）主要关注预算分配，缺乏对复杂周期结构（如变长周期、多周期叠加）的鲁棒恢复能力。

### 2. 提出的方法论：CPR 框架
论文提出了 **CPR (Cycle and Phase Recovery)** 框架，主要包含两个核心阶段：
*   **周期恢复（Cycle Recovery）**：
    *   **多尺度频谱探测**：不依赖单一窗口的FFT，而是在多个窗口尺度下探测候选周期，以应对信号的局部非平稳性。
    *   **时域验证**：利用余弦相似度验证FFT提取的候选周期，剔除由噪声引起的虚假峰值或谐波干扰。
    *   **多共识投票**：通过跨尺度的中位数聚合和投票机制，确定最稳定的主导周期 $\hat{T}$。
*   **相位恢复（Phase Recovery）**：
    *   **相位分组与边界稳定**：根据估计的周期将噪声样本按相位对齐分组，并使用镜像填充减少边界效应。
    *   **隐私感知去噪（EM-then-KDE）**：针对Square Wave (SW) 机制产生的噪声分布，先利用期望最大化（EM）算法在离散网格上进行初步去噪，再通过核密度估计（KDE）提取众数（Mode）作为鲁棒的相位值估计。

### 3. 实验设计
*   **数据集**：使用了四个真实世界数据集：Darwin（手写压力轨迹）、Music（土耳其音乐频率包络）、Raisin（葡萄牙葡萄干特征数据）、Crowdsourced（众包图像映射数据）。这些数据被处理成具有准周期特性的流数据。
*   **对比方法（Benchmarks）**：
    1.  **Laplace**：标准的拉普拉斯机制。
    2.  **SW (Square Wave)**：直接使用SW扰动后的数据。
    3.  **SW moving / SW filter**：在SW基础上增加移动平均或滤波平滑。
    4.  **LBD**：一种先进的LDP流数据发布机制。
*   **评估指标**：周期检测准确率、重构序列与原始序列的余弦距离。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Core i7-13650HX 处理器**和 **16 GB RAM** 的工作站上运行（Windows 11 系统）。
*   **算力说明**：文中未提及使用 GPU，考虑到该方法主要涉及信号处理算法（FFT、EM、KDE），其计算开销主要集中在 CPU 端。论文未明确给出具体的训练或推理时长，但提到设备端仅需单次轻量级扰动，计算压力主要在服务器端。

### 5. 实验数量与充分性
*   **实验规模**：针对每个参数配置（隐私预算 $\epsilon$ 从 0.5 到 5.0，窗口大小 $w$ 从 5 到 25）进行了 **100 次独立重复实验**并取平均值。
*   **充分性评价**：实验覆盖了从严苛（$\epsilon < 1$）到宽松的多种隐私预算场景，并对比了多种平滑和流处理基准方法。使用了四个不同领域的真实数据集，证明了方法的通用性。实验设计较为客观，通过控制变量法清晰展示了 $\epsilon$ 和 $w$ 对准确率的影响。

### 6. 主要结论与发现
*   **重构精度领先**：CPR 在所有数据集和隐私预算下均实现了最低的余弦距离（即最高相似度），显著优于传统的平滑方法和 LBD。
*   **低预算下的鲁棒性**：在极低隐私预算（$\epsilon$ 较小）时，其他方法几乎无法识别周期，而 CPR 凭借多尺度共识机制仍能保持较高的周期检测准确率。
*   **参数敏感性**：周期检测准确率受单位事件预算 $\epsilon/w$ 的严格控制。窗口 $w$ 越大，分配给每个点的预算越少，检测难度随之剧增。

### 7. 优点与亮点
*   **周期感知设计**：不同于盲目平滑，CPR 显式地利用了信号的周期对称性，通过跨周期聚合有效提升了信噪比。
*   **统计鲁棒性**：结合 EM 解码和 KDE 众数提取，比简单的均值聚合更能抵抗 LDP 噪声产生的偏斜和异常值。
*   **后处理特性**：CPR 完全在服务器端执行，不增加用户的计算负担，也不消耗额外的隐私预算。

### 8. 不足与局限
*   **主导周期假设**：该方法假设信号存在一个明显的主导周期。对于具有多个重叠周期或周期随时间剧烈波动的极度非平稳信号，其表现可能会下降。
*   **计算复杂度**：服务器端需要执行多尺度 FFT 和 EM 迭代，对于超大规模并发流数据的实时处理可能存在计算压力。
*   **实验覆盖**：虽然使用了四个数据集，但主要集中在数值特征上，对于更复杂的多媒体原始数据（如原始视频帧序列）的直接重构效果尚待验证。

（完）
