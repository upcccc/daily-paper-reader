Title: Distributed Learning with Adversarial Gradient Perturbations

URL Source: https://arxiv.org/pdf/2605.03313v1

Published Time: Wed, 06 May 2026 00:33:35 GMT

Number of Pages: 23

Markdown Content:
# Distributed Learning with Adversarial Gradient Perturbations 

Nawapon Sangsiri CUHK 

sangsiri@cse.cuhk.edu.hk 

Yufei Tao CUHK 

taoyf@cse.cuhk.edu.hk 

May 6, 2026 

Abstract 

Privacy concerns in distributed learning often lead clients to return intentionally altered gradient information. We consider the problem of learning convex and L-smooth functions under adversarial gradient perturbation , where a client’s gradient reply to a server query can deviate arbitrarily from the true gradient subject to a distance bound. Our study focuses on two fundamental questions: (i) what is the smallest achievable sub-optimality gap (i.e., excess error in optimization) under such responses, and (ii) how many queries are sufficient to guarantee a given sub-optimality gap? We establish tight feasibility thresholds on the sub-optimality gap and provide algorithms that achieve these thresholds with provable query complexity guarantees. 

> arXiv:2605.03313v1 [cs.LG] 5 May 2026

# 1 Introduction 

Modern learning systems increasingly rely on edge-based architectures, where local devices compute gradient information and communicate it to a central server. In such environments, local gradient reports may be deliberately perturbed to protect sensitive data. This motivates the study of learning algorithms that operate under adversarially corrupted gradients , where each individual gradient reply may deviate arbitrarily from the true gradient, subject only to a known bound on the perturbation magnitude. Understanding what can and cannot be learned in this setting, as well as the query complexity required to do so, is a fundamental question. Formally, we consider a setting where there are n geographically distributed clients, each holding a convex and L-smooth loss function ℓi : Rd → R where i ∈ [n]. 1 Define 

f (w) = 1

n

> n

X

> i=1

ℓi(w). (1) which is minimized at some w∗ ∈ Rd — when multiple minimizers exist, we define w∗ to be one with the smallest ∥w∗∥.2 The sub-optimality gap of a vector w ∈ Rd is defined as the difference 

f (w) − f (w∗). A server’s goal is to find a vector whose sub-optimality gap is bounded by τ > 0. Communication between the server and clients is limited to the following query primitive: 

Approximate Gradient Query: The server selects an i ∈ [n] and sends the 

i-th client a vector w ∈ Rd. The client replies with a vector v satisfying 

∥v − ∇ ℓi(w)∥ ≤ ϵ (2) where ϵ is a value agreed upon in advance by the server and all clients. Crucially, the client may respond with an arbitrary vector v satisfying (2), including one chosen adversarially so as to impede the server’s optimization procedure. Larger values of ϵ expand the set of permissible replies and thus offer stronger privacy protection on the local data contributing to the computation of ∇ℓi(w). We refer to the above problem as distributed learning with adversarial gradient perturba-tions (DLAGP). Our goal is to characterize the fundamental feasibility of learning and derive interesting query bounds in this setting. In particular, we aim to answer two questions: 

• Q1 : How small can the sub-optimality gap be made? Formally, what is the smallest value 

τmin such that, for all legal inputs {ℓ1, ℓ 2, ..., ℓ n}, it is possible for the server to find a vector w satisfying f (w) ≤ f (w∗) + τmin , possibly using infinitely many queries? 

• Q2 : Given a value τ that is not “unreasonably small”, how many approximate gradient queries are needed for the server to guarantee a sub-optimality gap at most τ ?

Our Results and Implications. We establish non-trivial answers to both questions above. 

• Answers for Q1 — If no upper bound on ∥w∗∥ is known in advance, then no algorithm, even with infinitely many queries, can guarantee finding a vector w with a bounded sub-optimality gap, regardless of the target value τ .Motivated by this impossibility result, we consider R-instances of the DLAGP problem, where a bound ∥w∗∥ ≤ R is known. For this class of instances, we show that the smallest achievable sub-optimality gap τmin lies in the interval [ 12 ϵR, 5ϵR ]. In other words, when         

> 1[x] represents the set {1,2, ..., x }for an integer x≥1.
> 2All norms in this paper refer to the L2norm.

1τ < ϵR/ 2, no algorithm can guarantee a sub-optimality gap of τ over all R-instances, even with infinitely many queries. Conversely, the server can always achieve a sub-optimality gap at most τ for all R-instances whenever τ ≥ 5ϵR .

• Answers for Q2 — For any R-instance satisfying the condition τ ≥ 5ϵR , the server can deterministically ensure a sub-optimality gap at most τ using 

O(n · min {LR 2/τ, LR/ϵ }) (3) queries. For any R-instance satisfying the condition τ ≥ 5.01 ·ϵR (the constant 5.01 can be replaced with any value larger than 5), the server can, with probability at least 1 − δ, ensure a sub-optimality gap at most τ using ˜O

 LR 4(B0 + LR )2

τ 3



(4) queries, where 

B0 = n

max  

> i=1

∥∇ ℓi(0)∥. (5) and ˜O(.) hides factors logarithmic to L, R, 1/τ , and 1 /δ ; note that (4) does not depend on n or d. All queries in this algorithm select clients uniformly at random. By standard sampling arguments, when n is sufficiently large, with high probability each client is queried at most once, and most clients are never queried. 

# 2 Related Work 

In the study of oracle complexity in convex optimization — an area comprehensively surveyed by [5] — the goal is to minimize a convex function f : Rd → R by making the fewest calls to an “oracle”. Given a w ∈ Rd, the oracle returns an exact or approximate derivative of f of a specific order at w. For example, an exact zeroth-order oracle returns f (w), while a first-order counterpart returns ∇f (w). Our study contributes to the research on first-order oracles, of which see representative works [1, 4, 6, 8, 9, 10, 11, 13, 12, 15, 18, 17, 19, 20, 22, 24, 25, 26, 27] and the references therein. In the traditional studies of “inexact gradient methods”, an approximate first-order oracle returns a stochastically perturbed version of ∇f (w), typically a random oracle (w) ∈ Rd whose expectation is ∇f (w). In some cases, there may be a “concentration property” to limit how often oracle (w) can deviate from ∇f (w) significantly. The seminal work of [10] pioneered the concept of non-stochastic inexact first-order oracles with bounded errors in smooth convex optimization. Our permutation model — formalized as approximate gradient queries (see (2)) — aligns with their concept. Indeed, those queries promise neither expectation nor concentration guarantees, and their answers can be adversarial subject to bounded deviation. This model has gained attention in recent years [7, 14, 28, 29] — we will discuss the findings of these papers in Section 3.4. A parallel line of work studies Byzantine-resilient distributed optimization; see, e.g., [2, 3, 16, 30] and the references therein. These works typically assume that most clients return exact or stochastically perturbed gradients, while a fraction of “Byzantine” clients may behave arbitrar-ily. Algorithms in this literature crucially depend on the existence of sufficiently many honest clients and exploit redundancy through robust aggregation schemes. In contrast, our model does not posit any well-behaved clients: every gradient query may be adversarially perturbed, 2and no stochastic population-level structure is available. As a result, classical Byzantine-robust techniques do not apply in our scenarios. Loosely speaking, our work is also related to federated learning , which aims to train models across geographically distributed clients while allowing them to keep data localized; see [21] for an excellent introduction. A central focus in that area, however, is handling heterogeneous data distributions and ensuring convergence despite differences across client datasets. In con-trast, our study abstracts away data heterogeneity and instead examines fundamental limits on learnability, as well as query complexity, under worst-case, bounded adversarial gradient perturbations. 

# 3 Foundation: The Theory with One Client 

This section considers DLAGP with only n = 1 client. As we will see Section 4, the theory developed in this section plays a vital role in solving the problem in its general form. When n = 1, DLAGP can be rephrased in a more succinct manner. Let f : Rd → R be a function such that: 

• C1: f is convex and L-smooth; 

• C2: f is minimized at some w∗ ∈ Rd; if multiple minimizers exist, define w∗ to be one with the smallest norm. The goal of optimization is to find a vector w ∈ Rd whose sub-optimality gap f (w) − f (w∗) is bounded by a given τ . The only way for a learner algorithm to glean information about f is to interact with an adversarial gradient perturbation (AGP) oracle. Given a w ∈ Rd, the oracle returns a vector oracle (w) ∈ Rd satisfying 

∥oracle (w) − ∇ f (w)∥ ≤ ϵ (6) where ϵ ≥ 0; we will call it an ϵ-AGP oracle . Our goal is to understand how well and fast 

optimization can be done when oracle (w) can be an arbitrary vector obeying (6). 

3.1 No Norm Bounds, No Call Bounds 

We start with a result showing that if nothing is known about ∥w∗∥, it is impossible to guarantee a sub-optimality gap τ with a finite number of oracle calls. 

Theorem 1. Fix any L ≥ 1, any ϵ ≥ 0, and any τ > 0. If no upper bound on ∥w∗∥ is known in advance, no algorithm can guarantee — for every function f : R → R satisfying conditions 

C1 , C2 , and ∥∇ f (w)∥ ≤ 3τ for all w ∈ R — finding a w ∈ R with f (w) ≤ f (w∗) + τ by calling an ϵ-AGP oracle a finite number of times. Proof. It suffices to prove the theorem for ϵ = 0 (if ϵ > 0, simply treat a 0-AGP oracle as an 

ϵ-oracle and apply the argument below). Let us define a function from R to R,

f (w) = 



0 if w < −R

(L/ 2)( w + R)2 if −R ≤ w ≤ − R + 3 τ /L 

3τ w + 3 τ R − (3 τ )2/(2 L) if w > −R + 3 τ /L 

where R > 0 is a real value to be decided later. As shown in Appendix A, the function is convex, L-smooth, and ascending. Furthermore, it has the following properties: 

• Property 1: min w f (w) = 0, and f (w) = 0 if and only if w ≤ − R.3• Property 2: ∥∇ f (w)∥ ≤ 3τ for all w ∈ R, and ∇f (w) = 3 τ for all w > −R + 3 τ /L .

• Property 3: f (w) ≥ 3τ for any w ≥ − R + (3 τ /L ) + 1. Let A be an algorithm that calls a 0-AGP oracle a finite number of calls. We will show that an adversary can force A to incur a sub-optimality gap larger than τ . For this purpose, the adversary initially leaves open the value of R and decides this value only after A has terminated. Specifically, for any value (i.e., a 1D vector) w that A inquires the oracle with, the adversary always returns 3 τ . Let wi be the value that A provides in its i-th call to the oracle, for i ∈ [t]where t is the (finite) number of calls. Let wout be the value output by A. Define 

v = min {w1, w 2, ..., w t, w out }.

The adversary then sets 

R = max {0, (3 τ /L ) − v + 1 }

which ensures v ≥ − R + (3 τ /L ) + 1. The reader can verify that the function f — which is now finalized — has ∇f (w) = 3 τ

at every w ∈ { w1, w 2, ..., w t}. In other words, the oracle’s answers to A have been correct. Nevertheless, f (wout ) ≥ f (v) ≥ 3τ . Hence, A has failed to ensure f (wout ) ≤ f (w∗) + τ = τ .The above argument works only when the adversary does not promise any upper bound on 

∥w∗∥ in advance. Indeed, ∥w∗∥ equals R, which can be arbitrarily big because it depends on the value of v.

3.2 Sub-Optimality Gap 

Define 

F(R) = {f | f satisfies C1 and C2 with ∥w∗∥ ≤ R}. (7) Motivated by Theorem 1, we will study functions from F(R) with a known value of R. Even in this context, learning is still impossible if the target sub-optimality gap is too small: 

Theorem 2. Fix any R ≥ 1, any L ≥ 1, and any 0 < ϵ ≤ 1. No algorithm working with an 

ϵ-AGP oracle can guarantee — for every f ∈ F (R) (recall that the definition of F(R) relies on L due to C2 ) — returning a w ∈ Rd such that f (w) < f (w∗) + ϵR/ 2. This holds true even if the algorithm calls the oracle an infinite number of times. Proof. Let us define two functions from R to R:

f1(w) =



0 if w < −R

(L/ 2)( w + R)2 if −R ≤ w ≤ − R + ϵ/L ϵw + ϵR − ϵ2/(2 L) if w > −R + ϵ/L 

(8) 

f2(w) = f1(−w). (9) As shown in Appendix A: 

• f1 and f2 are convex and L-smooth; 

• f1 is ascending and f2 is descending; 

• ∥∇ f1(w)∥ ≤ ϵ and ∥∇ f2(w)∥ ≤ ϵ for all w ∈ R.Furthermore, the facts below are easy to verify: 4• For f1, the optimal vector with the smallest norm is w∗ 

> 1

= −R with f1(w∗

> 1

) = 0, while for 

f2, the optimal vector with the smallest norm is w∗ 

> 2

= R with f2(w∗

> 2

) = 0. Hence, both 

f1 and f2 belong to F(R). 

• For w ≥ 0, f1(w) ≥ f (0) = ϵR − ϵ2/(2 L) ≥ ϵR/ 2. 

• For w ≤ 0, f2(w) ≥ f (0) = ϵR − ϵ2/(2 L) ≥ ϵR/ 2. Let A be an algorithm relying on an AGP oracle to minimize f . We observe that A cannot always distinguish between f = f1 and f = f2. Indeed, regardless of f ∈ { f1, f 2}, when 

A solicits ∇f (w) from the oracle, the oracle can always return the value 0, which obeys (6) because ∥∇ f (w)∥ ≤ ϵ for both f = f1 and f = f2.Consider an adversary that is committed to choosing f ∈ { f1, f 2} but does so only after A

has terminated. Whenever A calls an oracle to inquire ∇f (w), the adversary instructs the oracle to return 0, which is correct no matter f = f1 or f2. Suppose that A finishes by outputting a vector wout . The adversary sets f = f2 if wout ≤ 0, or f = f1 otherwise. This makes sure 

f (wout ) ≥ ϵR/ 2, forcing the sub-optimality gap to be at least ϵR/ 2. 

3.3 An Optimization Algorithm 

Next, we consider f ∈ F (R) and values of τ that are reasonably large. Consider the algorithm 

AGP-opt below. 

algorithm AGP-opt (K)1. w0 ← 0, k ← 02. while k < K do 

3. gk ← oracle (wk) /* output of the ϵ-AGP oracle */ 4. if ∥gk∥ < 4ϵ then return wk

5. wk+1 ← wk − 12L gk

6. k ← k + 1 7. return wk

Theorem 3. Let f be a function from F(R). For any real value τ ≥ 5ϵ∥w∗∥, algorithm 

AGP-opt finds a w ∈ Rd satisfying f (w) ≤ f (w∗) + τ with parameter 

K = min {5LR 2/(4 τ ), LR/ (4 ϵ)}. (10) 

Furthermore, every wk in Line 3 of AGP-opt has norm at most (17 /8) R.

Several remarks are in order before delving into the proof: 

• Gradient descent (GD) can be regarded as a special instance of AGP-opt with ϵ = 0. The early termination condition at Line 4 is needed to guarantee a sub-optimality gap sensitive to ∥w∗∥. The sub-optimality gap would become O(ϵR ) without early termination as analyzed in Appendix E. 

• Theorem 3 extends the convergence bound of GD in a seamless manner: the first term of (10) captures the influence of optimization accuracy, while the second reflects the noise imposed by the ϵ-AGP oracle. For ϵ = 0, the second term disappears, leaving K =5LR 2/(4 τ ), which asymptotically matches the convergence bound of GD [11, Corollary 3.5]. 

• Setting w0 = 0 at Line 1 is needed for the claim (in Theorem 3) that ∥wk∥ ≤ (17 /8) R for all k. The claim will be useful in Section 4.2. Our analysis can be easily adapted to other choices of w0 by shifting the origin of the coordinate system to w0.5Proof of Theorem 3. Define 

K′ = the value of k when AGP-opt terminates. (11) Note that K′ may be less than K — this happens when the termination is at Line 4. The following technical lemma is proved in Appendix B. 

Lemma 4. Let f be a function satisfying C1 and C2 . Algorithm AGP-opt outputs a vector 

w ∈ Rd such that f (w) − f (w∗) is bounded by 

ϵ∥w∗∥ + max 

n

4ϵ∥w∗∥, L · ∥ w∗∥2

K

o

. (12) 

Furthermore, For any k ∈ [0 , K ′ − 1] , it holds that 

∥wk+1 − w∗∥ ≤ ∥ wk − w∗∥ + ϵ/ (2 L). (13) As τ ≥ 5ϵ∥w∗∥ (a condition in Theorem 3), the value of (12) is at most τ when 

K ≥ L∥w∗∥2

τ − ϵ∥w∗∥ . (14) The inequality τ ≥ 5ϵ∥w∗∥ implies τ − ϵ∥w∗∥ ≥ (4 /5) τ . Hence, 

L∥w∗∥2

τ − ϵ∥w∗∥ ≤ 5L∥w∗∥2

4τ . (15) We can bound from above the right hand side of (15) in two different ways: 

• As τ ≥ 5ϵ∥w∗∥ gives ∥w∗∥/τ ≤ 1/(5 ϵ), we have (15) ≤ L∥w∗∥

4ϵ ≤ LR 

4ϵ

where the second inequality used ∥w∗∥ ≤ R (because f ∈ F (R)) 

• On the other hand, from ∥w∗∥ ≤ R, we directly have (15) ≤ 5LR 2

4τ .

It now follows from (14) and Lemma 4 that the choice of K in (10) allows AGP-opt to output a w ∈ Rd whose sub-optimalty gap is at most τ .For each k ∈ [0 , K ′], Lemma 4 tells us 

∥wk − w∗∥ ≤ ∥w0 − w∗∥ + kϵ/ (2 L)= ∥w∗∥ + kϵ/ (2 L). (16) For the value of K in (10), it holds that k ≤ K′ ≤ K ≤ LR/ (4 ϵ). Thus, kϵ/ (2 L) ≤ R/ 8, from which (16) yields ∥wk − w∗∥ ≤ ∥ w∗∥ + R/ 8, leading to 

∥wk∥ ≤ 2∥w∗∥ + R/ 8 ≤ (17 /8) R. 

This completes the proof of Theorem 3. 63.4 Discussion 

Optimization under an ϵ-AGP oracle has been studied in [7, 14, 28, 29]. We will focus on [14, 28] because the other two papers assume function classes different from ours. As detailed in Appendix C, the results of [14] could be used to show that a slightly modified version of GD finds a vector whose sub-optimality gap is 

O



∥w∗∥ · p(LU/K ) + Bϵ 



(17) where U is an upper bound on f (w) for every vector w produced at any iteration of GD, and B

is an upper bound on ∥∇ f (w)∥ over those vectors (see Appendix C for the precise definitions of 

U and B). On the other hand, by Lemma 4, we can find a vector whose sub-optimality gap is 

O



∥w∗∥ · max {ϵ, L ∥w∗∥/K }



. (18) Our bound (18) converges faster than (17) (specifically: 1/K vs. 1 /√K). As K → ∞ , our bound converges to O(∥w∗∥ · ϵ), while (17) to O(∥w∗∥ · √Bϵ ). Crucially, in the context of [14], a reasonable ϵ must be less than B — otherwise, the fact ∥∇ f (w)∥ ≤ B allows an ϵ-AGP oracle to 

always return vector 0, thereby giving no information to the algorithm at all. In fact, effective optimization demands ϵ ≪ B, in which case our sub-optimality gap O(∥w∗∥ · ϵ) is considerably smaller than that of [14]. [28] presented a mirror-descent algorithm that works when w comes from a domain with a finite radius R. In that scenario, their algorithm ensures a sub-optimality gap of 

O

 poly( R, L )

√K + Rϵ 



. (19) The bound converges slower than (18) ( K vs. 1 /√K). As K → ∞ , (19) converges to O(Rϵ )— note that the bound remains so even if ∥w∗∥ ≪ R; in that scenario our bound (18), which converges to O(∥w∗∥ϵ), is significantly smaller. 

# 4 DLAGP 

We will deploy our theory in Section 3 to tackle the DLAGP problem in its general form (with an arbitrary number n of clients). Every mention of “query” henceforth refers to an approximate gradient query. 

4.1 Answers for Q1 

Recall that w∗ is an optimal vector of f (i.e., w∗ minimizes the function f defined in (1)) with the smallest norm. If no upper bound is known about ∥w∗∥, no DLAGP algorithm can guarantee a sub-optimality gap at most τ using a finite number of queries. Indeed, any such algorithm must work in the special case of n = 1, which, however, gives a contradiction against Theorem 1. This is true even if the values L, ϵ , and τ are all known to the algorithm. Next, we consider ∥w∗∥ ≤ R. Recall (from our statement of Q1 in Section 1) that τmin is the minimum sub-optimality gap that we must tolerate. Theorem 2 implies 

τmin ≥ ϵR/ 2. (20) To see why, assume the existence of an DLAGP algorithm that can always ensure a sub-optimality gap at most τ for some τ < ϵR/ 2. Such an algorithm must work in the special case of n = 1, which, however, gives a contradiction against Theorem 2. This is true even if the values ϵ, τ , R, and L are all known to the algorithm. 7Next, we argue that τmin ≤ 5ϵR , i.e., DLAGP is solvable on any R-instance as long as 

τ ≥ 5ϵR . As ℓi is L-smooth for each i ∈ [n], so is the function f in (1). The server only needs to execute the algorithm AGP-opt in Section 3.3. To execute Line 3 of AGP-opt , which demands 

oracle (wk), the server issues a query to every client i ∈ [n], solicits the client’s reply vi, and then defines 

oracle (wk) = 1

n

> n

X

> i=1

vi.

Accordingly, 

∥oracle (wk) − ∇ f (wk)∥ = 1

n

> n

X

> i=1

vi −

> n

X

> i=1

∇ℓi(wk)

≤ 1

n

> n

X

> i=1

∥vi − ∇ ℓi(wk)∥

which is at most ϵ because ∥vi − ∇ ℓi(wk)∥ ≤ ϵ for each i ∈ [n]; see (2). Theorem 3 thus permits the server to ensure a sub-optimality gap at most τ by issuing K · n queries, where K is given in (10). This yields the bound claimed in (3). 

4.2 Answer for Q2 

Next, we improve the query bound from the previous subsection when a small failure probability 

δ is permitted. 

Center Estimation. We first take de-tour to discuss a relevant problem. Denote by P =

{p1, p 2, ..., p n} a set of n vectors in Rd such that ∥pi∥ ≤ B for every i ∈ [n]. Let ( s1, s 2, ..., s m)be a sequence uniformly sampled from [ n]m. Equivalently, each sj (j ∈ [m]) is independently drawn from [ n] uniformly at random. Variations of the following result have appeared before (e.g., Lemma 18 of [18]); a proof of our version can be found in Appendix D. 

Lemma 5. For any t ≥ 0, by setting m = O(( B/t )) 2 log(1 /δ )) , we can guarantee 

Pr 

h 1

n

> n

X

> i=1

pi − 1

m

> m

X

> j=1

psj > t 

i

≤ δ. 

A Randomized Algorithm for DLAGP. Let us return to the DLAGP problem, assuming 

τ ≥ 5.01 · ϵR. (21) Suppose that Line 3 of AGP-opt asks for oracle (wk). Instead of querying all clients (as done in Section 4.1), we draw m numbers s1, s 2, ..., s m independently from [ n] uniformly at random — effectively sampling m clients with replacement. For each j ∈ [m], the server issues a query to solicit a vector vsj from client sj , which comes with the guarantee ∥vsj − ∇ ℓsj (wk)∥ ≤ ϵ. Then, it computes 

oracle (wk) = 1

m

> m

X

> j=1

vsj . (22) To decide m, we need: 

Lemma 6. Suppose that we run AGP-opt by setting K as in (10) . For any i ∈ [n] and 

k ∈ [0 , K ], it holds that ∥∇ ℓi(wk)∥ ≤ B0 + (17 /8) LR (see (5) for B0). 

8The proof can be found in Appendix D. From now, we fix B = B0 +(17 /8) LR . By Lemma 5, for m = O(( Bt )2 log 1 

> δ′

) — where t and δ′ will be decided later — it holds that 

Pr 

h 1

n

> n

X

> i=1

∇ℓi(wk) − 1

m

> m

X

> j=1

∇ℓsj (wk) > t 

i

≤ δ′. (23) Note that 1

> n

Pni=1 ∇ℓi(wk) is exactly ∇f (wk). On the other hand, ∥oracle (wk)− 1

> m

Pmj=1 ∇ℓsj (wk)∥,which by (22) is 1 

> m

∥Pmj=1 vsj − Pmj=1 ∇ℓsj (wk)∥, is no more than ϵ because ∥vsj − ∇ ℓsj (wk)∥ ≤ ϵ

for all j ∈ [m]. Plugging these into (23) gives: 

Pr 

h

∇f (wk) − oracle (wk) > t + ϵ

i

≤ δ′. (24) In other words, (22) serves the purpose of a ( t + ϵ)-AGP. To apply Theorem 3 and Lemma 6, we require 

τ ≥ 5( t + ϵ)R

as ∥w∗∥ ≤ R. We therefore set 

t = τ / (5 R) − ϵ = Ω( τ /R ) (25) where the last step used (21). It remains to fix the value of δ′. Henceforth, set K = 5 LR 2/(4 τ ); by Theorem 3, AGP-opt 

performs at most K iterations. By setting δ′ to δ/K , we make sure that the inequality ∥∇ f (wk)−

oracle (wk)∥ ≤ t + ϵ holds for all relevant k with probability at least 1 − δ. We thus determine the value of m to be: 

m = O

 B0 + LR τ /R 

2

log Kδ

!

= ˜O

 (B0 + LR )2R2

τ 2



(26) where ˜O(.) hides factors logarithmic to L, R, 1/τ , and 1 /δ .We can now conclude that in total m · K queries suffice for the server to guarantee a sub-optimality gap at most τ with probability at least 1 − δ. Plugging in the values of K and m

yields the bound claimed in (4). 

# 5 Experiments 

We used three real-world datasets from the LIBSVM library 3: ijcnn1, covtype, and HIGGS; see Appendix F for details on the normalization and cleansing steps taken. Each dataset consists of n pairs ( xi, y i) ( i ∈ [n]), where xi ∈ Rd is a feature vector and yi ∈ { 0, 1} is a binary label. The values of ( n, d ) for ijcnn1, covtype, and HIGGS are (141691, 22 + 1), (581012, 54 + 1), and (11m, 24 + 1), respectively; see Appendix F for the meaning of “+1”. Each pair defines a local loss function ℓi(w) where w ∈ Rd. The goal of optimization is to minimize the function 

f (w) defined in (1). We employed two loss functions for each i ∈ [n]: (i) robust regression 

(RR), ℓi(w) = ( σ(⟨w, x i⟩) − yi)2, where σ : R → (0 , 1) is defined as σ(z) = 1 /(1 + e−z ); and (ii) 

binary cross-entropy (BCE), ℓi(w) = −yi log( σ(⟨w, x i⟩)) − (1 − yi) log(1 − σ(⟨w, x i⟩)). Each ℓi is a convex and Li-smooth function, where the value of Li was calculated based on ( xi, y i). The value L (in our problem definition) was determined as max ni=1 Li.

Optimization under AGP Oracles. The first set of experiments studies the convergence behavior of the AGP-opt algorithm in Section 3.3. We examined three ϵ-AGP oracles: 

> 3Https://www.csie.ntu.edu.tw/ ∼cjlin/libsvm

9(a) opp-ijcnn1-rr (b) opp-covtype-rr (c) opp-HIGGS-rr (d) opp-ijcnn1-bce (e) opp-covtype-bce (f) opp-HIGGS-bce           

> (g) amp-ijcnn1-rr (h) amp-covtype-rr (i) amp-HIGGS-rr (j) amp-ijcnn1-bce (k) amp-covtype-bce (l) amp-HIGGS-bce
> (m) fixed-ijcnn1-rr (n) fixed-covtype-rr (o) fixed-HIGGS-rr (p) fixed-ijcnn1-bce (q) fixed-covtype-bce (r) fixed-HIGGS-bce

Figure 1: Behavior of optimization under an ϵ-AGP oracle 

• Opposing: the oracle perturbs ∇f (w) by distance ϵ toward the direction opposite to that of ∇f (w); 

• Amplifying: the oracle perturbs ∇f (w) by distance ϵ in the same direction as ∇f (w); 

• Fixed-Direction: the oracle perturbs ∇f (w) by distance ϵ toward a fixed direction, chosen to be the negative direction of the first dimension of Rd.Figure 1 plots the f (wk) value (labeled as “loss” in the diagrams) as a function of k (the current iteration number) for four ϵ values of different magnitudes: ϵ = 0 represents conventional gradient descent, and the other three values form a geometric progression. The caption of each diagram indicates the oracle type, dataset, and local loss functions; e.g., opp-ijcnn1-rr means that oracle is opposing, the dataset is ijcnn1, and all ℓ1, ℓ 2, ..., ℓ n adopt the RR function. We disabled the “early termination” condition at Line 4 of AGP-opt . In theory, the condition is needed because when ∥f (w)∥ drops to near ϵ, the perturbation of an ϵ-AGP oracle becomes significant and, in the worst case, can be made to harm optimization (as analyzed in Section E, the provable sub-optimality gap is higher without such a condition). In practice, however, such worst-case scenarios rarely happen, thus allowing further optimization even beyond the moment of early termination. The behavior of an opposing oracle in Figures 1(a)-(f) contrasts that of an amplifying oracle in Figures 1(g)-(l). In essence, an opposing oracle suppresses the “step length” of optimization, while an amplifying oracle strengthens it. Under an opposing oracle, higher ϵ values incurred larger sub-optimality gaps, but the opposite was observed under an amplifying oracle. This seemingly surprising phenomenon is in fact consistent with the understanding that a step length chosen by theoretical analysis — 12L oracle (wk) for AGP-opt — is often conservatively small, such that an amplifying oracle “happened to” perform optimization at a better rate. Figures 1(m)-(r) show that, under a fixed-direction oracle, the red curve — representing a relatively large ϵ value — exhibited a V-shape. To explain why, first note that at the early stages of optimization, the real gradient ∇f (wk) has a large norm such that perturbation has little effect. As the algorithm approaches the optimal vector w∗, the magnitude of ∥∇ f (wk)∥

diminishes, i.e., only small updates are intended. In this case, perturbation dictates the update direction, pulling the algorithm past w∗ and thus causing the loss to go up. Eventually, the optimization converges toward the w where ∇f (w) = ( ϵ, 0, 0, ..., 0) T. In fact, for small ϵ values such V-shapes also exist but they are difficult to observe. 

Query Budget Allocation in Distributed Learning. The second set of experiments inves-tigates distributed learning under a specific query budget Q, i.e., the number of queries that the 10 (a) ijcnn1-rr ( ϵ = 0 .003) (b) covtype-rr ( ϵ = 0 .0025) (c) HIGGS-rr ( ϵ = 0 .0004)      

> (d) ijcnn1-bce ( ϵ= 0 .01) (e) covtype-bce ( ϵ= 0 .005) (f) HIGGS-bce ( ϵ= 0 .002)

Figure 2: Effects of query allocation in distributed learning server can afford to issue. We consider that the server allocates the budget as follows. First, it chooses a parameter K ≤ Q, which is the total number of iterations of AGP-opt (without early termination) to perform. Then, in each iteration, it issues m = Q/K queries to compute 

oracle (wk) as in (22). There is a tradeoff: K needs to be sufficiently large for AGP-opt to do enough iterations to approach w∗, but increasing K reduces m, which elevates the uncertainty in center estimation (see Lemma 5). Our purpose is to provide a guideline for choosing K

properly. Figure 2 plots, as a function of K, the final outcome of optimization — namely, the value 

f (wK ) obtained by AGP-opt after K iterations; we will refer to f (wK ) as the final loss . The caption under a diagram indicates the dataset, local loss functions, and ϵ value deployed. In these experiments, when queried with w, a client i ∈ [n] perturbs ∇ℓi(w) using one of the three strategies described earlier — opposing, amplifying, or fixed-direction perturbation — with equal probability. Each diagram includes 4 curves: the orange, green, and red curves are obtained with geometrically-increasing values of Q, as well as a blue curve, marked as “ Q = nK 

with ϵ = 0”, corresponding to the “unrealistic” method where the server simply queries every client in each iteration and each client performs no perturbation at all. The blue curve serves as a reference for the best achievable final loss at each K. To account for the randomness in the strategy described in Section 4.2, we use shaded regions to report the range of final loss values across multiple runs. Evidently, K must not be too small: optimization through gradient-based adjustments does 

require many steps. In Figures 2(a) and 2(d), the best K turned out to be Q, i.e., only one query was issued per iteration. However, this is not representative: in Figures 2(b), 2(c), 2(e), and 2(f), the shaded regions start to grow considerably after Q/K has dropped below a certain threshold. On the other hand, when Q/K ≥ 100, shaded regions are hardly noticeable in all experiments. Furthermore, at the same K, as long as Q/K ≥ 100, increasing Q provided little help in lowering the final loss value. The above observations suggest that m = 100 queries per iteration are adequate for reliable center estimation, because of which K = Q/ 100 is a robust choice in reality. 

# 6 Conclusions 

We have presented a systematic study of distributed optimization under adversarial gradient perturbations, a setting in which each client’s gradient reply to a server query may arbitrarily deviate from the true gradient, subject only to a known distortion bound. Our work addresses two fundamental questions: (i) what level of optimization accuracy can be guaranteed in the presence of such adversarial replies, and (ii) how many gradient queries are sufficient to reach a given accuracy level. We establish sharp feasibility thresholds on achievable optimization accu-11 racy, showing a strict regime in which no algorithm can guarantee meaningful progress, as well as a complementary regime in which reliable optimization is always possible. For the attain-able regime, we develop algorithms that provably reach the target accuracy using a bounded number of queries. Future directions include proving tight bounds on the query complexity and extending this framework to richer function classes. 

# References 

[1] Alekh Agarwal, Peter L. Bartlett, Pradeep Ravikumar, and Martin J. Wainwright. Information-theoretic lower bounds on the oracle complexity of stochastic convex opti-mization. IEEE Transactions on Information Theory , 58(5):3235–3249, 2012. [2] Dan Alistarh, Zeyuan Allen-Zhu, and Jerry Li. Byzantine stochastic gradient descent. In 

Proceedings of Neural Information Processing Systems (NIPS) , pages 4618–4628, 2018. [3] Peva Blanchard, El Mahdi El Mhamdi, Rachid Guerraoui, and Julien Stainer. Machine learning with adversaries: Byzantine tolerant gradient descent. In Proceedings of Neural Information Processing Systems (NIPS) , pages 119–129, 2017. [4] Digvijay Boob, Qi Deng, and Guanghui Lan. Stochastic first-order methods for convex and nonconvex functional constrained optimization. Mathematical Programming , 197(1):215– 279, 2023. [5] Sebastien Bubeck. Convex optimization: Algorithms and complexity. Foundations and Trends in Machine Learning , 8(3-4):231–357, 2015. [6] Chengliang Chai, Jiayi Wang, Nan Tang, Ye Yuan, Jiabin Liu, Yuhao Deng, and Guoren Wang. Efficient coreset selection with cluster-based methods. In Proceedings of ACM Knowledge Discovery and Data Mining (SIGKDD) , pages 167–178, 2023. [7] Fu-Chieh Chang, Farhang Nabiei, Pei-Yuan Wu, Alexandru Cioba, Sattar Vakili, and Al-berto Bernacchia. Gradient descent: Robustness to adversarial corruption. In Proceedings of International OPT Workshop on Optimization for Machine Learning , 2022. [8] Alexandre d’Aspremont. Smooth optimization with approximate gradient. SIAM Journal on Optimization , 19(3):1171–1183, 2008. [9] Ofer Dekel, Ran Gilad-Bachrach, Ohad Shamir, and Lin Xiao. Optimal distributed online prediction using mini-batches. Journal of Machine Learning Research (JMLR) , 13:165–202, 2012. [10] Olivier Devolder, Francois Glineur, and Yurii E. Nesterov. First-order methods of smooth convex optimization with inexact oracle. Mathematical Programming , 146(1-2):37–75, 2014. [11] Guillaume Garrigos and Robert M. Gower. Handbook of convergence theorems for (stochas-tic) gradient methods. ArXiv , abs/2301.11235, 2024. [12] Nadav Hallak and Kfir Yehuda Levy. A study of first-order methods with a determinis-tic relative-error gradient oracle. In Proceedings of International Conference on Machine Learning (ICML) , 2024. [13] Jiawei Huang, Ruomin Huang, Wenjie Liu, Nikolaos M. Freris, and Hu Ding. A novel sequential coreset method for gradient descent algorithms. In Proceedings of International Conference on Machine Learning (ICML) , pages 4412–4422, 2021. 12 [14] Zachary Izzo, James Zou, and Lexing Ying. How to learn when data gradually reacts to your model. In Proceedings of International Conference on Artificial Intelligence and Statistics (AISTATS) , pages 3998–4035, 2022. [15] Martin Jaggi. Revisiting frank-wolfe: Projection-free sparse convex optimization. In Pro-ceedings of International Conference on Machine Learning (ICML) , pages 427–435, 2013. [16] Sai Praneeth Karimireddy, Lie He, and Martin Jaggi. Learning from history for byzantine robust optimization. In Proceedings of International Conference on Machine Learning (ICML) , pages 5311–5319, 2021. [17] Phillip A. Kerger, Marco Molinaro, Hongyi Jiang, and Amitabh Basu. A universal transfer theorem for convex optimization algorithms using inexact first-order oracles. In Proceedings of International Conference on Machine Learning (ICML) , 2024. [18] Jonas Moritz Kohler and Aurelien Lucchi. Sub-sampled cubic regularization for non-convex optimization. In Proceedings of International Conference on Machine Learning (ICML) ,pages 1895–1904, 2017. [19] Guanghui Lan. First-order and Stochastic Optimization Methods for Machine Learning .Springer, 2020. [20] Yunwen Lei and Ke Tang. Learning rates for stochastic gradient descent with nonconvex objectives. IEEE Transactions on Pattern Analysis and Machine Intelligence (PAMI) ,43(12):4505–4511, 2021. [21] Tian Li, Anit Kumar Sahu, Ameet Talwalkar, and Virginia Smith. Federated learning: Challenges, methods, and future directions. IEEE Signal Processing Magazine , 37(3):50– 60, 2020. [22] Baharan Mirzasoleiman, Jeff A. Bilmes, and Jure Leskovec. Coresets for data-efficient train-ing of machine learning models. In Proceedings of International Conference on Machine Learning (ICML) , pages 6950–6960, 2020. [23] Mehryar Mohri, Afshin Rostamizadeh, and Ameet Talwalkar. Foundations of machine learning . MIT Press, 2018. [24] Arkadi Nemirovski, Anatoli B. Juditsky, Guanghui Lan, and Alexander Shapiro. Robust stochastic approximation approach to stochastic programming. SIAM Journal on Opti-mization , 19(4):1574–1609, 2009. [25] Arkadi Nemirovsky and David Berkovich Yudin. Problem Complexity and Method Effi-ciency in Optimization . John Wiley & Sons, 1983. [26] Yurii E. Nesterov. Efficiency of coordinate descent methods on huge-scale optimization problems. SIAM Journal on Optimization , 22(2):341–362, 2012. [27] Alexandre B. Tsybakov. Optimal rates of aggregation. In Bernhard Scholkopf and Man-fred K. Warmuth, editors, Proceedings of Conference on Learning Theory (COLT) , volume 2777, pages 303–313, 2003. [28] Shuche Wang and Vincent Y. F. Tan. Robust distributed gradient descent to corruption over noisy channels. In IEEE International Symposium on Information Theory , pages 2520–2525, 2024. [29] Shuche Wang and Vincent Y. F. Tan. A mirror descent-based algorithm for corruption-tolerant distributed gradient descent. IEEE Transactions on Signal Processing , 73:827–842, 2025. 13 [30] Dong Yin, Yudong Chen, Kannan Ramchandran, and Peter L. Bartlett. Byzantine-robust distributed learning: Towards optimal statistical rates. In Proceedings of International Conference on Machine Learning (ICML) , pages 5636–5645, 2018. 

# Appendix A Completing the Proofs of Theorems 1 and 2 

This section discusses the function f constructed in the proof of Theorem 1 and the functions 

f1, f 2 in the proof of Theorem 2. We will focus on f1 and f2 because f is a special case of f1

with ϵ set to 3 τ .

Convexity and Smoothness. We will first show that f1 and f2, given in (8) and (9), are convex and L-smooth. In fact, since f2 is the reflection of f1 across the y-axis and this transfor-mation preserves convexity and smoothness, it suffices to prove the convexity and smoothness of only f1.The derivative of f1 is 

f ′

> 1

(x) = 



0 if x < −RL(x + R) if −R ≤ x ≤ − R + ϵ/L ϵ if x > −R + ϵ/L 

(27) Clearly, f ′

> 1

(x) is non-decreasing, implying that f1 is convex. For ∀x, y ∈ R, next we will prove 

|f ′

> 1

(x) − f ′

> 1

(y)| ≤ L · | x − y| (28) which asserts that f1 is L-smooth. Assume, w.l.o.g., that x ≤ y. The subsequent discussion proceeds in four cases. 

Case 1: y < −R or −R ≤ x ≤ y ≤ − R + ϵ/L or x > −R + ϵ/L . In this case, f1(x) and 

f1(y) are in the same “if-branch” of (8). The function in each if-branch of (8) is L-smooth. Hence, (28) holds by definition of L-smoothness. 

Case 2: x < −R and −R ≤ y ≤ − R + ϵ/L .

|f ′(x) − f ′(y)| = |f ′(−R) − f ′(y)|

(corollary of Case 1) ≤ L · | (−R) − y|≤ L · | x − y|.

Case 3: −R ≤ x ≤ − R + ϵ/L and y > −R + ϵ/L .

|f ′(x) − f ′(y)| = |f ′(x) − f ′(−R + ϵ/L )|

(corollary of Case 1) ≤ L · | x − (−R + ϵ/L )|≤ L · | x − y|.

Case 4: x < −R and y > −R + ϵ/L .

|f ′(x) − f ′(y)| = |f ′(−R) − f ′(−R + ϵ/L )|

(corollary of Case 1) ≤ L · | − R − (−R + ϵ/L )|≤ L · | x − y|.

This completes the proof of (28). 14 Monotonicity. Function f1 (resp., f2) is ascending (resp., descending) because f ′

> 1

(x) ≥ 0(resp., f ′

> 2

(x) ≤ 0) for all x ∈ R.

Gradient Norms. ∥∇ f1(x)∥ equals |f ′

> 1

(x)| where f ′

> 1

(x) is given in (27). It is easy to verify that |f ′

> 1

(x)| ≤ ϵ for all x ∈ R. For any x ∈ R, it holds that ∥∇ f2(x)∥ = ∥∇ f1(−x)∥, which, as just proved, is at most ϵ.

# B Proof of Lemma 4 

B.1 Proof of (12) 

Recall that K′ (see (11) is the value of k when AGP-opt terminates. That is, the algorithm outputs wK′ ; and our goal is to prove that f (wK′ ) − f (w∗) is bounded by (12). Also, define 

K′′ =

 0 if f (w1) − f (w∗) < ϵ ∥w∗∥

the largest k ∈ [1 , K ′] s.t. f (wk) − f (w∗) ≥ ϵ∥w∗∥ otherwise (29) We will prove the next two lemmas later. 

Lemma 7. f (wk+1 ) < f (wk) for every k ∈ [0 , K ′ − 1] .

Lemma 8. If K′′ > 0, then 

∥wK′′ − w∗∥ ≤ ∥ wK′′ −1 − w∗∥ ≤ ... ≤ ∥ w0 − w∗∥ = ∥w∗∥.

If K′′ < K ′, then f (wK′′ +1 ) − f (w∗) < ϵ ∥w∗∥. In this case, Lemma 7 tells us f (wK′ ) ≤

f (wK′′ +1 ) < f (w∗) + ϵ∥w∗∥, satisfying the requirement in (12). The subsequent discussion will therefore focus on the case where K′′ = K′.We will distinguish two cases based on how AGP-opt terminates. 

• Termination at Line 4. By the convexity of f , we have: 

f (wK′ ) + ⟨∇ f (wK′ ), w ∗ − wK′ ⟩ ≤ f (w∗)where ⟨., . ⟩ represents dot product. We can then derive 

f (wK′ ) − f (w∗) ≤ − ⟨∇ f (wK′ ), w ∗ − wK′ ⟩ ,

≤ ∥∇ f (wK′ )∥ · ∥ w∗ − wK′ ∥

(by Lemma 8) ≤ ∥∇ f (wK′ )∥ · ∥ w∗∥.

To analyze ∥∇ f (wK′ )∥, recall that ∥∇ f (wK′ ) − oracle (wK′ )∥ is at most ϵ. Further recall that gK′ = oracle (wK′ ) and that ∥gK′ ∥ < 4ϵ (because Algorithm 1 terminated at Line 4). Hence: 

∥∇ f (wK′ )∥ ≤ ∥ gK′ ∥ + ϵ < 5ϵ. 

We thus conclude f (wK′ ) − f (w∗) ≤ 5ϵ∥w∗∥, which is bounded by (12). 

• Termination at Line 7. For each k ∈ [0 , K ′], define 

Ek = L · ∥ wk − w∗∥2 + k(f (wk) − f (w∗) − ϵ∥w∗∥).

We will prove later: 15 Lemma 9. Ek+1 ≤ Ek for all k ∈ [0 , K ′ − 1] .

This yields 

EK′ ≤ EK′−1 ≤ ... ≤ E0 = L · ∥ w0 − w∗∥2 = L · ∥ w∗∥2.

Plugging the definition of EK′ into the above and applying ∥wk − w∗∥ ≥ 0 gives: 

K′(f (wK′ ) − f (w∗) − ϵ∥w∗∥) ≤ L · ∥ w∗∥2

⇒ f (wK′ ) − f (w∗) ≤ L∥w∗∥2

K′ + ϵ∥w∗∥

= L∥w∗∥2

K + ϵ∥w∗∥

where the last step used K = K′ (as AGP-opt finishes at Line 7). This establishes the claim in Lemma 4. It remains to prove Lemmas 7-9, In the rest of the section, we introduce γ = 1 /(2 L). Accordingly, Line 5 of AGP-opt can be written as wk+1 ← wk − γg k.

Proof of Lemma 7. As f is L-smooth, [11, Lemma 2.25] gives 

f (wk+1 ) − f (wk)

≤ ⟨∇ f (wk), w k+1 − wk⟩ + L

2 ∥wk+1 − wk∥2

= ⟨∇ f (wk), −γg k⟩ + L

2 ∥− γg k∥2

= −γ ⟨∇ f (wk), g k⟩ + γ2L

2 ∥gk∥2

= −γ ⟨gk + ( ∇f (wk) − gk), g k⟩ + ( γ/ 4) ∥gk∥2

= −γ  ∥gk∥2 + ⟨∇ f (wk) − gk, g k⟩ + ( γ/ 4) ∥gk∥2

≤ −γ  ∥gk∥2 − ϵ∥gk∥ + ( γ/ 4) ∥gk∥2 (here we applied ∥∇ f (wk) − gk∥ ≤ ϵ)= (−3γ/ 4) ∥gk∥2 + γϵ ∥gk∥

= γ∥gk∥(ϵ − 3∥gk∥/4) . (30) By the definition of K′, we must have ∥gk∥ ≥ 4ϵ and, thus, ϵ − 3∥gk∥/4 ≤ ϵ − 3ϵ < 0. Hence, (30) is negative, meaning that f (wk+1 ) < f (wk). 

Proof of Lemma 8. Let us first prove a technical lemma: 

Lemma 10. For every k ∈ [0 , K ′ − 1] , it holds that 

∥wk+1 − w∗∥2 − ∥ wk − w∗∥2 ≤ f (w∗) − f (wk+1 ) + ϵ∥wk − w∗∥

L .

Proof. We derive 

L · ∥ wk+1 − w∗∥2 − L · ∥ wk − w∗∥2

= L · ∥ wk+1 − w∗∥2 − L · ∥ wk − wk+1 + wk+1 − w∗∥2

= L · ∥ wk+1 − w∗∥2 − L · ∥ wk − wk+1 ∥2 − 2L · ⟨ wk − wk+1 , w k+1 − w∗⟩ − L · ∥ wk+1 − w∗∥2

= −L · ∥ γg k∥2 − 2L · ⟨ γg k, w k+1 − w∗⟩

= −(γ/ 2) · ∥ gk∥2 − ⟨∇ f (wk), w k+1 − w∗⟩ − ⟨ gk − ∇ f (wk), w k+1 − w∗⟩≤ −(γ/ 2) · ∥ gk∥2 − ⟨∇ f (wk), w k+1 − w∗⟩ + ϵ∥wk+1 − w∗∥ (here we applied ∥∇ f (wk) − gk∥ ≤ ϵ)16 ≤ −(γ/ 2) · ∥ gk∥2 − ⟨∇ f (wk), w k+1 − w∗⟩ + ϵ · (∥wk+1 − wk∥ + ∥wk − w∗∥)= −(γ/ 2) · ∥ gk∥2 − ⟨∇ f (wk), w k+1 − w∗⟩ + ϵγ ∥gk∥ + ϵ∥wk − w∗∥. (31) By the convexity of f , we know 

⟨∇ f (wk), w ∗ − wk⟩ ≤ f (w∗) − f (wk). (32) As f is L-smooth, [11, Lemma 2.25] gives 

− ⟨∇ f (wk), w k+1 − wk⟩ ≤ L

2 ∥wk+1 − wk∥2 + f (wk) − f (wk+1 ). (33) Adding both sides of (32) and (33) yields: 

− ⟨∇ f (wk), w k+1 − w∗⟩ ≤ L

2 ∥wk+1 − wk∥2 + f (w∗) − f (wk+1 ).

By substituting the above into (31), we have: (31) ≤ −(γ/ 2) · ∥ gk∥2 + L

2 ∥wk+1 − wk∥2 + f (w∗) − f (wk+1 ) + ϵγ ∥gk∥ + ϵ∥wk − w∗∥

= f (w∗) − f (wk+1 ) + ϵ∥wk − w∗∥ − (γ/ 2) · ∥ gk∥2 + Lγ 2

2 ∥gk∥2 + ϵγ ∥gk∥

= f (w∗) − f (wk+1 ) + ϵ∥wk − w∗∥ − γ

4 ∥gk∥(∥gk∥ − 4ϵ)

≤ f (w∗) − f (wk+1 ) + ϵ∥wk − w∗∥

where the last step used the fact that ∥gk∥ ≥ 4ϵ. The lemma now follows. We are now ready to prove Lemma 8 with an inductive argument. First, applying Lemma 10 with k = 0 gives 

∥w1 − w∗∥2 − ∥ w0 − w∗∥2 ≤ (1 /L ) · (f (w∗) − f (w1) + ϵ∥w0 − w∗∥)= (1 /L ) · (f (w∗) − f (w1) + ϵ∥w∗∥)

≤ 0where the last step used the definition of K′′ in (29) and the fact that K′′ > 0. Hence, 

∥w1 − w∗∥ ≤ ∥ w0 − w∗∥ = ∥w∗∥.Inductively, for a k ≤ K′′ −1, assuming ∥wk −w∗∥ ≤ ∥ w∗∥, next we will prove ∥wk+1 −w∗∥ ≤ ∥wk − w∗∥ (which implies ∥wk+1 − w∗∥ ≤ ∥ w∗∥). For this purpose, apply Lemma 10 to obtain 

∥wk+1 − w∗∥2 − ∥ wk − w∗∥2 ≤ (1 /L ) · (f (w∗) − f (wk+1 ) + ϵ∥wk − w∗∥)

≤ (1 /L ) · (f (w∗) − f (wk+1 ) + ϵ∥w∗∥)(by inductive assumption) 

≤ 0where the last step used the definition of K′′ and the fact that k + 1 ≤ K′′ .

Proof of Lemma 9. 

Ek+1 − Ek

= L ·  ∥wk+1 − w∗∥2 − ∥ wk − w∗∥2 + ( k + 1) f (wk+1 ) − kf (wk) − f (w∗) − ϵ∥w∗∥

= L ·  ∥wk+1 − w∗∥2 − ∥ wk − w∗∥2 + f (wk+1 ) − f (w∗) + k(f (wk+1 ) − f (wk)) − ϵ∥w∗∥≤ f (w∗) − f (wk+1 ) + ϵ∥w∗∥ + f (wk+1 ) − f (w∗) + k(f (wk+1 ) − f (wk)) − ϵ∥w∗∥

(by Lemma 10) = k(f (wk+1 ) − f (wk)) 

≤ 0. (by Lemma 7) 17 B.2 Proof of (13) 

Once again, we will use γ = 1 /(2 L) as a convenient notation. Let us first prove a technical lemma: 

Lemma 11. For any w ∈ Rd, it holds that 

∥w − γ∇f (w) − w∗∥ ≤ ∥ w − w∗∥.

Proof. Applying Lemma 10 with ϵ = 0 gives: 

∥wk+1 − w∗∥2 − ∥ wk − w∗∥2 ≤ (f (w∗) − f (wk+1 )) /L 

≤ 0.

When ϵ = 0, wk+1 = wk − γ∇f (wk). Hence, the above yields 

∥wk − γ∇f (wk) − w∗∥ ≤ ∥ wk − w∗∥. (34) A close look at the proof of Lemma 10 reveals that, when ϵ = 0, the proof holds for any 

wk ∈ Rd and wk+1 = wk − γ∇f (wk). As a result, (34) holds for any wk ∈ Rd. Setting wk to the vector w in the statement of Lemma 11 proves the claim. Recall that AGP-opt calculates wk+1 as wk − γg k, where gk = oracle (wk). We use this fact to derive: 

∥wk+1 − w∗∥ = ∥wk − γg t − w∗∥

= ∥wk − γ(∇f (wk) − ∇ f (wk) + gt) − w∗∥

= ∥wk − γ∇f (wk) − w∗ − γ(gt − ∇ f (wk)) ∥≤ ∥wk − γ∇f (wk) − w∗∥ + ∥γ(gt − ∇ f (wk)) ∥≤ ∥wk − w∗∥ + γ∥gt − ∇ f (wk)∥ (by Lemma 11) 

≤ ∥wk − w∗∥ + ϵ/ (2 L)where the last step used ∥oracle (wk) − ∇ f (wk)∥ ≤ ϵ.

# C A Review on [Izzo et al. , 2022] 

[14] studied the behavior of gradient descent (GD) when it runs under an ϵ-AGP, as described in the pseudocode below: 

algorithm GD (K)1. w0 ← 0, k ← 02. while k < K do 

3. gk ← oracle (wk) /* output of the ϵ-AGP oracle */ 4. wk+1 ← wk − 12L gk

5. k ← k + 1 6. return wk

For any K ≥ 1, [14, Lemma 5] proved 

> K

min  

> k=1

∥∇ f (wk)∥ = O

r L · UK + Bϵ 



(35) where 

U = K

max  

> k=1

f (wk)18 B = K

max  

> k=1

∥∇ f (wk)∥

Henceforth, we will use w+ to represent the vector among w1, ..., w K that achieves the minimization on the left hand side of (35); thus, ∇f (∥w+∥) is bounded by the right hand side of (35). The identification of w+ requires calculating the norms of the precise gradients ∇f (w1), 

∇f (w2), ..., ∇f (wK ). Such calculation is impossible under our perturbation model (where the only source of information about f is the ϵ-AGP oracle). Nevertheless, we proceed anyway by assuming that the vector w+ can be found “by magic”. [14] did not analyze the sub-optimality gap of w+. Nevertheless, we can do an analysis for them by resorting to our theory. For this purpose, let us augment their algorithm by requiring it to return the current wk as soon as ∥gk∥ falls below 4 ϵ (as in Line 4 of AGP-opt ). If the algorithm terminates this way, then our Lemma 4 applies; otherwise, the sub-optimality gap of 

w+ can be bounded as follows. First, the convexity of f indicates 

f (w+) − f (w∗) ≤ ∇f (w+), w + − w∗

≤ ∥∇ f (w+)∥ · ∥ w+ − w∗∥. (36) Second, Lemma 8 tells us ∥w+ − w∗∥ ≤ ∥ w∗∥. Combining this with (35) and (36) yields 

f (w+) − f (w∗) ≤ ∥w∗∥ · O

r L · UK + Bϵ 



as is the bound used in (17). 

# D Proofs for Section 4.2 

Proof of Lemma 5. We will need: 

Lemma 12 (McDiarmid’s Inequality [23]) . Let f : X1 × X 2 × ... × X m → R be a function satisfying the following condition for each i ∈ [m]:

sup 

> x1,...,x m,y

|f (x1, ..., x i, ..., x m) − f (x1, ..., y, ..., x m)| ≤ ci

where c1, ..., c m are real values. If X1, ..., X m are independent variables in X1, ..., Xm, respec-tively, then for every t > 0 we have 

Pr [|f (X1, ..., X m) − E[f (X1, ..., X m)] | ≥ t] ≤ 2 exp 



− 2t2

Pmi=1 c2

> i



.

We are now ready to prove Lemma 5. It will be convenient to move the coordinate system such that 1

> n

Pni=1 pi = 0. Because every point of P has norm at most B originally, in the new coordinate system every point has norm at most 2 B. The subsequent discussion assumes the new coordinate system. Given any ( x1, x 2, ..., x m) ∈ [n]m, define 

f (x1, ..., x m) = 1

n

> n

X

> i=1

pi − 1

m

> m

X

> j=1

pxj = 1

m

> m

X

> j=1

pxj .

For any j′ ∈ [m], we have sup 

> y∈[n]

|f (x1, ..., x m) − f (x1, ..., x j′−1, y, x j′+1 , ..., x m)|

19 = sup 

> y∈[n]

1

m

> m

X

> j=1

pxj − 1

m

 mX

> j=1

pxj



− pxj′ + py

≤ 1

m sup 

> y∈[n]

∥pxj′ − py∥≤ 4B/m 

where the last step used the fact that ∥pi∥ ≤ 2B (i ∈ [n]) in the new coordinate system. For each j ∈ [m], independently draw value sj from [ n] uniformly at random. By Lemma 12, we have: 

Pr [|f (s1, ..., s m) − E[f (s1, ..., s m)] | ≥ t/ 2] ≤ 2 exp 



− 2( t/ 2) 2

Pmi=1 (4 B/m )2



= 2 exp 



− t2m

32 B2



. (37) By Jensen’s inequality 

E[f (s1, ..., s m)] ≤ 1

m

vuutE

h mX

> j=1

psj

> 2

i

. (38) Clearly 

> m

X

> j=1

psj

> 2

=

> m

X

> j=1

∥psj ∥2 + 2 X

> 1≤j1<j 2≤m

D

psj1 , p sj2

E

.

For any 1 ≤ j1 < j 2 ≤ m, the random vectors psj1 and psj2 are independent and their ex-pectations are 0 — recall that 1

> n

Pni=1 pi = 0 in the new coordinate system. It follows that 

E[

D

psj1 , p sj2

E

] = 0. Thus: (38) = 1

m

vuutE

h mX

> j=1

∥psj ∥2

i

≤ 2B

√m (39) where the last inequality holds because ∥pi∥ ≤ 2B for all i ∈ [n]. By setting m = O(( B/t )2 log(1 /δ )), we can ensure at the same time that (37) is at most 

δ, and (39) is at most t/ 2. We can now conclude that Pr [f (s1, ..., s m) > t ] ≤ δ, as claimed in Lemma 5. 

Proof of Lemma 6. Theorem 3 ensures ∥wk∥ ≤ (17 /8) R. As ℓi is L-smooth, we have 

∥∇ ℓi(wk) − ∇ ℓi(0)∥ ≤ L · ∥ wk∥ ≤ (17 /8) LR . The claim now follows from ∇ℓi(0) ≤ B0.

# E Analysis of Simple Gradient Descent 

This section re-examines the gradient descent (GD) algorithm whose pseudocode is presented in Section C. We will extend our analysis on AGP-OPT to show that, given the same K in Theorem 3, GD can guarantee a sub-optimality gap of O(ϵR ) — note that this is not sensitive to ∥w∗∥.Define K′ to be the smallest value of k ∈ [0 , K − 1] satisfying gk < 4ϵ; if no such k exists, set 

K′ = K. Note that this is in fact the same K′ as defined in (11), but rephrased in the context here. If K′ = K (i.e., ∥gk∥ ≥ 4ϵ for all k ∈ [0 , K − 1]), GD behaves the same way as AGP-opt 

and, hence, ensures a sub-optimality gap at most 5 ϵ∥w∗∥.20 The rest of the section will focus on K′ < K . As wK′ is vector returned by AGP-opt ,Theorem 3 tells us 

f (wK′ ) − f (w∗) ≤ 5ϵ∥w∗∥. (40) Define 

S≥ = {k ∈ [K′, K − 1] : ∥gk∥ ≥ 4ϵ}

S< = {k ∈ [K′, K − 1] : ∥gk∥ < 4ϵ}.

Thus 

f (wK ) − f (wK′ ) =

> K−1

X

> k=K′

f (wk+1 ) − f (wk)= X

> k∈S≥

f (wk+1 ) − f (wk) + X

> k∈S<

f (wk+1 ) − f (wk). (41) The proof of Lemma 7 essentially shows that, for any k ∈ [0 , K − 1] satisfying ∥gk∥ ≥ 4ϵ, we have f (wk+1 ) ≤ f (wk), implying 

X

> k∈S≥

(f (wk+1 ) − f (wk)) ≤ 0. (42) For any k ∈ S<, by the convexity of f , we have 

f (wk+1 ) − f (wk) ≤ ⟨∇ f (wk), w k+1 − wk⟩

= − 12L ⟨∇ f (wk), g k⟩≤ 12L ∥∇ f (wk)∥ · ∥ gk∥.

The quantity ∥∇ f (wk)∥ is at most ∥∇ f (wk) − gk∥ + ∥gk∥ ≤ ϵ + 4 ϵ = 5 ϵ. Thus, the above yields 

f (wk+1 ) − f (wk) ≤ 12L · 5ϵ · 4ϵ = 10 ϵ2

L . (43) Plugging (42) and (43) into (41) gives 

f (wK ) − f (wK′ ) ≤ X

> k∈S<

f (wk+1 ) − f (wk)

≤ |S<| · 10 ϵ2

L ≤ K · 10 ϵ2

L

≤ LR 

4ϵ · 10 ϵ2

L = 5ϵR 

2 .

Combining the above with (40) yields 

f (wK ) − f (w∗) ≤ 5ϵ∥w∗∥ + 5ϵR 

2 = O(ϵR ).

# F Data Preprocessing for Experiments 

This section explains the steps taken to preprocess the three datasets used in our experiments after they were downloaded from the LIBSVM website. 21 The ijcnn1 collection from LIBSVM includes 49,990 training pairs and 91,701 testing pairs. We combined these into a single dataset with a total cardinality of 141,691. Each pair ( x, y )has a 22-dimensional vector x and a label y ∈ {− 1, 1}. We mapped the labels to {0, 1} by modifying every y = −1 to y = 0. Furthermore, we appended a constant coordinate of 1 to each x, resulting in a final dimensionality of 23. This is a standard procedure to incorporate a bias term into the dot product ⟨w, x ⟩ within the loss function. The covtype dataset (specifically, the covtype.binary.scaled version) includes 581,012 pairs. Each pair ( x, y ) has a 54-dimensional vector x and a label y ∈ { 1, 2}. We used all the pairs, but mapped the labels to {0, 1} by subtracting 1 from every y. We also appended a constant coordinate of 1 to each x, resulting in 55-dimensional vectors. The HIGGS dataset includes 11,000,000 pairs. Each pair ( x, y ) has a 28-dimensional vector 

x and a label y ∈ { 0, 1}. We utilized the entire dataset but excluded dimensions “9”, “13”, “17”, “21” because around half of the pairs have missing values on those dimensions. Finally, we also appended a constant coordinate of 1 to each x, bringing the total dimensionality to 25. 22