---
title: "SI-ChainFL: Shapley-Incentivized Secure Federated Learning for High-Speed Rail Data Sharing"
title_zh: SI-ChainFL：面向高铁数据共享的 Shapley 激励安全联邦学习
authors: "Mingjie Zhao, Cheng Dai, Fei Chen, Xin Chen, Kaoru Ota, Mianxiong Dong, Bing Guo"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07992v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 用于数据共享的安全联邦学习
tldr: 针对高铁数据共享中联邦学习面临的激励不足和中心化单点故障问题，本文提出SI-ChainFL框架。该框架引入多维度沙普利值（Shapley Value）量化客户端贡献，并利用稀疏正样本驱动的聚类策略加速评估。结合区块链共识协议实现去中心化聚合，将聚合权与激励挂钩，在保障安全性的同时显著提升了模型在复杂环境下的预测性能。
selection_source: fresh_fetch
motivation: 现有联邦学习方案在高铁场景中存在激励缺失导致的搭便车、模型投毒攻击以及中心化聚合带来的单点故障风险。
method: 提出一种基于改进沙普利值的贡献评估机制，并设计了将聚合权限与激励水平挂钩的区块链去中心化共识协议。
result: "在包含90%恶意客户端的极端攻击环境下，SI-ChainFL的准确率比现有RAGA算法高出14.12%。"
conclusion: 该研究通过贡献感知激励与去中心化聚合的深度融合，为高速铁路系统提供了安全、高效且具备理论性能保障的数据共享方案。
---

## 摘要
在高速铁路（HSR）系统中，联邦学习（FL）能够在不共享原始数据的情况下实现跨部门的流量预测。然而，现有方案存在两个关键局限性：（1）激励机制不足，导致搭便车行为和模型投毒；（2）中心化聚合，引入了单点故障。我们提出了一种安全高效的框架 SI-ChainFL，通过将贡献感知的激励机制与去中心化聚合相结合来解决这些问题。首先，我们使用 Shapley 值指标量化客户端贡献，该指标综合考虑了稀有事件效用、数据多样性、数据质量和及时性。为了降低计算开销，我们进一步开发了一种稀有正样本驱动的客户端聚类策略，以加速 Shapley 值的估算。此外，我们为去中心化聚合设计了一种基于区块链的共识协议，其中聚合资格与 Shapley 激励挂钩。这一设计激励客户端提交高质量的更新，并实现了高效且安全的全局聚合。在 MNIST、CIFAR 10、CIFAR 100 以及 HSR 流量数据集上的实验表明，SI-ChainFL 在 PA 攻击下即使存在 90% 的恶意客户端仍能保持有效，其准确率比 RAGA 高出 14.12%。理论分析进一步保证了性能的上界。

## Abstract
In high-speed rail (HSR) systems, federated learning (FL) enables cross-departmental flow prediction without sharing raw data. However, existing schemes suffer from two key limitations: (1) insufficient incentives, leading to free-riding and model poisoning; and (2) centralized aggregation, which introduces a single point of failure. We propose a secure and efficient framework SI-ChainFL that addresses these issues by combining contribution-aware incentives with decentralized aggregation. First, we quantify client contributions using a Shapley value metric that jointly considers rare-event utility, data diversity, data quality, and timeliness. To reduce computational overhead, we further develop a rare positive driven client clustering strategy to accelerate Shapley estimation. Moreover, we design a blockchain-based consensus protocol for decentralized aggregation, where aggregation eligibility is tied to Shapley incentives. This design motivates clients to submit high-quality updates and enables efficient and secure global aggregation. Experiments on MNIST, CIFAR 10 and CIFAR 100, and a HSR flow dataset show that SI ChainFL remains effective under 90% malicious clients in PA attacks, achieving 14.12% higher accuracy than RAGA. Theoretical analysis further guarantees an upper bound on performance

---

## 论文详细总结（自动生成）

### 论文总结：SI-ChainFL —— 面向高铁数据共享的 Shapley 激励安全联邦学习

#### 1. 核心问题与整体含义
*   **研究背景**：中国高铁（HSR）网络规模巨大，跨部门的客流预测对于调度和预警至关重要。然而，数据隐私保护（如 GDPR）限制了原始数据的直接共享。
*   **核心痛点**：
    1.  **激励缺失**：现有联邦学习（FL）缺乏公平的贡献量化，导致客户端倾向于“搭便车”（Free-riding）或发起“投毒攻击”（Poisoning）。
    2.  **单点故障**：传统的中心化聚合服务器容易受到攻击或产生性能瓶颈。
    3.  **稀有事件价值低估**：高铁场景中的突发大客流（稀有正样本）比常规数据更有价值，但现有机制往往只按样本量分配权重。
*   **研究动机**：设计一个既能公平量化多维度贡献，又能实现去中心化安全聚合的联邦学习框架。

#### 2. 方法论
*   **核心思想**：将多目标 Shapley 值（Shapley Value）评估与区块链共识机制深度耦合，通过贡献度决定激励分配与聚合权限。
*   **关键技术细节**：
    *   **多目标贡献量化**：定义了一个复杂的效用函数 $v(S)$，综合考虑：
        1.  **稀有事件效用**：使用 AUPRC 和 MCC 指标评估模型对突发客流的预测能力。
        2.  **数据多样性**：基于特征表示的余弦相似度矩阵评估数据的独特价值。
        3.  **数据质量**：量化数据缺失率、离群值及标签可靠性。
        4.  **时间衰减**：引入指数衰减因子，确保近期的贡献具有更高权重。
    *   **加速计算策略（正样本驱动聚类）**：针对 Shapley 值指数级计算复杂度问题，提出将贡献相似的客户端聚类，并仅针对关键的正样本进行评估，将复杂度降至近线性。
    *   **区块链安全聚合**：设计了一种基于共识的协议。验证节点根据 Shapley 分数对客户端更新进行投票，只有达标的客户端才能进入聚合集，且聚合权重与 Shapley 分数挂钩。

#### 3. 实验设计
*   **数据集**：
    *   **基准数据集**：MNIST、CIFAR-10、CIFAR-100（用于验证分类任务的通用性）。
    *   **真实场景数据集**：PeMS07 交通流数据、成都铁路局提供的真实高铁客流数据（HSR）。
*   **对比方法（Benchmarks）**：
    *   经典 FL：FedAvg, FedProx。
    *   边缘/图优化 FL：FedEdge, FedSage。
    *   鲁棒/安全 FL：FLTrust, RAGA, P2FMS。
*   **攻击模型**：模拟了 10% 到 90% 不同比例的**搭便车攻击（FR）**和**投毒攻击（PA，标签翻转）**。

#### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **NVIDIA RTX 5090 (32GB)** GPU 和 **25 vCPU Intel Xeon Platinum 8470Q** 的服务器上运行。
*   **软件环境**：PyTorch 2.8.0。
*   **训练细节**：设置了 100 轮通信，每轮随机选择 70% 的客户端参与，本地迭代次数为 5 次。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括：
    *   不同数据集下的收敛性分析。
    *   客户端数量（5 到 20 个）的扩展性实验。
    *   隐私预算（$\epsilon$）对准确率的影响。
    *   **极端鲁棒性测试**：在恶意节点占比高达 90% 的情况下进行对比。
    *   **消融实验**：验证了 Shapley 激励（SI）和区块链聚合（ChainFL）各自的贡献。
*   **充分性评价**：实验设计非常充分，不仅涵盖了合成噪声环境，还使用了真实的工业级高铁数据，对比基准全面，能够客观反映系统在极端对抗环境下的性能。

#### 6. 主要结论与发现
*   **极强的鲁棒性**：在 90% 恶意客户端的投毒攻击下，SI-ChainFL 的准确率比 RAGA 高出 **14.12%**，而传统方法（如 FedAvg）在恶意节点超过 50% 时基本失效。
*   **高效的计算性能**：通过聚类优化，Shapley 值的计算时间在 HSR 数据集上缩短至传统采样方法的 **1/8**。
*   **激励相容性**：通过将聚合权与贡献挂钩，有效地抑制了搭便车行为，实验证明该机制能显著提升高质量数据的参与度。

#### 7. 优点
*   **多维度评估**：突破了单一的“样本量”评估维度，特别强调了高铁场景中“稀有事件”的价值。
*   **去中心化安全**：利用区块链消除了单点故障，并将共识过程与贡献度验证结合，设计巧妙。
*   **理论支撑**：论文提供了关于收敛性、差分隐私（DP）保护以及恶意权重上界的严谨数学证明。

#### 8. 不足与局限
*   **验证集依赖**：Shapley 值的计算高度依赖于验证节点的验证集质量，若验证集本身存在偏差，可能导致贡献评估不公。
*   **通信开销**：虽然计算复杂度降低了，但区块链共识过程和频繁的 Shapley 评估在超大规模网络（如数万个传感器）中的通信延迟尚未深入讨论。
*   **动态性挑战**：虽然引入了时间衰减，但对于客户端频繁加入/退出的极端动态环境，系统的稳定性仍有待进一步验证。

（完）
