---
title: Noise-aware Client Selection for carbon-efficient Federated Learning via Gradient Norm Thresholding
title_zh: 通过梯度范数阈值化实现碳高效联邦学习的噪声感知客户端选择
authors: "Patrick Wilhelm, Inese Yilmaz, Odej Kao"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04194v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 具有隐私保护特性的碳效率联邦学习
tldr: 本研究针对碳高效联邦学习中数据质量不可知的问题，提出了一种噪声感知的客户端选择方法。虽然利用可再生能源能降低AI训练的碳足迹，但现有策略常因依赖局部损失而误选噪声数据客户端。本文通过引入梯度范数阈值机制和探测轮次，在现有碳效率策略基础上增加了噪声过滤模块。该方法不仅提升了模型在未知数据质量场景下的鲁棒性和性能，还平衡了碳预算与收敛效率，为实现可持续且高效的分布式学习提供了新方案。
selection_source: fresh_fetch
motivation: 现有的碳高效联邦学习策略在选择客户端时往往忽略了数据质量，容易选中含噪声数据的客户端，从而损害模型性能。
method: 提出一种基于梯度范数阈值和探测轮次的模块化方法，用于在碳预算限制下识别并过滤噪声客户端。
result: 实验表明，该方法能有效区分高质量与噪声数据，在提升模型准确率的同时降低了碳排放。
conclusion: 通过结合噪声感知与碳效率策略，可以实现更稳健且可持续的大规模分布式模型训练。
---

## 摘要
训练大规模神经网络需要大量的计算能力和能源。联邦学习支持在地理分布的数据中心之间进行分布式模型训练，利用可再生能源来减少人工智能训练的碳足迹。已经开发了各种客户端选择策略，以使可再生能源的波动性与联邦系统中稳定且公平的模型训练相协调。然而，由于联邦学习的隐私保护特性，客户端设备上的数据质量仍然未知，这给有效的模型训练带来了挑战。在本文中，我们在最先进的碳高效联邦学习客户端选择策略之上引入了一种模块化方法。我们的方法通过结合噪声客户端数据过滤来增强鲁棒性，在数据质量未知的情况下提高了模型性能和可持续性。此外，我们探讨了碳预算对模型收敛的影响，平衡了效率和可持续性。通过广泛的评估，我们证明了基于本地客户端损失的现代客户端选择策略倾向于选择具有噪声数据的客户端，最终降低了模型性能。为了解决这个问题，我们提出了一种利用探测轮次的梯度范数阈值化机制，以实现更有效的客户端选择和噪声检测，为碳高效联邦学习的实际部署做出了贡献。

## Abstract
Training large-scale Neural Networks requires substantial computational power and energy. Federated Learning enables distributed model training across geospatially distributed data centers, leveraging renewable energy sources to reduce the carbon footprint of AI training. Various client selection strategies have been developed to align the volatility of renewable energy with stable and fair model training in a federated system. However, due to the privacy-preserving nature of Federated Learning, the quality of data on client devices remains unknown, posing challenges for effective model training. In this paper, we introduce a modular approach on top to state-of-the-art client selection strategies for carbon-efficient Federated Learning. Our method enhances robustness by incorporating a noisy client data filtering, improving both model performance and sustainability in scenarios with unknown data quality. Additionally, we explore the impact of carbon budgets on model convergence, balancing efficiency and sustainability. Through extensive evaluations, we demonstrate that modern client selection strategies based on local client loss tend to select clients with noisy data, ultimately degrading model performance. To address this, we propose a gradient norm thresholding mechanism using probing rounds for more effective client selection and noise detection, contributing to the practical deployment of carbon-efficient Federated Learning.

---

## 论文详细总结（自动生成）

以下是对论文《Noise-aware Client Selection for carbon-efficient Federated Learning via Gradient Norm Thresholding》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：训练大规模神经网络会产生巨大的碳足迹。虽然“碳感知联邦学习（FL）”可以通过在可再生能源充足的地理区域选择客户端来降低排放，但联邦学习的隐私特性导致服务器无法预知客户端的数据质量。
*   **核心冲突**：现有的客户端选择策略（如 Oort）通常依赖“训练损失（Loss）”来衡量客户端的效用。然而，**高损失可能源于有价值的困难样本，也可能源于噪声或损坏的数据**。盲目选择高损失客户端会导致模型性能下降，并浪费宝贵的碳预算。
*   **研究目标**：在碳排放受限的情况下，如何识别并过滤掉含有噪声数据的客户端，从而实现既高效又可持续的联邦学习。

### 2. 论文提出的方法论
论文提出了一种模块化的噪声感知客户端选择方法，主要包含以下核心技术：

*   **梯度范数探测轮次（Probing Round）**：
    *   在训练初期引入探测轮次，评估所有客户端。
    *   **核心思想**：利用梯度范数（Gradient Norm）而非训练损失作为效用指标。梯度范数能更好地反映损失函数的曲率，从而区分信息量大的样本和纯噪声。
    *   **效用公式**：$U(i) = |B_i| \cdot \sqrt{\frac{1}{|B_i|} \sum_{k \in B_i} \|\nabla f(k)\|^2}$，其中 $\nabla f(k)$ 是样本 $k$ 的梯度，$B_i$ 是客户端的本地数据。
*   **梯度范数阈值化（Thresholding）**：
    *   服务器根据探测到的效用值设定阈值：$Utility \ge c \cdot \max(Utility)$（$c$ 为配置系数）。
    *   低于该阈值的客户端被视为噪声客户端，在后续训练中被剔除。
*   **效用感知的碳预算分配（OortCA）**：
    *   将客户端选择建模为受限优化问题：在每一轮中，在满足碳预算 $B_t$ 和选择数量 $K$ 的前提下，最大化所选客户端的总效用得分。
    *   该方法允许在预算内战略性地选择高排放但高价值的客户端，或在能源充足时选择低排放客户端。

### 3. 实验设计
*   **数据集与模型**：
    *   **CIFAR-10**：使用简单 CNN 模型。
    *   **CIFAR-100**：使用 DenseNet-121。
    *   **Tiny ImageNet**：使用 EfficientNet-B1。
*   **场景设置**：
    *   **客户端分布**：30个客户端，模拟分布在美国30个不同的能源区域。
    *   **数据分布**：采用 Dirichlet 分布（$\alpha=10$）模拟非独立同分布（Non-IID）场景。
    *   **噪声模拟**：随机选择6个客户端，对其图像添加高斯噪声（$\sigma=1$）。
    *   **碳数据**：使用来自 Electricity Maps 的真实历史每小时碳强度数据。
*   **对比基准（Benchmark）**：
    *   **Random**：随机选择客户端。
    *   **Oort**：基于损失的先进客户端选择策略（未考虑噪声过滤）。
    *   **OortWT / RandomWT**：加入梯度范数阈值化后的改进版本。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **能耗假设**：为了简化计算，论文假设所有客户端的功率需求相等，每个客户端在训练期间消耗 1kWh 能量，每轮训练持续一小时。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从简单 CNN 到复杂的 DenseNet 和 EfficientNet 模型。
    *   测试了三个不同规模的数据集（CIFAR-10/100, Tiny ImageNet）。
    *   进行了不同碳预算水平（从 0% 到 100%）的消融实验。
*   **充分性评价**：实验设计较为充分，通过对比“纯净数据”和“损坏数据”两种场景，客观地证明了基于损失的策略在面对噪声时的脆弱性。引入真实世界的碳强度数据增加了实验的实用价值和说服力。

### 6. 论文的主要结论与发现
*   **损失陷阱**：传统的基于损失的选择策略（如 Oort）会系统性地优先选择噪声客户端，因为噪声数据通常产生极高的损失，这严重损害了模型收敛。
*   **梯度范数的有效性**：梯度范数阈值化能有效识别并剔除噪声客户端，使模型在噪声环境下实现更快速、更稳定的收敛。
*   **碳效率提升**：通过效用感知的预算分配（OortCA），模型可以在仅消耗基准方法 **40% 碳排放**的情况下，达到与之相当的最终准确率。
*   **协同效应**：将噪声过滤与碳预算分配结合，可以确保有限的碳预算被花费在高质量、高回报的客户端上。

### 7. 优点
*   **鲁棒性强**：解决了碳感知 FL 中长期被忽视的数据质量问题，增强了系统对恶意或低质量数据的抵抗力。
*   **模块化设计**：提出的梯度范数探测和阈值化机制可以轻松集成到现有的各种客户端选择算法中。
*   **现实意义**：结合了真实的地理空间碳强度数据，为构建绿色、可持续的 AI 系统提供了切实可行的路径。

### 8. 不足与局限
*   **初始开销**：探测轮次（Probing Round）需要评估所有客户端，这在超大规模联邦网络中可能会带来显著的初始计算和通信开销。
*   **超参数敏感性**：阈值系数 $c$ 的选择对结果有较大影响，但在实际应用中，由于数据不可见，预先设定最优的 $c$ 值具有挑战性。
*   **能耗模型简化**：假设所有客户端功耗相同且恒定，忽略了不同硬件设备（如不同型号的 GPU 或边缘设备）之间的能效差异。
*   **隐私边界**：虽然梯度范数比原始数据更安全，但在某些极端隐私敏感的场景下，上传梯度统计信息是否会泄露数据分布特征仍需进一步探讨。

（完）
