---
title: Fast and Optimal Differentially Private Frequent-Substring Mining
title_zh: 快速且最优的差分隐私频繁子串挖掘
authors: "Peaker Guo, Rayne Holland, Hao Wu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09166v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 子串挖掘的差分隐私算法
tldr: "本研究针对差分隐私下的频繁子串挖掘问题，提出了一种高效且误差近乎最优的新算法。针对现有方法在处理大规模数据时面临的 $O(n^2\\ell^4)$ 高昂时空开销，本文通过改进候选子串生成策略并引入基于频率关系的搜索空间剪枝技术，将空间复杂度降低至 $O(n\\ell + |Σ|)$，时间复杂度降低至 $O(n\\ell \\log |Σ| + |Σ|)$，在保证隐私和精度的同时实现了极高的可扩展性。"
selection_source: fresh_fetch
motivation: 现有的差分隐私频繁子串挖掘算法虽然误差极低，但其极高的时空复杂度限制了其在大规模数据集上的实际应用。
method: 该方法采用自顶向下的探索策略，利用前缀和后缀的结构属性优化候选生成，并通过频率关系对搜索空间进行剪枝。
result: 新算法在保持近乎最优误差保证的同时，将空间和时间复杂度分别优化至线性及近线性级别。
conclusion: 该研究消除了先前算法中的二次方复杂度瓶颈，为实现高效且隐私保护的频繁子串挖掘提供了可扩展的方案。
---

## 摘要
给定一个包含 $n$ 条用户贡献字符串的数据集，每条字符串长度至多为 $\ell$，核心问题在于如何在保护用户隐私的前提下识别所有频繁子串。Bernardini 等人近期（PODS'25）提出了一种 $\varepsilon$-差分隐私算法，虽能达到近乎最优的误差界限，但其 $O(n^2\ell^4)$ 的空间与时间复杂度过高。本文提出了一种新的 $\varepsilon$-差分隐私算法，在保持相同近优误差保证的同时，将空间复杂度降低至 $O(n \ell+ |Σ| )$，时间复杂度降低至 $O(n \ell\log |Σ| + |Σ| )$（其中 $Σ$ 为输入字母表）。我们的方法基于自顶向下的候选子串探索，并引入了两项创新：(i) 利用频繁前缀与后缀结构特性的精细化候选生成策略；(ii) 基于频率关系的搜索空间剪枝。这些技术消除了先前工作中固有的二次项膨胀，实现了差分隐私下可扩展的频繁子串挖掘。

## Abstract
Given a dataset of $n$ user-contributed strings, each of length at most $\ell$, a key problem is how to identify all frequent substrings while preserving each user's privacy. Recent work by Bernardini et al. (PODS'25) introduced a $\varepsilon$-differentially private algorithm achieving near-optimal error, but at the prohibitive cost of $O(n^2\ell^4)$ space and processing time. In this work, we present a new $\varepsilon$-differentially private algorithm that retains the same near-optimal error guarantees while reducing space complexity to $O(n \ell+ |Σ| )$ and time complexity to $O(n \ell\log |Σ| + |Σ| )$, for input alphabet $Σ$. Our approach builds on a top-down exploration of candidate substrings but introduces two new innovations: (i) a refined candidate-generation strategy that leverages the structural properties of frequent prefixes and suffixes, and (ii) pruning of the search space guided by frequency relations. These techniques eliminate the quadratic blow-ups inherent in prior work, enabling scalable frequent substring mining under differential privacy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Fast and Optimal Differentially Private Frequent-Substring Mining》（快速且最优的差分隐私频繁子串挖掘）的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：在处理大规模用户贡献的文本（如 Reddit 评论、基因序列、交通轨迹）时，挖掘频繁出现的子串对于自动补全、模式识别等任务至关重要。
*   **核心挑战**：直接发布频繁子串会泄露用户隐私（如泄露特定医疗术语或家庭住址）。虽然差分隐私（DP）能提供保护，但现有的最优误差算法（如 Bernardini 等人于 PODS'25 提出的算法）在处理 $n$ 个长度为 $\ell$ 的字符串时，其时空复杂度高达 $O(n^2\ell^4)$。
*   **研究动机**：在现实场景中（如 $n=10^6, \ell=3000$），现有的二次方复杂度算法完全无法运行。本论文旨在寻找一种既能保持近乎最优误差界限，又能实现近线性复杂度的算法。

### 2. 方法论
论文提出了一种基于自顶向下探索的新算法，核心思想包括：
*   **二进制编码转换**：将原始字母表 $\Sigma$ 转换为二进制编码（每个字符后加结束符 `$`），将问题转化为二进制子串挖掘。这有效控制了搜索树的分叉率。
*   **精细化候选生成（核心创新）**：利用引理 4.4 证明，任何长度为 $k+t$ 的频繁子串，其长度为 $k$ 的前缀和后缀必须也是频繁的。算法通过为当前频繁集构建一个紧凑的后缀树（Suffix Tree），并重用该树来生成候选扩展，避免了前作中对所有频繁子串进行两两拼接的巨大开销。
*   **结构化剪枝**：在搜索过程中，一旦某个路径的噪声频率估计低于设定阈值，立即停止该分支的进一步探索。
*   **频率估计机制**：
    *   **重链剖分（Heavy-Light Decomposition）**：对搜索树进行剖分，将根到节点的路径分解为对数个重链。
    *   **二叉树机制（Binary Tree Mechanism）**：在每个重链上应用该机制来发布前缀和，从而在保证 $\varepsilon$-DP 的同时，将加性误差控制在 $\tilde{O}(\ell/\varepsilon)$，这在理论上是渐近最优的。

### 3. 实验设计
*   **研究性质**：本文主要是一篇**理论算法论文**，重点在于算法复杂度的改进和数学证明。
*   **Benchmark（基准）**：主要对比对象是 Bernardini et al. (PODS'25) 的算法。
*   **对比维度**：
    *   **时间复杂度**：从 $O(n^2\ell^4)$ 降低至 $O(n\ell \log |\Sigma| + |\Sigma|)$。
    *   **空间复杂度**：从 $O(n^2\ell^4)$ 降低至 $O(n\ell + |\Sigma|)$。
    *   **误差界限**：保持在 $\tilde{O}(\ell/\varepsilon)$，与已知理论下界匹配。
*   **提及场景**：文中引用了 Reddit 数据集作为背景，说明在百万级用户规模下，新算法是唯一具备可行性的方案。

### 4. 资源与算力
*   **说明**：论文中**未明确说明**使用了具体的 GPU 或 CPU 型号进行实验测试。
*   **原因**：作为理论计算机科学领域的论文，其核心贡献在于渐近复杂度的推导和隐私/效用边界的数学证明，而非特定硬件上的运行耗时。

### 5. 实验数量与充分性
*   **理论验证**：论文通过严谨的数学推导（定理 4.1）证明了算法的隐私性、效用（Utility）和性能。
*   **充分性评价**：从理论角度看，实验（证明过程）非常充分，涵盖了最坏情况下的复杂度分析和隐私预算分配。但从实证角度看，缺乏在真实数据集上的消融实验或运行时间对比图表，这在纯理论算法论文中较为常见。

### 6. 主要结论与发现
*   **性能突破**：证明了可以在近线性时间内完成具有最优误差保证的差分隐私频繁子串挖掘。
*   **消除二次方瓶颈**：通过巧妙利用后缀结构和重链剖分，消除了先前算法中由于候选子串爆炸导致的二次方复杂度。
*   **隐私与效用的平衡**：新算法在显著提升速度的同时，并未牺牲隐私保护强度或增加显著的加性误差。

### 7. 优点
*   **极高的扩展性**：将算法从“不可用”提升到了“工业级可用”的理论水平。
*   **优雅的结构设计**：利用后缀树和重链剖分解决 DP 敏感度问题，设计非常精巧。
*   **普适性**：二进制转换策略使得算法可以处理任何大小的字母表。

### 8. 不足与局限
*   **缺乏实证数据**：虽然理论复杂度极佳，但没有给出在具体机器上的实际运行耗时，难以观察常数项对性能的影响。
*   **二进制编码开销**：二进制转换虽然简化了逻辑，但将字符串长度增加了 $\log |\Sigma|$ 倍，这在某些超大字母表场景下可能会引入额外的处理负担。
*   **应用限制**：算法主要针对连续子串，对于非连续的序列模式（Sequential Patterns）挖掘尚需进一步扩展。

（完）
