---
title: Tight Auditing of Differential Privacy in MST and AIM
title_zh: MST 和 AIM 中差分隐私的紧致审计
authors: "Georgi Ganev, Meenatchi Sundaram Muthu Selva Annamalai, Bogdan Kulynych"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18352v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 审计合成数据生成器中的差分隐私
tldr: 针对MST和AIM等主流差分隐私（DP）合成数据生成器难以进行紧致审计的问题，本文提出了一种基于高斯差分隐私（GDP）的审计框架。该框架通过衡量全假阳性/假阴性权衡来评估隐私保证，并在最坏情况设置下首次实现了强隐私机制下的紧致审计。实验表明，在典型参数下理论与实践的隐私差距极小，为DP算法的安全性验证提供了有力工具。
selection_source: fresh_fetch
motivation: 现有的主流差分隐私合成数据生成器缺乏紧致的隐私审计手段，难以验证其理论隐私保证在实际中的有效性。
method: 提出一种基于高斯差分隐私（GDP）的审计框架，利用全假阳性/假阴性权衡曲线来精确测量隐私泄露。
result: 在强隐私机制下，实验测得的隐私参数与理论值高度接近（如0.43对0.45），证明了审计的紧致性。
conclusion: 该研究实现了对MST和AIM的首次紧致审计，证明了这些算法在实际应用中具有极小的理论与实践隐私差距。
---

## 摘要
MST 和 AIM 等最先进的差分隐私 (DP) 合成数据生成器已被广泛使用，但对其隐私保证进行紧致审计仍然具有挑战性。我们引入了一种基于高斯差分隐私 (GDP) 的审计框架，该框架通过完整的假阳性/假阴性权衡来衡量隐私。将该方法应用于最坏情况设置下的 MST 和 AIM，我们在强隐私机制下实现了首次紧致审计。对于 $(ε,δ)=(1,10^{-2})$，我们得到的 $μ_{emp}\approx0.43$，而理论推导的 $μ=0.45$，这表明理论与实践之间的差距很小。我们的代码已公开：https://github.com/sassoftware/dpmm。

## Abstract
State-of-the-art Differentially Private (DP) synthetic data generators such as MST and AIM are widely used, yet tightly auditing their privacy guarantees remains challenging. We introduce a Gaussian Differential Privacy (GDP)-based auditing framework that measures privacy via the full false-positive/false-negative tradeoff. Applied to MST and AIM under worst-case settings, our method provides the first tight audits in the strong-privacy regime. For $(ε,δ)=(1,10^{-2})$, we obtain $μ_{emp}\approx0.43$ vs. implied $μ=0.45$, showing a small theory-practice gap.   Our code is publicly available: https://github.com/sassoftware/dpmm.

---

## 论文详细总结（自动生成）

这篇论文针对差分隐私（DP）合成数据生成器的安全性验证问题，提出了一个高斯差分隐私（GDP）审计框架，并在 MST 和 AIM 两个主流模型上实现了首次“紧致”的审计。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：MST 和 AIM 是目前最先进的差分隐私合成数据生成算法，广泛应用于学术界和工业界（如英国人口普查数据发布）。
*   **核心问题**：尽管这些算法在理论上有隐私保证，但现有的审计方法在“强隐私机制”（如 $\epsilon=1$）下往往失效，得到的审计结果非常松散（甚至为零），无法有效验证实现过程是否正确或是否存在隐私泄露。
*   **动机**：现有的审计通常只报告单一的 $(\epsilon, \delta)$ 点，无法描述完整的隐私权衡（Trade-off）。作者旨在通过高斯差分隐私（GDP）框架，提供一种能够精确衡量理论与实践之间差距的紧致审计方法。

### 2. 方法论
*   **核心思想**：将隐私审计建模为一种基于假设检验的“成员推理攻击”（MIA）。通过观察模型在包含与不包含特定记录的相邻数据集上的输出差异，计算假阳性率（FPR）和假阴性率（FNR）的权衡曲线。
*   **关键技术细节**：
    *   **GDP 框架**：利用 GDP 中的单一参数 $\mu$ 来捕捉完整的 FPR/FNR 权衡曲线，而不是传统的单一隐私点。
    *   **最坏情况构建**：为了获得紧致界限，构建了极端相邻数据集：$D_{out}$ 包含 10 个相同的记录，而 $D_{in}$ 在此基础上增加一个目标记录，以最大化该记录对边际分布的影响。
    *   **攻击模型**：采用混合黑盒/白盒攻击。黑盒特征基于合成数据的查询结果，白盒特征则直接利用模型内部生成的带噪声的一维边际计数。
    *   **统计估计**：使用基于贝叶斯的方法来估计 $\mu$ 的下界（$\mu_{emp}$），确保结果具有统计置信度。

### 3. 实验设计
*   **实验对象**：MST 和 AIM 算法。
*   **实验场景**：
    *   基础场景：将模型简化为独立边际模型（仅测量一维边际）。
    *   扩展场景（附录）：训练包含高阶边际（二维、三维）的 MST/AIM 模型。
*   **基准（Benchmark）**：
    *   理论界限：基于算法内部使用的 $\rho$-zCDP 转换推导出的理论 $\mu$ 值。
    *   对比方法：对比了不同的分类器（XGBoost, Random Forest）、不同的阈值选择标准以及不同的攻击特征（黑盒 vs 白盒）。
*   **参数设置**：设定理论隐私预算为 $(\epsilon, \delta) = (1, 10^{-2})$。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实验规模**：作者为每个设置训练了 **10,000 个独立模型实例**（$D_{in}$ 和 $D_{out}$ 各 5,000 个），这种大规模的重复训练暗示了实验对计算资源有一定要求，尤其是为了获得统计上显著的审计曲线。

### 5. 实验数量与充分性
*   **实验数量**：进行了大规模的重复实验（1万次运行），并提供了详细的消融实验（图3），涵盖了阈值选择、下界估计方法、数据集大小、分类器选择等多个维度。
*   **充分性评价**：实验设计非常充分且严谨。通过在附录中加入高阶边际的审计，证明了该方法不仅适用于简化模型，也适用于更复杂的实际应用场景。采用最坏情况设置符合 DP 定义的本质，保证了审计的客观性。

### 6. 主要结论与发现
*   **首次实现紧致审计**：在 $\epsilon=1$ 的强隐私设置下，实验测得的 $\mu_{emp} \approx 0.43$，而理论推导的 $\mu = 0.45$。两者极其接近，证明了 MST/AIM 实现的正确性及其隐私保证的紧致性。
*   **理论与实践差距极小**：研究表明，MST 和 AIM 在实际运行中的隐私泄露几乎达到了理论上限，不存在明显的安全冗余或实现漏洞。
*   **GDP 的优越性**：相比传统的 $(\epsilon, \delta)$ 审计，GDP 框架能更直观地展示攻击者的完整权衡能力，并能更准确地捕捉到由于会计方法（Accounting）导致的细微隐私差异。

### 7. 优点
*   **紧致性**：解决了以往审计方法在低 $\epsilon$ 区域结果发散的问题。
*   **全面性**：提供了完整的 FPR-FNR 权衡曲线，而不仅仅是一个数值。
*   **实用性**：开源了审计代码，方便社区对其他 DP 合成数据生成器进行验证。

### 8. 不足与局限
*   **最坏情况依赖**：审计结果高度依赖于人工构建的“最坏情况”数据集，在实际平均分布的数据集上，隐私泄露可能会小得多（但这符合 DP 审计的标准做法）。
*   **模型范围**：目前主要针对基于高斯机制的边际模型（MST/AIM），对于基于其他机制（如拉普拉斯机制）或深度学习生成模型（如 DP-GAN）的适用性仍需进一步验证。
*   **计算开销**：为了获得紧致的统计下界，需要训练数千次模型，这对于计算资源受限的用户来说可能难以负担。

（完）
