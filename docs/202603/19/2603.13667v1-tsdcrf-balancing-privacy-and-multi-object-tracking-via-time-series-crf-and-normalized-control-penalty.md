---
title: "TSDCRF: Balancing Privacy and Multi-Object Tracking via Time-Series CRF and Normalized Control Penalty"
title_zh: TSDCRF：通过时间序列 CRF 和归一化控制惩罚平衡隐私与多目标跟踪
authors: "Bo Ma, Jinsong Wu, Weiqi Yan"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13667v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 通过在敏感区域校准高斯噪声实现差分隐私
tldr: 针对多目标跟踪中隐私保护与跟踪性能难以兼顾的问题，本文提出TSDCRF框架。该框架通过差分隐私高斯噪声保护敏感区域，利用归一化控制惩罚（NCP）在加噪前稳定类别预测，并结合时间序列动态条件随机场（DCRF）在加噪后修正轨迹偏差。实验证明，该方法在MOT16/17等数据集上实现了优于现有方法的隐私-效用平衡，有效减少了ID切换并增强了轨迹鲁棒性。
selection_source: fresh_fetch
motivation: 传统的隐私保护加噪手段会破坏跨帧关联，导致多目标跟踪出现频繁的ID切换或目标丢失。
method: 提出包含差分隐私噪声注入、NCP预测稳定机制以及DCRF时序一致性修正的即插即用精炼框架。
result: 在多个公开数据集上，TSDCRF相比白噪声和现有方法具有更低的跟踪误差和更强的抗干扰能力。
conclusion: TSDCRF为多目标跟踪提供了一种与具体检测器/跟踪器无关的高效隐私保护与性能平衡方案。
---

## 摘要
视频中的多目标跟踪通常需要外观或位置线索，这些线索可能会泄露敏感的身份信息，而添加隐私保护噪声通常会破坏跨帧关联，导致 ID 切换或目标丢失。我们提出了 TSDCRF，这是一种插件式细化框架，通过结合以下三个组件来平衡隐私与跟踪：(i) 在可配置的隐私预算下，通过对敏感区域施加校准高斯噪声实现 (ε,δ)-差分隐私；(ii) 一种归一化控制惩罚 (NCP)，在注入噪声前降低不稳定或冲突类别预测的权重，以稳定关联；(iii) 一种时间序列动态条件随机场 (DCRF)，用于强制执行时间一致性并纠正噪声后的轨迹偏差，从而减少 ID 切换并增强对轨迹劫持的抵御能力。该流水线与检测器和跟踪器的选择无关（例如 YOLOv4 和 DeepSORT）。我们在 MOT16、MOT17、Cityscapes 和 KITTI 上进行了评估。结果表明，TSDCRF 比白噪声和先前的方法（NTPD、PPDTSA）实现了更好的隐私-效用权衡：在保护隐私的同时，具有更低的 KL 散度偏移、更低的跟踪均方根误差 (RMSE) 以及在轨迹劫持下更高的鲁棒性。源代码见 https://github.com/mabo1215/TSDCRF.git

## Abstract
Multi-object tracking in video often requires appearance or location cues that can reveal sensitive identity information, while adding privacy-preserving noise typically disrupts cross-frame association and causes ID switches or target loss. We propose TSDCRF, a plug-in refinement framework that balances privacy and tracking by combining three components: (i) $(\varepsilon,δ)$-differential privacy via calibrated Gaussian noise on sensitive regions under a configurable privacy budget; (ii) a Normalized Control Penalty (NCP) that down-weights unstable or conflicting class predictions before noise injection to stabilize association; and (iii) a time-series dynamic conditional random field (DCRF) that enforces temporal consistency and corrects trajectory deviation after noise, mitigating ID switches and resilience to trajectory hijacking. The pipeline is agnostic to the choice of detector and tracker (e.g., YOLOv4 and DeepSORT). We evaluate on MOT16, MOT17, Cityscapes, and KITTI. Results show that TSDCRF achieves a better privacy--utility trade-off than white noise and prior methods (NTPD, PPDTSA): lower KL-divergence shift, lower tracking RMSE, and improved robustness under trajectory hijacking while preserving privacy. Source code in https://github.com/mabo1215/TSDCRF.git

---

## 论文详细总结（自动生成）

以下是对论文《TSDCRF: Balancing Privacy and Multi-Object Tracking via Time-Series CRF and Normalized Control Penalty》的结构化总结：

### 1. 论文的核心问题与整体含义
在多目标跟踪（MOT）任务中，系统依赖目标的外观和位置特征进行跨帧关联，但这会泄露敏感身份信息（如人脸、步态）。传统的隐私保护手段（如直接添加噪声）虽然能掩盖特征，但往往会破坏数据关联的稳定性，导致频繁的 ID 切换（ID Switch）或目标丢失。此外，跟踪系统还面临轨迹劫持（Trajectory Hijacking）等安全威胁。
**研究动机：** 开发一种既能提供形式化隐私保证（差分隐私），又能通过时序一致性修正来维持跟踪精度的插件式框架。

### 2. 论文提出的方法论
论文提出了 **TSDCRF** 框架，其核心思想是在噪声注入前后分别进行优化，以平衡隐私与效用。关键技术包括：
*   **$(\epsilon, \delta)$-差分隐私（DP）：** 针对检测到的敏感区域（ROI），根据预设的隐私预算 $\epsilon$ 注入校准后的高斯噪声，实现数学上的隐私保护。
*   **归一化控制惩罚（NCP）：** 在注入噪声**之前**应用。它根据检测置信度为不同类别分配权重，降低不稳定或冲突类别预测的权重，从而在特征被模糊前稳定关联逻辑。
*   **时间序列动态条件随机场（DCRF）：** 在注入噪声**之后**应用。将跨帧关联建模为线性链 CRF，利用一元项（NCP 加权的类别一致性）和二元项（时序平滑度约束）来修正因噪声引起的轨迹偏差，减少 ID 切换。
*   **算法流程：** 检测器（如 YOLOv4）输出 $\rightarrow$ 基础跟踪器（如 DeepSORT）关联 $\rightarrow$ 隐私模块（NCP + DP 噪声） $\rightarrow$ TSDCRF 精炼修正。

### 3. 实验设计
*   **数据集：** 使用了多目标跟踪领域的权威基准 MOT16、MOT17，以及自动驾驶场景数据集 Cityscapes 和 KITTI。
*   **对比方法（Baselines）：** 
    *   直接添加白噪声（White Noise）。
    *   NTPD（基于前缀树的轨迹隐私保护方法）。
    *   PPDTSA（隐私保护深度变换自注意力框架）。
*   **评估指标：** 
    *   **跟踪性能：** RMSE（均方根误差）、GIoU、mAP@0.5、Objectness。
    *   **隐私与失真：** KL 散度（衡量分布偏移）、PSNR（峰值信噪比）、隐私检索频率。
    *   **鲁棒性：** 在“轨迹劫持”和“盲目标检测攻击”场景下的表现。

### 4. 资源与算力
*   **硬件配置：** Intel Core i7 @2.4 GHz CPU，8 GB RAM。
*   **GPU：** 单块 NVIDIA GeForce RTX 3070 (CUDA 11.0)。
*   **软件环境：** Windows 10 (64-bit)。
*   **说明：** 文中未明确给出具体的训练总时长，但提到实验是在 30 FPS 的实时速率下进行的，表明该框架具有较好的推理效率。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个主流数据集上进行了广泛测试，每种设置运行 200 帧并取 20 个视频剪辑的平均值。
*   **充分性：** 实验涵盖了统计学分析（KL 散度、PSNR）、跟踪精度分析（RMSE）以及对抗性攻击测试。通过改变隐私预算 $\epsilon$ 展示了隐私与效用的权衡曲线。
*   **客观性：** 对比了多种不同类型的基准方法，并针对不同数据集（如 MOT 与 KITTI）的差异进行了深入讨论，实验设计较为全面、客观。

### 6. 论文的主要结论与发现
*   **性能平衡：** TSDCRF 在保护隐私的同时，其跟踪偏差（RMSE）显著低于白噪声和 NTPD/PPDTSA 方法。
*   **分布保持：** 相比于直接加噪，TSDCRF 处理后的敏感区域特征分布与原始分布的 KL 散度更小，意味着在保护隐私的同时保留了更多的有用特征。
*   **抗攻击能力：** DCRF 模块能有效修正攻击者诱导的轨迹偏差，增强了系统在轨迹劫持攻击下的鲁棒性。
*   **隐私有效性：** 随着隐私预算 $\epsilon$ 的增加，隐私泄露风险（检索频率）降低，证明了差分隐私机制的有效性。

### 7. 优点
*   **插件式设计：** 该框架不依赖特定的检测器或跟踪器，具有极强的通用性，可直接集成到现有工业流水线中。
*   **多阶段优化：** 创新性地结合了加噪前的 NCP 稳定和加噪后的 DCRF 修正，从时序角度解决了隐私保护带来的精度下降问题。
*   **形式化保证：** 引入差分隐私理论，提供了可量化的隐私保护水平。

### 8. 不足与局限
*   **极端攻击下的失效：** 实验显示，当检测器的分类器受到严重干扰（如类别混淆攻击）时，关联仍可能发生漂移，系统无法完全防御此类攻击。
*   **隐私覆盖范围：** 目前主要侧重于 ROI 区域和边界框轨迹的保护，对于更高级的身份推断（如通过步态或长期行为模式分析）尚未实现完全的匿名化。
*   **硬件限制：** 实验环境内存（8GB）相对较小，在大规模并发处理或超高分辨率视频下的性能表现有待进一步验证。

（完）
