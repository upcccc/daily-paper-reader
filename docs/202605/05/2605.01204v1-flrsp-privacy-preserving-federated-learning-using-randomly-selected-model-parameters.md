---
title: "FLRSP: Privacy-Preserving Federated Learning Using Randomly Selected Model Parameters"
title_zh: FLRSP：使用随机选择模型参数的隐私保护联邦学习
authors: "Hiroto Sawada, Shoko Imaizumi, Hitoshi Kiya"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01204v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 使用随机参数的隐私保护联邦学习
tldr: 针对联邦学习中模型更新可能导致隐私泄露以及现有防御方法牺牲准确率的问题，本文提出了一种名为FLRSP的隐私保护联邦学习方法。该方法通过在本地训练后随机选择部分模型参数上传至中央服务器来更新全局模型。实验证明，FLRSP在ResNet34和ViT架构下，不仅在图像分类任务中保持了高准确率，还展现出比现有方法更强的抗攻击鲁棒性，实现了隐私保护与模型性能的平衡。
selection_source: fresh_fetch
motivation: 联邦学习中共享的模型更新存在泄露原始训练数据的风险，且现有的鲁棒性提升方法通常会以降低模型准确率为代价。
method: 提出FLRSP方法，通过在本地服务器中随机选择部分计算出的模型参数并共享，以此来更新中央服务器的全局模型。
result: 在ResNet34和ViT架构上的实验表明，该方法在图像分类准确率和抵御最先进攻击的鲁棒性方面均优于现有方法。
conclusion: FLRSP是一种有效的联邦学习框架，能够在不显著损失准确率的情况下，显著增强模型对隐私攻击的防御能力。
---

## 摘要
在本文中，我们提出了一种利用随机选择的模型参数来更新全局模型的隐私保护联邦学习方法。高质量的深度神经网络（DNN）模型通常需要海量的训练数据，但在处理敏感或个人信息时，模型训练会引发隐私担忧。联邦学习是一种分布式机器学习框架，其中多个客户端和一个服务器协作训练模型。然而，如果共享的更新被泄露，攻击者可能会重构原始训练数据。此外，以往用于提高鲁棒性的方法通常会降低准确率。为了克服这些问题，在我们提出的名为“使用随机选择模型参数的联邦学习”（FLRSP）的方法中，每个本地服务器计算出的模型参数会被随机选择并共享，以更新中央服务器中的全局模型。在实验中，我们在 ResNet34 架构和 Vision Transformer (ViT) 上，结合联邦随机梯度下降 (FedSGD) 和联邦平均 (FedAvg) 进行了图像分类任务。结果表明，与以往的方法相比，我们的方法在图像分类准确率以及针对最先进攻击的鲁棒性方面均表现出有效性。

## Abstract
In this paper, we propose a method for privacy-preserving federated learning that uses randomly selected model parameters to update global models. High-quality deep neural networks (DNN) models require a huge amount of training data in general, but model training raises privacy concerns when dealing with sensitive or personal information. Federated learning is a distributed machine learning framework in which multiple clients and a server train a model collaboratively. However, if the shared updates are compromised, an attacker may reconstruct the original training data. In addition, previous methods for improving robustness generally reduce the accuracy. To overcome these issues, in our method called federated learning using randomly selected model parameters (FLRSP), model parameters computed in each local server are randomly selected and shared to update a global model in a central server. In experiments, image classification tasks were carried out on the ResNet34 architecture and the Vision Transformer (ViT) under the use of Federated Stochastic Gradient Descent (FedSGD) and Federated Averaging (FedAvg), and the results demonstrated our method's effectiveness in terms of image classification accuracy and robustness against state-of-the-art attacks compared with previous methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《FLRSP: Privacy-Preserving Federated Learning Using Randomly Selected Model Parameters》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**联邦学习（Federated Learning, FL）中的隐私泄露风险**。虽然联邦学习允许客户端在不共享原始数据的情况下协作训练模型，但研究表明，攻击者可以通过截获客户端上传的梯度或模型参数，利用“梯度泄露攻击”或“对抗优化攻击”重构出原始训练图像。
*   **研究动机**：现有的防御手段（如差分隐私 DP）虽然能提供隐私保护，但往往会引入大量噪声，导致模型准确率显著下降。
*   **核心目标**：提出一种既能有效抵御图像重构攻击，又能保持模型分类准确率的轻量级隐私保护方法。

### 2. 方法论：FLRSP 核心思想与技术细节
论文提出了 **FLRSP（Federated Learning using Randomly Selected model Parameters）**。
*   **核心思想**：在客户端向服务器发送更新之前，**随机选择**一部分模型参数（梯度或权重）进行共享，而将其余参数置为零。
*   **关键技术细节**：
    *   **随机选择机制**：每个客户端生成一个二值随机序列 $B$，以概率 $R$ 将参数置为 0。这意味着只有 $(1-R)$ 比例的参数被上传。
    *   **服务器端聚合**：服务器在聚合时，仅对那些非零的参数进行算术平均。公式上，服务器会记录每个参数位置被更新的次数，从而确保全局模型的更新是基于有效反馈的平均值。
    *   **算法兼容性**：该方法可无缝集成到 **FedSGD**（每步迭代共享梯度）和 **FedAvg**（每个 Epoch 共享权重）中。
    *   **收敛性保证**：论文通过数学推导证明，FLRSP 在 FedSGD 下的收敛性等效于调整了学习率的传统 FL，只要标准 FL 收敛，FLRSP 也能稳定收敛。

### 3. 实验设计
*   **数据集**：CIFAR-10（包含 50,000 张训练图，10,000 张测试图）。
*   **模型架构**：Vision Transformer (ViT-Small) 和 ResNet34。
*   **Benchmark 与对比方法**：
    *   **Standard FL**：无防御的基准。
    *   **Fixed-position 方法**：一种针对 ViT 的特定防御，通过固定位置嵌入层来防止泄露。
    *   **Differential Privacy (DP)**：高斯差分隐私，设置了不同的隐私强度 $\epsilon$。
*   **攻击场景**：
    *   **APRIL 攻击**：专门针对 ViT 的闭式解图像重构攻击。
    *   **Adversarial Optimization Attack**：基于余弦相似度优化的通用梯度反推攻击。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练总时长。
*   **实验参数**：提及了 Batch Size 为 32，客户端数量 $N=5$，学习率在 ViT 上为 0.00001，在 ResNet 上为 0.0001。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了从零训练（From Scratch）和微调（Fine-tuning）两种模式。
    *   测试了不同的随机丢弃概率 $R$（0.2, 0.5, 0.8）。
    *   考虑了数据分布的异质性，进行了 **Non-IID**（Dirichlet 分布 $a=0.1$）实验。
*   **充分性评价**：实验设计较为全面，涵盖了当前主流的 CNN 和 Transformer 架构，并针对性地使用了 SSIM（结构相似性）指标来量化攻击者的重构质量。通过 15 张代表性图像的重构实验展示了防御的直观效果，具有较强的说服力。

### 6. 主要结论与发现
*   **准确率保持**：FLRSP 在 $R$ 高达 0.8 时，最终分类准确率仍能逼近 Standard FL，显著优于差分隐私（DP）方法。
*   **防御有效性**：
    *   对于 ViT，FLRSP 成功破坏了 APRIL 攻击所需的数学条件，重构图像完全不可辨。
    *   对于 CNN，FLRSP 使得对抗优化攻击的 SSIM 值大幅下降（通常低于 0.1），攻击者只能得到噪声。
*   **架构优势**：相比于只能用于 ViT 的 Fixed-position 方法，FLRSP 在 ResNet 等 CNN 架构上同样表现优异。
*   **Non-IID 适应性**：在非独立同分布数据下，FLRSP 虽然准确率有所波动，但依然优于 DP。

### 7. 优点（亮点）
*   **通用性**：不依赖于特定的模型架构，适用于各种深度学习模型。
*   **高效性**：不需要复杂的加密计算或大量的噪声注入，计算开销极低。
*   **隐私与性能的平衡**：在极高防御强度的前提下，几乎不牺牲模型的最终推理准确率。
*   **理论支撑**：提供了收敛性分析，证明了方法的数学合理性。

### 8. 不足与局限
*   **数据集单一**：实验仅限于 CIFAR-10（$32 \times 32$ 分辨率），未在 ImageNet 等大规模、高分辨率数据集上验证其在大模型上的表现。
*   **超参数选择**：概率 $R$ 的选择目前依赖于实验经验，缺乏一种自适应确定最优 $R$ 值的自动化机制。
*   **客户端规模**：实验中客户端数量较少（$N=5$），在成千上万个客户端的大规模联邦学习场景下的表现有待观察。
*   **攻击覆盖面**：虽然针对重构攻击效果显著，但未讨论其对成员推理攻击（Membership Inference）或属性推理攻击的防御能力。

（完）
