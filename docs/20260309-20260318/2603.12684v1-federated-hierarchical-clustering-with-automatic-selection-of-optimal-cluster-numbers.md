---
title: Federated Hierarchical Clustering with Automatic Selection of Optimal Cluster Numbers
title_zh: 具有自动选择最优聚类数功能的联邦层次聚类
authors: "Yue Zhang, Chuanlong Qiu, Xinfa Liao, Yiqun Zhang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12684v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 具有隐私保护传输约束的联邦聚类框架
tldr: "针对联邦聚类中聚类数量未知且数据分布不均的挑战，本文提出Fed-k*-HC框架。该方法通过客户端生成微子聚类原型并上传至服务端进行层次合并，利用基于密度的合并策略和自终止机制自动确定最优聚类数量k*。实验证明该方法能有效处理不同规模和形状的簇，在保护隐私的同时实现了鲁棒且准确的联邦聚类。"
selection_source: fresh_fetch
motivation: 现有的联邦聚类方法通常假设聚类数量已知且簇大小均匀，难以应对现实场景中聚类数未知及数据分布不平衡的问题。
method: 提出一种基于层次聚类的联邦框架，通过在服务端对客户端上传的微子聚类原型进行密度感知合并，并根据邻域关系自动确定最优聚类数。
result: "在多种数据集上的实验结果表明，Fed-k*-HC能够准确识别合适的聚类数量，并有效处理各种形状和大小的簇。"
conclusion: 本研究为联邦学习环境下的无监督聚类提供了一种无需预设聚类数且能适应复杂数据分布的稳健解决方案。
---

## 摘要
联邦聚类（FC）是一种新兴且极具前景的解决方案，旨在以无监督的方式从分布式且受隐私保护的数据中探索数据分布模式。现有的联邦聚类方法隐含地依赖于一个假设，即客户端拥有已知数量且规模均匀的聚类。然而，在实际场景中，真实的聚类数量通常是未知的，且聚类规模在现实场景中自然是不平衡的。此外，联邦学习中的隐私保护传输约束不可避免地减少了可用信息，使得开发鲁棒且准确的联邦聚类变得极具挑战性。因此，我们提出了一种名为 Fed-$k^*$-HC 的新型联邦聚类框架，该框架能够根据通过层次聚类探索的数据分布，自动确定最优聚类数 $k^*$。为了获取用于确定 $k^*$ 的全局数据分布，我们让每个客户端生成微子聚类（micro-subclusters），并将其原型（prototypes）上传至服务器进行层次合并。基于密度的合并设计允许探索不同大小和形状的聚类，且渐进式合并过程可以根据原型之间的邻近关系自动终止，从而确定 $k^*$。在多种数据集上的广泛实验证明了所提 Fed-$k^*$-HC 的联邦聚类能力，能够准确地探索出合适的聚类数量。

## Abstract
Federated Clustering (FC) is an emerging and promising solution in exploring data distribution patterns from distributed and privacy-protected data in an unsupervised manner. Existing FC methods implicitly rely on the assumption that clients are with a known number of uniformly sized clusters. However, the true number of clusters is typically unknown, and cluster sizes are naturally imbalanced in real scenarios. Furthermore, the privacy-preserving transmission constraints in federated learning inevitably reduce usable information, making the development of robust and accurate FC extremely challenging. Accordingly, we propose a novel FC framework named Fed-$k^*$-HC, which can automatically determine an optimal number of clusters $k^*$ based on the data distribution explored through hierarchical clustering. To obtain the global data distribution for $k^*$ determination, we let each client generate micro-subclusters. Their prototypes are then uploaded to the server for hierarchical merging. The density-based merging design allows exploring clusters of varying sizes and shapes, and the progressive merging process can self-terminate according to the neighboring relationships among the prototypes to determine $k^*$. Extensive experiments on diverse datasets demonstrate the FC capability of the proposed Fed-$k^*$-HC in accurately exploring a proper number of clusters.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Fed-$k^*$-HC** 的新型联邦聚类框架，旨在解决联邦学习环境下聚类数目未知以及数据分布不平衡（非独立同分布 Non-IID）的挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的联邦聚类（FC）方法通常依赖两个理想化假设：1) 聚类数目 $k$ 预先已知；2) 各个簇的大小相对均匀。
*   **研究动机**：在现实场景中，真实的聚类数往往难以预知，且数据分布天然存在严重的不平衡性（即“长尾分布”）。此外，联邦学习的隐私约束限制了原始数据的传输，导致服务端难以获取全局分布信息，容易产生“均匀效应”（Uniform Effect），即强制将大小不一的簇划分为等规模的组，从而导致聚类精度下降。

### 2. 方法论
Fed-$k^*$-HC 采用了一种**“本地微划分 + 服务端层次合并”**的单轮（One-shot）联邦聚类范式：
*   **客户端：自动化微划分（SNP 算法）**
    *   每个客户端利用竞争学习机制（SNP）将本地数据划分为多个细粒度的“微子聚类”（Micro-subclusters），这些微单元能更精确地描述复杂的局部分布。
    *   **隐私保护传输**：客户端不上传原始数据，而是计算每个子聚类的均值和协方差，并基于多元正态分布生成等量的“替代数据”上传至服务端，从而在保护隐私的同时保留分布特征。
*   **服务端：层次合并与自动 $k^*$ 确定**
    *   **自动确定聚类数（SNC 算法）**：引入“严格自然邻居”（Strict Natural Neighbors, SNN）概念，通过分析子聚类原型的邻域结构和密度差异，自动识别最优聚类数 $k^*$，无需人工干预。
    *   **层次合并机制**：基于定义的重叠度、距离和相似度指标，迭代合并最相似的子聚类。这种自下而上的合并方式能有效识别不同形状和规模的簇，缓解分区算法中的“均匀效应”。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：包括 `ids2`、`gaussian` 及其变体（如 Non-IID 版本），专门用于模拟不平衡分布。
    *   **真实数据集**：来自 UCI 仓库的 `pageblock`、`yeast`、`abalone`、`breast` 和 `digits`。
*   **对比方法（Baselines）**：
    *   **SOTA 联邦聚类**：KFed (ICML 2021)、MUFC (ICLR 2023)、F3KM (PACMMOD 2023)、Orchestra (ICML 2022)。
    *   **改进/衍生方法**：Orchestra*（改进版）、Fed-AP（基于近邻传播）、Fed-MS（基于均值漂移）、Fed-KDPC（基于密度峰值）。
*   **评价指标**：F-measure、Accuracy、NMI、ARI 以及专门衡量不平衡聚类性能的 DCV。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **12th Gen Intel Core i7-12700 CPU (2.10 GHz)** 和 **32 GB RAM** 的工作站上完成。
*   **算力说明**：论文未提及使用 GPU，这表明该算法主要依赖 CPU 进行数值计算和逻辑处理。由于采用了单轮通信机制，其通信开销较低，对网络带宽要求不高。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 11 个数据集（6 个合成，5 个真实），进行了性能对比、Non-IID 场景测试、消融实验、参数敏感性分析（针对 $t, b, \beta$ 等参数）以及计算效率/可扩展性评估。
*   **充分性评价**：实验设计较为全面。通过 Wilcoxon 符号秩检验验证了结果的显著性；消融实验证明了 SNC 算法在确定 $k^*$ 上的准确性；效率实验展示了算法在客户端数量和数据规模增加时的线性扩展能力。整体而言，实验客观且对比公平。

### 6. 主要结论与发现
*   **性能优越性**：Fed-$k^*$-HC 在处理不平衡数据和 Non-IID 分布时显著优于现有的 SOTA 方法，尤其是在 DCV 指标上表现突出。
*   **自动估计准确**：SNC 算法在大多数数据集上能准确命中真实的聚类数目 $K$，即使在边界模糊的复杂数据集上，估计值也接近真实值。
*   **高效性**：单轮通信机制避免了多次迭代产生的延迟，使其非常适合通信受限的边缘计算场景。

### 7. 优点与亮点
*   **无需预设 $k$ 值**：打破了传统联邦聚类必须预知聚类数的限制，极大地提升了实际应用中的灵活性。
*   **抗不平衡性强**：通过微划分和层次合并，有效解决了小簇被大簇“吞噬”的问题。
*   **单轮通信**：在保证隐私的同时，极大地降低了联邦学习中的通信成本。

### 8. 不足与局限
*   **极端不平衡限制**：如果某些簇的规模极小（甚至小于客户端生成的微子聚类），算法可能无法有效识别。
*   **服务端计算压力**：随着客户端数量和子聚类原型的增加，服务端进行层次合并的计算复杂度会上升，在大规模场景下可能存在瓶颈。
*   **隐私深度**：虽然使用了替代数据，但尚未集成同态加密（HE）或差分隐私（DP）等更高级的隐私增强技术（作者将其列为未来工作）。
*   **特定数据集失效**：在如 `breast` 这种簇边界极度模糊的数据集上，自动确定 $k^*$ 的准确性仍有提升空间。

（完）
