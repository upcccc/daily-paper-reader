---
title: "TAPAS: Efficient Two-Server Asymmetric Private Aggregation Beyond Prio(+)"
title_zh: TAPAS：超越 Prio(+) 的高效双服务器非对称隐私聚合
authors: "Harish Karthikeyan, Antigoni Polychroniadou"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19949v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 联邦学习中的隐私保护聚合
tldr: 针对联邦学习等大规模分布式数据聚合中的隐私保护问题，本文提出TAPAS方案。该方案改进了传统的双服务器协议（如Prio），通过非对称设计使辅助服务器的计算和通信开销与输入维度无关。TAPAS基于格密码实现后量子安全，支持可识别中止的恶意安全性，且无需可信设置，显著降低了系统总成本并增强了非对抗假设。
selection_source: fresh_fetch
motivation: 现有的双服务器聚合协议在处理高维数据时面临对称的高昂计算与通信成本，难以扩展至现代大规模机器学习任务。
method: 采用非对称架构设计，结合新型高效的格基零知识证明，使主服务器承担主要聚合工作而辅助服务器仅需轻量级计算。
result: 实现了服务器间通信开销与数据维度无关，并提供了基于标准格假设的后量子安全性和可识别中止的恶意安全性。
conclusion: TAPAS在无需预处理的情况下，为大规模高维数据的隐私聚合提供了一个高效、鲁棒且具备后量子安全性的实用解决方案。
---

## 摘要
隐私保护聚合是 AI 系统的基石，它允许在不暴露个人记录的情况下从分布式数据中学习，特别是在联邦学习和遥测领域。现有的双服务器协议（例如 Prio 及其后续方案）通过验证输入并防止任何单方获知用户值，建立了一个实用的基准，但它们在两个服务器上施加了对称的成本，且通信开销随每个客户端的输入维度 $L$ 线性扩展。现代学习任务通常涉及数千万到数亿个模型参数的维度 $L$。我们提出了 TAPAS，一种双服务器非对称隐私聚合方案，从四个维度解决了这些局限性：(i) 无需可信设置或预处理，(ii) 服务器端通信与 $L$ 无关，(iii) 仅基于标准格假设（LWE、SIS）的后量子安全性，以及 (iv) 具有可识别中止（identifiable abort）和服务器全恶意安全性的更强鲁棒性。一个关键的设计选择是刻意的非对称性：一个服务器承担 $O(L)$ 的聚合和验证工作，而另一个服务器作为轻量级辅助者，其计算量与 $L$ 无关。这降低了总成本，使辅助服务器能够在通用硬件上运行，并强化了服务器之间的非共谋假设。我们的主要贡献之一是一套全新的、高效的基于格的零知识证明；据我们所知，我们是首个在双服务器设置中建立具有可识别中止的隐私性和正确性的方案。

## Abstract
Privacy-preserving aggregation is a cornerstone for AI systems that learn from distributed data without exposing individual records, especially in federated learning and telemetry. Existing two-server protocols (e.g., Prio and successors) set a practical baseline by validating inputs while preventing any single party from learning users' values, but they impose symmetric costs on both servers and communication that scales with the per-client input dimension $L$. Modern learning tasks routinely involve dimensionalities $L$ in the tens to hundreds of millions of model parameters.   We present TAPAS, a two-server asymmetric private aggregation scheme that addresses these limitations along four dimensions: (i) no trusted setup or preprocessing, (ii) server-side communication that is independent of $L$ (iii) post-quantum security based solely on standard lattice assumptions (LWE, SIS), and (iv) stronger robustness with identifiable abort and full malicious security for the servers. A key design choice is intentional asymmetry: one server bears the $O(L)$ aggregation and verification work, while the other operates as a lightweight facilitator with computation independent of $L$. This reduces total cost, enables the secondary server to run on commodity hardware, and strengthens the non-collusion assumption of the servers. One of our main contributions is a suite of new and efficient lattice-based zero-knowledge proofs; to our knowledge, we are the first to establish privacy and correctness with identifiable abort in the two-server setting.

---

## 论文详细总结（自动生成）

### TAPAS：高效双服务器非对称隐私聚合方案总结

#### 1. 核心问题与整体含义
在联邦学习（FL）和遥测任务中，隐私保护聚合（Secure Aggregation）是核心技术。现有的主流双服务器协议（如 Prio, Prio+, Elsa）采用**对称设计**，即两个服务器承担相同的计算和通信开销，且开销随模型参数维度 $L$ 线性增长。在现代 AI 模型（参数量达数千万甚至数亿）中，这导致了巨大的带宽压力和计算成本。
**TAPAS 的研究动机**在于打破这种对称性，通过非对称架构将辅助服务器的开销与 $L$ 解耦，同时引入后量子安全性和更强的恶意安全性（可识别中止），以适应大规模高维数据的聚合需求。

#### 2. 方法论：核心思想与关键技术
TAPAS 提出了**双服务器非对称隐私聚合方案**，其核心思想如下：
*   **非对称架构设计**：
    *   **服务器 1 (S1)**：作为主聚合器，接收 $O(L)$ 维度的加密数据，承担主要的验证和聚合工作。
    *   **服务器 2 (S2)**：作为轻量级协调者，仅接收 $O(\lambda)$ 维度（与安全参数相关，与 $L$ 无关）的种子或密钥分量。
*   **基于 LWE 的掩码机制**：利用 Regev 加密方案的加法同态性。客户端发送 $y_i = As_i + e_i + \Delta x_i$ 给 S1，发送种子 $s_i$ 给 S2。S2 汇总种子后交给 S1，S1 即可解密出聚合结果 $\sum x_i$。
*   **新型格基零知识证明 (ZK Proofs)**：
    *   **分块拒绝采样 (Block-wise Rejection Sampling)**：针对格密码证明中常见的“松弛（Slack）”问题，将高维向量分解为 $k$ 个小块，独立进行拒绝采样和重启。这避免了因维度过高导致的采样失败，显著提升了证明效率。
    *   **两种实例化**：提供了基于 Bulletproofs (TAPAS BP) 和纯格假设 (TAPAS LWE) 的两种版本，分别平衡了证明大小和计算速度。
*   **可识别中止 (Identifiable Abort)**：通过在聚合前引入签名的绑定阶段，确保如果协议因恶意行为中断，诚实方可以提供证据指认作弊者（服务器或客户端），防止恶意服务器通过“甩锅”给客户端来干扰协议。

#### 3. 实验设计
*   **对比基准 (Benchmarks)**：
    *   **Elsa**：目前最先进的具备恶意隐私保护的对称双服务器协议。
    *   **Prio (prio3)**：工业界标准化的隐私聚合协议实现。
*   **实验场景**：
    *   **维度扩展性**：向量维度 $L$ 从 $2^{16}$ 增加到 $2^{18}$（约 26 万参数）。
    *   **客户端扩展性**：客户端数量 $n$ 从 10 增加到 100。
*   **评估指标**：客户端证明生成时间、S1 验证时间、S2 处理时间。

#### 4. 资源与算力
*   **硬件环境**：实验在单个 **AWS c5.9xlarge** 实例上运行。
*   **配置细节**：该实例配备 36 个 vCPU（Intel Xeon Platinum 8000 系列 @ 3.0 GHz）和 72 GiB RAM。
*   **算力说明**：论文未提及 GPU 使用，所有密码学操作均在 CPU 上通过 Rust (v1.75) 实现。

#### 5. 实验数量与充分性
*   **实验规模**：论文针对不同维度和客户端数量进行了多组对比实验，并提供了详细的耗时分解。
*   **充分性评价**：
    *   **客观性**：对比了当前最先进的开源实现（如 prio3），实验设置较为公平。
    *   **局限性**：实验中的客户端数量（最高 100）相对于真实世界的大规模 FL 场景（可能成千上万）偏小；向量维度虽然达到了 $2^{18}$，但距离现代大模型（亿级参数）仍有一定差距。不过，作为密码学协议的微基准测试，其趋势展示已足够充分。

#### 6. 主要结论与发现
*   **显著的性能提升**：在 $L = 2^{18}$ 时，TAPAS 的客户端速度比 Prio 快约 **93 倍**，比 Elsa 快约 **17.5 倍**。
*   **非对称优势显著**：S2 的计算开销极低（100 个客户端仅需 0.521 秒），比 Prio 降低了 **30 倍**，证明了辅助服务器可以使用廉价的通用硬件（如微型云实例或笔记本电脑）。
*   **通信解耦成功**：成功实现了服务器间通信开销与模型维度 $L$ 的解耦，解决了高维模型聚合的带宽瓶颈。

#### 7. 优点
*   **高效性**：通过非对称设计和分块 ZK 证明，极大地降低了客户端和辅助服务器的负担。
*   **强安全性**：不仅提供后量子安全，还实现了“可识别中止”，这在双服务器设置中是首创。
*   **部署友好**：无需可信第三方设置（Trusted Setup），无需复杂的预处理阶段。

#### 8. 不足与局限
*   **格密码的固有开销**：尽管进行了优化，格基证明仍存在一定的“松弛”因子，可能导致在极高精度要求下需要更大的模数 $q$。
*   **实验规模上限**：实验未展示在数千名客户端或数亿维度下的真实网络延迟表现。
*   **存储压力**：虽然通信解耦了，但主服务器 S1 仍需存储所有客户端的 $O(L)$ 密文，对于超大规模模型，S1 的内存/存储压力依然存在。

（完）
