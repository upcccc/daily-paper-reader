---
title: Better Bounds for the Distributed Experts Problem
title_zh: 分布式专家问题的更优界限
authors: "David P. Woodruff, Samson Zhou"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09168v1"
tags: ["query:dml-dp"]
score: 6.0
evidence: 服务器中的分布式专家问题
tldr: 本研究探讨了分布式专家问题，即n个专家分布在s个服务器上，目标是在T个时间步内最小化累积遗憾R。论文提出了一种新的分布式协议，在保证遗憾界接近最优的同时，显著降低了通信复杂度。该协议在lp范数损失模型下，通过优化通信开销，实现了比以往研究更优的界限，为大规模分布式在线学习提供了更高效的理论方案。
selection_source: fresh_fetch
motivation: 旨在解决在分布式环境下，如何在最小化专家预测遗憾的同时，尽可能减少服务器间的通信开销。
method: 提出了一种新的分布式协议，针对lp范数定义的专家损失，优化了信息交换的频率和内容。
result: 在遗憾界R接近最优的情况下，将通信复杂度降低至与n/R^2和s/R^2相关且包含lp因子项的水平，优于前人工作。
conclusion: 该研究改进了分布式专家问题的通信与遗憾权衡界限，为分布式在线优化提供了更紧致的理论支撑。
---

## 摘要
在本文中，我们研究了分布式专家问题，其中 $n$ 个专家在 $T$ 个时间步内分布在 $s$ 台服务器上。每个专家在每个时间 $t$ 的损失是该专家在时间 $t$ 时于 $s$ 台服务器上各自损失所构成的向量的 $\ell_p$ 范数。目标是在使用最小通信量的情况下，最小化悔值（regret）$R$，即分布式协议的损失与最佳专家损失之差在所有 $T$ 个时间步上的摊销值。我们提出了一种协议，在实现大约 $R\gtrsim\frac{1}{\sqrt{T}\cdot\text{poly}\log(nsT)}$ 的悔值时，仅需 $\mathcal{O}\left(\frac{n}{R^2}+\frac{s}{R^2}\right)\cdot\max(s^{1-2/p},1)\cdot\text{poly}\log(nsT)$ 比特的通信量，这改进了以往的工作。

## Abstract
In this paper, we study the distributed experts problem, where $n$ experts are distributed across $s$ servers for $T$ timesteps. The loss of each expert at each time $t$ is the $\ell_p$ norm of the vector that consists of the losses of the expert at each of the $s$ servers at time $t$. The goal is to minimize the regret $R$, i.e., the loss of the distributed protocol compared to the loss of the best expert, amortized over the all $T$ times, while using the minimum amount of communication. We give a protocol that achieves regret roughly $R\gtrsim\frac{1}{\sqrt{T}\cdot\text{poly}\log(nsT)}$, using $\mathcal{O}\left(\frac{n}{R^2}+\frac{s}{R^2}\right)\cdot\max(s^{1-2/p},1)\cdot\text{poly}\log(nsT)$ bits of communication, which improves on previous work.