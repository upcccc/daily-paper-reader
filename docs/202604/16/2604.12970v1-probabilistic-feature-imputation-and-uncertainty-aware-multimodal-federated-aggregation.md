---
title: Probabilistic Feature Imputation and Uncertainty-Aware Multimodal Federated Aggregation
title_zh: 概率性特征插补与不确定性感知的多模态联邦聚合
authors: "Nafis Fuad Shahid, Maroof Ahmed, Md Akib Haider, Saidur Rahman Sagor, Aashnan Rahman, Md Azam Hossain"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12970v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 跨医疗机构的隐私保护协作模型训练
tldr: 本研究针对多模态联邦学习中临床站点模态缺失的问题，提出了概率特征补全网络（P-FIN）。该方法不仅能补全缺失模态特征，还能输出校准后的不确定性估计，并通过本地门控机制和全局Fed-UQ-Avg聚合策略来降低不可靠特征的影响。在胸部X射线分类实验中，该方法显著提升了模型在模态异构场景下的性能和可靠性。
selection_source: fresh_fetch
motivation: 现有的多模态联邦学习补全方法仅提供点估计而缺乏可靠性度量，在安全敏感的医疗应用中存在误导决策的风险。
method: 提出P-FIN网络进行概率特征补全，并结合本地S形门控机制与全局不确定性感知聚合策略（Fed-UQ-Avg）来优化特征融合与模型更新。
result: "在CheXpert、NIH Open-I和PadChest数据集上的实验表明，该方法相比确定性基准最高实现了5.36%的AUC提升。"
conclusion: 引入不确定性感知的补全与聚合机制能有效应对多模态联邦学习中的模态异构挑战，增强了医疗诊断模型的鲁棒性。
---

## 摘要
多模态联邦学习能够实现跨医疗机构的隐私保护协作模型训练。然而，模态异质性带来了一个根本性挑战：由于资源限制或工作流程差异，许多临床站点仅拥有部分模态。现有方法通过特征插补网络合成缺失的模态表示来解决这一问题，但这些方法产生的是没有可靠性度量的点估计，迫使下游分类器将所有插补特征视为同样可信。在安全至上的医疗应用中，这种局限性带来了重大风险。我们提出了概率特征插补网络（P-FIN），它在输出插补特征的同时提供校准后的不确定性估计。这种不确定性在两个层面得到利用：（1）在本地层面，通过 Sigmoid 门控在分类前衰减不可靠的特征维度；（2）在全局层面，通过 Fed-UQ-Avg 聚合策略，优先处理来自具有可靠插补结果的客户端的更新。在 CheXpert、NIH Open-I 和 PadChest 数据集上进行的联邦胸部 X 射线分类实验表明，该方法相比确定性基准模型有持续的改进，在最具挑战性的配置下 AUC 提升了 5.36%。

## Abstract
Multimodal federated learning enables privacy-preserving collaborative model training across healthcare institutions. However, a fundamental challenge arises from modality heterogeneity: many clinical sites possess only a subset of modalities due to resource constraints or workflow variations. Existing approaches address this through feature imputation networks that synthesize missing modality representations, yet these methods produce point estimates without reliability measures, forcing downstream classifiers to treat all imputed features as equally trustworthy. In safety-critical medical applications, this limitation poses significant risks. We propose the Probabilistic Feature Imputation Network (P-FIN), which outputs calibrated uncertainty estimates alongside imputed features. This uncertainty is leveraged at two levels: (1) locally, through sigmoid gating that attenuates unreliable feature dimensions before classification, and (2) globally, through Fed-UQ-Avg, an aggregation strategy that prioritizes updates from clients with reliable imputation. Experiments on federated chest X-ray classification using CheXpert, NIH Open-I, and PadChest demonstrate consistent improvements over deterministic baselines, with +5.36% AUC gain in the most challenging configuration.

---

## 论文详细总结（自动生成）

### 概率性特征插补与不确定性感知的多模态联邦聚合 (P-FIN & Fed-UQ-Avg) 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在医疗人工智能领域，结合影像、临床记录和报告的多模态模型性能优于单模态。然而，在**联邦学习（FL）**场景下，由于各医疗机构资源不均，普遍存在**模态异构性**（如大型中心有图有文，基层诊所仅有影像）。
*   **现有挑战**：目前的特征插补方法多为“确定性”的，即模型会强行生成一个补全特征（点估计），而不告知该生成的可靠性。在医疗安全敏感场景下，这种“盲目自信”的特征补全可能导致误诊。
*   **研究动机**：开发一种能够量化补全特征不确定性的框架，使模型在特征融合和全局聚合时能“学会怀疑”不可靠的生成数据。

#### 2. 论文提出的方法论
论文提出了一个双层不确定性感知框架，核心包括 **P-FIN 网络**和 **Fed-UQ-Avg 聚合算法**：
*   **P-FIN (Probabilistic Feature Imputation Network)**：
    *   **核心思想**：将缺失特征建模为异方差高斯分布 $p(z_T|z_I)$，输出均值 $\mu$（插补特征）和方差 $\sigma^2$（不确定性）。
    *   **技术细节**：采用 Transformer 编码器处理图像特征，通过双输出头分别预测 $\mu$ 和 $\sigma^2$。
    *   **损失函数**：使用 **$\beta$-NLL 损失**进行训练，通过停止梯度（Stop-gradient）机制防止模型通过预测无限大的方差来逃避学习，确保不确定性估计经过校准。
*   **本地不确定性感知融合**：
    *   **Sigmoid 门控**：利用 $g = \text{sigmoid}(-\log \sigma^2)$ 压制高不确定性的特征维度。
    *   **互注意力机制**：在图像特征与经过门控处理的插补特征之间进行双向注意力融合。
*   **全局聚合策略 (Fed-UQ-Avg)**：
    *   **双重加权**：聚合权重由“数据量权重”和“置信度权重”共同决定。置信度权重基于客户端插补特征的平均不确定性，通过温度缩放的指数函数计算。

#### 3. 实验设计
*   **数据集**：
    *   **CheXpert**：作为单模态数据源（仅影像）。
    *   **NIH Open-I**：作为多模态数据源（影像+报告对）。
    *   **PadChest**：用于外部验证，测试模型的泛化性。
*   **实验场景**：模拟 10 个客户端，设置三种单模态与多模态客户端比例（8:2, 6:4, 4:6），以体现不同的数据稀缺程度。
*   **对比方法 (Benchmarks)**：
    *   **Zero-filling / Uniform-filling**：简单的零填充或均值填充。
    *   **FIN + FedAvg**：最先进的确定性特征插补方法（基于 Transformer 解码器）。
    *   **P-FIN + FedAvg**：消融实验，仅使用概率插补但不使用不确定性聚合。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU 类型、数量）以及总训练时长。
*   **实现细节**：提到了使用 ResNet-50 作为视觉骨干，BERT-base 作为文本骨干，本地训练 4 个 Epoch，联邦通信 20 轮，Batch Size 为 32。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 种不同的模态分布配置。
    *   进行了 14 种病理类别的平均 AUC 评估。
    *   包含了**消融实验**，验证了 P-FIN 和 Fed-UQ-Avg 各自的增益。
    *   进行了**校准分析**（ECE 指标和可靠性图），证明了预测的不确定性与实际误差呈强正相关。
*   **充分性评价**：实验设计较为充分，不仅有性能提升的量化数据，还通过外部验证集和定性可视化（Case Study）展示了模型在模糊影像下的高不确定性表现，逻辑闭环完整。

#### 6. 主要结论与发现
*   **性能提升**：在最严苛的 8:2 比例下，该方法比确定性基准（FIN+FedAvg）的 AUC 提升了 **5.36%**。
*   **不确定性的价值**：单纯引入概率插补（P-FIN）已有提升，但结合不确定性感知聚合（Fed-UQ-Avg）能进一步带来约 2.4%~2.8% 的增益。
*   **校准有效性**：模型预测的高方差确实对应着高重建误差，证明了不确定性指标可以作为临床决策的可靠参考。

#### 7. 优点
*   **安全性高**：解决了医疗 AI 中“幻觉”特征被误信的问题，符合 FDA 对医疗设备不确定性量化的监管趋势。
*   **鲁棒性强**：通过 $\beta$-NLL 解决了概率网络常见的方差爆炸问题。
*   **即插即用**：Fed-UQ-Avg 策略对标准的 FedAvg 改进较小，易于集成到现有联邦框架中。

#### 8. 不足与局限
*   **单向插补**：目前仅实现了从图像到文本的单向补全，未探讨文本缺失（仅有报告无影像）的情况。
*   **不确定性类型**：主要关注数据导致的不确定性（Aleatoric），未深入探讨模型自身知识不足导致的不确定性（Epistemic）。
*   **算力透明度**：缺乏训练资源消耗的详细说明，难以评估其在资源受限的基层医疗机构的部署成本。

（完）
