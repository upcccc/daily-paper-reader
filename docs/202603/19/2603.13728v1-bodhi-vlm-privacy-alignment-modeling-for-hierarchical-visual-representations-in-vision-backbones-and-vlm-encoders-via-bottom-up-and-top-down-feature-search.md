---
title: "Bodhi VLM: Privacy-Alignment Modeling for Hierarchical Visual Representations in Vision Backbones and VLM Encoders via Bottom-Up and Top-Down Feature Search"
title_zh: Bodhi VLM：通过自底向上与自顶向下特征搜索实现视觉骨干网络与 VLM 编码器中层级视觉表示的隐私对齐建模
authors: "Bo Ma, Jinsong Wu, Wei Qi Yan"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13728v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 神经表示中的隐私对齐建模与噪声注入
tldr: 本研究针对视觉骨干网络和视觉语言模型（VLM）中隐私保护噪声与隐私预算对齐建模的难题，提出了Bodhi VLM框架。该框架通过自底向上和自顶向下的特征搜索策略定位敏感区域，并利用期望最大化隐私评估（EMPA）模块生成可解释的预算对齐信号。在多种目标检测器和VLM编码器上的实验证明，该方法能有效评估分层神经表示的隐私对齐程度，为隐私保护提供了可学习且可解释的建模视角。
selection_source: fresh_fetch
motivation: 现有的隐私保护系统在分层视觉表示中缺乏一种可解释且通用的方法来建模扰动与隐私预算之间的对齐关系。
method: 提出Bodhi VLM框架，利用聚类技术关联敏感概念，通过自底向上和自顶向下策略定位特征，并使用EMPA模块产生对齐信号。
result: 在多种目标检测器和VLM编码器上的实验验证了该框架的稳定性，且BUA/TDA策略在不同模型下表现出一致的偏差趋势。
conclusion: 该研究为隐私对齐的分层表示提供了一种可学习且可解释的建模视角，而非仅仅是事后审计工具。
---

## 摘要
保护隐私的学习系统通常在层级视觉表示中注入噪声；一个核心挑战是以一种可解释且适用于视觉骨干网络和视觉语言模型（VLM）的方式，对这些扰动如何与声明的隐私预算对齐进行“建模”。我们提出了 Bodhi VLM，一种针对“层级神经表示”的“隐私对齐建模”框架：它（1）通过基于 NCP 和 MDAV 的聚类将敏感概念与逐

## Abstract
Learning systems that preserve privacy often inject noise into hierarchical visual representations; a central challenge is to \emph{model} how such perturbations align with a declared privacy budget in a way that is interpretable and applicable across vision backbones and vision--language models (VLMs). We propose \emph{Bodhi VLM}, a \emph{privacy-alignment modeling} framework for \emph{hierarchical neural representations}: it (1) links sensitive concepts to layer-wise grouping via NCP and MDAV-based clustering; (2) locates sensitive feature regions using bottom-up (BUA) and top-down (TDA) strategies over multi-scale representations (e.g., feature pyramids or vision-encoder layers); and (3) uses an Expectation-Maximization Privacy Assessment (EMPA) module to produce an interpretable \emph{budget-alignment signal} by comparing the fitted sensitive-feature distribution to an evaluator-specified reference (e.g., Laplace or Gaussian with scale $c/ε$). The output is reference-relative and is \emph{not} a formal differential-privacy estimator. We formalize BUA/TDA over hierarchical feature structures and validate the framework on object detectors (YOLO, PPDPTS, DETR) and on the \emph{visual encoders} of VLMs (CLIP, LLaVA, BLIP). BUA and TDA yield comparable deviation trends; EMPA provides a stable alignment signal under the reported setups. We compare with generic discrepancy baselines (Chi-square, K-L, MMD) and with task-relevant baselines (MomentReg, NoiseMLE, Wass-1). Results are reported as mean$\pm$std over multiple seeds with confidence intervals in the supplementary materials. This work contributes a learnable, interpretable modeling perspective for privacy-aligned hierarchical representations rather than a post hoc audit only. Source code: \href{https://github.com/mabo1215/bodhi-vlm.git}{Bodhi-VLM GitHub repository}

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Bodhi VLM** 的框架，旨在解决隐私保护视觉模型中，层级神经表示（Hierarchical Neural Representations）与声明的隐私预算（Privacy Budget, $\epsilon$）之间如何对齐建模的问题。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在隐私保护的学习系统中，通常会向视觉特征（如检测器的特征金字塔或 VLM 的编码器层）注入噪声。目前的挑战在于，如何以一种**可解释且通用**的方式，建模这些层级扰动与声明的隐私预算 $\epsilon$ 之间的对齐关系。
*   **研究动机**：现有的隐私审计工具多为事后（post hoc）分析，缺乏一种能够深入神经架构内部、结合层级特征结构的建模视角。作者希望提出一种能够跨视觉骨干网络（Backbones）和视觉语言模型（VLM）使用的隐私对齐建模框架。

### 2. 论文提出的方法论：Bodhi VLM 框架
该框架由三个核心模块组成，不改变底层架构，仅作为特征层的分析工具：
*   **敏感特征定位（BUA & TDA）**：
    *   **自底向上策略 (BUA)**：从底层开始，利用 **MDAV**（微聚合）和 **NCP**（归一化确定性惩罚）聚类技术，逐层将特征划分为敏感组（$G^{(s)}$）和非敏感组（$G^{(n)}$）。
    *   **自顶向下策略 (TDA)**：从高层语义层开始向下传播分组信息，通过跨层链接细化低层的敏感区域定位。
*   **期望最大化隐私评估 (EMPA)**：
    *   **核心思想**：将观测到的敏感特征分布与一个“参考分布”（如基于 $\epsilon$ 的拉普拉斯或高斯分布）进行对比。
    *   **技术细节**：使用 **EM 算法** 拟合一个 K 分量的高斯混合模型（GMM）。通过计算拟合参数与参考参数之间的 $\ell_2$ 距离，生成**预算对齐信号（BAS）**。
*   **参考相对性**：该输出是相对于评估者指定的参考机制而言的，并非正式的差分隐私（DP）估计器。

### 3. 实验设计
*   **数据集/场景**：
    *   目标检测：MOT20（行人追踪）、COCO（通用物体检测）。
    *   合成数据：用于验证协议的可重复性。
*   **基准模型 (Benchmarks)**：
    *   目标检测器：YOLOv4, PPDPTS, DETR。
    *   VLM 视觉编码器：CLIP (ViT-B/32), LLaVA-1.5 (7B), BLIP (ResNet-50)。
*   **对比方法**：
    *   通用分布差异度量：卡方检验 (Chi-square)、K-L 散度、最大均值差异 (MMD)。
    *   任务相关基准：MomentReg（矩回归）、NoiseMLE（最大似然估计）、Wass-1（沃瑟斯坦距离）。
    *   VLM 隐私相关工作：ViP, DP-Cap, DP-MTV, VisShield（定性对比）。

### 4. 资源与算力
*   **算力说明**：文中提到使用了 **NVIDIA GPU** 和 PyTorch 框架进行实验。
*   **缺失信息**：论文未明确给出具体的 GPU 型号数量、具体的训练时长或推理开销。作者指出详细的硬件配置和代码可在其 GitHub 仓库中获取。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从传统的 CNN 骨干网络到最新的 ViT 架构。
    *   针对不同隐私预算（$\epsilon \in \{0.1, 0.01, 0.001\}$）进行了多组实验。
    *   包含完整的**消融实验**（如：w/o BUA, w/o TDA, w/o EMPA）。
*   **客观性与公平性**：实验采用了 **5 个随机种子** 并报告了“均值 ± 标准差”，在补充材料中提供了 95% 置信区间。通过合成数据验证了实验协议的可重复性，整体设计较为严谨、客观。

### 6. 论文的主要结论与发现
*   **策略一致性**：BUA 和 TDA 在不同模型下表现出相似的偏差趋势，其中 BUA 对噪声水平的变化略显敏感。
*   **EMPA 的稳定性**：EMPA 模块在多种检测器和 VLM 编码器上均能提供稳定的对齐信号，比 PSNR 或 SIFT 等传统指标更能准确反映隐私预算。
*   **跨架构适用性**：该框架成功应用于 CNN（如 ResNet）和 Transformer（如 ViT）架构，证明了其在层级表示建模上的通用性。

### 7. 优点：亮点与创新
*   **建模视角新颖**：将隐私评估从简单的黑盒审计提升到了对神经表示层级结构的建模。
*   **可解释性强**：通过 BUA/TDA 定位敏感特征，结合 EMPA 的分布拟合，使隐私噪声的分布变得直观可理解。
*   **架构无关性**：无需修改模型权重或结构，即可对现有的视觉骨干网络和 VLM 进行隐私对齐分析。

### 8. 不足与局限
*   **范围限制**：目前仅验证了 VLM 的**视觉塔（Vision Tower）**，尚未扩展到全模态融合层或语言解码路径。
*   **参考依赖性**：评估结果高度依赖于预设的参考机制（$M_{ref}$），如果实际注入噪声的机制与参考机制不匹配，结果可能存在偏差。
*   **非正式证明**：该框架提供的是经验性的对齐信号，不能替代正式的差分隐私数学证明或端到端的隐私认证。
*   **敏感集定义**：敏感概念（如“人”、“车”）仍需人工或规则预定义，缺乏完全自动化的敏感特征发现机制。

（完）
