Title: Secure Two-Party Matrix Multiplication from Lattices and Its Application to Encrypted Control

URL Source: https://arxiv.org/pdf/2603.22857v1

Published Time: Wed, 25 Mar 2026 00:51:01 GMT

Number of Pages: 6

Markdown Content:
# Secure Two-Party Matrix Multiplication from Lattices and Its Application to Encrypted Control 

Kaoru Teranishi 

Abstract — In this study, we propose a two-party computation protocol for approximate matrix multiplication of fixed-point numbers. The proposed protocol is provably secure under standard lattice-based cryptographic assumptions and enables matrix multiplication at a desired approximation level within a single round of communication. We demonstrate the feasibility of the protocol by applying it to the secure implementation of a linear control law. Our evaluation reveals that the client achieves lower online computational complexity compared to the original controller computation, while ensuring the privacy of controller inputs, outputs, and parameters. Furthermore, a numerical example confirms that the proposed method main-tains sufficient precision of control inputs even in the presence of approximation and quantization errors. 

I. I NTRODUCTION 

Encrypted control is a paradigm for ensuring the security and privacy of cyber-physical systems by leveraging crypto-graphic primitives [1], [2]. In this framework, control algo-rithms are executed directly on encrypted data. This enables a client owning a physical plant to outsource controller com-putation to untrusted computing resources without disclosing sensitive information, such as sensor measurements, control actions, and controller parameters. A primary architecture for encrypted control is the single-server setting using homomorphic encryption [2]. Despite significant theoretical advances, in such a setting, the compu-tational burden on the client often exceeds that of the original unencrypted computation, even when implementing simple state-feedback controllers. This issue stems from the intrinsic computational overhead of the encryption and decryption processes in homomorphic encryption. To overcome the limitation of the single-server setting, several studies have explored encrypted control in multi-party architectures. These frameworks usually employ secret sharing to protect sensitive information. For example, linear control laws were initially implemented in a two-party setting using secret sharing [3]. This approach was subsequently extended to polynomial control laws by integrating homo-morphic encryption [4]. The polynomial control was also realized using only secret sharing in settings with more than three parties [5]. Recent advancements have also enabled the implementation of neural-network-based controllers [6] and dynamic controllers [7] in two-party settings. A fundamental challenge in encrypted control using secret sharing is the secure evaluation of multiplications between                 

> This work was not supported by any organization. K. Teranishi is with the Department of Information and Physical Sci-ences, Graduate School of Information Science and Technology, The University of Osaka, 1-5 Yamadaoka, Suita, Osaka 565-0871, Japan

k-teranishi@ist.osaka-u.ac.jp .

two secret-shared values. The initial implementation by-passed this issue by assuming that one of the values was pub-lic [4]. Furthermore, a naive interactive protocol leveraging homomorphic encryption typically requires multiple rounds of communication between the computing parties [4]. While replicated secret sharing [8], as employed in [5], offers an ef-ficient alternative, it necessitates at least three non-colluding parties, thereby increasing the architectural complexity and deployment costs. Another common strategy is the use of Beaver triples [9], as seen in [6], [7]. However, their online generation again requires multi-round communication. Even if these triples are generated and provided by the client to the computing parties [7], this approach inherently increases the computational complexity and communication overhead of the client. Consequently, simultaneously achieving low client-side complexity in a two-party setting with a single-round communication remains a significant challenge. To address these challenges, this paper constructs a one-round two-party computation protocol for approximate ma-trix multiplication. Our approach integrates additive secret sharing with lattice-based cryptographic primitives, specif-ically encryption and commitment schemes. By exploiting the linearity of these lattice-based primitives, the proposed protocol enables the computing parties to securely evaluate approximate matrix multiplication within a single round of communication. This approach eliminates the need for multiple interactions between the parties. Moreover, the framework ensures that the online tasks of the client are restricted to the generation and reconstruction of shares, resulting in an online computational complexity lower than that of the original controller computation. The main contributions of this study are summarized as follows.  

> •

We propose a one-round two-party computation proto-col for the approximate matrix multiplication of fixed-point numbers. The security of this protocol is guaran-teed under the Learning With Errors and Short Integer Solution assumptions, which are standard assumptions in lattice-based cryptography.  

> •

We provide a theoretical analysis of the approximation errors inherent in the proposed protocol. Sufficient conditions for the precision of fixed-point numbers are derived to guarantee that the approximation error remains below a specified level.  

> •

By applying the protocol to the implementation of linear control laws, we demonstrate that the proposed frame-work simultaneously achieves low online computational complexity for the client, one-round communication 

> arXiv:2603.22857v1 [eess.SY] 24 Mar 2026

between the two parties, and the concealment of both signals and feedback gains. The remainder of this paper is organized as follows. Section II introduces a secret sharing scheme. Section III reviews the Learning With Errors and Short Integer Solution problems. Section IV describes the proposed protocol and its application to the encrypted implementation of linear control laws. Finally, Section VI concludes the paper. II. P RELIMINARIES 

A. Notations 

Let Z and R denote the sets of integers and real numbers, respectively. For an integer q ≥ 2, Zq := Z ∩ [−q/ 2, q/ 2) 

denotes the set of integers within [−q/ 2, q/ 2) . For any x ∈

Z, the reduction of x modulo q is given by x mod q := 

x − ⌊ x+q/ 2 

> q

⌋q, where ⌊·⌋ denotes the floor function. This modular reduction is applied for each element of vectors and matrices. The maximum norm of a vector or matrix is denoted by ∥·∥ max . The notation x ←$ X indicates that x is sampled either uniformly at random from a finite set X or according to a specified distribution X.

B. Secret sharing 

A secret sharing scheme distributes a secret among mul-tiple parties by splitting it into shares such that the original secret can be reconstructed from a subset of those shares. In this study, we employ a standard secret sharing scheme defined over a matrix message space, configured for a two-party setting. 

Definition 1 (Secret sharing): A secret sharing scheme consists of polynomial-time algorithms Share and Reconst : 

> •

JXK ← Share (X): The share generation algorithm takes a matrix X over Zq as input and outputs shares 

JXK = ( JXK0, JXK1) = ( R, X − R mod q), where R

is a random matrix over Zq of appropriate dimensions.  

> •

X ← Reconst (JXK): The reconstruction algorithm takes shares JXK = ( JXK0, JXK1) as input and outputs 

X = JXK0 + JXK1 mod q.The secret sharing scheme satisfies Reconst (Share (X)) = 

X for any matrix X over Zq . Moreover, each share JXKi

held by party Pi is uniformly random and thus leaks no information about X to that party. We respectively define the addition/subtraction and constant multiplication of shares by 

JXK±JY K := ( JXK0±JY K0 mod q, JXK1±JY K1 mod q) and 

XJY K := ( XJY K0 mod q, X JY K1 mod q), where X and Y

are matrices over Zq of compatible dimensions. It then holds that 

JXK ± JY K = JX ± Y mod qK, (1) 

XJY K = JXY mod qK. (2) We also define JXKY as similar to XJY K, and the transpose of shares as JXK⊤ := ( JXK⊤ 

> 0

, JXK⊤ 

> 1

). Since these operations can be performed locally by each party without any interac-tion, no information about the secrets X and Y is revealed throughout the computations. III. L ATTICE PROBLEMS 

Learning With Errors (LWE) [10] and Short Integer Solu-tion (SIS) [11] are standard cryptographic problems defined over lattices. These problems serve as the foundation for lattice-based cryptography and are applied to a wide range of applications, including encryption and commitment schemes. This section provides a brief overview of the definitions and applications of the LWE and SIS problems. 

A. Learning with errors 

We begin by defining the LWE problem. This problem requires distinguishing a structured distribution over a lattice from a uniform distribution. 

Definition 2 (LWE): Let n, m, and q be positive integers, and let χd be a distribution defined over Zd. The (decision) LWE problem is to distinguish between the two distribu-tions (A, A ⊤s + e mod q) and (A, w ), where A ←$ Zn×mq ,

s ←$ χn, e ←$ χm, and w ←$ Zmq . The LWE assumption states that no polynomial-time algorithm can distinguish these two distributions except with negligible probability. In our framework, we employ discrete Gaussian distri-butions for the secret and error distributions. A discrete Gaussian distribution χdσ over Zd with a width parameter 

σ > 0 is defined by Pr[ X = x] = ρσ (x)/ P 

> y∈Zd

ρσ (y) for each x ∈ Zd, where ρσ (x) = exp( −π∥x∥2/σ 2). In lattice-based cryptography, these distributions are typically treated as bounded distribution. This property is formally captured by the following assumption. 

Assumption 1: The discrete Gaussian distribution χdσ is 

B-bounded, i.e., for any sample x = [ x1 . . . x d]⊤ ←$ χdσ ,the condition |xi| < B holds for some B > 0 except with negligible probability. For the width σ = 3.2 commonly adopted in the lit-erature [12], the bound B = 10 σ is sufficient to satisfy the negligible probability requirement in practice. Given 

χn = χnσ and χm = χmσ , the hardness of the LWE problem primarily depends on the dimension n, the modulus q, and the width parameter σ. In general, the problem becomes more difficult as n or the ratio σ/q increases. Because the number of samples m provided to a challenger does not affect the underlying hardness, we refer to (n, q, σ ) as the LWE parameters. The LWE problem is of significant interest because it pro-vides a template for encryption schemes [10]. For instance, a message μ ∈ Zmq can be encrypted using a secret key s as 

c = A⊤s + μ + e mod q. (3) The LWE assumption implies that A⊤s + e mod q is com-putationally indistinguishable from a uniform distribution, meaning that a polynomial-time adversary learns nothing about μ from c. Furthermore, the encrypted message can be decrypted by 

A⊤ c −s

1



= μ + e mod q ≈ μ, 

which allows for the approximate recovery of the original message. B. Short integer solution 

We next introduce the SIS problem. This problem requires finding a short nonzero vector that satisfies a linear equation over a lattice, given a random coefficient matrix and a random bias. 

Definition 3 (SIS): Let n, t, and q be positive integers, and let β > 0 be a real number. The (inhomogeneous) SIS problem is to find a nonzero vector r ∈ Zt such that 

h = Ar mod q with ∥r∥ ≤ β for some norm ∥·∥ , given 

A ←$ Zn×tq and h ←$ Znq . The SIS assumption states that no polynomial-time algorithm can find such a short vector except with negligible probability. The hardness of the SIS problem depends on the dimen-sions n and t, the modulus q, and the norm bound β. For our purposes, n and q are chosen to be consistent with the LWE parameters, while β is determined by protocol construction. Consequently, we refer to t as the SIS parameter. The SIS problem can be employed for constructing com-mitment schemes. A commitment scheme emulates the func-tionality of a locked box. In the commitment phase, a sender places a message into a box, locks it, and sends the locked box to a receiver. Later, in the opening phase, the sender provides the original message and the key, allowing the receiver to verify the content. The receiver cannot read the message before the opening phase since the box is locked, and the sender cannot alter the message once the box has been sent. In cryptography, these two properties are referred to as hiding and binding, respectively. Following this intuition, we compute a commitment to a message μ ∈ Zmq as 

h = Aμ + Br mod q, (4) where A ←$ Zn×mq and B ←$ Zn×tq are random matrices, and r ∈ Zt is a short random vector [11]. By the Leftover Hash Lemma [13], the term Br is statistically close to a uniform random vector, thereby ensuring the hiding property. Moreover, the SIS assumption implies that no polynomial-time adversary can find a distinct pair (μ′, r ′) such that 

h = Aμ ′ + Br ′ mod q = A B  μ′

r′



mod q

with μ′̸ = μ, thereby satisfying the binding property. IV. T WO -P ARTY MATRIX MULTIPLICATION 

This section proposes a two-party matrix multiplication protocol. The protocol is built based on an approximate inner-product computation from the LWE and SIS problems. We first revisit this underlying computation and then present the detailed construction of the proposed protocol. Further-more, we derive the conditions for the bit lengths of input data to guarantee the desired precision of the results. Finally, we provide a security analysis of the protocol. 

A. Approximate inner product from LWE and SIS 

The approximate inner-product computation using LWE ciphertexts and SIS commitments is introduced in [14], [15]. Let (n, q, σ ) and t be the LWE and SIS parameters, respectively. Suppose we have LWE ciphertexts (3) of a vector x ∈ Zmq and the zero vector, and an SIS commitment (4) to a vector y ∈ Zmq , that is, 

c = A⊤s + x + e mod q, c′ = B⊤s + e′ mod q, h = Ay + Br mod q, 

where A ←$ Zn×mq and B ←$ Zn×tq are public random ma-trices, s ←$ χnσ is a secret key, e ←$ χmσ and e′ ←$ χtσ are error vectors, and r ∈ Zt is a short random vector. We now consider the inner products c⊤y = x⊤y + s⊤Ay + e⊤y,

(c′)⊤r = s⊤Br + ( e′)⊤r, and s⊤h = s⊤Ay + s⊤Br .By subtracting the third from the sum of first two, we can observe that the terms s⊤Ay and s⊤Br are eliminated. This yields an approximate inner product of x and y as 

c⊤y + ( c′)⊤r − s⊤h mod q = x⊤y + ( noise ) mod q, 

≈ x⊤y mod q, (5) where the term e⊤y + ( e′)⊤r constitutes the noise. The previous studies [14], [15] have utilized the property (5) to compute shares of an inner product in the scenarios where the two parties respectively hold x and y. By contrast, our focus is on computing the inner product while concealing 

x and y from the parties, ensuring that only the client knows these values. The core observation to achieve this is that the left-hand side of (5) is linear in y, r, and s, which are the secret components of the LWE ciphertext and the SIS commitment. Therefore, by leveraging the properties (1) and (2) of the secret sharing scheme, the approximate inner product can be computed over the shares of the secrets. This approach suggests a method for outsourcing the inner-product computation to two untrusted parties. Building on this idea, the next section constructs a multiplication protocol tailored for matrix messages of fixed-point numbers. 

B. Proposed protocol 

The approximate inner product (5) can be extended to the multiplication of d1 × d2 and d2 × d3 matrices. Let 

xi, y j ∈ Zd2 

> q

for i = 1 , . . . , d 1 and j = 1 , . . . , d 3. For each vector, we compute the LWE ciphertexts (3) and the SIS commitments (4) as ci = A⊤si + xi + ei mod q,

c′ 

> i

= B⊤si + e′ 

> i

mod q, and hj = Ay j + Br j mod q, where 

A ←$ Zn×d2 

> q

, B ←$ Zn×tq , si ←$ χnσ , ei ←$ χd2 

> σ

, e′ 

> i

←$ χtσ ,and rj ∈ Zt. By arranging these vectors as columns, we obtain the following matrix representations, 

C = c1 · · · cd1

 = A⊤S + X⊤ + E mod q, C′ = c′ 

> 1

· · · c′

> d1

 = B⊤S + E′ mod q, H = h1 · · · hd3

 = AY + BR mod q, 

where the uppercase letters denote the horizontal concatena-tion of their lowercase counterparts, e.g., S = [ s1 · · · sd1 ].Note that X⊤ = [ x1 · · · xd1 ] ∈ Zd2×d1 

> q

appears in a trans-posed form within C to ensure dimensional compatibility. As a result, these matrix forms satisfy the approximate relation 

C⊤Y + ( C′)⊤R − S⊤H mod q ≈ XY mod q (6) analogous to the vector case in (5). Although the approximate matrix multiplication (6) is defined over Zq , practical applications, including control systems, require computations to be performed over real-valued matrices. To bridge this gap, we introduce a fixed-point number representation. Let Q⟨k,ℓ ⟩ := {2−ℓz | z ∈

Z⟨k⟩} denote the set of k-bit fixed-point numbers with an 

ℓ-bit fractional part, where Z⟨k⟩ := {− 2k−1, . . . , 2k−1 − 1}

is the set of k-bit integers. A matrix X ∈ Qd1×d2 

> ⟨k,ℓ ⟩

is encoded into an integer matrix ¯X ∈ Zd1×d2 

> ⟨k⟩

by scaling with 2ℓ, i.e., 

¯X = 2 ℓX. By replacing X and Y in (6) with their encoded values ¯X = 2 ℓX and ¯Y = 2 ℓY , we obtain an approximate matrix multiplication framework over fixed-point numbers. Consequently, we propose a two-party matrix multiplica-tion protocol in Fig. 1. The proposed Mult protocol consists of three subroutines: Setup , Offline , and Online . The Setup 

and Offline procedures are performed once during an offline phase, whereas the Online procedure can be executed repeat-edly for various inputs during an online phase. In the offline phase, a client invokes Mult .Setup to gen-erate random matrices A ←$ Zn×d2 

> q

and B ←$ Zn×tq , and publishes a common reference string crs = ( A, B ) to the parties. The client then invokes Mult .Offline with crs and the input matrix X ∈ Qd1×d2 

> ⟨k,ℓ ⟩

. During this procedure, the client computes the LWE ciphertexts C = A⊤S + ¯X⊤ + E mod q

and C′ = B⊤S + E′ mod q, where ¯X = 2 ℓX ∈ Zd1×d2 

> ⟨k⟩

,

S ←$ χn×d1 

> σ

, E ←$ χd2×d1 

> σ

, and E′ ←$ χt×d1 

> σ

. Finally, it also generates the shares JSK of the secret matrix S and distributes (C, C ′, JSKi) to the party Pi.In the online phase, the client and the two parties invoke 

Mult .Online . The client first encodes the input matrix Y ∈

Qd2×d3 

> ⟨k,ℓ ⟩

into ¯Y = 2 ℓY ∈ Zd2×d3 

> ⟨k⟩

. It then generates the shares J ¯Y K and sends J ¯Y Ki to the party Pi. Each party 

Pi locally samples JRKi ←$ Zt×d3 

> 3

to constitute the random shares JRK = ( JRK0, JRK1). The parties then locally compute 

JHK = AJ ¯Y K + BJRK using the properties (1) and (2). Subsequently, they open the shares to reconstruct H =

Reconst (JHK). Using the LWE ciphertexts C, C ′ and the SIS commitment H, the parties compute J ¯ZK = C⊤J ¯Y K +(C′)⊤JRK − JSK⊤H locally and returns the resulting shares to the client. Upon receiving these shares, the client com-putes Z = 2 −2ℓReconst (J ¯ZK) as the result of the matrix multiplication. We now establish the approximate correctness of the proposed protocol. The correctness relies on the noise term inherent in the computation of (6). To ensure that the protocol output Z accurately approximates the desired matrix product 

XY , we need to show that the reconstructed value ¯Z does not suffer from modular wrap-around (i.e., overflow) and that the magnitude of the noise term remains sufficiently small. The following lemma guarantees that such overflow is avoided when the bit length of the inputs is bounded according to the cryptographic parameters and matrix dimensions. 

Lemma 1: Consider the Mult protocol in Fig. 1. Under 

Mult .Setup () : 

> 1:

A ←$ Zn×d2 

> q

, B ←$ Zn×tq 

> 2:

Output crs ← (A, B )

Mult .Offline (crs , X ): 

> 1:

¯X ← 2ℓX 

> 2:

S ←$ χn×d1 

> σ

, E ←$ χd2×d1 

> σ

, E′ ←$ χt×d1 

> σ
> 3:

C ← A⊤S + ¯X⊤ + E mod q 

> 4:

C′ ← B⊤S + E′ mod q 

> 5:

JSK ← Share (S) 

> 6:

Output (C, C ′, JSK)

Mult .Online (crs , C, C ′, JSK, Y ): 

> 1:

¯Y ← 2ℓY 

> 2:

J ¯Y K ← Share ( ¯Y ) 

> 3:

JRK0 ←$ Zt×d3 

> 3

, JRK1 ←$ Zt×d3 

> 3
> 4:

JRK ← (JRK0, JRK1) 

> 5:

JHK ← AJ ¯Y K + BJRK 

> 6:

H ← Reconst (JHK) 

> 7:

J ¯ZK ← C⊤J ¯Y K + ( C′)⊤JRK − JSK⊤H 

> 8:

Output Z ← 2−2ℓReconst (J ¯ZK)          

> Fig. 1. Two-party matrix multiplication protocol Mult consisting of three subroutines: Setup ,Offline , and Online . Public parameters include the bit lengths (k, ℓ ), matrix dimensions (d1, d 2, d 3), LWE parameters (n, q, σ ),and the SIS parameter t.

Assumption 1 with σ = 3 .2 and B = 10 σ, if 

6 ≤ k < 12 log 2

q − 128 td2

, (7) then it holds that 

¯Z = ¯X ¯Y + E ¯Y + E′R

for all X ∈ Qd1×d2 

> ⟨k,ℓ ⟩

and Y ∈ Qd2×d3 

> ⟨k,ℓ ⟩

, where ¯Z =

Reconst (J ¯ZK) and R = Reconst (JRK).

Proof: The properties (1) and (2) of the secret sharing scheme imply that 

J ¯ZK = JC⊤ ¯Y + ( C′)⊤R − S⊤H mod qK,

= J ¯X ¯Y + E⊤ ¯Y + ( E′)⊤R mod qK.

Let ˆZ = ¯X ¯Y + E⊤ ¯Y + ( E′)⊤R. If ˆZ belongs to Zd1×d3 

> q

,its reduction modulo q satisfies ˆZ mod q = ˆZ. In this case, the reconstruction implies ¯Z = Reconst (J ¯ZK) =

Reconst (J ˆZK) = ˆZ.The maximum norm of ˆZ is bounded by 

ˆZ max ≤ ¯X ¯Y max + E⊤ ¯Y max + (E′)⊤R max ,

≤ 22( k−1) d2 + 2 k−1Bd 2 + 2 Bt. 

For k ≥ 6, we have 

22( k−1) d2 + 2 k−1Bd 2 + 2 Bt ≤ 22k−1d2 + 2 6t, 

where 2k−1 ≥ B = 2 5. Hence, the condition ˆZ ∈ Zd1×d3

> q

holds if 22k−1d2 +2 6t < q/ 2. Multiplying by two and taking the logarithm, we obtain 2k+log 2 d2 < log 2(q−128 t), which implies (7). While Lemma 1 establishes an upper bound on the data size to prevent modular wrap-around, the following theorem provides a lower bound on the fractional bit length required to achieve a desired level of precision. 

Theorem 1: Consider the Mult protocol in Fig. 1. Suppose the condition (7) is satisfied. Under Assumption 1 with σ =3.2 and B = 10 σ, for every ϵ > 0, if 

ℓ > 12



k + 4 + log 2

d2 + tϵ



, (8) then it holds that 

∥XY − Z∥max < ϵ 

for all X ∈ Qd1×d2 

> ⟨k,ℓ ⟩

and Y ∈ Qd2×d3 

> ⟨k,ℓ ⟩

.

Proof: Since the condition (7) is satisfied, Lemma 1 guarantees that Z = XY + 2 −2ℓ E⊤ ¯Y + ( E′)⊤R. Thus, the maximum norm of the difference is bounded by 

∥XY − Z∥max ≤ 2−2ℓ E⊤ ¯Y + ( E′)⊤R max ,

≤ 2−2ℓ 2k−1Bd 2 + 2 Bt ,

≤ 2k−2ℓ+4 (d2 + t),

where 2k−1 ≥ B and B = 2 5 are applied. This implies that 

∥XY − Z∥max < ϵ holds if 2k−2ℓ+4 (d2 + t) < ϵ . Taking the logarithm of both sides, we obtain (8). This completes the proof. Note that the fractional bit length ℓ is implicitly con-strained by (7), as the total bit length k must exceed ℓ.This inherent trade-off between k and ℓ defines a feasible parameter window that simultaneously avoids overflow and attains the target precision. We later demonstrate that such a parameter window indeed exists for standard LWE and SIS parameters. 

C. Security Analysis 

We next proceed to analyze the security of the pro-posed protocol. In this section, the computing parties are assumed to be semi-honest and non-colluding. Under this model, the parties follow the protocol specifications but may individually attempt to infer private information from transcripts generated during the protocol execution. Due to space limitations, we provide a high-level summary of the security analysis here. The security of the protocol is formally established via the simulation paradigm [16], demonstrating that no polynomial-time adversary can learn any information beyond its own inputs and outputs. 

Theorem 2: Under the LWE and SIS assumptions, the computing parties P0 and P1 learn nothing about the inputs 

X and Y and the output Z throughout the execution of the 

Mult protocol in Fig. 1. 

Sketch of proof: The privacy of the input matrices X and 

Y is guaranteed by the LWE and SIS assumptions. Similar to the vector case discussed in Section III, under these assumptions, the LWE ciphertexts C and C′ are compu-tationally indistinguishable from uniform random matrices. Furthermore, the SIS commitment H is not only unforgeable but also statistically close to a uniform random matrix due to the Leftover Hash Lemma. Moreover, by the construction of the secret sharing scheme, the shares JSKi and J ¯Y Ki are distributed uniformly at random for each party Pi. Conse-quently, every message that Pi receives during the protocol execution appears uniformly random and independent of the input matrices, thereby ensuring that the inputs and the output remain concealed throughout the entire computation. V. A PPLICATION TO ENCRYPTED CONTROL 

In this section, we present an application of the proposed protocol to encrypted control. To this end, consider a linear control law of the form 

u(τ ) = Kx (τ ) + v(τ ), (9) where τ = 0 , 1, 2, . . . is the time index, u(τ ) ∈ Rnu is the control input, x(τ ) ∈ Rnx is the system state, v(τ ) ∈ Rnu is the reference input, and K ∈ Rnu×nx is the feedback gain. We assume a scenario where a client wishes to outsource the computation of (9) to two untrusted parties while concealing 

u(τ ), x(τ ), v(τ ), and K from them. In this setting, the feedback gain K is designed by the client, and the reference input v(τ ) is provided by an external operator, while the state 

x(τ ) originates from a system owned by the client. Let 

˜u(τ ) := ˜K ˜x(τ ) + ˜ v(τ ) (10) be a quantized control input, where ˜K ∈ Qnu×nx 

> ⟨k,ℓ ⟩

, ˜x(τ ) ∈

Qnx

> ⟨k,ℓ ⟩

, and ˜v(τ ) ∈ Qnu 

> ⟨k,ℓ ⟩

are the fixed-point number representations of K, x(τ ), and v(τ ), respectively. These representations can be obtained using a typical mid-tread quantizer with a step size of 2−ℓ and a saturation thresh-old of 2k−1. To apply the Mult protocol to the controller computation (10), we assign the quantized feedback gain 

˜K and the quantized state ˜x(τ ) to the inputs X and Y

in Fig. 1, respectively. Additionally, we slightly modify the 

Mult .Online subroutine to incorporate the reference input. The modified subroutine takes an additional input J¯v(τ )K

and computes J ¯ZK = C⊤J¯xK+( C′)⊤JRK−JSK⊤H +J¯v(τ )K,where ¯x(τ ) = 2 ℓ ˜x(τ ), C is the LWE ciphertext of ¯K = 2 ℓ ˜K,and J¯v(τ )K denotes the shares of ¯v(τ ) = 2 2ℓ ˜v(τ ) provided to the parties by the operator. Fig. 2 depicts the signal flow of the modified Mult .Online 

subroutine. At each time step τ , the client and the operator simultaneously send the shares J¯x(τ )Ki and J¯v(τ )Ki to each party Pi, respectively. The parties compute the shares J ¯ZK

according to the modified protocol and return them to the client. The client then recovers an approximate control input 

ˆu(τ ) := 2 −2ℓReconst (J ¯ZK) (11) from the shares. 

Remark 1: In the online phase, the client only needs to generate J¯x(τ )K and reconstruct the control input from J ¯ZK.These operations incur O(nx) and O(nu) computational complexities, respectively, resulting in a total online cost of 

O(nx+nu). This complexity is lower than O(nunx) required for the direct evaluation of (9). Furthermore, the computing Client 

P0

P1

Operator JHK0JHK1

J¯xK0 J¯xK1J ¯ZK0J ¯ZK1

J¯vK0

J¯vK1Fig. 2. Signal flow of the modified Mult .Online subroutine. The time index τ is omitted for clarity. 0 10 20 30 40 50 

¿

0.0 

0.5 

1.0 

> Error

£10 −12 

Fig. 3. Control input errors ∥˜u(τ ) − ˆu(τ )∥max (blue solid line) and 

∥u(τ ) − ˆu(τ )∥max (red dashed line). Here, u(τ ), ˜u(τ ), and ˆu(τ ) denote the control inputs computed by (9), (10), and (11), respectively. 

parties require only a single round of communication to ex-change JHK, which is the minimum for interactive protocols. To the best of our knowledge, this is the first realization of encrypted control that simultaneously achieves reduced online complexity for the client and confidentiality of both signals and gains in one-round two-party computation. To provide a proof of concept, we consider a linear time-invariant system 

x(τ + 1) = 

1.1 −0.50 0.1



x(τ ) + 

 00.5



u(τ ), x (0) = 

 1

−1



controlled by (9) with the feedback gain K = [3 .84 − 2.4] .The LWE parameters are set to (n, q, σ ) = (2 12 , 2108 , 3.2) ,which ensures 128 bit security [12]. Furthermore, a standard SIS parameter t = 2 n log 2 q is employed to satisfy the hiding property [11]. For these parameters, Lemma 1 and Theorem 1 guarantee that if the bit lengths are chosen such that 43 .7 <ℓ < k < 53 .5, the approximation error ∥˜u(τ ) − ˆu(τ )∥ max 

remains below ϵ = 2 −10 when v(τ ) = 0 . Accordingly, we set k = 53 and ℓ = 44 .Fig. 3 shows the control input errors for τ = 0 , . . . , 50 

with v(τ ) = 10 sin(0 .2πτ ). The blue solid line depicts 

∥˜u(τ )− ˆu(τ )∥max , while the red dashed line depicts ∥u(τ )−

ˆu(τ )∥max , where u(τ ), ˜u(τ ), and ˆu(τ ) are respectively computed by (9), (10), and (11). The behavior of the blue solid line supports Theorem 1 because the error remains below the theoretical bound ϵ. Furthermore, the red dashed line indicates that the encrypted control preserves the original control performance, as the error remains sufficiently small despite including quantization errors. VI. C ONCLUSION 

We proposed a one-round two-party computation protocol for the approximate matrix multiplication of fixed-point numbers based on the LWE and SIS problems. We also established sufficient conditions for fixed-point number pre-cision to guarantee the desired approximation error bound. When applied to linear control laws, the protocol achieved lower online computational complexity for the client than the original computation, thereby mitigating the client burden of encrypted controls. Future work will extend this framework to dynamic controllers and optimize the computational costs for the computing parties. ACKNOWLEDGEMENTS 

The author used Gemini 3 [17] throughout the manuscript to improve its grammar and clarity. REFERENCES [1] K. Kogiso and T. Fujita, “Cyber-security enhancement of networked control systems using homomorphic encryption,” in IEEE Conference on Decision and Control , 2015, pp. 6836–6843. [2] M. S. Darup, A. B. Alexandru, D. E. Quevedo, and G. J. Pappas, “Encrypted control for networked systems: An illustrative introduction and current challenges,” IEEE Control Systems Magazine , vol. 41, no. 3, pp. 58–78, 2021. [3] M. S. Darup and T. Jager, “Encrypted cloud-based control using secret sharing with one-time pads,” in IEEE Conference on Decision and Control , 2019, pp. 7215–7221. [4] M. S. Darup, “Encrypted polynomial control based on tailored two-party computation,” International Journal of Robust and Nonlinear Control , vol. 30, no. 11, pp. 4168–4187, 2020. [5] S. Schlor, M. Hertneck, S. Wildhagen, and F. Allg¨ ower, “Multi-party computation enables secure polynomial control based solely on secret-sharing,” in IEEE Conference on Decision and Control , 2021, pp. 4882–4887. [6] K. Tjell, N. Schl¨ uter, P. Binfet, and M. S. Darup, “Secure learning-based MPC via garbled circuit,” in IEEE Conference on Decision and Control , 2021, pp. 4907–4914. [7] K. Teranishi and T. Tanaka, “Client-aided secure two-party computa-tion of dynamic controllers,” IEEE Transactions on Control of Network Systems , vol. 12, no. 4, pp. 2967–2979, 2025. [8] T. Araki, J. Furukawa, Y. Lindell, A. Nof, and K. Ohara, “High-throughput semi-honest secure three-party computation with an honest majority,” in ACM SIGSAC Conference on Computer and Communi-cations Security , 2016, pp. 805–817. [9] D. Beaver, “Efficient multiparty protocols using circuit randomiza-tion,” in Annual International Cryptology Conference on Advances in Cryptology , 1991, pp. 420–432. [10] O. Regev, “On lattices, learning with errors, random linear codes, and cryptography,” Journal of the ACM , vol. 56, no. 6, pp. 1–40, 2009. [11] M. Ajtai, “Generating hard instances of lattice problems,” in Annual ACM Symposium on Theory of Computing , 1996, pp. 99–108. [12] J.-P. Bossuat, R. Cammarota, I. Chillotti, B. R. Curtis, W. Dai, H. Gong, E. Hales, D. Kim, B. Kumara, C. Lee, X. Lu, C. Maple, A. Pedrouzo-Ulloa, R. Player, Y. Polyakov, L. A. R. Lopez, Y. Song, and D. Yhee, “Security guidelines for implementing homomorphic encryption,” Cryptology ePrint Archive , 2024. [13] R. Impagliazzo, L. A. Levin, and M. Luby, “Pseudo-random generation from one-way functions,” in Annual ACM Symposium on Theory of Computing , 1989, pp. 12–24. [14] D. Abram, L. Roy, and P. Scholl, “Succinct homomorphic secret sharing,” in Annual International Conference on the Theory and Applications of Cryptographic Techniques , 2024, pp. 301–330. [15] E. Boyle, L. Devadas, and S. Servan-Schreiber, “Non-interactive distributed point functions,” in IACR International Conference on Practice and Theory of Public-Key Cryptography , 2025, pp. 3–35. [16] O. Goldreich, Foundations of cryptography: Basic applications . Cam-bridge University Press, 2009. [17] Google, “Gemini 3,” Accessed: 2026-03-09. [Online]. Available: https://gemini.google.com/