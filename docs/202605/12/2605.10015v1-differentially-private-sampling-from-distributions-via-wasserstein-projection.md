---
title: Differentially Private Sampling from Distributions via Wasserstein Projection
title_zh: 基于 Wasserstein 投影的分布差分隐私采样
authors: "Shokichi Takakura, Seng Pei Liew, Satoshi Hasegawa"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10015v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 通过 Wasserstein 投影的差分隐私采样机制
tldr: 本研究针对差分隐私（DP）下的分布采样问题，指出传统基于KL散度的方法无法捕捉几何结构且受限于支撑集一致性。为此，论文提出了一种以Wasserstein距离为效用度量的新框架，并设计了Wasserstein投影机制（WPM）。该机制在理论上达到了极小极大最优性，同时作者开发了高效的近似计算算法并提供了收敛性保证，为隐私保护下的数据采样提供了更稳健的几何视角。
selection_source: fresh_fetch
motivation: 传统的差分隐私采样度量（如KL散度）忽略了空间的几何结构，且在分布支撑集不一致时无法适用。
method: 提出了一种基于Wasserstein投影的差分隐私采样机制（WPM），利用Wasserstein距离作为效用衡量标准。
result: 证明了WPM机制具有极小极大最优性，并开发了具备收敛保证的高效近似计算算法。
conclusion: 该研究为差分隐私采样提供了一个全新的几何框架，有效解决了传统方法在几何感知和支撑集限制方面的不足。
---

## 摘要
本文研究了在差分隐私（DP）约束下从分布中采样的问题。先前的研究使用基于密度比的度量（如 KL 散度）来衡量 DP 采样的效用。然而，此类公式存在两个关键局限性：1) 它们无法捕捉支撑集的几何结构；2) 当分布的支撑集不同时，它们不再适用。为了解决这些问题，我们开发了一种以 Wasserstein 距离作为效用度量的 DP 采样新框架。在该框架下，我们提出了 Wasserstein 投影机制（WPM），这是一种基于 Wasserstein 投影的极小极大最优机制。此外，我们开发了用于近似计算所提机制的高效算法，并提供了收敛性保证。

## Abstract
In this paper, we study the problem of sampling from a distribution under the constraint of differential privacy (DP). Prior works measure the utility of DP sampling with density ratio-based measures such as KL divergence. However, such formulations suffer from two key limitations: 1) they fail to capture the geometric structure of the support, and 2) they are not applicable when the supports of the distributions differ. To deal with these issues, we develop a novel framework for DP sampling with Wasserstein distance as the utility measure. In this formulation, we propose Wasserstein Projection Mechanism (WPM), a minimax optimal mechanism based on Wasserstein projection. Furthermore, we develop efficient algorithms for computing the proposed mechanisms approximately and provide convergence guarantees.

---

## 论文详细总结（自动生成）

这是一份关于论文《Differentially Private Sampling from Distributions via Wasserstein Projection》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了在**本地差分隐私（LDP）**约束下如何从给定分布中进行采样。
*   **研究动机**：现有的私有采样机制（如 KL 投影机制 KPM）主要依赖于 KL 散度或 f-散度等基于密度比的度量。这些度量存在两个致命缺陷：
    1.  **几何忽略**：无法捕捉支撑集（Support）的几何结构（例如，在地理位置或语义嵌入空间中，距离远近对效用影响巨大）。
    2.  **支撑集不匹配**：当输入分布和输出采样空间的支撑集不一致时（例如从受限的候选集中采样），基于密度比的度量会失效或变得无意义。
*   **核心目标**：引入 **Wasserstein 距离**作为效用度量，设计一种既能感知几何结构，又能处理支撑集不匹配问题的差分隐私采样框架。

### 2. 方法论：核心思想与技术细节
论文提出了 **Wasserstein 投影机制（WPM）**，其核心流程如下：
*   **核心思想**：将输入分布 $\mu$ 投影到满足 $\epsilon$-LDP 约束的概率分布集合（即 LDP 多面体 $Q_{m,\epsilon}$）中，使得投影后的分布与原分布之间的 Wasserstein 距离最小。
*   **极小极大最优性**：论文证明了在最坏情况下，WPM 是 Wasserstein 距离意义下的极小极大最优机制。
*   **最优基度量（Base Measure）优化**：不同于 KL 散度使用均匀分布作为基度量，WPM 的最优基度量取决于空间的几何结构。论文将其转化为一个凸优化问题，并证明可以通过镜像下降法高效求解。
*   **高效近似算法（算法 1）**：
    *   由于精确计算 Wasserstein 投影涉及复杂的线性规划，论文引入了**熵正则化（Entropic Regularization）**。
    *   利用**交替投影（Alternating Projections）**思想，开发了类似于 Sinkhorn 算法的迭代流程。
    *   **收敛保证**：证明了该算法在 Hilbert 度量下具有**线性收敛性**，这比一般的 Dykstra 算法具有更强的理论保障。

### 3. 实验设计
论文通过三种场景验证了 WPM 的有效性：
*   **合成数据**：在环形（Ring）支撑集上进行实验，观察 privatized 分布是否能保持原始分布的形状。
*   **MovieLens 100k（电影推荐）**：
    *   **场景**：根据用户历史评分分布推荐新电影（支撑集不匹配：历史电影 vs 候选电影）。
    *   **度量**：Hit Rate@1（命中率）。
    *   **Benchmark**：KPM（KL 投影）、指数机制（Exponential Mechanism）、基于类别的启发式方法。
*   **Gowalla（地理位置数据）**：
    *   **场景**：在 20x20 的网格中保护用户的签到位置隐私。
    *   **度量**：估计分布与真实分布之间的 Wasserstein 距离。

### 4. 资源与算力
*   **硬件环境**：论文明确指出实验运行在 Intel Xeon Silver 4316 CPU 和 **8 台 NVIDIA A100-SXM4-80GB GPU** 上。
*   **软件/时长**：虽然未详细列出总训练时长，但提到算法 1 在 40 次迭代内即可收敛，且在 MovieLens 实验中使用了矩阵分解预计算嵌入。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了合成数据、推荐系统、地理位置服务三个完全不同的领域，实验设计较为全面。
*   **充分性**：
    *   考虑了不同的隐私预算 $\epsilon$（从 0.2 到 5.0）。
    *   针对 WPM 的关键参数（如正则化参数 $\lambda$、基度量 $m$）进行了消融分析或超参数搜索。
    *   **客观性**：对比了当前最先进的 KPM 机制和经典的指数机制，实验结果显示 WPM 在几何敏感场景下具有压倒性优势。

### 6. 主要结论与发现
*   **几何感知的重要性**：在合成实验中，WPM 能够将概率质量集中在原始高概率区域附近，而 KPM 则会产生均匀扩散，导致几何信息丢失。
*   **处理不匹配支撑集**：在 MovieLens 实验中，WPM 显著优于 KPM，证明了其在输入/输出空间不一致时的鲁棒性。
*   **性能优势**：随着隐私预算 $\epsilon$ 的增加，WPM 利用几何信息的能力进一步增强，性能提升幅度远超基准方法。
*   **算法效率**：提出的熵正则化算法不仅计算速度快，且线性收敛理论在实验中得到了验证。

### 7. 优点（亮点）
*   **理论完备**：不仅提出了机制，还证明了其极小极大最优性、必要性以及算法的收敛性。
*   **灵活性**：框架通用，可以处理离散空间，也能通过对称性简化处理特定的连续空间（如球体）。
*   **实用性强**：解决了实际应用中常见的“支撑集不匹配”问题，使 DP 采样能应用于更复杂的推荐和定位场景。

### 8. 不足与局限
*   **计算复杂度**：尽管有近似算法，但计算开销仍随支撑集大小 $k$ 的增加而增加（涉及 $k \times k_v$ 的代价矩阵），对于超大规模词表（如 LLM 采样）可能仍有挑战。
*   **连续空间限制**：虽然讨论了连续空间，但大多数高效实现和实验仍集中在离散化后的空间。
*   **超参数依赖**：最优基度量 $m^*$ 的确定依赖于对空间几何的预先了解，在某些动态变化的几何空间中可能难以实时更新。

（完）
