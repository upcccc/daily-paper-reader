---
title: Modulated learning for private and distributed regression with just a single sample per client device
title_zh: 针对每个客户端设备仅有一个样本的隐私和分布式回归的调制学习
authors: "Praneeth Vepakomma, Amirhossein Reisizadeh, Samuel Horváth, Munther Dahleh"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07233v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 单样本联邦学习中的隐私分布式回归
tldr: 针对每个客户端仅有一个样本的分布式学习场景（如健身追踪器），传统联邦学习因局部更新质量差及隐私噪声干扰而失效。本文提出一种调制学习方法，通过对每个客户端样本注入精心校准的噪声扰动并共享处理后的表示，使服务器能聚合得到无偏梯度更新。该方法在保护隐私的同时，实现了与非隐私中心化梯度一致的学习效果，解决了极度稀疏数据下的协作建模难题。
selection_source: fresh_fetch
motivation: 传统联邦学习在每个客户端仅有一个样本且需满足隐私保护时，局部更新质量极低且噪声干扰严重。
method: 客户端对单个样本注入校准噪声并进行变换，将处理后的表示发送至服务器以聚合生成无偏梯度。
result: 该方法能够获得在期望上与非隐私中心化梯度一致的更新，从而在极少数据量下实现准确的模型学习。
conclusion: 调制学习为极度受限数据的分布式场景提供了一种高效且保护隐私的回归建模方案。
---

## 摘要
本研究关注从大量设备中进行学习的问题，其中每个设备仅持有一个数据样本。这种“每个客户端一个样本”的设置存在多种实际应用，包括从健身追踪器、数据/应用使用聚合器、佩戴式传感设备以及日常事件监测器中学习等。当客户端只有一个样本时，标准的联邦学习范式会失效，因为基于该单点的局部更新远非有效，尤其是在模型系数估计的早期轮次中。每一轮应用的隐私诱导噪声进一步削弱了这种效用。本研究针对这一问题，使此类客户端能够协同贡献，在不泄露数据隐私的情况下有效地学习全局模型。所提出的方法注入单一且经过仔细校准的噪声扰动来转换每个客户端的样本，随后将后处理的表示共享给服务器。在服务器端聚合的这些表示经过处理后，可获得无偏梯度更新，其期望值与非隐私的集中式梯度一致，同时保留了数据隐私。这种方法不同于传统的隐私联邦学习，后者的通信负载涉及模型系数，而非隐私转换后的数据样本。该方法使数据极其有限的设备能够协作并学习准确且保护隐私的模型，而无需大型本地数据集或牺牲个人隐私。

## Abstract
This work focuses on the question of learning from a large number of devices with each device holding only a single sample of data. Several real-world applications exist to this one sample per client setup up including learning from fitness trackers, data/app usage aggregators, body-worn sensing devices, and daily event monitors to name a few. When a client has only one sample, the standard federated learning paradigm breaks down as a local update based on that single point is far from being useful, especially in the earlier rounds for estimation of the model coefficients. This utility is further weakened by the privacy-inducing noise applied at every round. This work caters to this problem to enable such clients to collaboratively contribute to effectively learn a global model without leaking the privacy of their data. The proposed approach injects a single, carefully calibrated noisy perturbation to transform the sample at each client, followed by a post-processed representation which is shared with the server. These representations aggregated at the server are processed to obtain an unbiased gradient update that in expectation matches the non-private centralized gradient while preserving data privacy. This approach is different than traditional private federated learning, where the communication payloads involve model coefficients as opposed to privately transformed data samples. This method enables devices with extremely limited data to collaborate and learn accurate, privacy-preserving models without requiring large local datasets or sacrificing individual privacy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Modulated learning for private and distributed regression with just a single sample per client device》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在联邦学习（FL）中，通常假设每个客户端拥有足够的本地数据。但在现实场景（如健身追踪、移动健康应用）中，每个设备往往只能提供**单个数据样本**。
*   **研究动机**：在“单样本”极端场景下，传统的差分隐私随机梯度下降（DP-SGD）会失效。原因在于：
    1.  基于单点的局部更新本身质量极差，无法代表全局分布。
    2.  为了满足隐私保护而注入的噪声会彻底淹没微弱的信号，导致模型无法收敛。
*   **整体含义**：本文旨在设计一种新的分布式学习范式，使服务器能够从大量仅持有一个隐私样本的设备中，聚合出准确且无偏的线性回归模型。

### 2. 核心方法论
论文提出了一种**调制学习（Modulated Learning）**框架，其核心思想是改变通信内容——不再传输模型权重，而是传输经过隐私处理的“数据表示”。

*   **客户端操作（调制与加噪）**：
    *   **变换**：客户端使用余弦函数对原始特征 $x_i$ 进行收缩变换。公式为：$g(x) = (1-\alpha)x + \lambda \cos(\omega \langle x, v \rangle + \phi)v$。其中 $v$ 是服务器发送的调制方向，$\phi$ 是随机相位。
    *   **隐私保护**：基于变换函数的 Lipschitz 连续性计算敏感度，注入高斯噪声 $\xi_i \sim N(0, \sigma^2 I)$，确保满足 $(\epsilon, \delta)$-局部差分隐私（LDP）。
*   **服务器操作（去偏与聚合）**：
    *   服务器接收到隐私表示后，利用代数手段抵消调制引入的偏差（如减去 $\frac{\lambda^2}{2}vv^\top$ 和噪声方差项）。
    *   **无偏梯度估计**：证明了处理后的聚合结果在期望上等于非隐私环境下的中心化梯度 $\nabla L(\beta)$。
*   **变体设计**：
    *   **多向量调制**：使用 $m$ 个正交向量进行调制，以降低方差并提高收敛速度。
    *   **非迭代方案**：提出了一种基于矩估计的一步法（One-shot），无需多次往返通信。

### 3. 实验设计
*   **数据集**：使用了 5 个真实的回归任务数据集：
    1.  **CO2**：二氧化碳排放预测。
    2.  **Fair**：婚姻状况相关预测。
    3.  **ModeChoice**：交通方式选择。
    4.  **RandHIE (lncoins/fmde)**：医疗保险实验数据。
*   **Benchmark（基准）**：非隐私状态下的普通最小二乘法（OLS）参考线。
*   **对比方法**：
    *   **Modulated Iterative**：本文提出的迭代梯度下降法。
    *   **Modulated One-shot Ridge**：本文提出的带岭回归正则化的单次估计法。
    *   **DP-SGD FedAvg**：经过精细调优的传统隐私联邦平均算法。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件型号（如 GPU/CPU 数量）或具体的训练时长。
*   **实现细节**：实验主要侧重于统计效用（$R^2$ 分数）与隐私预算 $\epsilon$ 之间的权衡，而非计算性能。由于是线性回归任务，其计算开销通常远低于深度学习模型。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同领域的数据集上进行了完整的隐私保护水平（$\epsilon$ 从 0.5 到 10.0）扫描。
*   **充分性**：
    *   **多维度对比**：不仅对比了基准算法，还对比了自身框架下的迭代法与单次法。
    *   **超参数调优**：对所有对比方法（包括 DP-SGD）都进行了详尽的超参数搜索（如剪切范数、学习率等），确保了对比的**公平性**。
    *   **理论验证**：实验结果很好地印证了文中推导的方差分解和收敛性定理。

### 6. 主要结论与发现
*   **性能优越性**：在单样本场景下，调制学习显著优于传统的 DP-SGD。
*   **隐私-效用权衡**：
    *   在**强隐私（低 $\epsilon$）**下，One-shot（单次）法表现最好，因为它避免了多轮迭代导致的隐私预算快速消耗。
    *   在**弱隐私（高 $\epsilon$）**下，迭代法表现更优，因为它能通过多次优化逼近非隐私 OLS 的性能。
*   **维度影响**：实验证实了重构误差受数据维度 $d$ 的线性影响，符合理论预期。

### 7. 优点与亮点
*   **数学严谨性**：提供了详尽的无偏性证明、方差分析以及针对攻击者的重构误差下界（Cramér-Rao Bound）分析。
*   **范式创新**：打破了 FL 必须传输模型梯度的固有思维，通过传输“隐私化数据表示”解决了极度稀疏数据下的学习难题。
*   **实用性**：提出的多向量调制技术能有效降低方差，使算法在实际应用中更稳健。

### 8. 不足与局限
*   **模型限制**：目前该方法主要局限于**线性回归**。虽然线性模型在医疗和传感领域很常用，但无法直接推广到复杂的非线性深度学习任务。
*   **隐私假设**：实验假设标签 $y$ 是非敏感的（或公开的）。如果标签也需要高强度隐私保护，算法的效用可能会大幅下降。
*   **通信开销**：虽然解决了学习问题，但传输数据表示（维度为 $d$）在某些高维场景下可能比传输模型参数更频繁或更占带宽。
*   **重构风险**：尽管有理论下界，但传输与原始数据维度一致的表示，在直觉上比传输梯度存在更高的成员推理或重构风险（尽管文中通过调制进行了缓解）。

（完）
