Title: Bodhi VLM: Privacy-Alignment Modeling for Hierarchical Visual Representations in Vision Backbones and VLM Encoders via Bottom-Up and Top-Down Feature Search

URL Source: https://arxiv.org/pdf/2603.13728v2

Published Time: Thu, 19 Mar 2026 00:59:15 GMT

Number of Pages: 13

Markdown Content:
> 1

# Bodhi VLM: Privacy-Alignment Modeling for Hierarchical Visual Representations in Vision Backbones and VLM Encoders via Bottom-Up and Top-Down Feature Search 

Bo Ma 1, 2 Wei Qi Yan 1 Jinsong Wu 3,41 Auckland University of Technology, Auckland 1024 NZ,  

> 2

Resideo Technologies, Inc., Austin, TX 78759, USA,  

> 3

Guilin University of Electronic Technology, Gui Lin, China,  

> 4

Universidad de Chile, Santiago, Chile, 

Learning systems that preserve privacy often inject noise into hierarchical visual representations; a central challenge is to model 

how such perturbations align with a declared privacy budget in a way that is interpretable and applicable across vision backbones and vision–language models (VLMs). We propose Bodhi VLM , a privacy-alignment modeling framework for hierarchical neural representations : it (1) links sensitive concepts to layer-wise grouping via NCP and MDAV-based clustering; (2) locates sensitive feature regions using bottom-up (BUA) and top-down (TDA) strategies over multi-scale representations (e.g., feature pyramids or vision-encoder layers); and (3) uses an Expectation-Maximization Privacy Assessment (EMPA) module to produce an interpretable 

budget-alignment signal by comparing the fitted sensitive-feature distribution to an evaluator-specified reference (e.g., Laplace or Gaussian with scale c/ϵ ). The output is reference-relative and is not a formal differential-privacy estimator. We formalize BUA/TDA over hierarchical feature structures and validate the framework on object detectors (YOLO, PPDPTS, DETR) and on the visual encoders of VLMs (CLIP, LLaVA, BLIP). BUA and TDA yield comparable deviation trends; EMPA provides a stable alignment signal under the reported setups. We compare with generic discrepancy baselines (Chi-square, K-L, MMD) and with task-relevant baselines (MomentReg, NoiseMLE, Wass-1). Results are reported as mean ±std over multiple seeds with confidence intervals in the supplementary materials. This work contributes a learnable, interpretable modeling perspective for privacy-aligned hierarchical representations rather than a post hoc audit only. Source code: Bodhi-VLM GitHub repository 

Index Terms —Bodhi VLM, privacy-alignment modeling, hierarchical visual representations, bottom-up analysis (BUA), top-down analysis (TDA), expectation-maximization privacy assessment (EMPA), vision-language model (VLM), neural representation learning, sensitive feature localization. 

I. I NTRODUCTION 

Privacy-preserving learning systems often inject noise into hierarchical visual representations (e.g., feature pyramids in detectors or vision-encoder layers in VLMs). A central ques-tion for neural network modeling is how to characterize and interpret the alignment between observed layer-wise pertur-bations and a declared privacy budget ϵ under an evaluator-specified reference mechanism (e.g., Laplace or Gaussian with scale c/ϵ ). We propose Bodhi VLM , a privacy-alignment mod-eling framework for hierarchical visual representations that: (1) models sensitive feature structure in a layer-wise manner via bottom-up (BUA) and top-down (TDA) grouping over multi-scale features; and (2) produces an interpretable budget-alignment signal via an Expectation-Maximization Privacy Assessment (EMPA) module that compares the fitted sensitive-feature distribution to the reference. The output is reference-relative and does not constitute formal differential privacy certification or estimation of effective ϵ.BUA and TDA traverse hierarchical feature structures (e.g., feature pyramid networks in detectors, or the visual encoder layers in CLIP [1] and LLaVA [2]), producing paired sensitive and non-sensitive feature groups per layer using NCP and MDAV-based clustering; EMPA then uses these groups in an EM-like procedure to output a discrepancy signal. The framework does not modify the underlying architecture but provides a learnable, interpretable view of how privacy-related noise is distributed across layers and how it aligns with a declared budget. Contributions: (1) We formalize BUA and TDA for sensitive feature search over FPN and VLM vision backbones using NCP and MDAV-based clustering. (2) We introduce EMPA as a feature-level budget-alignment (discrepancy) estimator and integrate it into Bodhi VLM. (3) We show how Bodhi VLM applies to the vision towers of representative VLMs (Section III-H); extending the assessment to full cross-modal fusion and language-decoding pathways remains future work. (4) We report experiments on MOT20 and COCO and design an experimental protocol for comparison (BUA vs. TDA, EMPA vs. Chi-square, K-L, MMD, and task-relevant baselines MomentReg, NoiseMLE, Wass-1). (5) We compare Bodhi VLM with representative VLM privacy methods (ViP, DP-Cap, DP-MTV, VisShield) and with distribution-level and task-specific baselines (Section II-D); we demonstrate the framework on CLIP, LLaVA, and BLIP vision encoder features (Section IV-B6). 

a) Scope and relevance to TNNLS.: The core con-tribution of this work is modeling for hierarchical neural representations: we propose a formalization of layer-wise sensitive-feature structure (BUA/TDA), a grouping mecha-nism (NCP/MDAV) over multi-scale visual features, and an interpretable budget-alignment estimator (EMPA). This is a contribution to learning systems and representation structure ,not only to applied privacy evaluation. The problem is situated 

> arXiv:2603.13728v2 [cs.CV] 18 Mar 2026 2

within neural network architectures (vision backbones and VLM encoders) and is of interest to the TNNLS readership because it addresses how to model and interpret privacy-related perturbations in hierarchical representations produced by learned vision and vision–language models. II. R ELATED WORK 

A. Differential Privacy and Privacy Auditing 

Differential privacy (DP) [3] provides a rigorous notion of privacy loss via a budget ϵ. Assessing whether a trained model satisfies a given ϵ has been studied from both theoretical and empirical angles. One-line or few-run privacy auditing [4], [5] aims to estimate the effective ϵ from model behavior; our EMPA layer provides a complementary, feature-level assessment that consumes the outputs of BUA/TDA. Hitaj et al. [6] use deep generative models to study information leakage in collaborative learning. Agrawal and Aggarwal [7] quantify privacy in data mining. DP for deep learning [8] and for federated settings [9] are standard baselines; Bodhi VLM does not replace training-time DP but evaluates whether the observed noise aligns with the declared budget. 

B. Hierarchical and Multimodal Representations 

Feature pyramid networks (FPN) [10] offer multi-scale representations used in object detection; we leverage their hier-archical structure for BUA and TDA. Vision-language models (VLMs) combine a visual encoder with a language model: CLIP [1] aligns image and text embeddings via contrastive learning; LLaVA [2] projects visual features into the LLM space for instruction following; BLIP [11] unifies encoding and decoding for vision-language understanding and genera-tion. Recent VLMs such as Qwen2-VL [12] and InternVL [13] scale to 4K resolution and support video understanding, ex-panding the scope of multimodal tasks. These architectures expose layer-wise visual features (ViT blocks or CNN stages), to which BUA and TDA can be applied without modifying the model. 

C. Privacy in Vision and Vision-Language Models 

Privacy for VLMs has gained attention with several dis-tinct approaches. ViP [14] trains differentially private vi-sion transformers via masked autoencoders and DP-SGD on LAION400M ( ϵ=8 ), achieving 55.7% ImageNet linear prob-ing. DP-Cap [15] improves upon ViP using image captioning for multimodal DP representation learning, reaching 65.8% ImageNet-1K under ϵ=8 on LAION-2B. DP-MTV [16] en-ables many-shot multimodal in-context learning with (ε, δ )-DP by aggregating demonstrations into compact task vectors, preserving 50% accuracy on VizWiz at ε=1 .0. VisShield [17] adopts a de-identification paradigm: it uses instruction-tuned VLMs to locate and mask Protected Health Information (PHI) in medical images via OCR. Membership inference on VLMs [18] reveals that instruction-tuning data is vulnerable to attacks achieving AUC > 0.8 with few samples. Bodhi VLM complements these works: whereas ViP/DP-Cap/DP-MTV fo-cus on training with DP and VisShield on de-identification, Bodhi VLM assesses whether the observed privacy noise aligns with a declared budget, providing a feature-level audit tool that can be applied to any VLM vision tower. 

D. External Comparison Methods Used in This Work 

Our core innovation is hierarchical sensitive-feature modeling , grouping mechanisms (BUA/TDA with NCP/MDAV), and budget-alignment estimation (EMPA) for neural representations—i.e., a modeling and representation-level contribution rather than a purely applied privacy-evaluation tool. We position Bodhi VLM against the following external methods and baselines in our experiments (Section IV). VLM privacy methods: ViP [14], DP-Cap [15], DP-MTV [16], and VisShield [17] are contrasted with Bodhi VLM in Table VI; they represent training-time DP or de-identification rather than post hoc budget assessment, so the comparison is qualitative (paradigm and compatibility with our framework). Distribution-level and task-relevant baselines: 

We compare EMPA with generic discrepancy measures (Chi-square, K-L divergence, MMD) and with task-specific budget-alignment baselines: MomentReg (regression of declared ϵ from feature moments and layer-wise dispersion), 

NoiseMLE (maximum-likelihood under the evaluator-specified additive noise family), and Wass-1 (first-order Wasserstein distance between perturbed and reference sensitive-feature distributions). These appear in Tables III, VII, and in the supplementary materials. Random-partition baseline: On synthetic hierarchical features we add a random partition + EMPA baseline (same sensitive-set size as BUA/TDA but partition chosen uniformly at random) to show that structured grouping (BUA/TDA) yields a more stable and interpretable budget-alignment signal than a blind partition (Figure 7). Together, these external comparison methods allow readers to assess Bodhi VLM relative to both prior VLM privacy work and to generic distributional and non-structured baselines. 

E. Expectation-Maximization and Microaggregation 

The EM algorithm [19] and its extensions [20], [21] inspire our EMPA formulation. Microaggregation (e.g., MDAV [22]) and NCP-based grouping are used in both BUA and TDA to form sensitive/non-sensitive clusters. Similar ideas appear in 

k-anonymity and l-diversity [23] for tabular data; we adapt them to continuous feature vectors in neural backbones. III. M ETHOD 

A. Formal Problem Definition 

We distinguish three conceptual components: (1) the privacy mechanism that perturbs data or features; (2) sensitive feature localization , which identifies layer-wise features correspond-ing to a sensitive label set S (or Sϵ when we emphasize the budget under which it is used); and (3) reference-perturbation assessment , which measures how well the observed perturbed features match an evaluator-defined reference distribution. Bodhi VLM addresses (2) and (3) only; it does not certify formal end-to-end differential privacy guarantees. 3

a) Inputs and output (reference-relative).: Let D be an image dataset; f the original (unperturbed) layer-wise features; 

˜f the observed (perturbed) features; S a set of sensitive labels; ϵ a declared budget; and Mref (ϵ; η) an evaluator-defined reference mechanism with family η (e.g., Laplace with scale c/ϵ ). The assessment is reference-relative : given 

( ˜f , f, S, ϵ, Mref ), we compute a reference-perturbation dis-crepancy score BAS ϵ = A( ˜f , f, S, ϵ ; Mref ) that compares the distribution of observed sensitive-group features to the distribution induced by Mref on the same subset. The output is a scalar discrepancy (and optionally weight-only bias or rMSE to that reference). We do not claim that BAS ϵ estimates the effective ϵ of the true mechanism or provides formal DP certification; if the deployed mechanism differs from Mref , the score reflects model mismatch as well as budget alignment. 

B. Preliminary and Notation 

Let D be an image dataset and Sϵ the set of sensitive labels. A privacy-preserving pipeline processes D and adds noise to sensitive regions. We assume a backbone that produces feature maps at layers i = 1 , . . . , n , represented as layer-wise feature sets {Xi}ni=1 . At each layer i, G(n) 

> i

denotes the non-sensitive group and G(s) 

> i

the sensitive group. Table I summarizes the main notation. The goal is to (1) partition features at each layer into G(n) 

> i

and G(s) 

> i

via BUA or TDA, and (2) from these groups, compute a budget-alignment signal via EMPA (a bias and/or rMSE to a reference). Bodhi VLM achieves this via BUA, TDA, and EMPA. 

C. Sensitive Concepts and Scores 

In all experiments, Sϵ denotes a task-defined set of sen-sitive concepts rather than a quantity learned from ϵ itself. For MOT20, we set Sϵ = {person } for all tested bud-gets. For COCO, we consider a person-only” setting with 

Sϵ = {person } and an expanded traffic-sensitive” setting with Sϵ = {person , car , truck , bus }; the subscript in Sϵ

therefore indicates the sensitive set used under the declared budget, not that the semantic set is generated by ϵ.For a layer-wise feature vector xi,j ∈ Xi, the sensitivity score s(xi,j ; Sϵ) depends on the backbone family. For detector backbones, we first map xi,j to the corresponding region in the detector head and define s(xi,j ; Sϵ) as the maximum detector-head confidence among classes in Sϵ for that region. For VLM vision encoders (CLIP, LLaVA, BLIP), we compute 

s(xi,j ; Sϵ) = max 

> c∈Sϵ

sim  hi(xi,j ), q c

,

where hi(·) maps the local visual feature to the vision en-coder’s embedding space, qc is the text-embedding prototype for concept c, and sim is cosine similarity. Unless otherwise stated, we set the per-layer threshold τi as the 90th percentile of s(·; Sϵ) within layer i, so that approximately the top 10% most sensitive features (under this score) are assigned to G(s) 

> i

.

D. Model Structure 

The Bodhi VLM framework comprises three main com-ponents, as illustrated in Fig. 1 and Fig. 2: (1) Bottom-Up Strategy (BUA) aggregates low-level features upward through the backbone layers, using NCP and MDAV to form sensitive and non-sensitive groups per layer. (2) Top-Down Strategy (TDA) partitions high-level features downward and links layer-wise groups via set intersections and NCP. (3) Expectation-Maximization Privacy Assessment (EMPA) takes the layer-wise partitions {G (s) 

> i

}, {G (n) 

> i

} from either BUA or TDA, together with ϵ and Sϵ, and estimates whether the observed perturbation aligns with the declared budget via an EM-inspired procedure, outputting a discrepancy signal (bias). In this work, Bodhi VLM is validated primarily on the visual encoders (or vision towers) of representative vision-language models under an explicit perturbation mechanism; extending the same budget-alignment assessment to full cross-modal fusion and language-generation pathways remains future work. We detail each component next. 

E. Bottom-Up Strategy (BUA) 

BUA starts from the bottom layer of the backbone and iterates upward over layer-wise feature sets {Xi}ni=1 , where each Xi contains feature vectors (e.g., per-patch tokens for ViT, or per-location vectors for CNN feature maps). At each layer i, BUA outputs a partition of Xi into a sensitive group 

G(s) 

> i

and a non-sensitive group G(n) 

> i

based on a sensitivity score s(x; Sϵ) and a threshold τi:

s(xi,j ; Sϵ) ∈ [0 , 1] , (1) 

G(s) 

> i

:= {xi,j ∈ Xi : s(xi,j ; Sϵ) ≥ τi} , G(n) 

> i

= Xi\G (s) 

> i

.

(2) We use MDAV [22] as a microaggregation operator for group-ing (e.g., to enforce a minimum group size and stabilize the partition), and NCP as a quantitative penalty to mea-sure within-group generalization (defined below). Algorithm 1 gives a layer-wise specification. 

a) NCP and MDAV definitions.: For a cluster (group) 

C of d-dimensional feature vectors, we define the normalized certainty penalty (NCP) as 

NCP( C) := 

> d

X

> a=1

wa penalty a(C), (3) with 

penalty a(C) := max x∈C x(a) − min x∈C x(a)

Ra

, (4) where wa ≥ 0 are attribute weights and Ra is the range (or a robust range estimate) of dimension a. We apply MDAV to the feature vectors in Xi (or an explicit transform ϕ(Xi)

if used in implementation) to form microaggregated groups before computing s(·; Sϵ) and the partition in Eq. (2); we state the chosen space (raw feature vs. transformed) explicitly in experiments. 

b) BUA in VLMs.: In a VLM, the “bottom” layer i = 1 

is the patch embedding or the first CNN stage (e.g., CLIP ViT patch projection, LLaVA’s ViT [2], or BLIP’s ResNet stage 1). BUA then moves upward through the vision encoder: for a ViT, layer i is the output of the i-th transformer block (before or after the MLP); for a ResNet-based encoder, i4                                     

> TABLE I MAIN NOTATION .Symbol Meaning
> DImage dataset
> S,SϵSensitive label set (subscript ϵwhen emphasizing budget)
> Mref (ϵ;η)Evaluator-defined reference mechanism (family η)
> f,˜fOriginal and perturbed layer-wise features
> XiSet of feature vectors at layer i(e.g., tokens/patches/locations)
> G(n)
> i,G(s)
> iNon-sensitive and sensitive groups at layer it,u,v(EMPA) Raw sensitive data, interference (noise), observed data ( v=t+u)
> s(x;Sϵ),τiSensitivity score and layer-wise threshold
> λk,μk,σ2
> k(EMPA) Mixture weights, means, diagonal variances in EMPA

Algorithm 1 Bottom-Up Strategy (BUA) 

Input: Layer-wise feature sets {Xi}ni=1 ; sensitive concept set 

Sϵ; thresholds {τi}ni=1 

Output: Sensitive groups {G (s) 

> i

}ni=1 and non-sensitive groups 

{G (n) 

> i

}ni=1  

> 1:

for i = 1 , 2, . . . , n do  

> 2:

(Optional) Apply MDAV microaggregation to Xi (in the chosen space) to obtain stable groups.  

> 3:

Compute sensitivity scores s(x; Sϵ) for all x ∈ Xi. 

> 4:

Partition Xi into G(s) 

> i

= {x ∈ Xi : s(x; Sϵ) ≥ τi} and 

G(n) 

> i

= Xi \ G (s) 

> i

. 

> 5:

(Optional) Evaluate within-group dispersion using 

NCP( G(s) 

> i

) and NCP( G(n) 

> i

) for auditing/stability.  

> 6:

end for  

> 7:

return {G (s) 

> i

}ni=1 and {G (n) 

> i

}ni=1 

indexes the residual stages. At each i, the feature map is flattened into vectors (e.g., per-patch or per-spatial-location), and NCP and MDAV operate on these vectors with the same 

Sϵ used for the rest of the pipeline. The upward aggregation thus builds a hierarchy of sensitive/non-sensitive groups that aligns with the VLM’s internal representation levels, from local patches (low-level) to global semantics (high-level), and the layer-wise partitions {G (s) 

> i

}, {G (n) 

> i

} are passed to EMPA for budget assessment. Fig. 1 illustrates BUA in a VLM vision encoder: from image input and patch embedding (or first CNN stage), features are flattened to vectors at each layer; NCP and MDAV partition them into sensitive G(s) 

> i

and non-sensitive G(n) 

> i

groups along the bottom-up aggregation, and the layer-wise partitions are passed to EMPA for privacy budget assessment. Figure 1 illustrates BUA in a VLM vision encoder: the image input is processed by patch embedding (or the first CNN stage), yielding an initial sensitive vs. non-sensitive visualization; the framework then aggregates features bottom-up through Layer 1 to Layer n, with each layer producing flattened feature vectors partitioned into G(s) 

> i

(sensitive) and 

G(n) 

> i

(non-sensitive) via NCP and MDAV; the resulting layer-wise groups feed into EMPA for budget assessment and privacy budget bias estimation. 

F. Top-Down Strategy (TDA) 

TDA starts from the top layer and proceeds downward. Like BUA, it produces a sensitive/non-sensitive partition at each layer, but additionally uses inter-layer links to propagate the grouping from higher to lower layers. Because G(s) 

> i

and G(s)

> i−1

generally lie in different feature spaces (different dimensions or coordinate systems), we first map higher-layer elements to the lower layer via a correspondence operator Πi→i−1 : Xi →

Xi−1 (e.g., nearest spatial/patch correspondence or identity when dimensions match). We then define the top-down links: 

L(s) 

> i

= Π i→i−1(G(s) 

> i

)∩G (s)

> i−1

, L(n) 

> i

= Π i→i−1(G(n) 

> i

)∩G (n)

> i−1

,

(5) and use these links to refine the lower-layer grouping (e.g., by adjusting thresholds τi−1 or by reweighting sensitivity scores for elements connected to high-level sensitive groups). Algo-rithm 2 specifies the layer-wise operations. We use MDAV for microaggregation and NCP for within-group dispersion as in BUA; importantly, we avoid reusing EMPA variables ( u, v ) in the algorithmic notation. 

a) TDA in VLMs.: In a VLM, the “top” layer i = n is the last layer of the vision encoder before any projection into the language space (e.g., the last ViT block in CLIP [1] or LLaVA, or the final global-pooled features in BLIP [11]). TDA then walks backward: i = n − 1, n − 2, . . . , 1. At each step, the same MDAV/NCP logic partitions the current layer’s feature vectors into G(n) 

> i

and G(s) 

> i

, and the intersections G(n) 

> i

∩ G (n)

> i−1

and G(s) 

> i

∩ G (s) 

> i−1

tie high-level semantic groups to the lower layers that support them. This top-down pass is well-suited to VLMs because the highest layers encode task-relevant and often identity-sensitive semantics (e.g., faces, text in images); tracing these back yields which earlier layers contribute to sensitive outputs. The resulting per-layer partitions are again fed into EMPA together with ϵ and Sϵ.

G. Expectation-Maximization Privacy Assessment (EMPA) 

EMPA is a reference-relative discrepancy module: it takes the layer-wise groups from BUA or TDA, a declared budget 

ϵ, and an evaluator-defined reference mechanism Mref (ϵ; η),and outputs an empirical discrepancy between the observed sensitive-feature distribution and the reference. EMPA is not a formal estimator of end-to-end differential privacy parameters. 

a) Concrete instantiation and reference.: We use a K-component diagonal-Gaussian mixture on sensitive-group fea-ture vectors. Let V(s) = {vj }Ns 

> j=1

denote all feature vectors 5       

> Fig. 1. BUA in a VLM vision encoder: image input and patch embedding (or first CNN stage) at the bottom; bottom-up aggregation through Layer 1 to Layer nwith NCP/MDAV partitioning into sensitive G(s)
> iand non-sensitive G(n)
> iat each layer; output to EMPA for privacy budget assessment.

Algorithm 2 Top-Down Strategy (TDA) 

Input: Layer-wise feature sets {Xi}ni=1 ; sensitive concept set 

Sϵ; thresholds {τi}ni=1 

Output: Sensitive groups {G (s) 

> i

}ni=1 and non-sensitive groups 

{G (n) 

> i

}ni=1  

> 1:

Initialize top-layer grouping at i = n by computing 

s(x; Sϵ) for x ∈ Xn and applying Eq. (2).  

> 2:

for i = n, n − 1, . . . , 2 do  

> 3:

(Optional) Apply MDAV microaggregation to Xi−1 (in the chosen space).  

> 4:

Compute scores s(x; Sϵ) for all x ∈ Xi−1. 

> 5:

Form a preliminary partition (G(s)

> i−1

, G(n)

> i−1

) using Eq. (2).  

> 6:

Compute top-down links L(s) 

> i

= G(s) 

> i

∩G (s) 

> i−1

and L(n) 

> i

=

G(n) 

> i

∩ G (n)

> i−1

. 

> 7:

Refine the lower-layer partition using the propagated links (e.g., prioritize elements in L(s) 

> i

as sensitive).  

> 8:

end for  

> 9:

return {G (s) 

> i

}ni=1 and {G (n) 

> i

}ni=1 

in sensitive groups across selected layers (after perturbation, optionally pooled to a common space). We model 

p(v | Θϵ) = 

> K

X

> k=1

λk N  v; μk, diag( σ2

> k

), (6) with Θϵ = {λk, μ k, σ 2

> k

}Kk=1 , λk ≥ 0, PKk=1 λk =1. We fit Θϵ by standard EM: E-step responsibilities 

γjk = λkN (vj ; μk, diag( σ2

> k

)) / PKℓ=1 λℓN (vj ; μℓ, diag( σ2 

> ℓ

)) ,and M-step updates Nk = P 

> j

γjk , λnew  

> k

= Nk/N s, μnew  

> k

=(1 /N k) P 

> j

γjk vj , (σ2

> k

)new = (1 /N k) P 

> j

γjk (vj − μk)2.The reference Θref  

> ϵ

is obtained by the same fitting on sensitive-group features generated by Mref (ϵ; η) (e.g., Laplace with b = c/ϵ ) on the same subset. Thus the score is explicitly 

reference-relative ; if the true mechanism differs from Mref ,the discrepancy reflects both budget alignment and mechanism mismatch. Robustness to K ∈ {2, 4, 8}, diagonal vs. full covariance, and to the choice of ψ below is left for future work. 

b) Discrepancy function.: We define the reference-perturbation discrepancy score (reported as budget-alignment score in tables) by a parameterization ψ(Θ) and ℓ2 distance: 

ψ(Θ) = λ1, . . . , λ K , μ ⊤ 

> 1

, . . . , μ ⊤ 

> K

, (σ21 )⊤, . . . , (σ2 

> K

)⊤⊤,

(7) 

BAS ϵ := ψ(Θ ϵ) − ψ(Θ ref  

> ϵ

) 2 . (8) 6     

> Fig. 2. TDA and BUA in VLM: top-down and bottom-up feature search over the vision encoder layers (e.g., ViT blocks); layer-wise groups G(n)
> i,G(s)
> ifeed EMPA for feature-level budget-alignment assessment.

In our implementation we use the full ψ unless stated other-wise. When we focus on a weight-only view, we report 

Bias ϵ := λϵ − λref   

> ϵ2

, (9) where λϵ and λref  

> ϵ

denote the weight vectors from Θϵ and Θref  

> ϵ

.These quantities are empirical feature-level discrepancy scores under the chosen perturbation model and declared budget; they are not estimates of an effective differential-privacy parameter and should not be interpreted as such. 

c) Primary and secondary outputs.: The primary out-put is the reference-perturbation discrepancy score BAS ϵ =

∥ψ(Θ ϵ) − ψ(Θ ref  

> ϵ

)∥2, which compares the fitted sensitive-feature distribution to the reference under the same declared 

ϵ and family Mref . The quantity Bias ϵ = ∥λϵ − λref  

> ϵ

∥2 is a weight-only diagnostic. Neither is a formal estimator of effective ϵ; both are reference-relative empirical signals. 

H. Extension to Vision-Language Models 

Vision-language models (VLMs) such as CLIP [1], LLaVA [2], and BLIP [11] use a visual encoder (e.g., ViT or ResNet) that produces hierarchical or multi-layer features before fusion with text. We validate Bodhi VLM on the 

vision towers of these VLMs; extending the assessment to full cross-modal fusion and language-decoding pathways remains future work. Without architectural change: (1) Treat the visual encoder as a backbone with layers i = 1 , . . . , n (e.g., ViT blocks or CNN stages). (2) Run BUA from the earliest layer upward or TDA from the last visual layer downward to obtain sensitive/non-sensitive groups G(n) 

> i

, G(s) 

> i

per layer, using the same NCP and MDAV-based clustering over feature vectors and a given sensitive label set Sϵ. (3) Feed the resulting groups into EMPA to estimate the discrepancy signal between observed privacy noise and the declared budget ϵ.

a) Layer mapping and feature extraction.: For CLIP 

(ViT backbone), i = 1 is the patch embedding output; 

i = 2 , . . . , n are the transformer block outputs (e.g., n = 12 

for ViT-B/32). Each layer’s feature tensor has shape (patches, dim); we treat each patch (or pooled region) as one vector for NCP/MDAV. For LLaVA , the vision tower is typically a ViT (e.g., CLIP ViT-L/14); again i = 1 is patch embedding and i = 2, . . . , n are block outputs. The language model receives only the final layer (or a projected subset); Bodhi VLM runs BUA/TDA on all vision layers and does not modify the language branch. For BLIP with a ResNet image encoder, 

i indexes the residual stages (e.g., i = 1 , . . . , 4); features are spatial maps that we flatten to vectors per location. In all cases, 

Sϵ can be defined over visual concepts (e.g., person, vehicle) or over text labels if image-text pairs are available; the same grouping logic then identifies which layer-wise features align with those concepts. 

b) Text and multimodal sensitivity.: Text modalities can be incorporated by defining Sϵ over joint image-text sensitive concepts (e.g., person IDs, license plates) and applying the same grouping on the visual branch; the language branch can be left unmodified or assessed separately. In this work, Bodhi VLM is validated primarily on the visual encoders (or vision towers) of representative vision-language models under an explicit perturbation mechanism. Extending the same budget-alignment assessment to full cross-modal fusion and language-generation pathways remains future work. IV. E XPERIMENTS 

A. Experimental Setup 1) Datasets 

We use MOT20 [24] (150 frames, uniformly sampled from the designated train/val sequences as in the official split) and COCO [25] (train2017/val2017 splits, k-fold with k = 10 7

where applicable). For VLM experiments we use COCO image-text pairs or 500 COCO images for CLIP, LLaVA, and BLIP. Synthetic hierarchical features (4 layers, 200 samples, 8-D per layer, 30% sensitive ratio) are used to validate the experimental protocol. Privacy budgets: ϵ ∈ { 0.1, 0.01 , 0.001 }.

2) Evaluation Metrics 

We report deviation between original and privacy-noised features (BUA vs. TDA), rMSE for budget alignment (EMPA vs. Chi-square, K-L divergence, MMD), and EMPA bias (de-viation of fitted mixture weights from uniform). For detector experiments we also report PSNR and SIFT-based metrics where applicable [26], [27]. In the main tables, the target 

of assessment is the observed feature perturbation relative to a reference (e.g., Laplace) or to the declared budget; rMSE is the root mean squared error between the metric’s output and this reference (or between noised and original features where stated). Lower rMSE is better when measuring budget-alignment error. We do not claim that these metrics estimate the effective ϵ of a formal DP mechanism; they provide empirical alignment signals for comparison. 

3) Baselines 

We compare EMPA with generic distributional baselines (Chi-square, K-L divergence, MMD) and with task-relevant budget-alignment baselines: MomentReg (regression of de-clared ϵ from feature moments and layer-wise dispersion), 

NoiseMLE (MLE under the evaluator-specified additive noise family), and Wass-1 (first-order Wasserstein distance between perturbed and reference sensitive-feature distributions). Re-sults for MomentReg, NoiseMLE, and Wass-1 are reported in the main tables where space permits and in the supplemen-tary materials (Tables S1–S2). For VLM privacy we contrast Bodhi VLM with ViP [14], DP-Cap [15], DP-MTV [16], and VisShield [17] (Table VI). 

4) Implementation Details 

Detectors: YOLOv4 [28], PPDPTS, MDCRF+YOLO, DETR [29]. VLMs: CLIP ViT-B/32 [1], LLaVA-1.5 7B [2], BLIP (ResNet-50) [11]. We apply additive Gaussian or Laplace noise to sensitive regions/features. Perturbation cal-ibration used in controlled experiments: for Laplace per-turbation we use u ∼ Laplace(0 , b ) with b = c/ϵ , and for Gaussian perturbation u ∼ N (0 , σ 2I) with σ = c/ϵ , where 

c is a fixed constant shared across all methods in a given experiment. This controlled setup is used only to generate a reference perturbation family for empirical auditing and is not claimed as a formal DP calibration. It is important to distinguish: the declared budget ϵ is the parameter under the (assumed) mechanism; the noise scale used in simulation ( c/ϵ )is a choice for controlled experiments; the observed feature perturbation is what we measure (e.g., deviation, rMSE). Our experiments evaluate budget-alignment under these controlled additive mechanisms and do not certify formal end-to-end DP for full VLM pipelines. BUA/TDA use NCP and MDAV with sensitive set Sϵ; EMPA runs E/M steps until convergence. 

a) Statistical reporting.: Where multi-seed results are reported (e.g., synthetic experiments in Section IV-B4, Ta-ble IV), we repeat the evaluation over five random seeds 

{0, 1, 2, 3, 4} covering perturbation sampling, grouping, and EMPA initialization, and we report mean ± standard deviation.  

> Fig. 3. BUA vs. TDA deviation on YOLO (MOT20).

Detector and VLM tables (Tables III, VII) also use mean ±std over the same five seeds; corresponding 95% confidence in-tervals are computed from these multi-seed runs and provided in the supplementary materials. 

5) Reproducibility Configuration 

For clarity, Table II summarizes the main implementation choices and hyperparameters used in our experiments; these defaults are shared across detector and VLM vision-encoder settings unless otherwise specified. 

B. Overall Performance 

We summarize the main results below; detailed figures and tables follow in the next sub-subsections. Bodhi VLM achieves comparable deviation trends for BUA and TDA on both detectors and VLM vision encoders, with BUA slightly more sensitive to noise level. EMPA shows stable budget-alignment (rMSE and bias) across MDCRF, DETR, and PPDPTS, and on CLIP vision encoder features (Tables III, VII); BLIP results are omitted when the model is unavailable in the run. 

1) BUA vs. TDA 

Figures 3 and 4 show the deviation between original and privacy-noised images (MOT20) under BUA and TDA with 

ϵ ∈ { 0.1, 0.01 }. Both strategies track similar trends; BUA yields slightly larger deviation differences (e.g., max differ-ence TDA vs. BUA ≈ 3.2; average ≈ 0.57 on YOLO and 

≈ 0.34 on PPDPTS). Thus BUA and TDA have comparable discriminative power, with BUA slightly more sensitive to noise level. 

2) EMPA with BUA/TDA 

Figure 5 plots deviation for TDA+EMPA and BUA+EMPA on 150 MOT20 images (PPDPTS, ϵ = 0 .001 ). TDA+EMPA is slightly lower on average; between indices 100–130, TDA+EMPA becomes higher than BUA+EMPA. Overall, both combinations behave similarly for privacy-level detection. 

3) Comparison with Other Metrics 

Table III reports rMSE (mean ± std over seeds) for Chi-square, K-L divergence, MMD, and EMPA on MDCRF-0.1, DETR-0.1, PPDPTS-0.1, PPDPTS-0.01 from our multi-seed evaluation. EMPA (BUA) is stable at ≈ 0.89 for YOLO-based detectors (MDCRF, PPDPTS) and lower for DETR ( ≈ 0.56 ); K-L and MMD show smaller numeric values in these runs. 8                                        

> TABLE II REPRODUCIBILITY DETAILS FOR BUA/TDA/EMPA USED IN OUR EXPERIMENTS .Item Specification Feature normalization ℓ2normalization per feature vector before MDAV/NCP (where applied) MDAV group size k= 8 (minimum cluster size for microaggregation) NCP weights waUniform: wa= 1 /d for a d-dimensional feature Range RaInter-quantile range (q0.95 −q0.05 )computed on the training split for each dimension Sensitivity threshold τi90 th percentile of s(·;Sϵ)within layer i(per Section III-C) BUA aggregation unit Grouping computed per image; summary statistics averaged across images TDA cross-layer mapping Nearest spatial/patch correspondence between consecutive layers (same grid or nearest-neighbor in coordinates) EMPA component number KK= 4 diagonal-Gaussian components in all reported experiments EMPA stopping criterion Relative log-likelihood improvement <10 −5or a maximum of 100 EM iterations Reported values Mean ±std over 5 seeds for detector (Table III), synthetic (Table IV), and VLM (Table VII); confidence intervals estimated from the multi-seed runs (see supplementary materials) Hardware NVIDIA GPUs (exact model and PyTorch version documented in the code release)
> Fig. 4. BUA vs. TDA deviation on PPDPTS (MOT20).

EMPA serves as a budget-alignment signal across the reported detector backbones. Figure 6 shows privacy budget (y-axis) vs. rMSE (x-axis) for TDA+EMPA and BUA+EMPA on COCO; Laplace is the reference. TDA+EMPA has the smallest deviation from ground truth at each budget level; BUA+EMPA is close. PSNR and SIFT [26], [27] are less accurate for budget estimation. 

4) Results on Synthetic Hierarchical Features 

To validate the protocol in Section IV-A, we use synthetic hierarchical features (4 layers, 200 samples, 8-D per layer, 30% sensitive ratio) with Gaussian noise scaled by 1/ϵ . Ta-ble IV reports mean ±std over 5 random seeds for Chi-square statistic, K-L divergence, MMD, rMSE, 1-Wasserstein distance (Wass-1), and EMPA bias (BUA and TDA) for ϵ ∈ { 0.1, 0.01 };the numerical summaries are obtained from the same evalua-tion procedure and metrics as in the rest of the experiments. As 

ϵ decreases, the injected noise magnitude increases ( ∝ 1/ϵ ), so rMSE between original and noised features grows. The Chi-square and K-L metrics likewise increase with stronger noise, while MMD stays in a narrow range. EMPA bias remains stable across budgets and is identical for BUA and TDA in this setup, indicating that both grouping strategies yield the same sensitive/non-sensitive partition on the synthetic data; the bias value (about 0.89) reflects the deviation of the fitted mixture weights from a uniform distribution over components. 

a) Diagnostic: why EMPA bias is approximately constant across ϵ.: On the synthetic hierarchical data, the EMPA bias remains nearly constant ( ≈ 0.894 ) for ϵ ∈ { 0.1, 0.01 } while rMSE and K–L divergence change substantially. This is a central diagnostic: in the current formulation, the bias is driven more strongly by the induced group structure (and the fixed partition on synthetic data) than by the perturbation magnitude. We therefore interpret this bias as a structural discrepancy signal relative to the reference, not as a standalone estimator of the effective privacy budget. The primary score BAS ϵ (full ψ)may still carry budget-dependent information in other settings; a decomposition study (fixed partition with varying scale vs. fixed scale with varying partition) to separate structure and perturbation effects is left for future work. Figure 7 plots the EMPA bias against ϵ for BUA+EMPA, TDA+EMPA, and a random partition+EMPA baseline (same sensitive-set size, but partition chosen at random). BUA and TDA coincide on this synthetic setup; the baseline curve is distinct and typically higher, illustrating that structured grouping (BUA/TDA) yields a more interpretable and stable budget-alignment signal than a blind partition. Figure 8 (left) adds contrast by plotting both rMSE and Chi-square (scaled) vs. ϵ; the right panel shows Chi-square, K-L divergence, and MMD vs. ϵ. These figures and the corresponding numerical results follow the same evaluation procedure as the rest of the paper; full details are provided in the supplementary materials. 

5) BUA vs. TDA on VLM Vision Encoders 

To compare how BUA and TDA each combine with differ-ent VLMs, we run both strategies on the vision encoders of CLIP [1] (ViT-B/32), LLaVA-1.5 [2] (7B vision tower, CLIP ViT-L/14), and BLIP [11] (ResNet-50 image encoder). For each VLM we extract layer-wise features from 500 COCO images, inject Laplace noise with ϵ ∈ { 0.1, 0.01 }, and record (i) the deviation between original and noised features under BUA and under TDA, and (ii) the rMSE of BUA+EMPA and TDA+EMPA when estimating budget alignment. Table V reports the results. Across all three VLMs, BUA and TDA yield comparable rMSE (e.g., CLIP: BUA 1.89 vs. TDA 1.92 at ϵ=0 .1; LLaVA: BUA 2.11 vs. TDA 2.08; BLIP: BUA 2.35 vs. TDA 2.41). Deviation trends are similar to the detector case: BUA tends to be slightly more sensitive to noise level. The same NCP/MDAV grouping and EMPA pipeline apply without modification; only the layer indexing (ViT blocks vs. ResNet stages) differs per architecture, as in Section III-H. 9

Fig. 5. Deviation: TDA+EMPA vs. BUA+EMPA (MOT20, PPDPTS ϵ = 0 .001 ). TABLE III 

> R

MSE ( MEAN ± STD OVER 5 SEEDS ) FOR CHI -SQUARE , K-L, MMD, EMPA ON DETECTOR BACKBONES (AGGREGATED OVER MULTI -SEED EVALUATION ; CONFIDENCE INTERVALS AVAILABLE IN THE SUPPLEMENTARY MATERIALS ). Indicator MDCRF-0.1 DETR-0.1 PPDPTS-0.1 PPDPTS-0.01 Chi-square (rMSE) 0.40 ± 0.01 0.36 ± 0.00 0.40 ± 0.01 0.40 ± 0.01 K-L Div. 0.01 ± 0.00 0.47 ± 0.07 0.01 ± 0.00 0.01 ± 0.00 MMD 0.001 ± 0.000 0.005 ± 0.000 0.001 ± 0.000 0.001 ± 0.000 EMPA 0.89 ± 0.00 0.63 ± 0.13 0.89 ± 0.00 0.89 ± 0.00 TABLE IV METRICS FROM SYNTHETIC HIERARCHICAL FEATURE EXPERIMENTS (MEAN ±STD OVER 5 SEEDS ; FULL NUMERICAL SUMMARIES PROVIDED IN THE SUPPLEMENTARY MATERIALS ). 

ϵ Chi-square K-L MMD rMSE Wass-1 EMPA (BUA) EMPA (TDA) 0.1 (1 .69 ± 0.31) × 10 7 2.045 ± 0.219 0.0135 ± 0.0020 5.45 ± 0.21 1.94 ± 0.13 0.894 ± 0.000 0.894 ± 0.000 

0.01 (6 .27 ± 0.96) × 10 7 4.507 ± 0.516 0.0197 ± 0.0028 54 .50 ± 2.15 23 .24 ± 1.59 0.894 ± 0.000 0.894 ± 0.000 

TABLE V BUA+EMPA VS . TDA+EMPA ON CLIP, LL AVA, AND BLIP VISION ENCODERS (COCO, 500 IMAGES , L APLACE NOISE ). D EVIATION : MEAN 

|noised − original |; RMSE: BUDGET -ALIGNMENT ERROR W .R.T. THE LAPLACE REFERENCE TARGET UNDER THE SAME DECLARED ϵ.CLIP ViT-B/32 LLaVA-1.5 7B BLIP ResNet-50 BUA+EMPA TDA+EMPA BUA+EMPA TDA+EMPA BUA+EMPA TDA+EMPA Deviation ( ϵ=0 .1) 0.52 0.48 0.58 0.54 0.61 0.59 Deviation ( ϵ=0 .01 ) 1.24 1.18 1.31 1.26 1.38 1.35 rMSE ( ϵ=0 .1) 1.89 1.92 2.11 2.08 2.35 2.41 rMSE ( ϵ=0 .01 ) 2.34 2.38 2.67 2.62 2.89 2.95 

6) VLM Privacy Methods Comparison 

Table VI contrasts representative VLM privacy methods with Bodhi VLM. ViP [14] and DP-Cap [15] train vision (or vision-language) encoders with DP-SGD; DP-MTV [16] ap-plies DP to multimodal in-context task vectors; VisShield [17] performs de-identification via VLM-based OCR and masking. Bodhi VLM addresses a different need: it does not train or de-identify but assesses whether observed feature-level noise aligns with a declared budget. We extended our protocol to VLM vision encoders (CLIP ViT-B/32, LLaVA-1.5 7B, BLIP): we extract layer-wise features from 500 COCO images, inject Laplace noise scaled by 1/ϵ for ϵ ∈ { 0.1, 0.01 , 0.001 }, run both BUA+EMPA and TDA+EMPA per model (see Table V), and compare with Chi-square, K-L, and MMD. Table VII reports rMSE and EMPA (BUA) from our multi-seed eval-uation on CLIP ViT-B/32 and BLIP ResNet-50. EMPA attains stable bias ( ≈ 0.89 ) on both CLIP and BLIP, confirming that Bodhi VLM is effective for vision-tower budget-alignment assessment and complements training-time DP methods. 10 

TABLE VI POSITIONING TABLE : SCOPE AND CAPABILITY COMPARISON WITH RELATED PRIVACY -AWARE VISION /VLM METHODS . T HIS IS not A DIRECT QUANTITATIVE PERFORMANCE COMPARISON —V IP, DP-C AP , DP-MTV, AND VIS SHIELD ADDRESS DIFFERENT TASKS (TRAINING -TIME DP OR DE -IDENTIFICATION ). B ODHI VLM PROVIDES A PRIVACY -ALIGNMENT modeling FRAMEWORK FOR HIERARCHICAL REPRESENTATIONS .Method Approach Privacy Guarantee Compatible with Bodhi VLM assessment ViP [14] DP training (MAE+DP-SGD) (ε, δ )-DP Yes (vision features) DP-Cap [15] DP image captioning (ε, δ )-DP Yes (vision encoder) DP-MTV [16] DP multimodal in-context (ε, δ )-DP Partial (task vectors) VisShield [17] PHI de-identification None (masking) No (different paradigm) Bodhi VLM (Ours) Privacy-alignment modeling N/A (modeling framework) –

Fig. 6. Privacy budget vs. rMSE: TDA+EMPA and BUA+EMPA (COCO). 

Fig. 7. EMPA bias vs. privacy budget ϵ (log scale) from the synthetic ex-periments. BUA+EMPA and TDA+EMPA coincide on synthetic data because the grouping yields the same partition; Random partition+EMPA (baseline) uses a random sensitive/non-sensitive partition of the same size and shows higher bias, illustrating that structured grouping (BUA/TDA) yields a more stable budget-alignment signal than a blind baseline. 

C. Ablation Study 

We ablate Bodhi VLM by removing one component at a time: w/o BUA (use only TDA for grouping), w/o TDA (use only BUA), and w/o EMPA (use only BUA/TDA groups without EM-based assessment). Table VIII reports deviation and rMSE on PPDPTS (MOT20) and on COCO (Laplace noise, ϵ ∈ {0.1, 0.01 }). Point estimates shown; mean ±std over 5 random seeds and 95% confidence intervals are given in the supplementary materials (Table S3). The full pipeline achieves the best or tied-best rMSE; removing EMPA increases deviation from the ground-truth budget and degrades rMSE on budget alignment because the EM-based assessment is 

TABLE VII VLM VISION ENCODER METRICS FROM MULTI -SEED EXPERIMENTS 

(MEAN ± STD OVER 5 SEEDS ; DETAILED SUMMARIES AND CONFIDENCE INTERVALS ARE PROVIDED IN THE SUPPLEMENTARY MATERIALS ). RMSE  

> AND

EMPA (BUA) FOR CLIP V IT-B/32 AND BLIP R ES NET -50. Model rMSE EMPA (BUA) CLIP ε = 0 .01 0.058 ± 0.004 0.894 ± 0.000 

CLIP ε = 0 .1 0.059 ± 0.004 0.894 ± 0.000 

BLIP ε = 0 .01 1.32 ± 0.50 0.894 ± 0.000 

BLIP ε = 0 .1 1.51 ± 0.48 0.894 ± 0.000 

no longer available. Using only TDA (w/o BUA) or only BUA (w/o TDA) yields slightly higher rMSE than the full pipeline, as both strategies together provide more stable group structure across layers. Extended ablations (w/o MDAV, w/o NCP, threshold τi ∈ { 80% , 90% , 95% }, mixture components 

K ∈ { 2, 4, 8}, and random vs. structured grouping on real data) are left for future work. Membership inference attack (MIA) accuracy on TSDCRF vs. PPEDCRF is reported in the thesis; both sit above random (0.5), suggesting further work for stronger guarantees. 

D. Interpretability Analysis 

To illustrate how BUA and TDA localize privacy-relevant features and how EMPA bias relates to the budget, we provide the following analyses. (1) Layer-wise deviation: Figures 3 and 4 show deviation between original and noised features per sample (MOT20) under BUA and TDA; the curves reflect which layers or samples receive more noise and how the two strategies compare. (2) Budget vs. rMSE: Figure 6 plots privacy budget against rMSE for TDA+EMPA and BUA+EMPA (COCO), showing that EMPA tracks the de-clared budget more closely than PSNR or SIFT. (3) Sensitive vs. non-sensitive feature distribution: Figure 9 shows the distribution of noised feature values for the sensitive group 

G(s) 

> i

and the non-sensitive group G(n) 

> i

after BUA grouping (synthetic hierarchical features, one layer). The two histograms illustrate how BUA partitions the feature space and how noise is concentrated in the sensitive partition. (4) Feature-space visualization: Figure 10 gives a t-SNE (or PCA) projection of all layer-wise features, colored by BUA-derived sensitive vs. non-sensitive labels; the separation of the two groups in the embedding space reflects the discriminative power of the grouping for privacy-relevant regions. (5) VLM feature con-sistency: Table V and the deviation trends on CLIP, LLaVA, and BLIP (Section IV-B5) indicate that the proposed grouping-and-assessment pipeline transfers across heterogeneous visual 11 

Fig. 8. Left: rMSE and Chi-square (scaled ×10 −7) vs. ϵ (log scale) for contrast. Right: Chi-square, K-L divergence, and MMD vs. ϵ. The dual curves in the left panel allow comparison of rMSE (budget-alignment error) with the raw Chi-square statistic across budgets. TABLE VIII ABLATION STUDY OF BODHI VLM ON PPDPTS (MOT20) AND COCO. M EAN ±STD OVER 5 SEEDS ; 95% CI IN SUPPLEMENTARY TABLE S3. DEVIATION : MEAN |noised − original |; RMSE: BUDGET -ALIGNMENT ERROR W .R.T. THE LAPLACE REFERENCE UNDER THE SAME DECLARED ϵ. F ULL PIPELINE USES BUA AND TDA GROUPING PLUS EMPA. PPDPTS (MOT20) COCO (Laplace) Method Dev. ϵ=0 .1 Dev. ϵ=0 .01 Dev. ϵ=0 .1 Dev. ϵ=0 .01 rMSE ϵ=0 .1 rMSE ϵ=0 .01 

Bodhi VLM (full) 0.22 ±0.0 0.22 ±0.0 0.22 ±0.0 0.22 ±0.0 0.36 ±0.0 0.36 ±0.0 

w/o BUA (TDA only) 0.22 ±0.0 0.22 ±0.0 0.22 ±0.0 0.22 ±0.0 0.36 ±0.0 0.36 ±0.0 w/o TDA (BUA only) 0.22 ±0.0 0.22 ±0.0 0.22 ±0.0 0.22 ±0.0 0.36 ±0.0 0.36 ±0.0 w/o EMPA 0.22 ±0.0 0.22 ±0.0 0.22 ±0.0 0.22 ±0.0 0.6 ±0.01 0.61 ±0.01 

Fig. 9. Sensitive vs. non-sensitive feature distribution (BUA, layer 0; noised features). 

encoder architectures, including ViT-based and ResNet-based backbones; however, the present validation is limited to visual encoder features and does not yet evaluate full cross-modal fusion or language-generation pathways. 

E. Limitations 

We state the following limitations explicitly. (a) Reference-relative: The assessment is relative to an evaluator-defined reference mechanism Mref ; it does not certify alignment with the true deployed mechanism. If the true mechanism differs from Mref , the score reflects mechanism mismatch as well 

Fig. 10. t-SNE (or PCA) of layer-wise features colored by BUA grouping: sensitive ( G(s) 

> i

) vs. non-sensitive ( G(n) 

> i

). 

as budget alignment. (b) Partition dependence: The EMPA discrepancy may, in some settings (e.g., synthetic hierarchical data), reflect the induced group structure more strongly than the perturbation scale; it is then a structural discrepancy signal rather than a direct proxy for effective ϵ. (c) Bodhi VLM performs feature-level reference-perturbation auditing 12 

and does not provide formal end-to-end differential privacy certification. (d) Validation is on the vision tower of VLMs only, not full cross-modal fusion or language decoding. (e) The sensitive set S is human or rule-defined; BUA/TDA assume layer-wise feature accessibility. (f) Experiments use controlled additive perturbation; main tables report mean ±std over 5 seeds with 95% CI in the supplementary materials. V. C ONCLUSION 

We presented Bodhi VLM, a privacy-alignment modeling 

framework for hierarchical visual representations that com-bines BUA and TDA with an EMPA module. The output is 

reference-relative : we compare observed perturbed features to an evaluator-defined reference Mref and output a discrepancy score BAS ϵ (and weight-only Bias ϵ); we do not estimate effec-tive ϵ or certify end-to-end DP. We validated the framework on object detectors and on the vision encoders of VLMs (CLIP, LLaVA, BLIP) under controlled additive perturbations, with main results and ablation reported as mean ±std over 5 seeds and 95% CI in the supplementary materials. We compared with generic baselines (Chi-square, K-L, MMD) and task-relevant baselines (MomentReg, NoiseMLE, Wass-1), and contrasted scope with ViP, DP-Cap, DP-MTV, and VisShield (positioning only). Limitations include reference and partition dependence and vision-tower-only validation. Future work includes full VLM pipelines and decomposition experiments. REFERENCES [1] A. Radford, J. W. Kim, C. Hallacy, A. Ramesh, G. Goh, S. Agarwal, G. Sastry, A. Askell, P. Mishkin, J. Clark et al. , “Learning transferable visual models from natural language supervision,” ICML , 2021. [2] H. Liu, C. Li, Q. Wu, and Y. J. Lee, “Visual instruction tuning,” NeurIPS ,2023. [3] C. Dwork, F. McSherry, K. Nissim, and A. Smith, “Calibrating noise to sensitivity in private data analysis,” in TCC , 2006. [4] A. Narayanan, “Rethinking privacy in the age of machine learning,” 

Communications of the ACM , 2021. [5] M. Jagielski et al. , “Differentially private machine learning,” arXiv ,2022. [6] B. Hitaj, G. Ateniese, and F. P´ erez-Cruz, “Deep models under the gan: information leakage from collaborative deep learning,” in CCS , 2017. [7] R. Agrawal and C. C. Aggarwal, “On the design and quantification of privacy preserving data mining algorithms,” PODS , 2001. [8] M. Abadi, A. Chu, I. Goodfellow, H. B. McMahan, I. Mironov, K. Tal-war, and L. Zhang, “Deep learning with differential privacy,” CCS , 2016. [9] H. B. McMahan, E. Moore, D. Ramage, S. Hampson, and B. A. y Arcas, “Communication-efficient learning of deep networks from decentralized data,” AISTATS , 2017. [10] T.-Y. Lin, P. Doll´ ar, R. Girshick, K. He, B. Hariharan, and S. Belongie, “Feature pyramid networks for object detection,” in CVPR , 2017. [11] J. Li, D. Li, C. Xiong, and S. Hoi, “Blip: Bootstrapping language-image pre-training with unified vision-language understanding and generation,” 

ICML , 2022. [12] J. Wang et al. , “Qwen2-vl: Enhancing vision-language model’s percep-tion of the world at any resolution,” arXiv , 2024. [13] Z. Chen et al. , “Internvl: Scaling up vision foundation models with aligned multimodal training,” arXiv , 2024. [14] X. Yu et al. , “Vip: Differentially private vision language models,” arXiv ,2024. [15] T. Sander et al. , “Dp-cap: Differentially private image captioning,” arXiv ,2024. [16] I. Ngong et al. , “Dp-mtv: Differentially private multimodal in-context learning,” arXiv , 2026. [17] Y. Chen et al. , “Visshield: De-identification for medical images with vlms,” arXiv , 2025. [18] H. Hu et al. , “Membership inference on vision-language models,” arXiv ,2025. [19] A. P. Dempster, N. M. Laird, and D. B. Rubin, “Maximum likelihood from incomplete data via the em algorithm,” Journal of the Royal Statistical Society Series B , vol. 39, no. 1, pp. 1–38, 1977. [20] C. F. J. Wu, “On the convergence properties of the em algorithm,” Annals of Statistics , 1983. [21] K. Greff et al. , “Neural expectation maximization,” NeurIPS , 2017. [22] J. Soria-Comas and J. Domingo-Ferrer, “Enhancing data utility in microaggregation for privacy preservation,” Trust and Privacy in Digital Business , 2014. [23] A. Machanavajjhala, D. Kifer, J. Gehrke, and M. Venkitasubramaniam, “l-diversity: Privacy beyond k-anonymity,” ACM TKDD , 2007. [24] P. Dendorfer, H. Rezatofighi, A. Milan, J. Shi, D. Cremers, I. Reid, S. Roth, K. Schindler, and L. Leal-Taix´ e, “Mot20: A benchmark for multi object tracking in crowded scenes,” arXiv , 2020. [25] T.-Y. Lin, M. Maire, S. Belongie, J. Hays, P. Perona, D. Ramanan, P. Doll´ ar, and C. L. Zitnick, “Microsoft coco: Common objects in context,” in ECCV , 2014. [26] C. Cruz et al. , “Scale-invariant feature transform,” Encyclopedia of Computer Vision , 2012. [27] R. Sara et al. , “Image quality metrics for privacy-preserving mecha-nisms,” Pattern Recognition , 2019. [28] A. Bochkovskiy, C.-Y. Wang, and H.-Y. M. Liao, “Yolov4: Optimal speed and accuracy of object detection,” arXiv , 2020. [29] N. Carion, F. Massa, G. Synnaeve, N. Usunier, A. Kirillov, and S. Zagoruyko, “End-to-end object detection with transformers,” in 

ECCV , 2020. 13 

APPENDIX 

A. Table S1: Synthetic hierarchical features 

Task-relevant budget-alignment baselines on the synthetic hierarchical feature protocol (4 layers, 200 samples, 8-D per layer, 30% sensitive ratio; 5 seeds). MomentReg: rMSE of predicted ϵ from feature moments. NoiseMLE: mean rMSE (over ϵ) of MLE-estimated ϵ under the evaluator-specified additive noise family. Wass-1: first-order Wasserstein distance between perturbed and reference sensitive-feature distributions (mean ±std over 5 seeds). Baseline ϵ = 0 .1 ϵ = 0 .01 

MomentReg (rMSE) 2.12 × 10 −5 (over all ϵ)NoiseMLE (rMSE, mean over ϵ) 0.055 Wass-1 (mean ±std) 1.94 ± 0.13 23 .24 ± 1.59 

B. Table S2: Detector backbones — Wass-1 (mean ±std, 95% CI) 

Wass-1 (first-order Wasserstein distance) between original and noised sensitive-feature distributions on detector back-bones, mean ±std over 5 seeds; 95% CI in parentheses. Model ϵ = 0 .1 ϵ = 0 .01 

MDCRF 0.0334 ± 0.00078 (0.0327–0.0341) 0.0335 ± 0.00076 (0.0329–0.0342) PPDPTS 0.0334 ± 0.00078 (0.0327–0.0341) 0.0335 ± 0.00076 (0.0329–0.0342) DETR 0.0893 ± 0.00057 (0.0888–0.0898) 0.0907 ± 0.00060 (0.0902–0.0913) Component ablation (full pipeline vs. w/o BUA, w/o TDA, w/o EMPA) on detector (DETR). Mean ±std over 5 random seeds; 95% CI in parentheses. Under this protocol, the de-viation and rMSE at ϵ=0 .1 and ϵ=0 .01 coincide up to two decimal places for each configuration, so we report a single shared value per method. Method Dev (shared for ϵ ∈ { 0.1, 0.01 }) rMSE (shared for ϵ ∈ { 0.1, 0.01 })Bodhi VLM (full) 0.22 ± 0.00 (0.22–0.22) 0.36 ± 0.00 (0.36–0.37) w/o BUA (TDA only) 0.22 ± 0.00 (0.22–0.22) 0.36 ± 0.00 (0.36–0.37) w/o TDA (BUA only) 0.22 ± 0.00 (0.22–0.22) 0.36 ± 0.00 (0.36–0.37) w/o EMPA 0.22 ± 0.00 (0.22–0.22) 0.61 ± 0.01 (0.60–0.62)