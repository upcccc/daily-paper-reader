---
title: Optimal partition selection with Rényi differential privacy
title_zh: 基于 Rényi 差分隐私的最优分区选择
authors: "Charlie Harrison, Pasin Pasin Manurangsi"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09167v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: Rényi差分隐私下的最优算法
tldr: 本文研究差分隐私中的分区选择问题，旨在满足隐私约束的同时最大化发布的分区数量。研究者将单分区用户的最优算法推广至近似 Rényi 差分隐私（RDP）框架，实现了更紧致的组合分析。针对多分区用户，提出了一种适用于 L2 有界加权选择的改进算法，可作为高斯机制的优选替代。实验表明，该方法显著提升了 PolicyGaussian 和 MAD2R 等前沿算法的性能，并揭示了同时发布分区及其频率时的性能权衡与成本。
selection_source: fresh_fetch
motivation: 旨在解决在差分隐私约束下，如何更有效地从海量数据中筛选并发布尽可能多的分区集合，同时优化隐私组合分析的紧致性。
method: 将单分区最优算法推广至近似 RDP 框架，并针对多分区场景设计了 L2 有界加权分区选择算法，以改进传统的高斯机制。
result: 该算法成功集成到 PolicyGaussian 和 MAD2R 等最先进框架中，在并行和顺序自适应算法上均实现了性能提升。
conclusion: 研究证明了同时发布分区及其频率存在固有成本，并量化了加性噪声与非加性噪声机制在分区选择问题上的性能差距。
---

## 摘要
隐私数据分析中的一个常见问题是分区选择问题，其中每个用户持有一组来自可能无限集合的分区（例如 GROUP BY 操作中的键）。这里的挑战在于在遵守差分隐私约束的同时，最大化发布的分区集合。先前的研究 [Desfontaines et al., PoPETS 2022] 提出了一种在每个用户仅提交单个分区时的最优 (ε, δ)-DP 算法。我们将该方法推广到 δ-近似 (α, ε)-Rényi 差分隐私 (RDP) 下，以寻找最优算法，这在组合情况下允许更紧致的分析。针对每个用户提交多个分区的情况下尚不存在通用最优性结果这一现状，我们提出了一种针对 L² 有界加权分区选择进行调整的最优算法扩展，只要不需要分区频率，该算法就可以作为高斯机制的直接替代改进方案。我们展示了我们的原语可以轻松插入到最先进的分区选择算法中（如 [Gopi et al., ICML 2020] 的 PolicyGaussian 和 [Chen et al., ICML 2025] 的 MAD2R），从而提高并行和顺序自适应算法的性能。最后，我们证明了对于支持同时发布频率和分区的算法，存在一种固有的代价。具体而言，我们为使用加性噪声的分区选择制定了最优近似 RDP 算法的基本概念，并证明了在该问题中加性噪声机制与非加性噪声机制之间存在数值差异。

## Abstract
A common problem in private data analysis is the partition selection problem, where each user holds a set of partitions (e.g. keys in a GROUP BY operation) from a possibly unbounded set. The challenge here is in maximizing the set of released partitions while respecting a differential privacy constraint. Previous work [Desfontaines et al., PoPETS 2022] presented an optimal $(\varepsilon, δ)$-DP algorithm when each user submits only a single partition. We generalize this approach to find the optimal algorithm under $δ$-approximate $(α, \varepsilon)$-Rényi differential privacy (RDP), which allows much tighter analysis under composition. Motivated by the non-existence of a general optimality result in the case where users submit multiple partitions each, we present an extension of our optimal algorithm tuned for $L^2$ bounded weighted partition selection which can be used as a drop-in improvement over the Gaussian mechanism any time the partition frequency is not also needed. We show that our primitive can be easily plugged into state of the art partition selection algorithms (PolicyGaussian from [Gopi et al., ICML 2020] and MAD2R from [Chen et al., ICML 2025]), improving performance both for parallel and sequential adaptive algorithms. Finally, we show that there is an inherent cost to algorithms which do support releasing the frequency as well as the partitions. Specifically, we formulate a basic notion of optimal approximate RDP algorithm for partition selection using additive noise, and show that there is a numerical separation between additive and non-additive noise mechanisms for this problem.

---

## 论文详细总结（自动生成）

### 论文总结：基于 Rényi 差分隐私的最优分区选择

#### 1. 核心问题与整体含义
在隐私数据分析中，“分区选择”（Partition Selection）是一个基础问题，即在满足差分隐私（DP）的前提下，从可能无限的候选集中识别并发布数据集中存在的“键”（如 SQL 中的 `GROUP BY` 键）。
*   **研究动机**：先前的最优算法仅针对标准的 $(\epsilon, \delta)$-DP 且假设每个用户只贡献一个分区。然而，在实际应用中，隐私预算通常需要通过多次查询的组合来管理，而 Rényi 差分隐私（RDP）在处理组合分析时比标准 DP 更加紧致。
*   **核心目标**：将最优分区选择理论推广到近似 RDP 框架下，并解决用户贡献多个分区或带有权重时的实用性挑战。

#### 2. 方法论
论文提出了多项技术创新：
*   **RDP 下的最优原语 ($\pi^*$)**：通过递归公式定义了在 $\delta$-近似 $(\alpha, \epsilon)$-RDP 约束下的最优发布概率函数。当 $\alpha \to \infty$ 时，该算法退化为先前的 $(\epsilon, \delta)$-DP 最优算法。
*   **SNAPS 机制（平滑范数感知分区选择）**：针对用户持有多个分区且满足 $L_r$ 范数限制的情况，设计了一种加权分区选择原语。它允许根据用户贡献的权重提供“平滑”的隐私损失，可作为高斯机制的直接替代品。
*   **快速计算算法**：提出了一种基于“注水法”（Water-filling）的 $O(n \log n)$ 算法，用于数值计算离散分布之间的近似 Rényi 散度。
*   **加性噪声的最优性分析**：通过凸优化程序寻找最优的加性噪声分布，并证明了在 RDP 框架下，加性噪声机制（如拉普拉斯或高斯）与非加性最优机制之间存在性能差距。

#### 3. 实验设计
*   **数据集**：使用了 6 个常用的 NLP 和社交媒体数据集：Reddit（论坛帖子）、Wiki（维基百科摘要）、Twitter（客服推文）、Finance（财经新闻）、Amazon（评论）和 IMDb（电影评论）。
*   **对比基准（Benchmarks）**：
    *   **PolicyGaussian**：一种经典的顺序自适应分区选择算法。
    *   **MAD2R**：一种最先进的、可高度并行的自适应加权算法。
*   **实验场景**：将上述算法中的高斯机制替换为本文提出的 SNAPS 机制，在相同的隐私预算（$\epsilon=1, \delta=10^{-5}$）和贡献限制（$\Delta_0=100$）下对比输出的分区数量。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练/运行耗时。
*   由于分区选择算法主要涉及数值计算和概率采样，通常不需要大规模 GPU 集群，普通的 CPU 服务器即可完成模拟实验。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个不同领域、不同规模的数据集。
*   **重复性**：每组实验均进行了 5 次独立试验，并报告了均值和标准差。
*   **充分性**：实验对比了并行（MAD2R）和顺序（PolicyGaussian）两种主流机制，验证了 SNAPS 作为“插件”的通用性。实验设计客观，通过控制变量法（仅替换核心选择原语）公平地展示了性能提升。

#### 6. 主要结论与发现
*   **性能提升**：在所有测试数据集中，使用 SNAPS 机制替代高斯机制后，发布的分区数量提升了 **10% 到 20%**。
*   **最优性边界**：证明了当用户贡献多个分区时，不存在单一的通用最优机制。
*   **发布频率的代价**：揭示了“同时发布分区及其频率”存在固有成本。如果只需要发布分区集合而不需要精确计数，非加性噪声机制（如本文方法）显著优于传统的加性噪声机制。

#### 7. 优点
*   **理论严谨**：将分区选择的最优性理论从标准 DP 扩展到了更现代、更强大的 RDP 框架。
*   **实用性强**：SNAPS 机制可以作为现有成熟框架（如 Google 或其他公司的隐私 SQL 引擎）中高斯机制的“掉入式”（drop-in）改进方案。
*   **数值工具创新**：提供的注水算法和凸优化方案为后续研究 RDP 下的最优机制提供了有力工具。

#### 8. 不足与局限
*   **功能限制**：该最优机制不支持同时发布分区的频率（Count），这在某些需要聚合统计的场景下受限。
*   **参数敏感性**：SNAPS 的性能依赖于超参数（如 $\alpha, \epsilon_0, \delta_0$）的选择，论文中未提供自动化的超参数优化方案。
*   **适用范围**：主要针对 $L_r$ 灵敏度设置，对于更复杂的邻域定义（如图数据隐私）可能需要进一步调整。

（完）
