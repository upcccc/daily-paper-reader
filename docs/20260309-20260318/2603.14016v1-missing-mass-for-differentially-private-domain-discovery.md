---
title: Missing Mass for Differentially Private Domain Discovery
title_zh: 差分隐私域发现中的缺失质量
authors: "Travis Dick, Matthew Joseph, Vinod Raman"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14016v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 使用加权高斯机制的差分隐私领域发现
tldr: 本研究探讨了差分隐私下的领域发现问题，即在未知全集的情况下识别用户持有的项目。研究证明了加权高斯机制（WGM）在集合并集问题中具有近乎最优的遗漏质量保证，包括针对Zipf数据的l1保证和无分布的linf保证。通过将WGM作为前置步骤，研究还改进了私有Top-k和k-命中集算法，实验证明该方法在多个任务上均优于现有基准。
selection_source: fresh_fetch
motivation: 在差分隐私保护下，如何从用户持有的未知数据域中高效且准确地发现信息丰富的项目子集是一个关键挑战。
method: 提出并分析了加权高斯机制（WGM），并将其作为一种通用的领域发现预处理步骤应用于多种私有算法。
result: 理论上证明了WGM在Zipf分布下的近优性，实验上显示其在集合并集、Top-k和k-命中集任务中均表现优异。
conclusion: WGM是一种简单且强大的差分隐私领域发现工具，能有效提升未知领域下各类私有统计任务的效用。
---

## 摘要
我们研究了差分隐私域发现中的若干问题。在这些问题中，每个用户持有来自一个共享但未知域的项子集，目标是输出一个具有信息量的项子集。对于集合并集问题，我们证明了简单的基准方法——加权高斯机制（WGM）在 Zipf 分布数据上具有近乎最优的 $\ell_1$ 缺失质量保证，并具有无分布限制的 $\ell_\infty$ 缺失质量保证。随后，我们将 WGM 作为域发现的前置步骤，应用于现有的已知域隐私 top-$k$ 和 $k$-命中集算法，并为其未知域变体获得了新的效用保证。最后，实验表明，我们所有基于 WGM 的方法在上述三个问题上均与现有基准方法相当或更优。

## Abstract
We study several problems in differentially private domain discovery, where each user holds a subset of items from a shared but unknown domain, and the goal is to output an informative subset of items. For set union, we show that the simple baseline Weighted Gaussian Mechanism (WGM) has a near-optimal $\ell_1$ missing mass guarantee on Zipfian data as well as a distribution-free $\ell_\infty$ missing mass guarantee. We then apply the WGM as a domain-discovery precursor for existing known-domain algorithms for private top-$k$ and $k$-hitting set and obtain new utility guarantees for their unknown domain variants. Finally, experiments demonstrate that all of our WGM-based methods are competitive with or outperform existing baselines for all three problems.

---

## 论文详细总结（自动生成）

这篇论文《Missing Mass for Differentially Private Domain Discovery》深入探讨了在差分隐私（DP）保护下，如何从未知且可能极大的数据域中发现信息丰富的项。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在现代数据分析中，数据域（如搜索查询、购买历史）往往是先验未知的或规模巨大。在应用差分隐私时，传统的“已知域”算法无法直接使用。
*   **核心问题**：如何在保护用户隐私的前提下，从未知域中识别出高频项（即“域发现”）。
*   **关键指标**：论文引入了**缺失质量（Missing Mass, MM）**的概念，即未被算法输出的项在原始数据中所占的频率比例。相比于传统的项数量（基数），缺失质量能更客观地衡量发现结果的效用。

### 2. 方法论
论文的核心思想是验证并扩展了**加权高斯机制（Weighted Gaussian Mechanism, WGM）**在域发现任务中的应用。
*   **WGM 算法流程**：
    1.  **限制贡献**：每个用户随机采样最多 $\Delta_0$ 个项。
    2.  **加权直方图**：为每个项计算加权计数，权重设为 $1/\sqrt{|fW_i|}$（其中 $|fW_i|$ 是用户贡献的项数），这种权重设计能平衡不同贡献量的用户。
    3.  **添加噪声**：对加权计数添加均值为 0、标准差为 $\sigma$ 的高斯噪声。
    4.  **阈值过滤**：输出噪声计数超过阈值 $T$ 的项。
*   **理论贡献**：
    *   证明了 WGM 在 **Zipf 分布**（幂律分布）数据上具有近乎最优的 $\ell_1$ 缺失质量保证。
    *   证明了 WGM 具有**无分布限制**的 $\ell_\infty$ 缺失质量保证（即最大遗漏项的频率上界）。
*   **元算法（Meta-Algorithm）**：提出一种通用框架，先利用 WGM 发现一个候选域 $D$，再在 $D$ 上运行已有的已知域隐私算法（如用于 Top-k 的 Peeling Exponential 机制或用于 k-命中集的 User Peeling 机制）。

### 3. 实验设计
*   **数据集**：使用了 6 个真实世界数据集，分为两类：
    *   **大型数据集**：Reddit（文本令牌）、Amazon Games（评论）、Movie Reviews（电影评分）。
    *   **小型数据集**：Steam Games、Amazon Magazine、Amazon Pantry。
*   **任务场景**：集合并集（Set Union）、Top-k 选择、k-命中集（k-Hitting Set）。
*   **对比方法（Benchmarks）**：
    *   **集合并集**：对比了 Policy Gaussian 和 Policy Greedy 机制。
    *   **Top-k**：对比了 Durfee & Rogers (2019) 的有限域 Top-k 算法。
    *   **k-命中集**：对比了非隐私贪婪算法和假设域已知的隐私贪婪算法。

### 4. 资源与算力
*   **说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   **分析**：由于 WGM 和相关的 Gumbel 采样算法主要涉及直方图统计和加噪，其计算复杂度较低，通常在标准 CPU 服务器或单机环境下即可高效运行，不需要大规模 GPU 算力。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同的统计任务，在 6 个跨领域的真实数据集上进行了测试。
*   **参数覆盖**：对隐私预算 $\epsilon$（1.0 和 0.1）、用户贡献界限 $\Delta_0$、输出数量 $k$ 等关键超参数进行了详细的消融实验。
*   **客观性**：每组实验均进行了 5 次独立重复实验并标注了标准差（误差棒），实验设计较为客观、公平，涵盖了从重尾分布到稀疏分布的多种数据特征。

### 6 主要结论与发现
*   **WGM 的高效性**：简单的 WGM 在缺失质量指标上表现极佳，在 Zipf 数据上达到了理论上的近优性。
*   **性能优越**：在集合并集任务中，WGM 的表现与更复杂的顺序策略机制（如 Policy Greedy）相当，但计算开销更小。
*   **未知域扩展成功**：通过 WGM 预处理发现域，再结合已知域算法，在 Top-k 和 k-命中集任务中均显著优于现有的专门针对未知域设计的基准方法。
*   **理论与实践一致**：实验结果验证了 $\ell_\infty$ 缺失质量界限在指导 Top-k 选择中的有效性。

### 7. 优点
*   **理论完备性**：提供了首个关于差分隐私集合并集算法的**绝对效用保证**（而非相对保证）。
*   **简洁且可扩展**：WGM 算法简单、易于在大规模分布式系统中实现（如 Google 的 Plume 框架），且能作为其他复杂任务的通用预处理步骤。
*   **指标创新**：提倡使用“缺失质量”而非“项数量”作为衡量标准，更符合实际业务中关注“覆盖了多少流量”的需求。

### 8. 不足与局限
*   **理论间隙**：在 Top-k 和 k-命中集问题上，论文给出的上界与下界之间仍存在一定差距，尚未完全闭合。
*   **分布依赖**：$\ell_1$ 缺失质量的强保证依赖于数据符合 Zipf 分布的假设，虽然现实数据多符合此特征，但在极端非典型分布下的表现可能受限。
*   **采样策略**：目前采用的是简单的均匀无放回采样来限制用户贡献，未来可能需要更复杂的自适应采样策略（如 Adaptive Weighting）来进一步优化。

（完）
