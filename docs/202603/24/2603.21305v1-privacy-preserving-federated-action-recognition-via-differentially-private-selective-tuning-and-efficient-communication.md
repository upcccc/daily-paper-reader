---
title: Privacy-Preserving Federated Action Recognition via Differentially Private Selective Tuning and Efficient Communication
title_zh: 通过差分隐私选择性微调与高效通信实现隐私保护的联邦动作识别
authors: "Idris Zakariyya, Pai Chet Ng, Kaushik Bhargav Sivangi, S. Mohammad Sheikholeslami, Konstantinos N. Plataniotis, Fani Deligianni"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21305v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 针对动作识别的联邦差分隐私
tldr: "针对联邦视频动作识别中的隐私泄露和通信开销挑战，本文提出FedDP-STECAR框架。该方法结合差分隐私（DP）与选择性微调技术，仅对少量任务相关层进行扰动和更新，在降低信息泄露风险的同时，将通信流量减少了99%以上。实验证明，该方法在严格隐私保护下显著提升了模型精度，并大幅加快了训练速度，为大规模隐私保护视频识别提供了高效方案。"
selection_source: fresh_fetch
motivation: 联邦学习中的梯度交换存在隐私泄露风险，且高维视频模型全量同步带来了巨大的通信带宽成本。
method: 提出FedDP-STECAR框架，通过差分隐私仅对一小部分任务相关层进行选择性微调和参数扰动，并仅在聚合时传输这些层。
result: "在UCF-101数据集上，该方法在严格隐私预算下精度提升高达70.2%，通信量减少超99%，训练速度提升48%。"
conclusion: 该研究通过选择性微调与差分隐私的结合，有效解决了联邦视频识别在隐私保护、模型精度与通信效率之间的权衡难题。
---

## 摘要
联邦视频动作识别支持在不共享原始视频数据的情况下进行协作模型训练，但仍面临两个关键挑战：模型暴露（model exposure）和通信开销（communication overhead）。客户端与服务器之间交换的梯度可能会泄露私密的运动模式，而高维视频网络的全模型同步会导致巨大的带宽和通信成本。为了解决这些问题，我们提出了“基于选择性微调与高效通信的联邦差分隐私动作识别框架”，即 FedDP-STECAR。我们的 FedDP-STECAR 框架在差分隐私（DP）约束下，仅选择性地微调和扰动一小部分与任务相关的层，在减少信息泄露面的同时，保持了视频特征的时间相干性。通过在聚合过程中仅传输微调后的层，与全模型更新相比，通信流量减少了 99% 以上。在 UCF-101 数据集上使用 MViT-B-16x4 Transformer 进行的实验表明，FedDP-STECAR 在集中式设置下的严格隐私保护（ε=0.65）下，准确率提升高达 70.2%；在联邦设置下，训练速度加快了 48%，准确率达到 73.1%，从而实现了可扩展且隐私保护的视频动作识别。代码可在 https://github.com/izakariyya/mvit-federated-videodp 获取。

## Abstract
Federated video action recognition enables collaborative model training without sharing raw video data, yet remains vulnerable to two key challenges: \textit{model exposure} and \textit{communication overhead}. Gradients exchanged between clients and the server can leak private motion patterns, while full-model synchronization of high-dimensional video networks causes significant bandwidth and communication costs. To address these issues, we propose \textit{Federated Differential Privacy with Selective Tuning and Efficient Communication for Action Recognition}, namely \textit{FedDP-STECAR}. Our \textit{FedDP-STECAR} framework selectively fine-tunes and perturbs only a small subset of task-relevant layers under Differential Privacy (DP), reducing the surface of information leakage while preserving temporal coherence in video features. By transmitting only the tuned layers during aggregation, communication traffic is reduced by over 99\% compared to full-model updates. Experiments on the UCF-101 dataset using the MViT-B-16x4 transformer show that \textit{FedDP-STECAR} achieves up to \textbf{70.2\% higher accuracy} under strict privacy ($ε=0.65$) in centralized settings and \textbf{48\% faster training} with \textbf{73.1\% accuracy} in federated setups, enabling scalable and privacy-preserving video action recognition. Code available at https://github.com/izakariyya/mvit-federated-videodp

---

## 论文详细总结（自动生成）

以下是对论文《Privacy-Preserving Federated Action Recognition via Differentially Private Selective Tuning and Efficient Communication》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在联邦学习（FL）框架下进行视频动作识别时，面临**模型暴露（隐私泄露）**和**通信开销巨大**两大挑战。
*   **研究动机**：
    *   **隐私风险**：客户端与服务器交换的梯度可能被恶意恢复出原始视频内容或运动模式。
    *   **效率瓶颈**：视频识别模型（如Transformer）参数量巨大，全量同步会导致严重的带宽饱和与延迟。
    *   **DP的副作用**：传统的差分隐私（DP）在保护隐私时往往会导致模型精度大幅下降，且在视频这种高维时空数据上表现尤为不稳定。

### 2. 方法论：FedDP-STECAR 框架
该框架通过结合选择性微调、差分隐私和高效通信协议来解决上述问题：
*   **选择性微调（Selective Tuning）**：将模型参数分为冻结层（$\theta_f$）和可训练层（$\theta_t$）。仅对任务相关的关键层（如分类头和最后的Transformer阶段）进行更新。
*   **差分隐私保护（DP-SGD）**：仅在可训练层 $\theta_t$ 上应用梯度裁剪和高斯噪声扰动。这缩小了噪声传播范围，保护了视频特征的时间相干性。
*   **顶层采样（Top-Level Sampling, TLS）**：在本地训练中使用“无放回采样”策略。该技术不仅能通过采样放大隐私保护强度（Privacy Amplification），还能保持批次内的时空多样性。
*   **高效通信聚合**：客户端仅向服务器发送经过掩码处理的 $\theta_t$ 更新。由于 $\theta_t$ 远小于全量参数，通信量显著降低。
*   **聚合不可知性**：该框架可无缝集成到 FedAvg 或 FedNova 等多种联邦聚合算法中。

### 3. 实验设计
*   **数据集**：使用 **UCF-101** 动作识别数据集（包含101类动作，按80/20比例划分训练/测试集）。
*   **基础模型（Benchmark）**：预训练的 **MViT-B-16x4**（多尺度视觉 Transformer）。
*   **对比方法**：
    *   **全量微调（Full Fine-tuning, FT）** vs. **选择性微调（Selective Tuning, Sel）**。
    *   **有无 TLS 采样**的对比。
    *   **不同隐私预算（$\epsilon$）**下的性能表现。
    *   **不同联邦优化器**（FedAvg, FedNova）的对比。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **训练时长/效率指标**：文中提供了详细的运行时间对比。例如，在联邦设置下，选择性微调的运行时间约为 179 分钟，而全量微调需要 339 分钟；通信延迟从 174.72 秒降至 0.37 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **集中式实验**：测试了 $\epsilon$ 从 0.65 到 2.0 变化时的精度。
    *   **联邦实验**：涵盖了 2 到 20 个客户端、10 到 20 轮通信的多种组合。
    *   **消融实验**：验证了选择性微调和 TLS 对隐私-效用权衡的贡献。
*   **充分性评价**：实验设计较为客观，通过对比全量更新和选择性更新在相同隐私预算下的表现，清晰地展示了该方法的优势。但实验仅限于单一数据集（UCF-101），在跨数据集泛化性方面尚有提升空间。

### 6. 主要结论与发现
*   **精度提升**：在严格隐私保护（$\epsilon=0.65$）下，选择性微调比全量微调的准确率高出 **70.2%**。
*   **通信效率**：通信流量减少了 **99.8%**（从 1456 MB 降至 3.1 MB），实现了约 **470 倍**的加速。
*   **可扩展性**：在 20 个客户端的联邦环境下，即使在严格的隐私约束下，模型仍能保持 72% 以上的准确率，而全量微调方法则会崩溃（低于 20%）。

### 7. 优点
*   **多目标优化**：同时解决了隐私、精度和通信效率三个核心痛点。
*   **实用性强**：通过减少 99% 的通信量，使大规模视频模型在带宽受限的边缘设备上进行联邦学习成为可能。
*   **隐私放大**：TLS 采样的引入在不增加噪声的前提下增强了理论隐私保证。

### 8. 不足与局限
*   **数据集单一**：仅在 UCF-101 上进行了验证，未在更大规模（如 Kinetics）或更复杂的非独立同分布（Non-IID）场景下深入探讨。
*   **依赖预训练**：该方法高度依赖于在大型数据集上预训练好的模型特征，对于从零开始训练的场景可能不适用。
*   **客户端规模**：实验中的客户端数量（最高 20 个）相对较小，尚未验证在成千上万个超大规模客户端节点下的收敛稳定性。

（完）
