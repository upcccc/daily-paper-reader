---
title: "PAC-DP: Personalized Adaptive Clipping for Differentially Private Federated Learning"
title_zh: PAC-DP：面向差分隐私联邦学习的个性化自适应裁剪
authors: "Hao Zhou, Siqi Cai, Hua Dai, Geng Yang, Jing Luo, Hui Cai"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24003v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 联邦学习中针对记录级本地差分隐私的个性化自适应裁剪
tldr: 针对联邦学习中传统差分隐私方法使用固定裁剪阈值导致性能受损的问题，本文提出PAC-DP框架。该框架通过模拟曲线拟合方法，利用服务器端的公共代理数据集学习个性化隐私预算与裁剪阈值之间的映射关系，实现了预算感知的自适应阈值选择。实验证明，该方法在保证隐私的同时，显著提升了模型精度和收敛速度。
selection_source: fresh_fetch
motivation: 传统的固定梯度裁剪阈值忽略了客户端的异构性和不同的隐私敏感度，导致隐私与效用之间的权衡不佳。
method: 提出一种基于模拟曲线拟合的框架，利用服务器端的公共代理数据集建立个性化隐私预算与最优裁剪阈值的映射模型。
result: "在多个基准测试中，PAC-DP相比固定阈值方法提升了高达26%的准确率，并加快了45.5%的收敛速度。"
conclusion: PAC-DP通过个性化自适应裁剪有效解决了联邦学习中的异构隐私需求问题，在提升模型效用的同时保证了严谨的隐私保护。
---

## 摘要
差分隐私 (DP) 对于保护联邦学习 (FL) 中的敏感客户端信息至关重要，然而传统的 DP-FL 方法主要依赖于固定的梯度裁剪阈值。这种静态裁剪忽略了显著的客户端异构性和不同的隐私敏感度，可能导致不利的隐私-效用权衡。在本文中，我们提出了 PAC-DP，一种在记录级本地差分隐私下的联邦学习个性化自适应裁剪框架。PAC-DP 引入了一种“模拟-曲线拟合” (Simulation-CurveFitting) 方法，利用服务器托管的公共代理数据集来学习个性化隐私预算 epsilon 与梯度裁剪阈值 C 之间的有效映射，随后通过轻量级的逐轮调度进行在线部署。这种设计实现了基于预算条件的阈值选择，同时避免了训练过程中的数据依赖调优。我们提供了理论分析，确立了在样本级裁剪和高斯扰动机制下的收敛保证，以及可重复的隐私核算程序。在多个 FL 基准测试上的广泛评估表明，在相同的隐私预算下，PAC-DP 优于传统的固定阈值方法，在我们的评估设置中，准确率提升了高达 26%，收敛速度加快了高达 45.5%。

## Abstract
Differential privacy (DP) is crucial for safeguarding sensitive client information in federated learning (FL), yet traditional DP-FL methods rely predominantly on fixed gradient clipping thresholds. Such static clipping neglects significant client heterogeneity and varying privacy sensitivities, which may lead to an unfavorable privacy-utility trade-off. In this paper, we propose PAC-DP, a Personalized Adaptive Clipping framework for federated learning under record-level local differential privacy. PAC-DP introduces a Simulation-CurveFitting approach leveraging a server-hosted public proxy dataset to learn an effective mapping between personalized privacy budgets epsilon and gradient clipping thresholds C, which is then deployed online with a lightweight round-wise schedule. This design enables budget-conditioned threshold selection while avoiding data-dependent tuning during training. We provide theoretical analyses establishing convergence guarantees under the per-example clipping and Gaussian perturbation mechanism and a reproducible privacy accounting procedure. Extensive evaluations on multiple FL benchmarks show that PAC-DP surpasses conventional fixed-threshold approaches under matched privacy budgets, improving accuracy by up to 26% and accelerating convergence by up to 45.5% in our evaluated settings.

---

## 论文详细总结（自动生成）

以下是对论文《PAC-DP: Personalized Adaptive Clipping for Differentially Private Federated Learning》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：在差分隐私联邦学习（DP-FL）中，梯度裁剪阈值 $C$ 的选择至关重要。传统的固定裁剪阈值方法忽略了客户端数据的异构性（Non-IID）和用户对隐私敏感度的差异（个性化隐私预算 $\epsilon$）。
*   **核心挑战**：静态裁剪会导致“一刀切”的问题——阈值过大会引入过多噪声，过小则会导致梯度信息丢失严重，从而在隐私和模型效用（Utility）之间难以取得平衡。
*   **研究目标**：设计一种能够根据客户端个性化隐私预算动态调整裁剪阈值的自适应框架。

### 2. 方法论：PAC-DP 框架
PAC-DP 核心思想是建立隐私预算 $\epsilon$ 与最优裁剪阈值 $C^*$ 之间的映射关系。
*   **离线阶段（模拟与曲线拟合）**：
    *   服务器利用公共代理数据集（Proxy Dataset）模拟不同 $(\epsilon, C)$ 配置下的训练过程。
    *   记录各配置下的模型表现，确定每个 $\epsilon$ 对应的最优 $C^*$。
    *   使用多项式回归（如二次拟合 $F(\epsilon) = \alpha\epsilon^2 + \beta\epsilon + \gamma$）学习映射函数 $F$。
*   **在线阶段（个性化自适应训练）**：
    *   **阈值分配**：客户端 $i$ 根据其个性化预算 $\epsilon_i$，通过 $C_{ti} = F(\epsilon_i) \cdot \lambda(t)$ 计算当前轮次的裁剪阈值。
    *   **调度机制**：引入 $\lambda(t)$ 调度函数（平台期+余弦衰减），在训练后期减小阈值以降低噪声干扰。
    *   **本地扰动**：执行样本级裁剪（Per-example Clipping）并添加高斯噪声，确保满足记录级本地差分隐私（LDP）。
*   **理论保证**：论文证明了该方法在非凸、凸及强凸设置下的收敛性，并提供了基于 RDP（Rényi Differential Privacy）的隐私核算程序。

### 3. 实验设计
*   **数据集**：
    *   图像分类：MNIST, CIFAR-10, CIFAR-100。
    *   医疗表格数据：Heart Disease（模拟跨机构 Cross-silo 场景）。
*   **Benchmark 与对比方法**：
    *   **基准**：无隐私的 FedAvg。
    *   **对比方法**：NbAFL（固定全局边界）、rPDP-FL（百分位裁剪）、CGM Medium（中位数自适应）、AQC（分位数自适应）。
*   **场景设置**：涵盖了统一隐私预算和异构隐私预算（如不同比例的客户端分配不同的 $\epsilon$）两种场景。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了模型架构（轻量级 CNN, ResNet-18, ResNet-34, 逻辑回归等）和超参数设置（如 MNIST/CIFAR-10 的 Batch Size 为 128，本地迭代 5 次等），这暗示了实验是在标准的深度学习工作站环境下完成的。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同维度的基准数据集上进行了测试，涵盖了从简单 CNN 到复杂 ResNet 的多种架构。
*   **充分性**：
    *   进行了**消融实验**（如固定阈值 vs. 自适应阈值）。
    *   测试了**不同优化器**（SGD vs. Adam）的鲁棒性。
    *   分析了**通信效率**（达到目标准确率所需的轮数）。
*   **客观性**：通过匹配所有对比方法的隐私预算（相同的 $\epsilon, \delta$）进行公平比较，实验设计较为严谨。

### 6. 主要结论与发现
*   **效用提升**：在相同隐私预算下，PAC-DP 相比固定阈值方法准确率提升高达 **26%**。
*   **收敛加速**：在 MNIST 和 CIFAR-10 上，达到目标精度所需的通信轮数减少了 **33.3% 至 45.5%**。
*   **个性化适应**：实验证明 $C^*$ 与 $\epsilon$ 之间存在稳定的正相关映射，PAC-DP 能有效处理客户端隐私偏好的极端异构性。

### 7. 优点与亮点
*   **数据驱动的启发式改进**：通过离线模拟避免了在线训练时昂贵的超参数搜索。
*   **隐私感知**：是首个显式建模 $\epsilon$ 与 $C^*$ 关系的工作，比单纯基于梯度统计量的自适应方法更具理论支撑。
*   **轻量级部署**：在线计算开销极低，仅需执行一个简单的多项式函数。

### 8. 不足与局限
*   **代理数据依赖**：该方法依赖于服务器端拥有一个与真实数据分布相近的公共代理数据集。如果代理数据与实际数据偏差过大，拟合的映射函数可能失效。
*   **离线开销**：虽然在线阶段高效，但离线阶段需要进行大量的模拟实验来拟合曲线，这在模型极其庞大时可能产生较高的预计算成本。
*   **隐私范围限制**：主要关注记录级隐私，未深入探讨用户级（User-level）隐私下的自适应裁剪表现。

（完）
