---
title: Heterogeneity-Aware Personalized Federated Learning for Industrial Predictive Analytics
title_zh: 面向工业预测性分析的异构感知个性化联邦学习
authors: "Yuhan Hu, Xiaolei Fang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19451v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 工业预测分析中的个性化联邦学习
tldr: 针对工业预测分析中客户端退化过程的异构性问题，本文提出了一种感知异构性的个性化联邦学习模型。该模型通过识别具有相似退化模式的客户端并促进其成对协作，实现了定制化的故障时间预测。采用基于近端梯度下降的联邦参数估计算法，在保护隐私的同时提供完整的故障时间分布。实验证明该方法在模拟和真实数据集上均优于传统模型。
selection_source: fresh_fetch
motivation: 传统的联邦预测模型假设所有客户端的退化过程是同质的，这在实际工业场景中往往不成立，导致预测精度受限。
method: 提出一种个性化联邦预测模型，通过迭代识别相似退化模式的客户端进行成对协作，并利用近端梯度下降算法进行去中心化参数估计。
result: 在NASA涡扇发动机退化数据集和模拟研究中，该模型在处理异构数据方面表现出显著优于现有联邦学习方法的性能。
conclusion: 该研究成功解决了工业预测中的异构性挑战，实现了兼顾个性化、隐私保护和全面故障分布预测的联邦学习框架。
---

## 摘要
联邦预测使客户端（如公司、工厂和生产线）能够在保持各自数据本地化和机密性的同时，协作开发故障时间预测模型。然而，传统的联邦模型通常假设各客户端的退化过程具有同质性，这一假设在许多工业场景中可能并不成立。为克服这一问题，本文提出了一种个性化联邦预测模型，旨在适应具有异构退化过程的客户端，允许其构建定制化的预测模型。该预测模型迭代地促进具有相似退化模式的客户端之间潜在的两两协作，从而增强了个性化联邦学习的性能。为了利用去中心化数据集联合估计参数，我们开发了一种基于近端梯度下降的联邦参数估计算法。所提方法通过同时实现模型个性化、保护数据隐私并提供全面的故障时间分布，解决了现有联邦预测模型的局限性。通过广泛的仿真研究以及使用 NASA 存储库中的涡扇发动机退化数据集进行的案例研究，验证了所提模型的优越性。

## Abstract
Federated prognostics enable clients (e.g., companies, factories, and production lines) to collaboratively develop a failure time prediction model while keeping each client's data local and confidential. However, traditional federated models often assume homogeneity in the degradation processes across clients, an assumption that may not hold in many industrial settings. To overcome this, this paper proposes a personalized federated prognostic model designed to accommodate clients with heterogeneous degradation processes, allowing them to build tailored prognostic models. The prognostic model iteratively facilitates the underlying pairwise collaborations between clients with similar degradation patterns, which enhances the performance of personalized federated learning. To estimate parameters jointly using decentralized datasets, we develop a federated parameter estimation algorithm based on proximal gradient descent. The proposed approach addresses the limitations of existing federated prognostic models by simultaneously achieving model personalization, preserving data privacy, and providing comprehensive failure time distributions. The superiority of the proposed model is validated through extensive simulation studies and a case study using the turbofan engine degradation dataset from the NASA repository.

---

## 论文详细总结（自动生成）

这篇论文针对工业预测性分析（如设备剩余寿命预测）中的数据异构性和隐私保护问题，提出了一种创新的个性化联邦学习框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在工业场景中，准确预测设备剩余寿命（RUL）至关重要，但单个企业往往缺乏足够的故障数据。
*   **核心挑战**：
    *   **隐私限制**：企业间由于竞争或法规无法直接共享原始数据。
    *   **数据异构性**：不同客户端的设备型号、运行环境和工况不同，导致退化过程存在显著差异。传统联邦学习（如 FedAvg）假设数据同质，强制学习一个全局模型，在异构环境下效果极差。
    *   **模型局限**：现有深度学习联邦模型计算量大且通常只提供点估计，而工业决策（如维修计划）更需要故障时间的概率分布。
*   **整体含义**：本文旨在开发一种既能保护隐私，又能通过“求同存异”实现个性化预测的联邦统计学习模型。

### 2. 核心方法论
*   **核心思想**：利用**（对数）位置-尺度（LLS）回归模型**作为基础，通过正则化项鼓励模型参数在相似客户端之间保持接近，而在不相似客户端之间保持差异。
*   **关键技术细节**：
    *   **LLS 回归**：该模型能覆盖正态、威布尔、对数逻辑等多种工业常用的故障分布，不仅能预测 RUL，还能给出完整的概率分布。
    *   **目标函数**：构建了一个包含“局部损失函数（拟合本地数据）”和“相似性正则化项（衡量模型间差异）”的联合优化问题。
    *   **相似性函数**：采用负指数等核函数来量化客户端间的参数距离，实现自动化的“成对协作”。
*   **算法流程（基于 PGD 的联邦估计）**：
    1.  **服务端（梯度下降）**：根据各客户端上传的参数，利用相似性权重进行加权聚合，为每个客户端生成一个“个性化云模型”更新。
    2.  **客户端（近端映射）**：客户端下载个性化更新后，结合本地私有数据进行近端算子（Proximal Operator）优化，微调本地模型参数。
    3.  **迭代**：重复上述过程直至收敛，整个过程不交换任何原始数据。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真研究 I & II**：模拟了不同程度的异构性（高/低偏差）以及数据平衡性（样本量相等 vs. 严重不平衡）。
    *   **案例研究**：使用 NASA 的 **C-MAPSS 涡扇发动机退化数据集**，模拟 4 个客户端，涵盖两种不同的故障模式（高压压气机退化和风扇退化）。
*   **Benchmark（基准模型）**：
    1.  **Local Model**：各客户端仅使用本地数据独立训练（无协作）。
    2.  **Conventional Federated Learning (CFL)**：传统的联邦学习，所有客户端强制共享一个全局模型。
*   **对比指标**：平均绝对百分比误差（MAPE）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提到**具体的 GPU 型号或训练时长。
*   **隐含信息**：由于该方法基于统计学习模型（LLS 回归）而非深层神经网络，其参数量较小。作者在文中强调该方法“计算效率高”、“适合计算能力有限的边缘客户端”，暗示其对算力要求较低，普通的 CPU 即可胜任。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验进行了 20 次随机置换以消除偶然性。
    *   不平衡数据实验重复了 30-50 次。
    *   案例研究重复了 30 次。
*   **充分性评价**：实验设计较为充分。作者不仅验证了预测精度，还深入探讨了**数据异构程度**和**样本量多寡**对模型性能的影响，证明了该方法在极端不平衡（如某些客户端只有 5 个样本）的情况下依然稳健。

### 6. 主要结论与发现
*   **个性化优势**：在高度异构的环境下，传统联邦学习（CFL）的表现甚至不如本地独立训练，而本文的 PFL 始终保持最优。
*   **知识借用**：数据量极少的客户端从相似的“富数据”客户端中获益最大，预测精度显著提升。
*   **分布预测**：LLS 模型成功提供了故障时间的概率分布，为工业风险评估提供了依据。
*   **收敛性**：基于 PGD 的算法在去中心化设置下具有良好的收敛性能。

### 7. 优点
*   **异构感知**：通过相似性权重机制，自动识别并加强相似设备间的协作，避免了“负迁移”。
*   **统计解释性**：相比深度学习黑箱，LLS 模型具有更强的统计解释性，且能输出不确定性（分布）。
*   **轻量化**：算法通信开销小，适合工业物联网（IIoT）中的边缘计算场景。

### 8. 不足与局限
*   **相似性度量单一**：目前主要依赖模型参数的欧氏距离来衡量相似性，可能无法捕捉更复杂的非线性特征关联。
*   **超参数敏感性**：正则化参数 $\lambda$ 和相似性参数 $\theta$ 的选择对结果影响较大，虽然文中使用了交叉验证，但在完全去中心化的实际应用中，自动调参仍是挑战。
*   **模型族限制**：假设数据服从 LLS 分布族，如果实际退化过程极其复杂（如多模态分布），该模型的拟合能力可能受限。

（完）
