---
title: "LLM-Redactor: An Empirical Evaluation of Eight Techniques for Privacy-Preserving LLM Requests"
title_zh: LLM-Redactor：八种隐私保护大语言模型请求技术的实证评估
authors: "Justice Owusu Agyemang, Jerry John Kponyo, Elliot Amponsah, Godfred Manu Addo Boakye, Kwame Opuni-Boachie Obour Agyekum"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12064v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 差分隐私噪声与隐私保护大模型技术评估
tldr: 本研究针对LLM应用向云端API发送敏感数据带来的隐私风险，系统评估了本地推理、脱敏、语义重组等八种隐私保护技术。作者开发了开源工具LLM-Redactor，并在包含1300个样本的基准测试上进行了评估。结果显示，结合本地路由、脱敏与重组的组合方案在保护个人信息（PII）和专有代码方面表现最佳，为开发者提供了基于威胁模型和工作负载的选择指南。
selection_source: fresh_fetch
motivation: 现有的网络加密和组织级数据防泄露工具无法有效处理发送至云端LLM API的提示词内容隐私问题。
method: 评估了包括本地推理、占位符脱敏、语义重组和差分隐私在内的八种技术，并实现了一个兼容OpenAI API的开源中间件。
result: "组合方案（本地路由+脱敏+重组）在PII泄露率上降至0.6%，在专有代码泄露上降至31.3%，显著优于单一技术。"
conclusion: 没有单一技术能应对所有场景，开发者应根据威胁模型和工作负载特征，采用组合策略来平衡隐私保护与模型性能。
---

## 摘要
编程智能体和基于大语言模型（LLM）的应用通常会将潜在的敏感内容发送到云端 LLM API，这些内容可能会被记录、保留、用于训练或被法律调取。现有的隐私工具侧重于网络级加密和组织级数据泄露防护（DLP），但两者都无法解决提示词（Prompt）本身的内容隐私问题。我们对八种隐私保护 LLM 请求技术进行了系统性的实证评估：(A) 仅本地推理，(B) 带占位符恢复的脱敏，(C) 语义改写，(D) 受信执行环境（TEE）托管推理，(E) 拆分推理，(F) 全同态加密，(G) 通过多方计算实现的秘密共享，以及 (H) 差分隐私噪声。我们在一个兼容 MCP 和任何 OpenAI 兼容 API 的开源中间层（shim）中实现了所有八种技术（或在尚无法部署的情况下实现了可处理的研究阶段子集）。我们使用包含 1,300 个样本和 4,014 条标注的真值标签泄露基准测试，评估了四种实用选项（A、B、C、H）及其在四类工作负载下的组合。我们的主要发现是，没有单一技术能占据绝对优势：组合 A+B+C（尽可能本地路由，其余部分进行脱敏和改写）在个人身份信息（PII）上的综合泄露率为 0.6%，在专有代码上为 31.3%，且在 500 个样本中实现了 PII 的零精确泄露。我们提出了一种决策规则，可根据威胁模型预算和工作负载特征选择合适的选项。代码、基准测试和评估框架已发布在 https://github.com/jayluxferro/llm-redactor。

## Abstract
Coding agents and LLM-powered applications routinely send potentially sensitive content to cloud LLM APIs where it may be logged, retained, used for training, or subpoenaed. Existing privacy tooling focuses on network-level encryption and organization-level DLP, neither of which addresses the content of prompts themselves. We present a systematic empirical evaluation of eight techniques for privacy-preserving LLM requests: (A) local-only inference, (B) redaction with placeholder restoration, (C) semantic rephrasing, (D) Trusted Execution Environment hosted inference, (E) split inference, (F) fully homomorphic encryption, (G) secret sharing via multi-party computation, and (H) differential-privacy noise. We implement all eight (or a tractable research-stage subset where deployment is not yet feasible) in an open-source shim compatible with MCP and any OpenAI-compatible API. We evaluate the four practical options (A, B, C, H) and their combinations across four workload classes using a ground-truth-labelled leak benchmark of 1,300 samples with 4,014 annotations. Our headline finding is that no single technique dominates: the combination A+B+C (route locally when possible, redact and rephrase the rest) achieves 0.6% combined leak on PII and 31.3% on proprietary code, with zero exact leaks on PII across 500 samples. We present a decision rule that selects the appropriate option(s) from a threat-model budget and workload characterisation. Code, benchmarks, and evaluation harness are released at https://github.com/jayluxferro/llm-redactor.

---

## 论文详细总结（自动生成）

这篇论文对保护大语言模型（LLM）请求隐私的多种技术进行了深入的实证评估，并提出了一个名为 **LLM-Redactor** 的开源框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
随着 LLM API 成为开发基础设施，用户在发送提示词（Prompts）时经常无意中泄露敏感信息（如 PII、商业秘密、专有代码）。现有的安全手段（如 TLS 加密或组织级 DLP）无法解决云端厂商对提示词内容的访问、存储或用于训练的问题。论文的研究动机在于填补这一空白，系统性地评估并整合多种隐私保护技术，为开发者提供一套在 LLM 请求流水线中实时生效的防御方案。

### 2. 方法论：核心思想与关键技术
论文评估了八种技术（A-H），并将其整合进一个兼容 OpenAI API 和 MCP 协议的中间层（Shim）：
*   **A. 本地推理 (Local-only)：** 使用本地小模型（如 3B）对请求分类，简单任务本地处理，复杂任务才发往云端。
*   **B. 脱敏与占位符恢复 (Redaction)：** 利用命名实体识别（NER）和正则匹配识别敏感词，替换为占位符（如 `<EMAIL_1>`），并在响应返回时还原。
*   **C. 语义改写 (Semantic Rephrasing)：** 本地模型重写提示词，保留技术核心但移除身份特征。
*   **D. 受信执行环境 (TEE)：** 在硬件加密飞地（如 AWS Nitro Enclaves）中运行推理。
*   **E. 拆分推理 (Split Inference)：** 本地运行前几层，仅发送中间激活值。
*   **F. 全同态加密 (FHE)：** 在加密数据上直接进行数学运算（目前处于研究阶段）。
*   **G. 多方计算 (MPC)：** 通过秘密共享在多个不合谋的服务器间分配计算。
*   **H. 差分隐私噪声 (DP Noise)：** 在词级别注入校准后的噪声（语义替换）以模糊残余信号。

**处理流程：** 请求进入后，依次经过分类（A）、检测与脱敏（B）、改写（C）、注入噪声（H），最后路由至目标端（云端、TEE 或加密端），返回时进行脱敏还原。

### 3. 实验设计
*   **数据集与工作负载：** 构建了包含 1,300 个样本、4,014 条标注的基准测试，分为四类：
    1.  **WL1 (PII 密集型)：** 包含姓名、地址、社保号等。
    2.  **WL2 (密钥密集型)：** 包含 API Key、PEM 密钥、环境变量。
    3.  **WL3 (隐性身份)：** 不含直接 PII，但通过描述（如“某公司 CFO”）可推断身份。
    4.  **WL4 (专有代码)：** 包含内部函数名、数据库架构等。
*   **对比方法：** 对比了单一技术（A, B, C, H）及其组合（如 A+B, A+B+C）。
*   **评估指标：** 精确泄露率（Exact Leak）、部分泄露率（Partial Leak）、延迟（Latency）和效用损失（Utility Delta）。

### 4. 资源与算力
*   **本地模型：** 使用了 **Ollama** 运行 3B 级别的本地模型（如 Qwen 3.5 4B, Llama 3.2 3B）进行分类、改写和效用评估。
*   **硬件环境：** 提到了 AWS Nitro Enclaves 用于 TEE 演示，Zama 的 Concrete ML 用于 FHE 模拟。
*   **算力细节：** 论文未明确给出具体的 GPU 型号和数量，但强调了本地推理和改写阶段会带来约 1-2 秒的延迟，而脱敏阶段延迟极低（<50ms）。

### 5. 实验数量与充分性
实验涵盖了 1,300 个样本，并针对不同技术组合进行了详尽的消融实验。
*   **充分性：** 实验不仅测量了隐私泄露，还通过“法官模型（Judge Model）”进行了 A/B 测试以评估脱敏对回答质量的影响，实验设计较为全面。
*   **客观性：** 采用了真值标签（Ground-truth）进行自动化的泄露检测，减少了人为偏见。

### 6. 主要结论与发现
*   **组合方案最优：** 没有单一技术能完美平衡。**A+B+C 组合**表现最强，在 PII 上的综合泄露率仅为 0.6%，且在 500 个样本中实现了零精确泄露。
*   **隐性身份是瓶颈：** 对于 WL3（隐性身份），即使是 A+B+C 方案，语义泄露率仍高达 43.6% 以上。这表明内容转换无法完全消除语义层面的身份特征。
*   **本地路由的价值：** 仅通过本地路由（选项 A）就能拦截 94% 的简单 PII 请求，从而将泄露风险降至零。
*   **效用权衡：** 脱敏会导致约 20-25% 的回答质量下降，因为模型失去了部分上下文。

### 7. 优点
*   **系统性强：** 首次在统一基准下对比了从工程实用到前沿密码学的八种技术。
*   **实用性高：** 提供了开源的中间层工具，兼容主流 API，开发者可直接部署。
*   **决策指南：** 提出了基于“威胁模型预算”的决策规则，具有很强的实操指导意义。

### 8. 不足与局限
*   **合成数据偏差：** 实验使用的是基于模板生成的合成数据，可能无法完全覆盖真实世界中复杂、多变的提示词分布。
*   **语言限制：** 目前的检测器和 NER 模型主要针对英文，多语言支持尚待开发。
*   **研究阶段技术的局限：** FHE、MPC 和拆分推理在文中更多是协议模拟或小规模演示，在大规模 LLM 推理上的性能瓶颈尚未完全解决。
*   **隐性身份难题：** 论文承认目前的技术对语义层面的身份保护效果有限，这仍是一个开放性挑战。

（完）
