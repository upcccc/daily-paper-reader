---
title: Towards Privacy-Preserving Federated Learning using Hybrid Homomorphic Encryption
title_zh: 迈向使用混合同态加密的隐私保护联邦学习
authors: "Ivan Costa, Pedro Correia, Ivone Amorim, Eva Maia, Isabel Praça"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26417v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 使用同态加密的隐私保护联邦学习
tldr: 联邦学习中本地模型更新仍存在隐私泄露风险，现有的混合同态加密（HHE）方案因共享单一密钥而难以应对恶意客户端。本研究提出了掩码技术和RSA封装两种增强机制，旨在防止密钥滥用并提升系统安全性。通过在Flower框架上的实验验证，该方法在保持模型准确率的同时，仅引入了极低的计算和通信开销，显著增强了HHE-FL在对抗性环境下的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的混合同态加密联邦学习方案依赖所有客户端共享单一密钥，导致系统在面对恶意参与者时存在严重的隐私泄露风险。
method: 提出了掩码（Masking）和RSA封装（RSA encapsulation）两种机制，分别通过同态盲化和公钥封装来保护客户端密钥不被滥用。
result: 在MNIST数据集上的实验表明，两种机制均能保持模型精度，且掩码技术开销几乎可忽略，RSA封装的开销也处于较低水平。
conclusion: 该研究通过引入密钥保护机制，成功将混合同态加密联邦学习的安全边界扩展到了包含恶意参与者的复杂场景。
---

## 摘要
联邦学习 (FL) 能够在将敏感数据保留在客户端设备上的同时实现协作训练，但局部模型更新仍可能泄露隐私信息。混合同态加密 (HHE) 最近被应用于联邦学习，以在保护隐私的同时减轻客户端开销。然而，现有的 HHE-FL 系统依赖于所有客户端共享的单个同态密钥对，这迫使它们假设一个不切实际的弱威胁模型：如果某个客户端行为不当或拦截了其他客户端的流量，隐私更新可能会被泄露。我们通过在 HHE-FL 工作流中集成两种替代的密钥保护机制来消除这一弱点。第一种是掩码 (masking)，即在同态加密之前对客户端密钥进行盲化，随后由服务器进行同态去盲。第二种是 RSA 封装 (RSA encapsulation)，即在服务器的 RSA 公钥下对同态加密的密钥进行额外包装。这些对策防止了其他客户端对密钥的滥用，并将 HHE-FL 的安全性扩展到存在恶意参与者的对抗性环境。我们在 Flower 框架之上使用 PASTA/BFV HHE 方案实现了这两种方法，并在包含 12 个客户端的 MNIST 数据集上进行了评估。结果表明，这两种机制在保持模型准确性的同时仅增加了极小的开销：掩码产生的成本可以忽略不计，而 RSA 封装仅引入了适度的运行时间和通信开销。

## Abstract
Federated Learning (FL) enables collaborative training while keeping sensitive data on clients' devices, but local model updates can still leak private information. Hybrid Homomorphic Encryption (HHE) has recently been applied to FL to mitigate client overhead while preserving privacy. However, existing HHE-FL systems rely on a single homomorphic key pair shared across all clients, which forces them to assume an unrealistically weak threat model: if a client misbehaves or intercepts another's traffic, private updates can be exposed. We eliminate this weakness by integrating two alternative key protection mechanisms into the HHE-FL workflow. The first is masking, where client keys are blinded before homomorphic encryption and later unblinded homomorphically by the server. The second is RSA encapsulation, where homomorphically encrypted keys are additionally wrapped under the server's RSA public key. These countermeasures prevent key misuse by other clients and extend HHE-FL security to adversarial settings with malicious participants. We implement both approaches on top of the Flower framework using the PASTA/BFV HHE scheme and evaluate them on the MNIST dataset with 12 clients. Results show that both mechanisms preserve model accuracy while adding minimal overhead: masking incurs negligible cost, and RSA encapsulation introduces only modest runtime and communication overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Privacy-Preserving Federated Learning using Hybrid Homomorphic Encryption》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：联邦学习（FL）虽然不直接共享原始数据，但传输的模型梯度/更新仍可能通过推理攻击泄露隐私。现有的混合同态加密（HHE）方案虽然降低了客户端的计算负担，但存在一个严重的**安全漏洞**：所有客户端共享同一对同态加密密钥。这意味着如果一个客户端是恶意的，它可以拦截并解密其他客户端的更新。
*   **研究动机**：在资源受限的物联网（IoT）等场景下，需要一种既能保护隐私、计算开销又低，且能抵御“恶意客户端”拦截攻击的增强型 HHE-FL 系统。

### 2. 论文提出的方法论
论文在 PASTA/BFV 混合同态加密框架的基础上，引入了两种密钥保护机制：
*   **核心思想**：通过对客户端的对称密钥进行二次保护，确保只有服务器在特定步骤下才能访问用于解密模型更新的同态密钥信息。
*   **关键技术细节**：
    1.  **掩码机制 (Masking)**：客户端在同态加密其对称密钥前，先加上一个随机掩码（由可信第三方分发）。服务器收到后，利用同态加法减去该掩码，从而获得解密所需的同态密钥。
    2.  **RSA 封装 (RSA Encapsulation)**：客户端先用同态公钥加密对称密钥，再用服务器的 RSA 公钥对该结果进行封装。由于 RSA 无法直接处理巨大的同态密文，论文采用了**分块（Chunking）**技术。
*   **算法流程**：
    *   **客户端**：训练模型 -> 用对称加密（PASTA）加密权重 -> 用上述两种机制之一保护对称密钥 -> 发送至服务器。
    *   **服务器**：解封装/去掩码 -> 执行同态对称解密（HESD）将对称密文转为同态密文 -> 执行安全聚合（FedAvg） -> 发回更新后的全局模型。

### 3. 实验设计
*   **数据集**：使用经典的 **MNIST** 手写数字识别数据集。
*   **实验场景**：模拟 12 个客户端的中心化联邦学习环境，每轮选择 4 个客户端参与训练。
*   **Benchmark（基准）**：对比了 **Correia et al. (2025)** 提出的原始 HHE-FL 系统（即不具备密钥保护机制的共享密钥方案）。
*   **评估指标**：模型准确率（Accuracy）、损失值（Loss）、通信开销（密文大小）以及计算运行时间。

### 4. 资源与算力
*   **硬件环境**：使用 Google Colab T4 High-RAM 环境。
    *   **CPU**: Intel Xeon @ 2.20GHz。
    *   **GPU**: NVIDIA T4 (16GB VRAM)。
    *   **内存**: 51GB RAM。
*   **软件框架**：基于 Python 的 **Flower** 联邦学习框架，底层加密库使用 C++ 编写的 PASTA 框架，通过 pybind11 进行连接。

### 5. 实验数量与充分性
*   **实验规模**：进行了 10 轮联邦学习，每轮 10 个 Epoch。针对 RSA 封装，还专门对比了不同位长（1024 到 4096 位）的性能表现。
*   **充分性评价**：实验设计较为**标准且客观**。通过对比基准方案，清晰地展示了新机制引入的额外开销。然而，实验仅限于 MNIST 单一数据集和约 8000 个参数的小型 CNN 模型，对于大规模模型和复杂数据集的泛化性验证略显不足。

### 6. 论文的主要结论与发现
*   **准确性保持**：两种增强机制均未损失模型精度，掩码机制甚至达到了 98.35% 的最高准确率。
*   **开销对比**：
    *   **掩码机制**：计算和通信开销几乎可以忽略不计，是极其高效的解决方案。
    *   **RSA 封装**：引入了约 1.22x 到 1.31x 的通信开销，且服务器端解密 RSA 块会增加数十秒的延迟，但提供了更强的非对称安全保障。
*   **安全性提升**：成功消除了共享密钥带来的内部威胁，使 HHE-FL 能够适用于存在恶意参与者的对抗性环境。

### 7. 优点
*   **安全性增强**：针对现有 HHE-FL 方案中被忽视的“共享密钥”漏洞提出了切实可行的补丁。
*   **实用性强**：方案考虑了 IoT 设备的计算限制，将最沉重的同态运算（HESD）留在服务器端。
*   **模块化设计**：提出的掩码和 RSA 封装可以根据具体的安全需求和资源限制进行灵活切换。

### 8. 不足与局限
*   **服务器端瓶颈**：尽管减轻了客户端负担，但服务器执行 HESD（同态对称解密）的时间依然极长（每个客户端约 1400 秒），这限制了系统的实时性和大规模扩展性。
*   **实验覆盖面窄**：仅在 MNIST 这种简单任务上进行了验证，未测试更深层的网络（如 ResNet）或非独立同分布（Non-IID）数据下的表现。
*   **依赖第三方**：掩码机制仍依赖于一个完全可信的第三方（TPA）来分发掩码，这在某些去中心化场景下可能成为单点故障。

（完）
