---
title: "SuperDP: Differential Privacy Refutation via Supermartingales"
title_zh: SuperDP：基于超鞅的差分隐私证伪
authors: "Krishnendu Chatterjee, Ehsan Kafshdar Goharshady, Đorđe Žikelić"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26215v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 差分隐私属性的正式验证与驳回
tldr: 差分隐私（DP）的验证与驳回极具挑战且易出错。本文提出SuperDP，一种基于超鞅（Supermartingales）的自动化ε-DP形式化驳回方法。该方法通过同时搜索输入对和非负输出函数，利用概率程序分析中的上下期望鞅理论，建立了一套完备且可靠的证明规则。SuperDP支持离散和连续分布，是首个兼具自动化、通用性、可靠性及半完备性的驳回工具，在处理复杂实例上显著优于现有技术。
selection_source: fresh_fetch
motivation: 针对差分隐私属性难以手动验证且现有驳回方法在自动化程度、分布支持及完备性方面存在局限的问题。
method: 利用概率程序分析中的超鞅和子鞅理论，通过同时搜索输入对和非负输出函数来寻找违反ε-DP定义的证据。
result: 实验表明SuperDP在性能上优于现有最先进工具，并成功驳回了以往方法无法处理的多个复杂差分隐私机制。
conclusion: 本研究为ε-DP的自动化形式化驳回提供了一个理论完备且实践高效的框架，显著提升了隐私机制的安全性分析能力。
---

## 摘要
差分隐私 (DP) 已成为确保个人数据隐私的标准之一。然而，对 DP 的推理是一项具有挑战性且容易出错的任务，因此 DP 属性的形式化验证和证伪方法在近年来受到了广泛关注。在这项工作中，我们提出了一种用于 $ε$-DP 自动形式化证伪的新方法。我们的方法通过搜索一对输入以及一个定义在输出上的非负函数来证伪 $ε$-DP，该函数在这两个输入上的期望值存在显著差异。通过利用概率程序分析中的上期望超鞅和下期望亚鞅，我们同时计算这两个输入和输出上的非负函数，并据此引入了一个用于 $ε$-DP 证伪的可靠且完备的证明规则。据我们所知，我们的方法是首个提供以下四个理想特性的 $ε$-DP 证伪方法：(1) 全自动化；(2) 适用于包含离散和连续分布采样指令的随机机制；(3) 提供可靠性保证；(4) 提供半完备性保证。实验表明，我们的原型工具 SuperDP 与现有最先进技术相比具有更优越的性能，并成功证伪了文献中收集的许多具有挑战性的示例，其中包括先前方法无法处理的示例。

## Abstract
Differential privacy (DP) has established itself as one of the standards for ensuring privacy of individual data. However, reasoning about DP is a challenging and error-prone task, hence methods for formal verification and refutation of DP properties have received significant interest in recent years. In this work, we present a novel method for automated formal refutation of $ε$-DP. Our method refutes $ε$-DP by searching for a pair of inputs together with a non-negative function over outputs whose expected value on these two inputs differs by a significant amount. The two inputs and the non-negative function over outputs are computed simultaneously, by utilizing upper expectation supermartingales and lower expectation submartingales from probabilistic program analysis, which we leverage to introduce a sound and complete proof rule for $ε$-DP refutation. To the best of our knowledge, our method is the first method for $ε$-DP refutation to offer the following four desirable features: (1)~it is fully automated, (2)~it is applicable to stochastic mechanisms with sampling instructions from both discrete and continuous distributions, (3)~it provides soundness guarantees, and (4)~it provides semi-completeness guarantees. Our experiments show that our prototype tool SuperDP achieves superior performance compared to the state of the art and manages to refute $ε$-DP for a number of challenging examples collected from the literature, including ones that were out of the reach of prior methods.

---

## 论文详细总结（自动生成）

### 论文总结：SuperDP - 基于超鞅的差分隐私证伪

#### 1. 核心问题与整体含义（研究动机和背景）
差分隐私（Differential Privacy, DP）是保护个人数据隐私的数学标准，但其属性的验证（证明满足 DP）和证伪（证明不满足 DP）极具挑战。由于概率程序的复杂性，DP 检查在图灵完备语言中是不可判定的。
*   **研究动机**：现有的 DP 证伪方法存在局限性。静态分析方法通常不支持连续分布（如拉普拉斯分布）或缺乏自动化；动态（统计）方法依赖采样，缺乏形式化保证且在处理低概率事件时容易失效。
*   **核心目标**：开发一种全自动、支持离散和连续分布、具有可靠性（Soundness）和半完备性（Semi-completeness）保证的 $\epsilon$-DP 证伪工具。

#### 2. 方法论：核心思想与技术细节
该论文将 $\epsilon$-DP 的证伪问题转化为**“期望不匹配”（Expectation Mismatch）**的搜索问题。
*   **核心思想**：根据定义，若程序不是 $\epsilon$-DP，则存在一对相似输入 $x_1, x_2$ 和一个输出事件 $A$，使得 $P_{x_1}(A) > e^\epsilon \cdot P_{x_2}(A)$。论文证明了这等价于：存在一个非负函数 $f$，使得其在两个输入下的输出期望满足 $E_{x_1}[f] > e^\epsilon \cdot E_{x_2}[f]$。
*   **关键技术：超鞅与亚鞅**：
    *   利用**上期望超鞅（UESM）**来计算期望的上界 $U \ge E_{x_2}[f]$。
    *   利用**下期望亚鞅（LESM）**来计算期望的下界 $L \le E_{x_1}[f]$。
    *   如果能找到满足 $L > e^\epsilon \cdot U$ 的证明，则成功证伪 $\epsilon$-DP。
*   **算法流程**：
    1.  **模板设置**：为输入对、函数 $f$、UESM 和 LESM 设置多项式模板。
    2.  **约束收集**：根据鞅的定义、相似性关系和期望不等式生成数学约束。
    3.  **约束简化**：利用代数几何中的 Handelman 定理消除全称量词。
    4.  **SMT 求解**：使用 Z3 或 MathSAT5 求解约束系统，获得证伪证据。

#### 3. 实验设计
*   **实验场景**：选取了 15 个经典的概率程序和随机机制，包括：
    *   标准机制：直方图（Histogram）、随机响应（RR）、稀疏向量技术（SVT）、高斯机制、几何机制等。
    *   挑战性案例：包含低概率分支的程序（lowprob）、非多项式分布采样等。
*   **Benchmark 与对比方法**：
    *   **CheckDP**：最先进的静态分析工具（基于随机性对齐）。
    *   **StatDP**：主流的动态统计证伪工具（基于假设检验）。

#### 4. 资源与算力
*   **硬件环境**：第 11 代 Intel Core i5 CPU，16 GB RAM。
*   **软件实现**：使用 Java 编写，后端调用 Z3 和 MathSAT5 求解器，使用 ASPIC 生成线性不变式。
*   **算力消耗**：实验在单核 CPU 上运行，每个 benchmark 的超时限制为 5 分钟。大多数成功案例在数秒内完成。

#### 5. 实验数量与充分性
*   **实验规模**：共 15 组实验，涵盖了从简单到复杂的多种 DP 违规场景。
*   **充分性评价**：实验设计较为充分。它不仅对比了静态和动态两类代表性工具，还专门测试了现有工具的弱点（如 CheckDP 不支持的分布、StatDP 难以处理的低概率事件）。实验结果展示了 SuperDP 在证伪精度（找到更大的 $\epsilon$ 违规值）和运行效率上的优势。

#### 6. 主要结论与发现
*   **理论贡献**：提出了首个针对 $\epsilon$-DP 证伪的可靠且完备的证明规则。
*   **实践效果**：SuperDP 在 13/15 个案例中成功证伪，表现优于基准工具。
*   **发现**：SuperDP 能够处理包含循环和复杂连续分布的程序，而 CheckDP 在处理非拉普拉斯分布时受限，StatDP 在处理极低概率违规时会因采样不足而失效。

#### 7. 优点（亮点）
*   **全自动化**：无需人工干预即可生成证伪证据。
*   **通用性强**：支持离散和连续概率分布（如高斯、拉普拉斯、伯努利）。
*   **形式化保证**：基于鞅理论，提供的证伪结果具有数学上的可靠性。
*   **半完备性**：在多项式程序类中，只要存在多项式形式的证伪函数，该算法保证能找到。

#### 8. 不足与局限
*   **隐私定义限制**：目前仅支持纯 $\epsilon$-DP，不支持更广泛使用的 $(\epsilon, \delta)$-DP。
*   **程序类型限制**：算法假设程序算术逻辑为多项式形式，对于包含复杂非线性算术或特定 `if` 分支概率推理的程序，求解器可能会超时。
*   **状态空间**：对于具有极多位置（Locations）的大型程序，生成的 SMT 约束系统规模会迅速膨胀，导致求解困难。

（完）
