---
title: Improving Parameter-Efficient Federated Learning with Differentially Private Refactorization
title_zh: 通过差分隐私重分解改进参数高效联邦学习
authors: "Linh Tran, Ana Milanova, Stacy Patterson"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08443v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 差分隐私联邦学习中隐私与效用的权衡
tldr: 针对联邦学习中 LoRA 与差分隐私（DP）结合时产生的聚合误差和噪声放大问题，本文提出了 FedPower 框架。该框架在服务端通过 PowerDP 机制对重构后的全秩更新进行差分隐私低秩分解，利用子空间迭代并在正交化前注入噪声，有效保护了矩阵正交性并降低了噪声干扰。实验证明，FedPower 在保证样本级和客户端级隐私的同时，显著提升了模型在严苛隐私预算下的准确率。
selection_source: fresh_fetch
motivation: 现有的差分隐私 LoRA 联邦学习方法在低秩子空间中容易受到噪声干扰，导致聚合误差大且模型性能下降。
method: 提出 FedPower 框架，通过在服务端重构全秩更新并利用 PowerDP 机制进行带噪声的低秩分解，以优化隐私保护下的参数聚合。
result: 实验表明 FedPower 在多种语言理解任务中表现稳健，在极低隐私预算下仍能保持高准确率，且计算开销极小。
conclusion: 该研究通过改进服务端聚合逻辑和引入新型 DP 低秩分解机制，成功解决了隐私联邦学习中参数高效微调的性能瓶颈。
---

## 摘要
结合参数高效微调（如低秩自适应 LoRA）的联邦学习（FL）能够在分布式数据上实现可扩展的模型训练。然而，当与差分隐私（DP）结合时，LoRA 往往会在全局聚合过程中引入误差，并放大 DP 噪声的负面影响。现有的跨机构（cross-silo）联邦学习方法通过冻结一个 LoRA 模块并应用输出扰动来减轻聚合误差。然而，在受限的低秩子空间中，这种加性噪声经常会掩盖权重矩阵的信号，导致准确率不佳。为了应对这一脆弱性，我们提出了 FedPower，这是一个重塑了服务器端聚合的差分隐私跨机构联邦学习框架。FedPower 并非扰动不匹配的低秩因子，而是显式地重构并裁剪全秩客户端更新，以限制敏感度。随后，服务器使用 PowerDP（一种新型差分隐私低秩分解机制）将精确的聚合更新投影回安全的低秩空间。PowerDP 基于同步子空间迭代，在最终的正交化步骤之前注入校准后的 DP 噪声，通过保持矩阵正交性有效地减轻了 DP 噪声的负面影响。我们提供了严谨的理论分析，建立了子空间投影的敏感度边界，证明了 FedPower 实现了样本级和客户端级的 DP。在跨机构联邦学习设置下进行的各种语言理解任务的大量实验表明，FedPower 在严苛的隐私预算下具有鲁棒性，同时计算开销可以忽略不计。对不同 DP 噪声注入方案的额外实证研究验证了 PowerDP 在改善准确率与隐私权衡方面的有效性。对三种不同成员推理攻击的评估验证了所提框架的鲁棒性和隐私保护能力。

## Abstract
Federated Learning (FL) with parameter-efficient fine-tuning, such as Low-Rank Adaptation (LoRA), enables scalable model training on distributed data. However, when combined with Differential Privacy (DP), LoRA often introduces errors during global aggregation and amplifies the negative effect of DP noise. Existing cross-silo FL approaches mitigate the aggregation error by freezing one LoRA module and applying output perturbation. However, in a restricted low-rank subspaces, this additive noise frequently overwhelms the signals of the weight matrices, leading to suboptimal accuracy. To address this vulnerability, we propose FedPower, a differentially private cross-silo FL framework that reshapes server-side aggregation. Instead of perturbing mismatched low-rank factors, FedPower explicitly reconstructs and clips full-rank client updates to bound the sensitivity. The server then projects the exact aggregated update back into a secure low-rank space using PowerDP, a novel differentially private low-rank factorization mechanism. Based on simultaneous subspace iteration, PowerDP injects calibrated DP noise prior to the final orthonormalization step, effectively mitigates the negative effect of DP noise by preserving matrix orthogonality. We provide rigorous theoretical analyses establishing sensitivity bounds for subspace projections, proving that FedPower achieves both sample-level and client-level DP. Extensive experiments on various language understanding tasks in cross-silo FL settings show that FedPower is robust against tight privacy budgets while adding negligible computational overheads. Additional empirical study on different DP noise injection schemes validates the effectiveness of PowerDP in improving the tradeoff in accuracy and privacy. Evaluation on three different membership inference attacks validates the robustness and privacy-preserving capability of the proposed framework.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **FedPower** 的新型差分隐私联邦学习框架，旨在解决大语言模型（LLM）在联邦微调过程中，低秩自适应（LoRA）与差分隐私（DP）结合时产生的性能退化问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在联邦学习（FL）中，LoRA 被广泛用于降低通信和计算开销。然而，标准的“联邦 LoRA”（FedLoRA）在服务端聚合时存在数学上的不匹配——即“平均值的乘积不等于乘积的平均值”，这会引入聚合误差。
*   **核心冲突**：当引入差分隐私（DP）来保护客户端数据时，注入的噪声会与上述聚合误差产生耦合放大效应。在受限的低秩子空间中，这种放大的噪声往往会淹没模型更新的有效信号，导致微调后的模型准确率大幅下降。
*   **研究动机**：寻找一种既能保持 LoRA 的高效性，又能消除聚合误差并减轻 DP 噪声负面影响的方法。

### 2. 方法论：FedPower 与 PowerDP
*   **FedPower 框架流程**：
    1.  **客户端**：照常进行本地 LoRA 训练，并将低秩矩阵 $A$ 和 $B$ 发送至服务器。
    2.  **服务端重构与聚合**：服务器不再直接平均 $A$ 和 $B$，而是先将它们相乘重构为全秩更新 $\Delta W = BA$。随后对全秩矩阵进行**裁剪（Clipping）**以限制敏感度，并进行精确的全秩聚合。
    3.  **PowerDP 重分解**：为了将聚合后的全秩更新重新转回低秩形式以供下一轮训练，论文提出了 **PowerDP** 机制。
*   **PowerDP 关键技术**：
    *   **核心思想**：基于同步子空间迭代（幂迭代）。
    *   **噪声注入策略**：不同于传统的“输出扰动”（在分解完成后加噪声），PowerDP 在迭代过程中的**正交化步骤之前**注入校准后的高斯噪声。
    *   **结构保护**：通过随后的格拉姆-施密特（Gram-Schmidt）正交化过程，噪声被“吸收”到子空间的方向中，从而保持了矩阵的正交结构，减少了对信号强度的破坏。

### 3. 实验设计
*   **数据集与场景**：使用了 GLUE 基准测试中的四个代表性任务：**MNLI**（自然语言推理）、**SST-2**（情感分析）、**QQP**（语义等价检测）和 **QNLI**（问答推理）。
*   **模型基础**：以 **RoBERTa-large** 作为预训练基础模型。
*   **对比基准（Benchmarks）**：
    *   **FedLoRA**：标准聚合 $A$ 和 $B$ 的方法。
    *   **FFA-LoRA**：通过冻结一个 LoRA 模块来减少误差的现有先进方法。
    *   **消融对比**：对比了“输入扰动”和“输出扰动”两种不同的 DP 噪声注入方案。
*   **隐私评估**：针对三种成员推理攻击（MIA）进行了鲁棒性测试：Shadow Model Attack、Loss Based Attack 和 Calibration Attack。

### 4. 资源与算力
*   **硬件环境**：实验在一个包含 **3 台 NVIDIA V100 (32GB) GPU** 的集群上完成。
*   **训练细节**：设置了 200 个全局通信轮次，客户端采样率为 0.5，使用了 Adam 优化器。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个数据集，每个实验均重复运行 **5 次**并取平均值以确保结果的统计显著性。
*   **充分性评价**：实验设计非常充分。不仅对比了最终准确率，还分析了收敛速度（准确率 vs 轮次）、通信效率（准确率 vs 传输比特数）、重分解频率的影响，以及在不同隐私预算（$\epsilon = 3, 6, 9$）下的表现。此外，通过三种攻击模型的测试，客观地验证了隐私保护的真实效果。

### 6. 主要结论与发现
*   **性能优越性**：FedPower 在所有隐私预算下均显著优于 FedLoRA 和 FFA-LoRA。在严苛的隐私设置（$\epsilon=3$）下，平均准确率比 FedLoRA 高出约 1.65%。
*   **收敛与效率**：FedPower 的收敛速度明显快于 FFA-LoRA，且在达到相同准确率时所需的通信数据量更少。
*   **隐私防御**：实验证明，当 $\epsilon \le 9$ 时，所有成员推理攻击的成功率均降至接近 0.5（即随机猜测），证明了其强大的隐私保护能力。
*   **计算开销**：虽然服务端增加了全秩重构和分解的步骤，但实验显示其带来的额外计算时间开销不到 10%，在可接受范围内。

### 7. 优点
*   **理论严谨**：为子空间投影建立了敏感度边界，并证明了算法符合 $(\epsilon, \delta)$-差分隐私。
*   **架构创新**：通过“全秩聚合 + 私密重分解”的思路，从根本上解决了 LoRA 聚合的数学失真问题。
*   **噪声处理巧妙**：PowerDP 利用正交化过程吸收噪声，是处理矩阵型数据 DP 扰动的一个亮点。

### 8. 不足与局限
*   **服务端内存压力**：虽然对于 RoBERTa 规模的模型有效，但如果应用到超大规模模型（如 70B+ 参数），服务端重构全秩矩阵可能会面临巨大的内存挑战。
*   **数据分布假设**：实验主要基于 IID（独立同分布）数据划分，在极端非 IID 环境下的表现尚需进一步探索。
*   **任务局限**：目前仅在自然语言理解（NLU）任务上进行了验证，未在生成式任务（如文本摘要或对话生成）上进行测试。

（完）
