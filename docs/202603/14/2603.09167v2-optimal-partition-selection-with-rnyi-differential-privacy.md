---
title: Optimal partition selection with Rényi differential privacy
title_zh: 基于 Rényi 差分隐私的最优分区选择
authors: "Charlie Harrison, Pasin Manurangsi"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09167v2"
tags: ["query:dml-dp"]
score: 9.0
evidence: 基于Rényi差分隐私的最优分区选择
tldr: "本研究探讨了差分隐私下的分区选择问题，旨在最大化发布的分区集合。作者将前人针对单分区用户的最优(ε, δ)-DP算法推广至δ近似Rényi差分隐私（RDP）框架，实现了更紧致的组合分析。针对多分区用户，提出了一种适用于L2有界加权分区选择的改进算法，可直接替代高斯机制并集成到现有先进算法中。此外，研究还揭示了同时发布分区及其频率时存在的固有成本及噪声机制间的性能差异。"
selection_source: fresh_fetch
motivation: 旨在解决在满足Rényi差分隐私约束下，如何最大化从海量数据中安全发布分区集合并优化组合分析的问题。
method: 将单分区最优算法推广至δ近似RDP，并针对多分区场景开发了基于L2有界权重的改进机制以替代传统高斯机制。
result: 提出的算法显著提升了PolicyGaussian和MAD2R等现有先进算法在并行和顺序自适应场景下的性能。
conclusion: 证明了在分区选择中同时发布频率会带来额外成本，并揭示了加性噪声与非加性噪声机制在处理该问题时的数值差异。
---

## 摘要
隐私数据分析中的一个常见问题是分区选择问题，其中每个用户持有一组来自可能无限集合的分区（例如 GROUP BY 操作中的键）。这里的挑战在于在遵守差分隐私约束的同时，最大化发布的分区集合。先前的研究 [Desfontaines et al., PoPETS 2022] 提出了一种在每个用户仅提交单个分区时的最优 (ε, δ)-DP 算法。我们将该方法推广到 δ-近似 (α, ε)-Rényi 差分隐私 (RDP) 下，以寻找最优算法，这在组合情况下允许更紧致的分析。针对每个用户提交多个分区的情况下尚不存在通用最优性结果这一现状，我们提出了一种针对 L² 有界加权分区选择进行调整的最优算法扩展，只要不需要分区频率，该算法就可以作为高斯机制的直接替代改进方案。我们展示了我们的原语可以轻松插入到最先进的分区选择算法中（如 [Gopi et al., ICML 2020] 的 PolicyGaussian 和 [Chen et al., ICML 2025] 的 MAD2R），从而提高并行和顺序自适应算法的性能。最后，我们证明了对于支持同时发布频率和分区的算法，存在一种固有的代价。具体而言，我们为使用加性噪声的分区选择制定了最优近似 RDP 算法的基本概念，并证明了在该问题中加性噪声机制与非加性噪声机制之间存在数值差异。

## Abstract
A common problem in private data analysis is the partition selection problem, where each user holds a set of partitions (e.g. keys in a GROUP BY operation) from a possibly unbounded set. The challenge here is in maximizing the set of released partitions while respecting a differential privacy constraint. Previous work [Desfontaines et al., PoPETS 2022] presented an optimal $(\varepsilon, δ)$-DP algorithm when each user submits only a single partition. We generalize this approach to find the optimal algorithm under $δ$-approximate $(α, \varepsilon)$-Rényi differential privacy (RDP), which allows much tighter analysis under composition. Motivated by the non-existence of a general optimality result in the case where users submit multiple partitions each, we present an extension of our optimal algorithm tuned for $L^2$ bounded weighted partition selection which can be used as a drop-in improvement over the Gaussian mechanism any time the partition frequency is not also needed. We show that our primitive can be easily plugged into state of the art partition selection algorithms (PolicyGaussian from [Gopi et al., ICML 2020] and MAD2R from [Chen et al., ICML 2025]), improving performance both for parallel and sequential adaptive algorithms. Finally, we show that there is an inherent cost to algorithms which do support releasing the frequency as well as the partitions. Specifically, we formulate a basic notion of optimal approximate RDP algorithm for partition selection using additive noise, and show that there is a numerical separation between additive and non-additive noise mechanisms for this problem.

---

## 论文详细总结（自动生成）

### 论文总结：基于 Rényi 差分隐私的最优分区选择

#### 1. 核心问题与研究背景
在隐私数据分析中，“分区选择”（Partition Selection）是一个基础问题，例如在执行 `GROUP BY` 操作时，需要决定哪些“键”（Keys）可以安全地发布而不泄露单个用户的信息。
*   **研究动机**：先前的最优算法主要针对 $(\epsilon, \delta)$-DP 且假设每个用户仅贡献一个分区。然而，在实际应用中，用户往往贡献多个分区，且需要通过多次查询的组合（Composition）来分析。
*   **核心挑战**：如何在满足更严苛、更易于组合的 **Rényi 差分隐私（RDP）** 约束下，最大化发布的分区数量。

#### 2. 方法论
论文提出了一套从理论最优原语到实际应用机制的完整框架：
*   **最优 RDP 分区原语 ($\pi^*$)**：通过解决 Bernoulli 分布间的近似 Rényi 散度约束，推导出单分区贡献下的理论最优释放概率函数。
*   **SNAPS 机制（Smooth Norm-Aware Partition Selection）**：
    *   针对用户持有多个分区且具有 $L_r$ 范数限制（如 $L_2$ 敏感度）的情况，设计了“平滑范数感知”的分区选择机制。
    *   **关键技术**：利用离散化权重和 RDP 的组合特性，将复杂的范数约束分解为可计算的释放概率。
*   **加性噪声分析**：通过凸优化（Convex Programming）寻找最优的加性噪声分布，并证明了在 RDP 框架下，若要同时发布“分区”和“频率（Count）”，必然会损失分区发布的效用（即存在数值上的性能差距）。

#### 3. 实验设计
*   **数据集**：使用了 6 个公开的真实数据集，涵盖社交媒体、百科、金融和电商评论：Reddit, Wiki, Twitter, Finance, Amazon, IMDb。
*   **Benchmark 与对比方法**：
    *   **基准算法**：PolicyGaussian (ICML 2020) 和 MAD2R (ICML 2025)。
    *   **对比方式**：将上述算法中的核心组件“高斯机制”替换为本文提出的 **SNAPS 机制**，在相同的隐私预算（$(1, 10^{-5})$-DP）和贡献限制（$\Delta_0 = 100$）下对比输出分区的规模。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   由于该算法主要涉及数值计算（二分查找、凸优化）和数据处理，其算力需求通常集中在 CPU 端，而非大规模深度学习所需的 GPU 算力。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同领域的数据集上进行了测试，每组实验重复 5 次并报告了标准差。
*   **充分性**：实验涵盖了并行（MAD2R）和顺序自适应（PolicyGaussian）两种主流算法框架，验证了 SNAPS 作为“插件”的通用性。
*   **客观性**：对比实验在完全相同的超参数和隐私核算条件下进行，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能提升**：在所有测试数据集上，使用 SNAPS 替代高斯机制后，发布的分区数量提升了 **10% 到 20%**，达到了目前该领域的 SOTA（最先进）水平。
*   **理论发现**：证明了在 $\Delta_1 > 1$（用户贡献多个分区）时，不存在单一的通用最优机制。
*   **加性噪声的代价**：研究发现，如果应用场景不需要发布分区的具体频率（Count），使用非加性噪声机制（如 SNAPS）比传统的加性噪声（如拉普拉斯或高斯）更有效。

#### 7. 优点
*   **理论严密**：将分区选择的最优性研究从传统的 $(\epsilon, \delta)$-DP 扩展到了更现代的 RDP 框架。
*   **实用性强**：SNAPS 机制可以作为高斯机制的“掉入式”（Drop-in）替代方案，无需改变现有复杂算法的整体结构。
*   **数值优化创新**：提出了一种高效的 $O(n \log n)$ 算法来计算离散分布间的近似 Rényi 散度。

#### 8. 不足与局限
*   **多分区最优性缺失**：虽然在单分区情况下达到了理论最优，但在多分区（$L_2$ 约束）下仅是经验性的改进，未能给出理论上的全局最优解。
*   **离散化依赖**：SNAPS 机制依赖于权重离散化参数（$\Delta_{disc}$），参数选择可能会影响特定场景下的性能。
*   **隐私转换损失**：从 RDP 转换回 $(\epsilon, \delta)$-DP 时，由于使用了通用的转换公式，可能会损失一小部分隐私预算的紧致性。

（完）
