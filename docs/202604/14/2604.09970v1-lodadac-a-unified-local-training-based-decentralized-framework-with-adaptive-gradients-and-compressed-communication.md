---
title: "LoDAdaC: a unified local training-based decentralized framework with adaptive gradients and compressed communication"
title_zh: LoDAdaC：一种具有自适应梯度和压缩通信的统一本地训练去中心化框架
authors: "Wei Liu, Anweshit Panda, Ujwal Pandey, Haven Cook, George M. Slota, Naigang Wang, Jie Chen, Yangyang Xu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.09970v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 去中心化分布式学习框架
tldr: 针对去中心化分布式学习中通信开销大和收敛慢的问题，本文提出LoDAdaC框架。该框架创新性地结合了多步本地训练（MLT）、自适应梯度优化（如Adam）以及通信压缩（CC）技术。LoDAdaC支持多种自适应优化器和有偏压缩算子，在理论上证明了其收敛性，并在图像分类和语言模型训练任务中展现出优于现有算法的通信效率和收敛速度。
selection_source: fresh_fetch
motivation: 旨在解决去中心化学习中自适应梯度方法与多步本地训练及通信压缩结合时的收敛性分析缺失与效率瓶颈问题。
method: 提出一种统一框架，集成了Adam类自适应更新、多步本地训练和兼容有偏算子的通信压缩技术。
result: 理论分析与实验验证均表明，该方法在图像分类和GPT模型训练中显著优于现有去中心化算法。
conclusion: LoDAdaC通过协同自适应优化与双重通信减负技术，为高效、可扩展的去中心化深度学习提供了强有力的支撑。
---

## 摘要
在去中心化分布式学习中，实现快速收敛和低通信成本对于可扩展性和高效率至关重要。自适应梯度方法（如 Adam）在深度学习和中心化分布式设置中展现了强大的实际性能。然而，在涉及多个本地训练步骤（如联邦学习）的去中心化设置中，其收敛特性在很大程度上仍未得到探索。为了解决这一局限性，我们提出了 LoDAdaC，这是一个具有 Adam 类更新和压缩通信 (CC) 的统一多本地训练 (MLT) 去中心化框架。LoDAdaC 的本地自适应更新支持广泛的优化器类别，包括 AMSGrad、Adam 和 AdaGrad；它还与标准的（可能是偏置的）压缩器兼容，如低比特量化和稀疏化。MLT 和 CC 使 LoDAdaC 能够实现通信成本的成倍降低，而自适应更新技术则实现了快速收敛。我们通过复杂度分析严格证明了这种组合优势。此外，在图像分类和 GPT 风格语言模型训练上的实验验证了我们的理论发现，并表明 LoDAdaC 在收敛速度和通信效率方面显著优于现有的去中心化算法。

## Abstract
In the decentralized distributed learning, achieving fast convergence and low communication cost is essential for scalability and high efficiency. Adaptive gradient methods, such as Adam, have demonstrated strong practical performance in deep learning and centralized distributed settings. However, their convergence properties remain largely unexplored in decentralized settings involving multiple local training steps, such as federated learning. To address this limitation, we propose LoDAdaC, a unified multiple Local Training (MLT) Decentralized framework with Adam-type updates and Compressed communication (CC). LoDAdaC accommodates a broad class of optimizers for its local adaptive updates, including AMSGrad, Adam, and AdaGrad; it is compatible with standard (possibly biased) compressors such as low-bit quantization and sparsification. MLT and CC enable LoDAdaC to achieve multiplied reduction of communication cost, while the technique of adaptive updates enables fast convergence. We rigorously prove the combined advantage through complexity analysis. In addition, experiments on image classification and GPT-style language model training validate our theoretical findings and show that LoDAdaC significantly outperforms existing decentralized algorithms in terms of convergence speed and communication efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《LoDAdaC: a unified local training-based decentralized framework with adaptive gradients and compressed communication》的结构化总结：

### 1. 核心问题与整体含义
该论文针对**去中心化分布式学习**（Decentralized Learning）中的两大瓶颈——**高通信开销**和**收敛速度慢**——提出了解决方案。虽然在中心化设置下，多步本地训练（MLT）、通信压缩（CC）和自适应梯度（如Adam）已被广泛研究，但在去中心化场景中，如何同时结合这三者并保证理论上的收敛性一直是一个挑战。论文的核心动机是探索是否能通过统一框架实现通信成本的“成倍降低”与“快速收敛”的并存。

### 2. 方法论
论文提出了 **LoDAdaC** 框架，其核心思想是将自适应优化器、多步本地更新和有偏通信压缩有机结合：
*   **核心思想**：每个代理（Agent）在通信轮之间执行 $K$ 次本地自适应更新，并在通信时仅传输经过压缩的模型差值。
*   **关键技术细节**：
    *   **本地自适应更新**：支持包括 Adam、AMSGrad、AdaGrad 和 Adam-Mini 在内的多种优化器。通过维护一阶动量 $m_t$ 和二阶动量 $u_t$ 来动态调整学习率。
    *   **多步本地训练 (MLT)**：每隔 $K$ 个迭代步进行一次邻居间的通信，显著减少通信频率。
    *   **压缩通信 (CC)**：兼容有偏压缩算子（如 Top-k 稀疏化、低比特量化），通过传输压缩后的模型增量来降低单次通信的数据量。
    *   **共识机制**：利用双随机混合矩阵（Gossip Matrix）在去中心化网络中实现代理间的模型同步。
*   **理论突破**：解决了 MLT、CC、自适应缩放与去中心化拓扑之间复杂的非线性耦合问题，证明了在非凸设置下的收敛性，并展示了 $O(1/\sqrt{nTK})$ 的收敛率。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：FashionMNIST（LeNet5 网络）、CIFAR-10（Fixup-ResNet-20 网络）。
    *   **语言模型**：tiny-shakespeare（10.7M 参数的 GPT 风格 Transformer 模型）。
*   **Benchmark 与对比方法**：
    *   **SQuARM-SGD**：结合了动量 SGD、压缩和本地训练的去中心化方法。
    *   **DADAM**：带压缩的去中心化 Adam（但每步都通信，$K=1$）。
    *   **CDProxSGT**：非自适应、无本地训练的压缩去中心化基准。
*   **变量控制**：测试了不同的本地步数 $K$、压缩率（Top-k %）、代理数量（4, 9, 16）以及网络拓扑（环形、2D 网格）。

### 4. 资源与算力
*   **CPU 实验**：在配备两路 64 核 AMD EPYC 7742 处理器和 2TB DDR4 内存的服务器上运行（主要用于图像分类）。
*   **GPU 实验**：在配备 **4 张 NVIDIA A100 GPU** 的服务器上训练 GPT 模型。
*   **软件环境**：Python 3.12.3, PyTorch 2.7.0。

### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **参数覆盖**：涵盖了 $K \in [1, 50]$、Top-k $\in [30\%, 60\%]$、多种拓扑结构和代理规模。
*   **数据分布**：除了 IID（独立同分布）数据，还专门针对**非 IID 场景**（使用 Dirichlet 分布 $\alpha=0.5, 1.0$）进行了鲁棒性测试。
*   **客观性**：对所有基准方法进行了学习率调优，确保对比的公平性。实验结果不仅展示了最终精度，还详细记录了共识误差和通信量变化。

### 6. 主要结论与发现
*   **通信效率极大化**：通过结合 MLT ($K=50$) 和 CC (Top-k 30%)，LoDAdaC 在几乎不损失精度的情况下，将总通信量降低了 **99% 以上**。
*   **收敛速度优势**：自适应更新（Adam/AMSGrad）在收敛速度上显著优于传统的动量 SGD（SQuARM-SGD），尤其在语言模型训练中表现突出。
*   **线性加速**：理论和实验均证实，该框架随代理数量增加表现出良好的线性加速特性。
*   **拓扑鲁棒性**：在环形和网格拓扑下均能稳定收敛，且对一定程度的数据异构性（Non-IID）具有韧性。

### 7. 优点
*   **统一性强**：一个框架涵盖了多种主流自适应优化器，具有极高的通用性。
*   **理论完备**：填补了去中心化场景下“自适应+多步本地+压缩”组合算法收敛性分析的空白。
*   **实战价值高**：在 GPT 模型的去中心化训练中表现优异，证明了其在大模型时代的潜力。

### 8. 不足与局限
*   **理论假设限制**：收敛性证明依赖于“梯度有界”假设，这在某些极端深度的神经网络中可能过于理想化。
*   **模型规模**：虽然测试了 GPT 模型，但 10.7M 的参数量仍属于小规模，在超大规模参数（数十亿级）下的表现尚待验证。
*   **极端异构性挑战**：实验显示在极端的 Non-IID（如 Dirichlet $\alpha=0.1$）情况下，收敛速度会显著变慢甚至失败，需进一步引入漂移修正技术。

（完）
