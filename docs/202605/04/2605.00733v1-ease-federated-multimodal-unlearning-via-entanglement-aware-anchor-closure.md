---
title: "EASE: Federated Multimodal Unlearning via Entanglement-Aware Anchor Closure"
title_zh: EASE：通过纠缠感知锚点闭合实现的联邦多模态遗忘学习
authors: "Zihao Ding, Beining Wu, Jun Huang"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00733v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 具有隐私和遗忘机制的联邦多模态学习
tldr: 本研究针对联邦多模态学习中的遗忘问题，指出跨模态双线性耦合和客户端子空间纠缠是导致知识残留的关键。为此提出EASE框架，通过双向模态位移关闭跨模态重建通道，利用余弦-正弦分解隔离遗忘方向，并引入遗忘锁限制残余漂移。该方法在多个数据集上表现优异，实现了接近重新训练的遗忘效果，为联邦多模态隐私保护提供了高效的解决方案。
selection_source: fresh_fetch
motivation: 联邦多模态学习中的联合嵌入训练导致遗忘知识在模态间和客户端子空间中深度纠缠，传统方法难以彻底擦除残留的关联。
method: 提出EASE框架，通过双向模态位移、子空间切除及方向选择性遗忘锁，从模态和子空间维度切断三个关键的知识残留锚点。
result: "在Flickr30K数据集上，EASE在遗忘侧和保留侧的R@1指标与重新训练基准相比，差距分别仅为0.2和4.2个百分点。"
conclusion: EASE通过统一设计有效解决了多维度的纠缠问题，在保证保留数据性能的同时，实现了高效且精准的联邦多模态知识擦除。
---

## 摘要
联邦多模态学习（FML）在保持图像-文本对私密性的同时，跨去中心化客户端训练多模态模型。然而，联合嵌入训练使待遗忘知识在两种模态和客户端梯度子空间中发生纠缠，阻碍了联邦遗忘学习。先前的联邦遗忘学习方法既没有切断由双线性耦合介导的跨模态重构通道，也没有将仅属于遗忘数据的更新方向与保留客户端共享的方向分离开来。我们为联邦多模态对比遗忘学习确定了一个“锚点原则”：被遗忘的对齐关系通过三个残余锚点持续存在，这些锚点源于双线性跨模态耦合、主角子空间纠缠以及持续的联邦更新。在模态层面，我们证明了视觉和语言分支的双边置换可以关闭跨模态重构通道。相应地，我们的方法通过客户端更新子空间的余弦-正弦（Cosine-Sine）分解来解决子空间纠缠问题，从而将遗忘独有的方向从保留支持中分离出来。此外，我们提出了一种方向选择性的“遗忘锁”（Forget Lock），用于限制跨轮次的残余漂移。结合这些策略，我们提出了 EASE，一个纠缠感知子空间切除框架，在统一设计下关闭了所有三个锚点通道。EASE 在多个数据集和遗忘场景中表现出一致的优越性，例如，在 Flickr30K 数据集上使用 CLIP-B/32 进行客户端遗忘时，其在遗忘侧和保留侧的 R@1 指标与重新训练参考值的差距分别在 0.2 和 4.2 个百分点以内。

## Abstract
Federated Multimodal Learning (FML) trains multimodal models across decentralized clients while keeping their image-text pairs private. However, joint embedding training entangles forgotten knowledge across both modalities and client gradient subspaces, hindering federated unlearning. Previous federated unlearning approaches neither sever the cross-modal reconstruction channel mediated by bilinear coupling nor separate forget-exclusive update directions from those shared with retained clients. We identify an Anchor Principle for federated multimodal contrastive unlearning: forgotten alignments persist through three residual anchors arising from bilinear cross-modal coupling, principal-angle subspace entanglement, and continued federated updates. At the modality level, we show that bilateral displacement of both visual and language branches closes the cross-modal reconstruction channel. Correspondingly, our method addresses subspace entanglement through Cosine--Sine decomposition of client-update subspaces, isolating forget-exclusive directions from retain support. Moreover, we propose a direction-selective Forget Lock that bounds residual drift across rounds. Combining these strategies, we present EASE, an Entanglement-Aware Subspace Excision framework that closes all three anchor channels under a unified design. EASE demonstrates consistent superiority across multiple datasets and unlearning scenarios, for instance, matching the retrain reference to within 0.2 and 4.2 R@1 points on the forget and retain sides under client unlearning on Flickr30K with CLIP-B/32.

---

## 论文详细总结（自动生成）

### 论文总结：EASE —— 联邦多模态遗忘学习的纠缠感知子空间切除框架

#### 1. 核心问题与研究动机
*   **研究背景**：联邦多模态学习（FML）允许客户端在不共享原始数据的情况下共同训练模型（如 CLIP）。然而，根据“被遗忘权”规定，当客户端退出或数据过时时，系统必须执行“遗忘学习”（Unlearning），即在不重新训练整个模型的前提下，消除特定数据的影响。
*   **核心挑战（锚点原则）**：论文指出多模态对比学习中存在三个导致知识残留的“锚点”：
    1.  **模态锚点**：双线性对齐损失使得单一模态的梯度依赖于另一模态，仅修改一侧会导致另一侧保留重构信息。
    2.  **唯一子空间锚点**：客户端更新的梯度子空间在连续光谱上纠缠，遗忘方向与保留方向难以区分。
    3.  **时间重锚点**：后续的联邦聚合轮次会逐渐重写已删除的方向，导致遗忘效果失效。

#### 2. 方法论（EASE 框架）
EASE 提出通过“纠缠感知”的子空间切除来关闭上述三个锚点通道：
*   **双边知识切除 (BKE)**：同时对视觉和语言分支进行参数位移，切断跨模态重构通道，确保双侧嵌入都不再编码被遗忘的关联。
*   **梯度子空间分解 (GSD)**：
    *   利用服务器端存储的客户端更新历史，通过奇异值分解（SVD）提取子空间基。
    *   采用**余弦-正弦（Cosine-Sine）分解**计算主角，将遗忘子空间划分为“遗忘独有方向”（需切除的锚点）和“保留支持方向”（需保留的知识）。
*   **带遗忘锁的投影 (PFL)**：
    *   **服务器端**：将全局模型位移投影到唯一子空间的补空间上。
    *   **客户端**：引入方向选择性的“遗忘锁”正则项，在后续训练中惩罚沿遗忘方向的漂移，同时允许模型在正交方向上恢复保留知识。

#### 3. 实验设计
*   **数据集**：Flickr30K、MS COCO、TextCaps（涵盖通用检索和场景文本检索）。
*   **遗忘场景**：客户端遗忘（Client）、类别遗忘（Class，基于聚类）、样本遗忘（Sample）。
*   **模型架构**：CLIP-B/32、CLIP-L/14、SigLIP（均结合 LoRA 适配器）。
*   **对比方法**：包括 8 种联邦遗忘基准（如 FedEraser, Ferrari, NoT, SoUL 等）、1 种中心化方法（GradAscent）以及“从头重新训练”（Retrain）作为金标准。
*   **评估指标**：Recall@k（检索精度）、MIA（成员推理攻击）、LiRA（似然比攻击）、通信成本。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率体现**：论文强调了其方法基于轻量级的 LoRA 适配器和投影操作，通信开销远低于完全重新训练（Retrain），且 GSD 步骤复用了已有的梯度历史，未增加额外通信。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个数据集、3 种模型架构、3 种遗忘场景下进行了详尽测试，共涉及 9 种对比算法。
*   **充分性**：
    *   进行了**逐锚点消融实验**，验证了 BKE、GSD 和 PFL 缺一不可。
    *   进行了**敏感性分析**，探讨了阈值 $\delta$、锁强度 $\alpha$、客户端数量 $K$ 和异构性 $\beta$ 的影响。
    *   使用了 MIA 和 LiRA 等隐私审计工具，从安全角度验证了遗忘的彻底性。实验设计客观、全面，覆盖了非独立同分布（non-IID）等实际联邦场景。

#### 6. 主要结论与发现
*   **性能优越性**：EASE 在所有场景下最接近“重新训练”的效果。在 Flickr30K 上，其遗忘侧 R@1 与 Retrain 仅差 0.2，保留侧仅差 4.2。
*   **锚点机制验证**：实验证明，如果只关闭部分锚点（如仅单侧切除），模型仍能通过残余通道重构遗忘知识。
*   **权衡优化**：通过子空间分解，EASE 有效解决了“遗忘彻底性”与“保留完整性”之间的权衡难题。

#### 7. 优点
*   **理论与直觉结合**：首次形式化了多模态遗忘中的“锚点原则”，并提供了几何保证。
*   **针对性强**：专门解决了双线性耦合导致的模态纠缠问题，这是单模态遗忘方法所忽略的。
*   **部署友好**：基于 LoRA 和服务器端投影，不改变现有联邦学习的主流流程，计算开销低。

#### 8. 不足与局限
*   **架构限制**：目前主要针对双编码器（Dual-encoder）结构的对比学习模型，尚未验证在生成式多模态模型（如多模态大语言模型）或后期融合模型上的效果。
*   **类别定义**：在缺乏标签的检索数据集中，类别遗忘依赖于伪标签聚类，其语义准确性受限于预训练模型的表征能力。
*   **算力细节缺失**：缺乏具体的硬件运行环境描述，不便于其他研究者进行精确的复现成本评估。

（完）
