---
title: "DDP-SA: Scalable Privacy-Preserving Federated Learning via Distributed Differential Privacy and Secure Aggregation"
title_zh: DDP-SA：通过分布式差分隐私与安全聚合实现的可扩展隐私保护联邦学习
authors: "Wenjing Wei, Farid Nait-Abdesselam, Alla Jammine"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07125v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 通过本地差分隐私和安全聚合实现的隐私保护联邦学习
tldr: 本研究提出DDP-SA框架，旨在解决联邦学习中的隐私泄露问题。该框架创新性地结合了客户端本地差分隐私（LDP）与全阈值加法秘密共享（ASS）技术。通过两阶段保护机制——先对梯度添加拉普拉斯噪声，再将其分解为秘密份额分发至多个中间服务器，实现了在不牺牲计算效率的前提下，提供比单一技术更强的端到端隐私保障和更高的模型精度。
selection_source: fresh_fetch
motivation: 现有的联邦学习隐私保护方法通常仅依赖差分隐私或多方安全计算，难以在隐私强度、模型精度和计算开销之间取得理想平衡。
method: 客户端首先对局部梯度进行拉普拉斯噪声扰动，随后利用加法秘密共享将噪声梯度分解并分发给多个中间服务器进行安全聚合。
result: 实验表明，DDP-SA在模型精度上显著优于纯LDP方案，且在隐私保护强度上超越了纯MPC方法，同时具备良好的线性扩展性。
conclusion: DDP-SA为联邦学习提供了一种实用且可扩展的隐私保护方案，在可控的开销下实现了隐私与效用的双重提升。
---

## 摘要
本文提出了 DDP-SA，这是一个可扩展的隐私保护联邦学习框架，它共同利用客户端本地差分隐私 (LDP) 和全阈值加法秘密共享 (ASS) 来实现安全聚合。与仅依赖差分隐私或安全多方计算 (MPC) 的现有方法不同，DDP-SA 整合了这两种技术，在保持计算实用性的同时，提供了更强的端到端隐私保证。该框架引入了一种两阶段保护机制：客户端首先使用校准的拉普拉斯噪声扰动其本地梯度，然后将含噪梯度分解为加法秘密份额，并分发到多个中间服务器。这种设计确保了：(i) 任何单个受损服务器或通信通道都无法泄露有关单个客户端更新的任何信息；(ii) 参数服务器仅重构聚合后的含噪梯度，而绝不会重构任何特定于客户端的贡献。大量实验表明，DDP-SA 实现了比独立 LDP 显著更高的模型准确度，同时提供了比仅使用 MPC 的方法更强的隐私保护。所提出的框架随参与者数量线性扩展，并为联邦学习应用提供了一种实用的隐私保护解决方案，且计算和通信开销可控。

## Abstract
This article presents DDP-SA, a scalable privacy-preserving federated learning framework that jointly leverages client-side local differential privacy (LDP) and full-threshold additive secret sharing (ASS) for secure aggregation. Unlike existing methods that rely solely on differential privacy or on secure multi-party computation (MPC), DDP-SA integrates both techniques to deliver stronger end-to-end privacy guarantees while remaining computationally practical. The framework introduces a two-stage protection mechanism: clients first perturb their local gradients with calibrated Laplace noise, then decompose the noisy gradients into additive secret shares that are distributed across multiple intermediate servers. This design ensures that (i) no single compromised server or communication channel can reveal any information about individual client updates, and (ii) the parameter server reconstructs only the aggregated noisy gradient, never any client-specific contribution. Extensive experiments show that DDP-SA achieves substantially higher model accuracy than standalone LDP while providing stronger privacy protection than MPC-only approaches. The proposed framework scales linearly with the number of participants and offers a practical, privacy-preserving solution for federated learning applications with controllable computational and communication overhead.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DDP-SA** 的联邦学习（FL）隐私保护框架，旨在通过结合差分隐私与密码学技术，在保证模型效用的同时提供更强的安全保障。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的联邦学习虽然不直接共享原始数据，但共享的梯度或模型参数仍易受到**隐私推理攻击**（如成员推理、模型反置、梯度泄露攻击等）。
*   **研究动机**：
    *   **差分隐私（DP）的局限**：虽然能提供统计学隐私保证，但为了获得高强度保护需要添加大量噪声，导致模型准确率大幅下降。
    *   **多方安全计算（MPC）的局限**：虽然能实现无损聚合，但计算和通信开销巨大，且无法防御针对聚合结果的推理攻击。
    *   **研究目标**：设计一个既能防御服务器端推理攻击，又能保持高准确率且具备线性扩展性的混合框架。

### 2. 方法论：DDP-SA 框架
DDP-SA 的核心思想是**“本地扰动 + 分布式聚合”**，具体流程如下：
*   **两阶段保护机制**：
    1.  **本地差分隐私（LDP）阶段**：客户端首先对局部梯度进行 $\ell_1$ 范数裁剪（Clipping），随后添加校准后的**拉普拉斯噪声**。
    2.  **安全聚合阶段**：客户端将含噪梯度进行定点编码（Fixed-point encoding），利用**全阈值加法秘密共享（ASS）**将其分解为 $m$ 个秘密份额。
*   **多层架构设计**：
    *   **客户端**：执行本地训练、加噪和分发份额。
    *   **中间服务器（$m$ 个）**：每个服务器接收所有客户端的一个份额并进行局部求和。
    *   **参数服务器（PS）**：收集所有中间服务器的局部和，重构出最终的聚合含噪梯度，并更新全局模型。
*   **关键技术细节**：
    *   利用 DP 的**后处理不变性**（Post-processing invariance），证明了在安全聚合后，端到端的隐私保证依然维持在 $(\epsilon, \delta)$ 水平。
    *   通过 ASS 确保只要不是所有中间服务器全部合谋，任何单一实体都无法获知单个客户端的梯度。

### 3. 实验设计
*   **数据集与场景**：使用合成数据集（10,000 条样本），执行线性回归任务（$y = x_1 + x_2 + 1$）。
*   **对比方法（Benchmark）**：
    1.  **No-Private**：无隐私保护的标准联邦学习。
    2.  **MPC**：仅使用安全聚合（无噪声）。
    3.  **LDP**：仅在客户端添加本地差分隐私噪声。
    4.  **DDP-SA**：本文提出的混合方案。
*   **评估指标**：测试损失（Test Loss）、决定系数（$R^2$）、通信轮数、训练时长、每轮平均耗时。

### 4. 资源与算力
*   **硬件环境**：实验在 GitHub Codespaces 上运行。
*   **配置**：16 核 CPU，64 GB RAM，128 GB 存储。
*   **算力说明**：论文**未提到使用 GPU**，主要依赖 CPU 进行计算。由于任务是简单的线性回归和两层神经网络，CPU 算力已足够。
*   **训练时长**：No-Private 约 112 分钟，DDP-SA 约 203 分钟（达到收敛）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了不同隐私预算 $\epsilon$（0.1 到 0.6）对准确率的影响。
    *   测试了不同客户端数量 $n$（2 到 6）的扩展性。
    *   进行了组件级的开销分解分析（计算与通信）。
*   **充分性评价**：实验在验证算法逻辑、隐私-效用权衡以及扩展性方面是**充分且客观**的。然而，由于仅使用了简单的合成数据集和低维模型，其在复杂深度学习任务（如 ImageNet 分类）上的表现尚待进一步验证。

### 6. 主要结论与发现
*   **准确率提升**：在相同隐私预算下，DDP-SA 的 $R^2$ 分数（约 0.9666）显著高于纯 LDP 方案（约 0.9357）。
*   **隐私增强**：DDP-SA 提供了双重保护，既有 DP 的统计保证，又有 ASS 的密码学隔离，能更有效地抵御多种推理攻击。
*   **开销可控**：虽然 DDP-SA 的通信量随中间服务器数量 $m$ 线性增加，但其计算瓶颈在于梯度裁剪而非加密操作。
*   **扩展性**：框架支持任意数量的客户端和中间服务器，且参数服务器的带宽压力从 $O(n)$ 优化为 $O(m)$。

### 7. 亮点与优点
*   **混合防御**：巧妙结合了 LDP 和 MPC 的优势，解决了 LDP 噪声过大和 MPC 无法防御聚合结果推理的问题。
*   **线性扩展性**：通过引入中间服务器层，实现了通信和计算开销的线性增长，适合大规模部署。
*   **理论完备**：提供了详尽的单轮及多轮隐私组合分析（基于高级组合定理）。
*   **实用性强**：相比复杂的同态加密，加法秘密共享的计算开销极低。

### 8. 不足与局限
*   **实验任务过于简单**：仅在合成数据和线性回归上验证，缺乏在非独立同分布（Non-IID）数据和大型卷积神经网络（CNN）上的测试。
*   **容错性缺失**：论文未讨论客户端掉线（Dropout）时的处理机制，而这在实际联邦学习场景中非常常见。
*   **合谋风险**：虽然是全阈值共享，但如果所有 $m$ 个中间服务器被攻击者控制，安全聚合阶段的加密将失效（退化为纯 LDP）。
*   **半诚实模型限制**：目前仅在半诚实（Semi-honest）模型下讨论，未考虑恶意（Malicious）参与者篡改数据的情况。

（完）
