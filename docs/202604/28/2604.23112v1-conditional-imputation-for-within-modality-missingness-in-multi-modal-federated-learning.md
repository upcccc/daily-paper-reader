---
title: Conditional Imputation for Within-Modality Missingness in Multi-Modal Federated Learning
authors: "Wugeng Zheng, Ziwen Kan, Katie Wang, Chen Chen, Song Wang"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23112v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 多模态联邦学习中的隐私保护协作训练
tldr: 针对多模态联邦学习中常见的模态内数据缺失问题，本文提出了CondI框架。该框架利用条件扩散模型，通过多模态上下文显式补全缺失的原始数据，而非仅在特征层面进行隐式对齐。CondI采用两阶段训练策略，先进行数据补全，再优化特征提取器与联合嵌入空间，从而在临床数据集上实现了更具鲁棒性的表现，有效提升了模型在严重数据缺失情况下的性能。
selection_source: fresh_fetch
motivation: 现有的多模态联邦学习方法在处理模态内缺失时，往往无法准确恢复真实的数据分布，导致模型在临床应用中性能受限。
method: 提出CondI框架，利用条件扩散模型结合多模态上下文显式补全缺失数据，并配合两阶段训练流程优化特征提取与下游任务。
result: 在PTB-XL、SLEEP-EDF和MIMIC-IV三个临床数据集上的实验表明，CondI在应对严重数据缺失时表现出极强的鲁棒性，达到了最先进的水平。
conclusion: 显式的数据补全能确保模型在完整的语义结构上运行，是解决多模态联邦学习中数据缺失问题的有效途径。
---

## Abstract
Multimodal Federated Learning (MMFL) enables privacy-preserving collaborative training, but real-world clinical applications often suffer from within-modality missingness caused by sensor intermittency or irregular sampling. Existing methods implicitly represent unobserved data via architectural alignment or missing embeddings, often failing to recover the true distribution and yielding sub-optimal performance. We propose CondI, a federated framework explicitly addressing this missingness using conditional diffusion models. CondI employs a two-phase training pipeline: first, imputing unobserved temporal components using available multimodal context and conditional embeddings; second, optimizing modality-specific extractors and joint embedding spaces. During inference, imputed raw data pass through trained extractors to generate robust features, providing a holistic representation for downstream tasks. Explicit data imputation ensures models operate on complete semantic structures, significantly enhancing resilience against severe data incompleteness. Experiments on three clinical datasets (PTB-XL, SLEEP-EDF, MIMIC-IV) demonstrate CondI achieves comparable results to state-of-the-art baselines. Code: https://github.com/ZhengWugeng/CondI

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **CondI** 的新型多模态联邦学习（MMFL）框架，旨在解决临床应用中常见的“模态内数据缺失”（Within-Modality Missingness）问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实的临床多模态联邦学习场景中，由于传感器间歇性故障或采样频率不一致，经常会出现模态内部的数据片段缺失（而非整个模态缺失）。
*   **研究动机**：
    *   **现有方法局限性**：目前的 MMFL 方法大多采用“隐式”处理方式，如通过架构对齐或学习一个代表“缺失”的嵌入向量（Missing Embedding）。这种方式无法恢复真实的数据分布，在缺失严重时会导致模型性能大幅下降。
    *   **语义断裂**：生理信号（如血压与心率）之间存在强相关性，隐式方法仅“标记”缺失而未利用这种相关性进行恢复，导致多模态融合空间出现语义歧义。
    *   **隐私挑战**：在联邦学习环境下，无法直接访问全局数据进行集中式补全，必须在保护隐私的前提下实现跨客户端的知识共享以修复缺失。

### 2. 方法论
**CondI** 的核心思想是利用**条件扩散模型（Conditional Diffusion Models）**显式地补全缺失的原始数据。

*   **核心技术细节**：
    1.  **可学习嵌入空间 ($W$)**：引入了三种嵌入：$W_{mod}$（模态特征）、$W_{ins}$（实例语义）和 $W_{cond}$（跨模态条件嵌入）。其中 $W_{cond}$ 用于建模传感器间的关联规则。
    2.  **两阶段训练流程**：
        *   **阶段 A（条件估计）**：利用观察到的模态上下文和 $W_{cond}$ 作为条件，指导扩散模型生成缺失的时间序列片段。通过自监督掩码策略进行训练。
        *   **阶段 B（任务优化）**：在补全数据的基础上，优化特征提取器和分类头，确保模型能够捕捉到完整的语义结构。
    3.  **推理流程**：在推理阶段，首先通过训练好的扩散模型对原始数据进行显式补全，然后将“完整”的数据输入特征提取器进行下游预测。
*   **算法逻辑**：通过 $W_{cond}$ 聚合来自存活模态的知识，为缺失模态提供结构化先验，公式化表示为条件向量 $c(b)$ 引导的去噪过程。

### 3. 实验设计
*   **数据集**：
    *   **PTB-XL**：大型临床心电图（ECG）数据集，涉及12个导联（模态）。
    *   **SLEEP-EDF**：睡眠分期数据集，包含EEG、EOG、EMG等5种生理信号模态。
    *   **MIMIC-IV**：综合性ICU电子健康记录，用于预测住院死亡率（48-IHM）和住院时长（48-LOS）。
*   **Benchmark 与对比方法**：
    *   对比了 **FedAvg**（基础联邦学习）、**FedMM**（多模态联邦基础方法）以及 **PEPSY**（目前处理缺失模态的最强基准之一）。
    *   设置了不同的样本缺失率 ($p_s$) 和模态内缺失率 ($p_w$)，最高达 80% 的极端缺失情况。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或具体的训练总时长。
*   **训练参数**：提到了 PTB-XL 和 SLEEP-EDF 训练了 70 个通信轮次，MIMIC-IV 训练了 30 个轮次，每轮客户端参与率为 0.5。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同类型的临床任务上进行了广泛测试，涵盖了分类、回归和时序预测。
*   **消融实验**：专门设计了消融实验验证“补全模块”和“CondI 条件模块”的必要性，结果显示移除任何一个都会导致性能下降。
*   **特征重建分析**：通过 L2 距离和余弦相似度分析了 628 个测试样本，证明补全后的特征在空间上确实更接近真实完整数据的特征。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还通过可视化和距离度量验证了生成数据的质量，客观性较高。

### 6. 主要结论与发现
*   **显式补全优于隐式对齐**：CondI 在所有数据集上的表现均优于隐式方法（如 PEPSY），尤其在缺失率高达 80% 的严苛条件下，性能优势更明显。
*   **稳定性强**：随着缺失程度增加，CondI 的性能下降幅度远小于基准方法，显示出极强的鲁棒性。
*   **跨客户端知识转移**：通过在服务器端聚合 $W_{cond}$，客户端可以利用其他医院的关联知识来补全本地缺失的传感器数据。

### 7. 优点
*   **创新性**：首次在联邦学习框架下引入条件扩散模型来解决细粒度的模态内缺失问题。
*   **语义保留**：通过显式补全原始信号，最大限度地保留了生理信号间的生物学关联。
*   **灵活性**：两阶段设计使得补全模块和分类模块可以解耦，易于适配不同的下游任务。

### 8. 不足与局限
*   **计算开销**：扩散模型的推理过程（去噪步骤）通常比简单的嵌入查找要耗时得多，论文未详细讨论在资源受限的边缘设备（如医疗传感器）上的部署可行性。
*   **数据量依赖**：在缺失率极低（如 $p_s=0.2$）时，扩散模型的优势不明显，甚至在某些指标上略逊于 PEPSY，说明该方法更适合“雪中送炭”而非“锦上添花”。
*   **通信成本**：虽然嵌入矩阵较小，但扩散模型的参数同步可能会增加联邦学习的通信负担，文中对此缺乏定量分析。

（完）
