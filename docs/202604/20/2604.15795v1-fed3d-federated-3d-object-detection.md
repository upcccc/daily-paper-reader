---
title: "Fed3D: Federated 3D Object Detection"
title_zh: Fed3D：联邦 3D 目标检测
authors: "Suyan Dai, Chenxi Liu, Fazeng Li, Peican Lin"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15795v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 具有隐私保护功能的联邦3D目标检测框架
tldr: 针对多机器人感知网络中3D目标检测面临的隐私泄露、数据异构性和通信带宽限制等挑战，本文提出了首个联邦3D目标检测框架Fed3D。该框架通过引入局部-全局类别感知损失来平衡不同类别的梯度回传，解决数据异构问题；同时设计了联邦3D提示模块，仅需传输少量可学习参数即可实现高效通信。实验证明，Fed3D在有限本地数据下性能显著优于现有算法，且通信成本更低。
selection_source: fresh_fetch
motivation: 现有的3D目标检测方法在多机器人协作时存在严重的隐私风险，且面临3D数据异构和通信带宽受限的挑战。
method: 提出了Fed3D框架，包含用于解决异构性的局部-全局类别感知损失，以及用于降低通信开销的联邦3D提示模块。
result: 实验结果显示，Fed3D在减少通信成本的同时，其检测性能显著超越了现有的最先进算法。
conclusion: Fed3D为隐私保护下的分布式3D目标检测提供了有效的解决方案，在处理异构数据和低带宽通信方面具有明显优势。
---

## 摘要
在单一服务器上训练的 3D 目标检测模型在自动驾驶、机器人操作和增强现实场景中发挥着重要作用。然而，当部署在多机器人感知网络中以探索大规模 3D 场景时，大多数现有方法都面临严重的隐私问题。同时，由于 3D 数据异构性和有限的通信带宽，在 3D 目标检测场景中采用传统的联邦学习方法具有极大的挑战性。在本文中，我们首次尝试提出一种新型联邦 3D 目标检测框架（即 Fed3D），以实现具有隐私保护的 3D 目标检测分布式学习。具体而言，考虑到本地机器人中不规则的输入 3D 物体以及机器人之间不同的类别分布可能分别导致局部异构性和全局异构性，我们针对 3D 数据异构性问题提出了一种局部-全局类别感知损失，该损失可以从局部和全局两个方面平衡不同 3D 类别的梯度反向传播速率。为了降低每一轮的通信成本，我们开发了一个联邦 3D 提示模块，该模块仅学习和通信具有少量可学习参数的提示。最后，在联邦 3D 目标检测上进行的多次广泛实验表明，在提供有限的本地训练数据时，我们的 Fed3D 模型显著优于最先进的算法，且通信成本更低。

## Abstract
3D object detection models trained in one server plays an important role in autonomous driving, robotics manipulation, and augmented reality scenarios. However, most existing methods face severe privacy concern when deployed on a multi-robot perception network to explore large-scale 3D scene. Meanwhile, it is highly challenging to employ conventional federated learning methods on 3D object detection scenes, due to the 3D data heterogeneity and limited communication bandwidth. In this paper, we take the first attempt to propose a novel Federated 3D object detection framework (i.e., Fed3D), to enable distributed learning for 3D object detection with privacy preservation. Specifically, considering the irregular input 3D object in local robot and various category distribution between robots could cause local heterogeneity and global heterogeneity, respectively. We then propose a local-global class-aware loss for the 3D data heterogeneity issue, which could balance gradient back-propagation rate of different 3D categories from local and global aspects. To reduce communication cost on each round, we develop a federated 3D prompt module, which could only learn and communicate the prompts with few learnable parameters. To the end, several extensive experiments on federated 3D object detection show that our Fed3D model significantly outperforms state-of-the-art algorithms with lower communication cost when providing the limited local training data.

---

## 论文详细总结（自动生成）

以下是对论文《Fed3D: Federated 3D Object Detection》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保护隐私的前提下，在多机器人感知网络中实现高效的分布式 3D 目标检测。
*   **研究动机**：
    *   **隐私风险**：传统的 3D 检测模型依赖集中式训练，但在自动驾驶和协作机器人场景中，直接共享原始点云数据存在严重的隐私泄露风险。
    *   **数据异构性**：3D 数据具有不规则性和稀疏性，不同机器人采集的数据在物体形状（局部异构）和类别分布（全局异构）上存在显著差异。
    *   **通信瓶颈**：3D 检测模型通常参数量巨大，在边缘设备（如移动机器人）之间传输完整模型会消耗极高的通信带宽。

### 2. 方法论
论文提出了 **Fed3D** 框架，主要包含两个核心创新点：
*   **局部-全局类别感知损失 (Local-global Class-aware Loss)**：
    *   **核心思想**：通过梯度反向传播速率来衡量和平衡数据不平衡。
    *   **技术细节**：引入“校正系数”。在本地训练时，根据样本类别的梯度幅值计算局部校正系数 $R_i$；在服务器端，汇总各客户端的类别分布信息生成全局校正系数 $G_o$。通过重加权损失函数，减缓多数类或简单样本的梯度更新，增强少数类或困难样本的影响，从而缓解非独立同分布（Non-IID）带来的性能下降。
*   **联邦 3D 提示学习 (Federated Prompt Learning, FPL)**：
    *   **核心思想**：借鉴自然语言处理中的 Prompt Tuning，仅训练和传输极少量的参数。
    *   **技术细节**：使用预训练且冻结的 ViT 作为骨干网络，在编码器层中插入可学习的“提示池”（Prompt Pool）。在联邦通信过程中，客户端仅上传/下载提示池参数和检测头参数，而非整个骨干网络。这显著降低了通信开销，并增加了攻击者逆向还原原始数据的难度。

### 3. 实验设计
*   **数据集**：使用了两个主流的室内 3D 目标检测数据集：**ScanNet V2**（1513 个室内场景，18 个类别）和 **SUN RGB-D**（10335 张图像，10 个主要类别）。
*   **实验设置**：模拟了两种联邦场景：
    *   100 个客户端，每轮随机选择 10% 参与。
    *   20 个客户端，每轮随机选择 25% 参与。
    *   数据分配采用非独立同分布（Non-IID）设置，每个客户端仅持有 70% 的类别和 70% 的样本。
*   **对比方法 (Baselines)**：
    *   **基准**：本地训练（Local-only）、集中式训练（Centralized）。
    *   **联邦学习算法**：FedAvg, FedProx, SCAFFOLD, MOON, FedDyn。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号和数量。
*   **训练细节**：提到了使用 AdamW 优化器，学习率为 $3.5 \times 10^{-4}$（编码器）和 $7.0 \times 10^{-4}$（其他模块），输入点云数量为 50k，Batch Size 为 8。共进行 100 轮全局通信，每轮本地训练 4 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集上进行了详尽的对比实验，涵盖了 mAP@0.25、mAP@0.5、AR@0.25 和 AR@0.5 等多个评价指标。
*   **消融实验**：专门针对 FPL（提示学习）、LCC（局部校正）和 GCC（全局校正）进行了消融研究，验证了各模块的有效性。
*   **客观性**：实验设置了统一的骨干网络（基于 PiMAE）和数据增强策略，对比了多种经典的联邦学习改进算法，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：Fed3D 在所有设置下均优于现有的联邦学习算法，在 ScanNet V2 上 mAP@0.25 提升了约 0.8%~4.8%。
*   **通信高效**：相比于传输完整模型的传统联邦学习，Fed3D 的通信成本降低了 **50%**（从 86.2M 降至 43.6M 参数量）。
*   **鲁棒性**：局部-全局类别感知损失能有效应对 3D 场景中严重的类别不平衡问题，使模型在异构数据下更易收敛。

### 7. 优点
*   **首创性**：这是首个专门针对 3D 目标检测任务设计的联邦学习框架。
*   **通信与隐私的双赢**：通过提示学习（Prompt Learning）同时解决了通信带宽受限和隐私保护增强的问题。
*   **梯度均衡策略**：提出的损失函数从梯度层面直接干预，比单纯的数据增强更适合隐私受限的联邦场景。

### 8. 不足与局限
*   **场景局限性**：实验仅限于**室内场景**（ScanNet, SUN RGB-D），未在自动驾驶等更复杂的**室外大规模场景**（如 KITTI, Waymo）上进行验证。
*   **预训练依赖**：该方法依赖于高质量的预训练骨干网络（PiMAE），如果初始模型与目标领域差异过大，提示学习的效果可能会受限。
*   **动态性考虑不足**：未讨论在机器人移动过程中，网络连接不稳定或客户端掉线（Stragglers）对模型聚合的影响。

（完）
