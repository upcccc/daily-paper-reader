---
title: "DECIFR: Domain-Aware Exfiltration of Circuit Information from Federated Gradient Reconstruction"
title_zh: DECIFR：基于联邦梯度重构的领域感知电路信息窃取
authors: "Gijung Lee, Wavid Bowman, Olivia P. Dizon-Paradis, Reiner N. Dizon-Paradis, Ronald Wilson, Damon L. Woodard, Domenic Forte"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19915v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 联邦学习中的隐私风险与梯度重构
tldr: 本研究针对硬件保证领域中的联邦学习安全性展开探讨，揭示了利用领域特定知识（如标准单元库布局SCLL）窃取敏感集成电路训练数据的漏洞。提出DECIFR，一种无需辅助数据集的两阶段成员推理攻击（MIA），通过引导式梯度反演从模型更新中重建训练图像。该方法证明了重建图像质量与成员身份直接相关，揭示了传统FL协议在具备领域知识的攻击者面前的脆弱性，强调了加强防御的必要性。
selection_source: fresh_fetch
motivation: 旨在探究联邦学习在硬件保证领域中，面对拥有领域特定知识（如电路布局）的攻击者时是否存在隐私泄露风险。
method: 提出DECIFR框架，利用标准单元库布局引导梯度反演攻击，从模型更新中重建图像并执行成员推理。
result: 重建图像的质量与成员身份显著相关，使攻击者能够在无需辅助数据集的情况下准确区分成员与非成员。
conclusion: 标准联邦学习协议不足以抵御基于领域知识的攻击，在硬件保证领域应用FL时必须部署更鲁棒的防御措施。
---

## 摘要
联邦学习 (FL) 作为硬件保证中的一种隐私保护技术，是多方协作的一种极具前景的方法，但其针对具有领域特定知识的攻击者的安全性尚未得到充分研究。本文揭示了一个关键漏洞，即可以利用现有的标准单元库布局 (SCLL) 来损害敏感集成电路 (IC) 训练数据的隐私。我们提出了 DECIFR，这是一种无需辅助数据集的新型两阶段成员推理攻击 (MIA)。该攻击采用引导式梯度反演攻击 (GIA)，从截获的模型更新中重构客户端的训练图像。我们的研究结果表明，这些重构图像的保真度与成员身份直接相关，使得攻击者能够根据图像质量可靠地区分成员与非成员。这项工作揭示了一种克服了传统攻击局限性的实际威胁，并强调了标准联邦学习协议不足以保护具有广泛知识的领域。我们的结论是，对于联邦学习在硬件保证中的安全应用，鲁棒的防御措施至关重要。

## Abstract
Federated Learning (FL) is a promising approach for multiparty collaboration as a privacy-preserving technique in hardware assurance, but its security against adversaries with domain-specific knowledge is underexplored. This paper demonstrates a critical vulnerability where available standard cell library layouts (SCLL) can be exploited to compromise the privacy of sensitive integrated circuit (IC) training data. We introduce DECIFR, a novel two-stage Membership Inference Attack (MIA) that requires no auxiliary dataset. The attack employs a guided Gradient Inversion Attack (GIA) to reconstruct a client's training images from intercepted model updates. Our findings reveal that the fidelity of these reconstructions directly correlates with membership status, allowing an adversary to reliably distinguish members from non-members based on image quality. This work exposes a practical threat that overcomes the limitations of conventional attacks and underscores that standard FL protocols are insufficient for securing domains with extensive knowledge. We conclude that robust defenses are essential for the secure application of FL in hardware assurance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DECIFR** 的新型攻击框架，旨在揭示联邦学习（FL）在硬件保证（Hardware Assurance）应用中的隐私脆弱性。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在集成电路（IC）安全领域，由于扫描电子显微镜（SEM）图像获取成本高且涉及敏感知识产权（IP），联邦学习被视为一种理想的隐私保护协作方案。然而，该研究探讨了一个关键漏洞：**攻击者是否可以利用公开或半公开的领域知识（如标准单元库布局 SCLL）来破解 FL 的隐私保护？**
*   **研究动机**：现有的成员推理攻击（MIA）通常需要辅助数据集，这在高度专业化的硬件领域很难获得。论文旨在证明，即便没有辅助数据，仅凭领域特定的设计知识，攻击者也能通过截获模型更新来推断客户端是否使用了特定的硬件设计（如特定的工艺节点或金属层）。

### 2. 论文提出的方法论：DECIFR 框架
DECIFR 是一种两阶段的成员推理攻击，核心思想是**“引导式梯度反演”**：
*   **第一阶段：梯度提取**。攻击者（通常是诚实但好奇的服务端）截获客户端发送的模型更新，通过当前权重与上一轮权重的差异推导出梯度。
*   **第二阶段：引导式梯度反演（GIA）**。
    *   利用 SCLL 作为“伪标签”（Dummy Labels）来引导优化过程。
    *   **核心公式/损失函数**：$L_{total} = L_{grad} + \lambda_{tv} L_{tv} - \lambda_{dummy} L_{dummy}$。
    *   **关键技术细节**：引入了**负前向传播损失（Negative Forward Pass Loss, $-L_{dummy}$）**。这一项的作用是防止优化器仅仅为了拟合伪标签而产生平庸结果，迫使优化器必须依赖梯度匹配来重建图像，从而放大成员与非成员之间的重建质量差异。
*   **第三阶段：成员推理**。对重建图像进行后处理（高斯模糊、Otsu 阈值处理等），计算其与 SCLL 的 Dice 相似系数。如果得分高于设定阈值，则判定为“成员”。

### 3. 实验设计
*   **数据集与场景**：
    *   使用 Synopsys 的 32nm 和 90nm 开源设计工具包（SAED）提取标准单元布局。
    *   利用 REFICS 工具生成合成的 SEM 图像（模拟真实成像中的噪声、亮度等）。
    *   **场景 A（跨层攻击）**：区分金属层（Metal）与扩散层（Diffusion）。
    *   **场景 B（跨节点攻击）**：区分 32nm 与 90nm 工艺节点。
*   **Benchmark 与对比**：
    *   模型架构：基于 Pix2Pix 的 16 层 U-Net 分割模型（约 54M 参数）。
    *   对比基准：主要对比了在不同 $\lambda_{dummy}$ 参数下的重建效果，以及成员与非成员在 Dice 得分分布上的差异。

### 4. 资源与算力
*   **硬件配置**：实验使用了高性能计算节点，配备 **AMD EPYC ROME CPU (32GB RAM)** 和 **NVIDIA B200 GPU (180GB vRAM)**。
*   **训练细节**：模拟了 2 个客户端的 FL 环境，使用 FedAvg 算法，进行了 200 轮通信，每个客户端每轮进行 2 个本地 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了跨层（金属 vs 扩散）和跨节点（32nm vs 90nm）的多种组合测试。
*   **消融实验**：专门针对 $L_{dummy}$ 项进行了消融研究，验证了其在处理复杂结构（如 32nm 金属层）时的必要性。
*   **充分性评价**：实验设计较为科学，通过 ROC 曲线和 AUC 指标量化了攻击性能。虽然客户端数量较少（2个），但作为概念验证（PoC）足以证明该攻击路径的有效性。

### 6. 主要结论与发现
*   **重建质量与成员身份挂钩**：如果客户端训练数据中包含某类硬件特征，利用对应的 SCLL 引导重构出的图像质量显著更高。
*   **领域知识的威力**：即便没有原始训练数据的分布信息，仅靠电路布局图（SCLL）也能实现高精度的成员推理。
*   **负损失项的关键作用**：对于复杂的金属层结构，标准的梯度反演效果较差，而引入 $-L_{dummy}$ 能显著拉开成员与非成员的得分差距，将 AUC 从 0.88 提升至 0.97 以上。
*   **隐私威胁升级**：这种攻击不仅泄露数据存在性，还泄露了底层的硬件元数据（如工艺节点），这会加速逆向工程和侧信道攻击。

### 7. 优点
*   **无需辅助数据**：克服了传统 MIA 攻击必须依赖相似分布数据集的局限性，利用领域知识（NDA 下的 PDK 信息）作为替代。
*   **针对分割模型**：大多数 GIA 研究集中在分类模型，本文成功将其扩展到更复杂的图像分割任务中。
*   **创新的损失函数**：提出的负前向传播损失项有效地解决了复杂精细结构重构中的瓶颈问题。

### 8. 不足与局限
*   **实验环境简化**：仅在 2 个客户端的理想 FL 环境下测试，未探讨在更大规模客户端（梯度聚合更严重）或存在非独立同分布（Non-IID）数据时的表现。
*   **合成数据的偏差**：实验基于合成的 SEM 图像，真实环境中的 SEM 图像噪声和伪影可能更复杂，会影响重构的 Dice 得分。
*   **防御手段探讨不足**：虽然提到了差分隐私（DP）和同态加密（HE），但未在实验中实际测试这些防御措施对 DECIFR 攻击的具体削弱程度。

（完）
