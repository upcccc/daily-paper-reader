---
title: Sample Selection Using Multi-Task Autoencoders in Federated Learning with Non-IID Data
title_zh: 非独立同分布数据下联邦学习中基于多任务自编码器的样本选择
authors: "Emre Ardıç, Yakup Genç"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26116v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 确保数据隐私的联邦学习
tldr: 针对联邦学习中非独立同分布（Non-IID）及噪声数据导致的模型性能下降问题，本文提出一种基于多任务自编码器的样本选择方法。该方法通过损失和特征分析评估样本贡献，结合一类支持向量机（OCSVM）、孤立森林等无监督异常检测技术过滤噪声，并引入联邦多类深层SVDD损失增强特征选择。实验证明，该方法在CIFAR10和MNIST数据集上显著提升了模型准确率和鲁棒性。
selection_source: fresh_fetch
motivation: 联邦学习中冗余、恶意或异常样本会严重阻碍模型训练效率并导致性能退化。
method: 利用多任务自编码器结合OCSVM、孤立森林及自适应阈值等技术，在客户端过滤噪声并利用中心服务器控制的SVDD损失优化特征选择。
result: "在不同噪声水平和客户端数量下，该方法在CIFAR10上最高提升了7.02%的准确率，在MNIST上提升了1.83%。"
conclusion: 该样本选择框架能有效应对非独立同分布数据和噪声干扰，显著增强了联邦学习模型的准确性与稳定性。
---

## 摘要
联邦学习是一种机器学习范式，其中多个设备在中央服务器的监督下协作训练模型，同时确保数据隐私。然而，其性能往往受到冗余、恶意或异常样本的阻碍，导致模型退化和效率低下。为了克服这些问题，我们针对图像分类提出了新颖的样本选择方法，利用多任务自编码器通过损失和特征分析来估计样本贡献。我们的方法结合了无监督异常检测，使用由中央服务器管理的单类支持向量机（OCSVM）、孤立森林（IF）和自适应损失阈值（AT）方法来过滤客户端上的噪声样本。我们还提出了一种由中央服务器控制的多类深度支持向量数据描述（SVDD）损失，以增强基于特征的样本选择。我们在 CIFAR10 和 MNIST 数据集上验证了我们的方法，涵盖了不同的客户端数量、非独立同分布（non-IID）分布以及高达 40% 的噪声水平。结果表明，基于损失的样本选择显著提高了准确率，在 CIFAR10 上使用 OCSVM 实现了高达 7.02% 的提升，在 MNIST 上使用 AT 实现了 1.83% 的提升。此外，我们的联邦 SVDD 损失进一步改进了基于特征的样本选择，在 CIFAR10 上使用 OCSVM 带来了高达 0.99% 的准确率增益。这些结果证明了我们的方法在不同客户端数量和噪声条件下提高模型准确性的有效性。

## Abstract
Federated learning is a machine learning paradigm in which multiple devices collaboratively train a model under the supervision of a central server while ensuring data privacy. However, its performance is often hindered by redundant, malicious, or abnormal samples, leading to model degradation and inefficiency. To overcome these issues, we propose novel sample selection methods for image classification, employing a multitask autoencoder to estimate sample contributions through loss and feature analysis. Our approach incorporates unsupervised outlier detection, using one-class support vector machine (OCSVM), isolation forest (IF), and adaptive loss threshold (AT) methods managed by a central server to filter noisy samples on clients. We also propose a multi-class deep support vector data description (SVDD) loss controlled by a central server to enhance feature-based sample selection. We validate our methods on CIFAR10 and MNIST datasets across varying numbers of clients, non-IID distributions, and noise levels up to 40%. The results show significant accuracy improvements with loss-based sample selection, achieving gains of up to 7.02% on CIFAR10 with OCSVM and 1.83% on MNIST with AT. Additionally, our federated SVDD loss further improves feature-based sample selection, yielding accuracy gains of up to 0.99% on CIFAR10 with OCSVM. These results show the effectiveness of our methods in improving model accuracy across various client counts and noise conditions.

---

## 论文详细总结（自动生成）

这篇论文针对联邦学习（Federated Learning, FL）在处理非独立同分布（Non-IID）及噪声数据时的性能退化问题，提出了一套基于多任务自编码器的样本选择框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在联邦学习中，客户端生成的数据往往包含冗余、恶意或异常样本（噪声），且数据分布呈现非独立同分布（Non-IID）特性。这些低质量数据会干扰全局模型的收敛，降低分类准确率。
*   **研究动机**：现有的数据估值方法（如沙普利值 Shapley Value）计算成本极高，不适用于资源受限的客户端。因此，需要一种高效、隐私保护且能自动过滤异常样本的本地样本选择机制。

### 2. 方法论
论文提出了**多任务自编码器（MTAE）**架构及配套的样本选择策略：
*   **核心思想**：利用自编码器同时执行“图像重建（IR）”和“图像分类（IC）”任务。通过分析重建损失（MSE）和分类损失（CE），识别像素级异常和标签级异常。
*   **关键技术细节**：
    *   **MTAE 架构**：包含编码器（提取特征 $z$）、解码器（重建图像）和分类器（预测标签）。总损失为 $L = \lambda_{rec} L_{rec} + \lambda_{cls} L_{cls}$。
    *   **无监督异常检测**：在服务器端利用客户端上传的损失或特征训练**一类支持向量机（OCSVM）**或**孤立森林（IF）**模型，随后下发至客户端过滤异常样本。
    *   **自适应阈值（AT）**：改进自 FedBalancer，通过服务器动态调整全局损失阈值，过滤掉高损失的疑似噪声样本。
    *   **联邦多类深度 SVDD 损失**：引入正则化项，在特征空间中为每个类别构建最小超球体，将正常样本聚集在球内，从而增强基于特征的异常检测效果。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：MNIST（按用户划分的天然 Non-IID）、CIFAR10。
    *   **噪声源（Open-set）**：SVHN、EMNIST、ImageNet32。
*   **噪声设置**：注入 40% 的噪声，包括**闭集噪声**（标签错误）和**开集噪声**（引入无关类别图像）。
*   **对比基准（Benchmark）**：标准的 FedAvg 算法（在无噪声、有噪声但无选择的情况下运行）。
*   **对比方法**：AT、OCSVM、IF 三种选择策略，以及基于损失（Loss-based）与基于特征（Feature-based）的两种选择维度。

### 4. 资源与算力
*   **硬件环境**：单台机器，配置为 **NVIDIA RTX 3090 GPU**、Ryzen 5900X CPU、32GB RAM。
*   **软件框架**：基于 **FedML** 库进行分布式模拟，使用 PyTorch 和 Scikit-Learn。
*   **训练参数**：训练 1000 轮，每轮客户端本地训练 5 个 Epoch，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 50 到 1000 个客户端的不同规模场景。
*   **充分性**：论文针对不同数据集、多种噪声类型（开集/闭集）、多种客户端数量进行了详尽的交叉实验。
*   **客观性**：采用了“最佳轮次（Best Round）”准确率而非最终轮次，以规避 Non-IID 带来的波动影响；同时使用了精确率、召回率和 F1 分数等宏观指标，实验设计较为公平客观。

### 6. 主要结论与发现
*   **准确率显著提升**：基于损失的样本选择效果最好。在 CIFAR10 上，OCSVM 带来了高达 **7.02%** 的准确率增益；在 MNIST 上，AT 提升了 **1.83%**。
*   **噪声敏感性**：闭集噪声（标签错误）对模型性能的破坏力远大于开集噪声。
*   **特征选择的挑战**：基于特征的选择在极高噪声下效果有限，但引入**联邦 SVDD 损失**后，在 CIFAR10 等复杂数据集上能进一步优化特征空间，带来约 0.99% 的额外增益。
*   **客户端规模影响**：随着客户端数量减少（单个客户端数据量增加），样本选择的优势更加明显。

### 7. 优点
*   **多任务协同**：结合重建任务，使模型不仅能识别“分错类”的样本，还能识别“长得不像”的异常图像。
*   **隐私友好**：样本选择在本地完成，服务器仅处理脱敏的损失值或特征向量。
*   **自适应性**：AT 方法能随训练进程动态调整，无需人工预设固定阈值。

### 8. 不足与局限
*   **计算开销**：服务器端训练 OCSVM（复杂度 $O(n^2 \sim n^3)$）在超大规模客户端场景下可能存在计算瓶颈。
*   **超参数敏感**：异常检测的“污染率（Contamination rate）”和样本选择的“启动轮次”需要精细调优，否则可能误删高质量样本。
*   **特征空间局限**：在极高维度的特征空间中，噪声样本可能因模型过拟合（记忆效应）而变得难以识别。
*   **Non-IID 适应性**：SVDD 损失在处理极度不平衡的类别分布时，可能需要更复杂的加权机制。

（完）
