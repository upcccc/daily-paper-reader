---
title: Federated Rule Ensemble Method in Medical Data
title_zh: 医疗数据中的联邦规则集成方法
authors: "Ke Wan, Kensuke Tanioka, Toshio Shimokawa"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17956v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 不共享原始数据的分布式环境联邦学习
tldr: 针对医疗数据隐私保护与模型可解释性的双重挑战，本文提出了一种联邦RuleFit框架。该方法通过差分隐私直方图确定共享切分点，利用梯度提升决策树生成局部规则，并采用联邦对偶平均算法进行稀疏系数估计。实验证明，该方法在保持与中心化模型相当性能的同时，提供了极高的临床可解释性，为分布式医疗协作提供了有效方案。
selection_source: fresh_fetch
motivation: 医疗数据受隐私法规限制难以跨机构共享，且现有联邦学习模型往往因黑盒特性而缺乏临床决策所需的可解释性。
method: 提出联邦RuleFit框架，通过差分隐私直方图对齐特征切分点，结合局部决策树生成规则并利用联邦对偶平均算法进行稀疏优化。
result: 实验结果表明，该方法在预测性能上可媲美中心化训练模式，且在准确性和可解释性方面均优于现有的联邦学习方法。
conclusion: 该框架为联邦学习环境下的医疗数据建模提供了一种兼顾隐私保护、预测精度与临床可解释性的实用解决方案。
---

## 摘要
机器学习已成为医学研究中不可或缺的一部分，并越来越多地应用于临床环境以支持诊断和决策；然而，其有效性取决于对大规模、多样化数据集的获取，而这些数据在单一机构内往往有限。尽管跨机构整合数据可以解决这一局限性，但隐私法规和数据所有权限制阻碍了这些努力。联邦学习能够在不共享原始数据的情况下进行协作模型训练；然而，大多数方法依赖于缺乏可解释性的复杂架构，限制了其临床适用性。因此，我们提出了一种联邦 RuleFit 框架，旨在为分布式环境构建一个统一且可解释的全局模型。该框架集成了三个组件：基于差分隐私直方图的预处理，用于估计共享切分值，从而实现一致的规则定义并减少客户端间的异质性；使用带有共享切分值的梯度提升决策树进行本地规则生成；以及通过联邦对偶平均算法进行 $\ell_1$ 正则化优化，以实现稀疏且一致的变量选择，从而进行系数估计。在仿真研究中，所提方法的性能与集中式 RuleFit 相当，同时优于现有的联邦学习方法。真实世界分析表明，该方法能够在提供具有竞争力的预测准确性的同时，提供可解释的见解。因此，所提出的框架为联邦学习环境下的可解释且可靠的建模提供了一种实用且有效的解决方案。

## Abstract
Machine learning has become integral to medical research and is increasingly applied in clinical settings to support diagnosis and decision-making; however, its effectiveness depends on access to large, diverse datasets, which are limited within single institutions. Although integrating data across institutions can address this limitation, privacy regulations and data ownership constraints hinder these efforts. Federated learning enables collaborative model training without sharing raw data; however, most methods rely on complex architectures that lack interpretability, limiting clinical applicability. Therefore, we proposed a federated RuleFit framework to construct a unified and interpretable global model for distributed environments. It integrates three components: preprocessing based on differentially private histograms to estimate shared cutoff values, enabling consistent rule definitions and reducing heterogeneity across clients; local rule generation using gradient boosting decision trees with shared cutoffs; and coefficient estimation via $\ell_1$-regularized optimization using a Federated Dual Averaging algorithm for sparse and consistent variable selection. In simulation studies, the proposed method achieved a performance comparable to that of centralized RuleFit while outperforming existing federated approaches. Real-world analysis demonstrated its ability to provide interpretable insights with competitive predictive accuracy. Therefore, the proposed framework offers a practical and effective solution for interpretable and reliable modeling in federated learning environments.

---

## 论文详细总结（自动生成）

这篇论文题为《医疗数据中的联邦规则集成方法》（Federated Rule Ensemble Method in Medical Data），提出了一种在保护隐私的前提下，针对分布式医疗数据构建可解释机器学习模型的新框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：医疗数据通常碎片化地存储在不同机构中，受隐私法规（如GDPR、HIPAA）限制难以整合。虽然联邦学习（FL）解决了数据共享问题，但现有的联邦学习模型多为“黑盒”架构（如深度神经网络），缺乏临床决策所需的**可解释性**。
*   **研究动机**：临床医生需要理解模型预测背后的逻辑。RuleFit 算法结合了规则集成的解释性和集成学习的预测力，但其原始设计是中心化的。本文旨在开发一种**联邦 RuleFit 框架**，在不泄露原始数据的情况下，构建一个统一、稀疏且高度可解释的全局模型。

### 2. 论文提出的方法论：联邦 RuleFit 框架
该框架由三个关键阶段组成：
*   **预处理（基于差分隐私直方图）**：
    *   为了确保各客户端生成的规则具有一致的切分点（Cut-off values），服务器通过**差分隐私（DP）直方图**估计全局协变量的分布。
    *   每个客户端对本地直方图计数添加拉普拉斯噪声，服务器聚合后计算分位数，生成共享的候选切分点集。
*   **本地规则生成**：
    *   各客户端利用本地数据训练**梯度提升决策树（GBDT）**，但限制树的节点分裂只能使用预定义的共享切分点。
    *   从生成的树中提取决策路径作为“规则项”，并汇总至服务器形成全局规则候选库。
*   **联邦规则集成（系数估计）**：
    *   将规则项与线性项结合，构建 $\ell_1$ 正则化逻辑回归模型（Lasso）。
    *   采用**联邦对偶平均算法（FedDA）**进行分布式优化。该算法通过在客户端和服务器之间交换对偶变量（梯度信息）而非原始数据，实现全局一致的变量选择和系数估计，最终生成稀疏模型。

### 3. 实验设计
*   **数据集与场景**：
    *   **数值仿真**：设计了线性（Model 1）和非线性（Model 2）两种数据生成过程。设置了三种挑战场景：客户端数量变化（2到20个）、客户端样本量不平衡、客户端标签（结局）分布不平衡。
    *   **真实世界应用**：使用了一个包含 371 名患者、来自 3 个不同医疗机构的**创伤数据集**，预测死亡率。
*   **对比方法（Benchmarks）**：
    1.  **Centralized RuleFit**：中心化训练（性能上限）。
    2.  **Local RuleFit**：各机构独立训练（无协作基准）。
    3.  **BFI (Bayesian Federated Inference)**：基于贝叶斯框架的联邦线性模型。
    4.  **FRF (Federated Random Forest)**：联邦随机森林（非线性集成基准）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   **算法效率**：文中提到通过预处理步骤限制了切分点数量，显著降低了后续规则集成阶段的计算负担。实验中 FedDA 算法设置了 300 轮通信和 20 次本地迭代。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖了从数据异质性到机构规模的多个维度，每组实验均进行了多次重复以观察标准差。
    *   进行了关于直方图箱数（Bins）和分位数选择对性能影响的消融分析（见附录）。
*   **充分性评价**：实验设计较为**充分且客观**。通过对比线性与非线性场景，验证了该方法在处理复杂医疗关系时的鲁棒性。真实世界数据的加入增强了研究的临床说服力。

### 6. 论文的主要结论与发现
*   **性能接近中心化**：所提联邦 RuleFit 在各种场景下的 AUC、准确率和 F1 分数均接近中心化训练的结果。
*   **优于现有联邦方法**：在非线性场景下，其表现显著优于 BFI；在数据异质性较高时，优于简单的模型聚合方法（如 FRF）。
*   **高解释性**：模型能够提取出如“年龄 < 57 且 GCS 评分 >= 5 且 ISS 评分 < 57”等具体的临床规则，并量化其对死亡风险的影响，这与临床直觉高度一致。

### 7. 优点：亮点与创新
*   **隐私与性能的平衡**：通过 DP 直方图和 FedDA 算法，在严格保护隐私的同时，解决了分布式环境下规则定义不一致的问题。
*   **内在可解释性**：不同于 SHAP 等事后解释方法，该模型本身由易于理解的逻辑规则组成，更符合医疗决策的需求。
*   **鲁棒性**：实验证明该方法对客户端之间的数据异质性（Non-IID）具有很强的抵抗力。

### 8. 不足与局限
*   **结局类型限制**：目前的研究仅限于**二分类结局**，尚未扩展到连续变量（回归）或生存分析（Time-to-event）数据。
*   **联邦模式限制**：该框架目前仅适用于**横向联邦学习**（特征空间相同，样本不同），不适用于纵向联邦场景。
*   **样本量规模**：真实世界应用的样本量（371例）相对较小，在大规模多中心医疗大数据上的扩展性仍需进一步验证。

（完）
