---
title: Protecting User Prompts Via Character-Level Differential Privacy
title_zh: 通过字符级差分隐私保护用户提示词
authors: "Shashie Dilhara Batan Arachchige, Hassan Jameel Asghar, Benjamin Zi Hao Zhao, Dinusha Vatsalan, Dali Kaafar"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26032v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 字符级差分隐私机制
tldr: 针对大语言模型提示词中可能包含敏感隐私信息的问题，本文提出一种基于字符级差分隐私（DP）的脱敏方法。该方法通过随机响应机制独立扰动单词中的每个字符，利用LLM的上下文恢复能力重建非敏感词汇，同时确保稀有的敏感信息难以被还原。实验表明，该方法在无需显式识别PII的情况下，实现了良好的隐私保护与任务效用平衡。
selection_source: fresh_fetch
motivation: 旨在解决用户在使用第三方大语言模型时，提示词中包含的敏感个人身份信息（PII）可能被泄露的隐私风险。
method: 采用差分隐私的随机响应机制对提示词中的每个字符进行独立扰动，并利用远程LLM的上下文理解能力来修复非敏感词汇并执行任务。
result: 实验证明敏感信息的重建率接近随机水平，而非敏感词汇能被有效还原，且在隐私与效用的权衡上优于词级DP和规则脱敏方法。
conclusion: 该字符级差分隐私方法无需预先识别隐私实体即可提供有效的隐私保护，是保护LLM提示词隐私的一种可行且高效的方案。
---

## 摘要
大语言模型（LLM）根据用户提示词生成响应。这些提示词通常可能包含高度敏感的信息，包括个人身份信息（PII），这些信息可能会泄露给托管这些模型的第三方。在这项工作中，我们提出了一种净化用户提示词的新方法。我们的机制利用差分隐私的随机响应机制，随机且独立地扰动单词中的每个字符。扰动后的文本被发送到远程 LLM，该模型首先执行提示词恢复，随后执行预期的下游任务。其核心思想是，由于上下文线索以及非敏感词通常非常常见，即使这些词受到扰动，恢复过程也能重建它们。另一方面，由于敏感词较为罕见，扰动将使它们的重建变得困难。我们在 i2b2/UTHealth 和 Enron 两个数据集上，使用 Llama-3.1 8B Instruct 和 GPT-4o mini 两个 LLM 对我们的方法进行了实验验证。我们还通过统一的隐私-效用评估，将我们的方法与词级差分隐私机制以及基于规则的 PII 脱敏基准进行了比较。结果表明，这些数据集中标记的敏感 PII 的重建率接近于重建完全随机单词的理论率，而非敏感词的重建率则高得多。我们的方法的优势在于，它可以在不显式识别提示词中敏感信息片段的情况下应用，同时在下游任务中表现出良好的隐私-效用权衡。

## Abstract
Large Language Models (LLMs) generate responses based on user prompts. Often, these prompts may contain highly sensitive information, including personally identifiable information (PII), which could be exposed to third parties hosting these models. In this work, we propose a new method to sanitize user prompts. Our mechanism uses the randomized response mechanism of differential privacy to randomly and independently perturb each character in a word. The perturbed text is then sent to a remote LLM, which first performs a prompt restoration and subsequently performs the intended downstream task. The idea is that the restoration will be able to reconstruct non-sensitive words even when they are perturbed due to cues from the context, as well as the fact that these words are often very common. On the other hand, perturbation would make reconstruction of sensitive words difficult because they are rare. We experimentally validate our method on two datasets, i2b2/UTHealth and Enron, using two LLMs: Llama-3.1 8B Instruct and GPT-4o mini. We also compare our approach with a word-level differentially private mechanism, and with a rule-based PII redaction baseline, using a unified privacy-utility evaluation. Our results show that sensitive PII tagged in these datasets are reconstructed at a rate close to the theoretical rate of reconstructing completely random words, whereas non-sensitive words are reconstructed at a much higher rate. Our method has the advantage that it can be applied without explicitly identifying sensitive pieces of information in the prompt, while showing a good privacy-utility tradeoff for downstream tasks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种通过**字符级本地差分隐私（Local Differential Privacy, LDP）**来保护大语言模型（LLM）用户提示词的新方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：用户在调用第三方托管的 LLM 时，提示词中往往包含敏感的个人身份信息（PII）或企业机密。现有的保护方法存在局限：
    *   **命名实体识别（NER）/规则过滤**：容易出现漏报（False Negatives），且依赖于预定义的实体类型。
    *   **加密技术（如同态加密）**：计算开销巨大，且需要修改模型架构。
    *   **词级差分隐私**：通过替换语义相近的词来保护隐私，但往往会破坏文本的连贯性和下游任务的效用。
*   **研究动机**：利用 LLM 强大的上下文修复能力，在字符层面引入噪声。其核心假设是：**常见词（通常是非敏感词）**在语境中具有高度相关性，LLM 容易修复；而**稀有词（通常是敏感 PII，如特定姓名、电话）**熵值高且缺乏上下文冗余，LLM 难以还原。

### 2. 提出的方法论
*   **核心思想**：在客户端对提示词中的每个单词进行字符级的随机扰动，然后利用远程 LLM 的“去噪”能力恢复非敏感内容并执行任务。
*   **关键技术细节**：
    1.  **噪声注入（$k$-RR 机制）**：使用 $k$ 阶随机响应（$k$-ary Randomized Response）算法。对于单词中的每个字符，以一定概率保留原字符，以剩余概率从 94 个可打印 ASCII 字符集中随机替换。
    2.  **本地差分隐私（LDP）**：该机制在用户侧运行，确保原始文本在离开本地设备前就已脱敏。
    3.  **提示词恢复与任务执行**：将扰动后的文本（如 `cbrr/MpondenXe`）发送给远程 LLM，通过特定的系统提示词（System Prompt）要求模型先修复错误（恢复为 `correspondence`），再执行下游任务（如摘要生成）。
*   **算法流程**：输入原始提示词 $\rightarrow$ 字符级 $k$-RR 扰动 $\rightarrow$ 生成噪声提示词 $\rightarrow$ 远程 LLM 修复 + 任务处理 $\rightarrow$ 输出结果。

### 3. 实验设计
*   **数据集**：
    *   **i2b2/UTHealth**：临床医疗记录，包含姓名、地点、日期等敏感健康信息（PHI）。
    *   **Enron**：电子邮件数据集，包含姓名、地址、电话等个人信息。
*   **对比基准（Benchmarks）**：
    *   **词级差分隐私（Word-level DP）**：基于 Feyisetan 等人的方法，在嵌入空间添加噪声。
    *   **基于规则的脱敏（Presidio）**：使用微软开源的 PII 识别框架进行硬掩码处理。
    *   **理论基准**：推导了完全随机单词在扰动后的重建概率作为隐私保护的底线。
*   **评估指标**：
    *   **隐私性**：敏感词重建率（Reconstruction Rate）。
    *   **效用性**：非敏感词重建率、摘要结果的语义相似度（Cosine Similarity）。

### 4. 资源与算力
*   **模型使用**：实验使用了 **GPT-4o mini**（通过 API）和 **Llama-3.1 8B Instruct**（开源模型）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。由于该方法主要涉及推理阶段的提示词处理和轻量级的字符扰动，不涉及模型训练，因此对本地算力要求极低（可在浏览器插件中运行）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个不同领域的真实数据集。
    *   测试了从 $\epsilon=1$ 到 $\epsilon=10$ 的多种隐私预算设置。
    *   进行了**迭代恢复实验**（测试二次恢复是否会泄露更多隐私）。
    *   针对姓名、地点等特定实体进行了细粒度的重建分析。
*   **充分性评价**：实验设计较为全面，通过对比理论随机基准证明了隐私保护的有效性，且对比了工业界常用的 Presidio 框架，具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **隐私-效用平衡**：当隐私预算 $\epsilon$ 在 5.5 到 6.0 之间时，模型能保持约 80% 的语义相似度，而敏感 PII 的重建率被压制在 20% 左右。
*   **敏感度差异**：敏感词（如特定街道名、罕见人名）的重建率接近随机猜测水平；而城市名、医院名等结构化程度较高的词较易被修复。
*   **优于词级 DP**：在相同的隐私保护水平下，字符级扰动比词级替换能保留更多的语义信息，下游任务表现更好。
*   **多词实体更安全**：完整姓名（如姓+名）的重建难度远高于单个人名单词。

### 7. 优点
*   **无需实体识别**：该方法是“不可知”的（Agnostic），不需要预先训练 NER 模型或定义隐私规则，避免了漏报风险。
*   **计算轻量**：字符级扰动算法极其简单，适合集成到各类客户端。
*   **数学保证**：基于差分隐私框架，提供了严格的隐私损失量化方法。
*   **利用 LLM 特性**：巧妙地将 LLM 的鲁棒性（纠错能力）转化为提升效用的工具。

### 8. 不足与局限
*   **依赖恢复模型的能力**：如果远程 LLM 的纠错能力较弱，任务效用会大幅下降。
*   **辅助信息攻击风险**：如果攻击者拥有外部辅助数据库（如员工名录），可能会通过链接攻击（Linkage Attack）提高重建率。
*   **隐私定义的局限**：目前主要针对 PII 单词，对于隐藏在句子结构或逻辑中的高层语义隐私（如特定事件的描述）保护能力有限。
*   **超参数敏感**：$\epsilon$ 的选择需要针对不同的模型和任务进行微调，缺乏一种通用的自动化选择机制。

（完）
