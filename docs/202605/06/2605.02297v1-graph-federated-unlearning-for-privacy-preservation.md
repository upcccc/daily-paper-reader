---
title: Graph Federated Unlearning for Privacy Preservation
title_zh: 面向隐私保护的图联邦遗忘学习
authors: "Ruotong Ma, Wentao Yu, Qizhou Wang, Jie Yang, Chen Gong"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02297v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 用于隐私保护的图联邦学习
tldr: 本研究针对图联邦学习（GFL）中用户退出后隐私残留的问题，提出了图联邦遗忘学习框架。为解决传统遗忘学习导致的性能下降，研究引入了正交梯度更新策略以减少对非目标数据的影响，并利用虚拟客户端维护图拓扑与全局嵌入。通过成员推理框架验证，该方法在保护隐私的同时，性能优于七种主流基准方法。
selection_source: fresh_fetch
motivation: 针对图联邦学习中用户退出后信息仍可能残留在模型中并导致隐私泄露的问题，探索彻底删除用户信息的方法。
method: 采用与非遗忘数据梯度正交的更新方向，并引入虚拟客户端来维护图拓扑结构和全局嵌入，从而在不恢复已删除实体信息的情况下保持模型性能。
result: 实验结果表明，该方法在隐私保护的可靠性上表现优异，且在整体性能上超越了七种现有的先进基准方法。
conclusion: 该研究通过结合正交更新与虚拟客户端技术，有效地实现了图联邦学习中的隐私遗忘，为去中心化图数据的隐私保护提供了新方案。
---

## 摘要
图联邦学习 (GFL) 促进了分布式图数据上的去中心化训练，同时将敏感的用户信息保留在本地，这符合 GDPR 和 CCPA 等赋予用户自由加入或退出学习系统权利的政策。然而，即使是去中心化的，用户信息在退出后仍可能留存，并可能传播到中央服务器，进而重新分发给恶意客户端。尽管这种用户退出过程中的隐私泄露非常重要，但在 GFL 中却鲜有关注。为了填补这一空白，我们探索了利用机器学习遗忘 (MU) 彻底删除用户信息的潜力。然而，众所周知，经典的 MU 方法会降低整体性能，由于本地消息传递和全局模型协作，这一问题在 GFL 中变得更加严重。为此，我们进行了两项调整以缓解 GFL 面临的这一挑战。首先，我们确保遗忘更新对整体性能的影响最小，将其引导至与学习其他数据的梯度正交的方向。其次，我们引入了由中央服务器维护的虚拟客户端，以在不恢复已删除实体信息的情况下保留图拓扑和全局嵌入。我们在具有代表性的用户退出场景下进行了全面的实验，并提出了一种新颖的成员推理框架，以严格评估和验证我们隐私保护的可靠性。实验结果证明了我们方法的有效性，其性能也超过了七种最先进的基准方法。

## Abstract
Graph federated learning (GFL) facilitates decentralized training on distributed graph data while keeping sensitive user information local, aligning with policies such as GDPR and CCPA that grant users the right to freely join or withdraw from learning systems. However, even decentralized, user information can persist after quitting, potentially propagating to central servers and then redistributing to malicious clients. This privacy leakage during user withdrawal, despite its importance, has received seldom attention in GFL. To fill the gap, we explore the potential of machine unlearning (MU) to thoroughly remove user information. However, classical MU methods are known to degrade overall performance, a problem that is exacerbated in GFL due to local message passing and global model collaboration. To this end, we make two adjustments to mitigate this challenge for GFL. First, we ensure unlearning updates that minimally affect overall performance, steering them in directions orthogonal to the gradients from learning other data. Second, we introduce virtual clients, maintained by the central server, to preserve graph topology and global embeddings without recovering information of removed entities. We conduct comprehensive experiments under a representative user-withdrawal scenario and propose a novel membership inference framework to rigorously evaluate and validate the reliability of our privacy preservation. The experimental results demonstrate the effectiveness of our approach, which also surpasses the performance of seven state-of-the-art baseline methods.

---

## 论文详细总结（自动生成）

以下是对论文《Graph Federated Unlearning for Privacy Preservation》（面向隐私保护的图联邦遗忘学习）的深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：图联邦学习（GFL）允许在不共享原始数据的情况下进行分布式图模型训练，符合 GDPR 等隐私法规。然而，现有的 GFL 无法完全满足“被遗忘权”（RTBF）。
*   **核心问题**：当某个用户（客户端）请求退出系统并删除其数据贡献时，由于图神经网络（GNN）的消息传递机制和联邦学习的参数聚合，该用户的信息已渗透进全局模型和其他客户端模型中。简单的“移除客户端”无法消除模型参数中残留的隐私信息，容易遭受成员推理攻击（MIA）。
*   **研究动机**：现有的机器学习遗忘（MU）方法在处理图数据时，往往会导致模型整体性能大幅下降。本论文旨在开发一种既能彻底删除特定客户端信息，又能保持全局模型效用的图联邦遗忘框架。

### 2. 方法论：FedGCV 框架
论文提出了 **FedGCV**（基于梯度校正和虚拟客户端的图联邦遗忘学习），核心包含两个关键技术：
*   **梯度校正（Gradient Correction）**：
    *   **核心思想**：将遗忘过程视为一个受限优化问题。为了防止遗忘更新（梯度上升）破坏模型在保留数据上的表现，算法会计算遗忘梯度与保留数据梯度之间的关系。
    *   **技术细节**：如果遗忘梯度方向与保留梯度方向冲突（点积为负），则将遗忘梯度投影到保留梯度的正交方向上。这样可以确保在“抹除”目标信息的同时，尽可能减少对正常预测能力的干扰。
*   **虚拟客户端（Virtual Client）**：
    *   **核心思想**：在客户端退出后，由服务器维护一个“虚拟客户端”来填补拓扑结构和全局嵌入的空白。
    *   **技术细节**：利用变分图自编码器（VGAE）提取被遗忘客户端的图谱信息（拉普拉斯矩阵特征值）和节点特征分布。生成不含原始隐私实体的合成子图，用于后续的联邦微调，以补偿因数据缺失导致的性能损失。

### 3. 实验设计
*   **数据集**：使用了四个主流图基准数据集：**Cora、CiteSeer、PubMed**（同质图）以及 **Tolokers**（异质图）。
*   **场景模拟**：使用 METIS 算法将大图划分为多个子图，模拟分布式客户端环境。
*   **对比方法（Benchmarks）**：
    *   **理想基准**：从头开始重新训练（Retrain）。
    *   **主流方法**：PGA（投影梯度上升）、SGA（随机梯度上升）、EWC-SGA（基于弹性权重整合）、Noisy-GD（噪声梯度下降）、ULKD（蒸馏法）以及最新的 **ReGEnUnlearn**。
*   **评估指标**：模型准确率（MA）衡量效用，成员推理攻击（MIA）成功率衡量隐私遗忘效果（MIA 率越低越好）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型配置**：提到使用了 2 层 GCN，隐藏层维度为 64，联邦训练 30 轮，遗忘优化 30 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上进行了完整对比，涵盖了从同质到异质的不同图特性。
*   **消融实验**：分别验证了“梯度校正”和“虚拟客户端”模块的有效性，证明两个模块缺一不可。
*   **敏感性分析**：对超参数 $\tau$（更新范数限制）进行了敏感性测试。
*   **充分性评价**：实验设计较为全面，对比了 7 种先进的基准方法，并首次在图联邦遗忘场景下引入了严谨的 MIA 评估框架，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能优异**：FedGCV 在所有数据集上的表现均优于现有的遗忘学习方法，且在隐私保护（低 MIA 率）方面非常接近“从头重训”的理想效果。
*   **效用保持**：在执行遗忘操作后，模型准确率能恢复到原始准确率的 90% 以上，解决了传统方法“一忘就崩”的问题。
*   **SOTA 对比**：相比于目前最强的 ReGEnUnlearn，FedGCV 在提升约 5.21% 准确率的同时，进一步降低了 2% 的隐私泄露风险。

### 7. 优点
*   **正交梯度策略**：巧妙地解决了遗忘与保留之间的目标冲突。
*   **图谱合成技术**：利用 VGAE 和谱信息生成虚拟客户端，这在图遗忘领域是一个创新的性能补偿方案。
*   **评估严谨**：引入了基于成员推理攻击的量化评估，比单纯看准确率下降更符合隐私保护的初衷。

### 8. 不足与局限
*   **计算开销**：引入 VGAE 生成虚拟子图会增加服务器端的计算负担。
*   **合成数据偏差**：虽然合成图保留了统计特性，但对于极度复杂的动态图或具有特殊语义结构的图，合成数据可能无法完全替代原始数据的结构贡献。
*   **应用限制**：目前主要针对节点分类任务，尚未探讨在链接预测或图分类任务中的适用性。

（完）
