---
title: Private Federated Learning for High-dimensional Time Series
title_zh: 高维时间序列的隐私联邦学习
authors: "Kejun Chen, Qianqian Zhu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07135v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 结合差分隐私表示学习的隐私保护联邦学习
tldr: 本文针对高维时间序列数据，提出了一种保护隐私的联邦学习框架，用于处理具有共享低秩结构和稀疏客户端偏差的向量自回归模型。研究开发了两阶段估计程序，将差分隐私表示学习与局部个性化调整相结合，在满足隐私约束的同时实现有效的信息聚合。通过建立非渐近误差界和证明秩选择一致性，本文揭示了隐私与效用之间的权衡，并在电力经济和宏观经济预测应用中证明了其优于单客户端基准方法的预测精度。
selection_source: fresh_fetch
motivation: 在大数据时代，如何在保护数据隐私的前提下，有效整合多个客户端的高维时间序列信息进行统计建模和预测是一个重大挑战。
method: 提出一种两阶段估计程序，通过差分隐私表示学习提取共享的低秩成分，并结合局部个性化方法处理客户端特有的稀疏偏差。
result: 建立了非渐近误差界并证明了秩选择准则的一致性，仿真和实证结果显示该方法在小样本情况下显著优于单客户端基准。
conclusion: 该框架成功实现了高维时间序列联邦学习中的隐私与效用平衡，为多任务宏观经济预测等领域提供了高效且安全的建模方案。
---

## 摘要
在大数据时代，如何在保护数据隐私的同时利用来自多个客户端的信息，已成为现代统计建模与预测中的一项关键挑战。本文针对高维向量自回归模型提出了一种隐私保护联邦学习框架，其中每个客户端的动态特性由一个公共低秩结构以及稀疏的客户端特定偏差共同表征。我们开发了一种两阶段估计程序，将共享组件的差分隐私表示学习与客户端特定调整的本地个性化相结合，从而在选择性隐私约束下实现有效的信息汇集。我们为单客户端和联邦估计量建立了非渐近误差界，以刻画内在的隐私-效用权衡，并证明了岭型秩选择准则的一致性。仿真研究表明，当本地样本量有限时，联邦学习能显著提高估计精度。对美国各州电力与经济联系的分析以及跨国多任务宏观经济预测的两个实证应用，突显了所提方法相比现有单客户端基准方法具有更优的预测准确性。

## Abstract
In the era of big data, leveraging information from multiple clients while preserving data privacy has emerged as a critical challenge in modern statistical modeling and forecasting. This paper introduces a privacy-preserving federated learning framework for high-dimensional vector autoregressive models, where each client's dynamics are characterized by a common low-rank structure augmented with sparse client-specific deviations. We develop a two-stage estimation procedure that integrates differentially private representation learning for the shared component with local personalization for client-specific adjustments, enabling effective information pooling under selective privacy constraints. Non-asymptotic error bounds are established for both the single-client and federated estimators to characterize the inherent privacy-utility trade-off, and consistency of a ridge-type rank selection criterion is proved. Simulation studies demonstrate that federation substantially improves estimation accuracy when local sample sizes are limited. Two empirical applications to analyzing electricity-economy linkages across U.S. states and conducting multi-task macroeconomic forecasting across countries, highlight the superior predictive accuracy of the proposed method over existing single-client benchmarks.

---

## 论文详细总结（自动生成）

这篇论文《Private Federated Learning for High-dimensional Time Series》（高维时间序列的隐私联邦学习）由上海财经大学的 Kejun Chen 和 Qianqian Zhu 撰写。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保护数据隐私的前提下，利用多个客户端（如不同地区、国家）的信息来提升高维向量自回归（VAR）模型的预测精度。
*   **研究动机**：
    *   **数据稀缺性**：在分析特定区域（如小州或小国）的经济指标时，本地样本量（$T_k$）通常远小于变量维度（$d$），导致单客户端建模效果差。
    *   **隐私约束**：许多敏感变量（如用电量、就业数据）受法律或商业机密保护，无法直接汇总。
    *   **异质性挑战**：不同客户端既有共同的宏观动态，又存在独特的局部偏差，传统的全局统一模型无法兼顾。

### 2. 方法论
论文提出了一种**两阶段隐私保护联邦学习框架**，核心模型假设系数矩阵 $A_k$ 可分解为：$A_k = A_0 + \Delta_k$。
*   **核心思想**：$A_0$ 是所有客户端共享的**低秩结构**（捕捉共同动态），$\Delta_k$ 是客户端特有的**稀疏偏差**（捕捉异质性）。
*   **关键技术细节**：
    *   **第一阶段：差分隐私（DP）表示学习**。客户端计算局部梯度，通过**高斯机制**注入噪声以满足 $(\epsilon, \delta)$-DP，并将噪声梯度投影到切空间。服务器聚合这些隐私梯度，通过奇异值分解（SVD）更新全局低秩矩阵 $\hat{A}_0$。
    *   **第二阶段：局部个性化细化**。客户端在给定全局 $\hat{A}_0$ 的基础上，利用本地数据通过 **FISTA 算法**（快速迭代收缩阈值算法）求解 $\ell_1$ 正则化问题，估计稀疏偏差 $\hat{\Delta}_k$。
    *   **秩选择**：提出了一种基于岭型比率（Ridge-type ratio）的准则，用于在联邦环境下一致地确定 $A_0$ 的秩。

### 3. 实验设计
*   **数据集/场景**：
    1.  **仿真实验**：设定 $d=50, p=1$，模拟不同样本量、隐私预算和客户端数量。
    2.  **实证应用 A**：美国五大湖地区 5 个州的电力与经济指标（12 维月度数据）。
    3.  **实证应用 B**：8 个国家的宏观经济预测（25 维季度数据）。
*   **对比方法（Benchmark）**：
    *   **单客户端基准**：包括核范数+$\ell_1$ 正则化 VAR（Nuc+$\ell_1$）、纯核范数正则化（Nuc）、纯 $\ell_1$ 正则化（$\ell_1$）以及普通最小二乘法（LS）。
    *   **隐私设置对比**：对比了非隐私（NDP）和不同隐私预算（如 $\epsilon=0.2, 0.1$）下的表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **推测**：由于 VAR 模型和 FISTA/ADMM 算法主要涉及矩阵运算和 SVD 分解，且实证数据规模属于中等统计范畴，该实验通常在标准工作站或服务器的 CPU 上即可完成，不依赖大规模 GPU 集群。

### 5. 实验数量与充分性
*   **实验规模**：仿真部分进行了 6 组核心实验，每组实验重复 **1000 次**以确保统计显著性。
*   **充分性**：实验涵盖了秩选择一致性、隐私-效用权衡、客户端数量 $K$ 的影响以及样本量 $T_k$ 的影响。
*   **客观性**：通过跨验证（Cross-validation）选择超参数，并使用了滚动窗口预测（Expanding window）评估实证效果，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **联邦优势**：当本地样本量有限时，联邦学习显著优于单客户端学习。
*   **隐私代价**：隐私保护会引入额外误差（Error DP），误差随隐私预算 $\epsilon$ 减小而增大。
*   **异质性平衡**：所提框架能有效分离共同动态和局部特性。在实证中，即使在适度的隐私保护下，联邦模型的预测精度（RMSFE）仍优于所有单客户端基准。
*   **正则化效应**：有趣的是，在某些情况下，适度的隐私噪声能起到类似正则化的作用，防止过拟合，从而略微提升预测性能。

### 7. 优点
*   **理论完备**：建立了非渐近误差界，清晰地刻画了统计误差、异质性误差和隐私误差的组成。
*   **结构巧妙**：低秩+稀疏的分解既实现了信息共享，又保留了客户端的个性化。
*   **选择性隐私**：支持仅对敏感变量（如电力数据）进行隐私保护，而对非敏感变量（如公开宏观指标）不加噪，提高了实用性。

### 8. 不足与局限
*   **线性假设**：基于 VAR 模型，可能无法捕捉时间序列中的非线性复杂关系。
*   **平稳性要求**：理论证明依赖于 VAR 过程的平稳性假设，对于非平稳数据的处理（如差分）可能增加预处理负担。
*   **噪声校准**：在实际应用中，敏感度（Sensitivity）的精确估计较为困难，通常需要保守估计，这可能导致注入过量噪声。
*   **通信开销**：虽然比传输原始数据安全，但第一阶段的迭代梯度传输在客户端数量极大时可能面临通信瓶颈。

（完）
