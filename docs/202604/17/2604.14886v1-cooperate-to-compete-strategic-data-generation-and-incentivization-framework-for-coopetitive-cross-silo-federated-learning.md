---
title: "Cooperate to Compete: Strategic Data Generation and Incentivization Framework for Coopetitive Cross-Silo Federated Learning"
title_zh: 以合作促竞争：竞合式跨孤岛联邦学习的策略性数据生成与激励框架
authors: "Thanh Linh Nguyen, Nguyen Van Huynh, Quoc-Viet Pham"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14886v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 数据敏感领域的跨孤岛联邦学习
tldr: 针对跨机构联邦学习中的“竞合”博弈，本文提出CoCoGen+框架。该框架将生成式AI合成数据作为战略决策，通过加权势博弈建模组织在提升模型性能与应对竞争损耗间的权衡。结合收益重分配激励机制，CoCoGen+在处理非独立同分布数据时，能有效平衡组织贡献与竞争成本，实现社会福利最大化并促进长期合作。
selection_source: fresh_fetch
motivation: 现有联邦学习激励机制未能考虑组织间在下游市场的竞争成本，以及非独立同分布数据导致的收益不对称问题。
method: 采用加权势博弈建模合成数据生成的战略决策，并整合基于收益重分配的激励机制以补偿竞争带来的效用损失。
result: 实验验证了CoCoGen+在多种任务中的可行性，其在效率上优于基准方法，并揭示了竞争强度对组织策略的影响。
conclusion: 该框架为竞合环境下的多机构联邦学习提供了有效的战略指导和激励保障，推动了数据敏感领域的协同AI训练。
---

## 摘要
在医疗保健等数据敏感领域，跨孤岛联邦学习 (CFL) 允许各机构在不共享原始数据的情况下协作训练 AI 模型。然而，实际的 CFL 部署本质上具有竞合性，即各机构在模型训练期间进行合作，同时在下游市场展开竞争。在这种背景下，包括数据量、质量和多样性在内的训练贡献虽然可以提升全局模型，但也会在无意中增强竞争对手的实力。非独立同分布 (non-IID) 数据进一步放大了这一困境，导致了非对称的学习收益并削弱了持续参与的动力。虽然现有的竞争感知型 CFL 和激励设计方法根据边际训练贡献来奖励机构，但它们未能考虑到增强竞争对手所带来的成本。在本文中，我们提出了 CoCoGen+，这是一个兼容竞合关系的数据生成与激励框架，它对 non-IID 数据和机构间竞争进行了联合建模，同时将基于生成式 AI (GenAI) 的合成数据生成内生化为一项策略性决策。具体而言，CoCoGen+ 将每一轮训练建模为一个加权势博弈，各机构通过权衡学习性能收益与计算成本及竞争导致的效用损失，策略性地决定生成多少合成数据。随后，我们提供了一个易于处理的均衡表征，并推导出可实施的生成策略以最大化社会福利。为了促进长期合作，我们整合了一个基于收益再分配的激励机制，以补偿各机构的贡献以及因竞争导致的效用下降。在不同学习任务上的实验验证了 CoCoGen+ 的可行性。结果表明了 non-IID 数据、竞争强度和激励措施如何塑造机构策略和社会福利，同时 CoCoGen+ 在效率上优于基准方法。

## Abstract
In data-sensitive domains such as healthcare, cross-silo federated learning (CFL) allows organizations to collaboratively train AI models without sharing raw data. However, practical CFL deployments are inherently coopetitive, in which organizations cooperate during model training while competing in downstream markets. In such settings, training contributions, including data volume, quality, and diversity, can improve the global model yet inadvertently strengthen rivals. This dilemma is amplified by non-IID data, which leads to asymmetric learning gains and undermines sustained participation. While existing competition-aware CFL and incentive-design approaches reward organizations based on marginal training contributions, they fail to account for the costs of strengthening competitors. In this paper, we introduce CoCoGen+, a coopetition-compatible data generation and incentivization framework that jointly models non-IID data and inter-organizational competition while endogenizing GenAI-based synthetic data generation as a strategic decision. Specifically, CoCoGen+ formulates each training round as a weighted potential game, where organizations strategically decide how much synthetic data to generate by balancing learning performance gains against computational costs and competition-caused utility losses. We then provide a tractable equilibrium characterization and derive implementable generation strategies to maximize social welfare. To promote long-term collaboration, we integrate a payoff redistribution-based incentive mechanism to compensate organizations for their contributions and competition-caused utility degradation. Experiments on varying learning tasks validate the feasibility of CoCoGen+. The results show how non-IID data, competition intensity, and incentives shape organizational strategies and social welfare, while CoCoGen+ outperforms baselines in efficiency.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **CoCoGen+** 的框架，旨在解决跨孤岛联邦学习（Cross-Silo FL, CFL）中机构间“既合作又竞争”的复杂博弈问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在医疗、金融等领域，各机构（如医院、银行）希望通过联邦学习共同训练高性能 AI 模型，但它们在下游市场往往是直接的竞争对手。
*   **核心矛盾：** 贡献高质量数据虽能提升全局模型，但也会增强竞争对手的实力，导致自身竞争优势流失。
*   **技术挑战：** 数据非独立同分布（non-IID）导致各方收益不对称，且现有激励机制大多只关注边际贡献，忽略了“增强对手”带来的隐性成本。
*   **研究目标：** 建立一个统一框架，将生成式 AI（GenAI）合成数据作为一种战略手段，平衡学习收益、计算成本与竞争损耗，实现社会福利最大化。

### 2. 方法论
CoCoGen+ 框架结合了博弈论、生成式 AI 和激励机制设计：
*   **核心思想：** 将每一轮训练中“生成多少合成数据”视为机构的战略决策。通过生成合成数据来弥补本地数据的分布缺陷（缓解 non-IID），同时考虑竞争压力。
*   **效用函数建模：** 机构的效用由四部分组成：
    1.  **合作收益：** 全局模型性能提升带来的业务收入。
    2.  **竞争损失：** 竞争对手因模型提升而从市场中获取的收益（负外部性）。
    3.  **计算成本：** 本地训练和 GenAI 生成数据的能耗成本。
    4.  **收益重分配（激励）：** 建立一个“补偿池”，由贡献较少的机构向贡献较多（且受竞争影响大）的机构转移支付。
*   **关键技术：**
    1.  **缩放法则（Scaling Laws）：** 使用幂律函数建模数据量（原始+合成）与模型误差之间的关系。
    2.  **加权势博弈（Weighted Potential Game）：** 证明了该战略互动属于势博弈，确保了纯策略纳什均衡（NE）的存在。
    3.  **不动点迭代算法：** 推导出一种可收敛的迭代算法，用于计算各机构的最优数据生成量。

### 3. 实验设计
*   **数据集：** 使用了三个难度递增的数据集：Fashion-MNIST（简单）、CIFAR-10（中等）、CIFAR-100（复杂）。
*   **场景设置：** 模拟了 10 个具有不同计算能力和 non-IID 程度（通过 Dirichlet 分布参数 $\alpha_D$ 控制）的机构。
*   **对比方法（Benchmarks）：**
    *   **VCFL：** 传统联邦学习（无 GenAI，无竞争意识）。
    *   **WCO：** 无竞争场景。
    *   **WDG：** 有竞争但无数据生成。
    *   **RaDG / MaDG：** 随机生成或最大量生成合成数据。

### 4. 资源与算力
*   **硬件环境：** 使用了 **4 台 NVIDIA L40 GPU**（每台 48GB 显存）。
*   **软件框架：** 基于 Flower 联邦学习框架实现。
*   **模型架构：** 针对不同任务分别使用了 MobileNetV3-small、MobileNetV2 和 ResNet-34。
*   **训练细节：** Batch size 为 16，学习率为 0.01。文中未明确给出总训练时长，但强调了算法的收敛性。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了不同数据异构度（$\alpha_D \in \{0.1, 0.5, 0.9\}$）、不同竞争强度（低、中、高）以及不同激励强度（$\xi$）的多组实验。
*   **充分性：** 实验不仅验证了模型性能，还深入分析了社会福利、个体效用和数据生成策略随环境参数变化的趋势。通过跨数据集的对比，证明了框架在不同任务复杂度下的鲁棒性，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **竞争的双重作用：** 适度的竞争强度和较低的数据异构性会刺激机构生成更多合成数据，从而提升整体社会福利。
*   **激励机制的非单调性：** 收益重分配强度（$\xi$）对社会福利的影响并非线性的。对于复杂任务（如 CIFAR-100），存在一个最优的重分配比例；过高的重分配反而会抑制机构的贡献积极性。
*   **性能优越性：** CoCoGen+ 在所有测试场景下的社会福利均优于基准方法，尤其在处理高度 non-IID 数据时表现突出。

### 7. 优点
*   **视角独特：** 首次将 GenAI 数据生成内生化为竞合 CFL 中的战略决策，填补了竞争成本量化研究的空白。
*   **理论完备：** 提供了严谨的博弈论证明（势博弈），保证了算法在理论上的稳定性和收敛性。
*   **机制公平：** 引入收益重分配机制，解决了“搭便车”问题，并补偿了因竞争导致的效用损失，有利于维持长期合作。

### 8. 不足与局限
*   **参数依赖：** 框架依赖于“竞争强度”（$\gamma$）的准确量化，但在现实商业环境中，精确测量对手间的竞争程度非常困难。
*   **中心化假设：** 收益重分配和策略计算依赖于一个可信的中心服务器，这在某些去中心化诉求极高的场景下可能存在信任瓶颈。
*   **GenAI 开销：** 虽然模型考虑了计算成本，但高质量 GenAI 模型的本地部署和推理对孤岛机构的算力要求依然较高。
*   **隐私风险：** 尽管不共享原始数据，但合成数据的分布信息或模型更新仍可能存在潜在的成员推理攻击风险（文中未深入讨论隐私防御）。

（完）
