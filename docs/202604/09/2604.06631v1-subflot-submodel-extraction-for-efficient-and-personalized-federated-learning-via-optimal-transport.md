---
title: "SubFLOT: Submodel Extraction for Efficient and Personalized Federated Learning via Optimal Transport"
title_zh: SubFLOT：基于最优传输的高效个性化联邦学习子模型提取
authors: "Zheng Jiang, Nan He, Yiming Chen, Lifeng Sun"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06631v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 用于在保护数据隐私的同时进行协作模型训练的联邦学习框架
tldr: 针对联邦学习中的系统和统计异构性挑战，本文提出SubFLOT框架。该框架通过在服务器端进行个性化剪枝，解决了现有方法中服务器端缺乏个性化或客户端计算负担过重的问题。SubFLOT利用最优传输技术（OTP）根据历史模型提取子模型，并结合缩放自适应正则化（SAR）缓解参数散度，实现了在资源受限设备上高效部署个性化模型。
selection_source: fresh_fetch
motivation: 现有的联邦剪枝方法在服务器端缺乏个性化，而在客户端则计算开销过大，且剪枝过程会导致模型参数散度过大。
method: 提出SubFLOT框架，包含利用最优传输最小化Wasserstein距离来生成子模型的OTP模块，以及根据剪枝率动态调整惩罚强度的SAR正则化模块。
result: 实验结果表明，SubFLOT在多种异构场景下均显著优于现有的最先进方法。
conclusion: SubFLOT通过服务器端个性化剪枝和自适应正则化，为在资源受限的边缘设备上部署高效且个性化的联邦学习模型提供了有效方案。
---

## 摘要
联邦学习 (FL) 在保护数据隐私的同时实现了协作模型训练，但其在实际部署中受到系统和统计异构性的阻碍。虽然联邦网络剪枝为缓解这些问题提供了一条途径，但现有方法面临着一个关键困境：服务器端剪枝缺乏个性化，而客户端剪枝对于资源受限的设备来说计算成本过高。此外，剪枝过程本身会导致异构子模型之间产生显著的参数差异，从而使训练不稳定并阻碍全局收敛。为了应对这些挑战，我们提出了 SubFLOT，这是一个用于服务器端个性化联邦剪枝的新型框架。SubFLOT 引入了一个最优传输增强剪枝 (OTP) 模块，该模块将历史客户端模型视为本地数据分布的代理，将剪枝任务表述为 Wasserstein 距离最小化问题，从而在不访问原始数据的情况下生成定制化的子模型。同时，为了抵消参数差异，我们的基于缩放的自适应正则化 (SAR) 模块自适应地惩罚子模型与全局模型之间的偏差，惩罚强度根据客户端的剪枝率进行缩放。全面的实验表明，SubFLOT 一贯且显著地优于现有最先进的方法，突显了其在资源受限的边缘设备上部署高效且个性化模型的潜力。

## Abstract
Federated Learning (FL) enables collaborative model training while preserving data privacy, but its practical deployment is hampered by system and statistical heterogeneity. While federated network pruning offers a path to mitigate these issues, existing methods face a critical dilemma: server-side pruning lacks personalization, whereas client-side pruning is computationally prohibitive for resource-constrained devices. Furthermore, the pruning process itself induces significant parametric divergence among heterogeneous submodels, destabilizing training and hindering global convergence. To address these challenges, we propose SubFLOT, a novel framework for server-side personalized federated pruning. SubFLOT introduces an Optimal Transport-enhanced Pruning (OTP) module that treats historical client models as proxies for local data distributions, formulating the pruning task as a Wasserstein distance minimization problem to generate customized submodels without accessing raw data. Concurrently, to counteract parametric divergence, our Scaling-based Adaptive Regularization (SAR) module adaptively penalizes a submodel's deviation from the global model, with the penalty's strength scaled by the client's pruning rate. Comprehensive experiments demonstrate that SubFLOT consistently and substantially outperforms state-of-the-art methods, underscoring its potential for deploying efficient and personalized models on resource-constrained edge devices.

---

## 论文详细总结（自动生成）

### 论文总结：SubFLOT —— 基于最优传输的高效个性化联邦学习子模型提取

#### 1. 核心问题与整体含义（研究动机和背景）
联邦学习（FL）在实际部署中面临两大挑战：**系统异构性**（设备硬件能力不同）和**统计异构性**（数据非独立同分布，即 Non-IID）。
*   **现有剪枝技术的困境**：服务器端剪枝虽然高效但缺乏个性化（不感知本地数据）；客户端剪枝（如“训练-剪枝-微调”）虽能个性化，但对资源受限的边缘设备而言，初始训练全量模型的计算负担过重。
*   **参数散度问题**：剪枝会导致不同稀疏度的子模型在参数空间产生剧烈偏移，破坏全局收敛稳定性。
*   **研究目标**：如何在不访问原始数据的前提下，在服务器端实现个性化剪枝，并解决异构子模型训练中的参数漂移问题。

#### 2. 方法论：核心思想与关键技术
SubFLOT 框架由三个核心模块组成：
*   **OTP（最优传输增强剪枝）**：
    *   **核心思想**：将客户端的历史模型视为其本地数据分布的“代理”。
    *   **技术细节**：在服务器端，通过最小化全局模型与历史模型之间的 **Wasserstein 距离**，计算层级（Layer-wise）传输方案。利用该方案对全局模型进行神经元对齐和融合，从而为每个客户端提取出既符合其硬件约束（剪枝率）又契合其数据特征的个性化子模型。
*   **SAR（基于缩放的自适应正则化）**：
    *   **核心思想**：在本地训练中，根据剪枝率动态调整正则化强度。
    *   **公式逻辑**：损失函数中加入项 $\rho_i \cdot \| W_i - \tilde{W}_i \|^2$，其中 $\rho_i$ 是剪枝率。剪枝越严重的模型（$\rho_i$ 越大），受到的约束越强，强制其靠近服务器下发的锚点模型，从而抑制参数散度。
*   **OTA（最优传输增强聚合）**：
    *   **核心思想**：在聚合阶段，再次利用最优传输将不同结构的子模型参数映射回全局参数空间，解决因剪枝导致的神经元错位问题，实现更稳定的模型融合。

#### 3. 实验设计
*   **数据集与场景**：
    *   **计算机视觉 (CV)**：CIFAR-10/100、Tiny-ImageNet、Digit5（5个域）、PACS（4个域）。
    *   **自然语言处理 (NLP)**：AG News。
    *   **物联网 (IoT)**：HAR（人类活动识别传感器数据）。
*   **异构设置**：涵盖标签偏移（Label Skew，包括 Dirichlet 分布和病态设置）、特征偏移（Feature Shift）以及真实世界场景。
*   **Benchmark（对比方法）**：对比了 10 种最先进的方法，包括 HeteroFL、FedRolex、FedDrop、FedMP、ScaleFL、Flado、DepthFL、FedDSE、AdaptiveFL 和 FlexFL。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **4 张 NVIDIA RTX 4090 GPU** 的服务器上运行。
*   **训练参数**：模拟 20 个客户端，进行 200 轮通信，每轮本地训练 5 个 Epoch。
*   **效率数据**：论文明确给出了服务器端 OTP 模块的延迟：VGG11 为 0.12s，MobileNetV2 为 1.15s，ResNet18 为 0.20s。相比 FedAvg，SubFLOT 减少了超过 50% 的通信和计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括不同非独立同分布（Non-IID）程度的测试、不同客户端数量（10 到 100 个）的扩展性测试、不同模型稀疏度（低、中、高、动态）的鲁棒性测试。
*   **消融实验**：分别验证了 OTP、SAR 和 OTA 三个模块的必要性。
*   **可视化分析**：使用 Grad-CAM 进行了特征注意力图的可视化，直观展示了 OTP 如何保留本地特征。
*   **评价**：实验设计非常充分且客观，涵盖了多模态数据和多种异构挑战，对比基准线全面，具有很强的说服力。

#### 6. 主要结论与发现
*   **性能领先**：SubFLOT 在所有数据集和异构设置下均显著优于现有 SOTA 方法，尤其在复杂任务（如 Tiny-ImageNet）和严重特征偏移（如 Digit5）中优势更明显。
*   **收敛性**：理论证明了 SubFLOT 具有线性收敛到全局最优解邻域的保证。
*   **资源友好**：在大幅降低边缘设备负担的同时，实现了比全量模型训练更高的个性化准确率。

#### 7. 优点
*   **创新的对齐机制**：首次将最优传输（OT）引入联邦剪枝，从参数空间几何对齐的角度解决了“服务器端如何做个性化”的难题。
*   **协同优化**：OTP、SAR 和 OTA 三者形成闭环，同时处理了特征空间异构和参数空间异构。
*   **实用性强**：不要求客户端具备训练全量模型的能力，非常适合真实的低功耗边缘计算场景。

#### 8. 不足与局限
*   **冷启动问题**：OTP 依赖于“历史模型”作为代理，在第一轮训练（无历史模型时）可能退化为普通剪枝。
*   **超参数敏感性**：虽然论文做了研究，但融合比例 $\alpha$ 和正则化系数 $\lambda$ 仍需根据具体任务手动调优。
*   **计算复杂度**：尽管层级 OT 降低了开销，但对于极深或极宽的网络，服务器端的 OT 求解计算量仍可能成为潜在瓶颈。

（完）
