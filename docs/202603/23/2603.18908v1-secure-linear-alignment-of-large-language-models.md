---
title: Secure Linear Alignment of Large Language Models
title_zh: 大语言模型的安全线性对齐
authors: "Matt Gorbett, Suman Jana"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18908v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 跨孤岛推理的隐私保护框架
tldr: 本研究利用大语言模型表示空间的收敛性，提出了一个保护隐私的跨模型推理框架。该框架通过在公共数据集上学习线性仿射变换来对齐不同模型的隐藏状态，并利用同态加密技术保护推理时的客户端数据。实验表明，该方法在保持高安全性的同时实现了亚秒级延迟，在嵌入分类、离群检测及跨模型文本生成任务中均表现出色，为隐私敏感场景下的模型协作开辟了新路径。
selection_source: fresh_fetch
motivation: 独立训练的大语言模型在表示空间上具有收敛性，这为跨模型协作和隐私保护下的推理提供了新机遇。
method: 提出一种隐私保护框架，通过在公共数据集上学习仿射变换，并结合同态加密技术来保护推理过程中的客户端查询。
result: 该方法在嵌入分类和离群检测任务中表现优异，实现了亚秒级推理延迟，并首次证明了线性对齐可支持跨模型的文本生成。
conclusion: 线性对齐结合加密技术能有效实现安全且高效的跨模型推理，为隐私受限场景下的模型协作提供了可行方案。
---

## 摘要
尽管在训练目标、架构和数据模态上存在差异，但语言模型似乎越来越多地学习到相似的表示。这种独立训练模型之间新兴的兼容性，为跨模型对齐下游目标提供了新的机遇。此外，它还开启了新的潜在应用领域，例如在安全性、隐私性或竞争约束禁止直接共享数据或模型的场景中。在这项工作中，我们提出了一个隐私保护框架，利用表示收敛（representational convergence）来实现独立语言模型之间的跨孤岛推理（cross-silo inference）。该框架在共享的公开数据集上学习仿射变换，并应用同态加密（homomorphic encryption）在推理过程中保护客户端查询。通过仅对线性对齐和分类操作进行加密，该方法在保持强大安全保障的同时，实现了亚秒级的推理延迟。我们通过对表示收敛的实证研究来支持这一框架，其中我们学习了独立模型最终隐藏状态之间的线性变换。我们在嵌入分类和分布外（out-of-distribution）检测上评估了这些跨模型映射，观察到模型对之间的性能下降极小。此外，我们首次展示了线性对齐有时能够实现跨独立训练模型的文本生成。

## Abstract
Language models increasingly appear to learn similar representations, despite differences in training objectives, architectures, and data modalities. This emerging compatibility between independently trained models introduces new opportunities for cross-model alignment to downstream objectives. Moreover, it unlocks new potential application domains, such as settings where security, privacy, or competitive constraints prohibit direct data or model sharing. In this work, we propose a privacy-preserving framework that exploits representational convergence to enable cross-silo inference between independent language models. The framework learns an affine transformation over a shared public dataset and applies homomorphic encryption to protect client queries during inference. By encrypting only the linear alignment and classification operations, the method achieves sub-second inference latency while maintaining strong security guarantees. We support this framework with an empirical investigation into representational convergence, in which we learn linear transformations between the final hidden states of independent models. We evaluate these cross-model mappings on embedding classification and out-of-distribution detection, observing minimal performance degradation across model pairs. Additionally, we show for the first time that linear alignment sometimes enables text generation across independently trained models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Secure Linear Alignment of Large Language Models》（大语言模型的安全线性对齐）的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）规模的扩大，研究发现不同架构、不同数据训练的模型在表示空间上呈现出一种“柏拉图式”的收敛（Platonic Representation Hypothesis），即它们对现实世界的理解在几何结构上趋于一致。
*   **核心问题**：如何在不直接共享私有模型参数、不泄露客户端查询数据的前提下，实现不同模型之间的互操作性（如用模型 A 的编码器配合模型 B 的分类头）？
*   **研究背景**：在医疗（HIPAA）、金融或跨境数据传输（GDPR）等隐私敏感场景中，直接共享数据或模型权重受到严格限制。现有的隐私保护推理方案（如加密整个 Transformer）计算开销巨大，难以实用。

### 2. 方法论：核心思想与技术细节
论文提出了 **HELIX**（Homomorphically Encrypted Linear Inference across models）框架，其核心思想是：**仅对轻量级的线性对齐层和分类层进行同态加密，而让模型主体在本地运行。**

*   **核心思想**：利用线性仿射变换（Affine Map）将源模型（客户端）的隐藏状态映射到目标模型（服务端）的特征空间。
*   **关键流程**：
    1.  **训练阶段（安全对齐）**：客户端和服务端在共享的公共数据集（如 Wikipedia）上提取嵌入。客户端使用 **CKKS 同态加密方案** 加密其嵌入并发送给服务端。服务端计算加密后的跨协方差矩阵并返回。客户端解密后，通过岭回归（Ridge Regression）公式计算对齐矩阵 $W^*$：
        $$W^* = (Z_B^\top Z_B + \lambda I)^{-1} Z_B^\top Z_A$$
    2.  **推理阶段（隐私推理）**：客户端在本地提取查询的嵌入，应用对齐矩阵 $W^*$，然后加密结果发送给服务端。服务端在加密域执行线性分类操作（$y = zV + c$），并将加密结果返回给客户端解密。
*   **关键技术**：使用 CKKS 方案支持实数运算，且仅涉及矩阵乘法和加法，避免了复杂的非线性算子加密。

### 3. 实验设计
*   **数据集与场景**：
    *   **下游分类任务**：SST-2, AG News, TREC, MNLI, DBpedia, Yahoo Answers。
    *   **文本生成任务**：MMLU（结构化生成）、Alpaca（指令遵循）。
    *   **分布外（OOD）检测**：评估对齐是否保留了模型的置信度结构。
*   **Benchmark 与对比对象**：
    *   **Baseline**：目标模型在原生嵌入上的表现。
    *   **对比方法**：与 BOLT、Nimbus、Nexus 等全模型加密推理系统进行延迟和通信量对比。
*   **模型覆盖**：涵盖了 OpenAI (text-embedding-3-small)、Google (Gemini)、Cohere、Mistral、Qwen、Llama 3 等主流闭源 API 和开源模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**训练对齐矩阵所使用的具体 GPU 型号和总训练时长。
*   **推理性能**：明确提到推理过程在 **CPU** 上运行，实现了**亚秒级（<1s）**的端到端延迟。
*   **通信开销**：每样本通信量小于 1MB，远低于传统 MPC 或全模型加密方案。

### 5. 实验数量与充分性
*   **实验规模**：
    *   分析了 **34 个模型对** 的跨模型文本生成。
    *   在 6 个主流分类数据集上进行了详尽的交叉验证。
    *   包含自动化评估（LLM-as-a-Judge，使用 GPT-4o 和 Claude 3.5）和 **200 组人工评估**。
*   **充分性评价**：实验设计非常充分且客观。作者不仅验证了分类任务，还深入探讨了文本生成的边界条件（如分词器兼容性、模型规模），并进行了消融实验（如公共数据量对对齐效果的影响）和隐私攻击测试（成员推理攻击）。

### 6. 主要结论与发现
*   **线性对齐的有效性**：简单的线性映射足以在不同厂商的模型之间转移分类能力，性能损失极小。
*   **文本生成的关键因素**：
    1.  **分词器（Tokenizer）兼容性**：这是成功的决定性因素。词表重合度（Jaccard Index）和精确 Token 匹配率与生成质量高度正相关（r=0.898）。
    2.  **模型规模阈值**：源模型若小于 4B 参数，即使分词器对齐，生成质量也会大幅下降。
*   **隐私与效率平衡**：HELIX 在保持 128 位安全强度的同时，比现有的全模型加密方案快 20-60 倍。
*   **不对称转移**：从强模型向弱模型映射时，性能保持较好；反之则退化严重。

### 7. 优点（亮点）
*   **极高的实用性**：避开了加密 Transformer 中最难处理的 Softmax 和 Attention 算子，通过“本地特征提取 + 云端加密线性推理”实现了工业级延迟。
*   **跨厂商协作**：首次系统性证明了在不获取对方权重的情况下，通过公共数据对齐即可实现闭源 API（如 OpenAI）与开源模型之间的功能迁移。
*   **理论支撑**：利用 CKA 和 SVCCA 指标量化了模型间的表示相似性，为线性对齐提供了实证基础。

### 8. 不足与局限
*   **分词器依赖**：如果两个模型的分词逻辑差异巨大（如 Gemma 与其他模型），线性对齐在生成任务上会失效。
*   **任务局限**：目前主要针对线性头（Linear Head）任务。对于需要复杂非线性解码的任务，该框架的适用性尚未验证。
*   **服务端隐私风险**：虽然保护了客户端查询，但服务端需要暴露其线性分类器的维度信息，且 W* 矩阵可能泄露服务端模型的部分几何特征。
*   **生成长度限制**：实验中仅测试了 128 个 Token 的生成，长文本生成中的误差累积效应有待进一步研究。

（完）
