---
title: "SplitFT: An Adaptive Federated Split Learning System For LLMs Fine-Tuning"
title_zh: SplitFT：一种用于大语言模型微调的自适应联邦拆分学习系统
authors: "Yimeng Shan, Zhaorui Zhang, Sheng Di, Yu Liu, Xiaoyi Lu, Benben Liu"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26388v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 用于大语言模型微调的联邦拆分学习
tldr: 本研究针对大语言模型在联邦拆分学习微调中面临的设备异构性和通信开销挑战，提出了SplitFT系统。该系统允许客户端根据自身计算资源和模型表现自适应选择拆分层，并引入LoRA秩缩减技术以降低传输数据量。此外，研究还提出基于长度的Dirichlet数据划分方法。实验证明，SplitFT在提升微调效率的同时保证了模型性能，是解决分布式LLM训练难题的有效方案。
selection_source: fresh_fetch
motivation: 旨在解决联邦拆分学习在微调大语言模型时，因设备与数据异构性导致的性能下降及高昂的通信开销问题。
method: 提出SplitFT系统，通过自适应拆分层设置、LoRA秩缩减技术以及基于长度的Dirichlet数据划分方法来优化训练过程。
result: 实验结果显示，SplitFT在多个主流基准测试中，其微调时间效率和模型性能均优于现有的最先进方法。
conclusion: SplitFT为异构环境下的分布式大语言模型微调提供了一种高效、灵活且能显著降低通信成本的自适应解决方案。
---

## 摘要
联邦拆分学习已被认为是解决经典联邦学习中客户端计算资源限制的一种有效方法，同时能保证跨数据所有者的分布式模型训练中的数据隐私。然而，当这种训练策略应用于大语言模型（LLMs）微调时，面临着一些严峻挑战。这些挑战包括在不同客户端之间自适应地设置切分层（cutlayer），以解决显著影响系统性能的数据和设备异构性问题。此外，在微调过程中有效降低通信开销也是另一个挑战。目前尚无工作尝试解决这些挑战。为了填补这一空白，我们提出了 SplitFT，一种用于 LLMs 微调的自适应联邦拆分学习系统。SplitFT 允许不同客户端根据其计算资源和训练模型性能设置不同的切分层。SplitFT 还提出降低切分层中的 LoRA 秩（rank），以减少通信开销。除了为我们提出的拆分联邦学习系统模拟现实应用中的异构数据外，我们还提出了一种基于长度的狄利克雷（Dirichlet）方法，将训练数据划分到不同的客户端。广泛的实验结果表明，在各种流行基准测试中，我们提出的方法在微调时间效率和模型性能方面均优于现有最先进的方法。

## Abstract
Federated Split Learning has been identified as an efficient approach to address the computational resource constraints of clients in classical federated learning, while guaranteeing data privacy for distributed model training across data owners. However, it faces some critical challenges when such a training strategy meets large language models (LLMs) for fine-tuning. Such challenges include setting the cutlayer adaptively across different clients to address the data and device heterogeneity issues, which affect the system performance significantly. In addition, efficiently reducing the communication overhead during the fine-tuning procedure is also another challenge. No work tries to address these challenges.   To bridge this gap, we propose SplitTF, an adaptive federated split learning system for LLMs fine-tuning. SplitFT enables different clients to set different cut layers according to their computation resources and trained model performance. SplitFT also proposes to reduce the LoRA rank in cutlayer to reduce the communication overhead. In addition to simulating the heterogeneous data in real-world applications for our proposed split federated learning system, we propose a length-based Dirichlet approach to divide the training data into different clients. Extensive experimental results show that our proposed approach outperforms the state-of-the-art approach for fine-tuning time efficiency and model performance based on various popular benchmarks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SplitFT** 的自适应联邦拆分学习系统，专门用于大语言模型（LLM）的微调。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在分布式环境下微调 LLM 时，传统的联邦学习（FL）对客户端计算资源要求过高，而拆分学习（SL）在处理设备异构性（计算能力差异）、数据异构性（Non-IID 数据）以及高昂的通信开销（中间层数据传输）方面存在显著挑战。
*   **研究动机**：现有的联邦拆分学习（FSL）方案通常采用固定的模型切分点，这导致高性能设备必须等待低性能设备（木桶效应），且未针对 LLM 微调中的参数高效技术（如 LoRA）进行通信优化。

### 2. 方法论
SplitFT 系统集成了以下核心技术：
*   **自适应层分配策略**：提出一种启发式算法，根据每个客户端的计算资源和模型表现（测试准确率）动态调整切分层（Cutlayer）。如果客户端表现优于平均水平，则增加其计算负载（分配更多层）；反之则减少，从而优化整体训练效率。
*   **切分层 LoRA 秩缩减（Rank Reduction）**：在切分层（客户端与服务器交界处）采用较小的 LoRA 秩（$r_{cut}$），而在其他层采用较大的秩（$r_{others}$）。这种非对称设计旨在显著降低传输“粉碎数据”（Smashed Data）时的通信开销，同时保持模型的表达能力。
*   **基于长度的 Dirichlet 数据划分**：为了模拟真实世界的异构数据，提出根据文本长度将数据集分为 $K$ 类，并利用狄利克雷分布（参数 $\alpha$ 控制异构程度）将数据分配给不同客户端。
*   **算法流程**：客户端执行前向传播至切分层 -> 发送中间数据至服务器 -> 服务器完成后续前向及反向传播 -> 服务器回传梯度 -> 客户端更新本地 LoRA 参数 -> 定期通过 FedAvg 聚合客户端侧的 LoRA 适配器。

### 3. 实验设计
*   **数据集**：主要使用 **Wikitext2-v1**（包含 36,700 条训练样本），用于下一句生成任务。
*   **评估模型（Benchmarks）**：选择了三种流行的轻量级 LLM：**GPT2-small**、**OPT-125M** 和 **GPT-Neo 125M**。
*   **对比方法**：
    *   **Same Split（基准）**：所有客户端使用固定的切分层。
    *   **IID vs. Non-IID**：对比不同数据分布下的表现。
    *   **不同 LoRA 秩配置**：对比对称与非对称秩设置对性能的影响。

### 4. 资源与算力
*   **硬件设备**：实验在 **NVIDIA GeForce RTX 3090 GPU** 上运行。
*   **软件框架**：基于 PyTorch 2.3 和 Flower 联邦学习框架实现。
*   **训练细节**：Batch size 为 4，学习率为 $5 \times 10^{-5}$，最大序列长度 512。文中未明确给出总训练时长，但记录了平均每轮（Round）的时间和通信开销。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多组实验，包括：
    1.  切分层位置对准确率和通信量的影响（消融实验）。
    2.  LoRA 秩大小对性能的影响。
    3.  自适应切分与固定切分的对比。
    4.  不同 $\alpha$ 值（0.1 到 100）下的 Non-IID 鲁棒性测试。
    5.  跨模型的泛化性验证。
*   **充分性评价**：实验设计较为全面，涵盖了性能、效率、通信量和鲁棒性等多个维度。对比实验设置了清晰的 Baseline，且在不同架构的 LLM 上进行了验证，结果具有较好的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：SplitFT 在收敛速度和最终模型困惑度（Perplexity）上均优于固定切分的基准方法。
*   **通信优化**：通过降低切分层的 LoRA 秩，可以在不损失模型质量的前提下有效减少通信开销。
*   **异构适应性**：自适应层分配机制能有效处理 Non-IID 数据，避免了过拟合问题，并在异构设备环境中实现了更好的负载均衡。
*   **泛化性**：该系统在 GPT2、OPT 和 GPT-Neo 上表现一致，证明了其作为通用 LLM 微调框架的潜力。

### 7. 优点（亮点）
*   **动态性**：打破了传统 FSL 静态切分的限制，引入了基于表现的反馈调节机制。
*   **通信感知**：敏锐地捕捉到切分层是通信瓶颈，并利用 LoRA 的低秩特性进行了针对性优化。
*   **数据模拟创新**：提出的基于长度的 Dirichlet 划分方法为 LLM 联邦学习的研究提供了更贴近实际的 Non-IID 模拟手段。

### 8. 不足与局限
*   **模型规模有限**：实验主要集中在 125M 左右的小规模模型上，对于百亿（7B+）甚至千亿参数规模的真正“大”语言模型，该系统的扩展性和切分策略的有效性尚待验证。
*   **任务单一**：实验仅限于文本生成（Wikitext2），未在逻辑推理、代码编写或多轮对话等更复杂的微调任务上进行测试。
*   **隐私深度分析不足**：虽然提到拆分学习增强了隐私，但未针对针对中间层数据的“模型反演攻击”或“成员推理攻击”提供定量的防御强度分析。

（完）
