---
title: "Adaptive Methods Are Preferable in High Privacy Settings: An SDE Perspective"
title_zh: 自适应方法在高隐私设置下更具优势：基于 SDE 的视角
authors: "Enea Monzio Compagnoni, Alessandro Stanghellini, Rustem Islamov, Aurelien Lucchi, Anastasiia Koloskova"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03226v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 隐私优化器的隐私-效用权衡
tldr: 本研究通过随机微分方程（SDE）视角，对比分析了差分隐私（DP）环境下的 SGD 与自适应优化器（如 DP-SignSGD）。研究发现，在高隐私（低 ε）或大噪声机制下，DP-SignSGD 的收敛速度和效用权衡优于 DP-SGD。更重要的是，自适应方法的最优学习率对隐私预算 ε 具有鲁棒性，无需针对不同隐私级别频繁调参，极大地提升了实际应用中的便利性，且该结论可推广至 DP-Adam。
selection_source: fresh_fetch
motivation: 旨在通过理论分析解释为何自适应优化器在差分隐私训练中通常比非自适应方法表现更好。
method: 采用随机微分方程（SDE）框架，对逐样本裁剪下的 DP-SGD 和 DP-SignSGD 的收敛行为进行建模。
result: 理论证明 DP-SignSGD 在高隐私下具有更优的收敛速度，且其最优学习率几乎不随隐私预算 ε 变化。
conclusion: 自适应优化方法在隐私保护学习中更具优势，其超参数的跨隐私级别迁移能力使其比 DP-SGD 更具实践价值。
---

## 摘要
随着隐私法规的收紧，差分隐私（DP）正成为大规模训练的核心。我们通过随机微分方程（SDE）的视角重新审视了 DP 噪声如何与优化中的自适应性相互作用，提供了首个基于 SDE 的隐私优化器分析。针对逐样本裁剪下的 DP-SGD 和 DP-SignSGD，我们展示了在固定超参数下的显著差异：DP-SGD 以与 $\varepsilon$ 无关的速度收敛，其隐私-效用权衡为 $\mathcal{O}(1/\varepsilon^2)$；而 DP-SignSGD 的收敛速度与 $\varepsilon$ 成线性关系，权衡为 $\mathcal{O}(1/\varepsilon)$，在高隐私或大批量噪声机制下占据主导地位。相比之下，在最优学习率下，两种方法达到了相当的理论渐近性能；然而，DP-SGD 的最优学习率随 $\varepsilon$ 线性缩放，而 DP-SignSGD 的最优学习率基本上与 $\varepsilon$ 无关。这使得自适应方法更具实用性，因为它们的超参数可以在不同的隐私级别之间迁移，几乎不需要重新调优。实验结果在训练和测试指标上证实了我们的理论，并从 DP-SignSGD 经验性地扩展到了 DP-Adam。

## Abstract
Differential Privacy (DP) is becoming central to large-scale training as privacy regulations tighten. We revisit how DP noise interacts with adaptivity in optimization through the lens of stochastic differential equations, providing the first SDE-based analysis of private optimizers. Focusing on DP-SGD and DP-SignSGD under per-example clipping, we show a sharp contrast under fixed hyperparameters: DP-SGD converges at a Privacy-Utility Trade-Off of $\mathcal{O}(1/\varepsilon^2)$ with speed independent of $\varepsilon$, while DP-SignSGD converges at a speed linear in $\varepsilon$ with an $\mathcal{O}(1/\varepsilon)$ trade-off, dominating in high-privacy or large batch noise regimes. By contrast, under optimal learning rates, both methods achieve comparable theoretical asymptotic performance; however, the optimal learning rate of DP-SGD scales linearly with $\varepsilon$, while that of DP-SignSGD is essentially $\varepsilon$-independent. This makes adaptive methods far more practical, as their hyperparameters transfer across privacy levels with little or no re-tuning. Empirical results confirm our theory across training and test metrics, and empirically extend from DP-SignSGD to DP-Adam.

---

## 论文详细总结（自动生成）

这篇论文通过随机微分方程（SDE）的视角，深入探讨了差分隐私（DP）优化器中自适应性与隐私噪声之间的相互作用。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：随着隐私法规（如欧盟 AI 法案、美国行政命令）的收紧，差分隐私随机梯度下降（DP-SGD）已成为敏感数据训练的标准。
*   **核心问题**：在 DP 训练中，注入的噪声和逐样本裁剪（Clipping）如何影响优化动力学？特别是，为什么自适应优化器（如 Adam）在隐私设置下往往比非自适应的 SGD 表现更稳健？
*   **研究动机**：填补 DP 优化器缺乏连续时间理论分析的空白，解释自适应方法在不同隐私预算（$\varepsilon$）下的优势。

### 2. 方法论
*   **核心思想**：利用 SDE 框架对离散的 DP 优化算法进行连续时间建模。通过分析 SDE 的漂移项（Drift）和扩散项（Diffusion），推导收敛速度和隐私-效用权衡（Privacy-Utility Trade-Off）。
*   **关键技术细节**：
    *   **SDE 建模**：首次为 DP-SGD 和 DP-SignSGD 推导了弱近似 SDE。
    *   **两阶段分析**：将动力学分为“裁剪阶段”（Phase 1，梯度模长超过阈值）和“非裁剪阶段”（Phase 2）。
    *   **噪声模型**：引入了重尾噪声模型（Student-t 分布）来捕捉逐样本裁剪后的梯度分布特征。
    *   **两种协议**：
        *   **Protocol A（固定超参数）**：分析在不重新调优学习率的情况下，改变 $\varepsilon$ 对性能的影响。
        *   **Protocol B（最优调优）**：分析针对每个 $\varepsilon$ 寻找最优学习率时，超参数的缩放规律。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成实验**：二次凸函数（Quadratic）和四次非凸函数（Quartic），用于验证 SDE 模型的准确性。
    *   **真实任务**：IMDB（情感分析，逻辑回归）、StackOverflow（标签预测，大规模逻辑回归）、MovieLens-100k（推荐系统，非凸矩阵分解）。
*   **对比方法**：DP-SGD、DP-SignSGD（作为理论代理）以及 DP-Adam（作为经验扩展）。
*   **Benchmark**：以经过网格搜索调优的最优 DP-SGD 为基准。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：实验基于 Google 发布的官方 `DP2` 代码库进行，表明其遵循了大规模隐私训练的标准实践。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单凸优化到大规模非凸矩阵分解的多种任务。
*   **充分性**：
    *   实验不仅验证了训练损失，还验证了测试损失，证明了结论的泛化性。
    *   通过 10 到 200 次不等的重复实验（如 SDE 验证重复 200 次，真实任务重复 3-10 次）来确保结果的统计显著性。
    *   **客观性**：对比了固定参数和动态调优两种实际场景，实验设计较为公平且符合工业界调参逻辑。

### 6. 主要结论与发现
*   **隐私-效用权衡差异**：在固定超参数下，DP-SGD 的效用损失随隐私预算缩放为 $\mathcal{O}(1/\varepsilon^2)$，而 DP-SignSGD 仅为 $\mathcal{O}(1/\varepsilon)$。这意味着在严苛隐私（小 $\varepsilon$）下，自适应方法更优。
*   **收敛速度**：DP-SGD 的收敛速度与 $\varepsilon$ 无关，而 DP-SignSGD 的速度随 $\varepsilon$ 线性减慢。
*   **超参数迁移性（核心发现）**：DP-SGD 的最优学习率 $\eta^*$ 与 $\varepsilon$ 呈线性关系（$\eta^* \propto \varepsilon$），而自适应方法（DP-SignSGD/Adam）的 $\eta^*$ 几乎与 $\varepsilon$ 无关。
*   **实践意义**：自适应方法在不同隐私级别间具有更好的超参数可迁移性，减少了昂贵的隐私预算消耗（因为每次调参都会消耗隐私预算）。

### 7. 优点
*   **理论深度**：提供了首个基于 SDE 的隐私优化器理论框架，揭示了自适应性如何自动调节隐私噪声。
*   **实用性强**：直接回答了开发者最关心的“如何选优化器”和“如何调参”的问题。
*   **鲁棒性验证**：理论推导虽基于 DP-SignSGD，但实验成功证明其结论完全适用于更复杂的 DP-Adam。

### 8. 不足与局限
*   **模型限制**：理论分析主要基于线性模型或具有特定平滑性的函数，对于深度神经网络（如 Transformer）中复杂的非凸动力学可能存在近似偏差。
*   **假设依赖**：SDE 近似依赖于高维假设（参数维度 $d$ 足够大）和特定的信号噪声比条件。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，难以评估其在大规模模型上的计算开销。

（完）
