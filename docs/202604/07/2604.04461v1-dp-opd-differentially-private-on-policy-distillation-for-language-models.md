---
title: "DP-OPD: Differentially Private On-Policy Distillation for Language Models"
title_zh: DP-OPD：语言模型的差分隐私同策略蒸馏
authors: "Fatemeh Khadem, Sajad Mousavi, Yi Fang, Yuhong Liu"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04461v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 差分隐私蒸馏中的隐私与效用权衡
tldr: 针对大语言模型在处理敏感数据时差分隐私（DP）训练导致的性能下降问题，本文提出DP-OPD框架。该方法通过在学生模型的生成轨迹上利用冻结的教师模型提供密集Token级目标，实现了无需合成数据的在线蒸馏。DP-OPD仅在学生端应用DP-SGD，简化了训练流程，在严格隐私预算下显著提升了模型性能，实现了高效且隐私保护的模型压缩。
selection_source: fresh_fetch
motivation: 传统的差分隐私微调在长文本生成中存在严重的效用损失，且现有的隐私蒸馏方法计算开销大或依赖复杂的离线合成流程。
method: 提出一种无需合成数据的在线蒸馏框架，通过在学生生成的轨迹上利用冻结教师模型进行私有化广义知识蒸馏。
result: 在严格隐私预算（ε=2.0）下，DP-OPD在多个数据集上的困惑度表现优于传统的DP微调和基于合成数据的蒸馏方法。
conclusion: DP-OPD通过将隐私压缩简化为单一的学生训练循环，在提升模型性能的同时大幅降低了隐私保护训练的复杂性。
---

## 摘要
大语言模型（LLMs）正越来越多地被应用于包含敏感信息的专有和特定领域语料库，这在形式化隐私保证与通过模型压缩实现高效部署之间产生了矛盾。差分隐私（DP）通常通过 DP-SGD 实现，提供记录级保护，但在自回归生成中往往会导致显著的效用损失，因为优化噪声会放大长序列生成过程中的曝光偏差和复合误差。现有的隐私蒸馏方法要么对教师模型和学生模型都应用 DP-SGD，从而加剧了计算开销和隐私-效用权衡；要么依赖于从经过 DP 训练的教师模型生成 DP 合成文本，虽然避免了在学生模型上应用 DP，但代价是需要对大型教师模型进行 DP 优化并引入离线生成流水线。我们提出了差分隐私同策略蒸馏（DP-OPD），这是一种无需合成的框架，仅通过在学生模型上应用 DP-SGD 来强制执行隐私保护，同时利用冻结的教师模型在学生生成的轨迹上提供密集的词元级目标。DP-OPD 通过对后续词元进行隐私广义知识蒸馏来实现这一想法。在严格的隐私预算（ε=2.0）下，DP-OPD 的困惑度优于 DP 微调和异策略 DP 蒸馏，并超过了基于合成的 DP 蒸馏（Yelp：44.15→41.68；BigPatent：32.43→30.63），同时显著简化了训练流水线。特别是，DP-OPD 通过消除 DP 教师训练和离线合成文本生成，将隐私压缩简化为单一的 DP 学生训练循环。代码将在论文发表时发布于 https://github.com/khademfatemeh/dp_opd。

## Abstract
Large language models (LLMs) are increasingly adapted to proprietary and domain-specific corpora that contain sensitive information, creating a tension between formal privacy guarantees and efficient deployment through model compression. Differential privacy (DP), typically enforced via DP-SGD, provides record-level protection but often incurs substantial utility loss in autoregressive generation, where optimization noise can amplify exposure bias and compounding errors along long rollouts. Existing approaches to private distillation either apply DP-SGD to both teacher and student, worsening computation and the privacy--utility tradeoff, or rely on DP synthetic text generation from a DP-trained teacher, avoiding DP on the student at the cost of DP-optimizing a large teacher and introducing an offline generation pipeline. We propose \textbf{Differentially Private On-Policy Distillation (DP-OPD)}, a synthesis-free framework that enforces privacy solely through DP-SGD on the student while leveraging a frozen teacher to provide dense token-level targets on \emph{student-generated} trajectories. DP-OPD instantiates this idea via \emph{private generalized knowledge distillation} on continuation tokens. Under a strict privacy budget ($\varepsilon=2.0$), DP-OPD improves perplexity over DP fine-tuning and off-policy DP distillation, and outperforms synthesis-based DP distillation (Yelp: 44.15$\rightarrow$41.68; BigPatent: 32.43$\rightarrow$30.63), while substantially simplifying the training pipeline. In particular, \textbf{DP-OPD collapses private compression into a single DP student-training loop} by eliminating DP teacher training and offline synthetic text generation. Code will be released upon publication at https://github.com/khademfatemeh/dp_opd.

---

## 论文详细总结（自动生成）

以下是对论文 **《DP-OPD: Differentially Private On-Policy Distillation for Language Models》** 的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：在将大语言模型（LLM）适配到包含敏感信息的专有领域时，面临**差分隐私（DP）保护**与**模型压缩（蒸馏）**的双重需求。
*   **核心痛点**：
    1.  **效用损失**：传统的 DP-SGD 优化会引入噪声，在自回归生成中会放大“曝光偏差”（Exposure Bias），导致长文本生成的复合误差严重。
    2.  **流程复杂**：现有的隐私蒸馏方法（如 DISTIL DP）通常需要先训练一个 DP 教师模型，再离线生成大量合成数据，最后训练学生模型，步骤繁琐且计算开销巨大。
*   **论文目标**：提出一种简化且高效的框架，在保证严格隐私的前提下，通过在线同策略蒸馏提升压缩模型的生成质量。

### 2. 方法论
*   **核心思想**：**DP-OPD（差分隐私同策略蒸馏）**。该方法放弃了离线合成数据，转而让学生模型在训练过程中实时生成轨迹（Rollouts），并由一个**冻结的、非私有的教师模型**在这些学生实际访问的状态上提供词元（Token）级的密集指导。
*   **关键技术细节**：
    *   **仅学生端 DP-SGD**：隐私保护仅通过在学生模型更新时应用 DP-SGD 实现，教师模型不参与隐私预算分配，简化了隐私会计（Privacy Accounting）。
    *   **同策略采样**：以概率 $\lambda$ 进行同策略采样（学生自生成续写），以 $1-\lambda$ 进行异策略采样（教师强制引导）。
    *   **广义知识蒸馏（GKD）**：利用 GKD 损失函数，通过参数 $\beta$ 在 Forward-KL（$\beta=0$）和 Reverse-KL（$\beta=1$）之间插值，以适应学生模型容量不足的问题。
    *   **控制代码（Control Codes）**：将结构化属性（如评分、类别）转化为文本前缀，实现受控生成。

### 3. 实验设计
*   **数据集**：
    *   **Yelp**：短文本评论，包含 1.9M 训练样本。
    *   **BigPatent**：长文本专利摘要，包含 200K 训练样本。
*   **模型配置**：教师模型为 **GPT-2 Large (774M)**，学生模型为 **DistilGPT-2 (82M)**，参数量差距约 9.5 倍。
*   **对比方法（Baselines）**：
    1.  **DP-SGD**：仅对学生进行私有微调，无教师指导。
    2.  **DPKD**：师生均应用 DP-SGD 的传统异策略蒸馏。
    3.  **DISTIL DP**：基于 DP 合成文本的蒸馏方法（ACL 2024 成果）。
*   **评估指标**：测试集困惑度（Perplexity, PPL）。

### 4. 资源与算力
*   **硬件设备**：所有实验均在单张 **NVIDIA RTX A6000 (40GB)** GPU 上完成。
*   **训练时长**：
    *   **BigPatent**：训练 40 个 epoch，耗时约 **2 天 7 小时**。
    *   **Yelp**：训练 5 个 epoch，耗时约 **3 天 16 小时**。
*   **对比说明**：论文强调其方法比 DISTIL DP（通常需要 8 张 A100 组成的集群）更具资源效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了短文本（Yelp）和长文本（BigPatent）两种典型生成场景。
*   **消融实验**：针对 GKD 的散度参数 $\beta$ 进行了详细消融，验证了不同散度度量对 PPL 的影响。
*   **公平性与客观性**：实验采用了与 SOTA 基线相同的预处理、控制代码格式和隐私预算（$\epsilon=2.0$），确保了对比的直接性和公平性。实验组数足以支撑其结论。

### 6. 主要结论与发现
*   **性能领先**：在 $\epsilon=2.0$ 的严格隐私预算下，DP-OPD 在两个数据集上的 PPL 均优于所有基线方法（例如在 Yelp 上将 PPL 从 44.15 降至 41.68）。
*   **同策略的优势**：通过在学生生成的轨迹上进行蒸馏，有效缓解了 DP 噪声导致的复合误差，使模型在推理时的状态分布更接近教师的指导。
*   **参数敏感性**：实验发现较小的 $\beta$ 值（更接近 Forward-KL）更有利于降低困惑度。

### 7. 优点
*   **架构简洁**：将隐私压缩简化为单一的 DP 学生训练循环，消除了 DP 教师训练和离线数据合成的复杂性。
*   **效用-隐私权衡优化**：在相同的隐私预算下，比现有的合成数据方法提供了更强的语言建模能力。
*   **实用性强**：单卡可练，降低了中小机构部署隐私保护 LLM 的门槛。

### 8. 不足与局限
*   **训练开销**：虽然简化了流程，但在线生成 Rollouts 显著增加了训练时的推理负担，导致单次迭代时间变长。
*   **教师模型假设**：该方法假设存在一个高质量的、可公开访问或已训练好的教师模型；若教师模型也需从私有数据中训练，则仍需考虑额外的隐私成本。
*   **分词器一致性**：实验主要基于师生共享分词器的场景，虽然文中提到了跨分词器的解决方案，但未给出详尽的实验验证。

（完）
