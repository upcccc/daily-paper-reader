---
title: "Convex Optimization with Local Label Differential Privacy: Tight Bounds in All Privacy Regimes"
title_zh: 本地标签差分隐私约束下的凸优化：所有隐私机制下的紧致界
authors: "Lynn Chua, Badih Ghazi, Ravi Kumar, Pasin Manurangsi, Ziteng Sun, Chiyuan Zhang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10200v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 本地标签差分隐私下的随机凸优化
tldr: 本研究探讨了在本地标签差分隐私（L-LDP）约束下的随机凸优化（SCO）问题。针对以往研究中超额风险对标签空间大小 K 呈线性依赖的局限性，本文提出了一种新型高效的非交互式 L-LDP 算法，将风险界限从 O(K) 改进至 O(sqrt(K))。此外，作者还证明了在所有隐私机制下该界限的信息论下界，从而确立了紧凑的理论界限。
selection_source: fresh_fetch
motivation: 旨在解决现有 L-LDP 随机凸优化算法中超额风险对标签空间大小 K 线性依赖导致的效率低下问题。
method: 提出了一种新型高效的非交互式 L-LDP 算法，通过优化标签随机化机制来降低估计误差。
result: 在所有隐私机制下将超额风险对标签空间大小的依赖从 O(K) 降低至 O(sqrt(K))，实现了二次方的性能提升。
conclusion: 通过证明匹配的信息论下界，确立了 L-LDP 约束下随机凸优化的紧凑风险界限，解决了该领域的开放性问题。
---

## 摘要
我们研究了在本地标签差分隐私（L-LDP）约束下的随机凸优化（SCO）问题。在这种设定下，特征被视为公开的，但相应的标签是敏感的，每个用户在将其发送给不可信的分析器之前必须在本地对其进行随机化。先前关于 L-LDP 下 SCO 的工作（Ghazi 等人，2021）建立了一个与标签空间大小 $K$ 呈线性依赖的超额总体风险界：在高隐私机制（$ε\leq 1$）下为 $O\left({\frac{K}{ε\sqrt{n}}}\right)$，在中等隐私机制（$1 \leq ε\leq \ln K$）下为 $O\left({\frac{K}{e^ε \sqrt{n}}}\right)$。这留下了一个悬而未决的问题，即这种线性代价是否是 L-LDP 模型所固有的。在本文中，我们解决了这个问题。首先，我们提出了一种新型且高效的非交互式 L-LDP 算法，该算法在高隐私机制（$ε\leq 1$）下实现了 $O\left({\sqrt{\frac{K}{εn}}}\right)$ 的超额风险，在中等隐私机制（$1 \leq ε\leq \ln K$）下实现了 $O\left({\sqrt{\frac{K}{e^ε n}}}\right)$ 的超额风险。这将对标签空间大小的依赖性从 $O(K)$ 二次改进为 $O(\sqrt{K})$。其次，我们证明了对于任何足够大的 $n$，在所有隐私机制下都存在匹配的信息论下界。

## Abstract
We study the problem of Stochastic Convex Optimization (SCO) under the constraint of local Label Differential Privacy (L-LDP). In this setting, the features are considered public, but the corresponding labels are sensitive and must be randomized by each user locally before being sent to an untrusted analyzer. Prior work for SCO under L-LDP (Ghazi et al., 2021) established an excess population risk bound with a \emph{linear} dependence on the size of the label space, $K$: $O\left({\frac{K}{ε\sqrt{n}}}\right)$ in the high-privacy regime ($ε\leq 1$) and $O\left({\frac{K}{e^ε \sqrt{n}}}\right)$ in the medium-privacy regime ($1 \leq ε\leq \ln K$). This left open whether this linear cost is fundamental to the L-LDP model. In this note, we resolve this question. First, we present a novel and efficient non-interactive L-LDP algorithm that achieves an excess risk of $O\left({\sqrt{\frac{K}{εn}}}\right)$ in the high-privacy regime ($ε\leq 1$) and $O\left({\sqrt{\frac{K}{e^ε n}}}\right)$ in the medium-privacy regime ($1 \leq ε\leq \ln K$). This quadratically improves the dependency on the label space size from $O(K)$ to $O(\sqrt{K})$. Second, we prove a matching information-theoretic lower bound across all privacy regimes for any sufficiently large $n$.

---

## 论文详细总结（自动生成）

这篇论文针对本地标签差分隐私（Local Label Differential Privacy, L-LDP）约束下的随机凸优化（SCO）问题进行了深入研究，确立了在所有隐私机制下的紧致理论界限。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在机器学习中，有时特征（Features）是公开的，而标签（Labels）是敏感的（如医疗诊断）。标签差分隐私（Label DP）允许在保护标签隐私的同时利用公开特征。
*   **核心问题**：在**本地模型**（Local Model，即用户不信任中央服务器，在本地对数据进行扰动）下，现有的随机凸优化算法在超额风险（Excess Risk）上对标签空间大小 $K$ 的依赖是线性的（$O(K)$）。
*   **研究动机**：探讨这种线性代价是否是 L-LDP 模型固有的，还是现有技术的局限。论文旨在寻找在所有隐私强度（$\epsilon$）下，超额风险与 $K$、样本量 $n$ 之间的最优权衡关系。

### 2. 论文提出的方法论
论文提出了一种新型的非交互式 L-LDP 算法（Algorithm 1），其核心思想包括：
*   **子集选择随机化机制**：每个用户 $t$ 拥有私有标签 $Y_t$，通过一个随机化器 $R$ 将其转换为标签空间 $[K]$ 的一个子集 $S_t$。
    *   **概率分配**：如果标签 $i = Y_t$，则 $i$ 进入子集 $S_t$ 的概率为 $1/2$；如果 $i \neq Y_t$，则概率为 $1/(e^\epsilon + 1)$。这种设计确保了 $\epsilon$-LDP 保证。
*   **无偏梯度估计器**：分析器接收到公开特征 $X_t$ 和加密后的子集 $S_t$，构造一个梯度的无偏估计 $\hat{g}_t$。该估计器通过对子集内外的梯度进行加权补偿，抵消随机化带来的偏差。
*   **投影随机梯度下降（SGD）**：利用上述无偏估计器，在参数空间 $W$ 内进行标准的投影 SGD 迭代。
*   **理论下界证明**：通过将问题归约为“具有 $\ell_2$ 误差的离散分布估计”问题，利用信息论工具证明了任何 L-LDP 算法都无法突破 $O(\sqrt{K})$ 的风险界限。

### 3. 实验设计
*   **性质说明**：这是一篇**纯理论性质**的论文，重点在于数学证明和算法复杂度分析。
*   **对比基准（Benchmark）**：
    *   **Ghazi et al. (2021)**：先前的 SOTA 结果，其风险界在不同隐私机制下分别为 $O(K/\epsilon\sqrt{n})$ 和 $O(K/e^\epsilon\sqrt{n})$。
    *   **非隐私版本**：作为性能上限，即 $O(1/\sqrt{n})$。
*   **对比方法**：论文主要对比了基于“随机响应（Randomized Response）”的传统方法和基于“高斯噪声”的近似 DP 方法。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何 GPU、CPU 型号或训练时长。
*   **原因**：由于该研究通过数学推导得出结论，不涉及大规模数值模拟或深度学习模型训练，因此不需要传统意义上的计算资源说明。

### 5. 实验数量与充分性
*   **理论覆盖面**：论文分析了三种隐私机制：高隐私（$\epsilon \le 1$）、中等隐私（$1 \le \epsilon \le \ln K$）和低隐私（$\epsilon \ge \ln K$）。
*   **充分性评价**：从理论角度看，实验（证明）是非常充分的。作者不仅给出了上界算法，还给出了匹配的下界证明，涵盖了所有可能的隐私参数范围。这种“上下界匹配”在理论计算机科学中被视为对一个问题最完备的解答。

### 6. 论文的主要结论与发现
*   **二次方改进**：将超额风险对标签空间大小 $K$ 的依赖从 $O(K)$ 降低到了 $O(\sqrt{K})$。
*   **紧致界限（Tight Bounds）**：证明了在 $n$ 足够大时，该算法达到的风险界是信息论意义上的最优解（Minimax Optimal）。
*   **隐私代价的本质**：揭示了在本地模型下，标签隐私的代价确实随 $\sqrt{K}$ 缩放，这与中央模型（Central Model）下仅对 $K$ 有对数依赖形成鲜明对比。

### 7. 优点
*   **理论突破**：解决了 L-LDP 领域的一个开放性问题，将风险界限提升到了最优。
*   **非交互性**：算法是单轮通信的（Non-interactive），每个用户只需发送一次数据，非常适合分布式和联邦学习场景。
*   **简洁高效**：提出的子集选择机制易于实现，且梯度估计器的方差计算比同类方法更简单。

### 8. 不足与局限
*   **缺乏实证验证**：虽然理论完备，但没有在真实数据集（如大规模分类任务）上验证 $O(\sqrt{K})$ 改进在实际训练中的具体表现。
*   **样本量限制**：下界证明要求样本量 $n$ 必须足够大（需满足关于 $K$ 和 $\epsilon$ 的特定函数关系），在极小样本场景下的界限可能仍有细微偏差。
*   **特征公开假设**：该模型假设特征 $X$ 是完全公开的，如果特征也需要隐私保护，该算法将不再适用。

（完）
