---
title: "GAPSL: A Gradient-Aligned Parallel Split Learning on Heterogeneous Data"
title_zh: GAPSL：一种面向异构数据的梯度对齐并行拆分学习框架
authors: "Zheng Lin, Ons Aouedi, Wei Ni, Symeon Chatzinotas, Xianhao Chen"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18540v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 异构数据上的并行拆分学习
tldr: 针对资源受限设备在联邦学习中的计算压力，本文提出GAPSL框架。该框架基于并行拆分学习（PSL），通过引入领导梯度识别（LGI）和梯度方向对齐（GDA）机制，解决了PSL在异构数据下因梯度方向不一致导致的训练发散问题。实验证明，GAPSL在提升模型收敛速度和准确率的同时，显著降低了计算与通信开销。
selection_source: fresh_fetch
motivation: 传统的并行拆分学习在处理异构数据时，由于缺乏聚合机制，容易因客户端间梯度方向不一致而导致训练难以收敛。
method: 提出GAPSL框架，通过动态识别代表全局趋势的领导梯度，并利用方向感知正则化引导各客户端梯度与其对齐。
result: 在原型计算测试平台上的实验表明，GAPSL在训练准确率和延迟方面均优于现有的最先进基准方法。
conclusion: GAPSL通过梯度对齐有效缓解了并行拆分学习中的发散问题，是实现高效、低延迟分布式学习的有效方案。
---

## 摘要
神经网络日益增长的复杂性为在资源受限的客户端设备上普及联邦学习（FL）带来了重大挑战。并行拆分学习（PSL）已成为一种极具前景的解决方案，它通过模型分区将大量计算负载卸载到服务器，从而减轻了客户端的计算负担，并消除了客户端模型聚合，降低了通信和部署成本。由于 PSL 无需聚合，它面临着由客户端间梯度方向不一致导致的严重训练发散问题。为了应对这一挑战，我们提出了 GAPSL，这是一个梯度对齐的 PSL 框架，包含两个关键组件：领导者梯度识别（LGI）和梯度方向对齐（GDA）。LGI 动态选择一组方向一致的客户端梯度，以构建捕捉全局收敛趋势的领导者梯度。GDA 采用方向感知正则化，将每个客户端的梯度与领导者梯度对齐，从而缓解设备间的梯度方向不一致并增强模型收敛性。我们在原型计算测试平台上评估了 GAPSL。广泛的实验表明，GAPSL 在训练准确率和延迟方面始终优于最先进的基准方法。

## Abstract
The increasing complexity of neural networks poses significant challenges for democratizing FL on resource?constrained client devices. Parallel split learning (PSL) has emerged as a promising solution by offloading substantial computing workload to a server via model partitioning, shrinking client-side computing load, and eliminating the client-side model aggregation for reduced communication and deployment costs. Since PSL is aggregation-free, it suffers from severe training divergence stemming from gradient directional inconsistency across clients. To address this challenge, we propose GAPSL, a gradient-aligned PSL framework that comprises two key components: leader gradient identification (LGI) and gradient direction alignment (GDA). LGI dynamically selects a set of directionally consistent client gradients to construct a leader gradient that captures the global convergence trend. GDA employs a direction-aware regularization to align each client's gradient with the leader gradient, thereby mitigating inter-device gradient directional inconsistency and enhancing model convergence. We evaluate GAPSL on a prototype computing testbed. Extensive experiments demonstrate that GAPSL consistently outperforms state-of-the-art benchmarks in training accuracy and latency.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GAPSL**（梯度对齐并行拆分学习）的新型分布式学习框架，旨在解决并行拆分学习（PSL）在处理异构数据（非独立同分布，Non-IID）时面临的训练发散问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：随着模型规模增大，资源受限的物联网（IoT）设备难以运行完整的联邦学习（FL）。拆分学习（SL）通过模型分区将计算压力转移到服务器。
*   **核心问题**：并行拆分学习（PSL）虽然比传统的拆分联邦学习（SFL）更高效（因为它消除了客户端之间的模型聚合，降低了通信开销），但由于**缺乏聚合校准机制**，在 Non-IID 数据下，不同客户端产生的梯度方向严重不一致（Gradient Directional Inconsistency），导致模型难以收敛或准确率大幅下降。
*   **整体含义**：论文旨在设计一种无需客户端模型聚合、但能通过服务器端梯度协调实现稳定收敛的高效 PSL 框架。

### 2. 方法论
GAPSL 框架由两个核心组件组成，均在服务器端运行：
*   **领导者梯度识别 (LGI)**：
    *   **梯度一致性评分**：计算每个客户端梯度与其他梯度的平均夹角偏差，评分越低表示该梯度越符合“大众趋势”。
    *   **自适应梯度选择**：根据训练阶段（时间）和梯度分散程度（标准差），动态调整选择比例 $K\%$。在训练初期或梯度差异大时更严格，后期逐渐放宽。
    *   **构建领导梯度**：聚合选定的高一致性梯度，生成一个代表全局收敛方向的“领导梯度”（Leader Gradient）。
*   **梯度方向对齐 (GDA)**：
    *   **自适应角度阈值**：基于当前梯度分布的统计特性（均值和标准差），设定动态阈值，过滤掉方向过于偏离的异常梯度。
    *   **方向感知正则化**：在局部损失函数中引入基于余弦相似度的正则化项 $\lambda(1 - \cos \theta)$。该项会惩罚与领导梯度方向偏差较大的更新，强制客户端梯度向全局趋势靠拢。

### 3. 实验设计
*   **数据集**：CIFAR-10 和 CIFAR-100。
*   **场景设置**：包含 IID（独立同分布）和 Non-IID（基于 Dirichlet 分布，$\alpha$ 取值 0.1 到 0.9）。
*   **模型架构**：卷积神经网络 VGG-16 和 Transformer 架构 ViT-Base。
*   **对比方法 (Benchmarks)**：
    1.  **Vanilla SL**：原始串行拆分学习。
    2.  **SFL**：带客户端模型聚合的拆分联邦学习。
    3.  **PSL**：标准的并行拆分学习（无聚合）。
    4.  **EPSL**：一种通过部分梯度聚合优化通信的 PSL 变体。

### 4. 资源与算力
*   **硬件平台**：构建了一个真实的物理原型测试床。
    *   **服务器**：NVIDIA Jetson Orin NX（1024核 Ampere GPU，32个 Tensor Cores）。
    *   **客户端**：10个 NVIDIA Jetson Nano（128核 Maxwell GPU）。
*   **软件环境**：Python 3.7, PyTorch 1.9.1，使用 HTTP/2 协议和 Protocol Buffers 进行通信。
*   **训练细节**：文中未明确给出总训练时长（以小时计），但记录了收敛所需的 Epoch 数量（如 VGG-16 在 Non-IID 下约 32-56 Epoch）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了不同数据集、不同模型架构、不同数据异构程度（$\alpha$ 值变化）、不同设备规模（10到40个设备）的对比实验。
*   **消融实验**：专门针对 LGI 和 GDA 模块进行了消融分析，验证了每个组件的必要性。
*   **客观性**：实验在真实嵌入式硬件（Jetson 系列）上运行而非纯模拟，考虑了计算和通信的真实约束，结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **性能优越性**：在 Non-IID 场景下，GAPSL 的准确率显著高于 PSL（提升约 13.8%-16%），甚至优于需要模型聚合的 SFL（提升约 2.6%-3.6%）。
*   **收敛速度**：GAPSL 达到收敛所需的 Epoch 数量最少，证明了梯度对齐能有效加速优化过程。
*   **可扩展性**：随着客户端数量增加，GAPSL 的性能下降幅度远小于其他基准方法，表现出极强的鲁棒性。

### 7. 优点与亮点
*   **创新性**：首次在无需客户端模型聚合的 PSL 框架中引入梯度方向对齐机制。
*   **低开销**：所有对齐逻辑均在服务器端完成，不增加客户端计算负担，且通信量远低于 SFL（无需传输模型参数）。
*   **自适应性**：通过动态调整选择比例和角度阈值，能够自动适应训练过程中的动态变化和不同程度的数据异构。

### 8. 不足与局限
*   **计算近似**：为了保持轻量化，正则化项采用了**一阶近似**（将梯度视为常量），虽然降低了开销，但可能在某些极端非线性场景下损失一定的对齐精度。
*   **任务覆盖**：实验主要集中在图像分类任务，尚未在自然语言处理（NLP）的大规模生成任务或强化学习等其他领域进行验证。
*   **超参数敏感性**：虽然有自适应机制，但正则化系数 $\lambda$ 和初始比例 $K_{min}/K_{max}$ 仍可能需要针对不同模型进行微调。

（完）
