---
title: "UMEDA: Unified Multi-modal Efficient Data Fusion for Privacy-Preserving Graph Federated Learning via Spectral-Gated Attention and Diffusion-Based Operator Alignment"
title_zh: UMEDA：通过谱门控注意力和基于扩散的算子对齐实现隐私保护图联邦学习的统一多模态高效数据融合
authors: "Shih-Yu Lai, Hirozumi Yamaguchi, Shang-Tse Chen, Yu-Lun Liu, Bing-Yu Chen"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08288v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 隐私保护图联邦学习框架
tldr: 针对设备无关定位中多模态传感器异构、数据漂移及隐私保护导致信号受损的问题，本文提出UMEDA框架。该框架将联邦学习聚合重构为谱信号处理，利用谱门控注意力机制对齐不同模态的低秩子空间，并通过扩散模型实现算子对齐，以处理异构图结构。同时，引入各向异性差分隐私机制，在保护隐私的同时保留核心信号。实验证明，UMEDA在多模态异构和严格隐私预算下，其准确率、收敛性和通信效率均优于现有方法。
selection_source: fresh_fetch
motivation: 解决联邦学习在处理异构传感器模态、数据分布漂移以及隐私噪声破坏定位信号时的脆弱性问题。
method: 提出一种结合谱门控注意力、扩散模型算子对齐以及各向异性差分隐私的图联邦学习框架。
result: 在MM-Fi和RELI11D基准测试中，UMEDA在准确率、收敛速度和通信效率方面均显著优于现有最先进的联邦学习基准。
conclusion: UMEDA通过谱域对齐和扩散模型有效解决了多模态联邦学习中的异构性与隐私保护之间的平衡难题。
---

## 摘要
无设备定位通过分布在边缘设备上的异构无线和视觉传感器（如 Wi-Fi、LiDAR）来训练模型。联邦学习提供了一个尊重隐私的框架，但当客户端在传感器模态和分辨率上存在差异、数据分布发生偏移，以及隐私噪声破坏了定位所需的结构信号时，该框架会变得脆弱。我们提出了 UMEDA，这是一个图联邦学习框架，其中客户端构成全局图的节点并共享一个连续积分算子，聚合过程被重新表述为该算子上的谱信号处理。每个客户端使用线性注意力层对其本地传感器进行编码，该层的核谱经过低秩过滤，从而抑制特定模态的残差，使具有不同传感器的客户端在共同的低秩子空间中对齐。随后，服务器通过核谱系数上的扩散模型聚合客户端更新，将更新视为共享算子的离散化，而非受拓扑限制的权重——这在无需节点级对应的情况下，吸收了变化的图规模和缺失的模态。为了平衡隐私和效用，我们添加了一种各向异性差分隐私机制，该机制优先将噪声投影到信号子空间的零空间中，在梯度裁剪下确保形式化 (ε, δ)-DP 的同时，保留了主特征方向。在 MM-Fi 和 RELI11D 分布外基准测试中，UMEDA 在准确性、收敛性和通信效率方面均优于最先进的联邦基准模型，特别是在高模态异构性和严格隐私预算的情况下。

## Abstract
Device-free localization trains models from heterogeneous wireless and visual sensors (e.g., Wi-Fi, LiDAR) distributed across edge devices. Federated learning offers a privacy-respecting framework, but is brittle when clients differ in sensor modality and resolution, when their data distributions drift, and when privacy noise destroys the structural signal needed for localization. We propose UMEDA, a graph federated learning framework in which clients form nodes of a global graph that share a continuous integral operator, and aggregation is reformulated as spectral signal processing on this operator. Each client encodes its local sensors with a linear-attention layer whose kernel spectrum is low-rank filtered, suppressing modality-specific residuals so clients with different sensors align in a common low-rank subspace. The server then aggregates client updates via a diffusion model over the kernel's spectral coefficients, treating updates as discretizations of a shared operator rather than topology-bound weights -- this absorbs varying graph sizes and missing modalities without node-wise correspondence. To balance privacy and utility, we add an anisotropic differential-privacy mechanism that projects noise preferentially into the null space of the signal subspace, preserving dominant eigendirections while ensuring formal $(ε, δ)$-DP under gradient clipping. On MM-Fi and the RELI11D out-of-distribution benchmark, UMEDA outperforms state-of-the-art federated baselines in accuracy, convergence, and communication efficiency, particularly under high modality heterogeneity and tight privacy budgets.

---

## 论文详细总结（自动生成）

### 论文总结：UMEDA 隐私保护图联邦学习框架

#### 1. 核心问题与研究动机
论文关注的是**设备无关定位（Device-free Localization, DFL）**在联邦学习（FL）环境下的应用。DFL 依赖于分布在边缘设备上的异构传感器（如 Wi-Fi CSI、LiDAR、毫米波雷达）。
*   **研究动机**：传统的联邦学习在处理此类任务时面临三大挑战：
    1.  **模态与分辨率异构性**：不同设备传感器类型不同（Wi-Fi vs LiDAR），数据张量形状不一，标准 FL 难以直接聚合权重。
    2.  **数据分布漂移（Non-IID）**：环境、人体尺寸和传感器位置的变化导致严重的分布偏移。
    3.  **隐私与效用的矛盾**：标准差分隐私（DP）注入的各向同性噪声会破坏定位所需的微弱结构信号，导致精度大幅下降。

#### 2. 方法论
UMEDA 提出将联邦聚合从“权重空间”转向“**算子空间（Operator Space）**”，核心组件包括：
*   **谱门控线性 Transformer (SGLT)**：在本地编码阶段，对线性注意力的核矩阵进行奇异值分解（SVD），通过一个可微的谱门控（Spectral Gate）保留低秩的语义特征（如人体姿态），抑制高频的模态特定噪声。
*   **基于扩散的图神经营算子 (Diff-GNO)**：服务器端聚合器。它将客户端更新视为连续积分算子的离散化，利用扩散模型（VE-SDE）在算子的谱系数上进行生成式去噪聚合。这使得框架能处理不同规模的图和缺失模态，无需节点级对应。
*   **子空间投影差分隐私 (SP-DP)**：一种各向异性 DP 机制。它利用全局基准将噪声优先投影到信号子空间的零空间（Null Space）中，在满足 $( \epsilon, \delta )$-DP 形式化保证的同时，保护了携带定位信息的关键特征方向。

#### 3. 实验设计
*   **数据集**：
    *   **MM-Fi**：大型多模态人体感知基准（Wi-Fi, LiDAR, 毫米波雷达等）。
    *   **RELI11D**：用于分布外（OOD）泛化测试，包含 RGB、事件相机、IMU 等完全不同的模态。
*   **任务**：3D 姿态估计（MPJPE）、动作识别（Top-1 Acc）、定位（RMSE）。
*   **对比方法**：
    *   **基础 FL**：FedAvg, FedProx, SCAFFOLD, FedNova, FedAdam, FedDyn, MOON。
    *   **个性化/规范化 FL**：FedBN, FedLC, FedRep。
    *   **图联邦学习**：FedGraphNN, GCFL+。
    *   **隐私保护 FL**：DP-FedAvg, AGC-DP, TA-DPFL。
    *   **先进架构**：Performer, xLSTM, Gated DeltaNet, X-Fi。

#### 4. 资源与算力
*   **训练配置**：模拟 100 个客户端，每轮采样率 0.4，共 1000 轮通信。本地训练使用 AdamW 优化器，E=100 个本地步长，Batch Size 为 64。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号和总训练时长。但在附录 A.14 中提供了客户端侧的延迟（ms）和峰值内存（MB）分析，显示其 SGLT 架构在边缘设备上具有可行性。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括主任务性能对比、跨数据集（MM-Fi 到 RELI11D）的零射转移实验、通信效率分析。
*   **消融实验**：针对 SGLT、Diff-GNO、SP-DP 三个核心组件分别做了消融，并研究了保留秩 $r$、门控阈值、噪声分配比例 $\kappa$ 等超参数的影响。
*   **鲁棒性测试**：涵盖了 Non-IID 程度（Dirichlet $\alpha$）、模态缺失比例、图规模偏移等压力测试。
*   **安全性评估**：通过梯度反向攻击（Gradient Inversion Attack）可视化展示了 SP-DP 的防御效果。
*   **评价**：实验设计非常全面、客观，对比基准涵盖了近几年的顶级会议成果，充分验证了方案的有效性。

#### 6. 主要结论与发现
*   **性能领先**：UMEDA 在所有任务上均显著优于现有 SOTA 联邦学习方法，尤其在强隐私预算（小 $\epsilon$）下优势更明显。
*   **跨模态对齐**：通过算子空间聚合，模型展现了极强的跨模态泛化能力，在 MM-Fi 训练后能直接在 RELI11D 的新模态上取得较好效果。
*   **隐私效用平衡**：SP-DP 成功在不牺牲形式化隐私保证的前提下，通过各向异性噪声注入保留了 80% 以上的定位精度。

#### 7. 优点
*   **理论创新**：将联邦聚合从参数平均提升到连续算子的谱对齐，解决了异构图结构的聚合难题。
*   **隐私设计巧妙**：利用信号子空间投影解决了 DP 噪声对几何结构破坏的痛点。
*   **通用性强**：不依赖于特定的传感器组合，能够处理动态变化的客户端模态。

#### 8. 不足与局限
*   **计算开销**：本地 SVD 分解和服务器端扩散模型的训练/采样增加了额外的计算负担。
*   **低秩假设**：方法依赖于“多模态共享语义存在于低秩子空间”的假设，若任务涉及极高维且分散的特征，效果可能受限。
*   **超参数敏感性**：虽然做了消融，但扩散模型的噪声计划（Noise Schedule）和谱门控参数在不同场景下可能需要精细调优。

（完）
