Title: Adaptive Selection of LoRA Components in Privacy-Preserving Federated Learning

URL Source: https://arxiv.org/pdf/2605.05769v1

Published Time: Fri, 08 May 2026 00:39:12 GMT

Number of Pages: 32

Markdown Content:
# Adaptive Selection of LoRA Components in Privacy-Preserving Federated Learning 

Myoungjun Kim 

Myongji University 

tjmjtjmj2237@mju.ac.kr 

Sangwoo Park 

King’s College London 

sangwoo.park@kcl.ac.uk 

Yoseob Han 

Soongsil University 

yoseob.han@ssu.ac.kr 

Jin-Hyun Ahn 

Myongji University 

wlsgus3396@mju.ac.kr 

## Abstract 

Differentially private federated fine-tuning of large models with LoRA suffers from aggregation error caused by LoRA’s multiplicative structure, which is further amplified by DP noise and degrades both stability and accuracy. Existing remedies apply a single update mode uniformly across all layers and all communication rounds (or alternate them on a fixed schedule), ignoring both the structural asym-metry between the two LoRA factors and the round-wise dynamics of training. We propose AS-LoRA, an adaptive framework defined by three axes (i) layer-wise freedom, in which each layer independently selects its active component, (ii) 

round-wise adaptivity, in which the selection updates over communication rounds, and (iii) a curvature-aware score derived from a second-order approximation of the loss. Theoretically, AS-LoRA eliminates the reconstruction-error floor of layer-tied schedules, accelerates convergence, implicitly biases solutions toward flatter minima, and incurs no additional privacy cost. Across GLUE, SQuAD, CIFAR-100, and Tiny-ImageNet under strict DP budgets and non-IID partitions, AS-LoRA improves over the federated LoRA baselines by up to +7 .5 pp on GLUE and +12 .5 pp on MNLI-mm for example, while matching or exceeding SVD-based aggregation methods at 33 –180 × lower aggregation cost and with negligible communication overhead. Code for the proposed method is available at 

https://anonymous.4open.science/r/as_lora-F75F/ .

## 1 Introduction 

Federated learning (FL) enables collaborative training over decentralized data without exposing raw samples [ 1, 2 ], yet the communicated updates remain vulnerable to attacks such as Deep Leakage from Gradients [ 3] and Membership Inference [ 4 ]. Differential privacy (DP) has thus become a standard safeguard in this setting [ 5 , 6]. Meanwhile, as foundation models such as GPT-4 [ 7], LLaMA [ 8], PaLM2 [ 9 ], ViT [ 10 ], and Swin-T [ 11 ] continue to scale, fine-tuning them on resource-constrained clients quickly becomes prohibitive. Low-Rank Adaptation (LoRA) [ 12 ], which adapts a frozen weight W0 via a small low-rank update W = W0 + BA , has therefore become the predominant approach for Parameter-Efficient Fine-Tuning (PEFT) in FL with foundation models, incurring minimal performance degradation compared to full fine-tuning. However, applying LoRA in private FL is non-trivial. Its multiplicative structure induces an aggrega-tion error across clients, which is further amplified into a quadratic noise term under DP-SGD, and we defer the detailed mechanism to Section 2. FedLoRA [ 13 ], which is the most direct adaptation of LoRA to FL, suffers from both effects and degrades sharply under tight privacy budgets and non-IID 

> Preprint.
> arXiv:2605.05769v1 [cs.LG] 7 May 2026

Figure 1: Comparison of update patterns in FedLoRA, FFA-LoRA, RoLoRA, and the proposed Adaptive Selection LoRA (AS-LoRA). partitions. Several remedies have since been proposed to alleviate those issues. FFA-LoRA [14 ]freezes A and trains only B, RoLoRA [ 15 ] alternates updates between A and B across rounds, and 

FedSVD [ 16 ] also trains only B while applying a server-side Singular Value Decomposition (SVD). Yet all of them adopt structurally simple update strategies that impose the same mode on every layer 

and follow temporally static patterns , which overlook both the structural asymmetry between A and 

B, and the round-wise variation of their relative importance. This motivates an adaptive framework in which the active LoRA component is selected per layer and per round, as depicted in Figure 1. Table 1: Performance comparison of fixed al-ternating LoRA optimization schedules on 3 GLUE tasks. Best results are shown in bold 

and second-best results are underlined.                      

> Schedule MNLI (m) MNLI (mm) SST-2 QQP
> BA [15] 81.17 81.96 92.09 84.45
> BAA 80.26 81.20 92.89 84.21
> BBA 81.10 81.98 92.30 84.45
> BBAA 80.78 81.55 92.66 84.49

In fact, prior work [ 17 ] shows that the two low-rank matrices contribute asymmetrically to the optimiza-tion process across training stages, owing to their different initialization schemes and structural char-acteristics. To examine whether this asymmetry can be handled by simple fixed schedules, we conduct preliminary experiments that compare several al-ternating patterns including BA , BBA , and BAA , as shown in Table 1. No single schedule attains the best performance across all tasks, which indicates that fixed and heuristic alternating schedules are in-herently suboptimal, and that the choice of which low-rank component to optimize should instead be made adaptively according to current training dynamics. Consistent with this observation, the active LoRA component selected by our proposed AS-LoRA varies dynamically across both layers and communication rounds throughout training, as illustrated in Figure 2, which presents a representative trajectory whose full details are deferred to Appendix J. Building on those observations, we propose AS-LoRA (Adaptive Selection LoRA), and the main contributions of this paper are summarized as follows. 

Figure 2: Selection of LoRA components with AS-LoRA (ours) on QNLI across lay-ers/rounds. Different layers favor different components, and the preferred component varies across rounds, supporting the need for adaptive selection. 

Layer-wise adaptive LoRA optimization. To the best of our knowledge, this is the first work that extends alternating LoRA optimization to a adap-tive selection framework. We further prove that this layer-wise freedom eliminates the irreducible reconstruction-error floor that any layer-tied schedule must incur. 

Round-wise adaptive mode selection. We intro-duce a temporally adaptive mechanism that updates the selected LoRA component across communication rounds based on EMA-smoothed historical scores, which allows the model to adjust its optimization strategy as training progresses. 

Curvature-aware scoring function. We design a scoring function derived from a second-order approx-imation of the loss, which captures curvature-aware component importance. We prove that this score-driven selection accelerates convergence with a quantifiable gain over fixed schedules and implicitly biases solutions toward flatter minima, which we further verify empirically through loss-landscape and sharpness analyses. To handle the channel-wise gradient outliers that are characteristic of LoRA [ 18 ], we further apply a Gaussian random projection 2[19], which yields more reliable score estimates. 

Privacy at no extra cost. We prove that the data-dependent component selection of AS-LoRA incurs no additional privacy cost beyond standard DP-SGD, which follows from the post-processing invariance of DP. 

## 2 Background 

Differential privacy [ 20 ]. Formally, a randomized mechanism M is said to satisfy (ϵ, δ )-differential privacy if, for any two neighboring datasets D and D′ that differ in exactly one data record, and for any measurable subset E of the output space, the following condition holds: Pr[ M(D) ∈ E] ≤

eϵ Pr[ M(D′) ∈ E] + δ. A common approach to achieving differential privacy in federated learning is differentially private stochastic gradient descent (DP-SGD) [ 21 ]. Formally, let gi = ∇θ ℓ(θ; xi)

denote the gradient computed from a single training example xi. DP-SGD first applies per-sample gradient clipping. And the clipped gradients are then aggregated and perturbed with Gaussian noise: 

(i) gclip  

> i

= gi · min 



1, C

> ∥gi∥2



, (ii) gnoisy = 1

> B

PBi=1 gclip  

> i

+ N  0, σ 2C2I

, where C is a predefined clipping norm that bounds the sensitivity, B is the batch size, σ is the noise multiplier, and N (0 , σ 2C2I) denotes isotropic Gaussian noise. Finally, the model parameters are updated using the noisy gradient: θt+1 = θt − η ¯g, where η is the learning rate. 

Aggregation error in federated LoRA [ 22 ]. Given a pre-trained weight matrix W0 ∈ Rdout ×din ,LoRA parameterizes the adapted weight as W = W0 + BA , where B ∈ Rdout ×r and A ∈ Rr×din .In FL, the server aggregates local LoRAs {(Ak, B k)}Kk=1 via averaging as ¯A = 1

> K

PKk=1 Ak, ¯B =

> 1
> K

PKk=1 Bk. However, due to the multiplicative structure of LoRA, 1

> K

PKk=1 BkAk̸ = ¯B ¯A. This mismatch leads to an aggregation error that accumulates across communication rounds, and becomes more pronounced under heterogeneous client data distributions. 

Figure 3: Scaling behavior of differ-ent noise components with respect to the noise scale σ.

DP noise amplification in LoRA [ 14 ]. When DP-SGD is ap-plied to LoRA parameters, Gaussian noise is added to the gradi-ents. Let ∆B and ∆A denote the updates to B and A in a given round, which consist of a true update component and a DP noise component: ∆B = ∆ B⋆ + NB , ∆A = ∆ A⋆ + NA.

The resulting update to the effective weight matrix is given by ∆W = B∆A⋆ + ∆ B⋆A + BNA + NB A + NB NA. As illustrated in Figure 3, different noise components exhibit dis-tinct scaling behaviors with respect to the noise scale σ. The full-model noise ∥N W ∥F grows linearly, whereas the multi-plicative cross-term ∥N B NA∥F exhibits quadratic growth and becomes dominant as σ increases. This phenomenon leads to 

noise amplification in the effective model update, which can significantly degrade convergence and stability. Additional related works are introduced in Appendix A. 

## 3 Method 

In this section, we introduce our AS-LoRA framework. We first describe the problem formulation of FL with LoRA, and then present the score computation algorithm S(·) and the mode selection algorithm M(·) that together determine which LoRA component is activated at each layer and communication round. The full algorithmic procedure and visual illustration of AS-LoRA are deferred to Appendix B. 

Problem formulation. We consider a FL setting with K clients and a central server. Each client holds a local dataset that is not shared with others, and the goal is to collaboratively fine-tune a large pre-trained model while preserving data privacy. Following Section 2, the adapted weight matrix is parameterized as W = W0 + BA , where A and B denote the two low-rank components. Since LoRA is applied to multiple layers of the model, we further decompose the update into layer-wise adapters. Specifically, for the n-th layer at round t, the adapted weight can be written as 

Wt(n) = W0(n) + Bt(n)At(n), n ∈ [N ] , where N denotes the total number of LoRA layers. 

Score computation algorithm. To determine which LoRA component should be activated at each communication round, we design a score function based on a second-order approximation of the 3expected loss decrease after a gradient update. Let a ∈ { A, B } denote a candidate LoRA component. For a given layer n, we consider one gradient descent step at round t, W ′ = Wt − η∇a(n)L(Wt),

whose resulting change in the loss admits the second-order Taylor approximation L(W ′) ≈ L(Wt) −

η∥∇ a(n)L(Wt)∥2 + η2 

> 2

∇a(n)L(Wt)⊤Ha(n)(Wt)∇a(n)L(Wt), where Ha(n)(Wt) is the Hessian with respect to the parameter block a(n). This motivates the following score function: 

Sa(n)(Wt) = ∥∇ a(n)L(Wt)∥2 − η

2 ∇a(n)L(Wt)⊤Ha(n)(Wt) ∇a(n)L(Wt), (1) where we omit the client index k throughout this derivation for notational clarity. The two terms in 

Sa(n) admit complementary interpretations from two well-established design principles. The first term extends the classical Gauss–Southwell rule of block coordinate descent (BCD) [ 23 ], which selects the block with the largest gradient magnitude and yields strictly faster convergence than uniform block selection. The second term introduces a curvature-induced penalty whose effect mirrors the implicit bias of sharpness-aware minimization (SAM) [ 24 ], which discourages updates along directions in which a gradient step would overshoot a sharp region of the loss surface. Selecting the block that maximizes Sa(n) therefore corresponds to choosing the LoRA component that offers the largest expected one-step loss decrease while preferring flatter regions of the loss landscape. We formally establish these two design benefits in Theorem 3 and Theorem 4. Now, we introduce the Gaussian random projection [ 19 ], which is applied to the computed score (1) . Recent studies have shown that large language models often exhibit channel-wise activation outliers [ 25 , 26 ]. [ 18 ] further shows that the gradient magnitude of the A component is highly concentrated in a small subset of channels, and the squared-gradient term ∥∇ a(n)L(Wt)∥2 in (1) )can lead to an overestimation of the score of the A component. To alleviate this, we apply the random projection step as follow. For each LoRA component, we construct a random projection matrix in the original gradient space that preserves the original dimensionality while redistributing channel-wise outliers across coordinates, denoted as RA(n), R B (n). For client k at round t, let 

gta,k (n) = ∇a(n)Ltk, denote the locally computed gradients with respect to the LoRA components of layer n. We project these gradients as egtA,k (n) = gtA,k (n)RA(n), egtB,k (n) = RB (n)gtB,k (n), so that the projected gradients redistribute channel-wise outliers across dimensions. The effectiveness of random projection is empirically validated in Appendix J.1. In additoin, we remark that our score computation is single-pass , i.e., the scores are obtained as a by-product of local training phase. As an alternative, one could consider a two-pass inner-loop computation that re-evaluates the score after the globally aggregated model is redistributed to the clients. We empirically compare the two designs in terms of performance, and overhead (Appendix J.2). 

Mode selection algorithm. We now design a mode selection algorithm, denoted by M(·) which determines the active LoRA component for each layer at round t using the layer-wise scores calculated from stored scores and aggregate scores in an Exponential Moving Average (EMA) manner. The algorithm outputs the mode vector indicating which component should be optimized. We denote the mode vector at round t as {M t(1) , M t(2) , . . . , M t(N )}, M t(n) ∈ { 0, 1}, where M t(n) = 0 

indicates that At(n) is optimized and M t(n) = 1 indicates that Bt(n) is optimized during round 

t. Accordingly, we define the layer-wise scores as ˆSta(n) := EMA 

 ˆSt−1 

> a

(n), S ta(n)



, where 

Sta(n) := 1

> K

PKk=1 Sta,k (n). Instead of deterministically selecting the component with the larger score, we use a temperature-scaled softmax strategy. This approach encourages exploration during training and is consistent with the common practice of temperature annealing [ 27 , 28 ]. The probability of selecting a at round t is defined as P t(a, n ) = exp ( ˆSta(n)/T t)

> exp

( ˆStA(n)/T t)+exp ( ˆStB (n)/T t) , where T t is the temperature parameter at round t. The temperature follows an annealing schedule 

T t =

T 0, t ≤ tw,

max  Tmin , T 0 · γ(t−tw ) , t > t w, (2) where T 0 is the initial temperature, Tmin is the minimum temperature, γ ∈ (0 , 1) is the decay factor, and tw denotes the warm-up period. During the warm-up stage ( t ≤ tw), the two LoRA components are alternately activated across rounds. The stochastic selection mechanism prevents premature convergence to a single component while allowing the framework to gradually favor the component with consistently higher scores as the temperature decreases. We remark that our mode selection operates on scores aggregated by uniform averaging across clients, which gives equal weight to every 4client and and can be regarded as the standard choice. In Appendix J.3, we compare this design against two alternative aggregation rules, namely majority voting and weighted averaging .

## 4 Theoretical Analysis 

We analyze AS-LoRA along two axes: (i) layer-wise freedom which is the ability to activate A or 

B independently per layer and (ii) score-driven selection which is arg max a Sa with Sa = ∥ga∥2 − 

> η
> 2

g⊤ 

> a

Haga. Each axis contributes a distinct and quantifiable advantage over the layer-tied or static baselines including FFA-LoRA [ 14 ], and RoLoRA [ 15 ]. To focus our goals, we do not consider the effect of random projection, EMA, temperature-scaled softmax strategy, and the variance of training and scoring caused by distributed settings in our analysis. We first prove that AS-LoRA requires no extra privacy cost. In fact, AS-LoRA’s selection at each layer depends on ∥g(n) 

> a

∥2 and 

(g(n) 

> a

)⊤H(n) 

> a

g(n) 

> a

, both of which are gradient-dependent statistics that in principle could be privacy-relevant. The following theorem resolves the concern based on the post-processing invariance of DP [20]. The proof of Theorem 1 can be found in Appendix D. 

Theorem 1 ((ϵ, δ )-DP at no privacy cost) . With per-round DP-SGD [ 21 ] (subsampled Gaussian, sensitivity C, noise scale σ, sampling rate q), AS-LoRA satisfies (ϵ, δ )-DP with ϵ ≤ q2C2Tσ2 log(1 /δ ),

the same bound with FFA-LoRA/RoLoRA via moments accountant / Rényi-DP composition [ 29 , 30 ]. 

Now, we show that any schedule which ties the mode across all layers must incur an irreducible loss floor, whereas AS-LoRA provably eliminates it. For the analysis, we first introduce layer-wise population-optimal LoRA pair that minimizes the population fine-tuning loss fn as 

 A⋆(n), B ⋆(n) := arg min 

> A∈Rr×d, B ∈Rd×r

EXn

ℓ f (Xn) . (3) Next, we define the subspace misalignment δt(n) as 

δt(n) := B⋆(n) A⋆(n) ( I − PAt(n)) F = ∆W ⋆(n)( I − PAt(n)) F ∈ [0 , ∥∆W ⋆(n)∥F ], (4) where PAt(n) := At(n)†At(n) ∈ Rd×d denote the orthogonal projector onto row( At(n)) , A†

denotes the Moore-Penrose pseudoinverse [ 31 ]. It quantifies the part of the optimal update B⋆A⋆

that lies outside row( At(n)) . The intermediate values with r = 1 was used by [ 15 ] to diagnose the fundamental obstruction of FFA-LoRA, where At(n) ≡ A0(n) so δt(n) persists forever. With 

Xn ∈ Rd×m stacking the layer-n inputs, we define per-layer reconstruction risk as 

Rrec (W) := 1

N

> N

X

> n=1

B⋆(n)A⋆(n)Xn − B(n)A(n)Xn 

> 2
> F

, (5) 

i.e. the average layer-wise squared Frobenius distance between each layer’s effective update and its optimal LoRA pair defined in (3) . While the reconstruction risk with one layer was also used in [ 15 ], 

Rrec in (5) is averaged layer-wise. Following Theorem 2 and Corollary 1 prove the strict dominance of layer-wise freedom over layer-tied schedules. 

Theorem 2 (Irreducible reconstruction floor of layer-tied schedules) . Under Assumptions 1–3 and a well-conditioned input regime that is σmax (Xn)/σ +min (XnA0(n)†) = O(1) uniformly over layers n,FFA-LoRA [14] which freezes A(n) at the random initialization A0(n) satisfies 

lim   

> T→∞

ERrec (WFFA  

> T

) = Θ 1

> NN

X

> n=1

 δ0(n)2

!

, (6) 

where δ0(n) = ∥B⋆(n)A⋆(n)( I −PA0(n))∥F is the initial subspace misalignment, and the implicit 

Θ(1) constants depend on the per-layer conditioning σ2max (Xn)/σ 2min (XnA0(n)†) = O(1) and absorb both the correction O(1 /√Km ) and the SGD noise floor O(ησ 2 

> g

). Moreover, any RoLoRA-type layer-tied alternation inherits Ω  1

> N

P

> n

(δ0(n)) 2 floor under adversarial layer configurations. 

The preliminaries can be found in Appendix C, the proof of Theorem 2 can be found in Appendix E, and the proof of Corollary 1 can be found in Appendix F. 5Corollary 1 (Strict improvement of AS-LoRA) . Under the same well-conditioned input regime as Theorem 2: if AS-LoRA activates A(n) on Θ( T ) rounds per layer, then for any ε > 0 there exists 

T = O(log(1 /ε )) with δT (n) ≤ ε for all n, and 

lim   

> T→∞

ERrec (WAS  

> T

) = O ε2 < lim   

> T→∞

ERrec (WFFA  

> T

) = Θ   1

> N

X

> n

(δ0(n)) 2 (7) 

whenever any δ0(n0) > ε for at least one layer n0 (the FFA floor’s Θ(1) constant is bounded below). 

Having established that per-layer freedom is the right design axis, we now ask which block to pick at each round. Here, we discuss the improvement via the selection based on S(n) 

> a

. For this, we first define the adaptive gain ∆t 

> S

(n) := Emax a∈{ A,B } Sta(n) − 12 E[StA(n) + StB (n)] the per-layer gap between the score of AS-LoRA’s greedy pick and the uniform baseline. This is the score-space version of the suboptimality gap used in Gauss–Southwell analyses of BCD [ 23 ]. In addition, We collect 

1 − η 

> 2

λ(n) 

> a

across the trajectory and across layers, and define ρ⋆ := inf t,n 



1 − η 

> 2

λ(n) 

> Mt(n)



∈ (0 , 1] ,the curvature-penalty ratio , where λ(n) 

> a

:= ( g(n) 

> a

)⊤Hna g(n) 

> a

/∥g(n) 

> a

∥2 the block Rayleigh quotient. Intuitively, ρ⋆ is the fraction of pure-descent score that survives after the curvature penalty under the AS-LoRA selection rule. Note that the curvature-penalty ratio with AS-LoRA is larger than the value with static schedules in the late-training regime where ∥g(n) 

> A

∥2 ≈ ∥ g(n) 

> B

∥2. The following theorem provides the convergence of AS-LoRA and improvement compared to the static schedules characterized by the factors ∆S , and ρ⋆

Theorem 3 (Accelerated Convergence with quantities ∆S , and ρ⋆). Under Assumptions 1–3 with 

η ≤ 1/β ,

min  

> t≤T

E∥∇L (Wt)∥2 ≤ 2( L(W0) − L ⋆)

η ρ ⋆ T − 2 ∆ S

ρ⋆ + ηβσ 2

> g

ρ⋆ , (8) 

where ∆S := 1

> T

PTt=1 1

> N

PNn=1 ∆t 

> S

(n). Under the additional PL condition (Assumption 4) [ 32 ], 

E[L(WT ) − L ⋆] ≤ (1 − ημρ ⋆)T (L(W0) − L ⋆) + O(ησ 2 

> g

/μ ).

The non-convex bound (8) recovers the standard O(1 /(ηρ ⋆T )) gradient-norm rate of SGD [ 32 ] but with two distinguishing features. The terms are multiplied by 1/ρ ⋆, and the second term −2∆ S /ρ ⋆

guarantees that AS-LoRA’s pick tightens the bound directly and beats the uniform baseline. Under the PL condition, the improvement compounds geometrically, yielding a (1 − ημρ ⋆)T rate that matches the best-known LoRA fine-tuning rate [ 33 , 17 ] but with μρ ⋆ in place of μ, i.e. AS-LoRA provably converges generally faster than any layer-tied or static baselines. The proof can be found in Appendix G. Finally, we discuss that AS-LoRA’s trajectory with S(n) 

> a

is biased toward the flatter of the two blocks, which can be seen as an implict SAM-style regularization [ 24 ] or cheap method to achieve the flatness without the SVD overhead in FedSVD [16]. The proof can be found in Appendix H. 

Theorem 4 (Implicit flatness via curvature-penalty score) . For each layer n, let H(n) 

> t

:= E[H(n) 

> Mt(n)

]

denote the expected block Hessian at the selected mode under AS-LoRA’s argmax rule. Under Assumptions 1–3, 

Eλmax 

 H(n),AS 

> t

 ≤ Eλmax 

 H(n),static 

> t

 for every n ∈ [N ], (9) 

with strict inequality at every layer at which the two per-block top-spectra 

{λmax (H(n) 

> A

), λ max (H(n) 

> B

)} differ. AS-LoRA’s trajectory is therefore biased toward the flat-ter of the two blocks, yielding an implicit SAM-style regularization [24]. 

## 5 Experiments 

Experimental setup. For language tasks, we use RoBERTa-large [ 34 ] as the backbone model, while ViT-large [ 10 ] is used for image tasks. The LoRA rank is set to r = 8 , and the scaling factor is set to 

α = 8 . We consider a federated setting with K = 6 clients for language tasks and K = 3 clients for the vision tasks, with full client participation in every communication round. Data are partitioned using a Dirichlet distribution with α = 0 .5, except for SQuAD [ 35 , 36 ], where data are equally partitioned across clients. Training is conducted for T = 100 communication rounds, with τ = 10 

local steps per round. Following the empirical setup of [ 14 ], the learning rate η is selected from 6{0.05 , 0.1, 0.25 } for FedLoRA and from {0.1, 0.25 , 0.5, 1.0} for all other methods. The batch size is set to B = 128 . For DP-SGD, we set the target failure probability to δ = 10 −5 and fix the clipping norm at C = 2 . The differential privacy budget ϵ is computed using Opacus [ 37 ]. All experiments were run on RTX Pro 6000 GPUs. Additional details are provided in Appendix I. Unless otherwise specified, all experiments follow the settings described in Section 5 and Appendix I. Table 2: Comparison on MNLI under DP-SGD ( ϵ = 3 ).               

> Method MNLI (m) MNLI (mm)
> Proposed (FD) 79.15 ±0.23 80.03 ±0.40
> Proposed (HVP) 80.29 ±0.84 81.29 ±0.80

For the proposed AS-LoRA, the warm-up ratio is set to 0.1.The temperature parameters are set to T 0 = 2 .0, Tmin =0.2, and γ = 0 .95 . While the proposed scoring function provides a theoretically grounded criterion, direct Hessian computation is computationally prohibitive at the scale of large pretrained models. To make the curvature-aware score practical, we consider two approximation strategies, Hessian-Vector Products (HVP) [ 38 ] and Finite-Difference (FD) [ 39 ] estimation, which are described in Appendix B. As shown in Table 2, HVP-based scoring achieves better performance, whereas FD-based scoring yields competitive results without explicitly forming or storing the Hessian matrix. We adopt the FD-based criterion in all subsequent experiments, as it preserves the curvature-aware nature of the proposed score while maintaining practical computational cost. 

Baselines. We compare our method with the following baselines, which directly align with the design objective of AS-LoRA. Methods that address only the aggregation error [ 22 , 40 , 41 ] are excluded from comparison, as they have been shown to suffer significant performance degradation under DP noise [16]. 

FedLoRA [13]: Both A and B are locally trained and independently aggregated at the server. 

FFA-LoRA [14]: A is frozen, and only B is locally trained and aggregated. 

RoLoRA [15 ]: In odd-numbered rounds, A is frozen and only B is locally trained and aggregated. In even-numbered rounds, B is frozen and only A is locally trained and aggregated. Table 3: Performance comparison on the GLUE benchmark [42] under different privacy budgets.                                                                                                                                                                                   

> DP Budget Method QNLI MNLI (m) MNLI (mm) SST-2 QQP SNLI AVG
> ϵ= 3
> FedLoRA 72.43 ±10.56 63.76 ±8.24 64.98 ±8.65 91.37 ±1.63 67.86 ±10.47 78.19 ±2.04 73.10 ±10.36
> FFA-LoRA 68.18 ±11.72 65.06 ±5.40 67.51 ±5.34 91.00 ±0.98 75.79 ±1.28 77.91 ±7.49 74.24 ±8.70
> RoLoRA 65.02 ±13.32 69.15 ±4.50 71.02 ±3.93 91.37 ±0.63 72.86 ±1.21 82.22 ±1.49 75.27 ±8.36
> Proposed 81.06 ±1.34 79.15 ±0.23 80.03 ±0.40 92.86 ±0.44 78.51 ±0.38 85.23 ±0.77 82.81 ±4.93
> ϵ= 1
> FedLoRA 55.72 ±1.40 58.03 ±8.05 59.37 ±8.94 57.57 ±0.37 65.72 ±9.90 74.79 ±1.63 61.87 ±6.56
> FFA-LoRA 71.95 ±7.92 62.70 ±7.67 65.04 ±7.72 79.27 ±11.71 71.00 ±2.41 81.37 ±1.57 71.89 ±6.56
> RoLoRA 53.85 ±5.34 59.91 ±10.66 61.37 ±10.73 62.99 ±13.43 73.10 ±1.59 80.97 ±0.79 65.37 ±9.26
> Proposed 71.52 ±12.50 76.71 ±0.92 77.54 ±0.77 80.76 ±14.08 74.94 ±1.78 84.32 ±0.64 77.63 ±4.10

Table 4: Performance comparison on SQuAD benchmarks [ 35 , 36 ],CIFAR-100 [ 43 ] and Tiny-imageNet [44] under different privacy budgets.                                                                                                                 

> DP Budget Method Question Answering Image Classification SQuAD v1.1 (EM) SQuAD v1.1 (F1) SQuAD v2.0 (EM) SQuAD v2.0 (F1) CIFAR-100 Tiny-ImageNet
> ϵ= 3
> FFA-LoRA 72.30 ±3.52 82.83 ±2.83 55.90 ±3.66 59.92 ±3.93 87.37 ±0.26 85.50 ±0.15
> RoLoRA 69.75 ±2.70 80.63 ±2.30 53.58 ±1.22 56.88 ±1.16 87.75 ±0.40 84.62 ±0.13
> Proposed 73.98 ±1.36 83.99 ±1.17 60.59 ±2.97 64.23 ±3.62 89.55 ±0.20 86.66 ±0.32
> ϵ= 1
> FFA-LoRA 70.42 ±3.91 81.24 ±3.26 54.33 ±3.91 58.58 ±4.16 84.79 ±0.23 83.52 ±0.26
> RoLoRA 61.69 ±3.81 73.94 ±3.10 51.48 ±1.47 53.59 ±1.80 84.64 ±0.38 82.34 ±0.14
> Proposed 72.78 ±1.50 83.23 ±1.37 58.09 ±2.57 61.64 ±3.16 85.62 ±0.24 83.71 ±0.13

Comparison results. All results are reported as mean ± standard deviation over 4 runs with different seeds. Best results are shown in bold and second-best results are underlined . Table 3 reports the performance on the GLUE benchmark [ 42 ] under different privacy budgets. The proposed method consistently outperforms all baselines across tasks. Under a privacy budget of ϵ = 3 , AS-LoRA achieves the best average score of 82 .81 , outperforming RoLoRA ( 75 .27 ) by + 7.54 percentage points (pp). Under a budget of ϵ = 1 , where training instability is more severe, AS-LoRA attains 

77 .64 , surpassing FFA-LoRA ( 71 .89 ) by + 5.74 pp. In particular, significant improvements are observed on MNLI (mm) (+ 12 .50 pp over FFA-LoRA) and consistent gains across SNLI and QQP. As shown in Table 4, these improvements extend beyond classification tasks. On SQuAD, AS-LoRA achieves higher performance across both v1.1 and v2.0 metrics, e.g., + 1.35 EM and + 1.96 F1 over 7FFA-LoRA on SQuAD v1.1 under ϵ = 3 . Moreover, the method consistently improves vision benchmarks, achieving + 2.34 pp on CIFAR-100 and + 1.19 pp on Tiny-ImageNet. Extended results on LoRA rank and heterogeneity of data distribution can be found in Appendix J.4. Table 5: Comparison across mode selection granularity.( Per-client : layer-wise and client-wise, 

AS-LoRA : layer-wise and client-tied, Global : layer-tied and client-tied)                                                            

> DP Budget Method MNLI (m) MNLI (mm) QQP
> ϵ= 3 Per-client 72.73 ±2.07 73.73 ±2.23 63.18 ±0.00
> AS-LoRA (ours) 79.15 ±0.23 80.03 ±0.40 78.51 ±0.38
> Global 75.74 ±2.53 76.98 ±2.44 73.61 ±2.75
> ϵ= 1 Per-client 34.60 ±1.72 34.53 ±1.37 63.18 ±0.00
> AS-LoRA (ours) 76.71 ±0.92 77.54 ±0.77 74.94 ±1.78
> Global 72.54 ±3.85 73.79 ±3.87 72.62 ±3.26

Granularity of mode selection scope. While AS-LoRA performs layer-wise mode selection that is shared across all clients, we evaluate two alternatives to justify this design choice. Both are round-wise adaptive as AS-LoRA, but differ in selection granularity. Global 

shares a single mode across all layers and clients, whereas Per-client selects modes in-dependently for each layer and each client. As shown in Table 5, Per-client , yields the worst performance across all tasks and privacy budgets. It is because the aggregated model suffers from aggregation error. Global also underperforms AS-LoRA. Sharing a single mode across all layers prevents the model from exploiting layer-specific dynamics. AS-LoRA strikes the right balance, reserving model expressiveness without compromising aggregation stability. Table 6: Comparison with FedSVD [ 16 ] in per-formance and aggregation time.                                                            

> DP Budget Method Time (s) MNLI (m) MNLI (mm)
> ϵ= 3
> FedSVD(10) 1.00 (33 ×)78.16 ±1.01 79.43 ±1.21
> FedSVD(5) 1.80 (60 ×)77.06 ±1.47 79.07 ±1.10
> FedSVD(1) 5.44 (181 ×) 78.49 ±0.89 79.75 ±0.68
> Proposed 0.03 (1 ×)79.15 ±0.23 80.03 ±0.40
> ϵ= 1
> FedSVD(10) 1.00 (33 ×)74.24 ±1.48 75.69 ±1.45
> FedSVD(5) 1.80 (60 ×)76.97 ±1.25 78.16 ±1.13
> FedSVD(1) 5.44 (181 ×) 76.62 ±0.77 77.79 ±0.60
> Proposed 0.03 (1 ×)76.71 ±0.92 77.54 ±0.77

Comparison with SVD-based method. Table 6 compares AS-LoRA with FedSVD [16 ] the rep-resentative SVD-based method. FedSVD im-proves performance by introducing an additional SVD computation at the server. FedSVD (n) ap-plies SVD at every n-th communication rounds with standard FedAvg used otherwise. Under 

ϵ = 3 , the proposed method achieves the best performance while requiring substantially lower aggregation time. Under ϵ = 1 , FedSVD (5) 

shows slightly higher performance, but the gap is marginal. Moreover, while the proposed method relies on the FD-based score, it can scale to heavier approximations (HVP) as shown in Table 2. 

Figure 4: Loss landscape visualization of FFA-LoRA, RoLoRA, and AS-LoRA trained on MNLI with T = 100 and τ = 10 under DP-SGD ( ϵ = 3 ). Table 7: Comparison of SAM-style perturbation sharpness on MNLI.                    

> Method PS ∆Acc
> FFA-LoRA 0.03608 ±0.04565 0.981 ±1.182
> RoLoRA 0.01972 ±0.02530 0.321 ±0.045
> AS-LoRA 0.00992 ±0.01281 0.247 ±0.282

Sharpness and loss landscape analysis. To empirically val-idate Theorem 4, which establishes that AS-LoRA’s curvature-penalty score implicitly biases the trajectory toward flatter regions of the loss landscape, we measure the SAM-style perturbation sharpness P S = L(w + ρˆg) − L(w), ˆg =

∇L(w)/∥∇ L(w)∥, together with the induced accuracy drop (∆Acc) [ 24 ]. The results in Table 7 indicates that the solution found by AS-LoRA is more robust to local perturbations and lies in a flatter region of the loss surface. This observation is further supported by the loss landscape visualization in Figure 4. Compared with FFA-LoRA and RoLoRA, the contour around the AS-LoRA 8solution appears to suggest a flatter optimization geometry. Such flatness is known to correlate with improved stability and generalization [ 45 ], which is consistent with the empirical results in TABLE 3 and TABLE 4. Table 8: Comparison of curva-ture computation schedules in AS-LoRA under DP-SGD ( ϵ = 3 ).                                     

> Method MNLI (m) MNLI (mm)
> Periodic computation
> (f=5 )77.99 ±1.17 79.39 ±1.07
> (f=10 )78.83 ±1.08 79.76 ±0.80
> Late-phase computation
> (T∗=80 )78.91 ±1.11 79.87 ±0.95
> (T∗=90 )78.85 ±0.88 80.11 ±0.74
> Original
> Proposed (ours) 79.15 ±0.23 80.03 ±0.40

Overhead analysis. Under AS-LoRA with FD approxima-tion, each client evaluates the local loss at two perturbed points 

Ltk



Wt ± ϵfd vta,k (n)



per layer (Algorithm 4). In our setting, this incurs a 160% increase in training cost over the baseline, which may be impractical for resource-constrained deployments. We therefore propose two complementary strategies that jointly reduce this overhead from 160% to 8% while preserving the performance gains. One-sided FD. Replacing the symmetric difference with the forward difference Ltk



Wt + ϵfd vta,k (n)



−Ltk(Wt) requires only a single additional forward pass, halving the curvature-evaluation cost. Occasional curvature computa-tion. We reduce the frequency of computing the curvature part 

g⊤ 

> a

Haga in Sa = ∥ga∥2 − η 

> 2

g⊤ 

> a

Haga, and uses only ∥ga∥2, which is free through training, otherwise. We compare two scheduling variants: (i) Periodic computation , inspired by the periodic SVD [ 16 ], evaluates the full score every f ∈ { 5, 10 } rounds, and (ii) Late-phase computation evaluates it only after round T ∗ ∈ { 80 , 90 }. The latter is motivated by our empirical observation that ∥ga∥2 dominate the optimization in early training, whereas g⊤ 

> a

Haga becomes informative as ∥ga∥2 diminishes. As shown in Table 8, late-phase computation matches the full FD-based AS-LoRA and incur only a marginal performance drop in terms of variance, causing only a 8% computation increase at T ∗ = 90 

(20 × reduction from the original). These results suggest that full per-round curvature scoring is unnecessary, and that selective computation, at the last phase in particular, can substantially reduce overhead. Communication overhead is negligible: AS-LoRA introduces only N scalar scores (up-link) and N mode bits (downlink) per round, accounting for less than 0.02% of the active LoRA parameter traffic in our 24 -layer configuration. Detailed analyses are provided in Appendix J.5 and Appendix J.6. 

Effect of gradient smoothing under DP-SGD. Motivated by prior work [ 46 , 47 , 48 ] demonstrating that applying smoothing after Gaussian noise injection in DP-SGD improves utility, we investigate several smoothing strategies such as Gaussian, Laplacian, and EMA applied to our method. Detailed results are provided in Appendix J.7. 

## 6 Conclusion 

In this paper, we proposed AS-LoRA, an adaptive optimization framework for LoRA in differentially private FL. Different to the prior work on federated LoRA whose optimization is fixed or layer-tied, AS-LoRA introduces a layer-wise adaptive selection mechanism that dynamically determines which component to update based on a theoretically grounded score function. We provided theoretical analysis showing that AS-LoRA eliminates the irreducible reconstruction error floor of static methods and achieves accelerated convergence with implicit flatter solution, without incurring additional privacy cost. Empirically, AS-LoRA consistently outperforms prior methods across language and vision tasks under non-IID and differentially private settings. 

Limitations and future works. Our current analysis and algorithm focus on layer-wise greedy selection. While this design enables tractable analysis and strong empirical performance, it does not explicitly account for possible dependencies across layers. In practice, the mode selection dynamics in Appendix J.8 suggest that, particularly for language tasks, cross-layer structure may exist in the selection process. Extending AS-LoRA to a joint layer-wise decision framework that models such inter-layer correlations is an important direction for future work. 

Broader impacts. The proposed method advances differentially private FL, which has clear positive societal impact by enabling collaborative model training without exposing private user data—relevant. Potential negative impacts are limited: the method is an optimization technique for fine-tuning foundation models and does not directly enable new harmful capabilities. Improving DP fine-tuning efficiency could in principle lower barriers to deploying powerful models, but this is a general concern shared by all efficiency improvements in machine learning and not specific to this work. 9References 

[1] Brendan McMahan, Eider Moore, Daniel Ramage, Seth Hampson, and Blaise Aguera y Arcas. Communication-efficient learning of deep networks from decentralized data. In Artificial intelligence and statistics , pages 1273–1282. PMLR, 2017. [2] Keith Bonawitz, Hubert Eichner, Wolfgang Grieskamp, Dzmitry Huba, Alex Ingerman, Vladimir Ivanov, Chloe Kiddon, Jakub Koneˇ cn `y, Stefano Mazzocchi, Brendan McMahan, et al. Towards federated learning at scale: System design. Proceedings of machine learning and systems ,1:374–388, 2019. [3] Ligeng Zhu, Zhijian Liu, and Song Han. Deep leakage from gradients. Advances in neural information processing systems , 32, 2019. [4] Reza Shokri, Marco Stronati, Congzheng Song, and Vitaly Shmatikov. Membership inference attacks against machine learning models. In 2017 IEEE symposium on security and privacy (SP) , pages 3–18. IEEE, 2017. [5] Qinqing Zheng, Shuxiao Chen, Qi Long, and Weijie Su. Federated f-differential privacy. In 

International conference on artificial intelligence and statistics , pages 2251–2259. PMLR, 2021. [6] Mohammad Naseri, Jamie Hayes, and Emiliano De Cristofaro. Local and central differential privacy for robustness and privacy in federated learning. arXiv preprint arXiv:2009.03561 ,2020. [7] Josh Achiam, Steven Adler, Sandhini Agarwal, Lama Ahmad, Ilge Akkaya, Florencia Leoni Aleman, Diogo Almeida, Janko Altenschmidt, Sam Altman, Shyamal Anadkat, et al. Gpt-4 technical report. arXiv preprint arXiv:2303.08774 , 2023. [8] Hugo Touvron, Thibaut Lavril, Gautier Izacard, Xavier Martinet, Marie-Anne Lachaux, Timo-thée Lacroix, Baptiste Rozière, Naman Goyal, Eric Hambro, Faisal Azhar, et al. Llama: Open and efficient foundation language models. arXiv preprint arXiv:2302.13971 , 2023. [9] Rohan Anil, Andrew M Dai, Orhan Firat, Melvin Johnson, Dmitry Lepikhin, Alexandre Passos, Siamak Shakeri, Emanuel Taropa, Paige Bailey, Zhifeng Chen, et al. Palm 2 technical report. 

arXiv preprint arXiv:2305.10403 , 2023. [10] Alexey Dosovitskiy, Lucas Beyer, Alexander Kolesnikov, Dirk Weissenborn, Xiaohua Zhai, Thomas Unterthiner, Mostafa Dehghani, Matthias Minderer, Georg Heigold, Sylvain Gelly, et al. An image is worth 16x16 words: Transformers for image recognition at scale. arXiv preprint arXiv:2010.11929 , 2020. [11] Ze Liu, Yutong Lin, Yue Cao, Han Hu, Yixuan Wei, Zheng Zhang, Stephen Lin, and Baining Guo. Swin transformer: Hierarchical vision transformer using shifted windows. In Proceedings of the IEEE/CVF international conference on computer vision , pages 10012–10022, 2021. [12] Edward J Hu, Yelong Shen, Phillip Wallis, Zeyuan Allen-Zhu, Yuanzhi Li, Shean Wang, Lu Wang, Weizhu Chen, et al. Lora: Low-rank adaptation of large language models. ICLR ,1(2):3, 2022. [13] Jianyi Zhang, Saeed Vahidian, Martin Kuo, Chunyuan Li, Ruiyi Zhang, Tong Yu, Guoyin Wang, and Yiran Chen. Towards building the federatedgpt: Federated instruction tuning. In 

ICASSP 2024-2024 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP) , pages 6915–6919. IEEE, 2024. [14] Youbang Sun, Zitao Li, Yaliang Li, and Bolin Ding. Improving lora in privacy-preserving federated learning. arXiv preprint arXiv:2403.12313 , 2024. [15] Shuangyi Chen, Yuanxin Guo, Yue Ju, Harik Dalal, Zhongwen Zhu, and Ashish Khisti. Robust federated finetuning of llms via alternating optimization of lora. arXiv preprint arXiv:2502.01755 , 2025. 10 [16] Seanie Li et al. FedSVD: Adaptive orthogonalization for private federated learning with LoRA. 2025. [17] Soufiane Hayou, Nikhil Ghosh, and Bin Yu. Lora+: Efficient low rank adaptation of large models. arXiv preprint arXiv:2402.12354 , 2024. [18] Yeonjoon Jung, Daehyun Ahn, Hyungjun Kim, Taesu Kim, and Eunhyeok Park. Gralora: Gran-ular low-rank adaptation for parameter-efficient fine-tuning. arXiv preprint arXiv:2505.20355 ,2025. [19] William B Johnson, Joram Lindenstrauss, et al. Extensions of lipschitz mappings into a hilbert space. Contemporary mathematics , 26(189-206):1, 1984. [20] Cynthia Dwork and Aaron Roth. The algorithmic foundations of differential privacy. Founda-tions and Trends in Theoretical Computer Science , 9(3-4):211–407, 2014. [21] Martin Abadi, Andy Chu, Ian Goodfellow, H Brendan McMahan, Ilya Mironov, Kunal Talwar, and Li Zhang. Deep learning with differential privacy. In Proceedings of the 2016 ACM SIGSAC conference on computer and communications security , pages 308–318, 2016. [22] Ziyao Wang, Zheyu Shen, Yexiao He, Guoheng Sun, Hongyi Wang, Lingjuan Lyu, and Ang Li. Flora: Federated fine-tuning large language models with heterogeneous low-rank adaptations. 

Advances in Neural Information Processing Systems , 37:22513–22533, 2024. [23] Yurii Nesterov. Efficiency of coordinate descent methods on huge-scale optimization problems. 

SIAM Journal on Optimization , 22(2):341–362, 2012. [24] Pierre Foret, Ariel Kleiner, Hossein Mobahi, and Behnam Neyshabur. Sharpness-aware min-imization for efficiently improving generalization. In International Conference on Learning Representations (ICLR) , 2021. [25] Guangxuan Xiao, Ji Lin, Mickael Seznec, Hao Wu, Julien Demouth, and Song Han. Smoothquant: Accurate and efficient post-training quantization for large language models. In International conference on machine learning , pages 38087–38099. PMLR, 2023. [26] Ji Lin, Jiaming Tang, Haotian Tang, Shang Yang, Wei-Ming Chen, Wei-Chen Wang, Guangxuan Xiao, Xingyu Dang, Chuang Gan, and Song Han. Awq: Activation-aware weight quantization for on-device llm compression and acceleration. Proceedings of machine learning and systems ,6:87–100, 2024. [27] Eric Jang, Shixiang Gu, and Ben Poole. Categorical reparameterization with gumbel-softmax. 

arXiv preprint arXiv:1611.01144 , 2016. [28] Chris J Maddison, Andriy Mnih, and Yee Whye Teh. The concrete distribution: A continuous relaxation of discrete random variables. arXiv preprint arXiv:1611.00712 , 2016. [29] Ilya Mironov. Rényi differential privacy. In IEEE Computer Security Foundations Symposium (CSF) , pages 263–275, 2017. [30] Yu-Xiang Wang, Borja Balle, and Shiva Prasad Kasiviswanathan. Subsampled Rényi differential privacy and analytical moments accountant. In AISTATS , 2019. [31] João Carlos Alves Barata and Mahir Saleh Hussein. The moore–penrose pseudoinverse: A tutorial review of the theory. Brazilian Journal of Physics , 42(1):146–165, 2012. [32] Hamed Karimi, Julie Nutini, and Mark Schmidt. Linear convergence of gradient and proximal-gradient methods under the Polyak–Łojasiewicz condition. ECML-PKDD , 2016. [33] Chaoyue Liu, Libin Zhu, and Mikhail Belkin. Loss landscapes and optimization in over-parameterized non-linear systems and neural networks. Applied and Computational Harmonic Analysis , 59:85–116, 2022. [34] Yinhan Liu, Myle Ott, Naman Goyal, Jingfei Du, Mandar Joshi, Danqi Chen, Omer Levy, Mike Lewis, Luke Zettlemoyer, and Veselin Stoyanov. Roberta: A robustly optimized bert pretraining approach. arXiv preprint arXiv:1907.11692 , 2019. 11 [35] Pranav Rajpurkar, Jian Zhang, Konstantin Lopyrev, and Percy Liang. Squad: 100,000+ questions for machine comprehension of text. In Proceedings of the 2016 conference on empirical methods in natural language processing , pages 2383–2392, 2016. [36] Pranav Rajpurkar, Robin Jia, and Percy Liang. Know what you don’t know: Unanswerable ques-tions for squad. In Proceedings of the 56th Annual Meeting of the Association for Computational Linguistics (Volume 2: Short Papers) , pages 784–789, 2018. [37] Ashkan Yousefpour, Igor Shilov, Alexandre Sablayrolles, Davide Testuggine, Karthik Prasad, Mani Malek, John Nguyen, Sayan Ghosh, Akash Bharadwaj, Jessica Zhao, et al. Opacus: User-friendly differential privacy library in pytorch. arXiv preprint arXiv:2109.12298 , 2021. [38] Barak A Pearlmutter. Fast exact multiplication by the hessian. Neural computation , 6(1):147– 160, 1994. [39] Jorge Nocedal and Stephen J Wright. Numerical optimization . Springer, 2006. [40] Raghav Singhal, Kaustubh Ponkshe, and Praneeth Vepakomma. Fedex-lora: Exact aggregation for federated and efficient fine-tuning of foundation models. arXiv preprint arXiv:2410.09432 ,2024. [41] Jiamu Bai, Daoyuan Chen, Bingchen Qian, Liuyi Yao, and Yaliang Li. Federated fine-tuning of large language models under heterogeneous tasks and client resources. Advances in Neural Information Processing Systems , 37:14457–14483, 2024. [42] Alex Wang, Amanpreet Singh, Julian Michael, Felix Hill, Omer Levy, and Samuel Bowman. Glue: A multi-task benchmark and analysis platform for natural language understanding. In 

Proceedings of the 2018 EMNLP workshop BlackboxNLP: Analyzing and interpreting neural networks for NLP , pages 353–355, 2018. [43] Alex Krizhevsky, Geoffrey Hinton, et al. Learning multiple layers of features from tiny images. 2009. [44] Jia Deng, Wei Dong, Richard Socher, Li-Jia Li, Kai Li, and Li Fei-Fei. Imagenet: A large-scale hierarchical image database. In 2009 IEEE conference on computer vision and pattern recognition , pages 248–255. Ieee, 2009. [45] Jean Kaddour, Linqing Liu, Ricardo Silva, and Matt J Kusner. When do flat minima optimizers work? Advances in Neural Information Processing Systems , 35:16577–16595, 2022. [46] Jin Liu, Yinbin Miao, Ning Xi, and Junkang Liu. Rethinking lora for privacy-preserving federated learning in large models. arXiv preprint arXiv:2602.19926 , 2026. [47] Zhicong Liang, Bao Wang, Quanquan Gu, Stanley Osher, and Yuan Yao. Differentially private federated learning with laplacian smoothing. Applied and Computational Harmonic Analysis ,72:101660, 2024. [48] Bao Wang, Quanquan Gu, March Boedihardjo, Lingxiao Wang, Farzin Barekat, and Stanley J Osher. Dp-lssgd: A stochastic optimization method to lift the utility in privacy-preserving erm. In Mathematical and Scientific Machine Learning , pages 328–351. PMLR, 2020. [49] Kevin Kuo, Arian Raje, Kousik Rajesh, and Virginia Smith. Federated lora with sparse communication. arXiv preprint arXiv:2406.05233 , 2024. [50] Yae Jee Cho, Luyang Liu, Zheng Xu, Aldi Fahrezi, and Gauri Joshi. Heterogeneous lora for federated fine-tuning of on-device foundation models. arXiv preprint arXiv:2401.06432 , 2024. [51] Zihan Fang, Zheng Lin, Zhe Chen, Xianhao Chen, Yue Gao, and Yuguang Fang. Automated federated pipeline for parameter-efficient fine-tuning of large language models. arXiv preprint arXiv:2404.06448 , 2024. [52] Liping Yi, Han Yu, Gang Wang, Xiaoguang Liu, and Xiaoxiao Li. pfedlora: Model-heterogeneous personalized federated learning with lora tuning. arXiv preprint arXiv:2310.13283 , 2023. 12 [53] Jabin Koo, Minwoo Jang, and Jungseul Ok. Towards robust and efficient federated low-rank adaptation with heterogeneous clients. In Proceedings of the 63rd Annual Meeting of the Association for Computational Linguistics (Volume 1: Long Papers) , pages 416–429, 2025. [54] Zhewei Yao, Amir Gholami, Kurt Keutzer, and Michael W Mahoney. PyHessian: Neural networks through the lens of the Hessian. In IEEE International Conference on Big Data , 2020. [55] Roger Penrose. A generalized inverse for matrices. Mathematical Proceedings of the Cambridge Philosophical Society , 51(3):406–413, 1955. [56] Roger A Horn and Charles R Johnson. Matrix analysis. Cambridge University Press , 2012. [57] Roman Vershynin. High-Dimensional Probability: An Introduction with Applications in Data Science . Cambridge University Press, 2018. [58] Yurii Nesterov. Lectures on Convex Optimization . Springer, 2nd edition, 2018. [59] Léon Bottou, Frank E. Curtis, and Jorge Nocedal. Optimization methods for large-scale machine learning. SIAM Review , 60(2):223–311, 2018. [60] Per-Åke Wedin. Perturbation bounds in connection with singular value decomposition. BIT Numerical Mathematics , 12(1):99–111, 1972. [61] Ajay Kumar Jaiswal, Yifan Wang, Lu Yin, Shiwei Liu, Runjin Chen, Jiawei Zhao, Ananth Grama, Yuandong Tian, and Zhangyang Wang. From low rank gradient subspace stabilization to low-rank weights: Observations, theories, and applications. In International Conference on Machine Learning , pages 26740–26756. PMLR, 2025. [62] Jared Kaplan, Sam McCandlish, Tom Henighan, Tom B Brown, Benjamin Chess, Rewon Child, Scott Gray, Alec Radford, Jeffrey Wu, and Dario Amodei. Scaling laws for neural language models. arXiv preprint arXiv:2001.08361 , 2020. 13 A Related Works 

A.1 Federated learning with LoRA 

Recent studies have explored integrating LoRA into FL to alleviate the communication and com-putation overhead associated with fine-tuning large-scale models [ 49 , 50 , 51 , 52 ]. FedIT [ 13 ] is one of the earliest attempts to incorporate LoRA into FL, demonstrating that low-rank adapters can effectively reduce communication costs while maintaining competitive performance. However, FedIT aggregates the two low-rank components in a straightforward manner and does not explicitly account for aggregation errors that arise during the aggregation process. To mitigate aggregation mismatch caused by heterogeneous client updates, several extensions have been proposed. FLoRA [ 22 ] ad-dresses heterogeneity and aggregation errors by stacking client-specific LoRA adapters at the server for aggregation. FedEx-LoRA [ 40 ] alleviates aggregation error by introducing a residual correction term added to a fixed pre-trained weight matrix. FlexLoRA [ 41 ] further addresses heterogeneity by reconstructing an ideal weight update matrix from the aggregated client adapters at the server, followed by singular value decomposition (SVD) to redistribute LoRA adapters with different ranks to individual clients. 

A.2 Differentially private federated LoRA 

More recent studies have investigated federated LoRA under differential privacy (DP) constraints. FFA-LoRA [ 14 ] provides a detailed analysis of the noise amplification problem that arises when DP is applied to federated LoRA training, and mitigates this issue by updating only the B component. However, restricting updates to the B component leads to a loss of expressiveness in LoRA. To address this limitation, FedSVD [ 16 ] similarly updates only the B component, while leveraging orthogonalization and low-rank decomposition to alleviate the expressiveness loss and improve training stability under DP constraints. Another line of work, which is most closely related to our study, aims to alleviate aggregation error by adopting alternating optimization strategies. RoLoRA [ 15 ]and LoRA-A 2 [ 53 ] alternate the optimization of the two LoRA components across communication rounds, updating only one component at a time to reduce aggregation mismatch. La-LoRA [ 46 ]further extend alternating optimization from communication rounds to local update steps. While these methods empirically improve training stability, they rely on fixed and symmetric alternating schedules and do not explicitly account for the asymmetric roles of the two LoRA components during training. In summary, existing federated LoRA-based approaches either restrict updates to a single component or adopt fixed, symmetric alternating schedules, both of which overlook the structural asymmetry between the two LoRA components and the round-wise dynamics of training. This motivates AS-LoRA, which makes principled, adaptive per-layer decisions based on training dynamics. 14 B AS-LoRA Algorithm 

Figure 5: Detail of the proposed AS-LoRA framework. 

Figure 6: Detailed visualization of the proposed AS-LoRA score computation and mode selection procedures. (a) Component-wise scores are computed from the projected gradients of LoRA A and 

B matrices. Random projection mitigates the effect of channel-wise gradient outliers before applying the squared-gradient and curvature-aware score terms. (b) The server smooths the aggregated scores using EMA and performs layer-wise mode selection. During warm-up, AS-LoRA alternates between components, while after warm-up it uses a temperature-scaled score-based selection rule to produce the final mode vector. 15 Algorithm 1 AS-LoRA  

> 1:

Input: communication rounds T , clients K, LoRA layers N 

> 2:

Initialize A0(n), B 0(n) and server scores ˆS0

> A

(n), ˆS0 

> B

(n) for all n 

> 3:

for t = 1 to T do  

> 4:

M t ← M ( ˆSt−1 

> A

, ˆSt−1 

> B

) 

> 5:

for k = 1 to K in parallel do  

> 6:

Update LoRA parameters using DP-SGD according to M t 

> 7:

Compute local scores StA,k (n), S tB,k (n) ← S (·) 

> 8:

Upload updated parameters and scores to the server  

> 9:

end for  

> 10:

for n = 1 to N do  

> 11:

if M t(n) = 0 then  

> 12:

At(n) ← 1

> K

PKk=1 Atk(n), Bt(n) ← Bt−1(n) 

> 13:

else  

> 14:

Bt(n) ← 1

> K

PKk=1 Btk(n), At(n) ← At−1(n) 

> 15:

end if  

> 16:

StA(n) ← 1

> K

PKk=1 StA,k (n), StB (n) ← 1

> K

PKk=1 StB,k (n) 

> 17:

ˆStA(n) ← EMA 

 ˆSt−1 

> A

(n), S tA(n)



, ˆStB (n) ← EMA 

 ˆSt−1 

> B

(n), S tB (n)

 

> 18:

end for  

> 19:

end for  

> 20:

Output: final LoRA parameters {AT (n), B T (n)}Nn=1 

Algorithm 2 Score Computation S∥g∥2 (·) with Random Projection  

> 1:

Input: local loss Ltk, LoRA parameters {Atk(n), B tk(n)}Nn=1 , projection matrices 

{RA(n), R B (n)}Nn=1  

> 2:

Output: local scores {StA,k (n), S tB,k (n)}Nn=1  

> 3:

for n = 1 to N do  

> 4:

Compute gradients: 

gtA,k (n) ← ∇ Atk (n)Ltk, gtB,k (n) ← ∇ Btk (n)Ltk 

> 5:

Apply Gaussian random projection: 

egtA,k (n) ← gtA,k (n)RA(n), egtB,k (n) ← RB (n)gtB,k (n) 

> 6:

Compute projected squared-gradient scores: 

StA,k (n) ← egtA,k (n) 22

StB,k (n) ← egtB,k (n) 22 

> 7:

end for  

> 8:

return {StA,k (n), S tB,k (n)}Nn=1 

16 Algorithm 3 Score Computation SHVP (·) with Random Projection 

1: Input: local loss Ltk, LoRA parameters {Atk(n), B tk(n)}Nn=1 , projection matrices 

{RA(n), R B (n)}Nn=1 , learning rate η

2: Output: local scores {StA,k (n), S tB,k (n)}Nn=1 

3: for n = 1 to N do 

4: Compute gradients: 

gtA,k (n) ← ∇ Atk (n)Ltk, gtB,k (n) ← ∇ Btk (n)Ltk

5: Apply random projection: 

egtA,k (n) ← gtA,k (n)RA(n), egtB,k (n) ← RB (n)gtB,k (n)

6: Compute Hessian-vector products: 

htA,k (n) ← HA(n)(Wt) egtA,k (n), htB,k (n) ← HB(n)(Wt) egtB,k (n)

7: Compute scores: 

StA,k (n) ← ∥ egtA,k (n)∥22 − η

2 egtA,k (n)⊤htA,k (n)

StB,k (n) ← ∥ egtB,k (n)∥22 − η

2 egtB,k (n)⊤htB,k (n)

8: end for 

9: return {StA,k (n), S tB,k (n)}Nn=1 

Algorithm 4 Score Computation SFD (·) with Random Projection 

1: Input: local loss Ltk, LoRA parameters {Atk(n), B tk(n)}Nn=1 , projection matrices 

{RA(n), R B (n)}Nn=1 , learning rate η, finite-difference radius ϵfd 

2: Output: local scores {StA,k (n), S tB,k (n)}Nn=1 

3: for n = 1 to N do 

4: Compute gradients: 

gtA,k (n) ← ∇ Atk (n)Ltk, gtB,k (n) ← ∇ Btk (n)Ltk

5: Apply random projection: 

egtA,k (n) ← gtA,k (n)RA(n), egtB,k (n) ← RB (n)gtB,k (n)

6: for a ∈ { A, B } do 

7: Normalize the projected gradient direction: 

vta,k (n) ← egta,k (n)

∥egta,k (n)∥2 + ϵ

8: Estimate curvature by finite difference: 

cta,k (n) ←Ltk



Wt + ϵfd vta,k (n)



− 2Ltk(Wt) + Ltk



Wt − ϵfd vta,k (n)



ϵ2fd 

9: Compute finite-difference score: 

Sta,k (n) ← ∥ egta,k (n)∥22 − η

2 cta,k (n)∥egta,k (n)∥22

10: end for 

11: end for 

12: return {StA,k (n), S tB,k (n)}Nn=1 

17 Algorithm 5 Mode Selection M(·) 

> 1:

Input: EMA scores { ˆStA(n), ˆStB (n)}Nn=1 , round t, warm-up period tw, temperature parameters 

(T 0, T min , γ ) 

> 2:

Output: mode vector M t = {M t(n)}Nn=1  

> 3:

if t ≤ tw then  

> 4:

// warm-up: alternating schedule  

> 5:

for n = 1 to N do  

> 6:

M t(n) ← (t mod 2)  

> 7:

end for  

> 8:

else  

> 9:

Compute temperature: 

T t ← max 



Tmin , T 0 · γ(t−tw ) 

> 10:

for n = 1 to N do  

> 11:

Compute selection probabilities: 

P t(A, n ) ←

exp 

 ˆStA(n)/T t



exp 

 ˆStA(n)/T t



+ exp 

 ˆStB (n)/T t

 

> 12:

Sample mode: 

M t(n) ∼ Bernoulli( P t(A, n ))  

> 13:

end for  

> 14:

end if  

> 15:

return M t

18 B.1 Visualization of AS-LoRA framework 

Fig 5 illustrates the overall workflow of the proposed AS-LoRA framework. At each communication round, the server first determines the layer-wise mode based on historical scores and broadcasts only the selected LoRA components. Each client then performs local updates using DP-SGD according to the assigned modes, computes component-wise scores, and uploads both the updated parameters and scores to the server. The server aggregates the selected components and updates the global scores, which are used for the next round of mode selection. This process enables adaptive, layer-wise optimization across communication rounds while mitigating aggregation error and unnecessary noise amplification. The overall training procedure of AS-LoRA is summarized in Algorithm 1. 

B.2 Score Computation and Mode Selection 

Figure 6 provides a detailed illustration of the score computation and mode selection procedures in AS-LoRA. 

Score Computation. Figure 6(a) depicts the score computation procedure performed on each client. As illustrated, the gradient of LoRA-A exhibits channel-wise outliers, where the gradient magnitude is highly concentrated in a small subset of channels. To mitigate this, a Gaussian random projection is applied to each gradient, redistributing the concentrated magnitude across dimensions and enabling more reliable score estimation. The score computation procedures with random projection are described in Algorithms 2–4. 

Mode Selection. Figure 6(b) illustrates the mode selection procedure. The server-side scores are smoothed via EMA to reduce round-to-round variance, and the mode for each layer is determined based on the smoothed scores. During the warm-up phase ( t ≤ tw), all layers follow a simple alternating schedule, where the A and B components are activated in alternation across rounds, and the mode vector M t(n) ∈ { 0, 1} changes accordingly. After the warm-up phase ( t > t w), score-based selection is activated. The selection probability for each component is computed via a temperature-scaled softmax. Here, the temperature T t follows an annealing schedule that decays from an initial value T 0 to a minimum Tmin over training rounds. This annealing encourages exploration of both components in early rounds while gradually shifting toward exploitation of the higher-scoring component as training progresses. The final output is the mode vector M t = {M t(1) , M t(2) , . . . , M t(N )},

M t(n) ∈ { 0, 1}, which specifies the active LoRA component for each layer at round t. The full mode selection procedure is summarized in Algorithm 5. 

## C Preliminaries for Theoretical Analysis 

C.1 Background 

We analyze AS-LoRA along two axes: (i) layer-wise freedom —the ability to activate A(n) or B(n)

independently per layer—and (ii) score-driven selection —the greedy rule arg max a∈{ A,B } S(n) 

> a

with 

S(n) 

> a

= ∥g(n) 

> a

∥2 − η 

> 2

(g(n) 

> a

)⊤H(n) 

> a

g(n) 

> a

. Each axis contributes a distinct and quantifiable advantage over the layer-tied or fixed-schedule baselines (FFA-LoRA [ 14 ], RoLoRA [ 15 ]). The analysis combines block-coordinate descent theory [ 23 ], Polyak–Łojasiewicz convergence [ 32 ], and DP-SGD privacy accounting [21, 29, 30]. 

C.2 Setup 

Let L(W ) = 1

> K

PKk=1 Lk(W ) be the federated loss over K clients with per-client dataset size 

m (total Km samples). For layer n ∈ [N ], the frozen weight is W (n)0 ∈ Rd×d and the LoRA update factorizes as B(n)A(n) with B(n) ∈ Rd×r , A(n) ∈ Rr×d, and rank r ≪ d, so that the effective weight is W (n) = W (n)0 + B(n)A(n) acting on an input X ∈ Rd×m as W (n)X(n) =

W (n)0 X + B(n)A(n)X(n)

19 Stochastic gradient and population Hessian. For each block label a ∈ { A, B }, let g(n) 

> a

de-note the mini-batch stochastic gradient of L with respect to the factor matrix a(n), satisfying 

E[g(n) 

> a

] = ∇a(n) L over the random sampling. Concretely, g(n) 

> A

∈ Rr×d and g(n) 

> B

∈ Rd×r are matrix-valued. The corresponding deterministic population Hessian H(n) 

> a

(W ) := ∇2 

> a(n)

L(W )

acts on the vectorized factor and is treated as a (dim a(n)) × (dim a(n)) matrix throughout (with 

dim A(n) = dim B(n) = rd ). Randomness enters only through g(n) 

> a

and DP-SGD noise 

Na ∼ N (0 , σ 2C2I) (clipping bound C), and ∇a(n) L and H(n) 

> a

(W ) are deterministic functions of W as in the standard convention for second-order SGD/BCD analyses [ 32 , 23 ]. With ∥ · ∥ de-noting the Frobenius norm for matrices and the Euclidean norm for vector objects, the per-layer block Rayleigh quotient is λ(n) 

> a

:= ⟨g(n) 

> a

, H (n) 

> a

g(n) 

> a

⟩/∥g(n) 

> a

∥2 and the per-layer block-greedy score is 

S(n) 

> a

:= ∥g(n) 

> a

∥2 − η 

> 2

⟨g(n) 

> a

, H (n) 

> a

g(n) 

> a

⟩.

Remark 1 (Practical estimation of H(n) 

> a

g(n) 

> a

). All theoretical statements use the population Hessian 

H(n) 

> a

(W ) = ∇2 

> a(n)

L(W ). In practice, the curvature term ⟨g(n) 

> a

, H (n) 

> a

g(n) 

> a

⟩ is computed via a single Hessian-vector product on the same mini-batch used for g(n) 

> a

(Pearlmutter trick) or via a Hutchinson-style sketch [ 54 ]; the resulting estimation bias is O(σ2 

> g

/m ) and is absorbed into the ηβσ 2 

> g

noise term of Theorem 3 without altering any rate. 

Moore–Penrose pseudoinverse and orthogonal projectors. Several reconstruction-floor and subspace-misalignment quantities are stated in terms of the Moore–Penrose pseudoinverse, which we briefly recall. For any matrix M ∈ Rp×q , the Moore–Penrose pseudoinverse M † ∈ Rq×p

is the unique matrix satisfying the four Penrose conditions M M †M = M , M †M M † = M †,

(M M †)⊤ = M M †, (M †M )⊤ = M †M [55 ]. Three properties are used repeatedly. (i) When M is square and invertible, M † = M −1. (ii) When M is row full-rank (the generic case for our LoRA factors At(n) ∈ Rr×d with r < d ), M † = M ⊤(M M ⊤)−1, and At(n) At(n)† = Ir acts as a right inverse. (iii) The two products M M † ∈ Rp×p and M †M ∈ Rq×q are precisely the orthogonal projectors onto col( M ) ⊆ Rp and row( M ) ⊆ Rq respectively. Operationally, the least-squares minimum-norm solution of M x = b is x⋆ = M †b, which is the property used to obtain the closed-form FFA-LoRA minimizer in the proof of Theorem 2. All projectors PAt(n) = At(n)†At(n) used in the analysis are of form (iii) and project onto the row-span of the corresponding factor matrix. 

C.3 Assumptions 

First, we collect here all assumptions invoked by the main text. Assumptions 1–4 are the standard block / per-layer counterparts of the smoothness, bounded-variance, Hessian-spectrum, and Polyak– Łojasiewicz (PL) conditions used in modern SGD/BCD/PL analyses [ 23 , 32 ]. Strong convexity of the Assumption 3 is invoked only on the per-layer reconstruction quadratic, not on the full non-convex loss, and the PL condition of Assumption 4 replaces it for end-to-end statements. 

Assumption 1 (Block smoothness) . Each Lk is L-smooth and block-smooth with constants 

L(n) 

> A

, L (n) 

> B

, and β := max a,n L(n) 

> a

.

Assumption 2 (Bounded stochastic variance) . E∥g(n) 

> a

− ∇ a(n)L∥ 2 ≤ σ2 

> g

for every layer n and block label a ∈ { A, B }, and per-sample clipping ∥g(n) 

> a,i

∥ ≤ C a.s. 

Assumption 3 (Block strong convexity and Lipschitz Hessian) . μ(n) 

> a

I ⪯ H(n) 

> a

⪯ L(n) 

> a

I, and H is 

LH -Lipschitz. 

Assumption 4 (PL condition, non-convex) . ∥∇L∥ 2 ≥ 2μ(L − L ⋆) for some μ > 0 [32]. 

C.4 Mathematical Preliminaries 

This appendix collects the standard tools from matrix analysis [ 56 ], high-dimensional probability [57], and convex optimization [58]. 

Lemma 1 (Norm submultiplicativity and SVD-based reverse bound) . For any M ∈ Rp×d and 

X ∈ Rd×m of compatible dimensions (so that M X ∈ Rp×m is well defined), 

∥M X ∥F ≤ σmax (X) ∥M ∥F , (10) 20 where σmax (X) = ∥X∥op is the largest singular value of X. We refer to this as the standard inequality. If the rows of M lie in col( X⊤) = row( X), equivalently M = M P X where PX =

X⊤(XX ⊤)−1X when X has full row rank, then the matching reverse inequality holds: 

∥M X ∥F ≥ σ+min (X) ∥M ∥F , (11) 

with σ+min (X) the smallest nonzero singular value of X.Proof. The upper bound is directly from [ 56 ]. For the proof of lower bound, let X = U ΣV ⊤ be the SVD with Σ = diag( σ1, . . . , σ k, 0, . . . , 0) , σ1 ≥ · · · ≥ σk > 0, k = rank( X). Then ∥M X ∥2 

> F

=

∥M U ΣV ⊤∥2 

> F

= ∥M U Σ∥2 

> F

(Frobenius is invariant under right multiplication by orthogonal V ⊤). Writing M U =: N with columns n1, . . . , n d, ∥M U Σ∥2 

> F

= Pdi=1 σ2 

> i

∥ni∥2. If rows of M lie in 

row( X), then ni = 0 for all i > k , so P 

> i

σ2 

> i

∥ni∥2 ≥ σ2

> k

P 

> i≤k

∥ni∥2 = ( σ+min (X)) 2∥M ∥2 

> F

.

Lemma 2 (High-probability bound of covariance estimation [ 57 ], Rem. 4.7.3) . Let x1, . . . , x n ∈ Rd

be i.i.d. zero-mean random vectors with population covariance Σ := E[xix⊤ 

> i

], satisfying 

⟨X, v ⟩ ψ2 ≤ κ ⟨X, v ⟩ L2 for every v ∈ Rd, (12) 

with parameter κ. Then for every u ≥ 0, with probability at least 1 − 2e−u,

> 1
> nn

X

> i=1

xix⊤ 

> i

− Σ op 

≤ C κ 2 ∥Σ∥op 

p (d + u)/n + ( d + u)/n 



, (13) 

for an absolute constant C > 0. In particular, for n ≳ d and any fixed confidence level 1 − 2e−u,

> 1
> nn

X

> i=1

xix⊤ 

> i

− Σ op = Op

 κ2∥Σ∥op 

pd/n . (14) 

Lemma 3 (Linear convergence of GD on strongly-convex smooth functions [ 58 ], Thm. 2.3.4) . Let 

f : Rp×q → R be twice differentiable with Hessian satisfying μI ⪯ ∇ 2f ⪯ LI for 0 < μ ≤ L. Let 

X⋆ = arg min f . Then gradient descent Xt+1 = Xt − η∇f (Xt) with step size η ≤ 1/L satisfies 

∥Xt+1 − X⋆∥2 

> F

≤ (1 − ημ ) ∥Xt − X⋆∥2 

> F

≤ (1 − μ/L ) ∥Xt − X⋆∥2 

> F

. (15) 

Lemma 4 (Second-order Taylor with cubic remainder [ 58 ], Lemma 1.2.4) . Let L : Rd → R be twice differentiable with LH -Lipschitz Hessian: ∥∇ 2L(x) − ∇ 2L(y)∥op ≤ LH ∥x − y∥. Then for any W, h ,

L(W + h) − L (W ) − ⟨∇L (W ), h ⟩ − 12 h⊤∇2L(W )h ≤ LH 

> 6

∥h∥3. (16) 

## D Proof of Theorem 1 

The DP-SGD mechanism of [ 21 ] uses subsampled Gaussian noise: at each round, sample a fraction q

of clients, compute clipped gradients ˜gi = Π ∥·∥≤ C (gi), aggregate ¯g = P 

> i∈batch

˜gi + N (0 , σ 2C2I).By the moments accountant / Rényi-DP analysis of [ 29 , 30 ], each round contributes at most q2C2 

> σ2

to the cumulative Rényi-DP (α, ϵ α) budget at order α. Rényi-DP composes additively. T rounds give cumulative Rényi-DP cost at most T · q2C2/σ 2 at order α. Converting to (ϵ, δ )-DP via the standard tail bound ϵ = ϵα + log(1 /δ )/(α − 1) and optimising over α yields ϵ ≤ q2C2T log(1 /δ )/σ 2.The AS-LoRA selection arg max a∈{ A,B } S(n),t a uses the per-block scores S(n),t a = ∥¯g(n) 

> a

∥2 − 

> η
> 2

⟨¯g(n) 

> a

, H (n) 

> a

¯g(n) 

> a

⟩, where ¯g(n) 

> a

is the already-clipped, already-noised, before being aggregated. Since 

H(n) 

> a

is also computed on the same mini-batch, the score is a deterministic function of ¯g(n) 

> a

. The post-processing invariance of differential privacy [ 20 ] says that any function applied to the output of an 

(ϵ, δ )-DP mechanism remains (ϵ, δ )-DP . Since M t(n) is computed deterministically from {¯g(n) 

> a

}a,n ,which are already DP-protected, no additional privacy cost is incurred. The total AS-LoRA privacy ledger therefore matches FFA-LoRA / RoLoRA exactly. □

21 E Proof of Theorem 2 

Fix layer n. Under FFA-LoRA, A(n) is permanently fixed at A0(n) and only B(n) is updated by SGD with step size η. Because the per-layer reconstruction loss is convex quadratic in B(n) (when 

A(n), Xn are fixed), standard SGD theory on convex quadratics guarantees that the iterates B(n)

> t

converge in expectation to the static minimizer BFFA (n) of 

ℓ(n)(B) = 12 B⋆(n)A⋆(n)Xn − B A 0(n)Xn 

> 2
> F

, (17) up to an O(ησ 2 

> g

) noise floor [59]. Consequently, 

lim   

> T→∞

E[R(n),FFA rec ] = min  

> B

ℓ(n)(B)/(Km ) + O(ησ 2 

> g

), (18) and the first term is fully determined by the static least-squares minimization. The O(ησ 2 

> g

) correction is a standard SGD noise-floor term, not a function of the misalignment δ0(n), so it does not affect the qualitative shape of the floor. We absorb it into ˜c for the remainder of the proof. The function ℓ(n)(B)

is convex quadratic in B with Hessian A0(n)XnX⊤ 

> n

A0(n)⊤ ⊗ I, which is positive definite when 

A0(n)Xn has full row rank (the generic case). Setting ∇B ℓ(n)(B) = 0 gives the normal equation 

 B A 0(n)Xn − B⋆(n)A⋆(n)Xn

 (A0(n)Xn)⊤ = 0 , (19) which solves to 

BFFA (n) = B⋆(n)A⋆(n) XnX⊤ 

> n

A0(n)⊤ A0(n) XnX⊤ 

> n

A0(n)⊤−1. (20) This is the matrix Moore–Penrose form of the least-squares solution: writing Z = A0(n)Xn, the optimal B is precisely B⋆A⋆Xn · Z†. Recall Z† = Z⊤(ZZ ⊤)−1 for row full-rank Z. Substituting back, 

min  

> B

ℓ(n)(B) = 12 B⋆(n)A⋆(n) Xn (Im − P row  

> A0(n)Xn

) 2 

> F

, (21) where 

P row  

> A0(n)Xn

:= X⊤ 

> n

A0(n)⊤ A0(n) XnX⊤ 

> n

A0(n)⊤−1A0(n) Xn ∈ Rm×m (22) is the orthogonal projector onto row( A0(n)Xn) inside Rm (the sample-index space), confirming that 

B⋆(n)A⋆(n) Xn (Im − P row  

> A0(n)Xn

) is the part of B⋆A⋆Xn orthogonal to row( A0(n)Xn).Bring the residual into the parameter space via the trace identity 

B⋆A⋆Xn(I − P row  

> A0(n)Xn

) 2 

> F

= tr  (B⋆A⋆) Mn (B⋆A⋆)⊤, Mn := Xn(I − P row  

> A0(n)Xn

)X⊤ 

> n

.

(23) Setting S := XnX⊤ 

> n

and expanding P row  

> A0(n)Xn

explicitly, Mn admits the Schur complement repre-sentation 

Mn = S − S A 0(n)⊤ A0(n) S A 0(n)⊤−1 A0(n) S. (24) We assume the per-layer input distribution D(n) 

> X

produces zero-mean random vectors x ∼ D (n)

> X

satisfying the shape-aware sub-Gaussian condition (12) in Lemma 2 with parameter κ(n) = O(1) 

uniformly across layers and clients, which is the standard condition in or sample-covariance concen-tration in operator norm [ 57 ]. In specific, it is satisfied by Gaussian and log-concave distributions [57]. Throughout we treat κ(n), ∥Σ(n) 

> X

∥op , and ∥B⋆A⋆∥op as Θ(1) constants in the rate notation. Lemma 2 gives S/ (Km ) = Σ (n) 

> X

+ Op(1 /√Km ), which under the well-conditioned input regime propagates through the Schur complement to 

Mn/(Km ) = Σ ⊥ 

> X

+ Op(1 /√Km ), Σ⊥ 

> X

:= Σ (n) 

> X

−Σ(n) 

> X

A0(n)⊤ A0(n)Σ (n) 

> X

A0(n)⊤−1A0(n) Σ (n) 

> X

.

(25) Here, the Schur complement Σ⊥ 

> X

∈ Rd×d has kernel row( A0(n)) , which can be easily derived. We have  

> 1
> Km

∥B⋆A⋆Xn(I − P row  

> A0(n)Xn

)∥2 

> F

= tr  (B⋆A⋆) Σ ⊥ 

> X

(B⋆A⋆)⊤ + Op(1 /√Km ), (26) by combining (23) – (25) . Because Σ⊥ 

> X

has kernel row( A0(n)) , only the part of B⋆A⋆ outside 

row( A0(n)) contributes, i.e. B⋆A⋆(I − PA0(n)). Now, we apply Lemma 1 to 

tr  (B⋆A⋆) Σ ⊥ 

> X

(B⋆A⋆)⊤ = ∥B⋆A⋆(Σ ⊥ 

> X

)1/2∥2 

> F

= ∥B⋆A⋆(I − PA0(n))(Σ ⊥ 

> X

)1/2∥2 

> F

(27) 22 with M = B⋆A⋆(I − PA0(n)) and X = (Σ ⊥ 

> X

)1/2 and obtain 

λ+min (Σ ⊥ 

> X

) ( δ0(n)) 2 ≤ tr  (B⋆A⋆) Σ ⊥ 

> X

(B⋆A⋆)⊤ ≤ λmax (Σ ⊥ 

> X

) ( δ0(n)) 2, (28) where λ+min (Σ ⊥ 

> X

) > 0 is the smallest nonzero eigenvalue (acting on ker A0(n)) and λmax (Σ ⊥ 

> X

) ≤∥Σ(n) 

> X

∥op = O(1) . Under the well-conditioned input regime, both eigenvalue bounds are Θ(1) , so the sandwich’s two endpoints have the same order Θ(( δ0(n)) 2). Combined with the Op(1 /√Km )

correction (26), we have 

min  

> B

ℓ(n)(B) · 1 

> Km

= Θ  (δ0(n)) 2. (29) Because Rrec is layer-additive by definition , summing the per-layer Θ statements and dividing by N

gives, with no further independence assumption, 

lim   

> T→∞

ERrec (WFFA  

> T

) = Θ 

 1

> N

X

> n

(δ0(n)) 2

, (30) the implicit constant absorbing the per-layer conditioning σ2max (Xn)/σ 2min (XnA0(n)†), the correc-tion Op(1 /√Km ), and any SGD noise floor O(ησ 2 

> g

).

RoLoRA lower bound via pigeonhole. For any layer-tied schedule M t ∈ { 1A, 1B } with A-active set RA := {t ≤ T : M t = 1A} of size |R A| = ⌊T / 2⌋, consider an adversarial layer configuration where the per-layer optimal A-active sets S1, . . . , SN ⊆ [T ] partition [T ] into N equal parts of size 

T /N each (assuming N | T for clarity; the general case differs by O(1) ). Define the mismatch set for layer n as Sn \ R A, which are the rounds where layer n’s optimal protocol calls for A-active but the schedule has B active globally, so At(n) remains unchanged and δt(n) cannot contract on these rounds. Since {S n} partition [T ],

> N

X

> n=1

|R A ∩ S n| = |R A| = T / 2, (31) so by averaging, at least one layer n⋆ has |R A ∩ S n⋆ | ≤ T / (2 N ). The mismatch count for this layer is 

|S n⋆ \ R A| = |S n⋆ | − |R A ∩ S n⋆ | ≥ T /N − T / (2 N ) = T / (2 N ) = Ω( T ). (32) On each mismatch round, M t = 1B so At(n⋆) is unchanged; the contracting (matched) rounds for layer n⋆ are at most T / (2 N ), half of what AS-LoRA-style adaptive selection could achieve. In the regime where this bounded number of contractions is insufficient to drive δ to zero (e.g., finite-T

analysis or contraction ratio close to 1), δT (n⋆) ≥ (1 − ϵ) δ0(n⋆) persists for some ϵ ∈ (0 , 1) .Plugging into the per-layer Θ floor yields 

ERrec (WRoLoRA  

> T

) = Ω 

 1

> N

X

> n

(δ0(n)) 2

, (33) matching the FFA Θ floor up to the multiplicative factor (1 − ϵ)2 ∈ (0 , 1] . □

## F Proof of Corollary 1 

Fix layer n and consider a round t at which AS-LoRA activates the down-projection block A(n).Recall δt(n) = ∥B⋆(n)A⋆(n)( I − PAt(n))∥F . The per-layer reconstruction loss ℓ(n)(A, B ) =  

> 12

∥B⋆A⋆Xn − BAX n∥2 

> F

, viewed as a function of A alone (with B fixed at any value achieving the layer-n optimum given A), is convex quadratic in A with Hessian sandwich μ(n) 

> A

I ⪯ H(n) 

> A

⪯ L(n) 

> A

I

(Assumptions 1, 3). Applying Lemma 3 with step size η ≤ 1/L (n) 

> A

yields 

∥At+1 (n) − Aopt (n)∥2 

> F

≤  1 − μ(n) 

> A

/L (n)

> A

 ∥At(n) − Aopt (n)∥2 

> F

, (34) where Aopt (n) is the local minimizer at layer n and a particular gauge representative of A⋆(n). As we have seen in the proof of Theorem 2, the map A 7 → PA = A†A is locally Lipschitz on the 23 manifold of full-row-rank matrices. By Davis–Kahn–Wedin sin Θ theorem [ 60 ], for A, eA ∈ Rr×d

both row-full-rank with smallest singular values bounded below by σ > 0,

PA − P eA F = √2 sinΘ F ≤ Cσ A − eA F (35) for an absolute constant C. Applying with A = At+1 (n), eA = Aopt (n), and σ = σmin (Aopt (n)) 

gives 

∥PAt+1 (n) − PAopt (n)∥F ≤ Cσmin (Aopt (n)) ∥At+1 (n) − Aopt (n)∥F . (36) Using the fact that B⋆A⋆(I − PAopt ) = 0 (ker( A⋆) = ker( Aopt )), we have δt(n) = ∥B⋆A⋆(PAopt −

PAt )∥F , so 

δt+1 (n) ≤ ∥ B⋆A⋆∥op ∥PAopt − PAt+1 ∥F . (37) Combining (34) and (36) , using ∥B⋆A⋆∥op = Θ(1) , σmin (Aopt ) = Θ(1) under the assumption and discussion with Theorem 2, one obtains the per-layer geometric contraction 

δt+1 (n) ≤

q

1 − μ(n) 

> A

/L (n) 

> A

· δt(n) =: γn · δt(n), γn ∈ (0 , 1) . (38) On rounds where M t(n) = B instead, A(n) is unchanged, so PAt(n) and hence δt(n) are unchanged. Thus δt(n) is monotonically non-increasing across the trajectory, and contracts strictly only on 

A-active rounds. By the hypothesis of the corollary, there exists c ∈ (0 , 1) such that |{ t ≤ T : M t(n) = A}| ≥ cT for every n (with probability one for argmax, in expectation for softmax). Combining with (38) , we have 

δT (n) ≤ γcT n · δ0(n). (39) This is the geometric-decay form, where every A-active round multiplies δ by at most γn < 1, and we have at least cT such rounds. Solve γcT n δ0(n) ≤ ε for T as 

T ≥ log( δ0(n)/ε )

c log(1 /γ n) . (40) To guarantee this for every layer n simultaneously, take 

T ≥ max 

> n

log( δ0(n)/ε )

c log(1 /γ n) = O log(1 /ε ), (41) where the absolute constant hides c−1 and max n(log γ−1 

> n

)−1, both finite by the assumption of this corollary itself and Assumptions 1–3. The per-layer Θ floor derived in Theorem 2’s proof depends on the current iterate only through δ(n).Substituting δT (n) ≤ ε uniformly into the same sandwich (with ε in place of δ0(n)): 

lim   

> T→∞

ERrec (WAS  

> T

) = O(ε2), (42) where the implicit O(1) constant matches the Θ(1) constant of Theorem 2’s FFA floor (same per-layer conditioning σ2max (Xn)/σ 2min (XnA0(n)†)). Whenever δ0(n0) > ε for some layer n0, the FFA floor of Theorem 2 is Θ( 1

> N

P

> n

(δ0(n)) 2) ≥ Θ(( δ0(n0)) 2/N ) = Θ( δ0(n0)2) (treating N as Θ(1) in the rate notation), so the AS-LoRA bound O(ε2) is strictly smaller in order whenever ε ≪ δ0(n0). This proves the strict inequality. □

## G Proof of Theorem 3 

Fix layer n and block label a ∈ { A, B }. Write ∆ := L(W ) − E[L(W − η g (n) 

> a

)] and let h := η g (n)

> a

(the perturbation at the a(n) block). Apply the Taylor expansion of Lemma 4 to L as a function of the block factor a(n):

L(W − h) = L(W ) − ⟨∇ a(n) L, h ⟩ + 12 h⊤H(n) 

> a

(W )h + R, |R| ≤ LH 

> 6

∥h∥3. (43) 24 Substituting h = ηg (n) 

> a

and rearranging, 

L(W ) − L (W − ηg (n) 

> a

) = η⟨∇ a(n) L, g (n) 

> a

⟩ − η2 

> 2

(g(n) 

> a

)⊤H(n) 

> a

g(n) 

> a

− R, (44) with |R| = O(η3∥g(n) 

> a

∥3) uniformly bounded under Assumption 3. Now take expectation over the gradient noise. Decompose g(n) 

> a

= ∇a(n) L+ξ(n) 

> a

with E[ξ(n) 

> a

] = 0 and 

E∥ξ(n) 

> a

∥2 ≤ σ2 

> g

(Assumption 2). Then we have first-order term. as E[⟨∇ a(n) L, g (n) 

> a

⟩] = ∥∇ a(n) L∥ 2

by unbiasedness, and second-order term as E[( g(n) 

> a

)⊤H(n) 

> a

g(n) 

> a

] = (∇a(n) L)⊤H(n) 

> a

∇a(n) L +tr( H(n) 

> a

Σξ ) where Σξ = E[ξξ ⊤] has trΣ ξ ≤ σ2 

> g

and H(n) 

> a

⪯ βI , so tr( H(n) 

> a

Σξ ) ≤ βσ 2 

> g

.Plugging back to (44), we have 

∆ = η∥∇ a(n) L∥ 2 − η2 

> 2

(∇a(n) L)⊤H(n) 

> a

∇a(n) L − η2 

> 2

βσ 2 

> g

+ O(η3). (45) The score satisfies E[Sa(n)] = ∥∇ a(n) L∥ 2 − η 

> 2

(∇a(n) L)⊤H(n) 

> a

∇a(n) L − η 

> 2

tr( H(n) 

> a

Σξ ) + O(σ2 

> g

).Accordingly, modulo O(ησ 2 

> g

) correction which is absorbed for η ≤ 1/β ,

∆ ≥ η S a(n) − η2β 

> 2

σ2 

> g

. (46) Therefore, we can say that the greedy rule arg max a Sa(n) thus maximizes ∆ up to O(η2σ2 

> g

).Applying (46) at round t with the selected block M t(n), summing across layers n ∈ [N ] and rounds 

t ∈ [T ], and using L(WT ) ≥ L ⋆, we have 

L(W0) − L ⋆ ≥ E[L(W0)] − E[L(WT )] ≥ η

> T

X

> t=1
> N

X

> n=1

EStM t(n)(n) − η2βN T  

> 2

σ2 

> g

. (47) Decompose AS-LoRA’s chosen score against the uniform-random baseline 12 (StA(n) + StB (n)) :

StM t(n)(n) = 12

 StA(n) + StB (n) +



StM t(n)(n) − 12

 StA(n) + StB (n)

. (48) Under AS-LoRA’s argmax rule, StM t(n)(n) = max a Sta(n), so the second term has expectation exactly ∆t 

> S

(n) (the adaptive gain, defined as the gap between E[max a Sta(n)] and the uniform-baseline expectation 12 E[StA(n) + StB (n)] in the main text). Hence 

E[StM t(n)(n)] = 12 E[StA(n) + StB (n)] + ∆ t 

> S

(n). (49) To convert the score expression into a gradient-norm bound, we apply Assumption 3 to each indi-vidual block-score Sa(n). Using H(n) 

> a

⪯ L(n) 

> a

I ⪯ βI and η ≤ 1/β , the curvature term satisfies 

(g(n) 

> a

)⊤H(n) 

> a

g(n) 

> a

≤ β∥g(n) 

> a

∥2, so 

Sa(n) = ∥g(n) 

> a

∥2 − η 

> 2

λ(n) 

> a

∥g(n) 

> a

∥2 =  1 − η 

> 2

λ(n)

> a

∥g(n) 

> a

∥2 ≥ ρ⋆ ∥g(n) 

> a

∥2, (50) by definition of ρ⋆ = inf t,n (1 − η 

> 2

λ(n) 

> Mt(n)

) ≥ 1/2. Combining (49) – (50) and E∥g(n) 

> a

∥2 ≥ ∥∇ a(n)L∥ 2

(since E[g(n) 

> a

] = ∇a(n)L and variance ≥ 0), we have 

X

> n

E[StM t(n)(n)] ≥ ρ⋆

> 2

X

> n

E∥∇ M t(n)L∥ 2 + X

> n

∆t 

> S

(n). (51) Combining (47), (51) and min t≤T E∥∇L (Wt)∥2 ≤ 1

> T

P 

> t

E∥∇L (Wt)∥2, we obtain 

min  

> t≤T

E∥∇L (Wt)∥2 ≤ 2( L(W0) − L ⋆)

ηρ ⋆T − 2 ∆ S

ρ⋆ + ηβσ 2

> g

ρ⋆ , (52) by dividing by ηρ ⋆T / 2 where ∆S = 1

> T

P 

> t1
> N

P 

> n

∆t 

> S

(n). This is the non-convex bound. 

PL refinement to geometric rate. Under Assumption 4 ( ∥∇L∥ 2 ≥ 2μ(L − L ⋆)), the per-round inequality (46) sharpens from a gradient-norm lower bound to a function-value recursion. Combining (46) with (50), we have 

E[L(Wt) − L ⋆] ≤ E[L(Wt−1) − L ⋆] − ηρ ⋆ 

> 2

E∥∇L (Wt−1)∥2 + η2βσ 2 

> g
> 2

. (53) 25 Applying PL to the negative-coefficient gradient-norm term which is − ηρ ⋆ 

> 2

E∥∇L∥ 2 ≤ − ημρ ⋆ E[L− L⋆], (53) can be rewritten as 

E[L(Wt) − L ⋆] ≤ (1 − ημρ ⋆) E[L(Wt−1) − L ⋆] + η2βσ 2 

> g
> 2

. (54) This is a first-order recursion in E[L(Wt) − L ⋆], contracted by γ := 1 − ημρ ⋆ ∈ (0 , 1) each round (provided η < 1/(μρ ⋆), automatically satisfied under η ≤ 1/β in the typical regime β ≥ μρ ⋆). Considering the iteration, the first-order recusrion is extended to 

E[L(WT ) − L ⋆] ≤ (1 − ημρ ⋆)T (L(W0) − L ⋆) + ηβσ 2 

> g
> 2μρ ⋆

. (55) 

□

## H Proof of Theorem 4 

Fix layer n and let κ(H(n) 

> a

) := λmax (H(n) 

> a

)/λ min (H(n) 

> a

) denote the condition number of the per-block Hessian. Under AS-LoRA’s argmax rule M t(n) = arg max a∈{ A,B } Sa(n), the selected block is a deterministic function of the stochastic gradients {g(n) 

> a

}a∈{ A,B }. Since g(n) 

> a

is random (mini-batch noise, Assumption 2), M t(n) inherits this randomness, and we define the expected Hessian at the selected mode 

H(n) 

> t

:= EH(n) 

> Mt(n)

 = X

> a∈{ A,B }

pt(a, n ) H(n) 

> a

, (56) where pt(a, n ) := Pr M t(n) = a is the induced selection probability which is the probability that argmax picks block a, taken over the distribution of stochastic gradients. H(n) 

> t

is a convex combination of H(n) 

> A

, H (n) 

> B

with weights pt(·, n ). The score Sa(n) = ∥g(n) 

> a

∥2 − η 

> 2

(g(n) 

> a

)⊤H(n) 

> a

g(n)

> a

contains a curvature-penalty term that is large when λ(n) 

> a

= ( g(n) 

> a

)⊤H(n) 

> a

g(n) 

> a

/∥g(n) 

> a

∥2 is large, i.e. when block a has high effective curvature along the gradient direction. Hence among two blocks with comparable gradient norms, the block with smaller λ(n) 

> a

has larger Sa(n) and is therefore selected by argmax. Formally, under heterogeneity λ(n)

> A

̸ = λ(n) 

> B

, the argmax-induced selection probability satisfies pt(a⋆, n ) > 1/2 where a⋆ := arg min a λ(n) 

> a

, with the gap pt(a⋆, n ) − 1/2 strictly positive and growing with the curvature heterogeneity |λ(n) 

> A

− λ(n) 

> B

|. Accordingly, we can say that AS-LoRA favors to select a ∈ A, B with low λ(n) 

> a

, and also maximum eigenvalue of H(n) 

> a

following the observation in [ 61 ] that demonstrates that the gradient subspace aligns with the dominant directions in the Hessian .For symmetric matrices the maximum eigenvalue is convex [56], so by Jensen’s inequality 

λmax 

 H(n)

> t

 = λmax 

 Ea[H(n) 

> a

] ≤ Ea[λmax (H(n) 

> a

)] = X

> a

pt(a, n ) λmax (H(n) 

> a

). (57) Comparing AS-LoRA’s induced distribution pt against the uniform baseline, 

X

> a

pt(a, n ) λmax (H(n) 

> a

) ≤ 12 (λmax (H(n) 

> A

) + λmax (H(n) 

> B

)) = Eunif  

> a

[λmax (H(n) 

> a

)] , (58) because AS-LoRA favors low maximum eigenvalue as we discussed above. Composing, 

E[λmax (H(n),AS  

> t

)] ≤ E[λmax (H(n),static  

> t

)] , (59) strict whenever the per-block top-spectra differ. This is the SAM-style implicit-flatness statement of Theorem 4. □

## I Experimental Detail 

I.1 Differential Privacy Implementation 

We implement differentially private training using the Opacus library. To track the privacy budget, we primarily adopt the PRV (Privacy Random Variable) accountant, which provides tighter estimates 26 compared to the standard RDP accountant. For a subset of kernel-based experiments, we instead use the RDP accountant due to technical limitations in the PRV implementation under those settings. We employ Poisson subsampling using the UniformWithReplacementSampler provided by Opa-cus. Each data point is independently sampled with probability q = B/N k, where B is the expected batch size and Nk denotes the dataset size of client k. This sampling scheme ensures correct privacy accounting for DP-SGD. 

I.2 Model and LoRA Configuration 

For language tasks, we apply LoRA to all transformer layers, specifically to the query and value projection matrices. The LoRA modules are configured with a dropout rate of 0.05 and without bias terms (i.e., bias="none" ). For vision tasks, LoRA is similarly applied to the query and value projections across all layers. In addition, the classifier head is trained jointly without LoRA adaptation. 

I.3 Tokenization and Input Processing 

For GLUE tasks, the maximum sequence length is set to 128 , while for SQuAD tasks it is set to 384 .For question answering, we tokenize the input using the following setting: 

tokenizer(question, context, truncation="only_second", max_length=L, padding="max_length", return_offsets_mapping=True) 

I.4 Implementation Details 

All experiments are conducted in float32 precision to ensure numerical stability under differential privacy. We reinitialize the model, optimizer, and privacy engine at each communication round to avoid unintended state accumulation in the privacy accountant. During training, only the selected LoRA components are optimized according to the layer-wise mode configuration, while all other parameters remain frozen. 

## J Additional Experiments 

Unless otherwise specified, all experiments follow the settings described in Section 5 and Appendix I. 

J.1 Effect of random projection 

Figure 7: Comparison of LoRA-A score ratio with and without random projection across different batch sizes. To examine whether the benefit of random projection is related to channel-wise gradient outliers, we vary the batch size in DP-SGD, since larger batches can average stochastic gradient fluctuations and reduce the effect of outliers. As shown in Table 9, the performance gap between Proposed and 27 Proposed w/o RP decreases as the batch size increases. At B=32 , random projection improves QQP accuracy by 2.03 pp, while the improvement reduces to 0.79 pp at B=128 . This trend suggests that random projection is particularly effective when smaller batch sizes make channel-wise outliers less likely to be canceled through batch averaging. As the batch size increases and this implicit averaging effect becomes stronger, the additional benefit of random projection becomes smaller. Table 9: Comparison with/without random projection under DP-SGD ( ϵ = 6 ).                    

> Method QQP ( B=32) QQP ( B=64) QQP ( B=128) Proposed 63.19 ±0.01 73.99 ±1.41 79.94 ±0.11
> Proposed w/o RP 61.16 ±2.03 70.90 ±2.78 79.15 ±0.69

Figure 7 further corroborates this interpretation by visualizing the LoRA-A score ratio across different batch sizes. At small batch sizes (e.g., B=16 ), the LoRA-A score ratio under non-projection is substan-tially higher than that under random projection, indi-cating that channel-wise outliers inflate the A com-ponent’s score and cause it to be disproportionately favored in mode selection. As the batch size increases, the gap between non-projection and random projection narrows steadily, and at B=128 the two nearly coincide. This confirms that larger batches implicitly suppress outlier effects through averaging, reducing the discrepancy between the two scor-ing strategies. Taken together, these results support that the proposed random projection effectively mitigates channel-wise gradient outliers and leads to more balanced and reliable score estimation, particularly in the small-batch regime typical of DP-SGD training. 

J.2 Single-Pass vs. Two-Pass Score Computation 

Table 10: Comparison of single-pass and two-pass under DP-SGD ( ϵ = 3 ).              

> Method MNLI (m) MNLI (mm)
> single-pass (ours) 79.15 ±0.23 80.03 ±0.30
> two-pass inner-loop computation 79.37 ±0.31 80.29 ±0.26

We compare two designs for score computation that differ in when and how the scores are evaluated rela-tive to the communication rounds. In the single-pass design (ours), each client computes the scores as a by-product of local training and uploads them to the server together with the updated LoRA pa-rameters. The server then maintains EMA-smoothed scores across rounds and uses them to determine the mode vector for the next round, requiring no additional communication beyond the standard parameter exchange. As a result, the mode selection at round t is based on historically smoothed scores rather than the current global model state. In the two-pass inner-loop design, the server first broadcasts both the A and B components of the current global model to all clients. Each client then evaluates the scores for both components locally and sends them back to the server. The server aggregates the received scores and determines the mode vector based on the current global model, after which the selected components are redistributed to the clients for local training. This design enables greedy, round-wise mode selection based on the most up-to-date global model state, at the cost of an additional communication round and extra local computation per round. As shown in Table 10, the two-pass inner-loop computation achieves slightly higher performance than the single-pass design, improving MNLI (m) and MNLI (mm) by +0 .22 and +0 .26 pp, respectively. However, the performance gap is marginal, and the single-pass design avoids the additional commu-nication and computation overhead introduced by the two-pass approach. These results demonstrate that the single-pass design provides a favorable trade-off between performance and efficiency. 

J.3 Effect of aggregation rule 

Table 11: Comparison of mode selec-tion rules. Experiments are conducted under DP-SGD ( ϵ = 6 ) with B = 64 .                   

> Mode Selection Strategy MNLI (m) MNLI (mm)
> Majority voting 75.56 ±1.51 76.31 ±2.38
> Weighted averaging 75.50 ±1.58 76.60 ±1.97
> Ours (Uniform averaging) 74.48 ±0.04 75.15 ±0.35

We further study alternative rules for aggregating layer-wise mode decisions across clients. Specifically, we consider three variants: (i) majority voting , where each client inde-pendently selects its preferred mode and the server adopts the mode receiving the majority of votes; (ii) weighted av-eraging , which computes a data-size weighted average of client scores before selecting a mode; and (iii) uniform av-eraging (ours), which averages client scores uniformly and determines the mode accordingly. As shown in Table 11, majority voting and weighted averaging achieve higher performance than uniform averaging on both 28 MNLI (m) and MNLI (mm), suggesting that the aggregation rule for mode selection can meaningfully affect the quality of the selected modes. We adopt uniform averaging as the default aggregation strat-egy throughout this paper. Even with this simple aggregation, AS-LoRA outperforms prior federated LoRA methods; further gains are expected when combined with more sophisticated aggregation rules including majority rule, weighted aggregation. 

Figure 8: (a): Comparison under varying dirichlet distribution α ∈ { 0.1, 0.5, 1.0} on the MNLI dataset. (b): Comparison under varying LoRA rank r ∈ { 4, 8, 16 } on the MNLI dataset. The number of clients is fixed at K = 3 under DP-SGD ( ϵ = 6 ). 

J.4 Effect of LoRA rank and data distribution 

In Figure 8(a), the proposed method consistently achieves the best performance across all α values and shows larger gains at smaller α. This suggests stronger robustness under highly heterogeneous settings. The advantage of the proposed method is maintained across all heterogeneity levels. In Figure 8(b), performance generally improves as the LoRA rank increases. The proposed method consistently outperforms the baselines across all rank settings and maintains its advantage even at low ranks. Notably, the performance improvement of FFA-LoRA is more pronounced than that of RoLoRA as the rank increases, suggesting that FFA-LoRA benefits more from the increased expressive capacity provided by larger ranks. 

J.5 Communication and memory overhead analysis 

We analyze the additional overhead introduced by AS-LoRA. Let Mn denote the set of LoRA modules in Transformer layer n. We define 

PA =

> N

X

> n=1

X

> m∈M n

rd in ,m (n), PB =

> N

X

> n=1

X

> m∈M n

rd out ,m (n),

where PA and PB denote the number of trainable parameters in the A and B LoRA components, respectively. FedLoRA communicates PA + PB parameters per client per round, whereas FFA-LoRA communicates PB , and RoLoRA communicates either PA or PB . AS-LoRA also communicates only one active component per layer and therefore has the same order of parameter communication as alternating methods. The additional communication of AS-LoRA comes only from the score and mode information. In our implementation, all LoRA modules within the same Transformer layer share one layer-level mode decision. Thus, each client uploads two scalar scores per Transformer layer, and the server broadcasts one binary mode indicator per layer. The additional uplink and downlink costs are therefore N

floating-point values and N bits per round, respectively. For a 24-layer backbone, this corresponds to only 24 FP32 values, or 96 bytes, per client per round, which is less than 0.02% of the active LoRA parameter communication when the hidden dimension is 1024. 

J.6 Computational overhead analysis 

We analyze the computational overhead of the proposed curvature-based scoring and present practical strategies to overcome it. We note that ∥˜g∥2 is obtained as a byproduct of the standard backward pass and incurs no additional computation. 29 FLOPs accounting. Following [ 62 ], we set Cbwd ≈ 2Cfwd , so one local update step costs 3Cfwd .Over T = 100 rounds with τ = 10 local steps, the baseline FLOPs are: 

Fbase = 3 τ T = 3 × 10 × 100 = 3 ,000 Cfwd .

The proposed method uses a central-difference FD scheme requiring two forward passes per scoring target per round. With Ntarget = 24 targets (one active component per layer), the full per-round curvature scoring costs: 

Ffull = Ntarget × 2 × T = 24 × 2 × 100 = 4 ,800 Cfwd ,

yielding an overhead of 4,800 /3,000 = 160 % over the baseline, which is prohibitive for resource-constrained deployments. We propose two complementary strategies to reduce this overhead. First, replacing the central-difference scheme with a one-sided scheme: 

cta,k (n) ≈ Ltk(Wt + ϵfd vta,k (n)) − L tk(Wt)

ϵ2fd 

,

halves the per-target cost by reusing Ltk(Wt) already computed during training. Second, applying curvature scoring only every f rounds reduces the number of scoring events from T to T /f . Under both strategies with f = 10 , the curvature overhead becomes: 

Fcurv = Ntarget × 1 × Tf = 24 × 10 = 240 Cfwd ,

reducing the overhead to 240 /3,000 ≈ 8%—a 20 × reduction from the original 160% .Table 12: Comparison of curva-ture computation schedules in AS-LoRA under DP-SGD ( ϵ = 3 ).                                     

> Method MNLI (m) MNLI (mm)
> Periodic computation
> (f=5 )77.99 ±1.17 79.39 ±1.07
> (f=10 )78.83 ±1.08 79.76 ±0.80
> Late-phase computation
> (T∗=80 )78.91 ±1.11 79.87 ±0.95
> (T∗=90 )78.85 ±0.88 80.11 ±0.74
> Original
> Proposed (ours) 79.15 ±0.23 80.03 ±0.40

Scheduling strategies. We consider two scheduling variants: (i) Periodic : compute curvature scores every f ∈ { 5, 10 } rounds and use ∥˜g∥2 otherwise. (ii) Late-phase : apply ∥˜g∥2 for the first T ∗ ∈ { 80 , 90 } rounds and switch to curvature scoring thereafter, motivated by the observation that ∥˜g∥2 dominates early optimization while g⊤ 

> a

Haga becomes informative as ∥˜g∥2

diminishes near convergence. 

Analysis. As shown in Table 12, late-phase scheduling ( T ∗=80 )incurs only −0.24 degradation on MNLI (m) relative to the full method. Notably, T ∗=90 surpasses the full method on MNLI (mm) while reducing overhead from 160% to 8% (20 ×

reduction). These results confirm that full per-round curvature scoring is unnecessary: selective scheduling preserves most of the performance gain at a fraction of the computational cost. 

J.7 Effect of gradient smoothing under DP-SGD 

Table 13: Effect of gradient smoothing methods under DP-SGD ( ϵ = 3 ).                          

> Method MNLI (m) MNLI (mm)
> Proposed (w/o smoothing) 79.15 ±0.23 80.03 ±0.40
> Gaussian 78.85 ±0.14 79.74 ±0.21
> Laplacian 79.48 ±0.16 80.31 ±0.16
> EMA 77.70 ±0.19 78.65 ±0.14

We investigate whether applying smoothing to DP-SGD gradients can further improve utility, motivated by the observation that injected Gaussian noise in DP-SGD is high-frequency in nature and may be partially miti-gated by frequency-selective filtering. Specifically, we compare three smoothing strategies applied to the ag-gregated gradients: (i) a standard 5-tap Gaussian filter 

[1 /16 , 4/16 , 6/16 , 4/16 , 1/16] as proposed in [ 46 ], (ii) Laplacian smoothing [47], and (iii) EMA across communication rounds The Gaussian noise injected by DP-SGD is i.i.d. across parameters and thus behaves as white noise in the parameter space, concentrating its energy uniformly across all frequencies.In contrast, the true gradient signal is typically smooth and sparse in the frequency domain, with energy concentrated at low frequencies.In the frequency domain, Laplacian smoothing acts as: 

ˆgsmooth (ω) = ˆg(ω)1 + σλ (ω) , (60) 30 where λ(ω) denotes the eigenvalue of the graph Laplacian at frequency ω and σ > 0 is a smoothing coefficient.Since λ(ω) ≈ 0 for low frequencies, the true gradient signal passes through largely intact, whereas high-frequency noise components are strongly suppressed as λ(ω) grows. By contrast, the 5-tap Gaussian filter is a weighted average with fixed kernel [1 /16 , 4/16 , 6/16 , 4/16 , 1/16] , which attenuates all frequency components including low-frequency signal, leading to signal distortion. EMA operates along the time axis rather than the parameter space, and therefore cannot exploit the spatial structure of DP noise within a single round, making it the least compatible with the spatial nature of DP-SGD noise. Table 13 reports the effect of each smoothing method under ϵ = 3 on the MNLI matched (m) and mismatched (mm) benchmarks. The results are consistent with our frequency-domain analysis. Lapla-cian smoothing is the only method that improves over the no-smoothing baseline, gaining +0 .33 and 

+0 .28 pp on MNLI (m) and MNLI (mm), respectively. Gaussian filtering slightly degrades perfor-mance relative to the baseline ( −0.30 pp on MNLI (m)), confirming that indiscriminate attenuation of all frequency components distorts the gradient signal. EMA yields the largest degradation ( −1.45 pp on MNLI (m)), as temporal averaging across rounds is ill-suited for suppressing within-round spatial noise introduced by DP-SGD. These findings suggest that frequency-selective smoothing, specifically Laplacian smoothing, is the most compatible post-processing strategy for DP-SGD gradients. 

J.8 Analysis of Layer-wise Mode Selection Patterns. 

Figure 9 visualizes the layer-wise mode selection patterns over training rounds across different tasks, revealing several non-random and task-dependent behaviors. Overall, the proposed method does not produce uniformly alternating selections across layers; instead, each task exhibits its own characteristic layer-wise preference structure, indicating that the adaptive scores capture meaningful optimization differences between LoRA-A and LoRA-B components rather than inducing arbitrary switching behavior. For language understanding tasks such as QNLI, MNLI, SST-2, QQP, and SNLI, mode selection patterns often exhibit partially coherent structures across neighboring layers. Certain intermediate or upper layers consistently favor the same component over many rounds, while other layers switch more frequently. This suggests that the relative utility of LoRA-A and LoRA-B varies across Transformer depth, with different layers playing distinct roles in private federated adaptation depending on both task and layer. In contrast, vision tasks exhibit more mixed and fine-grained switching. On CIFAR-100 and Tiny-ImageNet, selected modes distribute more evenly across layers and rounds, with fewer large contigu-ous regions dominated by a single component. This implies that in vision tasks, the relative advantage between LoRA-A and LoRA-B varies more locally across rounds, leading to less stable layer-wise specialization than in language tasks. These observations have two key implications. First, they support the need for adaptive layer-wise mode selection, as a fixed global schedule cannot capture such heterogeneous preferences across tasks and layers. Second, recurring structures in language tasks suggest cross-layer dependencies not explicitly modeled by the current greedy formulation. Although each AS-LoRA layer independently selects its active component based on its score, the dynamics imply that groups of layers may prefer similar modes or evolve in a correlated manner over training rounds. This also highlights a limitation of the current framework. Our analysis and algorithm focus on layer-wise greedy selection, enabling tractable optimization and strong empirical performance, but do not exploit inter-layer correlations. Future work could extend AS-LoRA to a joint decision framework that models structured dependencies across layers, for example by encouraging coordinated mode assignments among related layers or learning higher-level scheduling policies over layer groups. Such extensions may improve global coordination and yield more efficient update schedules beyond the current independent layer-wise strategy. 31 (a) QNLI (b) MNLI 

(c) SST-2 (d) QQP 

(e) SNLI (f) SQuAD v1.1 

(g) SQuAD v2.0 (h) CIFAR-100 

(i) Tiny-ImageNet 

Figure 9: Layer-wise mode selection patterns across different tasks. 32