Title: A Theoretical Framework for Energy-Aware Gradient Pruning in Federated Learning

URL Source: https://arxiv.org/pdf/2603.22465v1

Published Time: Wed, 25 Mar 2026 00:08:32 GMT

Number of Pages: 8

Markdown Content:
# A Theoretical Framework for Energy-Aware Gradient Pruning in Federated Learning 

Emmanouil M. Athanasakos 

National and Kapodistrian University of Athens Department of Informatics and Telecommunications 

Athens, Greece emathan@di.uoa.gr 

Abstract —Federated Learning (FL) is constrained by the communication and energy limitations of decentralized edge devices. While gradient sparsi-fication via Top-K magnitude pruning effectively reduces the communication payload, it remains in-herently energy-agnostic. It assumes all parameter updates incur identical downstream transmission and memory-update costs, ignoring hardware re-alities. We formalize the pruning process as an energy-constrained projection problem that accounts for the hardware-level disparities between memory-intensive and compute-efficient operations during the post-backpropagation phase. We propose Cost-Weighted Magnitude Pruning (CWMP), a selection rule that prioritizes parameter updates based on their magnitude relative to their physical cost. We demonstrate that CWMP is the optimal greedy solution to this constrained projection and provide a probabilistic analysis of its global energy efficiency. Numerical results on a non-IID CIFAR-10 bench-mark show that CWMP consistently establishes a superior performance-energy Pareto frontier com-pared to the Top-K baseline. 

Index Terms —Federated learning, sparsification, energy efficiency, hardware-aware optimization. 

I. I NTRODUCTION 

Federated Learning (FL) has emerged as the definitive paradigm for privacy-preserving de-centralized optimization, enabling collaborative model training without the exfiltration of sensitive local data [1]. As neural architectures scale in complexity, the research focus has shifted toward sustainability [2], where resource efficiency is pri-oritized alongside model performance. In wireless edge environments, FL is bottlenecked by the dual constraints of limited communication bandwidth and finite battery life [3]. To mitigate communication overhead, gradient sparsification has become the de facto standard [4], [5]. Techniques such as Deep Gradient Com-pression [6] and single-shot pruning [7] rely pri-marily on magnitude-based selection (Top-K) to reduce the update entropy. While effective for bandwidth reduction, these methods are inherently energy-agnostic: they assume a uniform counting measure for the cost of model updates, implicitly treating every parameter as having an identical thermodynamic footprint. A recent comprehensive survey of energy effi-ciency in FL [8] reveals a rich landscape of high-level orchestration strategies, including Lyapunov-based resource allocation [9], broadband analog aggregation [10], and adaptive client selection [11]. Recently in [12] the authors reduced the total energy footprint of AIoT networks through clustering-informed client selection. While these works address system-level heterogeneity [13], [14], the underlying sparsification mechanism re-main primitive. Recent results in the information geometry of local generalization dynamics [15] and natural gradient descent [16] suggest that the true utility of a parameter update is governed by the local curvature of the loss landscape. In resource-constrained systems, this utility must be weighed against the physical cost of acquisition. Drawing inspiration from the Information Bottle-neck principle [17] and the Lottery Ticket Hypoth-

> arXiv:2603.22465v1 [cs.LG] 23 Mar 2026

esis [18], we argue that an optimal update strategy must formally co-optimize for informational rele-vance and physical energy. In this work, we address the energy-agnosticism of standard pruning by formalizing the update pro-cess as an energy-constrained projection problem. We introduce a computational energy measure to model the hardware-level cost disparity between memory-intensive and compute-efficient parame-ters during the post-backpropagation phase. The core contributions are:  

> •

We define a framework for energy-aware learning, characterizing parameter transmis-sion and memory-update costs as atoms of a discrete energy measure on the parameter index set.  

> •

We derive CWMP, a selection rule that ranks parameters by their efficiency density . We prove that CWMP is the optimal greedy so-lution to the resource-constrained projection.  

> •

Through numerical experiments on a non-IID CIFAR-10 benchmark, we demonstrate that CWMP establishes a superior Performance-Energy Pareto frontier and follows a conver-gence trajectory nearly identical to the dense-informed Top-K. II. P ROBLEM FORMULATION 

We begin by formalizing the FL process and the standard method of gradient sparsification. We then introduce a model for computational cost, revealing a critical oversight in current energy-agnostic pruning techniques. Let (Ω , F, P) be a probability space. We consider the collaborative optimization of a global param-eter w ∈ Rd. The objective is to minimize a global risk functional F (w) ≜ PKk=1 pkFk(w),where each local loss Fk(w) ≜ R ℓ(w; ξ)dDk(ξ)

is defined with respect to a local data measure 

Dk on a client k. The standard approach for this task is Federated Averaging (FedAvg) [1]. Let 

{F t}t≥0 be the filtration representing the history of the learning process up to round t. The protocol proceeds as follows: 1) The server broadcasts the current global parameters w(t) to the clients. 2) Each client k computes a local update via Stochastic Gradient Descent (SGD) on its local measure Dk. This yields a local gradi-ent estimate g(t) 

> k

, which is an Ft-measurable random variable. 3) Clients transmit a processed version of their gradients, ˜g(t) 

> k

, back to the server. 4) The server updates the global state: 

w(t+1) = w(t) − η PKk=1 pk ˜g(t) 

> k

.This formulation acknowledges that the local up-dates are stochastic realizations of the underlying local measures, where the non-IID nature of the data is captured by the divergence between the measures {D k}. To mitigate communication bot-tlenecks in Step 3, clients apply a sparsification operator. We formalize this as a projection ΠS :

Rd → Rd onto the coordinate subspace defined by a support set S ⊂ { 1, . . . , d } of cardinality k ≪ d.The sparse update is given by: 

˜g(t) 

> k

= Π S (g(t) 

> k

) = m(t) 

> k

⊙ g(t) 

> k

, (1) where m(t) 

> k

∈ { 0, 1}d is a binary pruning mask with || m(t) 

> k

|| 0 = k. The standard for generating this mask is Top-K magnitude pruning. Let I(t) 

> k

be the set of indices corresponding to the k elements in g(t) 

> k

with the largest absolute values. The Top-K mask is defined as m(t) 

> k,j

= 1 if j ∈ I(t) 

> k

,and 0 otherwise. This rule is effective for com-munication efficiency but is energy-agnostic: it assumes that every parameter wj incurs an iden-tical physical cost to compute and transmit. The subsequent post-backpropagation phases (payload index-encoding, wireless transmission, and down-stream memory-update accesses at the aggregator) exhibit severe hardware-level cost disparities that Top-K ignores. To address this, we introduce a computational energy measure μ on the power set of parameter indices to model these bottlenecks. For any S, μ(S) quantifies the physical energy consumed to process the selected components. This measure is characterized by its density with respect to the counting measure, represented by a parameter cost vector c ∈ Rd

> +

, such that cj

represents the physical cost of the j-th parameter. The total energy footprint of an update ˜g(t) 

> k

is modeled as: 

E(˜ g(t) 

> k

) ≜ μ(S) = 

> d

X

> j=1

m(t) 

> k,j

cj = m(t)

> k
> ⊤

c. (2) Under Top-K pruning, the communication cost 

|| m(t) 

> k

|| 0 is fixed, but the resulting energy E(˜ g(t) 

> k

)is an uncontrolled, stochastic quantity. This leads to suboptimal resource allocation, where a client expends its finite energy budget on gradient com-ponents with high magnitude but poor informa-tional utility relative to their physical processing cost. This motivates the following optimization problem: 

S∗ = arg max 

> S⊆{ 1,...,d }

ΠS (g(t) 

> k

) 1

(3) subject to |S| ≤ ksparsity , (C1) 

μ(S) ≤ Ebudget . (C2) Solving this requires a selection rule that considers the density of the gradient power with respect to the energy measure μ, leading to the algorithm proposed in the following section. III. C OST -W EIGHTED MAGNITUDE PRUNING 

The formulation in Section II implies that an op-timal pruning strategy must maximize the utility of the update signal relative to the physical resources consumed. A gradient component, however large in magnitude, may represent a suboptimal use of the communication budget if its underlying computational cost is disproportionately high. This motivates a selection process governed by update efficiency, which is the ratio of its local contribu-tion to the learning dynamics versus its associated energy cost. While the exact contribution of a parameter to generalization is determined by the local information geometry [15], the gradient mag-nitude |g(t) 

> k,j

| serves as a robust and tractable first-order proxy for the update’s informational utility. To this end, we introduce the efficiency score ,defined as the density of the gradient magnitude with respect to the cost measure μ:

s(t) 

> k,j

≜ |g(t) 

> k,j

|

cj

, (4) where cj > 0 is the parameter cost atom. This score normalizes the importance of each gradient component by its physical expense. In this regime, a component must exhibit a significantly higher magnitude to be selected if it resides in a high-cost region of the parameter space (e.g., within a massive, memory-intensive fully-connected layer). Using this score, we propose CWMP. The selec-tion process remains analogous to standard spar-sification, but the ranking criterion is replaced by (4). Let J (t) 

> k

be the set of indices corresponding to the k largest efficiency scores {s(t) 

> k,j

}dj=1 . The CWMP pruning mask is formally defined as: 

m(t) 

> k,j

=

(

1 if j ∈ J (t)

> k

0 otherwise (5) The client-side procedure is detailed in Alg. 1. CWMP is a generalization of Top-K pruning; for a 

Algorithm 1 CWMP  

> 1:

Require: Global state w(t), local measure Dk,budget k, Cost Vector c. 

> 2:

Client training:  

> 3:

gk ← LocalSGD (w(t), Dk) ▷ Estimate 

Ft-measurable gradient  

> 4:

sk ← | gk| ⊘ c ▷ Compute efficiency scores (element-wise)  

> 5:

Jk ← TopKIndices (sk, k ) ▷ Rank by efficiency density  

> 6:

mk ← ConstructMask (Jk) 

> 7:

˜gk ← mk ⊙ gk ▷ Generate energy-aware projection  

> 8:

return ˜gk to server uniform cost measure ( c = α1), the score sk,j be-comes proportional to |gk,j |, causing the CWMP selection to converge to the standard Top-K mask. For non-uniform costs, CWMP reallocates the communication budget towards parameters with higher arithmetic intensity and lower memory-access energy. 

A. Theoretical Justification 

Problem in (3) is a multidimensional 0/1 Knap-sack problem, which is strongly NP-hard. To derive a computationally tractable O(d log k) se-lection rule, we isolate the energy-constrained subproblem. The following lemma establishes the optimal scoring metric for maximizing gradient mass under energy budget. 

Lemma 1. Let g ∈ Rd be a gradient vector and c ∈ Rd 

> +

be a strictly positive cost vector. Consider the energy-constrained gradient mass maximization over a support set S ⊆ { 1, . . . , d }:

max 

> S

X

> j∈S

|gj | subject to X

> j∈S

cj ≤ Ebudget , (6) 

where Ebudget > 0. The optimal selection policy for the continuous relaxation of (6) is to rank and select parameters in descending order of the efficiency score sj = |gj |/c j .Proof: The proof is presented in Appendix A. Lemma 1 establishes sj as the theoretically optimal density for the energy-constrained re-laxation. To satisfy the joint constraints of (3), CWMP employs this optimal density as the rank-ing criterion while applying a hard truncation at the k-th index. This ensures compliance with (C1) while providing a high-fidelity greedy approxima-tion to the multidimensional optimum. The follow-ing proposition characterizes the expected global energy profile of this selection rule compared to the energy-agnostic baseline. 

Proposition 2. Let the gradient components 

{| gj |} dj=1 and parameter costs {cj }dj=1 be drawn from independent distributions. For a fixed com-munication budget k, let mTopK and mCWMP be the pruning masks generated by the Top-K and CWMP algorithms, respectively. The expected computa-tional energy cost satisfies: 

E m⊤

> CWMP

c ≤ E m⊤

> TopK

c , (7) 

where equality holds if and only if the cost mea-sure μ is the counting measure. Proof: The proof is presented in Appendix B. This result provides the theoretical justification for the empirical results that follow. By construction, CWMP is biased toward selecting indices with high information density per unit of energy, en-suring that the device’s energy budget is allocated only to the most efficient gradient updates. IV. E XPERIMENTAL VALIDATION 

We evaluate our method on the CIFAR-10 im-age classification task and utilize a ResNet-18 architecture [19] adapted for 32 × 32 inputs. 

a) Federated Configuration: We simulate a federation of K = 10 clients with statistical heterogeneity modeled via a Dirichlet distribution with concentration α = 0 .5. The global model is trained for T = 50 rounds. Each client performs local optimization using SGD with a learning rate of 0.05, momentum of 0.9, and a batch size of 64. 

b) Memory-Centric Cost Model: Vector c is defined based on the physical energy disparity between memory access and computation in edge hardware [20]. Generating the dense gradient re-quires a full backward pass; therefore, our cost model explicitly targets the post-backpropagation bottlenecks. In modern CMOS accelerators, the energy cost of a DRAM fetch—required for the massive, non-reused weights in the classi-fier—dominates the budget [21]. Conversely, con-volutional filters exhibit high arithmetic intensity and SRAM reuse [22]. We thus define: 

cj =

(

5.0 if wj ∈ Classifier Layers 

1.0 if wj ∈ Feature Extr. (Conv) Layers (8) Measure μ decouples the informational utility of a parameter from its physical processing cost, allowing us to evaluate the impact of constraint (C2). 

c) Baselines: We compare CWMP against Top-K Magnitude Pruning, which serves as the energy-agnostic baseline. Both methods are eval-uated across a range of sparsity levels k ∈{1% , 5% , 10% , 20% } to trace the Pareto frontier. 0.00 0.25 0.50 0.75 1.00 1.25 1.50 1.75 2.00    

> Average Energy Cost [(S)] 1e6
> 64
> 65
> 66
> 67
> 68
> Test Accuracy (%)
> 1%
> 1%
> 5%
> 5%
> 10%
> 10%
> 20%
> 20%
> Performance-Energy Pareto Frontier
> CWMP (Ours)
> Top-K (Baseline)

Fig. 1: Performance-Energy Pareto Frontier. CWMP is the dominant strategy in the extreme scarcity regime (1%) and maintains a superior accuracy-per-energy pro-file as the budget increases, avoiding the performance regression observed in Top-K. 

As illustrated in Fig. 1, at a 1% communication budget, CWMP acts as a strictly dominant strat-egy. It achieves a test accuracy of 64.56% while incurring a normalized energy cost of 0.06 × 10 6

units. In contrast, the energy-agnostic Top-K base-line requires 0.09 × 10 6 energy units—a 48% increase—to achieve a lower accuracy of 64.11%. This confirms that when resources are most scarce, energy-blind pruning wastes the budget on high-cost updates that offer marginal generalization returns. At the 20% sparsity level, we observe a divergence in the behavior of the two strate-gies. While the Top-K baseline incurs a signif-icant increase in computational cost, its test ac-curacy exhibits a slight regression (from 68.01% to 67.65%). In this specific architectural setup, this suggests that magnitude-based selection may prioritize high-variance updates in the massive classifier layers, leading to marginal overfitting. Conversely, the CWMP trajectory remains robust, reaching a peak accuracy of 68.49%. This sug-gests that the cost-weighted selection rule acts as an implicit structural regularizer. By impos-ing a higher ”price” on the memory-intensive parameters of the fully-connected layers, CWMP naturally shifts the learning focus toward the more compact and robust features in the convolutional layers. While these results are observed within the context of ResNet-18, they highlight the potential for energy-awareness to serve as a functional prior in resource-constrained learning. Fig. 2 confirms that incorporating the cost mea-sure does not destabilize the stochastic dynamics. CWMP follows a convergence trajectory nearly identical to the dense-informed Top-K, reaching the 70% accuracy threshold within 15 rounds,but with a significantly lower cumulative energy foot-print. 10 20 30 40 50 

> Communication Rounds
> 60
> 62
> 64
> 66
> 68
> 70
> Test Accuracy (%)
> FL Convergence at 10% Sparsity
> CWMP (Ours)
> Top-K (Baseline)

Fig. 2: Convergence Dynamics at 10% Sparsity. CWMP matches the convergence rate of the Top-K baseline while achieving a higher accuracy, demonstrat-ing a more efficient allocation of the fixed communica-tion budget. 

V. C ONCLUSION AND FUTURE WORK 

In this work, we addressed the energy-agnosticism of standard gradient sparsification in FL. By formalizing the update process as an energy-constrained projection, we proposed a se-lection rule that prioritizes parameters based on their efficiency density rather than raw magnitude. Empirical evaluations on a non-IID CIFAR-10 benchmark demonstrated that CWMP establishes a strictly superior performance-energy Pareto fron-tier, acting as an implicit regularizer that pre-vents the model from over-allocating resources to high-cost, noisy updates. Our evaluation isolates this sparsification primitive to highlight the pure impact of energy-awareness. Because CWMP is entirely orthogonal to error compensation mecha-nisms, it serves as a replacement for the standard Top-K operator. It can be readily integrated into Error-Feedback frameworks [6] to further enhance the Pareto efficiency of modern federated optimiz-ers. Ultimately, the success of this cost-aware pro-jection demonstrates that physical energy metrics are not merely peripheral constraints, but func-tional priors that guide the model toward more robust representations. This motivates a deeper, information-theoretic investigation to derive prov-ably optimal compression strategies that formally co-optimize learning performance, communication bandwidth, and hardware-level energy expendi-ture. REFERENCES [1] B. McMahan, E. Moore, D. Ramage, S. Hampson, and B. A. y Arcas, “Communication-efficient learning of deep networks from decentralized data,” in AISTATS . PMLR, 2017, pp. 1273–1282. [2] R. Schwartz, J. Dodge, N. A. Smith, and O. Etzioni, “Green AI,” Communications of the ACM , vol. 63, no. 12, pp. 54–63, 2020. [3] D. G¨ und¨ uz, Z. Qin, I. E. Aguerri, H. S. Dhillon, Z. Yang, A. Yener, K. K. Wong, and C.-B. Chae, “Beyond trans-mitting bits: Context, semantics, and task-oriented com-munications,” IEEE JSAC , vol. 41, no. 1, pp. 5–41, 2022. [4] A. F. Aji and K. Heafield, “Sparse communication for distributed gradient descent,” in EMNLP , 2017, pp. 440– 447. [5] S. U. Stich, J.-B. Cordonnier, and M. Jaggi, “Sparsified sgd with memory,” in NeurIPS , vol. 31, 2018. [6] Y. Lin, S. Han, H. Mao, Y. Wang, and B. Dally, “Deep gradient compression: Reducing the communica-tion bandwidth for distributed training,” in ICML , 2018. [7] N. Lee, T. Ajanthan, and P. H. Torr, “SNIP: Single-shot network pruning based on connection sensitivity,” in ICLR , 2019. [8] S. Sezgin, K. Mokrani, J. Jacques, and S. Allio, “Energy efficiency in federated learning: A survey on models, strategies and perspectives,” HAL Preprint hal-05037784, 2025. [9] Z. Yang, M. Chen, W. Saad, C. S. Hong, and M. Shikh-Bahaei, “Energy-efficient federated learning on wireless communication networks,” IEEE TWC , vol. 20, no. 7, pp. 4617–4629, 2021. [10] G. Zhu, Y. Wang, and K. Huang, “Broadband analog aggregation for low-latency federated edge learning,” 

IEEE TWC , vol. 19, no. 1, pp. 491–506, 2019. [11] S. Wang, T. Tuor, T. Salonidis, K. K. Leung, C. Makaya, T. He, and K. Chan, “Adaptive federated learning in resource constrained edge computing systems,” IEEE JSAC , vol. 37, no. 6, pp. 1205–1221, 2019. [12] R. Pereira, F. Fam´ a, C. Kalalas, and P. Dini, “Energy-efficient federated learning for AIoT using clustering methods,” IEEE Internet of Things Journal , 2025. [13] T. Li, A. K. Sahu, M. Zaheer, M. Sanjabi, A. Talwalkar, and V. Smith, “Federated optimization in heterogeneous networks,” in PMLR , vol. 2, 2020, pp. 429–450. [14] S. P. Karimireddy, S. Kale, M. Mohri, S. Reddi, S. Stich, and A. T. Suresh, “SCAFFOLD: Stochastic controlled averaging for federated learning,” in ICML . PMLR, 2020, pp. 5132–5143. [15] E. M. Athanasakos, “The information geometry of local generalization dynamics,” in AISTATS , vol. PMLR, 2026. [16] S.-I. Amari, “Natural gradient works efficiently in learn-ing,” N. computation , vol. 10, no. 2, pp. 251–276, 1998. [17] G. Chechik and N. Tishby, “Extracting relevant structures with side information,” in NeurIPS , S. Becker, S. Thrun, and K. Obermayer, Eds., 2003, pp. 689–696. [18] J. Frankle and M. Carbin, “The lottery ticket hypothesis: Finding sparse, trainable neural networks,” in ICLR ,2019. [19] K. He, X. Zhang, S. Ren, and J. Sun, “Deep residual learning for image recognition,” in IEEE CVPR , 2016, pp. 770–778. [20] M. Horowitz, “1.1 Computing’s energy problem (and what we can do about it),” in 2014 IEEE ISSCC . IEEE, 2014, pp. 10–14. [21] S. Han, H. Mao, and W. J. Dally, “Deep compression: Compressing deep neural networks with pruning, trained quantization and huffman coding,” in ICLR , 2016. [22] V. Sze, Y.-H. Chen, T.-J. Yang, and J. S. Emer, “Effi-cient processing of deep neural networks: A tutorial and survey,” Proceedings of the IEEE , vol. 105, no. 12, pp. 2295–2329, 2017. [23] J. F. C. Kingman, Mathematical Foundations of the General Theory of Random Sets . Academic Press, 1978. [24] S. Boucheron, G. Lugosi, and P. Massart, Concentration Inequalities: A Nonasymptotic Theory of Independence .Oxford University Press, 02 2013. 

APPENDIX APROOF OF LEMMA 1

Proof: We represent the selection of the support set S via a binary mask m ∈ { 0, 1}d,where mj = 1 if j ∈ S and 0 otherwise. To identify the optimal selection rule, we consider the continuous relaxation where mj ∈ [0 , 1] . The resulting optimization problem is a linear program (LP): 

max 

> m∈Rd
> d

X

> j=1

mj |gj | (9) subject to 

> d

X

> j=1

mj cj ≤ Ebudget , (10) 

0 ≤ mj ≤ 1, ∀j ∈ { 1, . . . , d }.

(11) Let λ ≥ 0 be the Lagrange multiplier associated with the energy constraint (10). Let αj ≥ 0 and 

βj ≥ 0 be the multipliers for the lower bound (−mj ≤ 0) and upper bound ( mj − 1 ≤ 0)constraints of (11), respectively. The Lagrangian is given by 

L(m,λ, α, β)=

> d

X

> j=1

mj |gj | − λ



> d

X

> j=1

mj cj − Ebudget 



+

> d

X

> j=1

αj mj −

> d

X

> j=1

βj (mj − 1) . (12) The stationarity condition for each component mj

is: 

∂L

∂m j

= |gj | − λc j + αj − βj = 0 =⇒ | gj | − λc j = βj − αj . (13) From the complementary slackness, the following conditions hold for all j:

αj m∗ 

> j

= 0 , (14) 

βj (m∗ 

> j

− 1) = 0 . (15) We now analyze the possible states of the optimal allocation m∗ 

> j

based on the stationarity condition (13) and complementary slackness: 1) Case m∗ 

> j

= 1 (Fully selected): From (14), we must have αj = 0. Since βj ≥ 0,equation (13) implies |gj | − λc j = βj ≥ 0.Dividing by the strictly positive cost cj , we obtain: |gj |

cj

≥ λ. (16) 2) Case m∗ 

> j

= 0 (Not selected): From (15), we must have βj = 0 . Since αj ≥ 0, equation (13) implies |gj | − λc j = −αj ≤ 0. Thus: 

|gj |

cj

≤ λ. (17) 3) Case 0 < m ∗ 

> j

< 1 (Fractionally selected): From (14) and (15), we must have αj = 0 

and βj = 0. Equation (13) therefore re-quires: 

|gj |

cj

= λ. (18) These conditions dictate that the optimal con-tinuous allocation m∗ 

> j

is entirely determined by comparing the ratio |gj |/c j against the optimal dual variable λ. To satisfy the constraints while maximizing the objective, the optimal policy must assign m∗ 

> j

= 1 to parameters with the highest 

|gj |/c j ratios, iteratively lowering the threshold λ

until the energy budget is exhausted. Therefore, selecting parameters in descending order of the efficiency score sj = |gj |/c j precisely matches the optimal greedy policy for the relaxed energy-constrained projection problem and that completes the proof. APPENDIX BPROOF OF PROPOSITION 2

Proof: We model the gradient magnitudes 

Gj ≜ |gj | and the parameter costs Cj ≜ cj

as sequences of i.i.d. random variables drawn from continuous, strictly positive distributions FG

and FC , respectively. Furthermore, we assume 1

Gj and Cj are independent of each other for all j ∈ {1, . . . , d }. Let k be the fixed com-munication budget (sparsity level). The selection policies for Top-K and CWMP define binary masks mTopK , mCWMP ∈ { 0, 1}d subject to the cardinality constraint Pdj=1 mj = k. The Top-K algorithm selects indices based solely on the gradient magnitude Gj . The mask is defined via the indicator function: 

mTopK  

> j

= I {Gj ≥ τG} , (19) where τG is the (d − k + 1) -th order statistic of the sequence {G1, . . . , G d}.Because the selection threshold τG and the sequence Gj are entirely independent of the hard-

> 1While architectural priors in deep networks often induce correlations between gradient magnitudes and layer-wise costs, this assumption establishes a baseline analytical bound by mathematically isolating the energy savings attributable strictly to the algorithmic selection rule.

ware costs Cj , the random variables mTopK  

> j

and Cj

are independent. Therefore, the expected energy cost for a single parameter is separable: 

E

h

mTopK  

> j

Cj

i

= E

h

mTopK 

> j

i

E [Cj ] . (20) By the exchangeability of the i.i.d. variables, the joint distribution is invariant under any per-mutation of the indices [23]. Consequently, the marginal probability P(j ∈ S) must be identical for all j ∈ {1, . . . , d }. Given the cardinality constraint Pdj=1 E[mTopK  

> j

] = k, it follows that 

E[mTopK  

> j

] = kd . Summing over all d yields the baseline expected energy: 

E (mTopK )⊤c =

> d

X

> j=1

E

h

mTopK  

> j

Cj

i

= kE[Cj ].

(21) The CWMP algorithm selects indices based on the efficiency score Sj ≜ Gj /C j . The mask is defined as: 

mCWMP  

> j

= I {Sj ≥ τS } , (22) where τS is the (d − k + 1) -th order statistic of the sequence {S1, . . . , S d}. Unlike Top-K, the selection variable mCWMP  

> j

is strongly coupled to the cost Cj . To isolate this dependency, we define the conditional selection probability given a fixed cost realization Cj = c:

ϕ(c) ≜ E mCWMP  

> j

| Cj = c

= P

 Gj

c ≥ τS | Cj = c



. (23) Because the ratio Gj /c is strictly decreasing with respect to c, for Gj > 0, a higher cost c low-ers the efficiency score Sj , making it less likely to exceed the top-k threshold τS . Consequently, 

ϕ(c) is monotonically decreasing with respect to 

c. The expected energy of the j-th parameter under CWMP is: 

E mCWMP  

> j

Cj

 = ECj

E mCWMP  

> j

Cj | Cj

 

= ECj [Cj ϕ(Cj )] . (24) We evaluate the expectation (24) utilizing Cheby-shev’s association inequality [24]. Consider the random variable Cj . The identity function f (c) = c is strictly monotonically in-creasing, while the conditional probability func-tion ϕ(c) is monotonically decreasing. The covari-ance of two functions with opposite monotonici-ties applied to the same random variable is strictly non-positive: Cov (Cj , ϕ (Cj )) ≤ 0. (25) Expanding the covariance formulation yields: 

E [Cj ϕ(Cj )] − E[Cj ]E[ϕ(Cj )] ≤ 0=⇒ E [Cj ϕ(Cj )] ≤ E[Cj ]E[ϕ(Cj )] .

(26) By the law of total expectation, E[ϕ(Cj )] =

E[mCWMP  

> j

] and by the symmetry of the selection 

P mCWMP  

> j

= k, we maintain E[mCWMP  

> j

] = kd .Therefore: 

E mCWMP  

> j

Cj

 ≤ kd E[Cj ]. (27) Summing this upper bound across all d gives: 

E (mCWMP )⊤c ≤ kE[Cj ]. (28) Substituting the Top-K baseline derived in (21) concludes the proof: 

E (mCWMP )⊤c ≤ E (mTopK )⊤c . (29) The equality holds if and only if Cov (Cj , ϕ (Cj )) = 0. Since f (c) = c is strictly increasing, the covariance is zero if and only if the random variable Cj is a constant almost surely. Thus equality is achieved when the parameter cost vector c is uniform, corresponding to the unweighted counting measure.