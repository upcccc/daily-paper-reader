---
title: A Unifying Primal-Dual Proximal Framework for Distributed Nonconvex Optimization
title_zh: 分布式非凸优化的统一原对偶近端框架
authors: "Zichong Ou, Jie Lu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09524v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 具有私有本地目标的分布式非凸优化框架
tldr: 本研究针对无向网络中的分布式非凸优化问题，提出了一个统一的原始-对偶近端（UPP）框架。该框架通过线性化增广拉格朗日函数并引入时变近端项，整合了多种一阶和二阶方法。基于此框架，作者推导出UPP-MC和UPP-SC两种实现，并证明了其在非凸平滑问题下的次线性收敛性及在P-Ł条件下的线性收敛性。此外，通过引入切比雪夫加速，进一步优化了通信效率，实验证明其性能优于现有主流算法。
selection_source: fresh_fetch
motivation: 旨在解决分布式环境下非凸优化问题的复杂性，并统一现有的多种一阶和二阶优化方法。
method: 提出了UPP框架，通过线性化增广拉格朗日函数并结合时变近端项，衍生出具有不同通信策略的UPP-MC和UPP-SC算法。
result: 证明了算法在非凸平滑问题上的次线性收敛性，并在P-Ł条件下实现了线性收敛，同时通过切比雪夫加速达到了最优通信复杂度。
conclusion: 该框架不仅为现有方法提供了更强的理论保证，还通过新算法在收敛速度和通信效率上显著超越了现有最先进技术。
---

## 摘要
我们考虑无向网络上的分布式非凸优化问题，其中每个节点私有地拥有其局部目标函数，并仅与其相邻节点通信，旨在共同实现一个全局最优解。为了处理目标的非凸性，我们对增广拉格朗日函数进行线性化，并引入一个随时间变化的近端项。这种方法产生了一个统一原对偶近端（UPP）框架，该框架统一了多种现有的一阶和二阶方法。基于该框架，我们进一步推导了两种具有不同通信策略的具体实现，即 UPP-MC 和 UPP-SC。我们证明了 UPP-MC 和 UPP-SC 均能以次线性速率达到非凸平滑问题的驻点解。此外，在附加的 Polyak-Łojasiewics (P-Ł) 条件下，UPP-MC 线性收敛至全局最优解。这些收敛结果为许多可被视为 UPP-MC 或 UPP-SC 特例的现有方法提供了新的或改进的保证。为了进一步优化混合过程，我们将切比雪夫加速引入 UPP-SC，得到了 UPP-SC-OPT，它达到了最优通信复杂度界限。在多种网络拓扑结构上的广泛实验表明，我们提出的算法在收敛速度和通信效率方面均优于现有最先进的方法。

## Abstract
We consider distributed nonconvex optimization over an undirected network, where each node privately possesses its local objective and communicates exclusively with its neighboring nodes, striving to collectively achieve a common optimal solution. To handle the nonconvexity of the objective, we linearize the augmented Lagrangian function and introduce a time-varying proximal term. This approach leads to a Unifying Primal-Dual Proximal (UPP) framework that unifies a variety of existing first-order and second-order methods. Building on this framework, we further derive two specialized realizations with different communication strategies, namely UPP-MC and UPP-SC. We prove that both UPP-MC and UPP-SC achieve stationary solutions for nonconvex smooth problems at a sublinear rate. Furthermore, under the additional Polyak-Łojasiewics (P-Ł) condition, UPP-MC is linearly convergent to the global optimum. These convergence results provide new or improved guarantees for many existing methods that can be viewed as specializations of UPP-MC or UPP-SC. To further optimize the mixing process, we incorporate Chebyshev acceleration into UPP-SC, resulting in UPP-SC-OPT, which attains an optimal communication complexity bound. Extensive experiments across diverse network topologies demonstrate that our proposed algorithms outperform state-of-the-art methods in both convergence speed and communication efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Unifying Primal-Dual Proximal Framework for Distributed Nonconvex Optimization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在无向网络中高效地解决分布式非凸优化问题。即多个节点协作最小化一个全局目标函数（各节点局部目标函数之和），且每个节点仅能与邻居通信。
*   **研究动机**：
    *   **缺乏统一框架**：现有的分布式算法（如 EXTRA, DIGing, ADMM 等）大多针对凸优化设计，虽然有一些针对非凸问题的改进，但缺乏一个能同时涵盖一阶和二阶方法的统一理论框架。
    *   **通信效率低下**：在稀疏网络中，传统的“一次计算-一次通信”模式会导致严重的通信瓶颈。
    *   **理论保证不足**：许多现有非凸算法的收敛性分析不够深入，尤其是在放宽强凸性条件（如 P-Ł 条件）下的全局收敛性。

### 2. 方法论
*   **核心思想**：提出了 **UPP (Unifying Primal-Dual Proximal)** 框架。该框架通过线性化增广拉格朗日函数（Augmented Lagrangian），并引入一个**随时间变化的近端项（Time-varying proximal term）**来保证非凸环境下的稳定性。
*   **关键技术细节**：
    *   **框架构建**：通过调整近端矩阵 $G_k$ 和权重矩阵 $D_k, \tilde{D}_k$，UPP 可以演化为不同的算法。
    *   **UPP-MC（多内环通信）**：将 $G_k$ 设计为图拉普拉斯矩阵的多项式，主要统一了一阶方法（如 L-ADMM, Prox-GPDA 等）。
    *   **UPP-SC（单内环通信）**：采用块对角矩阵 $G_k$，允许节点利用局部二阶信息（如 Hessian 矩阵），从而统一了二阶方法（如 DQM, SoPro）。
    *   **切比雪夫加速（Chebyshev Acceleration）**：在 UPP-SC 的基础上引入切比雪夫多项式来优化信息混合过程，推导出 **UPP-SC-OPT** 算法。
*   **算法流程**：
    1.  **原始更新**：基于当前梯度的线性近似和近端项更新局部变量。
    2.  **混合加速**：利用 Oracle A（多项式混合）或 Oracle A'（切比雪夫加速）进行多轮邻居间的数据交换。
    3.  **对偶更新**：根据约束残差更新对偶变量。

### 3. 实验设计
*   **实验场景**：分布式二分类问题，采用带有非凸正则化项（如 $x^2/(1+x^2)$）的逻辑回归模型。
*   **网络拓扑**：测试了四种不同稀疏度的图结构：环形图（Ring）、网格图（Grid）、正则图（Regular）和几何图（Geometric）。
*   **对比方法（Benchmarks）**：
    *   **一阶方法**：L-ADMM, SUDA, Prox-GPDA, D-GPDA, ADAPD-OG, ADAPD-OG-MC。
    *   **最优复杂度方法**：xFILTER。
    *   **二阶方法**：作为 UPP-SC-SO 的对比参考。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件资源（如 GPU/CPU 型号、数量）及训练时长。由于该研究侧重于分布式算法的收敛理论和通信效率仿真，通常是在标准工作站上使用 MATLAB 或 Python 进行数值模拟。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种不同的网络拓扑分别进行了实验，每组实验均对比了迭代次数与最优性差距（Optimality Gap）、通信轮数与最优性差距的关系。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅对比了传统的迭代收敛速度，还重点对比了在不同网络条件下的通信成本，验证了切比雪夫加速在稀疏网络中的显著优势。

### 6. 主要结论与发现
*   **收敛性保证**：证明了 UPP-MC 和 UPP-SC 在非凸平滑问题下均能以 $O(1/T)$ 的次线性速率收敛至驻点。
*   **线性收敛**：在满足 P-Ł 条件（比强凸性更弱）时，UPP-MC 能够线性收敛至全局最优解。
*   **通信最优性**：UPP-SC-OPT 达到了理论上的最优通信复杂度界限 $O(\bar{M}\sqrt{\gamma}/\epsilon)$，其中 $\gamma$ 是图的条件数。
*   **性能表现**：实验表明，UPP 系列算法在收敛速度和通信效率上均优于现有的 SOTA（最先进）方法，尤其是在稀疏网络中。

### 7. 优点
*   **高度统一**：一个框架同时涵盖了一阶、二阶、凸、非凸以及多种通信策略的分布式算法，具有极强的理论普适性。
*   **通信优化**：通过切比雪夫加速有效解决了分布式优化中常见的通信瓶颈问题。
*   **理论突破**：为多种现有算法（如 EXTRA, DIGing）在非凸领域的使用提供了新的理论支撑。

### 8. 不足与局限
*   **参数依赖**：算法的最优参数设置（如步长、混合轮数）依赖于全局信息（如 Hessian 的边界、图的特征值），在完全去中心化的实际场景中，这些信息的获取可能需要额外的预计算。
*   **计算开销**：虽然 UPP-SC-SO（二阶版本）通信轮数少，但每个节点计算 Hessian 逆矩阵的本地计算负担较重，论文未深入讨论计算与通信之间的权衡（Trade-off）。
*   **应用限制**：目前主要针对平滑非凸问题，对于非平滑（Non-smooth）或带有复杂约束的非凸问题尚未讨论。

（完）
