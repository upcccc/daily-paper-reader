---
title: A Step Toward Federated Pretraining of Multimodal Large Language Models
title_zh: 迈向多模态大语言模型联邦预训练的一步
authors: "Baochen Xiong, Yifan Xu, Xiaoshan Yang, Yaguang Song, Yaowei Wang, Changsheng Xu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.26786v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 多模态大语言模型的联邦预训练
tldr: 针对多模态大语言模型（MLLM）预训练中高质量公开数据饱和及私有数据孤岛问题，本文提出了联邦多模态对齐（Fed-MA）任务。该任务通过冻结视觉编码器和LLM，仅协作训练跨模态投影器。为解决参数干扰和梯度震荡挑战，提出了Fed-CMP框架，利用规范可靠性感知聚合和正交保持动量机制，有效提升了分布式预训练的性能与稳定性。
selection_source: fresh_fetch
motivation: 旨在解决MLLM预训练中公开数据枯竭与私有数据隐私保护之间的矛盾，探索如何在保护隐私的前提下利用分布式资源进行跨模态对齐。
method: 提出Fed-CMP框架，通过规范空间分解减少客户端参数干扰，并利用正交保持动量机制稳定优化方向。
result: 在四个构建的联邦预训练场景中，Fed-CMP的表现显著优于现有的联邦学习基准算法。
conclusion: 本研究通过轻量化的联邦预训练范式，为利用隐私敏感数据构建高性能多模态大模型开辟了新路径。
---

## 摘要
多模态大语言模型（MLLMs）的快速演进正受到高质量公共数据饱和的瓶颈限制，而海量的多样化多模态数据仍被困在对隐私敏感的数据孤岛中，无法获取。联邦学习（FL）为解锁这些分布式资源提供了一个极具前景的解决方案，但现有研究主要集中在微调阶段，而基础的预训练阶段在很大程度上仍未被探索。在本文中，我们正式引入了联邦多模态大语言模型对齐（Fed-MA）任务，这是一种轻量级的预训练范式，在冻结视觉编码器和 LLM 的同时，协同训练跨模态投影器。我们在该设置中识别出两个关键挑战：（i）聚合本地投影器时的参数干扰；（ii）单轮协同随机梯度下降（one-pass collaborative SGD）中的梯度振荡。为了应对这些挑战，我们提出了 Fed-CMP，这是一个用于联邦多模态大语言模型预训练的开创性框架。Fed-CMP 采用了典型可靠性感知聚合，该方法构建了一个典型空间，将客户端投影器分解为共享对齐基和客户端特定系数，然后进行可靠性加权融合以抑制参数干扰。此外，Fed-CMP 引入了正交保持动量，通过正交投影将动量应用于共享对齐基，在保留几何结构的同时累积历史优化方向。我们基于公开数据集构建了四个联邦预训练场景，广泛的实验验证了 Fed-CMP 显著优于现有的基准方法。

## Abstract
The rapid evolution of Multimodal Large Language Models (MLLMs) is bottlenecked by the saturation of high-quality public data, while vast amounts of diverse multimodal data remain inaccessible in privacy-sensitive silos. Federated Learning (FL) offers a promising solution to unlock these distributed resources, but existing research focuses predominantly on fine-tuning, leaving the foundational pre-training phase largely unexplored. In this paper, we formally introduce the Federated MLLM Alignment (Fed-MA) task, a lightweight pre-training paradigm that freezes the vision encoder and LLM while collaboratively training the cross-modal projector. We identify two critical challenges in this setting: (i) parameter interference in aggregating local projectors; and (ii) gradient oscillations in one-pass collaborative SGD. To address these challenges, we propose Fed-CMP, a pioneering framework for federated MLLM pre-training. Fed-CMP employs Canonical Reliability-Aware Aggregation, which constructs a canonical space to decompose client projectors into a shared alignment basis and client-specific coefficients, then performs reliability-weighted fusion to suppress parameter interference. Furthermore, Fed-CMP introduces Orthogonality-Preserved Momentum, which applies momentum to the shared alignment basis via orthogonal projection, accumulating historical optimization directions while preserving geometric structure. We construct four federated pre-training scenarios based on public datasets, and extensive experiments validate that Fed-CMP significantly outperforms existing baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Step Toward Federated Pretraining of Multimodal Large Language Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：多模态大语言模型（MLLM）的性能高度依赖大规模高质量数据。然而，公开数据正趋于饱和，而海量的多样化数据（如医疗、个人隐私数据）分布在各机构或设备的“数据孤岛”中，受隐私法规限制无法集中训练。
*   **核心问题**：现有的联邦学习（FL）研究多集中在 MLLM 的指令微调阶段，而忽略了最基础的**预训练（跨模态对齐）阶段**。
*   **挑战**：
    1.  **参数干扰**：由于数据非独立同分布（Non-IID），不同客户端学习到的对齐方向冲突，直接聚合会导致性能崩溃。
    2.  **梯度震荡**：预训练通常采用“单次通过（One-pass）”模式（数据不重复使用），导致模型在联邦聚合时缺乏历史记忆，容易产生优化震荡和灾难性遗忘。

### 2. 方法论：Fed-CMP 框架
论文提出了 **Fed-CMP** 框架，旨在冻结视觉编码器和 LLM 的前提下，协同训练轻量级的跨模态投影器（Projector）。

*   **核心思想**：通过规范空间分解解决参数冲突，并通过保持几何结构的动量机制稳定训练。
*   **关键技术细节**：
    1.  **规范可靠性感知聚合 (CRA)**：
        *   **空间分解**：利用奇异值分解（SVD）将各客户端的投影器矩阵分解为“共享对齐基（Shared Basis）”和“客户端特定系数”。
        *   **可靠性加权**：根据客户端的“更新幅度”和“跨模态对齐质量（特征距离）”计算权重，抑制低质量客户端的干扰。
    2.  **正交保持动量 (OPM)**：
        *   **符号对齐**：解决 SVD 分解中的符号歧义，确保动量累积方向一致。
        *   **正交投影**：在应用动量更新共享基时，通过极分解（Polar Decomposition）将结果投影回正交流形，确保投影器的几何结构不被破坏。
        *   **自适应系数**：根据当前更新与历史方向的余弦相似度动态调整动量大小。

### 3. 实验设计
*   **数据集与场景**：基于 **CC12M** 构建了四个联邦预训练场景，通过不同的聚类策略模拟数据异构性：
    *   `MI-I`（图像-图像聚类）、`MT-T`（文本-文本聚类）、`Mjoint`（联合聚类）、`Mcross`（跨模态聚类）。
*   **Benchmark**：在 7 个主流 MLLM 评测集上进行评估：MM-Vet, MMBench, SEED, LLaVA-Bench, POPE, MME, MMVP。
*   **对比方法**：
    *   **非联邦**：本地独立训练（Local Training）。
    *   **经典 FL**：FedAvg, FedAdam, FedProx, MOON。
    *   **模型合并方法**：Task Arithmetic (TA), TIEs, DARE。

### 4. 资源与算力
*   **硬件配置**：使用了 2 台机器，每台配备 **8 张 NVIDIA A100 (40GB) GPU**，总计 **16 张 GPU**。
*   **训练时长**：每个实验大约耗时 **10 小时**。
*   **模型规格**：视觉编码器为 CLIP ViT-L/14，语言模型为 Vicuna-v1.5-7B，投影器为两层 MLP。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 种不同的异构数据分布场景。
    *   对比了 8 种基准算法。
    *   进行了详细的**消融实验**（验证 CRA、OPM、CSC 等模块的有效性）。
    *   包含**超参数敏感性分析**（如权重系数 $\alpha$）和**时间稳定性分析**（展示 10 轮通信中的性能曲线）。
    *   额外进行了**下游指令微调（Instruction Tuning）**的迁移实验。
*   **充分性评价**：实验设计非常充分且客观。通过 t-SNE 可视化展示了数据异构性的构建过程，确保了 Benchmark 的公平性，且评测维度涵盖了理解、推理、幻觉等多个方面。

### 6. 主要结论与发现
*   **Fed-CMP 表现卓越**：在所有异构场景下，Fed-CMP 均显著优于现有的联邦学习和模型合并算法，尤其在高度异构的图像聚类（MI-I）场景下优势最明显。
*   **稳定性是关键**：实验证明，在 One-pass 预训练模式下，普通 FL 算法性能波动剧烈，而 OPM 机制能提供平滑且持续上升的性能曲线。
*   **预训练对下游有益**：联邦预训练得到的投影器在后续的指令微调任务中表现出更好的初始化效果。

### 7. 优点与亮点
*   **开创性**：率先探索了 MLLM 的**联邦预训练**任务，填补了该领域的空白。
*   **数学严谨性**：针对 SVD 分解中的符号歧义和正交性保持给出了严谨的理论推导和解决方案。
*   **轻量化**：仅训练投影器，极大地降低了联邦学习中的通信开销和客户端计算负担，具有很强的实际落地潜力。

### 8. 不足与局限
*   **参数覆盖有限**：目前仅针对投影器（Projector）进行联邦训练，尚未涉及视觉编码器或 LLM 全参数/高效微调（如 LoRA）在预训练阶段的联邦协作。
*   **计算开销**：在服务端进行 SVD 分解和正交投影会带来额外的计算负担，当客户端数量极大规模增加时，可能成为瓶颈。
*   **理想化假设**：实验在模拟的联邦环境中进行，未充分考虑现实中的网络延迟、客户端掉线（Stragglers）以及异步通信等复杂问题。

（完）
