---
title: "HeteroFedSyn: Differentially Private Tabular Data Synthesis for Heterogeneous Federated Settings"
title_zh: HeteroFedSyn：针对异构联邦设置的差分隐私表格数据合成
authors: "Xiaochen Li, Fengyu Gao, Xizixiang Wei, Tianhao Wang, Cong Shen, Jing Yang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08832v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 异构联邦设置下的差分隐私表格数据合成
tldr: 针对横向联邦学习中异构数据分布导致的差分隐私（DP）表格数据合成难题，本文提出了HeteroFedSyn框架。该框架基于2维边际分布合成范式，通过引入基于随机投影的L2依赖度量、噪声纠正无偏估计器及自适应选择策略，有效解决了联邦环境下噪声累积和数据冗余问题。实验证明，该方法在保持隐私的同时，其数据可用性可媲美中心化合成方法。
selection_source: fresh_fetch
motivation: 现有的差分隐私数据合成方法多针对中心化场景，在处理异构联邦数据时面临严重的偏差或过量噪声问题。
method: 提出HeteroFedSyn框架，利用随机投影度量相关性、无偏估计器修正噪声并采用自适应策略动态更新边际选择。
result: 在范围查询、Wasserstein保真度和机器学习任务上，HeteroFedSyn达到了与中心化合成方法相当的性能。
conclusion: HeteroFedSyn是首个专门为异构横向联邦环境设计的差分隐私表格数据合成框架，实现了隐私与实用性的平衡。
---

## 摘要
传统的差分隐私（DP）机制通常针对特定的分析任务进行定制，这限制了受保护数据的可重用性。DP 表格数据合成通过生成可共享用于任意下游任务的合成数据集来克服这一限制。然而，现有的合成方法主要假设中心化或本地设置，而忽略了更具实际意义的横向联邦场景。简单地在本地合成数据或对单个记录进行扰动，要么会产生有偏差的混合结果，要么会引入过多的噪声，特别是在参与者之间存在异构数据分布的情况下。我们提出了 HeteroFedSyn，这是首个专门为横向联邦设置设计的 DP 表格数据合成框架。HeteroFedSyn 基于 PrivSyn 的二阶边缘分布（2-way marginal）合成范式，为分布式边缘选择引入了三项关键创新：(i) 一种带有随机投影的基于 L2 的依赖性度量，用于高效噪声下的相关性测量；(ii) 一种用于修正乘性噪声的无偏估计器；(iii) 一种动态更新依赖性分数以避免冗余的自适应选择策略。在范围查询、Wasserstein 保真度和机器学习任务上的广泛实验表明，尽管联邦执行本身会增加噪声，但 HeteroFedSyn 仍能实现与中心化合成相当的效用。我们的代码已通过链接开源。

## Abstract
Traditional Differential Privacy (DP) mechanisms are typically tailored to specific analysis tasks, which limits the reusability of protected data. DP tabular data synthesis overcomes this by generating synthetic datasets that can be shared for arbitrary downstream tasks. However, existing synthesis methods predominantly assume centralized or local settings and overlook the more practical horizontal federated scenario. Naively synthesizing data locally or perturbing individual records either produces biased mixtures or introduces excessive noise, especially under heterogeneous data distributions across participants.   We propose HeteroFedSyn, the first DP tabular data synthesis framework designed specifically for the horizontal federated setting. Built upon the PrivSyn paradigm of 2-way marginal-based synthesis, HeteroFedSyn introduces three key innovations for distributed marginal selection: (i) an L2-based dependency metric with random projection for noise-efficient correlation measurement, (ii) an unbiased estimator to correct multiplicative noise, and (iii) an adaptive selection strategy that dynamically updates dependency scores to avoid redundancy. Extensive experiments on range queries, Wasserstein fidelity, and machine learning tasks show that, despite the increased noise inherent to federated execution, HeteroFedSyn achieves utility comparable to centralized synthesis. Our code is open-sourced via the link.

---

## 论文详细总结（自动生成）

这篇论文《HeteroFedSyn: Differentially Private Tabular Data Synthesis for Heterogeneous Federated Settings》已被 SIGMOD 2026 接收。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：差分隐私（DP）表格数据合成旨在生成可重用的合成数据，以支持任意下游任务。然而，现有方法大多假设数据是中心化的。在现实的**横向联邦（Horizontal Federated）**场景中，多个机构拥有相同属性但样本不同的异构数据。
*   **核心挑战**：
    1.  **本地合成偏差**：各参与方独立合成再合并会导致数据分布偏差。
    2.  **噪声累积**：在分布式环境下，为了保护隐私而添加的噪声会随参与者数量增加而累积，严重破坏数据可用性。
    3.  **通信开销**：直接交换高维的二阶边缘分布（2-way marginals）会产生巨大的通信负担。
*   **研究目标**：开发首个专门针对异构联邦环境的差分隐私表格数据合成框架，在保护隐私的同时，实现与中心化合成相当的数据效用。

### 2. 方法论
HeteroFedSyn 框架基于 **PrivSyn** 范式（利用二阶边缘分布进行合成），并提出了以下关键技术：
*   **边缘分布共享与压缩**：
    *   参与方本地计算 1 维和 2 维边缘分布。
    *   使用**随机投影（Random Projection）**将高维 2 维边缘分布压缩为 $k$ 维向量，以减少通信量和注入的噪声规模。
*   **基于 $L_2$ 的依赖度量**：
    *   定义了 $InDif2$ 指标，通过计算实际 2 维边缘分布与假设独立时的分布之间的 $L_2$ 距离来衡量属性间的相关性。
*   **噪声纠正的无偏估计器**：
    *   由于服务器只能获得加噪且压缩后的统计量，直接计算相关性会产生偏差。论文推导了一个严谨的数学公式，利用加噪统计量构造出 $InDif2$ 的**无偏估计器**，从而在不接触原始数据的情况下准确识别高价值的属性对。
*   **自适应边缘选择（AdaFedPrivSyn）**：
    *   **动态更新**：不同于一次性选择所有属性对，该策略在每轮选择一个边缘分布后，利用当前合成的数据更新剩余属性对的依赖分数，避免选择冗余信息，从而更高效地利用隐私预算。
*   **数据合成**：使用 GUM 算法通过迭代复制和替换样本值，使合成数据集逐步拟合选定的加噪边缘分布。

### 3. 实验设计
*   **数据集**：使用了 5 个真实数据集：Adult（人口普查）、Abalone（鲍鱼）、Obesity（肥胖）、Insurance（保险）、Shoppers（购物者）。
*   **下游任务与指标**：
    1.  **范围查询（Range Queries）**：衡量合成数据在统计查询上的准确性。
    2.  **Wasserstein 保真度**：衡量合成数据与原始数据分布的距离。
    3.  **机器学习任务**：使用随机森林（RF）、多层感知机（MLP）和 XGBoost 在合成数据上训练，在原始数据上测试（分类用 F1，回归用 RMSE）。
*   **对比基准（Benchmark）**：
    *   **PrivSyn**：中心化环境下的经典算法（作为性能上限）。
    *   **FedPrivSyn-allMarg**：不进行筛选，共享所有边缘分布。
    *   **FedPrivSyn-RandMarg**：随机选择边缘分布。
    *   **FedPrivSyn-w/o RP**：不使用随机投影的对比版本。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **Intel E5-2620 v4 (2.10GHz) CPU** 和 **128GB 内存**的服务器上运行，操作系统为 Ubuntu 22.04。
*   **算力说明**：文中**未提及使用 GPU**，这表明该方法主要依赖 CPU 进行统计计算和迭代合成。
*   **训练时长**：未明确给出具体的训练时长，但复杂度分析显示服务器端复杂度约为 $O(d^4)$（$d$ 为属性数），在属性较多时计算压力较大。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个不同领域的数据集，设置了三个隐私强度等级（$\epsilon = 0.2, 1, 5$），参与者数量从 5 扩展到 25。
*   **消融与参数分析**：对随机投影维度 $k$、隐私预算分配比例、数据分布的异构程度（均匀、随机、有偏差分布）以及自适应选择的假设边界进行了详细的敏感性分析。
*   **客观性**：每组实验重复 5 次以消除随机性影响。实验设计考虑了现实中的数据异构性，对比了多种基准方法，整体评估较为全面、客观。

### 6. 主要结论与发现
*   **效用接近中心化**：尽管联邦环境引入了更多噪声，但 HeteroFedSyn 生成的数据在下游任务中的表现与中心化 PrivSyn 处于同一数量级。
*   **自适应策略的优越性**：在属性较多（如 Adult, Shoppers）的数据集上，自适应选择策略（AdaFedPrivSyn）显著优于静态选择，能更有效地捕捉复杂相关性。
*   **对异构性的鲁棒性**：实验证明，通过比例加权聚合局部统计量，该框架能有效处理参与方之间的数据分布偏差。
*   **噪声与压缩的平衡**：随机投影维度 $k=10$ 在大多数情况下能达到压缩误差与噪声抑制的最佳平衡。

### 7. 优点
*   **首创性**：填补了横向联邦环境下差分隐私表格数据合成的空白。
*   **数学严谨性**：为压缩加噪数据提供了无偏估计方案，解决了分布式环境下相关性度量难的问题。
*   **实用性**：通过随机投影大幅降低了通信开销，使其在带宽受限的联邦场景中更具可行性。
*   **任务无关**：生成的合成数据可直接用于多种下游分析，无需为每个任务重新设计 DP 机制。

### 8. 不足与局限
*   **计算复杂度**：服务器端计算 $InDif2$ 分数和进行自适应选择的复杂度较高（$O(d^4)$ 到 $O(d^6)$），对于超高维属性（如成百上千个属性）的表格可能存在扩展性问题。
*   **超参数敏感性**：自适应策略依赖于对“最终选择边缘数量”的预先假设，若假设不当可能影响隐私预算的分配效率。
*   **高阶相关性缺失**：目前主要基于 2 维边缘分布，对于极复杂的、涉及 3 个及以上属性的高阶相关性捕捉能力有限。

（完）
