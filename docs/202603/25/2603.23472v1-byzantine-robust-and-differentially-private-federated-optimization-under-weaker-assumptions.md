---
title: Byzantine-Robust and Differentially Private Federated Optimization under Weaker Assumptions
title_zh: 更弱假设下的拜占庭鲁棒且差分隐私的联邦优化
authors: "Rustem Islamov, Grigory Malinovsky, Alexander Gaponov, Aurelien Lucchi, Peter Richtárik, Eduard Gorbunov"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23472v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 在联邦优化中整合了差分隐私与拜占庭鲁棒性
tldr: 联邦学习面临隐私泄露和拜占庭攻击的双重挑战，但现有方案常依赖梯度有界等苛刻假设或需辅助数据。本文提出 Byz-Clip21-SGD2M 算法，通过结合鲁棒聚合、双重动量机制和精心设计的裁剪技术，在更弱的 L-光滑及次高斯噪声假设下实现了高概率收敛。该方法不仅在无攻击时达到最优收敛率，还在拜占庭和差分隐私环境下提升了模型效用，并通过 MNIST 实验验证了其有效性。
selection_source: fresh_fetch
motivation: 针对联邦学习中现有差分隐私与拜占庭鲁棒性结合方案依赖不切实际的梯度有界假设且缺乏收敛保证的问题。
method: 提出 Byz-Clip21-SGD2M 算法，集成鲁棒聚合、双重动量机制与裁剪技术，并在较弱的假设下进行理论分析。
result: 在标准 L-光滑和次高斯噪声假设下证明了高概率收敛，并在拜占庭和差分隐私设置下提升了效用保证。
conclusion: 该研究通过放宽理论假设，为构建兼具隐私保护与抗攻击能力的联邦优化算法提供了更具普适性的理论支撑和实践方案。
---

## 摘要
联邦学习（FL）使异构客户端能够在不集中原始数据的情况下协作训练共享模型，从而提供了一种内在的隐私保护。然而，梯度和模型更新仍可能泄露敏感信息，而恶意服务器可能会发起诸如拜占庭操纵之类的对抗性攻击。这些漏洞凸显了在统一框架内解决差分隐私（DP）和拜占庭鲁棒性的必要性。然而，现有方法通常依赖于不切实际的假设（如梯度有界），需要辅助的服务器端数据集，或者无法提供收敛性保证。为了解决这些局限性，我们提出了 Byz-Clip21-SGD2M，这是一种将鲁棒聚合与双重动量以及精心设计的裁剪（clipping）相结合的新算法。我们在标准的 $L$-光滑性和 $σ$-亚高斯梯度噪声假设下证明了高概率收敛保证，从而放宽了先前研究中占主导地位的条件。我们的分析在不存在对抗节点的情况下恢复了最先进的收敛速率，并改进了拜占庭和 DP 设置下的效用保证。在 MNIST 数据集上对 CNN 和 MLP 模型进行的实证评估进一步验证了我们方法的有效性。

## Abstract
Federated Learning (FL) enables heterogeneous clients to collaboratively train a shared model without centralizing their raw data, offering an inherent level of privacy. However, gradients and model updates can still leak sensitive information, while malicious servers may mount adversarial attacks such as Byzantine manipulation. These vulnerabilities highlight the need to address differential privacy (DP) and Byzantine robustness within a unified framework. Existing approaches, however, often rely on unrealistic assumptions such as bounded gradients, require auxiliary server-side datasets, or fail to provide convergence guarantees. We address these limitations by proposing Byz-Clip21-SGD2M, a new algorithm that integrates robust aggregation with double momentum and carefully designed clipping. We prove high-probability convergence guarantees under standard $L$-smoothness and $σ$-sub-Gaussian gradient noise assumptions, thereby relaxing conditions that dominate prior work. Our analysis recovers state-of-the-art convergence rates in the absence of adversaries and improves utility guarantees under Byzantine and DP settings. Empirical evaluations on CNN and MLP models trained on MNIST further validate the effectiveness of our approach.

---

## 论文详细总结（自动生成）

以下是对论文《Byzantine-Robust and Differentially Private Federated Optimization under Weaker Assumptions》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
联邦学习（FL）虽然通过本地训练保护了原始数据，但仍面临两大核心威胁：
*   **隐私泄露**：模型梯度和更新可能通过逆向工程泄露敏感信息。
*   **拜占庭攻击**：恶意客户端可能发送伪造更新以破坏模型收敛或植入后门。
**研究动机**：现有的同时解决差分隐私（DP）和拜占庭鲁棒性（BR）的方案通常依赖于**梯度有界（Bounded Gradients）**等过于苛刻且不切实际的假设，或者需要服务器端拥有辅助数据集。本文旨在探索：是否能在仅满足标准 $L$-光滑和次高斯噪声的弱假设下，开发出一种既能防范恶意攻击又能提供强隐私保证的联邦优化算法？

### 2. 方法论：Byz-Clip21-SGD2M 算法
论文提出了 **Byz-Clip21-SGD2M** 算法，其核心思想是集成多种机制来平衡隐私、鲁棒性和效用：
*   **双重动量机制（Double Momentum）**：
    *   **客户端动量 ($\beta$)**：用于平滑本地小批量梯度的随机噪声，解决 EF21 等算法在噪声下无法收敛到任意精度的问题。
    *   **服务器端动量 ($\hat{\beta}$)**：用于抵消由于差分隐私注入的噪声在缓冲区中的累积效应。
*   **精心设计的裁剪（Clipping）**：在注入 DP 噪声前对更新进行范数裁剪，确保敏感度受控，同时利用裁剪作为一种收缩算子来辅助鲁棒性。
*   **误差反馈（Error Feedback, EF21 风格）**：引入误差补偿机制来处理客户端之间的数据异构性（Client Drift），从而无需“梯度有界”假设。
*   **鲁棒聚合（Robust Aggregation）**：在服务器端使用 $( \delta_{byz}, c )$-鲁棒聚合规则（如坐标中位数 Coordinate-wise Median），过滤拜占庭节点的恶意干扰。
*   **理论突破**：提供了**高概率（High-probability）**收敛分析，而非仅是期望收敛，这在处理对抗性环境时更具说服力。

### 3. 实验设计
*   **数据集**：MNIST 手写数字识别。
*   **模型架构**：卷积神经网络（CNN）和多层感知机（MLP）。
*   **攻击场景**：
    *   **内积操纵攻击（IPM）**：恶意节点发送与真实梯度方向相反且模长巨大的向量。
    *   **标签翻转攻击（Label Flipping）**：恶意节点通过翻转训练标签来误导模型。
*   **对比基准（Benchmarks）**：
    *   **Byz-Clip-SGD**：一种将 DP-SGD 与鲁棒聚合直接结合的变体。
    *   **Safe-DSHB**：当时最先进的（SOTA）结合 DP 和拜占庭鲁棒性的算法，但依赖梯度有界假设。
*   **隐私设置**：测试了不同的隐私预算 $\epsilon \in \{3, 8, 13, 18, 23\}$。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到实验基于已有的分布式模拟代码库（Horváth & Richtárik, 2020）开发，并在 25 个客户端的模拟环境下运行。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种模型（CNN, MLP）、两种主流攻击（IPM, Label Flipping）、五个隐私预算等级以及不同数量的拜占庭客户端（从 2 个到 11 个不等）。
*   **充分性评价**：实验设计较为充分，通过改变拜占庭节点的比例和隐私强度，全面展示了算法的鲁棒性。
*   **客观性**：每组实验均运行了 3 个随机种子并标注了标准差，超参数搜索范围（学习率、裁剪阈值等）在各算法间保持一致，确保了对比的公平性。

### 6. 主要结论与发现
*   **理论一致性**：Byz-Clip21-SGD2M 在 $L$-光滑非凸设置下达到了 $\tilde{O}(1/\sqrt{GT})$ 的收敛率，与无攻击/无隐私环境下的最优速率匹配。
*   **效用提升**：在相同的隐私预算和攻击强度下，Byz-Clip21-SGD2M 的测试准确率普遍优于 Byz-Clip-SGD 和 Safe-DSHB，尤其是在隐私预算较紧（$\epsilon$ 较小）时。
*   **假设放宽**：证明了即使在梯度不满足全局有界的情况下，通过裁剪和误差反馈的结合，依然可以保证高概率收敛。
*   **三者权衡**：实验证实了隐私、鲁棒性和效用之间存在乘法级的相互影响，而本文算法在三者权衡中达到了更优的边界。

### 7. 优点
*   **理论严密**：在比前人更弱、更现实的假设下给出了高概率收敛证明，填补了理论空白。
*   **机制创新**：双重动量与误差反馈的结合巧妙地解决了 DP 噪声累积和数据异构性带来的收敛难题。
*   **实用性强**：算法不要求服务器端拥有非私有的辅助数据，更符合实际联邦学习场景。

### 8. 不足与局限
*   **实验数据集单一**：仅在 MNIST 上进行了验证，缺乏在更大型数据集（如 CIFAR-100 或 ImageNet）和更复杂模型（如 Transformer）上的表现数据。
*   **计算开销**：虽然理论上高效，但双重动量和误差反馈增加了客户端和服务器端的存储负担（需要维护多个缓冲区）。
*   **攻击类型覆盖**：虽然测试了 IPM 和标签翻转，但未探讨更隐蔽的后门攻击（Backdoor Attacks）或自适应攻击。
*   **重尾噪声**：目前的分析仍基于亚高斯噪声，对于实际中可能出现的重尾（Heavy-tailed）梯度噪声，其鲁棒性尚待进一步研究。

（完）
