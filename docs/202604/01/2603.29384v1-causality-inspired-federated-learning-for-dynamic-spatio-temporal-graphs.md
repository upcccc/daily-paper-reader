---
title: Causality-inspired Federated Learning for Dynamic Spatio-Temporal Graphs
title_zh: 针对动态时空图的因果启发式联邦学习
authors: "Yuxuan Liu, Wenchao Xu, Haozhao Wang, Zhiming He, Zhaofeng Shi, Chongyang Xu, Peichao Wang, Boyuan Zhang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29384v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 在保护隐私的同时进行去中心化训练的联邦学习
tldr: 针对动态时空图联邦学习中存在的空间和时间异质性问题，本文提出了一种受因果启发的框架SC-FSGL。该框架通过条件分离模块和因果码本，显式地将可迁移的因果知识与客户端特定的噪声解耦，并利用对比学习促进跨客户端的知识共享，有效解决了表示纠缠和负迁移问题，在多个数据集上取得了领先性能。
selection_source: fresh_fetch
motivation: 现有联邦图学习方法忽略了时空图的异质性，导致表示纠缠和负迁移，影响了模型的泛化能力。
method: 提出SC-FSGL框架，利用条件分离模块解耦因果因子，并通过因果码本与对比学习实现跨客户端的知识对齐。
result: 在五个具有不同异质性的时空图数据集上的实验表明，SC-FSGL的性能优于现有的最先进方法。
conclusion: 通过因果干预和表示解耦，可以有效提升动态时空图在联邦学习场景下的鲁棒性和泛化性能。
---

## 摘要
联邦图学习 (FGL) 已成为一种在保护数据隐私的同时进行图神经网络去中心化训练的强大范式。然而，现有的 FGL 方法主要针对静态图设计，并依赖于参数平均或分布对齐，这隐含地假设所有特征在客户端之间具有同等的迁移性，忽略了现实世界图中存在的时空异质性以及客户端特有知识。在这项工作中，我们发现此类假设会导致伪表示纠缠、客户端特有干扰和负迁移的恶性循环，从而降低了动态时空图联邦学习 (FSTG) 的泛化性能。为了解决这一问题，我们提出了一种名为 SC-FSGL 的新型因果启发式框架，该框架通过表示层面的干预，显式地将可迁移的因果知识与客户端特有的噪声解耦。具体而言，我们引入了一个条件分离模块，通过客户端条件掩码模拟软干预，从而实现不变时空因果因子与伪信号的解离，并减轻由客户端异质性引起的表示纠缠。此外，我们提出了一个因果码本 (Causal Codebook)，通过聚类因果原型并通过对比学习对齐局部表示，从而促进跨客户端的一致性，并推动不同时空模式间的知识共享。在五个具有不同异质性的时空图 (STG) 数据集上的实验表明，SC-FSGL 的性能优于现有最先进的方法。

## Abstract
Federated Graph Learning (FGL) has emerged as a powerful paradigm for decentralized training of graph neural networks while preserving data privacy. However, existing FGL methods are predominantly designed for static graphs and rely on parameter averaging or distribution alignment, which implicitly assume that all features are equally transferable across clients, overlooking both the spatial and temporal heterogeneity and the presence of client-specific knowledge in real-world graphs. In this work, we identify that such assumptions create a vicious cycle of spurious representation entanglement, client-specific interference, and negative transfer, degrading generalization performance in Federated Learning over Dynamic Spatio-Temporal Graphs (FSTG). To address this issue, we propose a novel causality-inspired framework named SC-FSGL, which explicitly decouples transferable causal knowledge from client-specific noise through representation-level interventions. Specifically, we introduce a Conditional Separation Module that simulates soft interventions through client conditioned masks, enabling the disentanglement of invariant spatio-temporal causal factors from spurious signals and mitigating representation entanglement caused by client heterogeneity. In addition, we propose a Causal Codebook that clusters causal prototypes and aligns local representations via contrastive learning, promoting cross-client consistency and facilitating knowledge sharing across diverse spatio-temporal patterns. Experiments on five diverse heterogeneity Spatio-Temporal Graph (STG) datasets show that SC-FSGL outperforms state-of-the-art methods.

---

## 论文详细总结（自动生成）

### 针对动态时空图的因果启发式联邦学习 (SC-FSGL) 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
动态时空图（STG）广泛应用于交通预测、传感器网络等领域。由于隐私限制，数据通常分布在不同机构（客户端）中，催生了**联邦时空图学习（FSTG）**。
*   **核心挑战**：现有的联邦图学习方法大多针对静态图，且假设所有特征在客户端之间是同等可迁移的。然而，现实中的时空图具有强烈的**空间异质性**（路网结构不同）和**时间异质性**（交通流趋势不同）。
*   **研究动机**：这种异质性会导致“表示纠缠”（通用模式与特有噪声混合）、“特定干扰”（全局模型过拟合局部信号）和“负迁移”（聚合后的模型性能下降）。论文旨在通过因果推理，将**共享的因果知识**（如普遍的交通高峰规律）与**客户端特有的噪声**（如特定区域的特殊路况）显式解耦。

#### 2. 方法论：核心思想与关键技术
论文提出了 **SC-FSGL** 框架，其核心思想是利用因果干预手段实现特征解耦与对齐：
*   **条件分离模块 (Conditional Separation Module)**：引入可学习的“软掩码”（Soft Mask），在潜在表示空间模拟因果干预。通过掩码将时空特征分解为“共享因果特征”和“客户端特有特征”。
*   **因果码本 (Causal Codebook)**：在服务器端维护一个全局可学习的原型矩阵（码本）。客户端通过对比学习（Contrastive Learning）将自身的共享因果特征与码本中的原型对齐，确保跨客户端的语义一致性。
*   **不变风险最小化 (IRM) 正则化**：引入 IRM 惩罚项，强制预测器在不同客户端环境（异质分布）下保持最优性，从而提取出真正稳定的因果因子。
*   **通信机制**：客户端仅上传和下载轻量级的“因果码本”参数，而非整个模型权重，极大地降低了通信开销并增强了隐私保护。

#### 3. 实验设计
*   **数据集**：使用了 5 个真实的交通流数据集：METRLA、PEMSD4、PEMSD7(M)、PEMSD8 和 PEMSBAY。每个数据集代表一个独立的客户端，以模拟强异质性环境。
*   **Benchmark 与对比方法**：
    *   **通用联邦学习**：FedAvg, FedProx, Moon, FedRep。
    *   **联邦图学习**：FedStar。
    *   **时空图预测模型**：GMAN, MegaCRN。
    *   **最新联邦时空模型**：FUELS。
*   **评估指标**：平均绝对误差 (MAE)、均方根误差 (RMSE) 和平均绝对百分比误差 (MAPE)。

#### 4. 资源与算力
*   **硬件环境**：实验使用了 **2 台 NVIDIA 3090 GPU**。
*   **软件框架**：基于 PyTorch 开发。
*   **训练细节**：通信轮次设置了早停机制（Early Stopping），局部训练每轮执行 1 个 Epoch。论文特别提到通信开销极低，每轮仅需传输约 8KB 数据。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个不同规模和特征的数据集，并在 15/30/60 分钟三个预测时间步长上进行了测试。
*   **消融实验**：分别验证了“因果码本”和“条件分离模块”的有效性。
*   **可视化分析**：通过 t-SNE 展现了因果特征的解耦效果，并绘制了 MAE 随通信轮次变化的收敛曲线。
*   **充分性评价**：实验设计较为全面，不仅对比了多种基准模型，还通过数学证明（收敛性分析）和定性分析（可视化）支撑了方法的有效性，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能领先**：SC-FSGL 在所有数据集和所有预测时间步长上均显著优于 SOTA 方法（如 FUELS 和 GMAN）。
*   **解耦有效性**：t-SNE 可视化证明模型成功将共享特征聚类，同时保留了私有特征的离散性，有效抑制了负迁移。
*   **收敛与稳定**：相比于其他联邦学习方法，SC-FSGL 在异质环境下的训练曲线更加平滑，收敛速度更快。

#### 7. 优点
*   **因果视角**：首次在联邦时空图领域引入因果解耦思想，解决了特征纠缠导致的泛化难题。
*   **通信高效**：通过交换码本而非全模型参数，将通信量降至 KB 级别，非常适合带宽受限的边缘计算场景。
*   **理论支撑**：提供了详细的因果模型推导和算法收敛性证明。

#### 8. 不足与局限
*   **领域局限**：实验主要集中在交通预测领域，尚未在环境监测、智能电网等其他时空图场景中验证通用性。
*   **超参数敏感性**：模型引入了对比学习和 IRM 惩罚项，涉及多个超参数（如 $\alpha, \beta$），在不同异质程度的场景下可能需要精细调优。
*   **动态漂移**：虽然处理了异质性，但对于长期的“因果漂移”（即因果关系随时间发生根本性改变）缺乏显式的建模机制。

（完）
