---
title: Federated Active Learning Under Extreme Non-IID and Global Class Imbalance
title_zh: 极端非独立同分布与全局类别不平衡下的联邦主动学习
authors: "Chen-Chen Zong, Sheng-Jun Huang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10341v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 隐私约束下的联邦主动学习
tldr: 针对联邦主动学习（FAL）在极端非独立同分布（Non-IID）和全局类别不平衡下的性能下降问题，本文提出FairFAL框架。研究发现，促进少数类别的平衡采样是提升性能的关键。FairFAL通过预测差异自适应选择全局或局部查询模型，利用原型引导的伪标签实现类别感知查询，并结合不确定性与多样性采样。实验证明该方法在长尾和异构场景下显著优于现有技术。
selection_source: fresh_fetch
motivation: 现有的联邦主动学习方法在面对严重的全局类别不平衡和客户端数据高度异构时，往往无法有效选择最具代表性的样本进行标注。
method: 提出FairFAL框架，通过轻量级预测差异自适应切换查询模型，并结合原型引导的伪标签与两阶段不确定性-多样性采样策略。
result: 在五个基准数据集上的实验结果表明，FairFAL在挑战性的长尾和非独立同分布设置下一致优于当前最先进的方法。
conclusion: 类别公平的采样策略是优化联邦主动学习性能的核心，通过自适应模型选择和类别感知查询可有效应对数据异构与不平衡挑战。
---

## 摘要
联邦主动学习 (FAL) 旨在隐私约束下降低标注成本，但在存在严重全局类别不平衡和高度异构客户端的现实场景中，其有效性会下降。我们对 FAL 中的查询模型选择进行了系统研究，并揭示了一个核心见解：实现更类别平衡采样（特别是针对少数类）的模型，始终能带来更好的最终性能。此外，仅当全局分布高度不平衡且客户端数据相对同质时，全局模型查询才是有益的；否则，局部模型更具优势。基于这些发现，我们提出了 FairFAL，一个自适应类别公平的 FAL 框架。FairFAL (1) 通过轻量级预测差异推断全局不平衡和局部-全局散度，从而实现全局和局部查询模型之间的自适应选择；(2) 利用全局特征进行原型引导的伪标签标注，以促进类别感知查询；(3) 应用带有 k-center 优化的两阶段不确定性-多样性平衡采样策略。在五个基准测试上的实验表明，在具有挑战性的长尾和非独立同分布 (non-IID) 设置下，FairFAL 始终优于现有最先进的方法。代码可在 https://github.com/chenchenzong/FairFAL 获取。

## Abstract
Federated active learning (FAL) seeks to reduce annotation cost under privacy constraints, yet its effectiveness degrades in realistic settings with severe global class imbalance and highly heterogeneous clients. We conduct a systematic study of query-model selection in FAL and uncover a central insight: the model that achieves more class-balanced sampling, especially for minority classes, consistently leads to better final performance. Moreover, global-model querying is beneficial only when the global distribution is highly imbalanced and client data are relatively homogeneous; otherwise, the local model is preferable. Based on these findings, we propose FairFAL, an adaptive class-fair FAL framework. FairFAL (1) infers global imbalance and local-global divergence via lightweight prediction discrepancy, enabling adaptive selection between global and local query models; (2) performs prototype-guided pseudo-labeling using global features to promote class-aware querying; and (3) applies a two-stage uncertainty-diversity balanced sampling strategy with k-center refinement. Experiments on five benchmarks show that FairFAL consistently outperforms state-of-the-art approaches under challenging long-tailed and non-IID settings. The code is available at https://github.com/chenchenzong/FairFAL.

---

## 论文详细总结（自动生成）

以下是对论文《Federated Active Learning Under Extreme Non-IID and Global Class Imbalance》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实的联邦学习场景中，数据通常呈现**极端非独立同分布（Non-IID）**和**全局类别不平衡（长尾分布）**的特征。现有的联邦主动学习（FAL）方法往往假设全局分布相对平衡，导致在处理稀有但关键的少数类样本时效率低下，标注预算被浪费在头部类别上。
*   **研究动机**：作者试图回答一个根本性问题：在 FAL 中，应该使用“全局聚合模型”还是“独立训练的局部模型”作为查询选择器？研究发现，采样的类别平衡性（尤其是对少数类的覆盖）直接决定了最终模型的性能。

### 2. 方法论（FairFAL 框架）
FairFAL 提出了一套自适应的类别公平采样策略，包含三个核心组件：
*   **自适应模型选择（Adaptive Model-Selection）**：
    *   通过计算全局模型与局部模型在本地已标注数据上的**预测差异**，估计全局不平衡程度（$\bar{\gamma}$）和局部-全局分布散度（$d_k$）。
    *   **核心逻辑**：当全局不平衡严重且客户端数据相对同质时，优先选择全局模型；在全局平衡或高度异构时，优先选择局部模型。
*   **原型引导的伪标签标注（Prototype-Guided Pseudo-Labeling）**：
    *   利用全局模型提取的特征（更具判别性）为每个类别构建特征原型。
    *   通过计算未标注样本与原型的余弦相似度来分配伪标签，从而在不依赖偏置分类器的情况下实现类别感知的样本划分。
*   **两阶段不确定性-多样性平衡采样**：
    *   **第一阶段**：在每个伪标签类别内，根据不确定性（如熵）筛选出候选池。
    *   **第二阶段**：在梯度嵌入空间中应用 **k-center 算法**进行多样性精炼，确保选出的样本既具有信息量又能覆盖特征流形，减少冗余。

### 3. 实验设计
*   **数据集**：
    *   **自然图像**：FMNIST, CIFAR-10, CIFAR-100。
    *   **医疗图像**：OctMNIST, DermaMNIST（具有天然的长尾分布）。
*   **实验场景**：设置全局不平衡率 $\rho=20$，Dirichlet 分布参数 $\alpha \in \{0.1, 100\}$（分别代表极端异构和近乎同质）。
*   **对比方法（11 种基准）**：
    *   传统主动学习：Random, Entropy, Margin, Least Confidence, Coreset。
    *   混合/深度主动学习：BADGE, GCNAL, ALFA-Mix。
    *   联邦主动学习 SOTA：KAFAL, LoGo, IFAL。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA GeForce RTX 3090 GPU** 上运行。
*   **训练细节**：使用 FedAvg 框架，通信轮数为 100 轮，每轮本地训练 5 个 epoch。主动学习包含 9 个查询周期，每个周期标注 5% 的数据。
*   **算力说明**：文中未明确给出总训练时长，但提到使用了 5 个随机种子取平均值，且涉及多个模型架构（CNN, MobileNet, ResNet-18），计算量符合标准学术研究规模。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个不同领域的基准数据集，对比了 11 种主流算法，实验设计非常详尽。
*   **消融实验**：对自适应模型选择、类别感知采样、候选池大小（$\kappa$）等组件进行了逐一验证。
*   **鲁棒性测试**：在不同的联邦优化框架（FedProx, SCAFFOLD）、不同的客户端数量（5, 10, 20）、不同的网络架构以及不同的不确定性度量下均进行了测试。
*   **评价**：实验设计客观、公平，通过 AULC（曲线下面积）和 Wilcoxon 符号秩检验等统计手段增强了结论的可信度。

### 6. 主要结论与发现
*   **采样平衡性是关键**：无论何种设置，能实现更类别平衡采样（尤其是捕获少数类）的模型，其最终性能必然更优。
*   **模型选择的规律**：局部模型在大多数异构场景下优于全局模型，因为局部模型能更好地捕捉本地决策边界；但在全局极度不平衡且客户端分布一致时，全局模型更具优势。
*   **全局特征的价值**：全局模型虽然在分类上可能受偏置影响，但其提取的特征表示比局部模型更鲁棒，更适合用于构建原型和计算多样性。

### 7. 优点与亮点
*   **自适应性**：FairFAL 不需要预验知识，能根据本地数据动态决定查询策略，适应性强。
*   **隐私保护**：模型选择和不平衡估计仅依赖轻量级的标量统计量交换，不泄露原始数据。
*   **医疗场景表现优异**：在天然长尾的医疗数据集上表现突出，证明了其在现实高价值场景中的应用潜力。

### 8. 不足与局限
*   **超参数依赖**：模型选择阈值 $\delta$ 虽然在实验中表现稳定，但在极端未知的分布下可能仍需微调。
*   **计算开销**：两阶段采样涉及梯度嵌入的计算和 k-center 聚类，对于资源极度受限的边缘设备（如低功耗 IoT）可能存在计算负担。
*   **任务局限**：目前研究主要集中在图像分类任务，尚未在目标检测、分割或自然语言处理等更复杂的 FAL 任务中验证。

（完）
