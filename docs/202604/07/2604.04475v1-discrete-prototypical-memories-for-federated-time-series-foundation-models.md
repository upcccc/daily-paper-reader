---
title: Discrete Prototypical Memories for Federated Time Series Foundation Models
title_zh: 联邦时间序列基础模型的离散原型记忆
authors: "Liwei Deng, Qingxiang Liu, Xinhe Niu, Shengchao Chen, Sheng Sun, Yuankai Wu, Guodong Long, Yuxuan Liang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04475v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 保护私有数据访问的联邦学习框架
tldr: 本研究针对联邦学习环境下时间序列基础模型（基于LLM）存在的语义对齐偏差及跨域异构性问题，提出了FeDPM框架。该框架通过引入离散原型记忆机制，学习局部领域的时间序列先验，并利用跨域记忆对齐与领域特定更新机制，在保护隐私的同时实现了统一且个性化的离散潜空间建模，显著提升了时间序列预测的泛化能力与效率。
selection_source: fresh_fetch
motivation: 现有联邦时间序列模型难以解决LLM文本潜空间与时间序列语义的对齐问题，且无法有效建模离散循环的时间序列特征。
method: 提出FeDPM框架，利用离散原型记忆学习局部先验，并通过跨域对齐和领域特定更新机制平衡全局共享与个性化知识。
result: 实验结果表明，FeDPM在处理异构跨域时间序列数据时，在预测精度和计算效率上均优于现有方法。
conclusion: 通过引入离散原型记忆，可以有效提升联邦时间序列基础模型在复杂异构环境下的表征能力和泛化性能。
---

## 摘要
利用大语言模型（LLMs）作为基于联邦学习（FL）的时间序列基础模型，提供了一种在保护私有数据隐私的同时，将 LLMs 的泛化能力迁移到时间序列数据的有效途径。然而，时间序列数据与现有 LLMs 以文本为中心的潜空间之间的语义失配，往往导致性能下降。同时，现有联邦学习方法中的参数共享机制将异构的跨域时间序列数据建模到统一的连续潜空间中，这与时间序列语义通常表现为离散且循环出现的模式（regimes）这一事实相矛盾。为了解决这些局限性，我们提出了 FeDPM，这是一个基于离散原型记忆的联邦时间序列基础模型框架。具体而言，我们为域内时间序列数据学习局部原型记忆先验。随后，我们对齐跨域记忆以促进统一的离散潜空间，并引入一种特定于域的记忆更新机制，以平衡共享和个性化的原型知识。广泛的实验证明了 FeDPM 的效率和有效性。代码已在 https://anonymous.4open.science/r/FedUnit-64D1 公开。

## Abstract
Leveraging Large Language Models (LLMs) as federated learning (FL)-based time series foundation models offers a promising way to transfer the generalization capabilities of LLMs to time series data while preserving access to private data. However, the semantic misalignment between time-series data and the text-centric latent space of existing LLMs often leads to degraded performance. Meanwhile, the parameter-sharing mechanism in existing FL methods model heterogeneous cross-domain time-series data into a unified continuous latent space, which contradicts the fact that time-series semantics frequently manifest as discrete and recurring regimes. To address these limitations, we propose \textsc{FeDPM}, a federated framework for time-series foundation models based on discrete prototypical memories. Specifically, we learn local prototypical memory priors for intra-domain time-series data. We then align cross-domain memories to promote a unified discrete latent space and introduce a domain-specific memory update mechanism to balance shared and personalized prototypical knowledge. Extensive experiments demonstrate the efficiency and effectiveness of \textsc{FeDPM}. The code is publicly available at https://anonymous.4open.science/r/FedUnit-64D1.

---

## 论文详细总结（自动生成）

这是一份关于论文《Discrete Prototypical Memories for Federated Time Series Foundation Models》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了在**联邦学习（FL）**环境下构建**时间序列基础模型（TSFM）**的挑战。
*   **研究动机**：虽然利用预训练大语言模型（LLM）处理时间序列数据具有泛化潜力，但存在两大瓶颈：
    1.  **语义失配**：时间序列信号与 LLM 以文本为中心的潜空间之间存在根本性的表征差异。
    2.  **连续潜空间的局限性**：现有联邦学习方法通常将异构数据映射到统一的**连续**潜空间，但这忽略了时间序列模式往往是**离散且循环**（如交通流的相位转换）的特性，容易导致语义纠缠和负迁移。
*   **核心目标**：提出一种参数高效、通信高效且能捕捉离散循环模式的联邦时间序列预测框架。

### 2. 方法论：核心思想与技术细节
论文提出了 **FedPM** 框架，其核心思想是利用**离散原型记忆（Discrete Prototypical Memories）**作为跨域知识传递的媒介。
*   **局部原型记忆先验**：每个客户端（领域）拥有一个编码器-解码器结构。编码器将时间序列补丁（Patches）映射为潜向量，通过**原型记忆检索（PMR）**模块，从局部记忆库中寻找最相似的离散原型向量进行量化表征。
*   **跨域记忆对齐（服务器端）**：
    *   由于原型具有排列不变性，服务器通过计算不同客户端原型间的**余弦相似度**构建图。
    *   利用**广度优先搜索（BFS）**提取连通分量，将语义相似的原型聚类。
*   **领域特定记忆更新**：
    *   **全局共享知识**：提取聚类中心作为共享原型（Shared Prototypes）。
    *   **个性化知识**：利用 **Utility-Diversity Score（效用-多样性得分）** 从未聚类的原型中筛选出对本地领域重要且具有差异化的向量，保留为个性化原型。
*   **通信机制**：客户端与服务器之间**仅交换原型记忆向量**，而非完整的模型参数，极大地降低了带宽需求。

### 3. 实验设计
*   **数据集**：涵盖 7 个真实世界基准数据集，包括电力（Electricity）、天气（Weather）、汇率（Exchange）以及 4 个电力变压器数据集（ETTh1, ETTh2, ETTm1, ETTm2）。
*   **Benchmark 场景**：长程时间序列预测（预测长度 96, 192, 336, 720）。
*   **对比方法**：
    *   **联邦基础模型 (FL-FM)**：Time-FFM, FFTS, FL-iTransformer, FL-PatchTST。
    *   **集中式基础模型 (Cen-FM)**：TOTEM, UniTime, Cen-PatchTST。
    *   **集中式专家模型 (Expert)**：TimesNet, DLinear, FEDformer, iTransformer, PatchTST。

### 4. 资源与算力
*   **硬件设备**：
    *   主要实验在 **NVIDIA RTX 5090 GPU** 上完成。
    *   模型效率对比实验（训练时长测试）在 **NVIDIA A100-80G GPU** 上完成。
*   **训练细节**：采用 Adam 优化器，学习率 $1 \times 10^{-5}$，本地训练 5 个 epoch，联邦通信 100 轮，设置了早停机制（Patience=10）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的实验验证，包括：
    *   **全量数据预测**：在 7 个数据集、4 个预测长度上进行对比。
    *   **少样本预测（Few-shot）**：分别测试了 5% 和 10% 数据量下的表现。
    *   **消融实验**：验证了聚合策略（平均 vs. 语义聚类）、局部/全局记忆的影响、以及不同编码器骨干网（Transformer, CNN, RNN, FC）的鲁棒性。
    *   **隐私实验**：测试了在加入高斯、指数、拉普拉斯噪声（差分隐私常用手段）后的性能稳定性。
*   **充分性评价**：实验设计非常全面，不仅对比了预测精度，还深入分析了通信开销、参数量和训练耗时，客观地展示了该方法在资源受限环境下的优势。

### 6. 主要结论与发现
*   **性能卓越**：FedPM 在多数任务中达到了 SOTA 性能，尤其在少样本场景下表现稳健。
*   **极高效率**：相比现有联邦学习基准，FedPM 的**通信开销降低了 97.03% 以上**，**可训练参数量减少了 20.37% 以上**。
*   **LLM 局限性验证**：消融实验发现，在联邦场景下，将冻结的 LLM 替换为轻量级原生时间序列模型（如 Transformer 层）往往能获得更好的 MSE，证明了现有 LLM 在时间序列建模中存在严重的语义失配。

### 7. 优点（亮点）
*   **架构创新**：通过离散原型记忆巧妙地解决了时间序列的离散模式建模问题，并避开了 LLM 文本潜空间的对齐难题。
*   **通信友好**：仅交换记忆向量的设计非常适合带宽受限的边缘计算或移动设备场景。
*   **灵活性**：框架对编码器/解码器骨干网络不敏感，具有很强的通用性。
*   **隐私鲁棒性**：实验证明该方法对隐私保护噪声具有较强的容忍度。

### 8. 不足与局限
*   **超参数依赖**：模型性能受原型库大小 $M$、相似度阈值 $\delta$ 和共享比例 $\gamma$ 的影响，目前仍需手动调优，缺乏自适应机制。
*   **计算复杂度**：服务器端的跨域记忆对齐（BFS 聚类）在客户端数量极多时可能会产生较高的计算开销，导致训练时间增加。
*   **场景覆盖**：虽然在预测任务上表现优异，但尚未验证其在时间序列分类、异常检测等其他基础任务上的泛化能力。

（完）
