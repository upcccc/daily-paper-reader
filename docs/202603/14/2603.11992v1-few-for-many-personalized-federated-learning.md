---
title: Few-for-Many Personalized Federated Learning
title_zh: 以少服多的个性化联邦学习
authors: "Ping Guo, Tiantian Zhang, Xi Lin, Xiang Li, Zhi-Ri Tang, Qingfu Zhang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11992v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 具有数据隐私保护的个性化联邦学习
tldr: 针对个性化联邦学习（PFL）在大规模异构客户端场景下的可扩展性难题，本文提出了 FedFew 框架。该方法将 PFL 建模为“以少服多”的多目标优化问题，仅需维护少量（K个）共享模型即可服务全体（M个）客户端。FedFew 通过梯度更新自动发现最优模型多样性，无需手动分区或复杂调参，在理论上保证了近乎最优的个性化效果，并在多项任务中超越了现有技术。
selection_source: fresh_fetch
motivation: 现有的个性化联邦学习方法在平衡大量异构客户端目标时缺乏原则性机制，且为每个客户端维护独立模型存在严重的可扩展性瓶颈。
method: 提出 FedFew 算法，通过维护远少于客户端数量的 K 个共享模型，并利用高效的梯度优化过程自动为所有客户端提供个性化服务。
result: 在计算机视觉、自然语言处理及真实医学影像数据集上的实验证明，FedFew 仅需 3 个模型即可持续优于其他最先进的个性化联邦学习方法。
conclusion: 该研究证明了利用少量共享模型实现大规模个性化联邦学习的有效性，为解决异构数据环境下的模型扩展性提供了新的理论支撑和实践方案。
---

## 摘要
个性化联邦学习 (PFL) 旨在为具有高度异构数据分布的客户端训练定制化模型，同时保护数据隐私。现有方法通常依赖于聚类或模型插值等启发式方法，缺乏平衡异构客户端目标的原则性机制。为具有不同数据分布的 $M$ 个客户端提供服务本质上是一个多目标优化问题，理想情况下，实现最优个性化需要在帕累托前沿 (Pareto front) 上拥有 $M$ 个不同的模型。然而，在拥有数百或数千个客户端的联邦设置中，维护 $M$ 个独立的模型会带来显著的可扩展性挑战。为了应对这一挑战，我们将 PFL 重新表述为一个“以少服多” (few-for-many) 的优化问题，仅维护 $K$ 个共享的服务器模型 ($K \ll M$) 来共同服务于所有 $M$ 个客户端。我们证明了该框架实现了近乎最优的个性化：近似误差随着 $K$ 的增加而减小，且随着数据的增长，每个客户端的模型都会收敛到各自的最优解。基于这一重新表述，我们提出了 FedFew，这是一种通过高效的基于梯度的更新来联合优化 $K$ 个服务器模型的实用算法。与需要手动划分客户端的聚类方法或需要

## Abstract
Personalized Federated Learning (PFL) aims to train customized models for clients with highly heterogeneous data distributions while preserving data privacy. Existing approaches often rely on heuristics like clustering or model interpolation, which lack principled mechanisms for balancing heterogeneous client objectives. Serving $M$ clients with distinct data distributions is inherently a multi-objective optimization problem, where achieving optimal personalization ideally requires $M$ distinct models on the Pareto front. However, maintaining $M$ separate models poses significant scalability challenges in federated settings with hundreds or thousands of clients. To address this challenge, we reformulate PFL as a few-for-many optimization problem that maintains only $K$ shared server models ($K \ll M$) to collectively serve all $M$ clients. We prove that this framework achieves near-optimal personalization: the approximation error diminishes as $K$ increases and each client's model converges to each client's optimum as data grows. Building on this reformulation, we propose FedFew, a practical algorithm that jointly optimizes the $K$ server models through efficient gradient-based updates. Unlike clustering-based approaches that require manual client partitioning or interpolation-based methods that demand careful hyperparameter tuning, FedFew automatically discovers the optimal model diversity through its optimization process. Experiments across vision, NLP, and real-world medical imaging datasets demonstrate that FedFew, with just 3 models, consistently outperforms other state-of-the-art approaches. Code is available at https://github.com/pgg3/FedFew.

---

## 论文详细总结（自动生成）

# 论文总结：以少服多的个性化联邦学习 (FedFew)

## 1. 核心问题与整体含义
个性化联邦学习 (PFL) 的核心矛盾在于**异构性与可扩展性**的平衡。
*   **研究背景**：在非独立同分布 (Non-IID) 场景下，单一全局模型难以适配所有客户端。
*   **研究动机**：理想的个性化需要为 $M$ 个客户端训练 $M$ 个独立模型，但这在拥有成千上万客户端的系统中不可扩展。现有的聚类或插值方法多依赖启发式规则，缺乏严谨的多目标优化理论支撑。
*   **核心问题**：如何用极少量的共享模型（$K$ 个）有效地服务于海量的异构客户端（$M$ 个），并保证优化过程的原则性和收敛性？

## 2. 方法论
论文提出了 **FedFew** 算法，将 PFL 重新表述为一个“以少服多” (Few-for-Many) 的优化问题。
*   **核心思想**：在服务器端仅维护 $K$ 个共享模型 ($K \ll M$)。每个客户端根据本地数据分布，从这 $K$ 个模型中选择最匹配的一个。
*   **关键技术细节**：
    *   **多目标重构**：将 PFL 建模为最小化 $M$ 个客户端损失向量的多目标优化问题。
    *   **平滑切比雪夫集标量化 (STCH-Set)**：为了解决多目标优化中帕累托前沿难以覆盖的问题，引入切比雪夫标量化。由于原始的 $\max$ 和 $\min$ 操作不可微，论文采用 **Log-Sum-Exp** 技术进行两级平滑处理。
    *   **梯度分解机制**：推导出的梯度更新包含两个关键权重：
        1.  **外部权重 ($\alpha_i$)**：实现“硬样本挖掘”，给予表现较差的客户端更高权重。
        2.  **内部权重 ($w_{ik}$)**：实现“软模型选择”，自动将客户端关联至最适合的模型。
*   **算法流程**：服务器广播 $K$ 个模型 $\rightarrow$ 客户端计算各模型的本地梯度和损失 $\rightarrow$ 服务器根据 STCH-Set 公式计算权重并聚合更新 $K$ 个模型 $\rightarrow$ 训练结束后，客户端通过本地验证选择最优模型。

## 3. 实验设计
*   **数据集与场景**：
    *   **基准视觉任务**：CIFAR-10/100、TinyImageNet、FEMNIST（包含病理性标签倾斜和 Dirichlet 分布倾斜）。
    *   **自然语言处理**：AG News 文本分类。
    *   **真实医学影像**：Kvasir（胃肠道疾病检测）、FedISIC2019（皮肤病变分类），模拟真实的跨机构领域偏移。
*   **对比方法 (Benchmarks)**：
    *   **中心化/全局方法**：FedAvg, FedProx, FedMTL。
    *   **单客户端单模型方法**：APFL, Ditto, FedRep, FedAMP。
    *   **多模型/聚类方法**：IFCA（基于硬聚类的核心对比方法）。

## 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型架构**：使用了 4 层 CNN（用于 CIFAR/FEMNIST）、TextCNN（用于 AG News）以及 ResNet-18（用于医学影像）。
*   **训练规模**：基准实验运行 2000 个通信轮次，医学影像实验运行 1000 轮，每轮包含 1 个本地 Epoch。

## 5. 实验数量与充分性
*   **实验规模**：在 7 个不同领域的数据集上进行了测试，涵盖了从合成异构到自然异构的多种场景。
*   **消融与敏感性分析**：
    *   针对模型数量 $K$ 进行了深入研究（$K$ 从 1 到 10）。
    *   分析了平滑参数 $\mu$ 对模型选择（硬选择 vs 软选择）的影响。
    *   探讨了通信效率与本地迭代次数的权衡。
*   **客观性**：实验对比了多种不同范式的 PFL 算法，并引入了 Jain's Fairness Index 来评估公平性，实验设计较为全面且客观。

## 6. 主要结论与发现
*   **性能卓越**：FedFew 仅需 **3 个模型** 即可在几乎所有数据集上达到 SOTA 性能，显著优于维护 $M$ 个独立模型的方法。
*   **理论收敛**：证明了该框架的近似误差由“帕累托覆盖间隙”和“统计误差”组成，且随着 $K$ 和数据量的增加而消失。
*   **自动发现多样性**：与需要手动分区的聚类方法不同，FedFew 能通过梯度优化自动发现最优的模型多样性。
*   **公平性提升**：在医学影像任务中，FedFew 显著提升了表现最差客户端的准确率，展现了良好的公平性。

## 7. 优点
*   **高可扩展性**：通信和存储开销仅与 $K$ 相关，与客户端总数 $M$ 无关，极大地缓解了大规模联邦系统的压力。
*   **理论支撑深厚**：将 PFL 与多目标优化理论结合，并提供了严谨的收敛性证明。
*   **优化平滑**：通过两级平滑技术解决了硬聚类方法中优化目标不连续、难以收敛的问题。

## 8. 不足与局限
*   **客户端计算负担**：虽然通信高效，但客户端在训练期间需要计算 $K$ 个模型的梯度，本地计算量是单模型方法的 $K$ 倍。
*   **超参数依赖**：平滑参数 $\mu$ 对优化稳定性有一定影响，虽然论文给出了建议值，但在极端异构环境下可能仍需微调。
*   **架构限制**：目前假设 $K$ 个模型采用相同的网络架构，未探讨在模型架构也存在异构性时的表现。

（完）
