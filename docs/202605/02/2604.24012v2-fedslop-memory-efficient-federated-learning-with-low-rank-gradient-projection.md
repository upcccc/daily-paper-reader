---
title: "FedSLoP: Memory-Efficient Federated Learning with Low-Rank Gradient Projection"
title_zh: FedSLoP：基于低秩梯度投影的内存高效联邦学习
authors: "Yutong He, Zhengyang Huang, Jiahe Geng"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24012v2"
tags: ["query:dml-dp"]
score: 8.0
evidence: 用于分布式机器学习的联邦优化算法
tldr: 针对联邦学习在资源受限环境下面临的通信和内存开销大、收敛慢等挑战，本文提出FedSLoP算法。该方法通过梯度的随机低秩子空间投影，在保留优化进度的同时显著降低了更新数据的维度。理论证明其在非凸条件下具有O(1/√NT)的收敛率，实验表明其在减少资源消耗的同时，准确率优于或等同于FedAvg及其他低秩基准方法，为高效联邦学习提供了新方案。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习在异构和资源受限环境下，标准算法面临的高通信成本和内存压力问题。
method: 提出一种结合随机低秩子空间投影的联邦优化算法，通过降低梯度更新的维度来减少存储和传输开销。
result: 实验证明该方法在大幅降低通信量和客户端内存占用的同时，达到了与FedAvg相当或更优的分类精度。
conclusion: FedSLoP为实现通信和内存高效的联邦学习提供了一种具有理论保障且实践有效的随机子空间优化框架。
---

## 摘要
联邦学习使大量客户端能够在不交换原始数据的情况下协作训练机器学习模型，但标准算法（如 FedAvg）在异构、资源受限的环境中面临收敛缓慢以及通信和内存成本高昂的问题。我们提出了 FedSLoP，这是一种联邦优化算法，它结合了梯度的随机低秩子空间投影，从而在保持优化进度的同时，降低了通信和存储更新的维度。在理论方面，我们在标准的平滑性和有界方差假设下，进行了详细的非凸收敛性分析，证明了 FedSLoP 能够以 $O(1/\sqrt{NT})$ 的速率收敛到一阶驻点。在实证方面，我们在具有异构数据分区的联邦 MNIST 分类任务上进行了广泛的实验，结果表明，与 FedAvg 以及代表性的稀疏或低秩基准方法相比，FedSLoP 在显著减少通信量和客户端内存的同时，实现了具有竞争力或更好的准确率。总之，我们的结果表明，像 FedSLoP 这样的随机子空间动量方法为实现通信和内存高效的联邦学习提供了一种有原则且有效的方法。代码可在以下网址获取：https://github.com/pkumelon/FedSLoP.git。

## Abstract
Federated learning enables a population of clients to collaboratively train machine learning models without exchanging their raw data, but standard algorithms such as FedAvg suffer from slow convergence and high communication and memory costs in heterogeneous, resource-constrained environments. We introduce FedSLoP, a federated optimization algorithm that combines stochastic low-rank subspace projections of gradients, thereby reducing the dimension of communicated and stored updates while preserving optimization progress. On the theoretical side, we develop a detailed nonconvex convergence analysis under standard smoothness and bounded-variance assumptions, showing that FedSLoP is guaranteed to converge to a first-order stationary point at a rate of $O(1/\sqrt{NT})$. On the empirical side, we conduct extensive experiments on federated MNIST classification with heterogeneous data partitions, showing that FedSLoP substantially reduces communication volume and client-side memory while achieving competitive or better accuracy compared with FedAvg and representative sparse or low-rank baselines. Together, our results demonstrate that random subspace momentum methods such as FedSLoP provide a principled and effective approach to communication- and memory-efficient federated learning. Codes are available at: https://github.com/pkumelon/FedSLoP.git.

---

## 论文详细总结（自动生成）

这是一份关于论文《FedSLoP: Memory-Efficient Federated Learning with Low-Rank Gradient Projection》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
联邦学习（FL）在实际部署中面临两大瓶颈：**通信带宽限制**和**边缘设备内存不足**。
*   **研究动机**：标准的 FedAvg 算法在处理非独立同分布（Non-IID）数据时收敛缓慢。虽然一些改进算法（如 SCAFFOLD）通过引入控制变量缓解了数据异构性，但这些额外的变量加剧了客户端的内存负担。
*   **核心问题**：如何在不增加（甚至显著减少）客户端内存占用的前提下，实现高效、稳健且具有理论保障的联邦优化？

### 2. 方法论：核心思想与技术细节
论文提出了 **FedSLoP**（Federated Low-rank Projection），其核心思想是将局部优化限制在每轮随机采样的低维子空间中。
*   **核心思想**：利用大型模型优化轨迹往往存在于低维子空间的特性，通过随机投影矩阵压缩梯度和动量状态。
*   **关键技术细节**：
    1.  **随机子空间采样**：每轮通信开始时，服务器从 Stiefel 流形 $St(d, r)$ 中采样一个随机正交基 $P_t$，并构建投影矩阵 $\Pi_t = P_t P_t^\top$。
    2.  **投影动量更新**：客户端在局部迭代时，不仅对随机梯度进行投影，还将动量状态也限制在 $r$ 维子空间内：$v_{i,s+1} = \mu v_{i,s} + \Pi_t g_{i,s}$。
    3.  **内存优化**：由于动量 $v$ 始终在 $P_t$ 的列空间内，客户端只需存储 $r$ 维的系数向量而非 $d$ 维的全量参数，从而将优化器状态内存从 $O(d)$ 降至 $O(r)$。
    4.  **理论保障**：论文在非凸设置下证明了该算法具有 $O(1/\sqrt{NT})$ 的收敛率，且展示了收敛速度与子空间比例 $\delta = r/d$ 的显式关系。

### 3. 实验设计
*   **数据集与场景**：主要在 **MNIST** 数据集上进行验证，采用 **Dirichlet 分布**（$\alpha=0.1$）模拟高度非 IID 的数据分区。
*   **Benchmark 模型**：一个包含 128 个隐藏层神经元的两层多层感知机（SimpleMLP），参数量约为 $10^5$ 级。
*   **对比方法**：
    *   **全参数基准**：FedAvg-M（带动量的 FedAvg）。
    *   **稀疏/部分更新方法**：FedMef、NeuLite、FederatedSelect。
    *   **低秩模型微调**：FedLoRA-M（在模型层面进行低秩适配）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练总时长。
*   **训练配置**：实验设置了 100 轮通信，每轮 1 个局部 Epoch，Batch Size 为 32，客户端总数 $p=50$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：对比了 6 种算法在 Non-IID 环境下的收敛曲线和最终精度。
    *   **异构性敏感度分析**：测试了 $\alpha \in \{0.05, 0.1, 0.5, 1.0\}$ 四种不同程度的数据异构性。
    *   **消融实验**：针对投影秩 $r$（从 16 到 192）和客户端数量 $p$（从 10 到 100）进行了详细的参数搜索。
*   **充分性评价**：实验设计较为全面，涵盖了收敛性、鲁棒性和资源权衡。通过 3 次独立随机种子取平均值，保证了结果的客观性。但实验目前主要集中在 MNIST 这种相对小规模的数据集上，缺乏在大规模视觉（如 ImageNet）或语言模型上的验证。

### 6. 论文的主要结论与发现
*   **性能表现**：FedSLoP 的最终精度（95.11%）非常接近全参数的 FedAvg-M（95.51%），且显著优于其他稀疏化或低秩模型基准（如 FedLoRA-M 的 92.47%）。
*   **异构性鲁棒性**：在极端 Non-IID 环境下（$\alpha=0.05$），FedSLoP 的表现甚至超过了 FedAvg-M（约高出 2%），显示出随机子空间投影在抑制局部漂移方面的潜在优势。
*   **资源效率**：在压缩率达到 1/7 的情况下，FedSLoP 依然保持了极高的精度，证明了其在内存受限设备上的实用性。

### 7. 优点（亮点）
*   **内存友好**：通过投影动量，直接解决了联邦学习中优化器状态占用内存过大的痛点。
*   **理论与实证结合**：不仅提出了算法，还给出了严谨的非凸收敛性证明，并揭示了线性加速特性。
*   **无缝集成**：该方法不需要修改模型架构，也不需要像 SCAFFOLD 那样维护复杂的控制变量，易于集成到现有的 FL 框架中。

### 8. 不足与局限
*   **实验任务单一**：主要基于 MNIST 和简单的 MLP 模型，对于现代深度卷积网络（CNN）或 Transformer 架构的有效性尚待在大规模数据集上验证。
*   **投影开销**：虽然减少了内存和通信量，但频繁的矩阵投影操作可能会增加客户端的计算负担（CPU/GPU 计算开销未详细量化）。
*   **超参数敏感性**：投影秩 $r$ 的选择对性能有较大影响，虽然实验给出了建议，但在不同任务中可能需要重新调优。

（完）
