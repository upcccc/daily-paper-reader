---
title: Differentially Private Manifold Denoising
title_zh: 差分隐私流形去噪
authors: "Jiaqi Wu, Yiqing Sun, Zhigang Yao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00942v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 具有严格隐私核算的差分隐私流形去噪
tldr: "本文提出了一种差分隐私流形去噪框架，旨在利用敏感参考数据修正非私有的噪声查询点。该方法通过迭代地在差分隐私保护下估计局部均值和切线几何，并将查询点向估计的子空间投影，实现了在保护参考数据隐私的同时保留流形几何特征。研究提供了严格的(ε, δ)-DP隐私核算和非渐近效用保证，为受监管环境下的流形学习任务提供了高效且可扩展的解决方案。"
selection_source: fresh_fetch
motivation: 旨在解决如何在利用包含敏感信息的参考数据集进行流形去噪时，提供形式化的差分隐私保护并保留数据几何特征。
method: 采用迭代过程，结合校准灵敏度的局部几何估计与查询点投影，并利用隐私预算调度器进行跨迭代的隐私核算。
result: 理论分析给出了高概率效用保证，实验表明在适度隐私预算下能准确恢复信号并有效支持嵌入、聚类等下游任务。
conclusion: 该框架实现了隐私与效用的平衡，是流形学习工作流中一个模块化、可扩展且易于部署的差分隐私组件。
---

## 摘要
我们提出了一种差分隐私流形去噪框架，允许用户利用敏感的参考数据集来纠正带噪声的非隐私查询点，且不会损害隐私。该方法遵循一个迭代过程：(i) 在校准灵敏度下，利用参考数据隐私地估计局部均值和切线几何；(ii) 在每次迭代中，通过校正步骤将查询点沿着隐私估计的子空间向局部均值投影；(iii) 使用 (ε,δ)-差分隐私 (DP) 对跨迭代和查询的过程进行严格的隐私核算。从概念上讲，该框架将差分隐私引入流形方法，在为参考数据提供正式 DP 保证的同时，为嵌入、聚类和可视化等下游任务保留了足够的几何信号。在实践中，该程序是模块化且可扩展的，它将受 DP 保护的局部几何（均值和切线）与受预算限制的查询点更新分离，并使用简单的调度器在迭代和查询之间分配隐私预算。在关于流形正则性、采样密度和测量噪声的标准假设下，我们建立了高概率效用保证，表明校正后的查询以受样本量、噪声水平、带宽和隐私预算支配的非渐近速率向流形收敛。仿真和案例研究证明了在中等隐私预算下的准确信号恢复，展示了明确的效用-隐私权衡，并为受监管环境下的流形工作流提供了一个可部署的 DP 组件，而无需重新设计隐私系统。

## Abstract
We introduce a differentially private manifold denoising framework that allows users to exploit sensitive reference datasets to correct noisy, non-private query points without compromising privacy. The method follows an iterative procedure that (i) privately estimates local means and tangent geometry using the reference data under calibrated sensitivity, (ii) projects query points along the privately estimated subspace toward the local mean via corrective steps at each iteration, and (iii) performs rigorous privacy accounting across iterations and queries using $(\varepsilon,δ)$-differential privacy (DP). Conceptually, this framework brings differential privacy to manifold methods, retaining sufficient geometric signal for downstream tasks such as embedding, clustering, and visualization, while providing formal DP guarantees for the reference data. Practically, the procedure is modular and scalable, separating DP-protected local geometry (means and tangents) from budgeted query-point updates, with a simple scheduler allocating privacy budget across iterations and queries. Under standard assumptions on manifold regularity, sampling density, and measurement noise, we establish high-probability utility guarantees showing that corrected queries converge toward the manifold at a non-asymptotic rate governed by sample size, noise level, bandwidth, and the privacy budget. Simulations and case studies demonstrate accurate signal recovery under moderate privacy budgets, illustrating clear utility-privacy trade-offs and providing a deployable DP component for manifold-based workflows in regulated environments without reengineering privacy systems.

---

## 论文详细总结（自动生成）

### 论文总结：差分隐私流形去噪 (Differentially Private Manifold Denoising)

#### 1. 核心问题与研究动机
在生物医学、金融和公共卫生等领域，高维数据通常遵循“流形假设”，即集中在低维几何结构附近。利用这些结构进行去噪（Manifold Denoising）能显著提升后续分析的准确性。然而，这些参考数据集往往包含敏感的个人信息。
*   **核心冲突**：流形去噪需要精确的局部几何信息（如切空间），而差分隐私（DP）要求通过注入噪声来模糊信号。
*   **研究目标**：建立一个既能利用敏感参考数据纠正噪声查询点，又能提供严格隐私保证的框架，解决“隐私噪声”与“几何信号保留”之间的矛盾。

#### 2. 方法论
论文提出了一种名为 **DP-MD** 的框架，采用“私有参考集-公开查询点”的模式：
*   **核心思想**：不直接发布去噪后的数据，而是发布受隐私保护的局部几何摘要（局部均值和切向投影矩阵），并利用这些摘要迭代地引导查询点向流形靠拢。
*   **关键技术细节**：
    1.  **隐私局部几何估计**：在查询点附近构建局部协方差矩阵，通过校准后的高斯机制对秩为 $d$ 的正交投影矩阵（切空间估计）和局部加权均值进行私有化处理。
    2.  **灵敏度分析**：严格证明了局部投影矩阵和加权均值在单条记录变更时的灵敏度边界（Sensitivity Bounds），这是实现 DP 的理论基础。
    3.  **迭代校正过程**：查询点通过减去估计的法向分量（即向局部均值投影）来更新位置。
    4.  **隐私核算**：利用零集中差分隐私（zCDP）在多个查询和多次迭代之间分配隐私预算，并可透明地转换为标准的 $(\epsilon, \delta)$-DP。
*   **理论保证**：建立了非渐近误差界限，将误差分解为曲率偏差、测量噪声和隐私噪声三部分。

#### 3. 实验设计
*   **合成数据集**：包括圆（Circle）、环面（Torus）、瑞士卷（Swiss Roll）和高维空间中的球面（$S^2$），用于验证算法在不同曲率、拓扑和维度下的表现。
*   **真实应用场景**：
    1.  **UK Biobank**：包含 50,000 名参与者的 60 种血液和尿液生物标志物，用于下游的疾病风险预测（Cox 模型）。
    2.  **单细胞 RNA 测序 (scRNA-seq)**：使用了 10 个公开数据集（如 Goolam, Yan, Zeisel 等），评估去噪对细胞聚类性能（ARI, NMI）的提升。
*   **对比基准 (Benchmark)**：
    1.  **Raw Data**：原始带噪声数据。
    2.  **Non-private MD**：无隐私保护的流形去噪方法（性能上限）。
    3.  **Gradient Descent (GD)**：传统的梯度下降去噪。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件型号（如 GPU 数量）或总训练时长。
*   但文中提到算法具有良好的**可扩展性**：主导成本是局部 PCA 计算，复杂度随环境维度 $D$ 呈线性或平方级增长，实验中处理了 50,000 样本量级的数据，表明其在标准计算环境下是可行的。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种合成流形、1 个大规模医疗队列研究和 10 个生物信息学数据集。
*   **充分性**：实验设计非常充分。不仅验证了去噪精度，还通过消融实验探讨了隐私预算 $\epsilon$、样本量 $n$、噪声水平 $\sigma$ 和环境维度 $D$ 对结果的影响。
*   **客观性**：对比了非隐私版本的性能，清晰展示了为获得隐私保护而付出的“效用代价”（Utility-Privacy Trade-off），实验结果客观且具有说服力。

#### 6. 主要结论与发现
*   **隐私与效用的平衡**：在适度的隐私预算（如 $\epsilon=1$）下，DP-MD 能够恢复大部分几何信号，性能接近非隐私基准。
*   **高维鲁棒性**：实验表明，随着环境维度 $D$ 的增加，去噪误差保持稳定，证明了该方法能有效克服“维度灾难”。
*   **下游任务增益**：在 UK Biobank 数据中，DP-MD 改善了疾病风险分层的连贯性；在 scRNA-seq 数据中，显著提升了细胞聚类的准确度（ARI 平均提升）。

#### 7. 优点
*   **理论严密**：首次为未知流形上的差分隐私去噪提供了非渐近误差分析。
*   **模块化设计**：将隐私保护的几何估计与查询更新分离，易于集成到现有的流形学习工作流中。
*   **实用性强**：不需要重新设计复杂的隐私系统，通过简单的预算调度器即可部署。

#### 8. 不足与局限
*   **噪声假设**：目前主要针对有界噪声进行了理论证明，对于重尾分布或无界高斯噪声的扩展仍需进一步研究。
*   **查询点假设**：目前假设查询点是公开的（Public Queries），若查询点本身也携带敏感信息，则需要双向隐私保护机制。
*   **全局流形发布**：该方法侧重于点对点的去噪，如何以隐私保护的方式发布整个流形的全局结构（如网格或隐式函数）仍是一个开放性难题。

（完）
