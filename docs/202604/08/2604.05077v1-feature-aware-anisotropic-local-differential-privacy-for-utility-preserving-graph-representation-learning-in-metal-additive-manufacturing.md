---
title: Feature-Aware Anisotropic Local Differential Privacy for Utility-Preserving Graph Representation Learning in Metal Additive Manufacturing
title_zh: 金属增材制造中用于效用保持图表示学习的特征感知各向异性本地差分隐私
authors: "MD Shafikul Islam, Mahathir Mohammad Bappy, Saifur Rahman Tushar, Md Arifuzzaman"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05077v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 针对图表示学习提出了各向异性本地差分隐私（LDP）
tldr: 针对金属增材制造中数据隐私与质量检测效能的矛盾，本文提出FI-LDP-HGAT框架。该框架利用分层图注意力网络（HGAT）捕捉熔池间的空间与热力学耦合，并引入特征重要性感知的各向异性局部差分隐私（FI-LDP）机制。通过在关键特征上分配较少噪声，在保护隐私的同时显著提升了缺陷检测的准确性，实现了隐私保护与工业数据效能的平衡。
selection_source: fresh_fetch
motivation: 现有的金属增材制造质量检测模型忽略了层间物理耦合，且传统局部差分隐私技术因均匀加噪导致模型效能严重下降。
method: 提出结合分层图注意力网络（HGAT）与特征重要性感知各向异性高斯机制（FI-LDP）的计算框架，实现非交互式特征隐私化。
result: "在定向能量沉积数据集上，该方法在严苛隐私预算下仍保持高缺陷召回率，且效能恢复率达81.5%，显著优于传统机器学习和标准差分隐私方法。"
conclusion: 通过各向异性隐私预算分配，FI-LDP-HGAT在保障形式化隐私证明的同时，有效解决了工业数据共享中的隐私与效能权衡难题。
---

## 摘要
金属增材制造 (AM) 能够制造安全关键型部件，但可靠的质量保证依赖于包含专有工艺信息的高保真传感器流，这限制了协作数据共享。现有的缺陷检测模型通常将熔池观测视为独立样本，忽略了层间的物理耦合。此外，传统的隐私保护技术，特别是本地差分隐私 (LDP)，由于在所有特征维度上注入均匀噪声，会导致严重的效用下降。为了应对这些相互关联的挑战，我们提出了 FI-LDP-HGAT。该计算框架结合了两个方法论组件：一个分层图注意力网络 (HGAT)，用于捕获扫描轨迹和沉积层之间的空间和热依赖关系；以及一个特征重要性感知的各向异性高斯机制 (FI-LDP)，用于非交互式特征隐私化。与各向同性 LDP 不同，FI-LDP 使用编码器导出的重要性先验在嵌入坐标上重新分配隐私预算，在保持形式化 LDP 保证的同时，为任务关键的热特征分配较低的噪声，为冗余维度分配较高的噪声。在定向能量沉积 (DED) 孔隙率数据集上的实验表明，FI-LDP-HGAT 在中等隐私预算 (epsilon = 4) 下实现了 81.5% 的效用恢复，并在严格隐私 (epsilon = 2) 下保持了 0.762 的缺陷召回率，同时在所有评估指标上均优于经典机器学习、标准图神经网络 (GNN) 以及包括 DP-SGD 在内的其他隐私机制。机理分析证实了特征重要性与噪声强度之间存在强负相关（Spearman = -0.81），为隐私-效用增益是由有原则的各向异性分配驱动的提供了可解释的证据。

## Abstract
Metal additive manufacturing (AM) enables the fabrication of safety-critical components, but reliable quality assurance depends on high-fidelity sensor streams containing proprietary process information, limiting collaborative data sharing. Existing defect-detection models typically treat melt-pool observations as independent samples, ignoring layer-wise physical couplings. Moreover, conventional privacy-preserving techniques, particularly Local Differential Privacy (LDP), lead to severe utility degradation because they inject uniform noise across all feature dimensions. To address these interrelated challenges, we propose FI-LDP-HGAT. This computational framework combines two methodological components: a stratified Hierarchical Graph Attention Network (HGAT) that captures spatial and thermal dependencies across scan tracks and deposited layers, and a feature-importance-aware anisotropic Gaussian mechanism (FI-LDP) for non-interactive feature privatization. Unlike isotropic LDP, FI-LDP redistributes the privacy budget across embedding coordinates using an encoder-derived importance prior, assigning lower noise to task-critical thermal signatures and higher noise to redundant dimensions while maintaining formal LDP guarantees. Experiments on a Directed Energy Deposition (DED) porosity dataset demonstrate that FI-LDP-HGAT achieves 81.5% utility recovery at a moderate privacy budget (epsilon = 4) and maintains defect recall of 0.762 under strict privacy (epsilon = 2), while outperforming classical ML, standard GNNs, and alternative privacy mechanisms, including DP-SGD across all evaluated metrics. Mechanistic analysis confirms a strong negative correlation (Spearman = -0.81) between feature importance and noise magnitude, providing interpretable evidence that the privacy-utility gains are driven by principled anisotropic allocation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **FI-LDP-HGAT** 的计算框架，旨在解决金属增材制造（AM）领域中，如何在保护企业知识产权（IP）的同时，利用高保真传感器数据进行高精度的缺陷（孔隙）检测。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：金属增材制造（如定向能量沉积 DED）的质量检测依赖于熔池的热信号和空间数据，这些数据包含敏感的工艺“指纹”。为了协作优化，数据需要共享，但直接共享会泄露核心技术秘密。
*   **现有技术局限**：
    *   **建模缺陷**：传统模型（CNN, LSTM）通常将熔池观测视为独立样本，忽略了层与层之间、道与道之间的热累积和物理耦合效应。
    *   **隐私保护缺陷**：传统的本地差分隐私（LDP）采用各向同性加噪（对所有特征维度均匀加噪），导致关键的热特征被淹没在噪声中，模型预测效用（Utility）大幅下降。
*   **研究目标**：开发一种既能捕捉物理耦合关系，又能实现“效用保持”的隐私保护特征发布机制。

### 2. 论文提出的方法论
该框架由三个核心阶段组成：
*   **特征重要性感知预热（Warmup）**：
    *   使用 ResNet-18 提取热图像特征，结合多层感知机（MLP）处理工艺状态和几何上下文。
    *   通过一个简短的监督学习预热阶段，根据分类器权重的绝对值确定各特征维度的“重要性先验” $q$。
*   **FI-LDP（特征感知各向异性本地差分隐私）**：
    *   **核心思想**：各向异性预算分配。在满足总隐私预算 $(\epsilon, \delta)$ 的前提下，利用幂律分布重新分配各维度的 $\epsilon_d$。
    *   **技术细节**：对任务关键维度（重要性高）分配更多的隐私预算（即注入更少的噪声），对冗余维度注入更多噪声。通过模态感知裁剪（Clipping）控制敏感度。
*   **HGAT（分层图注意力网络）建模**：
    *   **图构建**：构建层级限制的混合 kNN 图。边仅存在于同一物理层内，连接空间邻近且热特征相似的节点。
    *   **注意力机制**：引入边缘亲和力先验（Edge-affinity prior），引导注意力机制关注物理上更有意义的邻居节点，从而捕捉热累积效应。

### 3. 实验设计
*   **数据集**：使用 Ti-6Al-4V 薄壁结构的实验数据集，由 OPTOMEC LENS™ DED 系统制造。包含 1,564 个观测点，其中孔隙样本仅占 4.47%（严重类别不平衡）。
*   **Benchmark 与对比方法**：
    *   **非隐私基准**：SVM (RBF), MLP, ResNet-18, GCN, Vanilla GAT。
    *   **隐私保护对比**：DP-SGD（梯度加噪）、Uniform-LDP（各向同性加噪）。
*   **评价指标**：AUC、AUPR（针对不平衡数据更关键）、Recall@0.5、F1-score。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
*   **实现细节**：提到了使用 Adam 优化器，Batch Size 为 64，训练 25 个 Epoch，并使用了加权焦距损失（Weighted Focal Loss）来处理类别不平衡。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了不同隐私预算 $\epsilon \in [0.5, 8.0]$ 下的效用缩放实验。
    *   包含了针对数据增强、各向异性机制、图连接策略的**消融实验**。
    *   进行了**机理分析**，计算了特征重要性与噪声强度的 Spearman 相关系数（$\rho = -0.81$）。
*   **充分性评价**：实验设计较为充分，涵盖了从基准对比到隐私-效用权衡分析的多个维度。通过 5 次随机种子取平均值，保证了结果的客观性。

### 6. 论文的主要结论与发现
*   **效用恢复显著**：在 $\epsilon=4$ 的中等隐私强度下，FI-LDP-HGAT 实现了 81.5% 的效用恢复（相对于无隐私保护的 Oracle 模型）。
*   **优于传统 DP**：在严格隐私预算（$\epsilon=2$）下，该方法保持了 0.762 的召回率，而 DP-SGD 在此类高维不平衡数据上几乎失效（F1 < 0.21）。
*   **各向异性的必要性**：实验证实，预测效用集中在少数特征维度上，FI-LDP 通过保护这些“关键少数”维度，成功缓解了隐私保护带来的性能崩溃。

### 7. 优点（亮点）
*   **物理与数据驱动结合**：HGAT 的层级图结构很好地映射了增材制造的物理过程（层内耦合）。
*   **非交互式隐私发布**：FI-LDP 支持单次特征发布，不需要像联邦学习那样进行多轮通信，更符合工业实时检测场景。
*   **可解释性强**：通过可视化重要性与噪声的关系，证明了性能提升源于有原则的预算分配，而非随机波动。

### 8. 不足与局限
*   **数据规模限制**：实验仅基于 1,564 个样本的小型数据集，在大规模工业生产线上的泛化能力有待验证。
*   **预热阶段依赖**：FI-LDP 需要一个“预热”阶段来估计重要性，这在完全冷启动或标签极度匮乏的场景下可能难以实施。
*   **工艺覆盖面**：目前仅验证了 DED 工艺，对于激光粉末床熔融（LPBF）等其他主流 AM 工艺的适用性尚未讨论。
*   **隐私定义范围**：主要关注特征隐私，未深入探讨图结构本身（节点连接关系）可能泄露的拓扑隐私。

（完）
