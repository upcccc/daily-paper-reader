---
title: Privacy-Preserving Reinforcement Learning from Human Feedback via Decoupled Reward Modeling
title_zh: 通过解耦奖励建模实现隐私保护的基于人类反馈的强化学习
authors: "Young Hyun Cho, Will Wei Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22563v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 针对人类反馈强化学习的差分隐私流水线
tldr: 针对大语言模型在基于人类反馈的强化学习（RLHF）阶段可能泄露敏感用户数据的问题，本文提出了一种解耦的差分隐私（DP）框架。该方法仅在奖励模型学习阶段施加差分隐私保护，并基于此私有奖励模型导出最终策略。理论分析揭示了隐私保护带来的次优性差距及其收敛界限，实验证明该方法在保持隐私的同时，其对齐性能优于现有基线方法。
selection_source: fresh_fetch
motivation: 旨在解决RLHF微调过程中人类偏好数据可能包含敏感隐私信息的问题。
method: 提出一种将差分隐私仅应用于奖励建模阶段，并利用得到的私有奖励模型指导策略优化的解耦框架。
result: 理论上确定了隐私保护下的次优性界限，实验上在Gemma-2B-IT模型上取得了优于现有差分隐私基线的效果。
conclusion: 该研究证明了通过解耦奖励建模实现隐私保护RLHF的有效性，为平衡模型对齐性能与数据隐私提供了新方案。
---

## 摘要
基于偏好的微调已成为训练大语言模型的重要组成部分，而这一阶段使用的数据可能包含敏感的用户信息。一个核心问题是如何设计一个适用于基于人类反馈的强化学习（RLHF）独特结构的差分隐私流水线。我们提出了一个隐私保护框架，该框架仅在奖励学习阶段施加差分隐私，并从生成的私有奖励模型中推导出最终策略。在理论上，我们研究了次优性间隙，并表明隐私保护在通常的非私有统计误差之外引入了一个额外的加性项。我们还建立了一个极小极大下界，并证明主导项随样本量和隐私水平而变化，从而刻画了上界在对数因子意义下达到率最优的区间。在实证方面，合成实验证实了理论预测的缩放规律，而在 Anthropic HH-RLHF 数据集上使用 Gemma-2B-IT 模型进行的实验表明，在各种隐私预算下，该方法比现有的差分隐私基准方法具有更强的私有对齐性能。

## Abstract
Preference-based fine-tuning has become an important component in training large language models, and the data used at this stage may contain sensitive user information. A central question is how to design a differentially private pipeline that is well suited to the distinct structure of reinforcement learning from human feedback. We propose a privacy-preserving framework that imposes differential privacy only on reward learning and derives the final policy from the resulting private reward model. Theoretically, we study the suboptimality gap and show that privacy contributes an additional additive term beyond the usual non-private statistical error. We also establish a minimax lower bound and show that the dominant term changes with sample size and privacy level, which in turn characterizes regimes in which the upper bound is rate-optimal up to logarithmic factors. Empirically, synthetic experiments confirm the scaling predicted by the theory, and experiments on the Anthropic HH-RLHF dataset using the Gemma-2B-IT model show stronger private alignment performance than existing differentially private baseline methods across privacy budgets.

---

## 论文详细总结（自动生成）

这是一份关于论文《Privacy-Preserving Reinforcement Learning from Human Feedback via Decoupled Reward Modeling》的深度结构化总结：

### 1. 核心问题与背景（研究动机）
*   **核心问题**：在大语言模型（LLM）的对齐阶段（RLHF），训练数据包含用户提示词（Prompts）、模型回复及人类偏好。这些数据（尤其是 Prompt）往往包含敏感隐私信息。现有的差分隐私（DP）方法要么只保护偏好标签（Label-DP），要么直接在策略优化（如 DPO 或 PPO）上施加 DP，导致训练极不稳定且性能大幅下降。
*   **研究背景**：RLHF 通常包含奖励建模（RM）和策略优化两个阶段。作者发现，直接对策略进行私有化训练会面临“得分函数（Score Function）”梯度无界的问题，导致梯度裁剪（Clipping）带来巨大的偏差。

### 2. 方法论：解耦奖励建模框架
*   **核心思想**：将隐私预算全部花费在**奖励建模（Reward Modeling）**阶段，而将后续的策略改进视为对私有奖励模型的“后处理（Post-processing）”。根据 DP 的性质，后处理不消耗额外的隐私预算。
*   **关键技术细节**：
    1.  **私有奖励学习**：在预训练模型的冻结表示层之上，训练一个轻量级的线性奖励头。使用 **DP-SGD** 算法进行训练。由于线性层的梯度易于控制且敏感度（Sensitivity）有界，因此比直接训练策略更稳定。
    2.  **策略导出（后处理）**：
        *   **精确推断**：在动作空间较小时，直接根据 Gibbs 分布导出最优策略 $\pi \propto \pi_0 \exp(\eta r)$。
        *   **Best-of-N (BoN) 采样**：在 LLM 等大动作空间场景下，从参考策略 $\pi_0$ 中采样 $N$ 个候选回复，利用私有奖励模型进行重排序，选出得分最高者。
    3.  **理论支撑**：推导了次优性间隙（Suboptimality Gap）的上界，证明隐私误差项以 $\tilde{O}(d/n^2\epsilon^2)$ 的速度衰减，并建立了极小极大下界（Minimax Lower Bound），证明了该方法在特定区间内的率最优性（Rate-optimality）。

### 3. 实验设计
*   **数据集/场景**：
    1.  **合成数据**：线性奖励模型模拟，用于验证理论中的缩放规律（Scaling Laws）。
    2.  **LLM 任务**：使用 **Anthropic HH-RLHF** 数据集（包含 40,000 条对话对），目标是实现“有用性”和“无害性”的对齐。
*   **Benchmark 与对比方法**：
    *   **DP-DPO**：直接在直接偏好优化（DPO）目标上应用 DP-SGD。
    *   **DP-RLHF (DP-RM + DP-PPO-like)**：将数据平分，一半用于私有奖励建模，另一半用于私有策略更新（类似 PPO 的离线版本）。
    *   **参考策略 ($\pi_0$)**：未经过偏好微调的原始模型。

### 4. 资源与算力
*   **硬件**：实验使用了单张 **NVIDIA A100 GPU**。
*   **模型**：基础模型为 **Gemma-2B-IT**。
*   **实现细节**：使用 PyTorch 的 **Opacus** 库进行差分隐私会计（Privacy Accounting）。奖励模型仅训练线性头（2049 个参数），而策略基线（DP-DPO 等）使用 LoRA 适配器（约 10 万个参数）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   合成实验：针对不同维度 $d$、样本量 $n$、隐私预算 $\epsilon$ 和正则化参数 $\eta$ 进行了详尽的参数扫描，每组实验重复 **30 次**并提供 95% 置信区间。
    *   LLM 实验：在 $\epsilon \in \{0.5, 1.0, 2.0\}$ 三种隐私强度下进行，每个设置重复 **3 个独立随机种子**。
*   **充分性评价**：实验设计较为充分。不仅对比了最终性能，还分析了“参考策略退化（Reference Underperformance）”现象和梯度裁剪敏感度，客观地展示了该方法在严苛隐私约束下的鲁棒性。

### 6. 主要结论
*   **性能优越性**：在 LLM 实验中，该方法在所有隐私预算下均显著优于 DP-DPO 和 DP-RLHF。例如在 $\epsilon=0.5$ 时，奖励准确率达到 58.93%，而基线方法仅略高于随机猜测（约 52%）。
*   **稳定性**：解耦框架避免了直接对策略参数进行噪声注入，解决了策略训练中梯度爆炸和裁剪偏差的问题。
*   **理论验证**：合成实验结果完美契合理论预测的收敛速率，证明了隐私代价随样本量增加而快速消失的特性。

### 7. 优点与亮点
*   **结构化创新**：巧妙利用了 RLHF 的两阶段结构，将 DP 压力转移到更易处理的奖励建模阶段。
*   **元算法特性**：该框架是一个“包装器”，可以与任何公共生成模型结合，只需一个私有的重排序层即可实现隐私对齐。
*   **理论完备**：提供了首个针对私有 RLHF 的极小极大下界分析，填补了该领域的理论空白。

### 8. 不足与局限
*   **表示能力受限**：在 LLM 实验中，为了稳定性和理论一致性，仅训练了线性奖励头。如果预训练模型的特征表示（Backbone）不足以捕捉复杂偏好，性能会受限。
*   **推理开销**：Best-of-N 方法在推理时需要生成多个候选样本并评分，相比于直接策略推断，计算成本更高。
*   **依赖参考策略**：该方法的效果高度依赖于参考策略 $\pi_0$ 的采样质量，如果 $\pi_0$ 无法生成高质量候选，奖励模型也无法选出好的结果。

（完）
