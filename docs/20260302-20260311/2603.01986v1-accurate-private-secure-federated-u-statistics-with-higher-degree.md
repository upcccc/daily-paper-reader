---
title: "Accurate, private, secure, federated U-statistics with higher degree"
title_zh: 准确、隐私、安全的高阶联邦 U-统计量
authors: "Quentin Sinh, Jan Ramon"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01986v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 中心差分隐私下的联邦U统计量
tldr: "本研究针对联邦学习环境下高阶 U-统计量（如 Kendall's τ、AUC 等）的计算难题，提出了一种结合多方计算（MPC）与中心化差分隐私（CDP）的安全计算协议。相比于现有的局部差分隐私（LDP）方案，该方法在保证隐私和安全的同时，显著提升了计算精度并解决了离散化规模扩展性差的问题，在实验中将均方误差降低了多达四个数量级。"
selection_source: fresh_fetch
motivation: 现有的联邦 U-统计量计算方法主要依赖局部差分隐私，导致数据效用较低且在处理大规模离散化定义域时扩展性较差。
method: 提出一种利用多方计算（MPC）在中心化差分隐私框架下安全计算 k ≥ 2 阶 U-统计量的协议。
result: "实验结果显示该方法大幅提升了精度，例如在计算 Kendall's τ 系数时，均方误差比现有基准降低了四个数量级。"
conclusion: 该协议为联邦学习中的高阶统计量计算提供了一个兼顾高精度、隐私安全和良好扩展性的有效解决方案。
---

## 摘要
我们研究了在联邦学习环境下，计算阶数为 k ≥ 2 且核函数为 f 的 U-统计量的问题，即计算所有 k 元实例组上函数 f 的平均值。二阶 U-统计量包括多种有用的统计指标，如 Kendall's τ 系数、受试者工作特征曲线下面积（AUC）以及基尼平均差。现有方法仅在效用较低的本地差分隐私模型下提供解决方案，且/或在定义域离散化规模上扩展性较差。在这项工作中，我们利用多方计算（MPC）提出了一种协议，能够在中心化差分隐私下安全地计算阶数 k ≥ 2 的 U-统计量。与之前的解决方案相比，我们的方法显著提高了准确性。我们对其准确性、通信和计算特性进行了详细的理论分析。我们通过实验评估了其性能并获得了良好的结果，例如，对于 Kendall's τ 系数，我们的方法比现有基准方法的均方误差降低了多达四个数量级。

## Abstract
We study the problem of computing a U-statistic with a kernel function f of degree k $\ge$ 2, i.e., the average of some function f over all k-tuples of instances, in a federated learning setting. Ustatistics of degree 2 include several useful statistics such as Kendall's $τ$ coefficient, the Area under the Receiver-Operator Curve and the Gini mean difference. Existing methods provide solutions only under the lower-utility local differential privacy model and/or scale poorly in the size of the domain discretization. In this work, we propose a protocol that securely computes U-statistics of degree k $\ge$ 2 under central differential privacy by leveraging Multi Party Computation (MPC). Our method substantially improves accuracy when compared to prior solutions. We provide a detailed theoretical analysis of its accuracy, communication and computational properties. We evaluate its performance empirically, obtaining favorable results, e.g., for Kendall's $τ$ coefficient, our approach reduces the Mean Squared Error by up to four orders of magnitude over existing baselines.

---

## 论文详细总结（自动生成）

这篇论文针对联邦学习环境下高阶 U-统计量的安全计算问题提出了创新方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：U-统计量（如 Kendall’s $\tau$、AUC、基尼平均差等）在机器学习和统计分析中至关重要，但其计算涉及多个数据实例的组合（$k \ge 2$ 阶），在联邦学习（FL）环境下极具挑战。
*   **现有局限**：目前的方案大多采用**本地差分隐私（LDP）**，这会导致极大的噪声（低效用），或者在处理大规模数据离散化时通信/计算开销呈指数级增长。
*   **核心目标**：在保证数据隐私和安全的前提下，利用**中心化差分隐私（CDP）**模型，在多个参与方之间高效、准确地计算高阶 U-统计量。

### 2. 方法论：核心思想与技术细节
该论文提出了一种名为 **$Q_{U-MPC}$** 的协议，其核心流程如下：
*   **不完全 U-统计量采样**：为了避免计算所有 $k$ 元组（复杂度随 $k$ 指数增长），协议首先通过 `BalancedSamp` 算法生成一个均匀的超图 $E$，仅计算部分元组的平均值。
*   **多方计算（MPC）集成**：
    *   **共享阶段**：参与方将其数据 $x_i$ 进行加法秘密共享（Additive Secret Sharing）。
    *   **计算阶段**：利用安全功能组件（如 FSS 或 GMW 协议）在加密状态下评估核函数 $f$。
    *   **噪声生成阶段**：利用分布式噪声生成技术（如分布式拉普拉斯或高斯机制），在不依赖可信第三方的情况下，共同生成符合 CDP 要求的共享噪声 $\eta$。
*   **聚合与重构**：各方将本地计算的秘密份额与噪声份额求和，最后由聚合器重构出最终的、已加噪的统计结果。
*   **关键算法**：`BalancedSamp` 确保每个数据点参与计算的次数尽可能相等，从而降低了函数的全局敏感度，进而减少了为实现隐私保护所需添加的噪声量。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：均匀分布数据，用于测试不同样本量 $n$ 下的性能。
    *   **Bank Marketing 数据集**：用于计算 Kendall’s $\tau$ 系数（年龄与余额的相关性）。
    *   **Heart Disease 数据集**：用于计算重复对比例（Duplicate Pair Ratio）等分类统计。
*   **对比基准（Benchmarks）**：
    *   **Bell (2020)**：基于 LDP 的通用协议。
    *   **Ghazi (2024)**：基于 Johnson-Lindenstrauss 变换的非交互式 LDP 协议。
    *   **GhaziSM**：Ghazi 协议在洗牌模型（Shuffled Model）下的变体。
*   **评估指标**：均方误差（MSE）、总通信开销、单方计算开销、服务器计算开销。

### 4. 资源与算力
*   **硬件环境**：实验在一台 Linux 服务器上运行，配置为 2.20GHz Intel Xeon 处理器，64GB RAM。
*   **加速器**：配备了一块 **NVIDIA Tesla P100 GPU (12GB)**。
*   **说明**：论文未明确给出具体的训练/运行总时长，但详细分析了通信复杂度和计算复杂度。

### 5. 实验数量与充分性
*   **实验规模**：针对三种不同的 U-统计量（基尼平均差、Kendall’s $\tau$、重复对比例）分别进行了实验。
*   **变量控制**：涵盖了不同参与方数量 $n$（从数百到数千）、不同离散化桶数 $t$、以及不同的隐私预算 $\epsilon$。
*   **充分性评价**：实验设计较为充分，不仅对比了精度（MSE），还深入分析了通信与计算的权衡。通过使用真实世界数据集和合成数据集，验证了算法在不同场景下的鲁棒性。

### 6. 主要结论与发现
*   **精度大幅提升**：在相同隐私保证下，该协议的 MSE 比现有 LDP 基准降低了多达 **4 个数量级**（例如在 Kendall’s $\tau$ 计算中）。
*   **通信效率**：虽然使用了 MPC，但通过不完全采样和优化的秘密共享，通信开销与最先进的 LDP 方案具有可比性，甚至在某些配置下更优。
*   **计算分布**：相比于 Bell 等方案将压力集中在服务器端，本协议通过 MPC 将计算负载更均匀地分布在参与方之间，显著降低了服务器端的负担。

### 7. 优点
*   **高精度（Utility）**：通过从 LDP 转向 CDP，极大地提高了统计结果的可用性。
*   **通用性**：协议框架支持任意阶数 $k \ge 2$ 的 U-统计量，不局限于特定的核函数。
*   **安全性强**：在半诚实（Semi-honest）威胁模型下提供了严格的隐私证明。
*   **采样优化**：`BalancedSamp` 算法有效平衡了计算成本与隐私敏感度。

### 8. 不足与局限
*   **威胁模型限制**：目前主要针对**半诚实**对手，在恶意（Malicious）对手模型下的鲁棒性有待进一步研究。
*   **通信轮数**：作为 MPC 协议，其交互轮数多于非交互式的 LDP 方案，在网络延迟极高的环境下可能受限。
*   **离散化依赖**：虽然比前人方案表现更好，但仍依赖于定点算术和有限域表示，对于极高精度的连续函数计算可能存在截断误差。

（完）
