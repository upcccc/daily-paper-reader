---
title: "FecalFed: Privacy-Preserving Poultry Disease Detection via Federated Learning"
title_zh: FecalFed：基于联邦学习的隐私保护家禽疾病检测
authors: Tien-Yu Chi
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00559v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 用于家禽疾病分类的隐私保护联邦学习框架
tldr: "本研究针对家禽疾病检测中的数据隐私和数据孤岛问题，提出了名为FecalFed的联邦学习框架。作者首先发布了经过严格去重的poultry-fecal-fl数据集，消除了公共数据中近47%的重复项。通过在非独立同分布环境下应用FedAdam优化和Swin Transformer模型，该框架在保护农场隐私的同时，实现了高达90.31%的分类准确率，显著优于单场训练效果，为全球粮食安全提供了隐私保护的监测方案。"
selection_source: fresh_fetch
motivation: 旨在解决家禽疾病检测中因隐私保护导致的数据孤岛问题，以及现有公开数据集中严重的重复污染现象。
method: 提出了FecalFed联邦学习框架，结合FedAdam优化算法与Swin Transformer架构，并在高度异构的非独立同分布数据上进行验证。
result: "联邦学习方案在非独立同分布条件下达到了90.31%的准确率，远超单场训练的64.86%，且接近中心化训练的性能。"
conclusion: 该研究证明了联邦学习在农业疾病监测中的可行性，为构建隐私保护且高效的农场监测系统提供了技术蓝图。
---

## 摘要
高致病性禽流感 (HPAI) 和地方性家禽疾病的早期检测对全球粮食安全至关重要。虽然计算机视觉模型在通过粪便图像进行疾病分类方面表现出色，但由于农场数据隐私担忧和机构间的数据孤岛，大规模部署这些系统面临瓶颈。此外，现有的开源农业数据集经常受到严重的、未记录的数据污染。在本文中，我们介绍了 FecalFed，这是一个用于家禽疾病分类的隐私保护联邦学习框架。我们首先整理并发布了 poultry-fecal-fl，这是一个经过严格去重的、包含四个疾病类别共 8,770 张唯一图像的数据集，揭露并消除了流行公共仓库中 46.89% 的重复率。为了模拟真实的农业环境，我们在高度异构、非独立同分布 (non-IID) 的条件下（Dirichlet α=0.5）评估了 FecalFed。虽然孤立的单农场训练在这种数据异构性下会崩溃，准确率仅为 64.86%，但我们的联邦方法在不集中敏感数据的情况下恢复了性能。具体而言，利用服务器端自适应优化 (FedAdam) 和 Swin-Small 架构实现了 90.31% 的准确率，接近 95.10% 的集中式训练上限。此外，我们证明了边缘优化的 Swin-Tiny 模型保持了 89.74% 的极具竞争力的性能，为农场禽类疾病监测建立了一个高效、隐私优先的蓝图。

## Abstract
Early detection of highly pathogenic avian influenza (HPAI) and endemic poultry diseases is critical for global food security. While computer vision models excel at classifying diseases from fecal imaging, deploying these systems at scale is bottlenecked by farm data privacy concerns and institutional data silos. Furthermore, existing open-source agricultural datasets frequently suffer from severe, undocumented data contamination. In this paper, we introduce $\textbf{FecalFed}$, a privacy-preserving federated learning framework for poultry disease classification. We first curate and release $\texttt{poultry-fecal-fl}$, a rigorously deduplicated dataset of 8,770 unique images across four disease classes, revealing and eliminating a 46.89$\%$ duplication rate in popular public repositories. To simulate realistic agricultural environments, we evaluate FecalFed under highly heterogeneous, non-IID conditions (Dirichlet $α=0.5$). While isolated single-farm training collapses under this data heterogeneity, yielding only 64.86$\%$ accuracy, our federated approach recovers performance without centralizing sensitive data. Specifically, utilizing server-side adaptive optimization (FedAdam) with a Swin-Small architecture achieves 90.31$\%$ accuracy, closely approaching the centralized upper bound of 95.10\%. Furthermore, we demonstrate that an edge-optimized Swin-Tiny model maintains highly competitive performance at 89.74$\%$, establishing a highly efficient, privacy-first blueprint for on-farm avian disease monitoring.

---

## 论文详细总结（自动生成）

这是一份关于论文《FecalFed: Privacy-Preserving Poultry Disease Detection via Federated Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在大规模部署家禽疾病（如禽流感、球虫病等）自动检测系统的同时，解决农场主因生物安全和商业利益而不愿共享数据的“数据孤岛”问题，并应对现有公开数据集中严重的“数据污染”现象。
*   **研究背景**：
    *   **疾病威胁**：高致病性禽流感（HPAI）严重威胁全球粮食安全。
    *   **隐私瓶颈**：中心化训练需要上传原始图像，存在隐私泄露风险。
    *   **数据质量危机**：作者发现现有的开源家禽粪便图像数据集存在大量未记录的重复项（重复率高达 46.89%），导致模型性能被虚假夸大。
    *   **单点失效**：在现实的非独立同分布（Non-IID）环境下，单个农场独立训练的模型准确率极低。

### 2. 核心方法论
*   **数据清洗（poultry-fecal-fl）**：
    *   **双哈希去重**：采用平均哈希（aHash）和感知哈希（pHash）结合的流水线，通过计算汉明距离识别并剔除重复或缩放后的图像。
    *   **标准化**：将来自 Zenodo 和 Roboflow 的多个源数据整合，清洗出 8,770 张唯一图像，涵盖健康、球虫病、新城疫和沙门氏菌四类。
*   **FecalFed 联邦学习框架**：
    *   **架构**：基于 Flower 框架的跨孤岛（Cross-silo）联邦学习。原始数据保留在农场边缘端，仅上传模型权重。
    *   **自适应优化（FedAdam）**：针对 Non-IID 数据导致的模型漂移，在服务器端使用 FedAdam 优化器进行权重聚合，以稳定训练过程。
    *   **模型选择**：对比了视觉 Transformer（ViT）和 Swin Transformer 架构，并重点评估了适合边缘部署的轻量化模型（如 Swin-Tiny）。
    *   **Non-IID 模拟**：使用 Dirichlet 分布（$\alpha=0.5$）对数据进行分区，模拟不同农场爆发不同疾病的真实异构场景。

### 3. 实验设计
*   **数据集**：自建的 `poultry-fecal-fl`（8,770 张图像）。
*   **实验场景对比**：
    1.  **中心化训练（Centralized）**：性能上限（Upper Bound）。
    2.  **孤立单农场训练（Isolated Single-Farm）**：性能下限，模拟不合作的情况。
    3.  **联邦学习（FecalFed）**：评估 FedAvg 和 FedAdam 两种聚合策略。
*   **模型 Benchmark**：Swin-Small (50M)、ViT-B/16 (86M)、Swin-Tiny (28M)、ViT-S/16 (22M)。
*   **消融实验**：针对通信轮数（5 到 20 轮）对收敛性的影响进行了消融研究。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA A100 GPU** 进行联邦学习过程的模拟。
*   **参数设置**：本地训练 Batch Size 为 256，每轮本地训练 1 个 Epoch。
*   **时长**：标准实验执行 10 个全局通信轮次，消融实验扩展至 20 轮。文中未明确给出总训练时长（小时/分钟），但强调了通过冻结主干网络仅微调分类头来降低计算开销。

### 5. 实验数量与充分性
*   **实验规模**：设置了 10 个模拟农场客户端，每轮随机抽取 50%（5 个）参与训练。
*   **充分性评价**：
    *   **客观性**：通过去重解决了数据泄露导致的性能虚高问题，实验基准更真实。
    *   **覆盖面**：涵盖了主流的 Transformer 架构，并对比了不同优化器和训练范式。
    *   **公平性**：在相同的 Non-IID 分布下对比单场训练与联邦训练，有力证明了协作的必要性。

### 6. 主要结论与发现
*   **单场训练崩溃**：在 Non-IID 条件下，单农场模型准确率仅为 **64.86%**，且方差极大，证明独立训练不可行。
*   **联邦学习性能优异**：FecalFed 使用 FedAdam + Swin-Small 达到了 **90.31%** 的准确率，仅比中心化训练低 4.79%。
*   **边缘友好性**：**Swin-Tiny** 模型以仅 28M 的参数量实现了 **89.74%** 的准确率，是农场嵌入式设备（如 Jetson Nano）部署的最优选择。
*   **通信效率**：通过冻结 Backbone 仅传输分类头权重，极大地降低了带宽需求，适合农村低带宽网络。

### 7. 优点与亮点
*   **数据贡献**：首次揭露了家禽疾病数据集严重的重复污染问题，并发布了高质量的去重基准数据集。
*   **现实针对性**：实验设计充分考虑了农业场景的 Non-IID 特性，而非理想化的 IID 假设。
*   **实用性强**：不仅关注准确率，还兼顾了边缘设备的算力限制和通信带宽限制。

### 8. 不足与局限
*   **客户端规模较小**：实验仅模拟了 10 个客户端，在更大规模（如成百上千个农场）下的收敛性和异构性挑战尚待验证。
*   **疾病种类有限**：目前仅针对 4 类常见情况，对于更多罕见病或变异毒株的泛化能力未讨论。
*   **缺乏真实硬件测试**：虽然提及了边缘部署，但实验是在 A100 上模拟的，缺乏在真实嵌入式设备（如手机或 IoT 网关）上的端到端延迟测试。
*   **隐私攻击防御**：论文主要关注数据不离开本地，但未深入探讨针对模型权重的成员推理攻击或梯度泄露攻击的防御措施（如差分隐私）。

（完）
