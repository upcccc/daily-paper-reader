Title: Differentially Private Synthetic Voltage Phasor Release for Distribution Grids

URL Source: https://arxiv.org/pdf/2605.02390v1

Published Time: Tue, 05 May 2026 01:48:20 GMT

Number of Pages: 13

Markdown Content:
> 1

# Differentially Private Synthetic Voltage Phasor Release for Distribution Grids 

Andrew Campbell, Chenyue Zhang, Anna Scaglione, Eli Kerr, Merilyn Chesler, and Sean Peisert 

Abstract —Training machine learning models, including Grid Foundation Models (GFMs), requires large volumes of realistic grid data, yet substantial privacy concerns discourage utilities and data providers from sharing load profiles and network parameters. We study the release of synthetic voltage phasor trajectories for distribution grids under differential privacy (DP). We first fit a DP generative model to historical customer loads, then propagate synthetic load trajectories through the AC power flow equations on the true admittance matrix to produce voltage phasors. The central question is whether the randomness already present in the DP synthetic loads is sufficient to protect not only the loads, but also the network topology encoded by the bus admittance matrix. We show that it is. The implication is that a corpus of voltage trajectories can be constructed from DP synthetic loads while preserving the statistics of AC power flow, which is critical for training GFMs. This preservation of the power flow statistics stands in contrast to approaches that perturb the admittance matrix directly or inject noise into the voltage outputs, both of which distort the underlying physics. Concretely, we derive (ε, δ )-DP guarantees for the released voltage trajectories with respect to the admittance matrix, meaning privacy of the network parameters is obtained without any additional noise mechanism. Our bound depends on the adjacency assumption, the Jacobian of the AC power flow, and the covariance of the synthetic DP-loads. Finally, we present a synthetic voltage generation procedure and an empir-ical evaluation against Gaussian output-perturbation baselines, demonstrating that our approach provides a clear advantage for enabling GFM training. 

I. I NTRODUCTION 

The electrification of transportation, the rapid deployment of distributed energy resources (DERs), and the increasing complexity of distribution-grid operations are driving unprece-dented demand for data-driven methods in power systems. Machine learning (ML) models trained on power flow data now support state estimation, fault detection, voltage regula-tion, load forecasting, and optimal power flow (OPF) approx-imation [1], [2]. More ambitiously, the community has begun exploring Grid Foundation Models (GFMs) [3]: large-scale pre-trained backbones for diverse grid tasks, which depend on massive heterogeneous datasets spanning load profiles, voltage trajectories, generation patterns, and network topologies across many feeders and operating conditions. Assembling such 

A. Campbell, C. Zhang, and A. Scaglione are with the Department of Electrical and Computer Engineering, Cornell Tech, Cornell University, New York, NY 10044, USA (e-mails: {ac2458, cz563, as337 }@cornell.edu). Eli Kerr and Merilyn Chesler are with Kevala Inc. S. Peisert is with Computing Sciences Research, Lawrence Berkeley National Laboratory, Berkeley, CA 94720, USA (e-mail: sppeisert@lbl.gov). This research was supported in part by the Director, Cybersecurity, Energy Security, and Emergency Response (CESER) office of the U.S. Department of Energy, under contract DE-AC02-05CH11231. 

Fig. 1. Two DP Paradigms for GFMs. Top is the proposed DP-GMM methodology. Bottom is the standard DP-SDG approach for model training. 

datasets, however, confronts a fundamental tension: the very data that would make GFMs most useful are precisely those that utilities are least willing to share .For distribution systems operators (DSOs), two categories of data are confidential: (1) load consumption data from advanced metering infrastructure (AMI) sensors, and (2) sys-tem parameters in the bus admittance matrix Y . Voltage phasor measurements depend on both, so their release can let an adversary infer individual consumption behaviors or reconstruct the admittance matrix [4]–[7]. Bus generation, by contrast, is generally treated as public [8]–[10] given the availability of irradiance measurements and public PV capacity registrations, a convention we adopt throughout. The goal for GFMs is therefore to develop privacy procedures that protect both the load data and the admittance matrix. 

A. Synthetic Data as the Grid AI Privacy-Preserving Enabler 

Synthetic data generation has emerged as a compelling solution to the data-scarcity and privacy bottleneck [1], [11], [12], and recent reviews highlight both the field’s rapid ex-pansion and open challenges around standardized evaluation and application-specific realism [12]. By fitting a generative model to real data and releasing samples from the model rather              

> arXiv:2605.02390v1 [eess.SY] 4 May 2026 2
> TABLE I ADVANTAGES AND DISADVANTAGES OF THE TWO DP PARADIGMS FOR GRID FOUNDATION MODELS ILLUSTRATED IN FIG . 1.
> DP training data (proposed) DP-SGD variants
> Advantages (1) Protects Yand loads from one mechanism (Thm. 1); (2) voltages are physics-consistent; (3) budget spent once at generation, so downstream training is free under post-processing; (4) DP-GMM preserves data statistics; (5) task-agnostic corpus. (1) Simple to implement; (2) no parametric assumption on loads. Disadvantages (1) DP-GMM is non-trivial to implement; (2) utility bounded by DP-GMM fit quality. (1) Budget composes over iterations; (2) i.i.d. noise destroys correlation; (3) sample-level guarantee requires lossy group-privacy reduction for Y; (4) output is a trained model, not a reusable dataset.

than the data itself, one can produce arbitrarily large training corpora while limiting disclosure of sensitive information. Approaches explored so far include Generative Adversarial Networks (GANs) for smart-grid time series [13], physics-informed synthetic datasets for transmission grids [11], and AI-based techniques for fault classification [2]. A critical shortcoming of these approaches is the lack of formal privacy guarantees . Differential privacy (DP) [14] provides a principled framework for bounding information leakage, but incorporating it into synthetic data pipelines for grid applications remains challenging, and recent surveys emphasize that strong formal privacy with downstream utility is especially difficult in domain-specific settings [15]. The literature is fragmented: some works generate realistic syn-thetic data with no privacy protection; others add i.i.d. noise to real data to achieve DP at the cost of destroying temporal correlations essential for ML training [16]–[18]. Still others train generative models with DP-SGD [19], [20], inheriting the well-known utility degradation of gradient-level perturba-tion. Recent constrained generative approaches improve the physical realism of synthetic power flow data but do not provide topology-level privacy guarantees [21]. See Figure 1 for an overview of the DP synthetic data corpus vs DP-SGD for machine learning (ML) tasks. Additionally, in Table I we provide the advantages and disadvantages of each approach. Prior work has developed along three separate threads: synthetic grid-data generation [1], [12], privacy-preserving smart-grid data release [10], and topology/state inference or obfuscation under power flow models [6]–[8], [22]. The closest prior work, and the only one to bridge these threads, is [23], which generates DP synthetic power-system datasets via Laplace and exponential mechanisms with convex post-processing to restore downstream-task feasibility. Their two algorithms protect wind power measurements (for regression) and DC-OPF transmission capacity vectors (for feasibility across OPF models). Our work differs in the protected object and the setting: (i) we protect loads and the full admittance matrix Y rather than wind generation or line capacity ratings; (ii) we operate on the AC power flow manifold rather than on DC-OPF; and (iii) we retain the true Y during power flow computation and release voltage phasor trajectories, whereas [23] releases synthetic network parameters consumed by a separate downstream OPF solve. By propagating a DP generative model for loads through the true AC power flow equations, our approach yields physics-consistent voltage tra-jectories that protect both consumer loads and the admittance matrix with no additional output perturbation. Table II synthesizes the state of the art and highlights the unique position of the present work. To the best of our knowledge, no prior approach simultaneously achieves all of the following: (i) releases data from a simple, interpretable generative model with strong (ε, δ )-DP guarantees directly on the model parameters; (ii) minimizes the divergence between the privatized and true data distributions, thereby bounding the degradation of downstream ML models trained on the synthetic data; (iii) enables the release of physics-consistent system states 

(voltage phasors) computed on the true network model, without additional output noise; and (iv) provides formal DP guarantees on the system topology (Y ) as a byproduct of the same noise mechanism that protects the loads. Our pipeline builds on two pieces of our prior work: [24] developed differentially private K-Means clustering for AMI data, and [25] generalized this to a full Gaussian Mixture Model (DP-GMM) that adds calibrated noise to the means, covariances, and mixture weights while minimizing the KL di-vergence between the privatized and true distributions subject to the (ε, δ )-DP constraint. The GAN-based approach of [20] offers an alternative generative mechanism compatible with our framework, but it trains via DP-SGD [19], which provides weaker control over distribution fidelity. 

B. From Private Loads to Private Grid States: Two Birds with One Stone 

We address the privacy challenge through a single mecha-nism that protects loads and voltage phasors simultaneously by: (a) fitting a DP generative model to the loads (DP-GMM), (b) sampling synthetic loads from it, and (c) solving AC power flow on the true admittance matrix Y with those synthetic loads. The key insight is that the privacy noise in the synthetic loads propagates through the power flow equations 

and induces sufficient randomness in the released voltage phasors to mask Y , so no additional noise is required on the output voltages. This “two birds with one stone” property has profound implications for GFMs. A utility or data aggregator, can release from a single privacy mechanism both differentially private load profiles and the corresponding voltage phasor trajectories. These voltage phasor trajectories are computed by solving power flow on the true system model with public PV generation profiles. The released voltage data are physics-consistent (they satisfy the AC power flow equations on the 3                                                                     

> TABLE II COMPARISON OF APPROACHES FOR PRIVACY -PRESERVING SYNTHETIC DATA RELEASE IN POWER SYSTEMS .
> Approach Load DP Topology DP Distrib. fidelity Unlimited load samples True Yin PF
> i.i.d. noise on loads [16]–[18] ✓×××N/A DP federated learning [26] ✓×××N/A DP-SGD GAN (DPWGAN) [20] ✓××∗✓N/A Noise on Yentries [22], [27] ×✓†×××Noise on OPF outputs [28] ××××✓
> Bilevel obfuscation [29] ✓×××✓
> DP K-Means clustering [24] ✓××✓N/A DP-GMM (KL-optimal) [25] ✓×✓✓N/A DP synthetic grid datasets [23] ××‡✓××
> This work (DP-Powerflow) ✓✓✓✓✓
> ∗GAN fidelity is implicit via adversarial training but not measured via a closed-form divergence bound.
> †Protects Yunder per-parameter metric DP rather than the r-adjacency used here (Definition 4); see Remark 3.
> ‡Provides DP on a scalar DC-OPF transmission capacity vector ¯funder entry-wise α-adjacency; the DC power-transfer distribution matrix (encoding graph topology and line susceptances) is held fixed and released in the clear, so no formal DP guarantee applies to topology or impedances.

real network) and distribution-preserving (the load distribution is not further corrupted by output noise), making them far more suitable for ML training than data from output, or network, perturbation baselines. This result is a first step toward a broader program. If power flow states can be released privately using synthetic loads on the true grid, then other derived quantities, such as OPF solutions, hosting-capacity assessments, or reliability indices, can also be released using the true system parameters and synthetic inputs, inheriting privacy guarantees from the same DP load model. Formalizing this extension is an important direction for future work. A naive way to protect both loads and topology would be to first privatize the loads via additive noise [16]–[18], [30], then apply the DP power flow release of [22] to protect Y .This benchmark has two shortcomings. First, the load-noising mechanisms of [16]–[18], [30] adopt user-level adjacency on individual consumption profiles and say nothing about the admittance matrix, while the adjacency notion of [22] is per-parameter metric DP on Y and does not permit topology changes. In contrast, we consider a Frobenius-ball adjacency on Y which admits joint perturbations across all entries of Y ,capturing genuine topology changes such as line switching rather than just small entry-wise deviations. Second, nois-ing loads and then separately privatizing the induced states compounds distortion, since the two mechanisms inject noise independently at different stages of the power flow. Other DP approaches to grid optimization face related limitations: [22] noises line conductances directly; [29] noises loads and re-stores feasibility via bilevel optimization; and [28] noises OPF solutions without releasing loads or states. In contrast, our mechanism obtains formal DP guarantees on the admittance matrix directly from the noise in the DP synthetic loads , with no additional perturbation of the power flow outputs. To our knowledge, this work is the first result of its kind. 

C. Contributions 

This paper makes the following contributions:  

> •

Joint DP for loads and topology from a single mecha-nism. We show that the randomness in DP synthetic loads, propagated through the power flow equations, suffices to guarantee (ε, δ )-DP on both the loads and the admittance matrix, with no additional noise on the released voltages.  

> •

Physically meaningful adjacency for admittance ma-trices. We introduce a Frobenius-ball adjacency on Yfull 

with a manifold-preservation condition (Definition 4), which admits joint perturbations across entries—including line switching—rather than fixing the sparsity pattern as in per-parameter metric DP.  

> •

Closed-form (ε, δ )-DP bound. We derive a tractable pri-vacy bound (Theorem 1) that decomposes into an injection-likelihood term and a Jacobian-determinant term, each bounded via the Kron amplification factor, the power flow Jacobian, and the load covariance.  

> •

Synthetic voltage generation pipeline. We present Algo-rithm 1 and empirically demonstrate a clear advantage over Gaussian output-perturbation baselines in terms of MSE at matched privacy levels. II. S YSTEM MODEL 

We consider a distribution grid with N buses described by a full complex admittance matrix Yfull ∈ CN ×N . We first de-scribe the Kron reduction that eliminates zero-injection buses, since the privacy guarantee is stated in terms of perturbations of Yfull while the analysis operates on the reduced matrix Y .

A. Kron Reduction and Zero-Injection Buses 

Let v ∈ CN denote the vector of complex bus voltage phasors, with [v]k the voltage at bus k. Partition the N

buses into the set R of retained buses (those carrying loads or generation, |R| = n) and the set Z of zero-injection 

buses ( |Z| = N − n), with corresponding voltage subvectors 

vR ∈ Cn and vZ ∈ CN −n. Block the full admittance matrix accordingly: 

Yfull =

YRR YRZ 

YZR YZZ 



. (1) Since zero-injection buses satisfy, where sk is the injection at bus k, sk = 0 for all k ∈ Z , the power flow equations at Z

reduce to the linear system YZR vR + YZZ vZ = 0 , which, 4

since YZZ is invertible for any connected network with at least one retained bus, yields 

vZ = −Y −1 

> ZZ

YZR vR =: Φ vR,

Φ : = −Y −1 

> ZZ

YZR ∈ C(N −n)×n. (2) Substituting (2) into the retained-bus equations gives the exact 

Kron-reduced system 

sR = Y v R, Y := YRR −YRZ Y −1 

> ZZ

YZR ∈ Cn×n, (3) which is the reduced admittance matrix used throughout. 

Remark 1. Equation (2) shows that vZ is a deterministic linear function of vR, so once vR is fixed the entire network voltage profile is fixed. That is, the randomness from the DP-GMM propagates to vZ through Φ and the reduced-system analysis is complete. Moreover, since |[vZ ]k| ≤ ∥ [Φ]k, :∥1 ·

max j |[vR]j | with row sums of Φ bounded by construction on radial feeders, vZ automatically satisfies ANSI C84.1 regulation limits whenever vR does. No separate treatment of or assumption on vZ is needed. 

A perturbation of Yfull induces a perturbation of the reduced matrix Y , and the induced Frobenius distance on the reduced system can be controlled by a scalar amplification factor κKron 

computable from network data alone. 

Definition 1 (Kron amplification factor) . For a reference full admittance matrix Yfull partitioned as in (1) , define 

κKron := 1+2 ∥YRZ ∥op ∥Y −1 

> ZZ

∥op +∥YRZ ∥2op ∥Y −1 

> ZZ

∥2op . (4) 

Corollary 1. For any two full admittance matrices Yfull , Y ′

> full

with ∥Yfull − Y ′

> full

∥F < r , the Kron-reduced matrices Y , Y ′

given by (3) satisfy, to leading order in ∥Yfull − Y ′

> full

∥F ,

∥Y − Y ′∥F ≤ κKron ∥Yfull − Y ′

> full

∥F < κ Kron r. (5) Throughout the remainder of the paper, the symbol Y (with-out subscript) always denotes the Kron-reduced admittance matrix (3), while Yfull denotes the original unreduced matrix. The voltage vector vt ∈ Cn refers to the retained-bus voltages 

vR,t . Next we introduce the AC power flow model on the reduced system. 

B. AC Power Flow 

The nonlinear AC power flow equations relate the complex injection vector st ∈ Cn to voltages and admittances through the map FY : Cn → Cn where: 

st := FY (vt) = diag( vt)   ¯Y ¯vt + ¯b , (6) with vt ∈ Cn denoting the retained-bus voltage trajectory at time t and b ∈ Cn the constant-current offset induced by the slack bus. Specifically, letting vslack ∈ C denote the (fixed) slack-bus voltage and indexing the retained buses to exclude the slack, 

b := [ Yfull ]retained , slack vslack , (7) so b is known exactly from the network model and the slack-bus reference. Since the privacy analysis requires real-valued Jacobians and determinants, we work with the real representation ˜FY : R2n → R2n, defined by 

˜FY :

Re[ vt]Im[ vt]



7→

Re[ st]Im[ st]



, st = FY (vt). (8) We denote its Jacobian by J ˜FY (v) ∈ R2n×2n. Since ˜FY is quadratic in vt and generally non-invertible, we restrict the analysis to a “good” voltage set defined in Section II-D on which ˜FY is invertible and J ˜FY is well defined. 

C. Injection Model 

We adopt the standard net injection convention where gen-eration is positive and load is negative, so the total complex injection decomposes as 

st = sgt − spt , (9) where spt ∈ Cn is the complex load vector with Re {[spt ]k} ≥ 0

the active load magnitude and Im {[spt ]k} ≥ 0 the reactive load magnitude. Since active load is well modeled by log-normal distributions [25], [31]–[33], we partition buses into L ≪ n

load classes C1, . . . , CL, each representing a consumer type (industrial, residential, etc.). Let pt ∈ Rn denote the active load at time t. For each bus k ∈ C ℓ, the T -dimensional log-load vector over the release horizon is jointly Gaussian: 



log[ pℓ

> 1

]k, . . . , log[ pℓT ]k

⊤

∼ N  μ(ℓ), Σ(ℓ)

> T

,

∀ k ∈ C ℓ, ℓ ∈ [L], (10) where μ(ℓ) ∈ RT is the time-varying class mean profile and 

Σ(ℓ) 

> T

∈ RT ×T with Σ(ℓ) 

> T

≻ 0 is the temporal covariance. The log-load vectors {(log[ pℓ

> 1

]k, . . . , log[ pℓT ]k)}k∈C ℓ are indepen-dent across buses, with common distribution (10). Each load bus k ∈ C ℓ has a fixed power factor angle θ(ℓ) 

> k

∈ [−π/ 2, π/ 2] ,so reactive power satisfies 

[Im {spt }]k = tan( θ(ℓ) 

> k

) [ pℓt ]k. (11) Let G ⊂ { 1, . . . , n } denote the buses with Photovoltaic (PV) generation. For each k ∈ G , |[sgt ]k| = γkhgt , where hgt ≥ 0 is the public irradiance and γk > 0 is installed capacity. Smart inverters couple the reactive injection to local voltage through a volt-var curve ϕk([ vt]k) : R+ → [−π/ 2, π/ 2] , giving 

[sgt ]k = γkhgt · ejϕ k (|[vt]k |). (12) Letting Γ = diag( γk)k∈G ∈ Rn×n and [hgt (vt)] k =

hgt ejϕ k (|[vt]k |) ∈ C for k ∈ G , the combined system becomes the fixed-point problem: 

diag( vt)   ¯Y ¯vt + ¯b = 

> sgt(vt)

z }| {

Γhgt (vt) − spt . (13) Thus (13) defines the load-to-voltage map only implicitly, and it does not necessarily admit a unique smooth solution every-where. The next subsection restricts to operating conditions under which this map is well-defined and locally invertible. 5

D. “Good” Voltage Set 

To address the invertibility concerns raised by (13), we restrict voltages to a “good” set defined as follows. 

Definition 2 (“Good” Voltage Set) . Fix Vmin , V max > 0.Define 

S0 := 



v ∈ Cn : Vmin ≤ | [v]k| ≤ Vmax , ∀ k



. (14) The geometric constraints in S0 reflect standard volt-age regulation limits (e.g., ANSI C84.1 specifies |vk| ∈

[0 .95 , 1.05] p.u.). However, restricting voltages to S0 alone does not guarantee that the power flow map is locally invertible or that the implied loads are physically meaningful. We there-fore require the admittance matrix to satisfy the following. 

Assumption 1 (Normal Operating Conditions) . The feeder operates under normal conditions; specifically: 

(i) Voltage stability margin. The system is not operating at or beyond its voltage stability limit, i.e. the power flow Jacobian is nonsingular on S0:

inf  

> v∈S 0, Y ∈Y feas

σmin 

 J ˜FY (v) > 0.

This is automatic for any normally loaded feeder under ANSI C84.1 regulation limits, and by the implicit function theorem is equivalent to local uniqueness of the high-voltage solution branch (i.e. no voltage collapse). 

(ii) Rated loading envelope. Every retained-bus voltage profile 

vR ∈ S 0 corresponds to active loads within the design range 

[p(ℓ)min , p (ℓ)max ] for each load class ℓ:

p(ℓ)min ≤  ˆpY (vR) 

> k

≤ p(ℓ)max , ∀ vR ∈ S 0, k ∈ C ℓ,

where [ ˆ pY (v)] k := ℜ([ sg (v)] k) − ℜ  [v]k(Pnj=1 Ykj [v]j +

bk) is the implied active load at bus k. This excludes both unloaded and overloaded feeders, with p(ℓ)min , p (ℓ)max read from the feeder’s load study or AMI history. By Remark 1 the zero-injection voltages are controlled automatically. 

(iii) IEEE 1547-2018 volt-var compliance (when appli-cable). When smart inverters operate under a volt-var curve ϕk, the combined Jacobian remains nonsingular on 

S0: inf v∈S 0 σmin (Jeff ˜FY

(v)) > 0. IEEE 1547-2018 requires 

|ϕ′

> k

(u)| < 1/(γkhg

> max

), which automatically enforces this for any certified deployment; absent volt-var coupling, con-dition (iii) reduces to (i). 

The margins [p(ℓ)min , p (ℓ)max ] in condition (ii) serve two pur-poses: they guarantee that the log-normal likelihood ratio in the privacy analysis is bounded, and they define the support of the truncated log-normal distribution from which synthetic loads are sampled (see Definition 5 and Algorithm 1). 

E. Pushed-Forward Voltage Distribution 

The net injection st is random only through the n-dim active-load vector pt since the reactive component qt =tan( Θ)pt, is a deterministic function of pt via the fixed power factors (11), and sgt (vt) is a deterministic function of vt

via (12). The distribution of st on R2n is therefore supported on an n-dim submanifold, and the voltage density must be written as a surface density on the corresponding n-dim image in R2n.Under Assumption 1, (13) admits a unique smooth solution 

v = GY (p) for each admissible p ∈ Rn, yielding a smooth injective map 

GY : Rn → R2n, MY := GY (Rn) ⊂ R2n,

whose image MY is the n-dim submanifold of Y -admissible voltages. Taking real parts of (13) and using Re[( I +

j tan Θ)p] = p gives the explicit closed form of the inverse: 

[G−1 

> Y

(v)] k = Re {[sg (v)] k} − Re [v]k[ ¯Y ¯v + ¯b]k , k ∈ [n].

(15) By the implicit function theorem applied to (13), 

DG Y (p) = − Jeff ˜FY (v)−1R, R := 

 I

tan Θ



∈ R2n×n,

(16) evaluated at v = GY (p), where R depends only on the power factors. Let fp denote the joint density of the active log-load vector 

p under the log-GMM model (10). Then the voltage v =

GY (p), as a random element of R2n supported on MY , has density, given by the n-dim change-of-variables formula: 

pv (v | Y ) = fp

 G−1 

> Y

(v) |JY (v)|, v ∈ M Y , (17) where the scalar volume factor |JY (v)| is the reciprocal square root of the Gramian determinant of DG Y :

|JY (v)| := 

q

det  DG ⊤ 

> Y

DG Y

 

> p=G−1
> Y(v)

. (18) Crucially, (17) rewrites the distribution of the voltage phasors in terms of the distribution of the active loads, giving a proce-dure for bounding the privacy loss of the voltage phasors w.r.t. the admittance matrix using the noise in the DP loads. When volt-var coupling is present, the generation injection depends on the voltage through the volt-var curve and J ˜FY must be replaced by an effective Jacobian Jeff ˜FY

throughout. Since this substitution does not impact the logic of the analysis, we proceed w.l.o.g. with J ˜FY and defer the details to Appendix E. III. P RIVACY PRELIMINARIES 

We introduce the threat model, DP definitions and adja-cency, and then the privacy mechanism. 

A. Threat Model 

We adopt the standard DP threat model used in prior work on privacy-preserving grid data release [8], [9], [22], [27]–[29]: the adversary is computationally unbounded, may possess arbitrary auxiliary information about loads, generation, and network topology, and observes every published output. Privacy is quantified by the distinguishability of the system state under adjacent admittance matrices Y , Y ′. This contrasts with DP graph release [23], [34], whose published artifact is the privatized graph itself. Our mechanism instead releases the voltage phasor trajectories {˜vτ }Tτ =1 computed on the true Y ,so the push-forward distribution (17) is the central object of the analysis. 6

B. Differential Privacy and Adjacency 

We adopt Probabilistic DP (PDP) [35], which implies the standard (ε, δ )-DP of [14] and connects directly with hypothesis testing over probability distributions: 

Definition 3 ((ε, δ )-PDP [35]) . Let D, D′ ∈ D be adjacent data sets in database D, and let M be a query mechanism over D with output ˜q ∼ f (˜ q|D). Then M is (ε, δ )-PDP if 

Pr 



ln f (˜ q|D)

f (˜ q|D′) > ε 



≤ δ. (19) In our DP power flow setting the database is Yfeas and we consider Yfull , Y ′ 

> full

adjacent if they satisfy: 

Definition 4 (Physical r-adjacency) . Two full admittance matrices Yfull , Y ′ 

> full

with Kron reductions Y , Y ′ ∈ Y feas are 

r-adjacent if 

∥Yfull − Y ′

> full

∥F < r, (20) 

MY = MY ′ , (21) 

where MY := GY (Rn) is the set of retained-bus voltages in 

S0 reachable by the mechanism under Y with the fixed power factors tan Θ. For a reference Yfull ,

Nr (Yfull ) := Y ′ 

> full

: ∥Yfull − Y ′

> full

∥F < r, MY ′ = MY .

Condition (20) is a physical adjacency notion: the mecha-nism protects against distinguishing any two feeders whose line admittances (including switch states) differ by a total Frobenius distance less than r. By Corollary 1, any such pair induces reduced matrices satisfying ∥Y − Y ′∥F < κ Kron r,which is the quantity that actually enters the privacy analysis of Section IV. The manifold condition (21) is measure-theoretic: it ensures that the pushforward measures induced by the mech-anism share a common support on the reduced system, so that their Radon–Nikodym derivative exists, which is a requirement for the LLR to be well defined. Linearizing the power-factor consistency residual around Y yields n homogeneous linear constraints on the induced ∆Y , so Nr (Yfull ) corresponds (via Corollary 1) to an (n2 − n)-dim subspace of the ∆Y -space intersected with a Frobenius ball of radius κKron r. This defines a rich family of admittance matrices against which the mechanism is formally DP. In contrast, prior DP power flow work [22], [27] adopts per-parameter metric DP, which protects each admittance entry independently up to a scalar threshold and thereby fixes Y ’s sparsity pattern a priori. On the other hand, [23] protects a scalar DC-OPF transmission capacity vector ¯f and thus defines their adjacency as entry-wise α-adjacency on ¯f , and neither the admittance matrix Y nor the loads enter the privacy guarantee. Our Frobenius-ball adjacency is instead applied to 

Yfull itself and is coupled with DP on the load distribution via the pushforward through the true Y , yielding joint protection of both objects. This dual protection is not offered by any prior DP power flow formulation. 

C. The Mechanism and Its Privacy Guarantee 

The data owner (utility) holds the true admittance matrix 

Y and uses a standard power flow solver (e.g., OpenDSS) 

Algorithm 1 Synthetic Voltage Phasor Release 

Require: Historical log-load data {log p(i) 

> k

}k∈[n], i ∈[d] with 

log p(i) 

> k

∈ RT ; true admittance matrix Y ; public ir-radiance {hgτ }Tτ =1 ; power factor angles {θ(ℓ) 

> k

}; number of classes L; per-class load margins p(ℓ)min , p (ℓ)max ; privacy budget εload . 

> 1:

Phase 1: Offline model fitting  

> 2:

C1, . . . , CL ← PARTITION 

 {log p(i) 

> k

}k,i 

 

> 3:

for ℓ = 1 , . . . , L do  

> 4:

Class data: Dℓ ← log p(i) 

> k

: k ∈ C ℓ, i ∈ [d] ⊂ RT 

> 5:

Fit DP Gaussian to 

Dℓ:   ˜μ(ℓ), ˜Σ(ℓ)

> T

 ← DP-GMM (Dℓ, ε load ) 

> 6:

end for  

> 7:

Phase 2: Synthetic voltage generation  

> 8:

for each bus k ∈ C ℓ, ℓ ∈ [L] do  

> 9:

Draw T -dimensional log-load from the truncated distri-bution: 

˜ξk ∼ N   ˜μ(ℓ), ˜Σ(ℓ)

> T

 exp( ˜ξk) ∈ [p(ℓ)min , p (ℓ)max ]T . 

> 10:

Set active load: [ ˜ pk]τ ← exp([ ˜ξk]τ ) for τ ∈ [T ]. 

> 11:

Reactive load: [ ˜ qk]τ ← tan( θ(ℓ) 

> k

) [ ˜ pk]τ for τ ∈ [T ]. 

> 12:

end for  

> 13:

for τ = 1 , . . . , T do  

> 14:

Form load injection: ˜spτ ← ˜pτ + j ˜qτ . 

> 15:

Solve power flow: 

˜vτ ← POWER FLOW SOLVE (Y , ˜spτ , sgτ ) 

> 16:

end for  

> 17:

Release {˜vτ }Tτ =1 .to compute voltage phasors from synthetic DP load inputs. We assume the DP loads are log-normal mixtures as in (10), which we call the DP-GMM; the specific DP-GMM procedure is irrelevant to the analysis and deferred to Section V. 

Definition 5 (DP-Powerflow) . Given the true admittance matrix Y , public irradiance data {hgτ }, and a DP-GMM fitted to historical loads, the mechanism MY operates as follows: (a) Draw a synthetic load vector ˜spt from the DP-GMM, truncated to [p(ℓ)min , p (ℓ)max ] per class. (b) Solve the power flow equations (13) with the true Y to obtain ˜vt;(c) Release ˜vt.

See Algorithm 1 for details. Because the synthetic load ˜spt

is random and never disclosed, the released voltage ˜vt is a random variable whose distribution depends on Y through the power flow map, and no additional voltage noise is needed since the randomness of the synthetic load input is the privacy mechanism. Formally, MY at time t outputs a sample from the pushforward density pv (· | Y ) in (17), and the DP guarantee with respect to Y follows from bounding the log-likelihood ratio across adjacent admittance matrices. IV. P RIVACY ANALYSIS 

We develop the (ε, δ )-PDP guarantee by decomposing the log-likelihood ratio (LLR) into an injection-likelihood term 7

(Term I) and a Jacobian-determinant term (Term II), bounding each, and combining them via a χ2 concentration inequality on the whitened drawn loads. 

A. Log-Likelihood-Ratio Decomposition 

Let Λ( v1: T ; Y , Y ′) := log  pv (v1: T | Y )/p v (v1: T | Y ′)

denote the LLR between the pushforward densities under 

Y and Y ′. By Definition 3, the mechanism is (ε, δ )-PDP if Pr[ |Λ| > ε ] ≤ δ. For Y ′ 

> full

∈ N r (Yfull ), the common-support condition (21) ensures that G−1  

> Y′

is well-defined on 

MY and the two surface densities share support. Using (17), 

Λ decomposes as 

Λ( v1: T ; Y , Y ′) = log fp

 pY

> 1: T



fp

 pY ′

> 1: T

| {z }

> Term I: injection likelihood

+

> T

X

> t=1

log |JY (vt)||JY ′ (vt)|

| {z }

> Term II: Jacobian ratio

,

(22) where pY 

> 1: T

:= (G−1 

> Y

(v1), . . . , G −1 

> Y

(vT )) and pY ′ 

> 1: T

:= (G−1  

> Y′

(v1), . . . , G −1  

> Y′

(vT )) are the implied active-load trajec-tories. Term I measures how much the implied active-load trajectory shifts when the admittance matrix changes from Y

to Y ′, evaluated under the class-ℓ log-normal load density with log-covariance Σ(ℓ).Term II captures how the change alters the n-dim surface volume element via the Gramian of DG Y . We bound each term separately and combine. 

B. Term I: Injection Sensitivity 

At a fixed voltage trajectory (v1, . . . , vT ) ∈ ST 

> 0

, ad-mittances Y and Y ′ = Y − ∆Y imply different ac-tive loads at each bus, with ∥∆Y ∥F < κKron r by Corollary 1. Generation terms cancel, leaving [ˆ pY (vt)] k −

[ˆ pY ′ (vt)] k = −ℜ{ [vt]k[∆ Y ¯vt]k}. The following proposition bounds Term I. 

Proposition 1. Let ∆Y := Y − Y ′ denote the induced per-turbation of the Kron-reduced matrices, which by Corollary 1 satisfies ∥∆Y ∥F ≤ κKron r for any r-adjacent Yfull , Y ′

> full

. For each class ℓ ∈ [L], define the entrywise sensitivity constant 

dℓ := V 2max 

√dmax 

p(ℓ)min 

, (23) 

where dmax := max k |{ j : Ykj ̸ = 0 }| is the maximum node degree, and the precision sum 

γ(ℓ) := 1⊤|(Σ(ℓ))−1|1 = X

> t,t ′

|[( Σ(ℓ))−1]tt ′ |. (24) 

Define the uniform whitened-shift bound 

¯ψ 2 := ( κKron r)2

> L

X

> ℓ=1

d 2 

> ℓ

γ(ℓ), (25) 

and the χ2 

> nT

tail factor 

τ (δR) := 

q

nT + 2 pnT log(1 /δ R) + 2 log(1 /δ R) . (26) 

Then for any δR ∈ (0 , 1) , with probability at least 1 − δR over the DP-GMM, Term I satisfies 

|Term I 1: T |≤ ¯ψ τ (δR) + 12 ¯ψ 2 + κKron r

> L

X

> ℓ=1

dℓ

q

γ(ℓ)|C ℓ|

p

1⊤Σ(ℓ)1.

(27) For a proof see Appendix B. With the injection likelihood (Term I) now bounded, it remains to bound the Jacobian determinant ratio (Term II). 

C. Bounding Term II: Jacobian Determinant Ratio 

Term II measures how changing the admittance matrix alters the volume element of the power flow map. The analysis relies on the normalized Jacobian ˜M , obtained by factoring out a diagonal voltage matrix from the Wirtinger Jacobian of the power flow map (see Appendix C for the derivation). Specifically, the Wirtinger Jacobian factors as J = D(v) ˜M ,where 

D(v) := diag( v1, . . . , v n, ¯v1, . . . , ¯vn) ∈ C2n×2n, (28) and the normalized Jacobian takes the form 

˜M =

diag( s/v2) ¯YY diag(¯ s/¯v2)



, (29) where si = vi( ¯Y ¯v + ¯b)i is the complex power injection at bus i.Define the worst-case operator norm ∥ ˜M −1∥⋆ := sup v∈S 0, Y ′∈N r (Yfull ) ∥ ˜M (v, Y ′)−1∥op and the geometric constant C⋆ := √2(1 + √n V max /V min ). Although a supre-mum, ∥ ˜M −1∥⋆ admits a closed-form upper bound from network data alone (see Appendix E for the full derivation). However, since this bound is naturally conservative, our eval-uation uses the Monte Carlo calibration of ˜M −1

> ∗

which is detailed in Remark 2. 

Proposition 2. Define the admissibility parameter 

α := ∥ ˜M −1∥⋆ C⋆ κKron r, (30) 

with ∥ ˜M −1∥⋆ and C⋆ as defined above. Under the admissi-bility condition α < 1/4,

|Term II 1: T | ≤ ¯ΛII := T √n α (2 + α)2(1 − 4α) . (31) For a proof see Appendix C. The bound is controlled by the conditioning of ˜M , the adjacency radius (through κKron r), the voltage bounds (through C⋆), and the trajectory length T . For small α it simplifies to ¯ΛII ≈ T √n α . Combining both terms gives the main privacy guarantee which is provided below. 

Theorem 1. Under the system model of Section II, the log-load model (10) , the feasibility conditions of Assumption 1, and the admissibility condition α < 1/4 with α as in (30) , the mechanism is (ε, δ )-PDP with respect to physical r-adjacency, with 

ε = B + ¯ψ τ (δ), (32) 8

where the total deterministic bias is 

B = T √n α (2 + α)2(1 − 4α) + 12 ¯ψ 2

+ κKron r

> L

X

> ℓ=1

dℓ

q

γ(ℓ)|C ℓ|

p

1⊤Σ(ℓ)1, (33) 

the uniform whitened-shift bound is 

¯ψ 2 = ( κKron r)2

> L

X

> ℓ=1

d 2 

> ℓ

γ(ℓ), (34) 

and the χ2 

> nT

tail factor is 

τ (δ) = 

q

nT + 2 pnT log(1 /δ ) + 2 log(1 /δ ) , (35) 

with dℓ = V 2max 

√dmax /p (ℓ)min and γ(ℓ) = 1⊤|(Σ(ℓ))−1|1.

For a proof see Appendix D. Theorem 1 delivers a formal 

(ε, δ )-DP guarantee on the admittance matrix Y from the DP-synthetic loads alone, yielding our contribution that no additional perturbation is necessary. The bound has three pieces. The Jacobian term T √n α (2+ α)/(2(1 −4α)) captures volume-element distortion of the power flow map and is controlled by the adjacency radius r through α. The deter-ministic bias scales as (κKron r)2 and is likewise driven by 

r. The tail term ¯ψ τ (δ) captures the stochastic fluctuation of Term I, where ¯ψ depends on the DP-GMM covariance Σ(ℓ)

and τ (δ) ≈ √nT for nT ≫ log(1 /δ ).The practitioner has two levers. The adjacency radius r is a sensitivity parameter that sets how broad a class of topology perturbations the mechanism protects against, and it controls all three terms of the bound. The DP-GMM covariance Σ(ℓ)

enters only the tail and bias where a larger covariance de-creases the precision sum γ(ℓ) and thus a noisier DP-GMM yields stronger topology-level privacy. Because the Jacobian term does not depend on Σ(ℓ), it imposes a floor on ε that no amount of DP-GMM noise can cross. Driving ε below this floor requires tightening r.The remaining quantities are fixed by the feeder but shape how forgiving the bound is. A well-conditioned admittance matrix has small ∥ ˜M −1∥⋆, which tightens α and shrinks the Jacobian term. Poorly connected feeders with near-singular ˜M

inflate α and can push the admissibility condition α < 1/4

toward its boundary. A wide voltage window [Vmin , V max ]

inflates C⋆ = √2(1 + √nV max /V min ), which also inflates 

α, so feeders operating close to nominal voltage give tighter guarantees than those with loose regulation. The load margins 

[p(ℓ)min , p (ℓ)max ] appear through dℓ = V 2max 

√dmax /p (ℓ)min where load classes with small p(ℓ)min , which correspond to feeders with lightly loaded buses, amplify the bias and tail terms. Consequently, the practitioner should set p(ℓ)min and p(ℓ)max as tight as feasibility allows, since wider margins trade directly against the privacy bound through dℓ. Finally, when volt-var coupling is present, Theorem 1 holds verbatim upon replacing 

˜M by the effective Jacobian ˜Meff = ˜M − D(v)−1JΓh in the definition of α.

Remark 2 (Monte Carlo calibration of ¯ΛII ). The worst-case 

∥ ˜M −1∥⋆ used in ¯ΛII is taken over all of S0, which may be overly conservative. In practice, one can replace ∥ ˜M −1∥⋆

by a probabilistic threshold μ0 and absorb the exceedance probability into δ. Specifically, define 

δM := Pr 

> p∼fp

 ∃ t : ∥ ˜M (vt, Y )−1∥op > μ ′

> 0

,μ′ 

> 0

= μ0

1 + μ0 C⋆ κKron r , (36) 

where the shift μ0 → μ′ 

> 0

accounts for the worst-case pertur-bation over Nr (Yfull ) via the induced reduced-system radius 

κKron r. The probability δM can be estimated by drawing 

Ncal trajectories from the DP-GMM, solving power flow with the utility’s own Y , and counting exceedances (with Clopper–Pearson confidence intervals). The mechanism is then 

(ε, δ + δM )-PDP with ¯ΛII computed using μ0 in place of 

∥ ˜M −1∥⋆, where ε is computed from Theorem 1. 

V. E MPIRICAL EVALUATION 

We conduct two complementary evaluations. The first mea-sures distributional fidelity of the released voltage phasors through the Wasserstein-1 distance between the privatized and true voltage magnitude distributions, swept over a grid of target ε values. The second evaluates downstream machine-learning utility by training a three-layer multilayer perceptron (MLP) on privatized voltage data for a missing-data recovery task and reporting test-set MSE. The Wasserstein distance upper-bounds the generalization gap of any downstream model trained on the synthetic data [36]. Making this distance small is therefore particularly valuable for training foundation models, since it directly limits how much worse a downstream model can perform when trained on synthetic rather than true data. The MLP experiment in turn demonstrates that naive DP-SGD training [19] is insufficient for the extensive training requirements of grid foundation models and indicates the benefit of our proposed method over naive noise approaches. Both experiments utilize the IEEE 123-bus test feeder with a full year of PV and loads provided by the OEDI dataset [37]. The distribution feeder is compiled in OpenDSS, and we apply the Kron reduction of Section II-A to elim-inate the zero-injection buses, yielding a reduced admit-tance matrix Y ∈ Cn×n and the constant-current offset 

b = [ Yfull ]retained , slack vslack as in (7). Historical load and generation profiles are obtained from the feeder’s loadshape definitions at 15-minute resolution ( T = 96 samples per day). 

A. Mechanisms Under Comparison 

We compare several voltage-release mechanisms under the same cumulative privacy budget. For target (ε, δ ), each mech-anism ensures the release of T d voltage phasor vectors ( d

days) is (ε, δ )-DP with respect to the quantity it protects. We write ∆load := pmax − pmin for the global active-load range and nL for the number of load buses. Per-sample sensitivities and noise calibrations follow standard Gaussian mechanism 9                                                                  

> TABLE III PER -SAMPLE ℓ2SENSITIVITIES AND NOISE STANDARD DEVIATIONS FOR EACH MECHANISM .
> Mechanism Noise target Per-sample ∆2Noise σProtects
> DP-GMM →PF (proposed) —— (Thm. 1) none Y+ loads Gauss. on voltages voltages ∆(Y)2
> √T∆(Y)2
> √2 ln(1 .25 /δ )
> εY
> Gauss. on YYentries rr√2 ln(1 .25 /δ )
> εY
> Joint voltage noise voltages max  ∆(Y)2,∆(load →v)2
> √T∆(joint) 2
> √2 ln(1 .25 /δ )
> εY+ loads DP-GMM + Gauss. volt. voltages ∆(Y)2
> √T∆(Y)2
> √2 ln(1 .25 /δ Y)
> εYY+ loads Noisy loads + Gauss. volt. load entries √nLT∆load
> √nLT∆load
> √2 ln(1 .25 /δ load )
> εload Y+ loads voltages ∆(Y)2
> √T∆(Y)2
> √2 ln(1 .25 /δ Y)
> εY
> See (37)–(38) for ∆(Y)2and ∆(load →v)2. Here ris the physical Frobenius radius on Yfull (Definition 4) and κKron is the Kron amplification factor (4).

arguments (Appendix F) and are collected in Table III. Two sensitivities recur: 

∆(Y )2 = V 2max 

√n κ Kron r ∥ ˜M −1∥⋆

Vmin 

, (37) 

∆(load →v)2 =

√2 ∆ load ∥ ˜M −1∥⋆

Vmin 

. (38) The mechanisms are:  

> •

DP-GMM → PF (proposed) : Algorithm 1 with synthetic loads at budget εload passed through AC power flow on the true Y . ε is computed via Theorem 1 with ∥ ˜M −1∥⋆

calibrated via Remark 2. We select εload that minimizes Wasserstein distance subject to the target ε. 

> •

Noise-free baseline : AC power flow with true loads and Y . 

> •

Joint Voltage Noise : a single Gaussian mechanism on the voltage output protects both Y and loads, with budget 

ε = min( εload , ε ), where sensitivity is given by the max sensitivity of the load and voltage release respectively.  

> •

DP-GMM + Gaussian Voltage Noise : DP-GMM loads at budget εload passed through the true Y , with an additional Gaussian mechanism on the voltages at budget ε calibrated to the sensitivity of Y . 

> •

Noisy Loads + Gaussian Voltage Noise : replaces the DP-GMM with i.i.d. Gaussian noise on the load matrix at εload ,clipped and passed through the true Y , with Gaussian noise added to the voltages at budget ε. 

> •

DP-SGD [19]: included only in the ML experiment since it protects the trained model rather than the released data. Matching the Y -level budget (ε, δ ) requires a group-privacy reduction εsample = ε/N (with adjusted δsample ) [14] to account for one feeder’s Y affecting all N samples we then convert to zero-concentrated DP [38] via ρ =(pln(1 /δ sample ) + εsample − pln(1 /δ sample )) 2 and cali-brate the per-step Gaussian noise multiplier under subsam-pling rate q = B/N (B = 64 , N = 1000 ). 

Remark 3. Prior work on admittance-matrix privacy [22], [27] adopts per-parameter metric DP with constraint-based feasibility-restoring post-processing, which fixes the sparsity pattern of Y a priori and is therefore not comparable to our r-  

> Fig. 2. Wasserstein-1 metric as a function of ε.

adjacency. Similarly, [23] provides DP with respect to thermal constraints and does not protect the topology. The natural re-maining comparison is a Gaussian mechanism applied directly to Y , but this is not viable without a feasibility-restoring step. Even at r = 10 −3 and ε = 100 , OpenDSS failed to con-verge because unstructured perturbation destroys the sparsity, symmetry, and shunt structure required for Assumption 1. We therefore omit this comparison and leave a feasibility-restoring variant to future work. B. Wasserstein Distance Experiment 

We sweep over a grid of per-sample (vt ∈ Rn) ε values 

{25 , 30 , 50 , 100 , 200 } and, for each value, generate voltage trajectories from all mechanisms over a fixed number of days, using the δ produced by the DP-GMM Monte Carlo calibration (Remark 2) as the common δ. Voltages are generated by solving AC power flow via OpenDSS with the true network model. For the proposed DP-GMM mechanism, we search over εload values, evaluate Theorem 1 via Monte Carlo, and select the configuration that minimizes the Wasserstein distance subject to the target ε for Y . Each configuration is evaluated over 20 independent Monte Carlo runs, and reported values are averaged across runs. The Wasserstein-1 distance is computed between the flat-tened daily voltage magnitude vectors of each mechanism and 10      

> Fig. 3. Training (left) and test (right) MSE on the masked entries of the per-bus missing-data recovery task (Section V-C), plotted against training epoch. All methods operate at the same Y-level privacy budget (ε, δ )and share the same true-data evaluation and test sets. Curves show the mean across runs.

the true (non-private) voltages. Figure 2 shows that the DP-GMM approach achieves a substantially smaller Wasserstein distance than the Joint Voltage and Noisy Loads + Gaussian Voltage baselines, and a moderate improvement over the DP-GMM + Gaussian output-perturbation variant that narrows at large ε. This improvement over the latter reflects the effective “free” privacy revealed by our analysis. The proposed mech-anism attains the same fidelity without additional Gaussian corruption of the voltages. 

C. Machine Learning Experiment 

We evaluate downstream ML utility via a per-bus missing-data recovery task. Each sample is a single bus’s voltage magnitude trajectory over a contiguous window of dw time steps, i.e., a vector in Rdw . We set dw = 48 (12 hours at 15-minute resolution) with a 3 hour overlap with subsequent samples. From each trajectory, a random contiguous block of length L = ⌊√0.25 dw⌋ = 24 is masked, and the task is to reconstruct the masked block from the observed context on the same bus. A three-layer MLP (with ReLU activations and hidden dimension 32 ) is trained to predict the full trajectory from the partial observation, with MSE loss evaluated on the masked entries only. The evaluation and test sets are drawn from the true (non-private) voltage distribution and are shared across all methods (including DP-SGD), which are compared at the same Y -level privacy budget (ε, δ ) as in Section V-A. For each day of T = 96 voltage vectors generated by a single feeder, the release is (εday , δ )-DP at the Y level, so a training corpus spanning d days has total cost ε = d ε day 

by basic composition. Training uses Adam (standard) or per-sample-clipped SGD (DP-SGD) with early stopping on the shared evaluation MSE. Each method is trained over 20 inde-pendent runs with different random seeds, and Fig. 3 reports the mean training and test MSE across runs. Fig. 3 shows two takeaways. First, DP-SGD converges slowly because its per-iteration budget composition forces substantial noise at matched Y -level privacy. Second, the proposed mechanism tracks the error-free baseline closely, reflecting the value of preserving load correlation structure via DP-GMM and using the true admittance matrix in power flow. Finally, Fig. 3 shows a clear gain from the proposed method over the DP-GMM + Gaussian output-perturbation variant. The separation between the two curves is entirely attributable to the “free” privacy enabled by our analysis, mirroring the gap observed in the Wasserstein experiment. VI. C ONCLUSION 

The practical barrier to training GFMs is the availability of data. Privacy concerns over customer loads and network topology keep utility datasets siloed. We showed that a single privacy mechanism, namely a DP-GMM fitted to historical loads and propagated through the true AC power flow, yields voltage phasor trajectories that are simultaneously (ε, δ )-DP with respect to both the loads and the admittance matrix Y ,with the guarantee on Y obtained for free from the DP noise already required by the load model. Empirical results on the IEEE 123-bus feeder confirm that the proposed mechanism closely tracks the no-noise baseline, while Gaussian output-perturbation baselines and DP-SGD degrade substantially at matched privacy budgets. The principle generalizes beyond voltage release. Any quantity computed from loads and a fixed network, such as OPF solutions, hosting-capacity assessments, or reliability indices, inherits privacy guarantees from the same DP load model, which we view as the natural next step toward privacy-preserving Grid Foundation Models. REFERENCES [1] M. Meiser and I. Zinnikus, “A survey on the use of synthetic data for enhancing key aspects of trustworthy ai in the energy domain: Challenges and opportunities,” Energies , vol. 17, no. 9, p. 1992, 2024. [2] P. K. Shukla and K. Deepa, “Ai-based synthetic data generation tech-niques for improved fault classification in power systems,” Ain Shams Engineering Journal , vol. 16, no. 8, p. 103485, 2025. [3] H. F. Hamann, T. Brunschwiler, B. Gjorgiev, L. S. A. Martins, A. Puech, A. Varbella, J. Weiss, J. Bernabe-Moreno, A. Blondin Mass´ e, S. Choi, I. Foster, B.-M. Hodge, R. Jain, K. Kim, V. Mai, F. Mirall` es, M. De Montigny, O. Ramos-Lea˜ nos, H. Suprˆ eme, L. Xie, E.-N. S. Youssef, A. Zinflou, A. J. Belyi, R. J. Bessa, B. P. Bhattarai, J. Schmude, and S. Sobolevsky, “Foundation models for the electric power grid,”     

> Joule , vol. 8, no. 12, pp. 3245–3258, 2024. [4] D. Deka, M. Chertkov, and S. Backhaus, “Topology estimation using graphical models in multi-phase power distribution grids,” IEEE Trans-actions on Power Systems , vol. 35, no. 3, pp. 1663–1673, 2020. [5] Y. Liao, Y. Weng, G. Liu, Z. Zhao, C. W. Tan, and R. Rajagopal, “Unbalanced multi-phase distribution grid topology estimation and bus phase identification,” IET Smart Grid , vol. 2, no. 4, pp. 557–570, 2019. 11

[6] D. Deka, V. Kekatos, and G. Cavraro, “Learning distribution grid topologies: A tutorial,” IEEE Transactions on Smart Grid , vol. 15, no. 1, pp. 999–1013, 2024. [7] Y. Yuan, S. H. Low, O. Ardakanian, and C. J. Tomlin, “Inverse power flow problem,” IEEE Transactions on Control of Network Systems ,vol. 10, no. 1, pp. 261–273, 2023. [8] T. W. Mak, F. Fioretto, and P. Van Hentenryck, “Privacy-preserving obfuscation for distributed power systems,” Electric Power Systems Research , vol. 189, p. 106718, 2020. [9] H. Sandberg, G. D´ an, and R. Thobaben, “Differentially private state estimation in distribution networks with smart meters,” in 2015 54th IEEE conference on decision and control (CDC) , pp. 4492–4498, IEEE, 2015. [10] H. Bibi, M. Abolhasan, J. Lipman, M. Abdollahi, and W. Ni, “A comprehensive survey on privacy-preserving technologies for smart grids,” Computers and Electrical Engineering , vol. 124, p. 110371, 2025. [11] M. Gillioz, G. Dubuis, and P. Jacquod, “A large synthetic dataset for machine learning applications in power transmission grids,” Scientific Data , vol. 12, no. 1, p. 168, 2025. [12] M. Turowski, B. Heidrich, L. Weing¨ artner, L. Springer, K. Phipps, B. Sch¨ afer, R. Mikut, and V. Hagenmeyer, “Generating synthetic energy time series: A review,” Renewable and Sustainable Energy Reviews ,vol. 206, p. 114842, 2024. [13] C. Zhang, S. R. Kuppannagari, R. Kannan, and V. K. Prasanna, “Gen-erative adversarial network for synthetic time series data generation in smart grids,” in 2018 IEEE international conference on communications, control, and computing technologies for smart grids (SmartGridComm) ,pp. 1–6, IEEE, 2018. [14] C. Dwork, A. Roth, et al. , “The algorithmic foundations of differential privacy,” Foundations and Trends® in Theoretical Computer Science ,vol. 9, no. 3–4, pp. 211–407, 2014. [15] V. Schlegel, A. A. Bharath, Z. Zhao, and K. Yee, “Generating synthetic data with formal privacy guarantees: State of the art and the road ahead,” 

arXiv preprint arXiv:2503.20846 , 2025. [16] G. ´Acs and C. Castelluccia, “I have a dream! (differentially private smart metering),” in Information Hiding (T. Filler, T. Pevn´ y, S. Craver, and A. Ker, eds.), (Berlin, Heidelberg), pp. 118–132, Springer Berlin Heidelberg, 2011. [17] G. Eibl and D. Engel, “Differential privacy for real smart metering data,” 

Computer Science-Research and Development , vol. 32, no. 1, pp. 173– 182, 2017. [18] P. Barbosa, A. Brito, H. Almeida, and S. Claus, “Lightweight privacy for smart metering data by adding noise,” in Proceedings of the 29th Annual ACM Symposium on Applied Computing , pp. 531–538, 2014. [19] M. Abadi, A. Chu, I. Goodfellow, H. B. McMahan, I. Mironov, K. Talwar, and L. Zhang, “Deep learning with differential privacy,” in 

Proceedings of the 2016 ACM SIGSAC conference on computer and communications security , pp. 308–318, 2016. [20] J. Huang, Q. Huang, G. Mou, and C. Wu, “Dpwgan: High-quality load profiles synthesis with differential privacy guarantees,” IEEE Transac-tions on Smart Grid , vol. 14, no. 4, pp. 3283–3295, 2023. [21] M. Hoseinpour and V. Dvorkin, “Constrained diffusion models for synthesizing representative power flow datasets,” arXiv preprint arXiv:2506.11281 , 2025. [22] F. Fioretto, T. W. K. Mak, and P. Van Hentenryck, “Differential privacy for power grid obfuscation,” IEEE Transactions on Smart Grid , vol. 11, no. 2, pp. 1356–1366, 2020. [23] V. Dvorkin and A. Botterud, “Differentially private algorithms for synthetic power system datasets,” IEEE Control Systems Letters , vol. 7, pp. 2053–2058, 2023. [24] N. Ravi, A. Scaglione, S. Kadam, R. Gentz, S. Peisert, B. Lunghino, E. Levijarvi, and A. Shumavon, “Differentially private k-means cluster-ing applied to meter data analysis and synthesis,” IEEE transactions on smart grid , vol. 13, no. 6, pp. 4801–4814, 2022. [25] H. Liu, A. Scaglione, and S. Peisert, “Differentially private distribution release of gaussian mixture models via kl-divergence minimization,” 

arXiv preprint arXiv:2506.03467 , 2025. [26] J. D. Fern´ andez, S. P. Menci, C. M. Lee, A. Rieger, and G. Fridgen, “Privacy-preserving federated learning for residential short-term load forecasting,” Applied energy , vol. 326, p. 119915, 2022. [27] D. Smith, F. Geth, E. Vercoe, A. Feutrill, M. Ding, J. Chan, J. Foster, and T. Rakotoarivelo, “Realistic differentially-private transmission power flow data release,” arXiv preprint arXiv:2103.14036 , 2021. [28] V. Dvorkin, F. Fioretto, P. Van Hentenryck, P. Pinson, and J. Kazempour, “Differentially private optimal power flow for distribution grids,” IEEE Transactions on Power Systems , vol. 36, no. 3, pp. 2186–2196, 2020. [29] T. W. K. Mak, F. Fioretto, L. Shi, and P. Van Hentenryck, “Privacy-preserving power system obfuscation: A bilevel optimization approach,” 

IEEE Transactions on Power Systems , vol. 35, no. 2, pp. 1627–1637, 2020. [30] G. Eibl, K. Bao, P.-W. Grassal, D. Bernau, and H. Schmeck, “The influence of differential privacy on short term electric load forecasting,” 

Energy Informatics , vol. 1, no. Suppl 1, p. 48, 2018. [31] J. Munkhammar, J. Ryd´ en, and J. Wid´ en, “Characterizing probability density distributions for household electricity load profiles from high-resolution electricity use data,” Applied Energy , vol. 135, pp. 382–390, 2014. [32] O. Mey, A. Schneider, O. Enge-Rosenblatt, Y. Bravo, and P. Stenzel, “Prediction of energy consumption for variable customer portfolios including aleatoric uncertainty estimation,” in 2021 10th International Conference on Power Science and Engineering (ICPSE) , pp. 61–71, IEEE, 2021. [33] P. Kuusela, I. Norros, R. Weiss, and T. Sorasalmi, “Practical lognormal framework for household energy consumption modeling,” Energy and Buildings , vol. 108, pp. 223–235, 2015. [34] N. D’Silva, S. Nepal, and S. S. Kanhere, “Sok: Practical aspects of re-leasing differentially private graphs,” arXiv preprint arXiv:2603.18779 ,2026. [35] A. Machanavajjhala, D. Kifer, J. Abowd, J. Gehrke, and L. Vilhuber, “Privacy: Theory meets practice on the map,” in 2008 IEEE 24th international conference on data engineering , pp. 277–286, IEEE, 2008. [36] A. T. Lopez and V. Jog, “Generalization error bounds using wasserstein distances,” in 2018 ieee information theory workshop (ITW) , pp. 1–5, IEEE, 2018. [37] T. Elgindy and K. Balasubramaniam, “Sample IEEE123 bus system for OEDI SI.” Open Energy Data Initiative (OEDI), National Renewable Energy Laboratory, Dataset, 2022. doi: 10.25984/2228282, accessed: 2026-04-16. [38] M. Bun and T. Steinke, “Concentrated differential privacy: Simpli-fications, extensions, and lower bounds,” in Theory of cryptography conference , pp. 635–658, Springer, 2016. [39] B. Laurent and P. Massart, “Adaptive estimation of a quadratic functional by model selection,” Annals of statistics , pp. 1302–1338, 2000. 

APPENDIX 

A. Supporting Lemma 

Lemma 1 (Log is Lipschitz on a positive interval) . If x, x ′ ∈

[pmin , p max ] with pmin > 0, then | log x − log x′| ≤ | x −

x′|/p min . Proof. By the mean value theorem, log x − log x′ =(x − x′)/ξ for some ξ ∈ [pmin , p max ], so |1/ξ | ≤ 1/p min .B. Proof of Proposition 1 

The proof proceeds in four steps: (i) derive the exact per-bus LLR via the midpoint formula, (ii) pass to whitened coordinates, (iii) bound P 

> k

∥ψk(v)∥2 uniformly in v, and (iv) apply Cauchy–Schwarz together with a χ2 tail on ∥zY ∥2.

Step 1: Midpoint formula. For bus k ∈ C ℓ, let ξk(v) := log pY 

> k

(v) and ξ′

> k

(v) := log pY ′ 

> k

(v) denote the implied log-load vectors at voltage v under the two admittance matrices. Define ∆k(v) := ξk(v) − ξ′

> k

(v) and the midpoint ¯ξk(v) :=  

> 12

(ξk(v) + ξ′

> k

(v)) .The log-density of the multivariate log-normal at pY 

> k

(v) is 

log f (pY 

> k

(v)) = const − 12 (ξk−μ(ℓ))⊤(Σ(ℓ))−1(ξk−μ(ℓ))−1⊤ξk.

In the ratio, the normalisation constant cancels. The quadratic term yields −∆⊤ 

> k

(Σ(ℓ))−1( ¯ξk − μ(ℓ)), using ξ′ 

> k

+ 12 ∆k = ¯ξk.The Jacobian term −1⊤ξk contributes −1⊤∆k. Combining: 

Λ(k) 

> I

= log f (pY 

> k

(v)) 

f (pY ′ 

> k

(v)) = ∆k(v)⊤(Σ(ℓ))−1(μ(ℓ)+Σ(ℓ)1− ¯ξk(v)) .

(39) 12 

Step 2: Whitened coordinates. Let β(ℓ) := (Σ(ℓ))1/21

and zY 

> k

:= (Σ(ℓ))−1/2(ξk(v) − μ(ℓ)) and ψk(v) := (Σ(ℓ))−1/2∆k(v). Under the Y -mechanism, power flow consistency implies ξk(v) = ˜ξk (the drawn log-load at bus k), so ξk(v) ∼ N (μ(ℓ), Σ(ℓ)) and consequently zY 

> k

∼N (0, IT ) exactly, independently across buses. Substituting 

∆k = (Σ(ℓ))1/2ψk and ¯ξk − μ(ℓ) = (Σ(ℓ))1/2 ¯zk with 

¯zk = zY 

> k

− 12 ψk into (39): 

Λ(k) 

> I

= −ψk(v)⊤( ¯ zk + β(ℓ))= −ψk(v)⊤zY 

> k

+ 12 ∥ψk(v)∥2 − ψk(v)⊤β(ℓ). (40) Note that ψk(v) is a function of the drawn sample through 

v: the counterfactual implied log-load ξ′

> k

(v) = log pY ′ 

> k

(v)

is a deterministic function of the released voltage, and v

itself is determined by the drawn loads via the power flow map. Consequently ψk(v) is not independent of zY 

> k

, which precludes analyzing ψ⊤ 

> k

zY 

> k

as a scalar Gaussian. We instead bound ∥ψk(v)∥ uniformly in v and apply Cauchy–Schwarz. 

Step 3: Uniform bound on ∥ψk(v)∥2. By Lemma 1, 

|[∆k(v)] t| ≤ | pY 

> k

(vt) − pY ′ 

> k

(vt)|/p (ℓ)min . Since the generation terms cancel, |pY 

> k

(vt) − pY ′ 

> k

(vt)| = |ℜ{ [vt]k[∆ Y ¯vt]k}| ≤ 

V 2max 

√dmax ∥(∆ Y )k, :∥2, so ∥∆k(v)∥∞ ≤ dℓ∥(∆ Y )k, :∥2

uniformly in v ∈ S T 

> 0

. Applying the precision-sum bound: 

∥ψk(v)∥2 = ∆k(v)⊤(Σ(ℓ))−1∆k(v)= X

> t,t ′

[( Σ(ℓ))−1]tt ′ [∆k]t[∆k]t′

≤ ∥ ∆k(v)∥2 

> ∞

γ(ℓ) ≤ d 2 

> ℓ

∥(∆ Y )k, :∥22 γ(ℓ). (41) Aggregating over buses with P 

> k∈C ℓ

∥(∆ Y )k, :∥22 ≤ (κKron r)2

(by Corollary 1) for all v ∈ S T 

> 0

it holds: 

> n

X

> k=1

∥ψk(v)∥2 ≤ (κKron r)2

> L

X

> ℓ=1

d 2 

> ℓ

γ(ℓ) = ¯ψ 2. (42) 

Step 4: Cauchy–Schwarz and χ2 tail. Summing (40) ∀k:

ΛI = − X

> k

ψk(v)⊤zY

> k

| {z } 

> =: Z(v)

+ 12

X

> k

∥ψk(v)∥2 − X

> k

ψk(v)⊤β(ℓk )

| {z }  

> =: BI(v)

.

(43) Let zY := ( zY 

> 1

, . . . , zY 

> n

) ∈ RnT , so ∥zY ∥22 ∼ χ2 

> nT

exactly. Cauchy–Schwarz on the stacked vectors gives 

|Z(v)| ≤ 

qP 

> k

∥ψk(v)∥2 · ∥ zY ∥2 ≤ ¯ψ · ∥ zY ∥2, (44) using (42). The Laurent–Massart inequality [39] gives, for any 

x > 0, Pr[ ∥zY ∥22 ≥ nT + 2 √nT x + 2 x] ≤ e−x. Setting 

x = log(1 /δ R) yields ∥zY ∥2 ≤ τ (δR) with probability at least 1 − δR. On this event, |Z(v)| ≤ ¯ψ τ (δR).For the deterministic bias BI (v), the uniform bound (42) gives 12

P 

> k

∥ψk(v)∥2 ≤ 12 ¯ψ 2. For the β-term, Cauchy–Schwarz over buses within each class (P 

> k∈C ℓ

∥(∆ Y )k, :∥2 ≤ p|C ℓ| κKron r) combined with 

∥ψk(v)∥ ≤ dℓ∥(∆ Y )k, :∥2

pγ(ℓ) and ∥β(ℓ)∥ = √1⊤Σ(ℓ)1

gives 

X

> k

ψk(v)⊤β(ℓk ) ≤ κKron r X

> ℓ

dℓ

q

γ(ℓ)|C ℓ|

p

1⊤Σ(ℓ)1.

(45) Combining yields (27). 

C. Proof of Proposition 2 Proof. Wirtinger factorization. The factorization J =

D(v) ˜M with D(v) as in (28) yields (29). From (16), at any v ∈ S0 ∩ M Y , DG Y (G−1 

> Y

(v)) = −(Jeff ˜FY

(v)) −1R

and similarly for Y ′. With ∆Jeff := Jeff ˜FY ′ − Jeff ˜FY

, the Neumann identity gives (Jeff ˜FY ′ )−1 = ( I + K)−1(Jeff ˜FY

)−1

with K := ( Jeff ˜FY

)−1∆Jeff . Since D(v) is independent of 

Y , ∆Jeff = D(v)∆ ˜M (the volt-var contribution cancels), so 

K = ˜M −1∆ ˜M ∈ C2n×2n,DG Y ′ (G−1  

> Y′

(v)) = ( I + K)−1DG Y (G−1 

> Y

(v)) . (46) 

Reduction to an n × n compression. From (46), 

DG ⊤  

> Y′

DG Y ′ = DG ⊤ 

> Y

S−1DG Y with S := ( I + K)( I +

K)⊤. Let DG Y = UGΣGV ⊤ 

> G

be a thin SVD with 

UG ∈ R2n×n. Then det( DG ⊤ 

> Y

DG Y ) = det( ΣG)2 and 

det( DG ⊤  

> Y′

DG Y ′ ) = det( ΣG)2 det( U ⊤ 

> G

S−1UG), so the 

det( ΣG)2 factor cancels in the ratio: 

det( DG ⊤  

> Y′

DG Y ′ )det( DG ⊤ 

> Y

DG Y ) = det( In − W ),

W := U ⊤ 

> G

(I − S−1)UG ∈ Cn×n. (47) As |JY (v)| =

q

det( DG ⊤ 

> Y

DG Y ), Term II per time step is 

Λ(t) 

> II

(v) = log |JY (v)||JY ′ (v)| = − 12 log det( In − W (v)) . (48) 

Matrix-log series on an n × n matrix. For k ≥ 2, Schur’s inequality and the ℓk–ℓ2 inequality give |tr( W k)| ≤ ∥ W ∥kF ;for k = 1 , |tr( W )| ≤ √n ∥W ∥F by ℓ1–ℓ2. Let ∥W ∥F < 1,

| log det( In − W )| ≤ 

> ∞

X 

> k=1 1
> k

|tr( W k)| ≤ √n ∥W ∥F

+

> ∞

X 

> k=2 1
> k

∥W ∥kF ≤√n ∥W ∥F

1 − ∥ W ∥F

. (49) 

Bounding ∥W ∥F . Extending UG to a full orthogonal ˜UG ∈

C2n×2n, U ⊤ 

> G

A U G is a principal n×n submatrix, so ∥W ∥F ≤∥I − S−1∥F . Writing I − S−1 = S−1(S − I) with S − I =

K + K⊤ + KK ⊤ and using ∥K∥op ≤ ∥ K∥F ≤ α, we have 

∥S − I∥F ≤ 2α + α2; combined with ∥S−1∥op ≤ (1 − α)−2

(since σmin (S) ≥ (1 − α)2), 

∥W ∥F ≤ ∥ I − S−1∥F ≤ α(2 + α)(1 − α)2 . (50) A direct calculation shows α(2 + α)/(1 − α)2 < 1 ⇔

4α < 1, so α < 1/4 guarantees ∥W ∥F < 1 and per-mits (49). The bound ∥K∥F ≤ α follows since ∥∆ ˜M ∥F ≤

C(v)∥∆Y ∥F with C(v) = √2(1 + ∥v∥/V min ), giving 

∥K∥F ≤ ∥ ˜M −1∥⋆C⋆ κKron r = α after taking the worst case over v ∈ S 0 and applying Corollary 1. 

Combining. Plugging (50) into (49) and using 1−∥ W ∥F ≥

(1 − 4α)/(1 − α)2 gives | log det( In − W )| ≤ √n α (2 + 

α)/(1 − 4α). By (48), |Λ(t) 

> II

| ≤ 12

√n α (2 + α)/(1 − 4α) per time step; summing over T steps yields (31). 13 

D. Proof of Theorem 1 Proof. By the LLR decomposition (22), Λ = ΛI + Λ II .Under α < 1/4, Proposition 2 gives |ΛII | ≤ ¯ΛII =

T √n α (2+ α)/(2(1 −4α)) deterministically. By Proposition 1, with probability at least 1 − δ over the DP-GMM, 

|ΛI | ≤ ¯ψ τ (δ) + 12 ¯ψ 2 + κKron r

> L

X

> ℓ=1

dℓ

q

γ(ℓ)|C ℓ|

p

1⊤Σ(ℓ)1.

Combining, on the same event of probability ≥ 1 − δ,

|Λ| ≤ ¯ψ τ (δ) + B, 

where B := T √n α (2 + α)/(2(1 − 4α)) + 12 ¯ψ 2 +

κKron r PLℓ=1 dℓ

pγ(ℓ)|C ℓ|√1⊤Σ(ℓ)1 is entirely deterministic. Setting ε = B + ¯ψ τ (δ) establishes Pr[ |Λ| > ε ] ≤ δ, i.e. (ε, δ )-PDP by Definition 3. 

E. Closed-Form Bound on ∥ ˜M −1∥⋆

Recall from Section IV-C that ∥ ˜M −1∥⋆ := sup v∈S 0, Y ′∈N r (Yfull ) ∥ ˜M (v, Y ′)−1∥op . The following remark gives an analytical upper bound on this worst-case quantity in terms of network parameters known to the utility. 

Remark 4. Define the voltage deviation bound ∆∞ := max( Vmax − 1, 1 − Vmin ) and the quantities 

σ ˜M := σmin (Y ) − ∥ Y 1 + b∥∞, (51) 

C3 := ∥Y ∥∞→∞ + ∥Y 1 + b∥∞

Vmin 

, (52) 

Cvv := hg

> max

Vmin 

max  

> k∈G

γk ∥ϕ′

> k

∥∞, (53) 

where ∥Y ∥∞→∞ := max k

P 

> j

|Ykj | is the row-sum norm, 

hg 

> max

:= sup τ ∈[T ] hgτ is the peak irradiance, and ∥ϕ′

> k

∥∞ := sup u∈[Vmin ,V max ] |ϕ′

> k

(u)| is the maximum volt-var slope at bus 

k. Without volt-var coupling, 

∥ ˜M −1∥⋆ ≤ 1

σ ˜M − C3 ∆∞ − C⋆ κKron r , (54) 

and with volt-var coupling, the same bound holds with σ ˜M

replaced by σ ˜M − Cvv , i.e., 

∥ ˜M −1eff ∥⋆ ≤ 1

σ ˜M − C3 ∆∞ − Cvv − C⋆ κKron r , (55) 

provided the respective denominators are positive. Proof. At v = 1, the normalized Jacobian decomposes as 

˜M (1, Y ) = ˜M0 + ˜M1, where 

˜M0 := 

 0 ¯YY 0



,

˜M1 := 

diag( ¯Y 1 + ¯b) 00 diag( Y 1 + b)



.

The block anti-diagonal matrix ˜M0 has singular values equal to those of Y (each with multiplicity two), so σmin ( ˜M0) = 

σmin (Y ). The block diagonal matrix ˜M1 satisfies ∥ ˜M1∥op =

∥Y 1 + b∥∞. By Weyl’s inequality, 

σmin 

  ˜M (1, Y ) ≥ σmin (Y ) − ∥ Y 1 + b∥∞

= σ ˜M . (56) At a general v ∈ S 0, only the diagonal blocks of ˜M change; entry i of the (1 , 1) -block changes from ( ¯Y 1 + ¯b)i to ( ¯Y ¯v +¯b)i/v i, and the difference satisfies 

( ¯Y ¯v + ¯b)i

vi

− ( ¯Y 1 + ¯b)i ≤ C3 ∥v − 1∥∞, (57) with C3 as in (52). This bounds the maximum diagonal entry, so ∥ ˜M (v, Y ) − ˜M (1, Y )∥op ≤ C3 ∆∞. For Y ′ 

> full

∈Nr (Yfull ), Corollary 1 gives ∥Y − Y ′∥F < κKron r, so 

∥ ˜M (v, Y ) − ˜M (v, Y ′)∥op ≤ C⋆ κKron r. Three applications of Weyl’s perturbation theorem give 

σmin 

  ˜M (v, Y ′) ≥ σ ˜M − C3 ∆∞ − C⋆ κKron r, (58) and since ∥ ˜M −1∥⋆ = 1/ inf v,Y ′ σmin ( ˜M (v, Y ′)) , the bound (54) follows. 

Volt-var coupling bound. With volt-var coupling, ˜Meff =˜M − D(v)−1JΓh. Since JΓh is block-diagonal (each PV bus couples only to itself through the volt-var curve), D(v)−1JΓh

is also block-diagonal with entries bounded, for generation bus 

k ∈ G , by 

γkhgt |ϕ′

> k

(|vk|)||vk| ≤ γkhg

> max

∥ϕ′

> k

∥∞

Vmin 

. (59) Therefore ∥D(v)−1JΓh∥op ≤ Cvv with Cvv as in (53). By one further application of Weyl’s inequality, σmin ( ˜Meff ) ≥

σmin ( ˜M ) − Cvv .

F. Sensitivity Derivations for Baseline Mechanisms 

We derive the per-sample ℓ2 sensitivities reported in Ta-ble III. For both derivations we use the following fact: the Wirtinger factorization J = D(v) ˜M (Section IV-C) gives 

∥J ˜FY (v)−1∥op ≤ ∥ ˜M −1∥⋆

Vmin 

. (60) 

Derivation of ∆(Y )2 . Fix a voltage trajectory vt ∈ S 0 and consider two r-adjacent full admittance matrices Yfull , Y ′

> full

with ∥Yfull −Y ′

> full

∥F < r . By Corollary 1, the induced reduced perturbation satisfies ∥∆Y ∥F < κKron r. By the implicit function theorem, 

vt(Y ) − vt(Y ′) ≈ J ˜FY (vt)−1  ˜FY (vt) − ˜FY ′ (vt), (61) and the injection perturbation satisfies ∥ ˜FY (vt)− ˜FY ′ (vt)∥2 ≤

V 2max 

√n ∥∆Y ∥F ≤ V 2max 

√n κ Kron r. Applying the Jacobian bound ∥J−1∥op ≤ ∥ ˜M −1∥⋆/V min from (60) yields (37). 

Derivation of ∆(load →v)2 . A single user’s load change perturbs the real-representation injection vector by at most √2 ∆ load 

in ℓ2 norm (the factor √2 accounts for real and imag-inary parts). By the implicit function theorem, ∥δv∥2 ≤∥J−1∥op 

√2 ∆ load . Applying (60) gives (38).