---
title: Decentralized Machine Learning with Centralized Performance Guarantees via Gibbs Algorithms
title_zh: 通过吉布斯算法实现具有中心化性能保证的去中心化机器学习
authors: "Yaiza Bermudez, Samir Perlaza, Iñaki Esnaola"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20492v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 通过 Gibbs 测度实现不共享本地数据集的去中心化学习
tldr: 本研究首次证明了在不共享原始数据的情况下，去中心化学习可达到与中心化学习相同的性能。通过采用带有相对熵正则化的经验风险最小化（ERM-RER）框架，并建立客户端间的正向-反向通信，将局部Gibbs测度作为后续节点的参考测度（先验信息）。在特定正则化因子缩放条件下，该方法实现了知识的有效传递，为去中心化学习提供了一种共享归纳偏置而非数据的新范式。
selection_source: fresh_fetch
motivation: 探索如何在保护隐私、不共享本地数据集的情况下，使去中心化学习达到中心化学习的性能水平。
method: 采用ERM-RER框架，通过正向-反向通信机制将前一客户端生成的Gibbs测度作为后一客户端的参考测度进行迭代。
result: 证明了通过特定比例缩放正则化因子，去中心化Gibbs算法能取得与访问全部数据的中心化算法完全一致的性能。
conclusion: 该研究提出了一种通过共享模型空间上的参考测度（归纳偏置）来实现去中心化协作的新型学习范式。
---

## 摘要
本文首次证明，在不共享本地数据集的情况下，去中心化学习可以达到中心化性能。具体而言，当客户端采用带有相对熵正则化的经验风险最小化（ERM-RER）学习框架，并建立客户端之间的前向-后向通信时，只需共享本地获得的吉布斯测度（Gibbs measures），即可达到与能够访问所有数据集的中心化 ERM-RER 相同的性能。其核心思想是，客户端 $k$ 生成的吉布斯测度被客户端 $k+1$ 用作参考测度。这有效地建立了一种通过参考测度对先验信息进行编码的有原则的方法。特别地，在去中心化设置中实现中心化性能，需要根据本地样本量对正则化因子进行特定的缩放。总的来说，这一结果为新型去中心化学习范式打开了大门，将协作策略从共享数据转变为通过模型集上的参考测度来共享本地归纳偏置。

## Abstract
In this paper, it is shown, for the first time, that centralized performance is achievable in decentralized learning without sharing the local datasets. Specifically, when clients adopt an empirical risk minimization with relative-entropy regularization (ERM-RER) learning framework and a forward-backward communication between clients is established, it suffices to share the locally obtained Gibbs measures to achieve the same performance as that of a centralized ERM-RER with access to all the datasets. The core idea is that the Gibbs measure produced by client~$k$ is used, as reference measure, by client~$k+1$. This effectively establishes a principled way to encode prior information through a reference measure. In particular, achieving centralized performance in the decentralized setting requires a specific scaling of the regularization factors with the local sample sizes. Overall, this result opens the door to novel decentralized learning paradigms that shift the collaboration strategy from sharing data to sharing the local inductive bias via the reference measures over the set of models.

---

## 论文详细总结（自动生成）

这篇论文《Decentralized Machine Learning with Centralized Performance Guarantees via Gibbs Algorithms》探讨了去中心化学习中的一个基础性问题：如何在不共享原始数据的情况下，使分布式系统达到与中心化系统完全一致的学习性能。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在去中心化学习中，客户端通过网络协作训练模型，通常无法直接交换原始数据（受限于带宽、隐私、所有权或法规）。
*   **核心问题**：标准基准通常是“中心化方案”（即汇总所有数据进行训练），但在去中心化环境下，由于通信限制和数据隔离，这种基准往往难以触及。
*   **研究动机**：探索是否有一种机制，能够通过交换某种“中间信息”而非原始数据，在数学上保证去中心化学习的结果与中心化学习完全等价。

### 2. 方法论
论文提出了一种基于**吉布斯算法（Gibbs Algorithms）**和**嵌套参考测度**的新型协作框架：
*   **核心框架**：采用带有相对熵正则化的经验风险最小化（ERM-RER）。该问题的最优解在数学上表现为吉布斯概率测度。
*   **嵌套结构（Nested Structure）**：
    *   **前向通信**：客户端 $k$ 在本地数据集上进行训练，生成的吉布斯测度（即模型分布）作为“参考测度”（先验信息）传递给客户端 $k+1$。
    *   **后向通信**：当最后一个客户端 $K$ 完成训练后，将最终的吉布斯测度沿链条传回给所有客户端，使大家共享最终模型。
*   **关键技术细节**：
    *   **正则化因子缩放**：为了实现等价性，每个客户端的正则化因子 $\lambda_k$ 必须根据其本地样本量 $n_k$ 与总样本量 $n_0$ 的比例进行缩放，公式为 $\lambda_k = \frac{n_0 \lambda_0}{n_k}$。
    *   **归纳偏置传递**：这种方法将协作策略从“共享数据”转变为“通过参考测度共享归纳偏置（Inductive Bias）”。

### 3. 实验设计
*   **实验性质**：这是一篇**纯理论证明型论文**。
*   **Benchmark（基准）**：研究对比的基准是“中心化 ERM-RER”，即假设一个中心节点可以访问所有客户端的汇总数据集 $z_0$ 并进行单次训练。
*   **对比方法**：论文通过数学推导（定理 3 和定理 5），证明了在特定通信协议下，去中心化生成的 Radon-Nikodym 导数与中心化生成的完全一致（比值为 1）。
*   **数据集/场景**：文中未进行数值仿真或真实数据集实验，而是通过严谨的数学分析涵盖了所有符合 Borel 可测损失函数的通用监督学习场景。

### 4. 资源与算力
*   **算力说明**：由于本文属于理论推导，**未提及任何具体的 GPU 型号、数量或训练时长**。
*   **计算复杂度**：理论上，每个客户端只需解决一个本地的 ERM-RER 问题，主要的计算开销取决于本地损失函数的复杂度和吉布斯测度的表示。

### 5. 实验数量与充分性
*   **充分性评价**：作为理论研究，其“实验”体现在数学证明的完备性上。论文提供了核心定理的证明梗概，并引用了详细的技术报告。
*   **客观性**：证明过程基于信息论中的相对熵性质和 Radon-Nikodym 定理，逻辑严密，不依赖于特定的随机实验结果，因此具有普适的客观性。

### 6. 主要结论与发现
*   **首次证明等价性**：论文首次证明了通过共享吉布斯测度，去中心化学习可以达到与中心化学习完全相同的性能保证。
*   **信息编码机制**：证明了参考测度（Reference Measure）是一种有效的原则性方法，可以编码来自其他节点的数据集信息，而无需直接访问数据。
*   **参数关系**：揭示了局部正则化参数与样本量之间的精确反比关系是实现性能对齐的必要条件。

### 7. 优点
*   **理论突破**：打破了“去中心化必然导致性能损失”的直觉，为分布式学习提供了坚实的理论支撑。
*   **隐私保护**：通过传输概率分布而非原始数据，提供了一种天然的隐私保护层。
*   **范式转移**：提出了一种从“共享数据”到“共享归纳偏置”的协作新范式。

### 8. 不足与局限
*   **通信开销**：传输完整的概率测度（尤其是高维模型空间）在实际中可能非常困难，其通信量可能并不亚于传输原始数据。
*   **有限速率失真**：论文结论建立在“精确传输测度”的假设下。在实际有限带宽中，测度传输会产生失真，论文提到这仍是一个待解决的开放问题。
*   **串行延迟**：前向-后向协议是串行的，客户端 $k+1$ 必须等待 $k$ 完成，这在节点数量巨大时会导致极高的延迟。
*   **缺乏实证**：虽然理论完美，但在深度神经网络等非凸、高维场景下的近似实现效果尚待验证。

（完）
