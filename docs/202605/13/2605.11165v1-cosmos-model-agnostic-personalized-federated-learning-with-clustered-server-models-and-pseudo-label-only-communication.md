---
title: "COSMOS: Model-Agnostic Personalized Federated Learning with Clustered Server Models and Pseudo-Label-Only Communication"
title_zh: COSMOS：基于聚类服务器模型和仅伪标签通信的模型无关个性化联邦学习
authors: "Ben Rachmut, Luise Ge, William Yeoh, Ning Zhang, Yevgeniy Vorobeychik"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.11165v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 带有伪标签通信的个性化联邦学习框架
tldr: 针对联邦学习中模型架构和数据分布的异构性挑战，本文提出COSMOS框架。该框架通过伪标签通信实现模型无关的个性化学习：客户端在公共数据上生成预测，服务端根据预测相似度对客户端聚类，并为每个簇训练专属模型，最后通过知识蒸馏将知识传回客户端。理论证明其具有指数级的个性化风险收缩，实验显示其性能优于现有模型无关基准。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中客户端模型架构不一且数据分布高度异构导致的协作训练难题。
method: 采用基于伪标签通信的聚类机制，在服务端为不同客户端簇训练个性化模型，并通过知识蒸馏实现跨架构的知识传递。
result: 实验证明COSMOS在多个基准测试中优于所有模型无关的联邦学习方法，并能与最先进的个性化联邦学习方法相媲美。
conclusion: 结合伪标签通信与服务端聚类学习是实现大规模、模型无关且高度异构联邦学习的一种极具前景的范式。
---

## 摘要
在异构环境中的联邦学习（FL）仍然具有挑战性，因为客户端模型在架构和数据分布上通常存在差异。虽然最近的方法尝试通过客户端聚类和知识蒸馏来应对这一挑战，但同时处理架构异构性和统计异构性仍然很困难。我们引入了 COSMOS，这是一个模型无关的框架，仅通过伪标签通信即可实现服务器端个性化。客户端训练本地模型并在公共数据上进行预测；服务器根据预测相似度对客户端进行聚类，利用自身的计算资源为每个组训练特定于聚类的模型，并将生成的模型蒸馏回客户端。我们提供了首个理论分析，证明从学习到的聚类模型中进行蒸馏可以产生指数级的个性化风险收缩，超越了模型无关联邦学习中通常提供的收敛至平稳点的保证。在各个基准测试中的实验表明，COSMOS 一贯优于所有模型无关的联邦学习基准方法，同时与最先进的个性化联邦学习方法相比仍具竞争力。更广泛地说，我们的结果强调了基于伪标签的个性化服务器端学习是高度异构环境下可扩展且模型无关联邦学习的一种极具前景的范式。

## Abstract
Federated learning (FL) in heterogeneous environments remains challenging because client models often differ in both architecture and data distribution. While recent approaches attempt to address this challenge through client clustering and knowledge distillation, simultaneously handling architectural and statistical heterogeneity remains difficult. We introduce COSMOS, a model-agnostic framework that enables server-side personalization using only pseudo-label communication. Clients train local models and predict on the public data; the server clusters clients by prediction similarity, trains a cluster-specific model for each group using its own compute, and distills the resulting models back to clients. We provide the first theoretical analysis showing that distillation from the learned cluster models can yield exponential personalization risk contraction, going beyond the convergence-to-stationarity guarantees typically provided in model-agnostic FL. Experiments across benchmarks demonstrate that COSMOS consistently outperforms all model-agnostic FL baselines while remaining competitive with state-of-the-art personalized FL methods. More broadly, our results highlight personalized server-side learning with pseudo-labels as a promising paradigm for scalable and model-agnostic federated learning in highly heterogeneous environments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **COSMOS**（Clustered Output-based Server MOdelS）的联邦学习框架，旨在解决异构环境下的个性化联邦学习难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实的联邦学习（FL）场景中，客户端往往存在**双重异构性**：
    1.  **统计异构性**：不同客户端的数据分布（Non-IID）差异巨大。
    2.  **架构异构性**：客户端可能使用不同的神经网络架构（如出于隐私、硬件限制或专有技术考虑），导致无法直接进行参数聚合。
*   **研究动机**：现有的模型无关（Model-Agnostic）方法（如 FedMD, COMET）要么未能充分利用服务器端的强大算力，要么缺乏严谨的理论保证（如风险收缩证明）。COSMOS 旨在通过仅交换“伪标签”来实现高效、模型无关且具有理论支撑的个性化联邦学习。

### 2. 方法论
COSMOS 的核心思想是利用服务器端的计算资源，根据客户端在公共数据集上的预测相似度进行聚类，并为每个簇训练专属的“教师模型”。
*   **关键技术流程**：
    1.  **本地预训练**：客户端在私有数据上训练本地模型，并对公共无标签数据集 $U$ 生成伪标签。
    2.  **贪心聚类（Greedy Clustering）**：服务器根据伪标签的 $\ell_1$ 距离，通过贪心算法将客户端划分为 $K$ 个簇。该算法不需要预先指定簇的数量，而是由距离阈值 $B_0$ 决定。
    3.  **服务器端蒸馏**：服务器为每个簇训练一个特定的模型（如 VGG），目标函数结合了对客户端平均伪标签的拟合以及输入空间的正则化（用于保证平滑性）。
    4.  **客户端蒸馏**：客户端下载所属簇的服务器模型生成的伪标签，通过知识蒸馏更新本地模型。
    5.  **迭代微调**：重复上述本地训练与双向蒸馏过程。
*   **理论创新**：论文首次在模型无关的 PFL 领域提供了**指数级个性化风险收缩**的理论证明，利用半监督学习中的“扩展性（Expansion）”和“鲁棒性（Robustness）”工具，证明了该框架能有效降低泛化风险。

### 3. 实验设计
*   **数据集**：EMNIST-balanced、CIFAR-10、CIFAR-100、Tiny ImageNet。
*   **异构场景设置**：采用 Dirichlet 分布（$\alpha$ 参数控制）进行 Non-IID 数据划分；客户端架构混合使用 AlexNet、ResNet18、MobileNet 和 SqueezeNet。
*   **对比基准（Benchmarks）**：
    *   **模型无关方法**：FedMD、FedCT、COMET。
    *   **同构个性化方法（作为参考）**：Ditto、FedBABU、pFedCK、pFedHN、FedSelect 等。
*   **公共数据**：使用总训练数据的 20% 作为无标签公共数据集 $U$。

### 4. 资源与算力
*   **硬件环境**：使用了 2 台 NVIDIA GeForce RTX 4090 GPU，94 GB RAM。
*   **训练时长**：
    *   使用 VGG 作为服务器模型时，25 个客户端运行 20 轮约需 **7 小时**（每轮约 20 分钟）。
    *   使用较轻量架构（如 AlexNet）时，全过程约需 **1.5 小时**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流基准数据集，进行了多组消融实验。
*   **充分性评价**：
    *   **客观性**：通过改变 Dirichlet 参数 $\alpha$（1, 5, 100）测试了不同程度的数据异构性。
    *   **公平性**：对比了多种客户端架构组合（如 AlexNet+MobileNet 等），证明了方法对架构异构的鲁棒性。
    *   **深度分析**：对超参数（正则化权重 $\lambda$、蒸馏温度 $T$）、客户端数量（扩展到 200 个）、公共数据比例以及服务器模型强度均做了详细评估。实验设计非常充分且客观。

### 6. 主要结论
*   **性能领先**：COSMOS 在所有模型无关的基准测试中均以显著优势胜出，尤其在复杂任务（如 Tiny ImageNet）上表现更优。
*   **竞争力强**：即使在同构架构设置下，COSMOS 也能与最先进的个性化 FL 方法（如 pFedCK）相媲美。
*   **通信高效**：由于仅传输伪标签而非模型参数，通信开销比传统参数聚合方法降低了 **1-2 个数量级**（例如在 CIFAR-100 上，参数传输需 114MB，而伪标签仅需 3.8MB）。

### 7. 优点
*   **真正的模型无关**：对客户端架构没有任何限制，甚至支持专有/闭源模型参与。
*   **理论与实践结合**：不仅实验效果好，还填补了模型无关 PFL 缺乏风险收缩保证的理论空白。
*   **服务器资源利用**：不同于以往服务器仅做被动聚合，COSMOS 积极利用服务器算力提升了个性化质量。

### 8. 不足与局限
*   **依赖公共数据**：虽然论文证明了即使公共数据比例较小也有效，但该框架仍需一个与任务领域相关的无标签数据集，这在某些极端隐私敏感的领域可能难以获取。
*   **聚类敏感性**：聚类效果高度依赖于公共数据集对客户端数据分布的覆盖程度，如果公共数据与本地数据完全脱节，聚类准确性可能会下降。
*   **计算重心偏移**：服务器端需要为每个簇训练独立模型，当簇数量 $K$ 很大时，服务器端的计算压力会显著增加。

（完）
