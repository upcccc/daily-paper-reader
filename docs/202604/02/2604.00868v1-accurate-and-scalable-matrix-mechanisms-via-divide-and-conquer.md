---
title: Accurate and Scalable Matrix Mechanisms via Divide and Conquer
title_zh: 基于分治策略的准确且可扩展的矩阵机制
authors: "Guanlin He, Yingtai Xiao, Jiamu Bai, Xin Gu, Zeyu Ding, Wenpeng Yin, Daniel Kifer"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00868v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 用于查询回答的差分隐私矩阵机制
tldr: 针对高维数据集差分隐私查询中的矩阵机制扩展性问题，本文提出 QuerySmasher。该方法采用分治策略，将复杂查询分解为相互正交的低维子工作负载，利用现有最优低维机制独立求解后再进行拼接。QuerySmasher 在理论上涵盖并优于 ResidualPlanner 和 WFF 等现有方法，在保证高精度的同时实现了卓越的可扩展性。
selection_source: fresh_fetch
motivation: 现有的高维矩阵机制在处理复杂边际查询时，虽然具有一定的扩展性，但在精度和适用范围上仍有提升空间。
method: 提出一种分治策略，将查询分解并重组为相互正交的低维子问题，独立求解后缝合结果。
result: 理论证明 QuerySmasher 在总平方误差上优于 RP、RP+ 和 WFF，且实验验证了其在高维场景下的准确性与扩展性。
conclusion: QuerySmasher 为大规模差分隐私数据发布提供了一个更通用、更精确且可扩展的矩阵机制框架。
---

## 摘要
矩阵机制常用于在发布统计数据或创建合成数据时提供无偏的差分隐私查询回答。近期的研究开发了如 ResidualPlanner 和 Weighted Fourier Factorizations 等矩阵机制，这些机制在扩展到高维数据集的同时，为边际查询（marginals）和循环乘积查询（circular product queries）等工作负载提供了最优性保证。它们通过向一组能够紧凑表示目标工作负载的线性无关查询集添加噪声来运行。在本文中，我们提出了 QuerySmasher，这是一种基于分治策略的可扩展替代方法。给定一个可以从各种数据边际中回答的工作负载，QuerySmasher 将每个查询拆分为子查询，并将这些部分重新组合成相互正交的子工作负载。这些子工作负载代表了小型、低维的问题，可以通过现有的低维矩阵机制独立且最优地回答。QuerySmasher 随后将这些解决方案缝合在一起，以回答原始工作负载中的查询。我们证明了 QuerySmasher 涵盖了之前的工作，如 ResidualPlanner (RP)、ResidualPlanner+ (RP+) 和 Weighted Fourier Factorizations (WFF)。我们还证明了在所有工作负载下，就均方误差而言，它可以优于这些方法。最后，我们通过实验验证了 QuerySmasher 的可扩展性和准确性。

## Abstract
Matrix mechanisms are often used to provide unbiased differentially private query answers when publishing statistics or creating synthetic data. Recent work has developed matrix mechanisms, such as ResidualPlanner and Weighted Fourier Factorizations, that scale to high dimensional datasets while providing optimality guarantees for workloads such as marginals and circular product queries. They operate by adding noise to a linearly independent set of queries that can compactly represent the desired workloads.   In this paper, we present QuerySmasher, an alternative scalable approach based on a divide-and-conquer strategy. Given a workload that can be answered from various data marginals, QuerySmasher splits each query into sub-queries and re-assembles the pieces into mutually orthogonal sub-workloads. These sub-workloads represent small, low-dimensional problems that can be independently and optimally answered by existing low-dimensional matrix mechanisms. QuerySmasher then stitches these solutions together to answer queries in the original workload.   We show that QuerySmasher subsumes prior work, like ResidualPlanner (RP), ResidualPlanner+ (RP+), and Weighted Fourier Factorizations (WFF). We prove that it can dominate those approaches, under sum squared error, for all workloads. We also experimentally demonstrate the scalability and accuracy of QuerySmasher.

---

## 论文详细总结（自动生成）

以下是对论文《Accurate and Scalable Matrix Mechanisms via Divide and Conquer》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在差分隐私（DP）领域，矩阵机制（Matrix Mechanism）是提供无偏查询回答的强大框架。然而，传统矩阵机制在处理高维数据集时面临严重的**可扩展性瓶颈**。优化一个任意工作负载的矩阵机制通常涉及半正定规划（SDP），其变量数量随数据域大小呈平方级增长，导致在高维场景下计算不可行。
*   **核心问题**：如何在保持高维数据集可扩展性的同时，为结构化查询工作负载（如边际查询、范围查询等）提供比现有方法（如 RP、WFF）更精确的差分隐私保护？

### 2. 方法论：QuerySmasher
QuerySmasher 采用**分治策略**，将一个高维的大规模优化问题分解为多个低维的小规模优化问题。
*   **核心思想**：利用“剩余空间”（Residual Spaces）的概念。任何基于边际（Marginals）的线性查询都可以分解为一组相互正交的子查询，每个子查询仅作用于特定的属性子集。
*   **关键技术细节**：
    1.  **查询分解**：将原始工作负载中的每个查询 $\mathbf{q}$ 投影到不同的剩余空间中，生成子查询 $\mathbf{q}_{\Rightarrow A'}$。这些子查询在逻辑上是相互正交的。
    2.  **子工作负载重组**：将属于同一剩余空间的子查询归类为独立的“子工作负载”。
    3.  **独立优化**：由于子工作负载之间相互正交，可以使用现有的最优低维矩阵机制求解器（如 Yuan 等人的算法）独立地为每个子工作负载生成策略矩阵和噪声协方差。
    4.  **全局噪声缩放**：通过解析解（基于各子工作负载的加权方差和）对各子机制的噪声进行最优缩放，以确保总隐私预算 $\beta$ 得到满足且总误差最小。
    5.  **结果缝合**：将各子机制的噪声回答相加，重构出原始查询的无偏答案。

### 3. 实验设计
*   **数据集**：
    *   **真实数据**：Adult（14个属性）、CPS（5个属性）、Loans（12个属性）。
    *   **合成数据**：采用 $[n]^d$ 架构，改变属性数量 $d$（最高达50）和域大小 $n$（最高达50），模拟极高维度的域（如 $40^{40}$）。
*   **查询类型（Workloads）**：涵盖了边际查询、前缀和查询、范围查询、循环范围查询、仿射查询（Affine）、绝对差查询（Abs）以及随机计数查询。
*   **对比方法（Benchmarks）**：
    *   **HDMM**：经典的结构化矩阵机制。
    *   **ResidualPlanner (RP/RP+)**：之前最先进的可扩展矩阵机制。
    *   **Weighted Fourier Factorizations (WFF)**：针对循环查询优化的最新方法。

### 4. 资源与算力
*   **硬件环境**：双路 AMD EPYC 7763 服务器（128核，1 TB RAM）。
*   **计算特性**：实验主要在 **CPU** 上运行。
*   **耗时与内存**：
    *   在处理包含 10,700 个数据视图的复杂工作负载（$n=40, d=40$）时，求解时间约为 3032 秒，内存占用约 227 GB。
    *   对于较简单的 1-way 和 2-way 查询，求解时间通常在几秒到几十秒之间，内存占用极低（<1 GB）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括不同数据集上的准确性对比（表2、表3、表4、表5）和可扩展性测试（表6、表7）。
*   **充分性**：实验设计非常全面。它不仅测试了标准查询，还引入了非轴对齐的仿射查询和绝对差查询，这些是之前方法难以处理的。
*   **公平性**：通过在相同隐私成本（$\beta=1$）下比较均方根误差（RMSE），并涵盖了之前方法擅长的特定场景（如 WFF 的循环查询），证明了 QuerySmasher 在所有场景下均不弱于甚至显著优于基准方法。

### 6. 主要结论与发现
*   **理论优势**：证明了 QuerySmasher 在数学上涵盖了 RP、RP+ 和 WFF。如果子问题使用最优求解器，QuerySmasher 在总平方误差（SSE）上**理论上优于**这些方法。
*   **准确性提升**：在仿射和绝对差查询上，QuerySmasher 相比次优方法降低了 15%-21% 的噪声。在混合复杂查询下，其 RMSE 始终最低。
*   **可扩展性**：成功处理了数据域高达 $10^{64}$ 的场景，其资源消耗随“数据视图”数量线性增长，而非随数据域大小指数增长。

### 7. 优点
*   **通用性强**：不依赖于特定的基（如 Fourier 基），而是根据工作负载自动寻找最优的紧凑线性表示。
*   **模块化设计**：可以灵活插入任何低维矩阵机制求解器，能够直接受益于未来更快的低维优化算法。
*   **理论完备**：不仅有实验支撑，还提供了严格的数学证明，确保其在 SSE 指标下的统治地位。

### 8. 不足与局限
*   **内存瓶颈**：虽然解决了高维扩展性，但如果单个边际（Marginal）本身的维度过大（单元格数量 $c$ 极多），由于需要处理 $O(c^2)$ 的空间，仍会面临内存压力。
*   **依赖子求解器**：整体效率高度依赖于底层 SDP 求解器的性能。
*   **应用限制**：目前主要针对线性计数查询，对于非线性查询或极度稀疏且无结构的工作负载，提升可能有限。

（完）
