---
title: "Function-Space ADMM for Decentralized Federated Learning: A Control Theoretic Perspective"
title_zh: 去中心化联邦学习的函数空间 ADMM：一种控制理论视角
authors: "Akihito Taya, Yuuki Nishiyama, Kaoru Sezaki"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09356v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 保护数据隐私的去中心化联邦学习
tldr: 针对去中心化联邦学习中非独立同分布（non-IID）数据导致的性能下降问题，本文提出了一种函数空间交替方向乘子法（FedF-ADMM）。该方法利用函数空间中损失泛函的凸性，通过知识蒸馏将更新方向投影回参数空间，并引入基于控制理论的比例-积分（PI）稳定系数。实验证明，该方法在极端non-IID场景下比现有方法具有更快的收敛速度、更高的精度和更好的设备间共识。
selection_source: fresh_fetch
motivation: 传统的去中心化联邦学习在处理非独立同分布数据时，由于参数空间优化的非凸性，容易出现性能严重退化和收敛困难。
method: 提出FedF-ADMM算法，在函数空间利用ADMM推导更新方向，通过知识蒸馏进行参数投影，并引入类PI控制器的稳定系数。
result: 在极端的单标签non-IID设置下，该方法在收敛速度、模型精度以及设备间的共识性方面均优于现有的去中心化联邦学习算法。
conclusion: 通过将优化视角从参数空间转向函数空间并结合控制理论，可以有效解决去中心化联邦学习中的数据异构性挑战。
---

## 摘要
去中心化联邦学习 (FL) 是一种在传感器网络、物联网 (IoT) 设备以及其他不存在中央服务器的边缘系统上训练机器学习模型的极具前景的方法。虽然联邦学习具有保护数据隐私等优势，但它经常受到设备间非独立同分布 (non-IID) 数据分布的影响，从而导致显著的性能下降。当直接优化模型参数时，这一问题尤为严重，因为神经网络训练本质上是非凸的，且凸优化的标准收敛保证并不适用。与主要在参数空间中运行的现有去中心化联邦学习方法不同，我们提出了联邦函数空间交替方向乘子法 (FedF-ADMM)。FedF-ADMM 利用函数空间内损失泛函的凸性来推导基于交替方向乘子法 (ADMM) 的更新方向，随后通过知识蒸馏将其投影到参数空间。我们进一步引入了一个稳定系数，以增强在严重非独立同分布设置下的鲁棒性，并将其解释为比例积分 (PI) 项，从控制理论的角度分析其行为。在具有挑战性的非独立同分布场景（包括每个设备仅拥有单一标签数据的设置）下的实验表明，与现有的去中心化联邦学习方法相比，FedF-ADMM 实现了更快、更稳定的收敛，同时获得了更高的准确率和更好的设备间共识。

## Abstract
Decentralized federated learning (FL) is a promising approach for training machine learning models on sensor networks, Internet of Things (IoT) devices, and other edge systems where no central server exists. While federated learning offers advantages such as preserving data privacy, it often suffers from non-independent and identically distributed (IID) data distributions across devices, which cause significant performance degradation. This issue is particularly severe when directly optimizing model parameters, because neural network training is inherently non-convex and standard convergence guarantees for convex optimization do not apply. Unlike existing decentralized FL methods that primarily operate in parameter space, we propose federated function-space alternating direction method of multipliers (FedF-ADMM). FedF-ADMM exploits the convexity of loss functionals within function space to derive alternating direction method of multipliers (ADMM)-based update directions, which are subsequently projected onto the parameter space via knowledge distillation. We further introduce a stabilization coefficient to enhance robustness under severe non-IID settings and analyze its behavior from a control-theoretic perspective by interpreting it as a proportional-integral (PI) term. Experiments under challenging non-IID scenarios, including settings where each device has data from only a single label, demonstrate that FedF-ADMM achieves faster and more stable convergence than existing decentralized FL methods, while attaining higher accuracy and better consensus among devices.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：去中心化联邦学习的函数空间 ADMM

#### 1. 核心问题与研究背景
*   **核心问题**：在去中心化联邦学习（DFL）中，设备间数据的**非独立同分布（non-IID）**会导致模型性能严重下降和收敛不稳定。
*   **研究背景**：
    *   传统的 DFL 方法（如 DecFedAvg）主要在**参数空间**进行聚合，但神经网络的参数优化本质上是非凸的，缺乏收敛保证。
    *   在极端 non-IID 场景下（如每个设备只有一类标签），本地更新方向会剧烈发散，导致“客户端漂移”现象。
    *   现有的基于知识蒸馏（KD）的方法虽然在函数空间操作，但往往收敛缓慢且在复杂拓扑下表现不佳。

#### 2. 方法论：FedF-ADMM
*   **核心思想**：将优化目标从参数空间转移到**函数空间**（预测输出空间）。由于损失泛函在函数空间通常是凸的，可以利用 **ADMM（交替方向乘子法）** 获得更优的更新方向，再通过知识蒸馏将该方向投影回参数空间。
*   **关键技术细节**：
    *   **函数空间 ADMM**：定义增强拉格朗日泛函，将设备间的共识约束施加在预测函数上。
    *   **虚拟目标生成**：通过 ADMM 迭代计算出每个设备在函数空间的“目标输出”，该目标结合了邻居的平均预测和本地的拉格朗日乘子。
    *   **知识蒸馏投影**：利用一个公共的无标签数据集 $D_s$，通过最小化本地模型输出与虚拟目标之间的均方误差，将函数空间的更新转化为参数更新。
    *   **稳定系数与 PI 控制**：引入稳定系数 $\nu$ 来平滑拉格朗日乘子的更新。论文通过数学推导证明，这种更新机制在动力学上等价于**比例-积分（PI）控制器**，其中 $\nu$ 充当积分项，有效消除了 non-IID 带来的稳态误差。

#### 3. 实验设计
*   **数据集**：Fashion-MNIST 和 CIFAR-10。
*   **实验场景**：
    *   **1-class/2-class 分布**：每个设备仅包含 1 个或 2 个类别的样本（极端 non-IID）。
    *   **Dirichlet 分布**：常用的 non-IID 模拟方式（$\alpha=0.1$）。
    *   **网络拓扑**：环形（Ring）、星形（Star）、随机图（Random）。
*   **对比方法（Benchmarks）**：
    *   参数级方法：DecFedAvg, DecFedProx, DFedAvgM（带动量）, DFedADMM（参数级 ADMM）。
    *   函数级方法：CMFD（作者之前的研究，基于简单共识）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型复杂度**：使用了简单的 CNN 架构（约 1.6M 至 2.1M 个参数）。
*   **计算开销**：作者提到 FedF-ADMM 的计算量约为标准本地 SGD 的两倍（因为增加了 KD 步骤），但通信开销显著降低（传输 Logits 而非参数）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了不同异构程度的对比实验。
    *   进行了详尽的超参数搜索（使用 Optuna 框架，对所有对比方法均进行了调优，确保公平性）。
    *   包含了针对稳定系数 $\nu$、共享数据集规模及分布、网络拓扑结构、设备数量（10 到 20 台）的消融实验和扩展性分析。
*   **充分性评价**：实验设计非常充分且客观，特别是挑战了“单标签（1-class）”这一极端场景，证明了算法的鲁棒性。

#### 6. 主要结论与发现
*   **性能卓越**：在 CIFAR-10 的 1-class 极端场景下，所有参数级方法均失效，而 FedF-ADMM 仍能达到约 60% 的准确率。
*   **共识性强**：FedF-ADMM 显著缩小了设备间的准确率差距（Accuracy Gap），实现了极高的模型一致性。
*   **稳定性**：稳定系数 $\nu$（即 PI 控制中的积分项）是成功的关键，它能抑制 non-IID 引起的输出波动，使收敛过程更加平滑。
*   **通信高效**：传输模型输出（Logits）的通信量远小于传输模型参数（例如 CIFAR-10 场景下，输出共享仅需 200 KB，而参数共享需 8.6 MB）。

#### 7. 优点
*   **理论创新**：首次从控制理论视角解释了函数空间 DFL 的更新动力学，为理解联邦学习提供了新维度。
*   **解决非凸性**：巧妙避开了神经网络参数空间的非凸优化难题。
*   **高鲁棒性**：对网络拓扑结构不敏感，在稀疏连接的拓扑下依然表现良好。

#### 8. 不足与局限
*   **依赖共享数据**：算法需要一个公共的无标签数据集作为“探针”，在某些完全封闭的隐私应用中可能受限。
*   **隐私风险**：虽然传输 Logits 比参数更安全，但仍存在通过 Logits 进行成员推理或模型反演攻击的风险（尽管文中提到可结合差分隐私，但未深入实验）。
*   **计算成本**：KD 步骤增加了边缘设备的计算负担。

（完）
