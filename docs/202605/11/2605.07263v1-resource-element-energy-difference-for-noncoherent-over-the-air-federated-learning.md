---
title: Resource-Element Energy Difference for Noncoherent Over-the-Air Federated Learning
title_zh: 针对非相干空中联邦学习的资源元素能量差方法
authors: "Hao Chen, Zavareh Bozorgasl"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07263v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 空中联邦学习技术
tldr: 针对空中联邦学习（OTA-FL）中相干聚合依赖瞬时信道状态信息（CSI）的难题，本文提出了一种名为REED的非相干聚合方案。该方案通过将更新的正负部分映射到两个正交资源元素的发射能量上，利用能量差实现符号聚合，仅需慢速信道校准。理论证明其具有无偏性且收敛速度达到1/sqrt(T)，实验验证了其在无需精确CSI的情况下仍能保持与相干聚合相近的性能。
selection_source: fresh_fetch
motivation: 传统的空中联邦学习方案高度依赖瞬时信道状态信息和相位对齐，在实际无线系统中难以实现。
method: 提出REED方案，将实值更新的正负部分映射到两个正交资源元素的能量上，通过能量差在服务端估计聚合结果。
result: 理论证明了REED是无偏估计且具有闭式方差，在MNIST等数据集上的实验显示其性能接近相干聚合。
conclusion: REED提供了一种无需瞬时CSI的高效非相干聚合机制，在保证收敛性的同时显著降低了系统实现的复杂性。
---

## 摘要
空中联邦学习 (OTA-FL) 通过利用波形叠加来降低上行链路延迟，但传统的模拟聚合方案通常需要瞬时信道状态信息 (CSI)、信道反转和相干相位对齐，这在实际无线系统中难以维持。本文提出了资源元素能量差 (REED)，这是一种用于连续有符号更新的非相干聚合原语，可避免瞬时 CSI。REED 将每个实值更新的正负部分映射到具有独立相位抖动的两个正交资源元素上的发射能量，服务器根据它们的能量差估计有符号聚合值。仅需对平均信道功率进行慢时间尺度校准，REED 即可实现对目标有符号和的无偏估计，并在瑞利衰落下获得精确的闭式方差。我们将 REED 整合到全参与 FedAvg 中，并证明了平滑非凸平稳性界限。在平均每客户端能量预算下，可以调度聚合增益，使 REED 引起的扰动与局部步长呈二次方比例缩放，从而产生规范的 (1/sqrt(T)) 平稳率。在 MNIST 和 Fashion-MNIST 上的实验表明，在独立同分布 (IID) 设置下，REED 与纯净的 FedAvg 和基于相干 CSIT 的聚合表现非常接近，同时在强数据异构性下保持稳定的收敛，且性能仅有适度下降。

## Abstract
Over-the-air federated learning (OTA-FL) reduces uplink latency by exploiting waveform superposition, but conventional analog aggregation schemes typically require instantaneous channel state information (CSI), channel inversion, and coherent phase alignment, which can be difficult to maintain in practical wireless systems. This paper proposes resource-element energy difference (REED), a noncoherent aggregation primitive for continuous signed updates that avoids instantaneous CSI. REED maps the positive and negative parts of each real-valued update to transmit energies on two orthogonal resource elements with independent phase dithers, and the server estimates the signed aggregate from their energy difference. With only slow-timescale calibration of average channel powers, REED is unbiased for the desired signed sum and admits an exact closed-form variance under Rayleigh fading. We incorporate REED into full-participation FedAvg and prove a smooth nonconvex stationarity bound. Under an average per-client energy budget, the aggregation gain can be scheduled so that the REED-induced perturbation scales quadratically with the local stepsize, yielding the canonical (1/sqrt(T)) stationarity rate. Experiments on MNIST and Fashion-MNIST demonstrate that REED closely matches clean FedAvg and coherent CSIT aggregation in IID settings, while maintaining stable convergence with a moderate performance degradation under strong data heterogeneity.

---

## 论文详细总结（自动生成）

### 针对非相干空中联邦学习的资源元素能量差（REED）方法分析总结

#### 1. 核心问题与整体含义
空中联邦学习（OTA-FL）利用无线信道的叠加特性实现高效的模型聚合，但传统的**相干聚合**方案存在严重的实际部署障碍：它要求精确的瞬时信道状态信息（CSI）、严格的相位对齐以及复杂的信道反转技术。在多设备、快速衰落和存在频率偏移的实际场景中，维持这种相干性极具挑战。
本文的核心动机是设计一种**非相干聚合原语**，在不需要瞬时 CSI 和相位同步的前提下，解决非相干接收机只能检测能量（非负值）而无法直接处理有符号（正负）梯度更新的“符号丢失”难题。

#### 2. 论文提出的方法论：REED
论文提出了**资源元素能量差（Resource-Element Energy Difference, REED）**方案，其核心思想是将实值更新的符号信息转化为能量的空间/频率分布：
*   **正负拆分**：每个客户端将实值更新 $u_k$ 拆分为正部 $[u_k]^+$ 和负部 $[u_k]^-$。
*   **双正交映射**：利用两个正交的资源元素（REs，如不同的子载波或时隙）。正部映射到第一个 RE 的发射能量，负部映射到第二个 RE 的发射能量。
*   **相位抖动**：在发射端加入独立的随机相位抖动，以消除相干干扰的影响。
*   **能量差估计**：服务端分别测量两个 RE 上的接收能量 $E_+$ 和 $E_-$，通过计算两者的差值并结合慢时间尺度的平均功率校准，来估计原始的有符号聚合和。
*   **理论保证**：论文证明了在瑞利衰落信道下，该估计量是**无偏**的，并推导出了精确的闭式方差表达式。

#### 3. 实验设计
*   **数据集**：MNIST（手写数字识别）和 Fashion-MNIST（服装分类）。
*   **实验场景**：
    *   **IID 制度**：数据在客户端间均匀分布。
    *   **异构（Non-IID）制度**：采用 Dirichlet 分布（$\alpha=0.3$）进行标签划分，模拟强数据异构性。
*   **Benchmark（基准方法）**：
    *   **Clean FedAvg**：理想的无损数字聚合，作为性能上限。
    *   **Coherent CSIT**：理想的相干空中计算方案（假设拥有完美瞬时 CSI 和相位补偿）。
*   **对比维度**：在 -10 dB 的低信噪比环境下，对比不同方案的测试准确率轨迹和最终收敛精度。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **开源情况**：作者提供了 GitHub 源代码链接，便于社区复现和评估其计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：设置了 10 个客户端，进行了 100 轮通信，每轮包含 10 个局部 SGD 步。
*   **重复性与客观性**：所有实验均重复运行了 **10 次独立实验**，并报告了均值和标准差（阴影区域）。这种做法有效地排除了随机性干扰，实验设计较为客观、公平。
*   **覆盖范围**：涵盖了从简单（MNIST）到中等难度（Fashion-MNIST）的数据集，以及从 IID 到强 Non-IID 的数据分布，实验覆盖面足以支撑其理论论点。

#### 6. 主要结论与发现
*   **性能匹配**：在 IID 设置下，REED 的表现几乎与理想的 Clean FedAvg 和相干 CSIT 方案重合，证明了非相干能量检测在良性数据分布下的高效性。
*   **异构代价**：在强异构（Fashion-MNIST, $\alpha=0.3$）场景下，REED 相比理想基准有约 3% 的准确率下降。这反映了非相干聚合引入的额外方差在处理客户端漂移（Client Drift）时会产生累积影响。
*   **收敛性验证**：理论证明与实验均证实，通过合理调度聚合增益，REED 可以达到 $O(1/\sqrt{T})$ 的标准非凸收敛速率。

#### 7. 优点
*   **低实现复杂度**：彻底摆脱了对瞬时 CSI 的依赖，仅需慢速功率校准，极大地降低了无线同步的开销。
*   **理论完备性**：不仅提出了方案，还给出了精确的方差闭式解和收敛性证明，这在 OTA-FL 论文中较为少见。
*   **鲁棒性**：在极低信噪比（-10 dB）下仍能保持稳定收敛。

#### 8. 不足与局限
*   **资源开销**：为了处理符号问题，REED 每个标量需要占用 2 个资源元素，相比理想相干方案，其**带宽效率降低了一半**。
*   **异构敏感性**：实验表明在数据高度异构时，REED 的性能退化比相干方案更明显，说明其抗非 IID 干扰的能力仍有提升空间。
*   **实验深度**：实验仅限于小型图像数据集，尚未在大规模模型（如 Transformer）或更复杂的无线信道模型（如非瑞利衰落）中进行验证。

（完）
