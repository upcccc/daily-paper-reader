---
title: Personalized Federated Learning with Residual Fisher Information for Medical Image Segmentation
title_zh: 基于残差 Fisher 信息的个性化联邦学习医学图像分割
authors: "Meilu Zhu, Yuxing Li, Zhiwei Wang, Edmund Y. Lam"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14848v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 隐私限制下的个性化联邦学习
tldr: 针对医疗图像分割中联邦学习面临的数据异构性挑战，本文提出了一种名为 pFL-ResFIM 的个性化联邦学习框架。该框架引入了残差费雪信息矩阵（ResFIM）来量化模型参数对领域差异的敏感度，并通过频谱迁移策略在保护隐私的前提下估计 ResFIM。通过将参数划分为领域敏感和领域不变部分，仅聚合领域不变参数，实现了高效的客户端自适应个性化，在多个公开数据集上取得了领先性能。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中由于不同医疗机构间数据异构性导致的模型性能下降问题。
method: 提出利用残差费雪信息矩阵（ResFIM）识别并分离领域敏感参数，仅在服务器端聚合领域不变参数以实现个性化。
result: 在公开数据集上的广泛实验表明，该方法在医疗图像分割任务中一致优于现有的最先进个性化联邦学习方法。
conclusion: pFL-ResFIM 通过参数级的精细化管理，有效提升了异构数据环境下个性化联邦学习的性能和鲁棒性。
---

## 摘要
联邦学习使多个客户端（机构）能够在不共享私有数据的情况下协作训练机器学习模型。为了应对客户端之间数据异构性的挑战，个性化联邦学习 (pFL) 旨在为每个客户端学习定制化模型。在这项工作中，我们提出了 pFL-ResFIM，这是一种在参数级别实现客户端自适应个性化的新型 pFL 框架。具体而言，我们引入了一种新的度量指标——残差 Fisher 信息矩阵 (ResFIM)，用于量化模型参数对领域差异的敏感性。为了在隐私约束下估计每个客户端模型的 ResFIM，我们采用了一种频谱迁移策略，生成反映不同客户端领域风格的模拟数据。基于估计的 ResFIM，我们将模型参数划分为领域敏感组件和领域不变组件。然后，通过在服务器上仅聚合领域不变参数，为每个客户端构建个性化模型。在公开数据集上的广泛实验表明，pFL-ResFIM 一致优于现有最先进的方法，验证了其有效性。

## Abstract
Federated learning enables multiple clients (institutions) to collaboratively train machine learning models without sharing their private data. To address the challenge of data heterogeneity across clients, personalized federated learning (pFL) aims to learn customized models for each client. In this work, we propose pFL-ResFIM, a novel pFL framework that achieves client-adaptive personalization at the parameter level. Specifically, we introduce a new metric, Residual Fisher Information Matrix (ResFIM), to quantify the sensitivity of model parameters to domain discrepancies. To estimate ResFIM for each client model under privacy constraints, we employ a spectral transfer strategy that generates simulated data reflecting the domain styles of different clients. Based on the estimated ResFIM, we partition model parameters into domain-sensitive and domain-invariant components. A personalized model for each client is then constructed by aggregating only the domain-invariant parameters on the server. Extensive experiments on public datasets demonstrate that pFL-ResFIM consistently outperforms state-of-the-art methods, validating its effectiveness.

---

## 论文详细总结（自动生成）

以下是对论文《Personalized Federated Learning with Residual Fisher Information for Medical Image Segmentation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在医疗图像分割任务中，不同机构（客户端）之间存在显著的**数据异构性**（统计异构性），导致标准的联邦学习（如 FedAvg）性能下降。
*   **研究背景**：现有的个性化联邦学习（pFL）方法（如 FedBN、FedPer）通常采用启发式的、层级的参数分离策略（例如固定保留 Batch Normalization 层或分类器层在本地）。
*   **动机**：现有的层级选择策略是经验驱动且静态的，无法在不同优化阶段动态平衡知识共享与本地个性化。本文旨在实现**原子级（参数级）**且**客户端自适应**的个性化策略。

### 2. 提出的方法论
核心框架为 **pFL-ResFIM**，其关键步骤如下：
*   **残差 Fisher 信息矩阵 (ResFIM)**：引入 ResFIM 指标来量化模型参数对领域差异的敏感度。公式定义为不同领域下 Fisher 信息矩阵（FIM）之差的绝对值，并进行归一化。ResFIM 值越高，表明该参数对领域变化越敏感。
*   **隐私保护的领域模拟（频谱迁移）**：为了在不泄露隐私的前提下估计 ResFIM，客户端利用傅里叶变换（FFT）提取图像的幅度谱（代表风格）和相位谱（代表语义）。客户端在初始化时共享幅度谱，训练时通过替换低频幅度谱生成具有其他客户端风格的模拟数据。
*   **参数级个性化聚合**：
    1.  客户端根据本地数据和模拟数据计算 ResFIM。
    2.  生成二进制掩码（Mask），选择 ResFIM 值最大的前 $\delta\%$ 参数作为“领域敏感”组件保留在本地。
    3.  服务器仅对掩码之外的“领域不变”参数进行聚合和分发。

### 3. 实验设计
*   **数据集**：
    1.  **前列腺分割 (Prostate)**：来自 6 个机构的 T2 加权 MRI 数据。
    2.  **血管分割 (Vessel)**：来自 6 个不同公开数据集的视网膜图像。
    3.  **睑板腺分割 (Gland)**：包含本地采集数据和公开数据集 MGD-1K。
*   **基准方法 (Benchmarks)**：对比了 FedAvg (标准 FL)、FedBN (保留 BN 层)、SioBN (孤岛式 BN)、FedPer (保留分类层)。
*   **评价指标**：Dice 相似系数（Dice similarity coefficient）。

### 4. 资源与算力
*   **软件环境**：使用 PyTorch 库实现。
*   **超参数**：Adam 优化器，Batch Size 为 4，本地训练 4 个 Epoch，总共 100 轮通信。
*   **算力说明**：**论文未明确提及具体的 GPU 型号、数量及总训练时长**。但提到为了降低计算开销，采用了 FIM 的对角近似计算。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同的医学影像任务（MRI、视网膜、红外图像）上进行了验证，涵盖了 14 个不同的客户端场景。
*   **消融实验**：针对关键超参数 $\delta\%$（个性化参数占比）进行了敏感性分析，并对不同层（编码器、分类器）的掩码率进行了可视化分析。
*   **充分性评价**：实验设计较为充分，通过跨多个异构数据集的对比证明了方法的鲁棒性。对比方法均为该领域的经典 SOTA，实验过程遵循了标准的联邦学习评估流程，具有较好的客观性。

### 6. 主要结论与发现
*   **性能领先**：pFL-ResFIM 在所有数据集上均一致优于 FedAvg 和现有的个性化 FL 方法，平均 Dice 分数提升显著。
*   **自适应性**：实验发现，浅层（输入层）和深层（预测层）的参数通常具有更高的 ResFIM 值，这与“浅层捕获风格、深层捕获任务特性”的经验共识相符，证明了 ResFIM 能够自动识别敏感参数。
*   **平衡性**：个性化参数占比 $\delta\%$ 存在最优值，过高或过低都会影响性能，说明平衡知识共享与个性化至关重要。

### 7. 优点
*   **精细化程度高**：打破了以往“按层划分”的局限，实现了参数级别的动态选择。
*   **无需额外辅助数据**：通过频谱迁移技术，在保护隐私的同时巧妙地解决了跨领域敏感度评估的问题。
*   **通用性强**：在多种不同模态的医疗图像任务中均表现出稳定的增益。

### 8. 不足与局限
*   **计算开销**：虽然使用了对角近似，但计算 FIM 仍比普通梯度更新增加了一定的计算负担，尤其是在模型参数量巨大时。
*   **超参数依赖**：性能对 $\delta\%$ 较为敏感，在实际应用中可能需要针对不同任务进行调优。
*   **通信开销**：除了模型参数，客户端还需要上传掩码信息（虽然掩码是二进制的，开销相对较小）。
*   **实验覆盖**：未讨论在极端非独立同分布（Non-IID）标签分布（如类别缺失）情况下的表现，主要侧重于特征层面的异构性。

（完）
