---
title: Federated Learning with Hypergradient-based Online Update of Aggregation Weights
title_zh: 基于超梯度在线更新聚合权重的联邦学习
authors: "Ayano Nakai-Kasai, Tadashi Wadayama"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00458v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 具有在线聚合权重更新的联邦学习
tldr: 针对联邦学习中数据异构和通信环境不稳定的问题，本文提出FedHAW方法。该方法通过超梯度（目标函数对权重的梯度）在线动态更新客户端的聚合权重，计算开销低且适应性强。实验证明，FedHAW在异构环境下具有优异的泛化能力，并能有效应对通信错误，显著提升了联邦学习系统的鲁棒性。
selection_source: fresh_fetch
motivation: 联邦学习在移动和物联网场景下亟需解决数据异构性及通信环境多变带来的性能下降问题。
method: 提出FedHAW，利用超梯度技术在线更新聚合权重，以极低的计算开销实现权重的动态优化。
result: 实验显示该方法在异构数据分布下具有高泛化性能，并对通信过程中的错误具有显著的鲁棒性。
conclusion: FedHAW通过超梯度驱动的权重更新机制，为复杂环境下的联邦学习提供了一种高效且稳健的聚合方案。
---

## 摘要
使用移动和物联网设备的联邦学习不仅需要处理客户端数据分布异构性的能力，还需要对多变的通信环境具有高度的适应性。我们提出了 FedHAW（基于超梯度更新聚合权重的联邦学习），它实现了聚合权重的在线更新。FedHAW 通过使用超梯度（即目标函数相对于权重的梯度）来更新聚合权重，该梯度的计算开销较低。仿真结果表明，所提方法在异构环境中具有较高的泛化性能，并且对通信错误具有较强的鲁棒性。

## Abstract
Federated learning using mobile and Internet of Things devices requires not only the ability to handle heterogeneity of clients' data distributions but also high adaptability to varying communication environments. We propose FedHAW (Federated Learning with Hypergradient-based update of Aggregation Weights) that implements online updates of aggregation weights. FedHAW updates the aggregation weights by using hypergradient, the gradient of the objective function with respect to the weights, which can be calculated with low computational overhead. Simulation results show that the proposed method possesses high generalization performance in heterogeneous environments and high robustness to communication errors.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FedHAW** 的新型联邦学习（FL）聚合算法，旨在通过在线更新聚合权重来解决数据异构性和通信不稳定性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在移动设备和物联网（IoT）场景下，联邦学习面临两大挑战：一是客户端数据分布的**异构性（Non-IID）**，导致模型偏差；二是**通信环境的不稳定**（如丢包、连接中断），影响训练效率和精度。
*   **研究动机**：现有的鲁棒性方法（如 FedLAW）虽然能通过调整聚合权重提升性能，但通常需要预先准备“代理数据集（Proxy Dataset）”并在每轮进行额外的优化训练，这带来了巨大的计算开销，且在实际 IoT 环境中难以获取能代表真实分布的代理数据。因此，需要一种**低开销、无需额外数据、能实时感知环境变化**的在线权重更新机制。

### 2. 核心方法论
*   **核心思想**：借鉴“超梯度下降（Hypergradient Descent）”优化学习率的思想，将其引入到联邦学习的聚合权重更新中。
*   **关键技术细节**：
    *   **权重参数化**：将聚合权重分解为相对权重 $\lambda$（控制各客户端贡献）和缩放因子 $\gamma$（起正则化作用）。
    *   **超梯度更新**：在每一轮聚合时，服务器利用目标函数对权重参数的梯度（即超梯度）来更新 $\gamma$ 和 $\lambda$。
    *   **近似计算**：为了避免直接计算全局梯度的巨大开销，FedHAW 利用当前轮次与上一轮次的模型参数差值（Local Model Updates）来近似估计超梯度。
    *   **算法流程**：
        1. 客户端进行本地训练并上传模型。
        2. 服务器根据上一轮的超梯度信息，在线更新本轮的 $\gamma$ 和 $\lambda$。
        3. 服务器根据更新后的权重进行加权聚合，生成新的全局模型。
        4. 该过程无需代理数据集，且计算复杂度仅略高于传统的 FedAvg。

### 3. 实验设计
*   **数据集**：MNIST（手写数字）、CIFAR-10（物体识别）、Stanford Dogs（细粒度犬类分类）。
*   **异构场景**：使用狄利克雷分布（Dirichlet distribution）模拟 Non-IID 程度，设置参数 $D\alpha \in \{1, 0.1\}$。
*   **通信错误模型**：模拟客户端上传失败的情况，设置最大错误概率 $p_e \in \{0.2, 0.8\}$。
*   **对比方法（Benchmarks）**：
    *   **FedAvg**：最基础的加权平均。
    *   **FedAdp / FedHyper-G**：现有的在线自适应权重方法。
    *   **FedLWS**：层级权重收缩方法。
    *   **FedLAW**：基于代理数据集的离线优化方法（作为性能上限参考）。

### 4. 资源与算力
*   **硬件环境**：Intel Xeon w7-2495X CPU (2.5GHz)，128GB 内存，**NVIDIA RTX A4000 GPU**。
*   **算力开销**：论文明确对比了执行时间。FedHAW 的服务器端聚合耗时约为 0.00056s，仅为 FedAvg 的 4 倍左右，而 FedLAW 则需要 2.31s（高出约 4000 倍）。这证明了 FedHAW 极低的计算开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个不同难度的图像数据集，设置了 2 种异构梯度和 2 种通信错误率。
*   **消融与组合实验**：除了独立测试，论文还验证了 FedHAW 与 **FedProx**（一种改进客户端本地更新的方法）结合的效果。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还深入分析了收敛过程中的波动情况（如在 $p_e=0.8$ 时的表现）。对比方法涵盖了从经典到最新的前沿算法，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **高性能与高泛化**：在大多数异构环境下，FedHAW 的准确率优于所有在线基准方法，甚至在某些情况下超过了需要额外数据的 FedLAW。
*   **强鲁棒性**：在极高通信错误率（$p_e=0.8$）下，FedHAW 表现出更平稳的准确率曲线，尤其在训练早期具有更强的适应能力。
*   **高效率**：FedHAW 成功消除了对代理数据集的依赖，同时保持了极低的计算延迟，非常适合资源受限的 IoT 设备。

### 7. 优点
*   **在线自适应**：能够根据每一轮的训练反馈实时调整权重，无需人工干预或预设参数。
*   **无数据依赖**：解决了联邦学习中“代理数据难获取”的痛点。
*   **通用性强**：作为一种聚合层面的改进，它可以与任何客户端优化算法（如 SGD, Adam, FedProx 等）无缝结合。
*   **计算友好**：超梯度的近似计算极大地降低了服务器端的负担。

### 8. 不足与局限
*   **客户端规模较小**：实验中仅设置了 $K=10$ 个客户端，在大规模（如成千上万个节点）联邦学习系统中的表现尚待验证。
*   **超参数敏感性**：虽然减少了权重的调节，但引入了“元学习率（meta learning rates）” $\eta_\gamma$ 和 $\eta_\lambda$，这些参数在不同数据集上需要手动微调。
*   **任务类型单一**：目前仅在图像分类任务上进行了验证，对于自然语言处理（NLP）或时间序列预测等其他领域的适用性有待考察。

（完）
