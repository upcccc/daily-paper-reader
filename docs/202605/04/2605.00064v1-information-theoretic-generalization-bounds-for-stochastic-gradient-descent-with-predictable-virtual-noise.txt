Title: Information-Theoretic Generalization Bounds for Stochastic Gradient Descent with Predictable Virtual Noise

URL Source: https://arxiv.org/pdf/2605.00064v1

Published Time: Mon, 04 May 2026 00:02:43 GMT

Number of Pages: 83

Markdown Content:
# Information-Theoretic Generalization Bounds for Stochastic Gradient Descent with Predictable Virtual Noise 

Mohammad Partohaghighi Electrical Engineering and Computer Science University of California, Merced Merced, CA, USA 

mpartohaghighi@ucmerced.edu 

Abstract 

Information-theoretic generalization bounds provide a principled framework for analyzing stochastic optimization algorithms by relating expected generalization error to the mutual information between the learned parameters and the training data. Recent virtual perturbation analyses of stochastic gradient descent introduce auxiliary Gaussian noise only at the level of the proof, thereby making mutual infor-mation analytically controllable while leaving the actual SGD trajectory unchanged. Existing bounds, however, typically require the perturbation variances or covariance matrices to be fixed independently of the optimization history. This fixed-noise restriction limits the ability of the theory to represent history-dependent perturbation geometries motivated by moving gradient statistics, gradient-deviation estimates, preconditioners, curvature proxies, and other long-term pathwise information. In this work, we introduce predictable history-adaptive virtual perturbations, where the perturbation covariance Σt at iteration t may depend on the past real SGD history Ht−1, but not on current or future randomness. This predictability condition enables a conditional Gaussian relative-entropy argument and leads to information-theoretic generalization bounds for SGD with history-adaptive virtual noise geometry. The resulting bounds replace fixed local gradient-deviation and gradient-sensitivity quantities with conditional adaptive counterparts and include an adaptive output-sensitivity penalty induced by the accumulated perturbation covariance. The gradient-deviation term reduces to a conditional variance only under an appropriate conditional unbiasedness condition. Because adaptive covariances are generally data-dependent through the optimization history, our formulation separates the local Gaussian smoothing step from the global reference-kernel comparison. In the general case, the bound contains an explicit covariance-comparison cost that measures the KL price of using an admissible reference perturbation geometry different from the actual adaptive covariance. The clean fixed-noise-style bound is recovered only under an admissible synchronization certificate, such as deterministic, public, or prefix-observable covariance constructions. The framework recovers fixed isotropic and fixed geometry-aware virtual perturbation bounds as special cases, while extending virtual perturbation analysis toward history-dependent stochastic optimization without modifying the underlying SGD algorithm. 

# 1 Introduction 

1.1 Motivation 

Stochastic gradient descent (SGD) is one of the central algorithmic primitives of modern machine learning. Its simplicity, scalability, and ability to train highly overparameterized models have made it the default optimization method behind many large-scale learning systems. Despite this practical success, the theoretical mechanisms behind the generalization behavior of SGD remain only partially understood. This gap is 1

> arXiv:2605.00064v1 [cs.LG] 30 Apr 2026

especially pronounced in high-dimensional and nonconvex regimes, where the number of model parameters can far exceed the number of training examples and where classical uniform convergence arguments often provide overly conservative explanations. A central question is why an SGD-trained model can achieve low test error despite being optimized directly on a finite training sample. One way to formalize this question is through the expected generalization error 

gen( W, S ) = E L(W, S ′) − L(W, S ) , (1) where S is the training sample, S′ is an independent ghost sample drawn from the same distribution, and 

W denotes the output of the learning algorithm. Information-theoretic generalization theory provides a principled approach to this problem by relating the generalization error to the mutual information between the learned parameters and the training data. A representative bound takes the form 

|gen( W, S )| ≤ 

r 2R2

n I(W ; S), (2) under an appropriate R-sub-Gaussian loss assumption [ 6]. Thus, controlling generalization can be reduced to controlling how much information about the training sample is retained in the output W . Applying (2) directly to deterministic or nearly deterministic SGD dynamics is challenging. Noisy iterative algorithms such as stochastic gradient Langevin dynamics (SGLD) are often more amenable to information-theoretic analysis because injected noise smooths the conditional distributions of iterates and helps control relative entropy. A typical noisy update has the form 

Wt+1 = Wt − ηtg(Wt, B t) + εt, εt ∼ N (0 , Σt), (3) where Bt is the minibatch and εt is Gaussian perturbation noise. The presence of εt makes one-step relative-entropy comparisons tractable. Yet this analytical advantage comes at a cost: injecting noise into the actual optimization trajectory changes the learning algorithm and may affect training performance. 

1.2 Virtual Perturbations for SGD 

A recent line of work shows that one can obtain information-theoretic generalization bounds for vanilla SGD without injecting noise into the actual training dynamics [ 7, 11 ]. The key idea is to introduce perturbations only as an analytical device. The true SGD trajectory remains 

Wt+1 = Wt − ηtg(Wt, B t), (4) while a virtual perturbed trajectory is defined only for the purpose of analysis: 

fWt+1 = fWt − ηtg(Wt, B t) + εt. (5) Crucially, the gradient in (5) is evaluated at the unperturbed SGD iterate Wt, not at the virtual iterate fWt.Thus, the perturbation process does not modify the optimization algorithm. It only produces a noisy analytical shadow of the SGD path. This virtual perturbation construction makes it possible to bound the mutual information between the perturbed final iterate and the training sample, and then transfer the resulting generalization guarantee back to the original output through a perturbation-sensitivity penalty. In the fixed-noise framework, the resulting bounds depend on local quantities measured along the SGD path, such as gradient-deviation terms, gradient sensitivity to perturbations, and the sensitivity of the final loss value to perturbing the output. The gradient-deviation term reduces to a stochastic gradient variance under appropriate conditional unbiasedness assumptions, but in general it also captures possible conditional bias relative to the population gradient. This is conceptually attractive because the bound reflects path-dependent properties of the actual SGD trajectory rather than relying solely on global Lipschitz or smoothness constants. 21.3 Limitation: Fixed Perturbation Geometry 

Despite its elegance, the standard virtual perturbation framework typically assumes that the perturbation schedule is fixed independently of the optimization history. For example, one may choose scalar isotropic perturbations 

εt ∼ N (0 , σ 2 

> t

I), (6) or deterministic matrix-valued perturbations 

εt ∼ N (0 , Σt), (7) where the sequence {σt}t or {Σt}t is specified in advance and does not depend on the observed SGD trajectory. This fixed-noise assumption is mathematically convenient, but it limits the flexibility of the theory. In realistic stochastic optimization, many important quantities evolve with the training path. Adaptive learning rates depend on past gradients; preconditioners such as AdaGrad- or Adam-type second-moment estimates accumulate historical information; moving averages summarize long-term gradient behavior; curvature proxies change as the optimization trajectory enters different regions of the loss landscape; and estimates of gradient deviation or stochastic gradient fluctuation may vary significantly between early and late training. A perturbation geometry fixed before training cannot adapt to these changes. This limitation is closely related to the broader issue of long-term dependence. Once a perturbation scale, covariance, or preconditioning geometry is allowed to depend on past iterates and gradients, the analysis must account for the dependence between the perturbation process, the data, and the optimization history. The challenge is to allow such adaptivity without destroying the conditional structure that makes information-theoretic analysis possible. 

1.4 Our Approach: Predictable History-Adaptive Perturbations 

We propose a history-adaptive extension of the virtual perturbation framework. Instead of requiring the perturbation covariance to be fixed independently of the optimization trajectory, we allow it to depend on the past real history of SGD. Let Ht−1 denote the real SGD history available before the perturbation at iteration t

is drawn. Informally, Ht−1 contains the past iterates, past minibatches, and past stochastic gradients, but not the current perturbation or future randomness. At iteration t, we allow the virtual perturbation covariance to take the form 

Σt = Φ t(Ht−1), (8) where Φt is a measurable covariance-selection rule. The key structural condition is predictability: 

Σt is Ht−1-measurable . (9) Equivalently, the covariance at step t may depend on the past optimization history, but not on the current perturbation, current unrevealed randomness, or future information. Under this condition, the virtual perturbation is conditionally Gaussian: 

εt | H t−1 ∼ N (0 , Σt). (10) This predictability condition is sufficient for the one-step conditional Gaussian smoothing argument: after conditioning on the relevant past, the covariance Σt is fixed, and conditional relative-entropy bounds can be applied. However, predictability alone is not the whole mutual-information argument. Because Σt is generally data-dependent through the SGD history, the reference-kernel comparison in the mutual-information decomposition must also be handled carefully. In the general case, comparing actual and reference virtual kernels with different adaptive covariances introduces an explicit covariance-comparison cost. The clean 3fixed-noise-style bound is recovered only when the synchronized reference covariance is admissible, for example under deterministic, public, or prefix-observable covariance constructions. The resulting theory is still purely analytical. The true SGD update remains (4) , and the adaptive perturbations appear only in the virtual path (5) . Thus, our framework does not propose a new optimizer. Rather, it extends the analytical machinery of virtual perturbation bounds to perturbation geometries that can adapt to the long-term history of the SGD trajectory. The accumulated perturbation covariance becomes random and history-dependent: 

Σ1: t =

> t−1

X

> k=1

Σk. (11) Consequently, the classical fixed-noise gradient-deviation and gradient-sensitivity terms are replaced by conditional adaptive counterparts. Let 

¯g(w) = EZ∼D [g(w, Z )] (12) denote the population gradient. At a high level, our bounds involve an adaptive gradient-deviation term of the form 

V ad  

> t

= E

h

∥g(Wt, B t) − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

, (13) and an adaptive gradient-sensitivity term of the form 

Γad  

> t

= E

h

∥¯g(Wt + ξt) − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

, (14) where ξt denotes the accumulated virtual perturbation up to time t, conditionally distributed as a centered Gaussian with covariance Σ1: t given the real SGD history. The term V ad  

> t

is a conditional mean-square deviation from the population gradient; it becomes a conditional variance only under the appropriate conditional unbiasedness condition. The final bound also includes an adaptive output perturbation-sensitivity penalty, which measures the cost of transferring the information-theoretic guarantee from the perturbed output back to the original SGD output. 

1.5 Contributions 

The main contribution of this paper is a conditional information-theoretic extension of virtual perturbation analysis from fixed perturbation schedules to predictable history-adaptive perturbation processes. Our contributions are summarized as follows. • Predictable virtual perturbations. We introduce a history-adaptive virtual perturbation framework in which the perturbation covariance at each step is measurable with respect to the past real SGD history. This allows the analytical noise geometry to adapt to the realized SGD trajectory while preserving the conditional Gaussian structure needed for the local one-step relative-entropy analysis. • Conditional relative-entropy analysis. We prove and use conditional Gaussian smoothing inequalities for perturbations with predictable covariance. This shows that full independence of the perturbation covariance from the optimization history is stronger than necessary for the local relative-entropy step; predictability is the relevant causal condition. • Certified reference-kernel comparison. We separate the local Gaussian smoothing step from the global mutual-information reference comparison. Since history-adaptive covariances are generally data-dependent, a reference kernel cannot automatically use the actual covariance. We identify S-admissible reference constructions, including deterministic, public, prefix-observable, and ghost-adaptive covariances. 4• General covariance-comparison bound. When the actual and reference perturbation geometries differ, the bound contains an explicit covariance-comparison cost. This term is the Gaussian KL price of comparing the actual adaptive smoothing covariance with an admissible reference covariance. The clean fixed-noise-style bound is recovered only under an admissible synchronization certificate. • Adaptive gradient-deviation and sensitivity terms. We derive conditional, history-adaptive ana-logues of the local gradient-deviation and gradient-sensitivity quantities appearing in fixed-noise virtual perturbation bounds. The gradient-deviation term reduces to a conditional variance only under condi-tional unbiasedness of the stochastic gradient; otherwise it also captures conditional bias relative to the population gradient. • Adaptive output-sensitivity penalty. We characterize the output-sensitivity term induced by the accumulated adaptive covariance. This term quantifies the cost of transferring a generalization guarantee from the virtually perturbed output back to the original SGD output. We control it under global smoothness and further refine it through a covariance-weighted local-curvature interpretation. • Recovery of fixed-noise bounds and adaptive examples. We show that fixed isotropic and fixed geometry-aware virtual perturbation bounds are recovered as special cases. We also discuss adaptive scalar, diagonal, Adam-like, curvature-aware, and low-rank covariance choices motivated by moving gradient statistics, preconditioned geometry, pathwise curvature proxies, and long-term optimization history. Overall, our framework extends virtual perturbation analysis beyond fixed-noise constructions while leaving the underlying SGD algorithm unchanged. It provides a theoretical bridge toward understanding generaliza-tion in settings where the analytical perturbation geometry reflects long-term dependence in the optimization history, while making explicit the reference-admissibility and covariance-comparison costs required in fully history-adaptive comparisons. 

Organization. The remainder of the paper is organized as follows. Section 2 reviews related work on stability-based generalization, information-theoretic generalization bounds, noisy iterative algorithms, virtual perturbation analysis, and adaptive optimization with long-term dependence. Section 3 introduces the learning setup, the SGD dynamics, the information-theoretic generalization bound, and the fixed virtual perturbation baseline. Section 4 explains why fixed perturbation schedules are restrictive and motivates predictable history-adaptive virtual perturbations. Section 5 formalizes the predictable covariance framework, including the real SGD history, augmented histories, accumulated perturbations, and conditional Gaussian structure. Section 6 defines the adaptive gradient-deviation, gradient-sensitivity, reference-geometry, and output-sensitivity quantities that appear in our bounds. Section 7 presents the main covariance-comparison generalization theorem, together with admissibly synchronized and comparable-covariance corollaries. Section 8 shows how fixed isotropic, fixed geometry-aware, adaptive scalar, diagonal, and Adam-like virtual perturbation geometries fit into the framework. Section 9 controls the adaptive output-sensitivity penalty under smoothness assumptions and refines it through local curvature. Section 10 discusses the role of long-term dependence, predictability, reference comparisons, and the limitations of the framework. Section 11 provides an optional numerical diagnostic illustration of the bound proxies and the information-sensitivity trade-off. Section 12 concludes and outlines future directions. The appendices contain the full technical details: Appendix 13 proves the conditional Gaussian relative-entropy lemmas, Appendix 14 proves the mutual-information decomposition and conditioning-compression step, Appendix 15 proves the main theorem, Appendix 16 recovers the fixed-noise bounds, Appendix 17 proves the smoothness and local-curvature sensitivity controls. 52 Related Work 

2.1 Stability and Generalization of SGD 

Algorithmic stability is a classical approach to understanding why learning algorithms generalize. At a high level, a stable learning algorithm is one whose output does not change significantly when a single training example is modified or removed. This notion gives a direct route from algorithmic sensitivity to bounds on the gap between training and population performance. For stochastic gradient descent, the work of Hardt et al. [1] initiated a systematic stability analysis and showed that SGD can enjoy strong generalization guarantees under smoothness assumptions. These results are especially transparent in convex and strongly convex settings, where contraction properties of gradient-based updates help control the propagation of perturbations in the data. The nonconvex setting is substantially more delicate. Modern deep learning models are high-dimensional, highly nonconvex, and often overparameterized. In such regimes, uniform stability bounds may depend sensitively on smoothness constants, step-size schedules, and the number of optimization steps. Subsequent work has refined stability analyses of SGD by weakening assumptions, sharpening rates, or consider-ing different loss geometries and sampling schemes [ 2– 4]. Nevertheless, stability-based approaches and information-theoretic approaches emphasize different mechanisms. Stability focuses on sensitivity to changes in individual samples, whereas the present work follows an information-theoretic route by controlling the mutual information between the training sample and a virtually perturbed version of the learned parameters. Our contribution is complementary to stability-based analyses. We do not attempt to prove uniform stability of SGD, nor do we analyze the stability of a new optimizer. Instead, we extend the virtual perturbation method for bounding mutual information. The perturbation process in our framework is purely analytical: the underlying SGD trajectory remains unchanged, while the proof introduces a predictable history-adaptive virtual noise geometry. The resulting bounds expose path-dependent gradient-deviation, gradient-sensitivity, covariance-comparison, and output-sensitivity terms rather than a uniform replacement-sensitivity coefficient. 

2.2 Information-Theoretic Generalization 

Information-theoretic generalization theory provides a broad and algorithm-agnostic framework for bounding expected generalization error. A representative result states that, under an R-sub-Gaussian loss assumption, the expected generalization error of a learning algorithm with output W trained on sample S can be bounded as 

|gen( W, S )| ≤ 

r 2R2

n I(W ; S), (15) where I(W ; S) denotes the mutual information between the output and the training data. This perspective was developed in influential works by Russo and Zou [5] , Xu and Raginsky [6] , and related subsequent studies. The central intuition is that a learning algorithm generalizes well when its output does not reveal too much information about the idiosyncrasies of the training sample. The strength of (15) is its generality: it applies to a wide class of randomized learning algorithms and does not rely directly on uniform convergence over a hypothesis class. Its main challenge is equally clear: to obtain a useful generalization guarantee, one must control the mutual information I(W ; S). For deterministic or nearly deterministic algorithms, this quantity can be large or even unbounded without additional structure. This difficulty is particularly relevant for vanilla SGD, whose randomness may come only from initialization, minibatch sampling, or data ordering. Consequently, a significant body of work has focused on exploiting algorithmic noise, conditional mutual information, or data-dependent refinements to make information-theoretic bounds informative [8–10]. 6Our work builds on this information-theoretic tradition but targets a specific limitation in virtual per-turbation analyses: the common assumption that the perturbation geometry is fixed independently of the optimization history. We relax this fixed-geometry requirement by allowing predictable history-adaptive perturbation covariances. At the same time, because such covariances are generally data-dependent through the SGD trajectory, the mutual-information comparison must account for the reference-kernel geometry. Our formulation therefore separates three issues that are often implicit in the fixed-covariance setting: the local conditional Gaussian smoothing step, the global admissibility of the reference kernel, and the covariance-comparison cost incurred when the actual and reference perturbation covariances differ. The clean fixed-noise-style bound is recovered only when the synchronized reference covariance is admissible. 

2.3 SGLD and Noisy Iterative Algorithms 

Noisy iterative algorithms are especially natural objects for information-theoretic analysis. In stochastic gradient Langevin dynamics and related methods, Gaussian noise is injected directly into the parameter update: 

Wt+1 = Wt − ηtg(Wt, B t) + εt, εt ∼ N (0 , Σt). (16) The injected noise smooths the conditional distributions of successive iterates and makes it possible to control relative entropy terms along the optimization path. This feature enables mutual-information bounds to decompose across iterations through chain-rule arguments. Works such as Pensia et al. [7] , Negrea et al. [8] , and Haghifam et al. [9] use this structure to derive generalization bounds for noisy iterative algorithms, including SGLD. The benefit of real injected noise is analytical tractability. However, the same noise also modifies the optimization trajectory. In practice, the amount and geometry of noise can affect training loss, convergence behavior, and optimization bias. Thus, while noisy algorithms provide a valuable theoretical lens, they do not directly explain the generalization behavior of vanilla SGD unless one can relate the noisy and noiseless dynamics. This motivates virtual perturbation approaches, which aim to obtain some of the analytical benefits of noise without altering the actual learning algorithm. The present work remains in the virtual perturbation setting rather than the real-noise setting. The Gaussian perturbations are introduced only inside the proof. Their covariance may adapt to the past real SGD history, but the SGD recursion itself is not changed. This distinction is important: our predictable covariance process is an analytical device for controlling mutual information, not a prescription for injecting adaptive noise during training. 

2.4 Virtual Perturbation Analysis 

Virtual perturbation analysis introduces noise only inside the proof. The actual SGD iterates follow 

Wt+1 = Wt − ηtg(Wt, B t), (17) while the analysis constructs a perturbed shadow trajectory 

fWt+1 = fWt − ηtg(Wt, B t) + εt. (18) The crucial feature of (18) is that the gradient is evaluated at the original SGD iterate Wt, not at the perturbed iterate fWt. Therefore, the perturbations do not define a new training algorithm. They are auxiliary random variables introduced to make the mutual information between the data and the perturbed output analytically controllable. The virtual perturbation framework of Neu et al. [11] shows that one can obtain information-theoretic generalization bounds for vanilla SGD by combining a perturbation analysis of the final output with a 7chain-rule decomposition of relative entropy along a virtual noisy path. The resulting bounds depend on local, pathwise quantities such as gradient-deviation terms, which reduce to stochastic gradient variance terms under appropriate unbiasedness assumptions, as well as gradient sensitivity to perturbations along the SGD path and the sensitivity of the final loss value to output perturbations. This is a significant conceptual advance because it avoids injecting noise into the real SGD trajectory while still exploiting noise in the proof. Existing virtual perturbation bounds, however, typically assume that the perturbation scale or covariance sequence is fixed independently of the observed optimization history. For instance, the perturbations may be isotropic with prescribed variances σ2 

> t

, or geometry-aware with deterministic covariance matrices Σt. Our work extends this line of analysis by allowing the virtual perturbation covariance to be a predictable process: 

Σt = Φ t(Ht−1), Σt is Ht−1-measurable , (19) where Ht−1 denotes the real SGD history available before the perturbation at iteration t is drawn. Under this condition, 

εt | H t−1 ∼ N (0 , Σt), (20) so after conditioning on the past, the covariance is fixed and the local one-step conditional Gaussian relative-entropy argument remains valid. This extension introduces an additional technical issue that is absent in the deterministic fixed-covariance case. Because Σt may depend on the data through the SGD history, the reference kernel used in the mutual-information decomposition cannot be treated as if it automatically had access to the same covariance. The full proof must distinguish the actual predictable covariance from the admissible reference covariance. If the actual and reference perturbation covariances differ, the Gaussian comparison produces an explicit covariance-comparison term. Thus, the clean bound familiar from fixed virtual perturbation analysis is recovered only when the synchronized comparison is admissible, while the general history-adaptive theorem includes the corresponding covariance-comparison cost. 

2.5 Adaptive Optimization and Long-Term Dependence 

Adaptive optimization methods such as AdaGrad, RMSProp, and Adam use historical gradient information to choose step sizes, preconditioners, or coordinate-wise scaling factors [ 12 –14 ]. These methods introduce long-term dependence because the update at time t depends not only on the current stochastic gradient but also on moving averages or accumulated statistics from previous iterations. Even when the underlying optimization algorithm is not changed, an analytical perturbation geometry that reflects local gradient scale, curvature proxies, gradient-deviation estimates, or stochastic fluctuation estimates naturally becomes history-dependent. Long-term dependence creates technical obstacles for information-theoretic analysis. If step sizes, preconditioners, or perturbation covariances depend on past gradients, then they also depend on the data through the optimization trajectory. This dependence complicates relative-entropy decompositions that are straightforward when perturbation covariances are fixed in advance. The key challenge is to identify a condition that permits adaptation to the past without allowing the perturbation process to depend on current or future randomness in a way that breaks the conditional proof. Our work addresses this challenge for virtual perturbation geometry. We do not analyze the true adaptive update dynamics of Adam, AdaGrad, or RMSProp, and we do not propose a new adaptive optimizer. Instead, we isolate a tractable analytical extension: the covariance of the virtual perturbation at time t may depend on the past real SGD history, provided that it is predictable with respect to Ht−1. This condition allows the local proof step to proceed through conditional Gaussian relative-entropy bounds. For the full mutual-information comparison, our framework further identifies the reference-admissibility requirement and the covariance-comparison cost that appears when the actual and reference perturbation geometries differ. As a result, the framework captures a controlled form of long-term dependence in the analytical perturbation process while recovering fixed isotropic and fixed geometry-aware perturbation bounds as special cases. 83 Preliminaries and Problem Setup 

This section introduces the notation and assumptions used throughout the paper. We first define the supervised learning problem, the expected generalization error, and the information-theoretic bound that forms the basis of our analysis. We then describe the SGD dynamics and the fixed virtual perturbation construction that serves as the baseline for our history-adaptive extension. Throughout the formal development, we use the following indexing convention. The initial iterate is W1,the final output is WT , and hence there are T − 1 SGD updates. For t = 1 , . . . , T − 1, the t-th update maps 

Wt to Wt+1 . This convention avoids the common ambiguity between WT and WT +1 .

3.1 Data, Loss, and Empirical Risk 

Let Z denote a data point taking values in a measurable space Z, and let D be the underlying data-generating distribution. The training sample is denoted by 

S = ( Z1, . . . , Z n), Zi i.i.d.

∼ D. (21) We also define an independent ghost sample 

S′ = ( Z′

> 1

, . . . , Z ′

> n

), Z′

> i
> i.i.d.

∼ D, (22) where S′ is independent of S and of all algorithmic and auxiliary randomness. In some reference-kernel constructions we also use optional public, auxiliary, or ghost randomness. We denote such randomness by U,and whenever it appears we assume 

U is independent of S. (23) If no such auxiliary randomness is used, U is understood to be the trivial sigma-field. This notation will be used later when defining admissible reference kernels. Let w ∈ Rd denote the parameter vector of the model. We consider a loss function 

ℓ : Rd × Z → R, (24) where ℓ(w, z ) is the loss of parameter w on data point z. For a finite sample s = ( z1, . . . , z n), define the empirical risk 

L(w, s ) = 1

n

> n

X

> i=1

ℓ(w, z i). (25) The corresponding population risk is 

LD(w) = EZ∼D [ℓ(w, Z )] . (26) We assume throughout that the loss is differentiable with respect to w. We write 

g(w, z ) = ∇wℓ(w, z ) (27) for the sample gradient and 

¯g(w) = EZ∼D [g(w, Z )] (28) for the population gradient. For a minibatch B = {zi : i ∈ J}, where J ⊆ [n], we use the notation 

g(w, B ) = 1

|J|

X

> i∈J

g(w, z i). (29) 9We impose the standard sub-Gaussian loss assumption used in information-theoretic generalization analysis. For every fixed w ∈ Rd, the random variable ℓ(w, Z ), with Z ∼ D , is assumed to be R-sub-Gaussian, meaning that for all λ ∈ R,

E exp {λ (ℓ(w, Z ) − E[ℓ(w, Z )]) } ≤ exp 

 λ2R2

2



. (30) This condition is satisfied, for example, when the loss is uniformly bounded on the support of the data distri-bution. The sub-Gaussian assumption is used only to connect mutual information to expected generalization error. 

3.2 Expected Generalization Error 

Let A be a possibly randomized learning algorithm, and let 

W = A(S) (31) denote its output after training on the sample S. The expected generalization error is defined as 

gen( W, S ) = E L(W, S ′) − L(W, S ) , (32) where the expectation is taken over the training sample S, the ghost sample S′, the internal randomness of the algorithm, minibatch sampling, initialization, and any auxiliary randomness introduced for analysis. Since S′

is independent of W , the first term can be interpreted as the population performance of the learned parameter: 

E[L(W, S ′)] = E[LD(W )] . (33) Thus, gen( W, S ) measures the expected gap between population risk and empirical training risk at the algorithmic output. 

3.3 Information-Theoretic Generalization Bound 

The starting point of our analysis is a classical information-theoretic generalization inequality. Under the sub-Gaussian assumption in (30) , the expected generalization error of any possibly randomized learning algorithm satisfies 

|gen( W, S )| ≤ 

r 2R2

n I(W ; S), (34) where I(W ; S) denotes the mutual information between the algorithmic output W and the training sample S

[6]. Equivalently, 

I(W ; S) = DKL (PW,S ∥ PW ⊗ PS ), (35) where PW,S is the joint distribution of (W, S ) and PW ⊗ PS is the product of the marginals. The inequality (34) reduces the problem of bounding expected generalization error to controlling how much information the learned parameter retains about the training data. For deterministic or nearly deterministic SGD, directly controlling I(WT ; S) can be difficult. The virtual perturbation approach addresses this challenge by applying (34) to a perturbed version of the SGD output and then controlling the discrepancy between the perturbed and unperturbed outputs through an output-sensitivity term. The present paper follows this high-level strategy, but replaces fixed virtual perturbation covariances with predictable history-adaptive ones. 10 3.4 Stochastic Gradient Descent Dynamics 

We now define the SGD dynamics considered in this paper. Let W1 be an initial parameter vector drawn from a distribution independent of the training sample S. For each update t = 1 , . . . , T − 1, let Jt ⊆ [n] denote the minibatch index set and let 

Bt = ZJt = {Zi : i ∈ Jt} (36) be the corresponding minibatch. We write bt = |Jt| for the minibatch size. The minibatch stochastic gradient is 

Gt = g(Wt, B t) = 1

bt

X

> i∈Jt

g(Wt, Z i). (37) The SGD recursion is 

Wt+1 = Wt − ηtGt, t = 1 , . . . , T − 1, (38) where ηt > 0 is the learning rate. The final output of the algorithm is WT . Unless otherwise stated, the learning-rate schedule and the minibatch-index selection mechanism are chosen independently of the training sample values. The index sets Jt may be deterministic, sampled with replacement, sampled without replacement, or generated according to any data-value-independent rule. The realized minibatches Bt = ZJt

and gradients Gt are, of course, data-dependent. For later use, we define the real SGD history available immediately before update t as 

Ht−1 = σ (W1, . . . , W t, J 1, . . . , J t−1, B 1, . . . , B t−1, G 1, . . . , G t−1) , t = 1 , . . . , T − 1. (39) The sigma-field Ht−1 contains the past SGD trajectory and past observed optimization information, but not the current perturbation, current unrevealed randomness, current stochastic gradient, or future information. Importantly, Ht−1 does not include virtual perturbations. This distinction will be essential when defining predictable history-adaptive covariance processes. 

Remark 1 (Gradient-deviation versus variance) . Throughout the paper, quantities of the form 

E

h

∥Gt − ¯g(Wt)∥2 

> M

| H t−1

i

are interpreted as conditional mean-square gradient-deviation terms from the population gradient. They reduce to conditional variance terms only when 

E[Gt | H t−1] = ¯ g(Wt).

This distinction is important for finite-sample sampling schemes and history-dependent conditioning, where the current stochastic gradient may not be conditionally unbiased for the population gradient. 

3.5 Fixed Virtual Perturbation Path 

The fixed virtual perturbation construction introduces an auxiliary noisy trajectory coupled to the original SGD path. For a deterministic sequence of positive definite covariance matrices 

Σ1, . . . , ΣT −1 ∈ Rd×d, Σt ≻ 0, (40) let 

εt ∼ N (0 , Σt), t = 1 , . . . , T − 1, (41) 11 be independent Gaussian perturbations, independent of S, the initialization, and the minibatch-index random-ness. The fixed virtual perturbation path is defined recursively by 

fWt+1 = fWt − ηtGt + εt, t = 1 , . . . , T − 1, (42) with fW1 = W1.The crucial feature of (42) is that Gt is the same stochastic gradient used by the original SGD update (38) . In particular, Gt is evaluated at the true iterate Wt, not at the virtual iterate fWt. Thus, the virtual perturbation path does not define a new optimization algorithm; it is an analytical construction used to control information-theoretic quantities. Let the accumulated perturbation before iterate t be 

ξt =

> t−1

X

> k=1

εk, ξ1 = 0 . (43) By comparing (38) and (42), we obtain 

fWt = Wt + ξt, t = 1 , . . . , T. (44) Since the perturbations are independent Gaussian random variables, the accumulated perturbation satisfies 

ξt ∼ N (0 , Σ1: t), Σ1: t =

> t−1

X

> k=1

Σk, (45) with Σ1:1 = 0 . In the isotropic special case Σt = σ2 

> t

I, this becomes 

Σ1: t = σ21: tI, σ21: t =

> t−1

X

> k=1

σ2

> k

. (46) This fixed perturbation construction is the baseline used in existing virtual perturbation analyses of SGD [ 11 ]. The main goal of the present paper is to extend this construction by replacing the deterministic covariance sequence {Σt} with a predictable covariance process of the form 

Σt = Φ t(Ht−1), Σt is Ht−1-measurable . (47) This replacement changes only the analytical perturbation process; the true SGD recursion (38) remains unchanged. Because a history-adaptive covariance is generally data-dependent through the real SGD trajectory, the later mutual-information comparison must also account for the reference perturbation geometry. Deterministic covariances are automatically admissibly synchronized. In the general history-adaptive setting, the clean fixed-noise-style bound is recovered only when the reference kernel has an admissible synchronization certificate. Otherwise, one must use an admissible reference covariance and retain the explicit covariance-comparison cost in the main theorem. 

# 4 Limitation of Fixed Virtual Perturbations 

Virtual perturbation analysis provides a way to study the generalization behavior of SGD without injecting noise into the actual training dynamics. The perturbations are introduced only in the proof, and the resulting noisy shadow trajectory is used to control information-theoretic quantities. Existing analyses typically rely on perturbation schedules that are fixed before the optimization trajectory is observed. This section explains 12 why such fixed perturbation geometries are analytically convenient but restrictive, and why predictable history-adaptive perturbations provide a natural relaxation. We also clarify a key technical point. Predictability is sufficient for the local conditional Gaussian smoothing step, but it is not by itself sufficient for the full mutual-information comparison. The latter also requires an admissible reference-kernel construction. When the actual and reference covariances differ, the bound must include an explicit covariance-comparison cost. 

4.1 Fixed Covariance Assumption 

In the standard virtual perturbation framework, the auxiliary noise variables are drawn from a predetermined Gaussian sequence. Following the indexing convention of this paper, W1 is the initial iterate, WT is the final output, and the updates are indexed by t = 1 , . . . , T − 1. One fixes positive definite covariance matrices 

Σ1, . . . , ΣT −1 ∈ Rd×d, Σt ≻ 0, (48) or, in the isotropic case, scalar perturbation variances 

Σt = σ2 

> t

I. (49) The virtual perturbations then satisfy 

εt ∼ N (0 , Σt), t = 1 , . . . , T − 1, (50) with the covariance sequence chosen independently of the optimization history. Under this assumption, the virtual path is defined by 

fWt+1 = fWt − ηtGt + εt, Gt = g(Wt, B t), (51) where Wt denotes the original SGD iterate and Bt is the minibatch at time t. The gradient is evaluated at Wt,not at fWt, so the perturbations do not modify the actual SGD algorithm. They only generate an analytical shadow trajectory. The fixed covariance assumption makes the accumulated perturbation particularly simple. Defining 

ξt =

> t−1

X

> k=1

εk, ξ1 = 0 , fWt = Wt + ξt, (52) we have 

ξt ∼ N (0 , Σ1: t), Σ1: t =

> t−1

X

> k=1

Σk, Σ1:1 = 0 . (53) The accumulated covariance Σ1: t is deterministic. This is one reason the fixed-noise analysis is technically clean: the perturbation law is independent of the data and of the realized SGD trajectory, which simplifies relative-entropy decompositions and mutual-information bounds [11]. However, this simplicity comes from a strong restriction. The analytical perturbation geometry must be specified without observing the path whose local geometry it is intended to probe. The rest of this section explains why this restriction becomes limiting when the goal is to capture history-dependent or adaptive perturbation geometries. 13 4.2 Why Fixed Perturbation Geometry Is Restrictive 

The geometry of the SGD trajectory is rarely stationary over training. At early stages, gradients may be large, noisy, and poorly aligned across minibatches. In the middle of training, local curvature, gradient-deviation behavior, stochastic fluctuation, and coordinate-wise scales can change as the trajectory moves across different regions of the loss landscape. Near the end of training, the sensitivity of the loss to parameter perturbations, often interpreted through flatness or sharpness, can become a dominant feature of the generalization behavior. A fixed perturbation covariance cannot adapt to these changes. For example, an isotropic covariance σ2 

> t

I

treats all directions in parameter space equally, even though the local loss geometry may be highly anisotropic. A deterministic matrix-valued sequence Σt can encode a preferred geometry, but only one selected before the optimization path is observed. Such a sequence cannot respond to realized gradient norms, minibatch fluctuation, gradient-deviation proxies, curvature proxies, or coordinate-wise scale changes along the actual trajectory. This restriction matters because virtual perturbation bounds involve a trade-off between two effects. Larger perturbations can reduce inverse-covariance weighted information terms by smoothing the conditional distributions of the perturbed iterates. At the same time, larger accumulated perturbations can increase the final output-sensitivity penalty, since the perturbed output may incur a different loss than the original SGD output. The favorable balance between these two effects is generally path-dependent. A covariance that is appropriate in a noisy, high-gradient region of training may be inappropriate near a sharp or flat final region, and vice versa. The point is not that the SGD algorithm itself should be changed. Rather, the analytical perturbations used to study SGD should be allowed to reflect the local and historical structure of the trajectory being analyzed. A more flexible theory should allow the virtual perturbation geometry to depend on past optimization information while preserving enough conditional structure for information-theoretic analysis. 

4.3 Long-Term Dependence and Data-Dependent Geometry 

The natural mathematical object for representing the information available before update t is the real SGD history 

Ht−1 = σ (W1, . . . , W t, J 1, . . . , J t−1, B 1, . . . , B t−1, G 1, . . . , G t−1) , (54) where 

Gs = g(Ws, B s) (55) is the stochastic gradient at time s. This history contains the past SGD trajectory and past observed optimization information, but it does not contain the virtual perturbations. Quantities such as moving averages of gradient norms, subbatch fluctuation diagnostics, gradient-deviation proxies, diagonal scale estimates, curvature proxies, and preconditioner-like statistics are naturally functions of Ht−1.Consequently, a natural history-adaptive virtual perturbation covariance would have the form 

Σt = Φ t(Ht−1), (56) where Φt is a measurable covariance-selection rule. This formulation allows the analytical noise geometry at time t to reflect information gathered along the previous SGD path. The difficulty is that Σt is then data-dependent. Indeed, the iterates W1, . . . , W t, past minibatches, and past gradients G1, . . . , G t−1 are functions of the training sample S, the minibatch-index schedule, and the initialization. Thus, the perturbation covariance in (56) is no longer independent of the training data or of the optimization trajectory. This dependence prevents the fixed-noise proof from being applied directly. In the deterministic covariance case, the conditional distribution of each perturbation is known in advance. When 14 the covariance is history-dependent, the perturbation law itself becomes random and path-dependent. The accumulated covariance 

Σ1: t =

> t−1

X

> k=1

Σk (57) is no longer deterministic; it is a random object determined by the past trajectory. As a result, the accumulated perturbation ξt is no longer governed by a fixed Gaussian law independent of the optimization history. The central issue, however, is not adaptivity itself. The central issue is whether the adaptivity is controlled in a way that preserves the conditional Gaussian structure needed for the local relative-entropy step, and whether the resulting data-dependent perturbation geometry can be compared to a valid reference kernel in the mutual-information decomposition. This observation motivates the predictability condition introduced next. 

4.4 Predictability as a Causal Relaxation 

Full independence of the perturbation covariance from the optimization history is stronger than necessary for the one-step Gaussian smoothing argument. What is needed locally is that Σt be known before the current virtual perturbation is drawn. This is precisely the notion of predictability. We require that 

Σt is Ht−1-measurable . (58) Under this condition, Σt may depend on past iterates, past minibatches, past stochastic gradients, and other statistics computed from the previous optimization history. However, it may not depend on the current perturbation εt, the current unrevealed minibatch randomness, the current stochastic gradient Gt, or future information. With (58), the virtual perturbation at time t satisfies the conditional Gaussian relation 

εt | H t−1 ∼ N (0 , Σt). (59) After conditioning on the relevant past, the covariance is fixed, so the conditional Gaussian relative-entropy argument can be applied at the current step. This is the main technical replacement for the fixed-noise assumption in the local smoothing part of the proof. There is, however, an additional issue at the level of the full mutual-information comparison. Because Σt may depend on the data through Ht−1, a reference kernel in the chain-rule decomposition cannot automatically be assumed to use the same covariance without justification. In the notation used later, the reference kernel may use a reference covariance Σref  

> t

that is admissible with respect to the reference-visible information, but this covariance need not equal the actual adaptive covariance Σt. If the actual and reference virtual perturbation covariances differ, the Gaussian comparison produces an explicit covariance-comparison, or covariance-mismatch, term. Thus, the clean fixed-noise-style bound is recovered only when the synchronized reference covariance is admissible, for example in deterministic, public-random, prefix-observable, or otherwise certified construc-tions. Without such a certificate, the general history-adaptive theorem must retain the covariance-comparison cost. The resulting framework keeps the original SGD trajectory unchanged: 

Wt+1 = Wt − ηtGt, Gt = g(Wt, B t). (60) Only the virtual perturbation process is generalized. Thus, the contribution is analytical rather than algorithmic: we do not propose a new optimizer, but rather a more flexible proof framework for studying the generalization of SGD under history-adaptive virtual perturbation geometry. The following sections formalize this predictable perturbation framework, define the corresponding adaptive gradient-deviation, gradient-sensitivity, reference-geometry, and output-sensitivity terms, and prove 15 the resulting information-theoretic generalization bounds. The fixed covariance setting is recovered as the special case in which every Σt is deterministic and hence automatically Ht−1-measurable. In the fully history-adaptive setting, the main theorem additionally tracks the covariance-comparison cost required by the reference-kernel argument. 

# 5 Predictable History-Adaptive Virtual Perturbations 

We now introduce the main analytical object of the paper: predictable history-adaptive virtual perturbations. The goal is to generalize fixed virtual perturbation schedules by allowing the covariance of the analytical noise to depend on the past real SGD trajectory. The true SGD dynamics remain unchanged. Only the virtual perturbation process used in the proof is made adaptive. A technical point is important at the outset. The covariance process should be predictable with respect to the real SGD history, not with respect to a filtration that already contains the realized virtual perturbations. If one conditions on a sigma-field containing 

ε1, . . . , ε t−1, then the accumulated perturbation 

ξt =

> t−1

X

> k=1

εk

is already known, and its conditional covariance is zero. For this reason, we distinguish between the real SGD history and the augmented history that also contains the virtual perturbations. 

5.1 SGD History and Augmented Filtrations 

Recall that the true SGD recursion is 

Wt+1 = Wt − ηtGt, Gt = g(Wt, B t), t = 1 , . . . , T − 1, (61) where W1 is the initial iterate and WT is the final output after T − 1 updates. The virtual perturbations introduced below are auxiliary random variables and do not influence the true iterates Wt. For r = 0 , . . . , T −

1, define the real SGD history after r updates by 

Hr = σ (W1, . . . , W r+1 , J 1, . . . , J r, B 1, . . . , B r, G 1, . . . , G r) . (62) Thus, immediately before update t, the available real history is Ht−1. This sigma-field contains the past optimization trajectory, past minibatch index sets, past realized minibatches, and past stochastic gradients. It does not contain the current minibatch index Jt, the current minibatch Bt, the current stochastic gradient 

Gt, the current virtual perturbation εt, or future randomness. Most importantly, Ht−1 does not contain any virtual perturbations. It is also useful to define the augmented history that includes the previously realized virtual perturbations. For r = 0 , . . . , T − 1, let 

Gr = Hr ∨ σ(ε1, . . . , ε r), (63) with the convention that σ(ε1, . . . , ε 0) is the trivial sigma-field. Thus, before update t, the augmented history is Gt−1. The distinction between Ht−1 and Gt−1 is essential. The real history Ht−1 is used to define predictable covariance selection. The augmented history Gt−1 describes the realized virtual path. Since Gt−1

contains ε1, . . . , ε t−1, it also contains ξt. Therefore, the accumulated perturbation is measurable with respect to Gt−1, but not with respect to Ht−1. This separation reflects the role of virtual perturbations in the analysis. The true SGD path is generated without using the analytical perturbations. The perturbations create a noisy shadow path, but they do not feed back into the optimization dynamics. 16 5.2 Predictable Covariance Processes 

We now define the class of adaptive perturbation covariances considered in this work. 

Definition. A sequence of random positive definite matrices 

{Σt}T −1

> t=1

is called a predictable history-adaptive covariance process if 

Σt ≻ 0, Σt is Ht−1-measurable , t = 1 , . . . , T − 1. (64) Thus, Σt may depend on the past iterates W1, . . . , W t, past minibatch index sets J1, . . . , J t−1, past realized minibatches B1, . . . , B t−1, past stochastic gradients G1, . . . , G t−1, or any statistic computed from them. For example, Σt may be chosen using moving averages of gradient norms, subbatch fluctuation diagnostics, gradient-deviation proxies, diagonal scale estimates, curvature proxies, or preconditioner-like quantities computed from the past trajectory. The covariance Σt may not depend on the current perturbation εt, the current minibatch index Jt, the current minibatch Bt, the current stochastic gradient Gt, or future information. This restriction ensures that, at the moment εt is drawn, the covariance matrix is already determined by the past. In this sense, predictability is the causal relaxation of the stronger fixed-covariance assumption used in classical virtual perturbation analysis. For technical convenience, the main theorem assumes nondegenerate perturbations. One may impose, for example, 

λmin (Σ t) ≥ λ0 > 0 almost surely for all t. (65) This condition guarantees that Σ−1 

> t

is well-defined and avoids singular perturbation laws. Singular or low-rank perturbations require either regularization or pseudo-inverse formulations together with support conditions; these variants are not part of the basic theorem. Deterministic fixed covariance schedules are recovered as a special case. Indeed, if each Σt is determinis-tic, then it is automatically Ht−1-measurable. Thus, the predictable framework extends fixed isotropic and fixed geometry-aware perturbation settings. 

5.3 Conditional Construction of the Virtual Perturbations 

Given a predictable covariance process 

{Σt}T −1 

> t=1

,

we construct the virtual perturbations conditionally on the real SGD trajectory. Equivalently, after conditioning on the final real history HT −1, the perturbations 

ε1, . . . , ε T −1

are conditionally independent centered Gaussian random vectors with 

εt | H T −1 ∼ N (0 , Σt), t = 1 , . . . , T − 1. (66) Because Σt is Ht−1-measurable and Ht−1 ⊆ H T −1, this conditional construction is well-defined. Sequentially, this implies 

εt | G t−1 ∼ N (0 , Σt), t = 1 , . . . , T − 1. (67) 17 Moreover, εt is conditionally independent of the current minibatch-index randomness, the current minibatch, the current stochastic gradient, and all future real-SGD randomness, given the appropriate past history. This conditional independence is used later when applying the conditional Gaussian relative-entropy lemmas. The adaptive virtual perturbation path is defined by 

fWt+1 = fWt − ηtGt + εt, t = 1 , . . . , T − 1, (68) with fW1 = W1. As in fixed virtual perturbation analysis [ 11 ], the stochastic gradient Gt in (68) is the gradient computed along the true SGD trajectory: 

Gt = g(Wt, B t). (69) It is not computed at the perturbed iterate fWt. Therefore, (68) does not define a new optimization algorithm. It is an analytical construction coupled to the original SGD path. 

5.4 Accumulated Adaptive Covariance 

For t = 1 , . . . , T , define the accumulated perturbation before iterate t as 

ξt =

> t−1

X

> k=1

εk, ξ1 = 0 . (70) Comparing the true recursion (61) with the virtual recursion (68), we obtain 

fWt = Wt + ξt, t = 1 , . . . , T. (71) Thus, the virtual path remains a perturbation of the true SGD path, but the perturbation covariance is now selected adaptively from the past real trajectory. The accumulated adaptive covariance before iterate t is 

Σ1: t =

> t−1

X

> k=1

Σk, Σ1:1 = 0 . (72) Unlike in the fixed-noise setting, Σ1: t is generally random. It depends on the realized SGD trajectory through the predictable covariance process. Nevertheless, Σ1: t is measurable with respect to the real SGD history 

Ht−1. Indeed, for each k < t , predictability gives that Σk is Hk−1-measurable. Since the histories are increasing, 

Hk−1 ⊆ H t−1, k < t, (73) each Σk, and hence their sum Σ1: t, is Ht−1-measurable. The matrix Σ1: t is the adaptive analogue of the deterministic accumulated covariance appearing in fixed perturbation analysis. It represents the total virtual noise geometry accumulated along the trajectory before iterate t. Because it is history-dependent, later gradient-deviation and sensitivity quantities will be defined conditionally with respect to the real SGD history. 

5.5 Conditional Perturbation Structure 

The key conditional distributional fact is that the accumulated perturbation remains Gaussian when condi-tioned on the real SGD history. Under the conditional construction above, for t = 1 , . . . , T ,

ξt | H t−1 ∼ N (0 , Σ1: t), (74) 18 where for t = T , HT −1 is the final real history after all T − 1 updates. Consequently, 

E[ξt | H t−1] = 0 , Cov( ξt | H t−1) = Σ 1: t. (75) The conditioning in (74) is crucial. If instead one conditions on the augmented history Gt−1, then the past perturbations ε1, . . . , ε t−1 are already known. Therefore ξt is Gt−1-measurable and 

Cov( ξt | G t−1) = 0 . (76) Thus, statements about the covariance of the accumulated perturbation must be made relative to the real SGD history Ht−1, not the augmented history containing the realized perturbations. This distinction prevents a common filtration mistake in adaptive perturbation analysis. The covariance process is predictable with respect to Ht−1, while the realized virtual trajectory is described by Gt−1. The former is used to define the conditional perturbation law; the latter describes the realized noisy path used in mutual-information decompositions. Finally, this section only defines the adaptive perturbation process. Because Σt is generally data-dependent through the real SGD history, the later mutual-information proof must compare the actual virtual kernel with a valid reference kernel. The reference kernel may use an admissible reference covariance Σref  

> t

, which need not equal the actual covariance Σt. When an admissible synchronization certificate permits Σref  

> t

= Σ t, the clean fixed-noise-style bound is recovered. When the actual and reference covariances differ, an explicit covariance-comparison term appears in the information bound. In the next section, we use the conditional perturbation structure developed here to define adaptive analogues of local gradient-deviation, gradient-sensitivity, reference-geometry, and output perturbation-sensitivity quantities. These quantities replace the fixed covariance terms appearing in classical virtual perturbation bounds and provide the ingredients for the main generalization theorem. 

# 6 Adaptive Local Sensitivity and Gradient-Deviation Measures 

The main history-adaptive generalization bounds involve three types of local quantities: stochastic gradient deviation, gradient sensitivity, and output perturbation sensitivity. In fixed virtual perturbation analysis, these quantities are evaluated under a deterministic perturbation geometry. In the present setting, the perturbation covariance Σt is predictable and may depend on the past real SGD history. Consequently, the relevant quantities are naturally defined conditionally with respect to the real SGD history and, in the general reference-geometry case, with respect to admissible reference-side randomness. The definitions below are analytical objects used to state and prove the generalization bounds. They do not modify the SGD update 

Wt+1 = Wt − ηtGt.

They only describe how the virtual perturbation geometry enters the information-theoretic analysis. 

6.1 Adaptive Gradient-Deviation Term 

Let 

¯g(w) = EZ∼D [g(w, Z )] (77) denote the population gradient at parameter w. For update t = 1 , . . . , T − 1, the true SGD recursion uses the minibatch gradient 

Gt = g(Wt, B t). (78) 19 The predictable covariance Σt determines the geometry in which the current stochastic gradient is compared to the population gradient. We define the adaptive local gradient-deviation term by 

V ad  

> t

= E

h

∥Gt − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

, t = 1 , . . . , T − 1. (79) Here 

∥x∥2Σ−1

> t

= x⊤Σ−1 

> t

x. (80) Although we retain the notation V ad  

> t

by analogy with variance-type quantities in previous virtual per-turbation bounds, the quantity in (79) is, in full generality, a conditional mean-square deviation from the population gradient. It is a genuine conditional variance only under a conditional unbiasedness condition. To see this, define the conditional mean gradient 

mt = E [Gt | H t−1] . (81) Since Σt is Ht−1-measurable, the usual conditional bias–variance decomposition gives 

V ad  

> t

= Tr  Σ−1 

> t

Cov ( Gt | H t−1) + ∥mt − ¯g(Wt)∥2Σ−1

> t

. (82) Thus V ad  

> t

contains two contributions. The first term is the conditional stochastic fluctuation of the minibatch gradient around its conditional mean. The second term is the squared conditional bias between the conditional mean gradient and the population gradient. If 

E [Gt | H t−1] = ¯ g(Wt), (83) then the bias term in (82) vanishes and V ad  

> t

reduces to the adaptive conditional gradient variance 

V ad  

> t

= Tr  Σ−1 

> t

Cov ( Gt | H t−1) . (84) Without (83) , the more precise interpretation is conditional mean-square gradient deviation. The expectation in (79) is over the current minibatch-index randomness, the current minibatch values not already revealed by the history, and any other randomness not included in Ht−1. This distinction is important in history-dependent sampling schemes, finite-sample minibatch sampling, sampling without replacement, adaptive sampling, or settings where previous observations reveal information about the training sample. In the isotropic special case Σt = σ2 

> t

I, the adaptive gradient-deviation term becomes 

V ad  

> t

= σ−2 

> t

E

h

∥Gt − ¯g(Wt)∥2 | H t−1

i

. (85) If the conditional unbiasedness condition (83) also holds, then (85) is the isotropic conditional variance scaled by σ−2 

> t

.If Σt is deterministic, then V ad  

> t

reduces to the corresponding history-conditioned fixed-geometry mean-square gradient-deviation term. Under conditional unbiasedness, this is the usual history-conditioned fixed-geometry variance term. A further reduction to a Wt-conditioned fixed-noise expression requires an additional condition ensuring that the conditional law of the current stochastic gradient depends on the past only through Wt.20 6.2 Adaptive Gradient Sensitivity 

The second quantity measures how sensitive the population gradient is to perturbing the current iterate by the accumulated virtual noise. Recall that for t = 1 , . . . , T ,

Σ1: t =

> t−1

X

> k=1

Σk, Σ1:1 = 0 . (86) Because Σ1: t is determined by the real SGD history up to time t − 1, it is fixed after conditioning on Ht−1.For the synchronized actual-geometry definition below, let 

ζt | H t−1 ∼ N (0 , Σ1: t) (87) be a fresh conditional perturbation independent of the current minibatch randomness given Ht−1. Equiva-lently, because the synchronized precision Σ−1 

> t

is Ht−1-measurable, one may use the accumulated virtual perturbation ξt under its conditional law given Ht−1. The fresh-copy notation emphasizes that the sensitivity is a local Gaussian average around Wt with covariance Σ1: t. We define the adaptive local gradient sensitivity by 

Γad  

> t

= E

h

∥¯g(Wt + ζt) − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

, t = 1 , . . . , T − 1. (88) This definition mirrors the gradient-sensitivity term in fixed virtual perturbation analysis, but with two adaptive features. First, the perturbation around Wt has accumulated covariance Σ1: t, which is random and history-dependent. Second, the discrepancy between population gradients is measured in the current inverse covariance geometry Σ−1 

> t

. Thus, Γad  

> t

captures how strongly the population gradient changes under the accumulated adaptive perturbation, measured in the geometry of the current virtual noise. If the covariance sequence is deterministic, then (88) reduces after averaging over the SGD trajectory to the fixed geometry-aware sensitivity 

ΓΣt,Σ1: t (Wt) = Eζ∼N (0 ,Σ1: t)

h

∥¯g(Wt + ζ) − ¯g(Wt)∥2Σ−1

> t

i

(89) provided the fixed-noise formulation is expressed as a function of Wt. The history-conditioned version is the more precise object when the sampling mechanism or conditioning contains information beyond Wt. In the isotropic case Σt = σ2 

> t

I, this becomes 

Γad  

> t

= σ−2 

> t

Eζ∼N (0 ,σ 21: tI)

h

∥¯g(Wt + ζ) − ¯g(Wt)∥2 | H t−1

i

, (90) where 

σ21: t =

> t−1

X

> k=1

σ2

> k

. (91) 

6.3 Reference-Geometry Gradient Deviation and Sensitivity 

The clean admissibly synchronized covariance bound uses the current precision matrix Σ−1 

> t

in (79) and (88) .For the general covariance-comparison theorem, however, the actual virtual kernel and the reference virtual kernel may use different covariances. If the reference covariance at time t is denoted by Σref  

> t

, the Gaussian mean-comparison term is naturally weighted by 

(Σ ref  

> t

)−1,

not necessarily by Σ−1 

> t

. To support this general form, we define reference-geometry versions of the two local quantities. Let 

Kt = Ht−1 ∨ U , (92) 21 where U denotes optional public, auxiliary, or ghost randomness used by the reference construction and independent of the original sample S. If no such auxiliary randomness is used, U is the trivial sigma-field and Kt = Ht−1. Let Λt ≻ 0 be the reference precision used in the one-step comparison. In the general covariance-comparison theorem, 

Λt = (Σ ref  

> t

)−1. (93) We define the reference-geometry gradient-deviation term by 

V ad  

> t

(Λ t) = E

h

∥Gt − ¯g(Wt)∥2Λt | K t

i

. (94) This quantity is a conditional mean-square deviation from the population gradient in the reference geometry. It should be called a reference-geometry variance only under the corresponding conditional unbiasedness condition. When Λt is Kt-measurable, define 

mK 

> t

= E [Gt | K t] . (95) Then 

V ad  

> t

(Λ t) = Tr (Λ t Cov ( Gt | K t)) + mK 

> t

− ¯g(Wt) 2Λt . (96) Thus, the reference-geometry term also consists of a conditional fluctuation part and a conditional bias part. If 

E[Gt | K t] = ¯ g(Wt),

then the second term vanishes and V ad  

> t

(Λ t) becomes a genuine conditional variance in the reference geometry. The reference-geometry sensitivity term is defined using the realized accumulated virtual perturbation 

ξt = fWt − Wt (97) whenever Λt may depend on the virtual prefix. This avoids implicitly assuming independence between the precision matrix and the perturbation being measured. Define 

Γad  

> t

(Λ t) = E

h

∥¯g(Wt + ξt) − ¯g(Wt)∥2Λt | K t

i

. (98) If Λt is Kt-measurable, then the realized ξt can equivalently be replaced by a fresh conditional Gaussian copy 

ζt | H t−1 ∼ N (0 , Σ1: t),

giving 

Γad  

> t

(Λ t) = E

h

∥¯g(Wt + ζt) − ¯g(Wt)∥2Λt | K t

i

. (99) Thus, the realized-perturbation definition (98) is the general definition, while the fresh-copy expression (99) is a convenient special case. The synchronized case corresponds to 

Λt = Σ −1 

> t

, (100) so that 

V ad  

> t

(Σ −1 

> t

) = V ad  

> t

, Γad  

> t

(Σ −1 

> t

) = Γ ad  

> t

. (101) This identity is used in the clean synchronized corollary only when the synchronization is admissible in the reference-kernel comparison. A useful simplification occurs when the actual and reference precisions are comparable. If there exists κ ≥ 1 such that 

(Σ ref  

> t

)−1 ⪯ κ Σ−1 

> t

, (102) then 

V ad 

> t

 (Σ ref  

> t

)−1 ≤ κV ad  

> t

, Γad 

> t

 (Σ ref  

> t

)−1 ≤ κΓad  

> t

. (103) This comparability condition is useful for stating readable versions of the general covariance-comparison theorem. 22 6.4 Adaptive Output Sensitivity 

The mutual-information part of the proof controls the generalization error of a perturbed output. To transfer this control back to the original SGD output, we must account for the loss difference between WT and a perturbation of WT . In the adaptive setting, the accumulated covariance at the final output is 

Σ1: T = 

> T−1

X

> k=1

Σk, (104) which is random and determined by the real SGD history HT −1. For any deterministic sample s =(z1, . . . , z n), define the adaptive output sensitivity by 

∆ad Σ1: T (WT , s ) = E [L(WT , s ) − L(WT + ζT , s ) | H T −1] , (105) where 

ζT | H T −1 ∼ N (0 , Σ1: T ) (106) is an independent final perturbation drawn conditionally on the accumulated adaptive covariance. The use of the fresh perturbation ζT rather than the realized accumulated perturbation ξT is deliberate. After conditioning on an augmented history that contains the realized perturbations, ξT would be fixed. The quantity in (105) instead measures the local sensitivity of the loss around WT under a fresh perturbation with the same accumulated adaptive covariance. Thus, it is a conditional analogue of the fixed-noise output sensitivity used in virtual perturbation analysis. The final perturbation-sensitivity penalty appearing in the main theorem is 

Rad ∆ = E

h

∆ad Σ1: T (WT , S ′) − ∆ad Σ1: T (WT , S )

i

. (107) Here S′ denotes an independent ghost sample drawn from the same population distribution as S. This term quantifies the cost of transferring a generalization guarantee from the perturbed output back to the original SGD output. When the loss is insensitive to perturbations around WT in the accumulated adaptive covariance geometry, this term is small. Later smoothness or local curvature assumptions can be used to control Rad ∆ in terms of trace or curvature-weighted quantities involving Σ1: T .

6.5 Interpretation and Comparison with Fixed-Noise Quantities 

The adaptive quantities introduced above are analogues of the gradient deviation, gradient-sensitivity, and output-sensitivity terms that appear in fixed virtual perturbation bounds. The main difference is that the covariance geometry is now random, predictable, and history-dependent. 

Role Fixed-noise quantity Adaptive quantity 

Gradient deviation V hist  

> t, Σt

or Vt, Σt (Wt) V ad 

> t

Gradient sensitivity Γhist Σt,Σ1: t or ΓΣt,Σ1: t (Wt) Γad 

> t

Output sensitivity ∆Σ1: T (WT , s ) ∆ad Σ1: T (WT , s )

Reference-geometry deviation not needed in the admissibly synchronized fixed-noise case V ad  

> t

(Λ t)

Reference-geometry sensitivity not needed in the admissibly synchronized fixed-noise case Γad  

> t

(Λ t)

The adaptive gradient-deviation term V ad  

> t

measures the conditional mean-square discrepancy between the current stochastic gradient and the population gradient in the inverse covariance geometry chosen from 23 the past. Under conditional unbiasedness, it reduces to an adaptive conditional gradient variance. Without conditional unbiasedness, it also includes the squared conditional bias term shown in (82). The adaptive gradient sensitivity Γad  

> t

measures how much the population gradient changes under accumu-lated adaptive perturbations. The adaptive output sensitivity ∆ad Σ1: T measures local flatness or perturbation sensitivity of the final output under the accumulated adaptive covariance. The reference-geometry quantities 

V ad  

> t

(Λ t) and Γad  

> t

(Λ t) support the general covariance-comparison theorem. They are not needed in the clean admissibly synchronized covariance corollary, where Λt = Σ −1 

> t

. When actual and reference covariances differ, these weighted quantities and the associated covariance-comparison term together describe the cost of the one-step comparison. These are all analytical quantities used to state and prove generalization bounds. They are not used to update the SGD iterates. The true SGD recursion remains 

Wt+1 = Wt − ηtGt. (108) When Σt is deterministic for every t, the adaptive definitions reduce to their fixed covariance counterparts in the history-conditioned sense. Under additional sampling assumptions, these may be further expressed as functions of Wt alone. When Σt = σ2 

> t

I, the framework reduces further to the scalar isotropic perturbation setting. Thus, the adaptive quantities extend the fixed-noise quantities while preserving their conceptual roles in the generalization analysis. 

# 7 Main Theoretical Results 

We now present the main information-theoretic generalization bound for SGD with predictable virtual perturbations. The central distinction in this section is between the covariance used by the actual virtual perturbation and the covariance available to the reference kernel in the mutual-information decomposition. Let 

S = ( Z1, . . . , Z n)

be the training sample. For a loss function ℓ(w, z ), define the population and empirical risks by 

L(w) = EZ [ℓ(w, Z )] , LS (w) = 1

n

> n

X

> i=1

ℓ(w, Z i).

We write 

¯g(w) = ∇L(w)

for the population gradient. The expected generalization error of an algorithmic output W is 

gen( W, S ) = EL(W ) − LS (W ).

The real SGD recursion is 

Wt+1 = Wt − ηtGt, Gt = g(Wt, B t), t = 1 , . . . , T − 1, (109) where Bt denotes the minibatch randomness at time t. The virtual trajectory is 

fWt+1 = fWt − ηtGt + εt, fW1 = W1, (110) where 

εt | H t−1 ∼ N (0 , Σt).

24 Here Ht−1 denotes the real SGD history up to time t − 1, excluding the virtual perturbation innovations, and 

Σt ≻ 0

is assumed to be Ht−1-measurable. Thus Σt is predictable: it may depend on the past real optimization history, but not on the current minibatch randomness, the current Gaussian innovation, or future randomness. We define the accumulated virtual perturbation by 

ξt = fWt − Wt. (111) Since ξ1 = 0 and 

ξt+1 = ξt + εt,

the perturbation at time t is generated by the previous virtual innovations. For a positive semidefinite matrix M , we use the notation 

∥x∥2 

> M

= x⊤M x. 

7.1 Conditional Gaussian Relative-Entropy Bounds 

We first record the conditional Gaussian comparison inequalities used throughout the proof. These are standard consequences of the formula for the relative entropy between Gaussian measures and convexity of relative entropy. Throughout this subsection, all conditional distributions are understood as regular conditional distributions, and all random variables are assumed to take values in standard Borel spaces. 

Lemma 1 (Conditional Gaussian smoothing with common covariance) . Let F be a sigma-field. Let X and 

Y be conditionally square-integrable random vectors in Rd. Let 

ε | F ∼ N (0 , Σ) ,

where Σ ≻ 0 is F-measurable, and assume that ε is conditionally independent of (X, Y ) given F. Then 

DKL 

 PX+ε|F PY +ε|F 

 ≤ 12 E ∥X − Y ∥2Σ−1 F . (112) 

Proof. Condition on F. For fixed x, y , the Gaussian relative entropy is 

DKL (N (x, Σ) ∥ N (y, Σ)) = 12 ∥x − y∥2Σ−1 .

The conditional distributions of X + ε and Y + ε are mixtures of these Gaussian measures. Applying joint convexity of relative entropy to any conditional coupling of (X, Y ) gives (112). 

Lemma 2 (Conditional Gaussian comparison with covariance mismatch) . Let F be a sigma-field. Let X be a conditionally square-integrable random vector in Rd. Let 

ε | F ∼ N (0 , Σ) ,

where Σ ≻ 0 is F-measurable, and assume that ε is conditionally independent of X given F. Let m and 

Σref ≻ 0 be F-measurable. Then 

DKL 



PX+ε|F N (m, Σref )



≤ 12 E

h

∥X − m∥2(Σ ref )−1 F

i

+ 12



Tr 



(Σ ref )−1Σ



− d + log det Σ ref 

det Σ 



. (113) 25 Proof. Condition on F. For fixed x,

DKL 



N (x, Σ) N (m, Σref )



= 12 ∥x − m∥2(Σ ref )−1 + 12



Tr 



(Σ ref )−1Σ



− d + log det Σ ref 

det Σ 



.

Since PX+ε|F is a mixture of N (x, Σ) over the conditional law of X, convexity of relative entropy in its first argument yields the claim. 

7.2 Mutual-Information Decomposition 

The information-theoretic generalization bound controls the expected generalization error of a randomized output by its mutual information with the sample. If ℓ(w, Z ) is R-sub-Gaussian in Z for every fixed w, then 

gen( fWT , S ) ≤

r 2R2

n I(fWT ; S). (114) Let U denote optional public, auxiliary, or ghost randomness used by the reference construction. We assume that 

U is independent of S. 

If no such auxiliary randomness is used, U is the trivial sigma-field. For each time t, define the reference-visible sigma-field 

FQt = σ(fW1: t) ∨ U . (115) The reference kernel at time t is allowed to be measurable with respect to FQt , but it is not allowed to condition directly on the sample S. Define also 

At = FQt ∨ σ(S). (116) 

Lemma 3 (Reference-kernel mutual-information decomposition) . Assume W1 is independent of S. For each 

t = 1 , . . . , T − 1, let Qt+1 |F Qt

be any probability kernel measurable with respect to FQt . Then 

I(fWT ; S) ≤ 

> T−1

X

> t=1

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

. (117) 

Proof. Because U is independent of S,

I(fWT ; S) ≤ I(fWT ; S | U ) ≤ I(fW1: T ; S | U ).

By the chain rule for mutual information and the independence of W1 and S,

I(fW1: T ; S | U ) =  

> T−1

X

> t=1

I(fWt+1 ; S | fW1: t, U). (118) For each t,

I(fWt+1 ; S | fW1: t, U) = E

h

DKL 



PfWt+1 |A t PfWt+1 |F Qt

i 

.

26 For any FQt -measurable reference kernel Qt+1 |F Qt

, the conditional KL identity gives 

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

= I(fWt+1 ; S | fW1: t, U) + E

h

DKL 



PfWt+1 |F Qt

Qt+1 |F Qt

i 

≥ I(fWt+1 ; S | fW1: t, U). (119) Summing over t proves the result. 

7.3 From History-Conditioned Comparisons to Virtual-Prefix KL 

The one-step KL term in Lemma 3 is conditioned only on the virtual prefix and the sample: 

PfWt+1 |A t = PfWt+1 |fW1: t,S, U .

However, the Gaussian comparison in Lemma 2 is naturally applied after conditioning on a richer sigma-field containing the real SGD history. This richer conditioning reveals Wt, the predictable covariance Σt, and the accumulated perturbation relation fWt = Wt + ξt.

Define the richer sigma-field 

Bt = At ∨ H t−1. (120) Then 

At ⊆ B t.

Lemma 4 (Conditioning compression for relative entropy) . Let X be a random element, and let 

A ⊆ B 

be sigma-fields. Let Q be an A-measurable probability kernel on the state space of X. Then 

E DKL 

 PX|A Q ≤ E DKL 

 PX|B Q . (121) 

Proof. Let 

μA = PX|A , μB = PX|B .

By the tower property for regular conditional distributions, 

μA = E[μB | A ],

where the equality is understood weakly after testing against bounded measurable functions. Since Q is 

A-measurable, it is fixed after conditioning on A. By convexity of relative entropy in its first argument, 

DKL (μA ∥ Q) ≤ E [DKL (μB ∥ Q) | A ] .

Taking expectations gives (121). 

Corollary 1 (History-to-prefix KL transfer) . For every t, let Qt+1 |F Qt

be an FQt -measurable reference kernel. Then 

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

≤ E

h

DKL 



PfWt+1 |B t Qt+1 |F Qt

i 

. (122) 27 Proof. Apply Lemma 4 with 

X = fWt+1 , A = At, B = Bt, Q = Qt+1 |F Qt

.

Since FQt ⊆ A t, the reference kernel is At-measurable. 

Remark 2. Corollary 1 does not make arbitrary data-dependent reference kernels valid. The reference kernel must still be constructed only from the virtual prefix and admissible sample-independent auxiliary information. The richer sigma-field Bt is used for the analysis, not for giving the reference kernel illegal access to S.

7.4 Conditioning-Safe Adaptive Gradient-Deviation and Sensitivity 

Let 

Σref  

> t

≻ 0

be the reference covariance used by the reference kernel at time t, and define the reference precision 

Λt = (Σ ref  

> t

)−1. (123) When Λt is deterministic or measurable with respect to the real history and admissible reference-side randomness, the adaptive sensitivity can be expressed using an independent fresh Gaussian copy of the accumulated perturbation. For prefix-observable adaptive covariances, however, Λt may depend on the realized virtual prefix fW1: t, and therefore on the realized perturbation ξt. In that case, the fresh-copy notation can be misleading. The conditioning-safe definition uses the realized perturbation. Let 

Kt = Ht−1 ∨ U ,

where U denotes optional public, auxiliary, or ghost randomness independent of the original sample S. If no such randomness is used, U is the trivial sigma-field. Define the reference-geometry gradient-deviation term by 

V ad  

> t

(Λ t) = E

h

∥Gt − ¯g(Wt)∥2Λt Kt

i

. (124) This quantity is called a variance term only by analogy with earlier work. In full generality it is a conditional mean-square deviation from the population gradient, and it becomes a conditional variance only under the corresponding conditional unbiasedness condition. Define the reference-geometry virtual-sensitivity term by 

Γad  

> t

(Λ t) = E

h

∥¯g(Wt + ξt) − ¯g(Wt)∥2Λt Kt

i

. (125) The conditional expectation averages over the virtual perturbation history whenever that history is not already revealed by the conditioning sigma-field. When Λt is Kt-measurable and does not depend on the realized perturbation beyond Kt, one may equivalently replace ξt by an independent fresh conditional Gaussian copy 

ζt | H t−1 ∼ N (0 , Σ1: t), Σ1: t =

> t−1

X

> s=1

Σs.

In this special case, 

Γad  

> t

(Λ t) = E

h

∥¯g(Wt + ζt) − ¯g(Wt)∥2Λt Kt

i

. (126) Thus, (125) is the default general definition, and (126) is a convenient special case. For the synchronized actual geometry, we write 

V ad  

> t

= V ad  

> t

(Σ −1 

> t

), Γad  

> t

= Γ ad  

> t

(Σ −1 

> t

). (127) 28 7.5 Certified Admissible Reference Comparisons 

In the fixed-covariance setting, the reference kernel can use the same perturbation covariance as the actual virtual kernel because the covariance schedule is deterministic and therefore does not encode information about the training sample. In the history-adaptive setting, this point is more delicate. The actual covariance 

Σt is predictable with respect to the real SGD history Ht−1, but it may still be data-dependent through the sample values observed along the trajectory. Thus, predictability is sufficient for the local conditional Gaussian smoothing step, but it is not sufficient by itself to justify a synchronized reference kernel in the mutual-information decomposition. The reference kernel must be constructed from information it is allowed to see. The canonical reference drift is chosen as the population-gradient drift at the current virtual iterate: 

fWt − ηt ¯g(fWt). (128) Given a positive definite reference covariance Σref  

> t

, the canonical Gaussian reference kernel is 

Qt+1 |F Qt

= N

fWt − ηt ¯g(fWt), Σref 

> t



. (129) 

Definition 7.1 (S-admissible reference covariance) . A reference covariance Σref  

> t

is called S-admissible at time t if the following conditions hold: 1. Σref  

> t

≻ 0 almost surely. 2. Σref  

> t

is measurable with respect to 

FQt = σ(fW1: t) ∨ U ,

where U is independent of the original sample S. The auxiliary sigma-field U may include public randomness or an independent ghost construction. 3. The Gaussian innovation used by the reference kernel is independent of the current minibatch ran-domness and of the actual Gaussian innovation conditional on the information used to construct 

Σref  

> t

.The covariance-comparison cost associated with an S-admissible reference covariance is 

Ccov  

> t

= 12 E



Tr 



(Σ ref  

> t

)−1Σt



− d + log det Σ ref 

> t

det Σ t



. (130) This is exactly the Gaussian covariance KL cost of comparing the actual smoothing covariance Σt with the reference smoothing covariance Σref  

> t

.

7.6 Conditioning-Complete Certified One-Step Comparison 

We now prove the one-step reference comparison with the conditioning transition made explicit. 

Proposition 1 (Certified one-step Gaussian reference comparison) . Fix t ∈ { 1, . . . , T − 1}. Suppose that 

Σref  

> t

is S-admissible and that the reference kernel is the canonical Gaussian kernel 

Qt+1 |F Qt

= N

fWt − ηt ¯g(fWt), Σref 

> t



.

Let 

Λt = (Σ ref  

> t

)−1.

Then 

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

≤ 2η2 

> t

E

h

V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t)

i

+ Ccov  

> t

. (131) 

Proof. The proof has three steps. 29 Step 1: Transfer from virtual-prefix conditioning to rich conditioning. By Corollary 1, 

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

≤ E

h

DKL 



PfWt+1 |B t Qt+1 |F Qt

i 

. (132) 

Step 2: Conditional Gaussian comparison under the rich sigma-field. Condition on Bt. Under this conditioning, fWt, Wt, Σt, and Σref  

> t

are fixed. The current minibatch gradient Gt may still be random, but the Gaussian innovation εt is conditionally Gaussian with covariance Σt and is independent of the current minibatch randomness. Let 

Mt = fWt − ηtGt, mref  

> t

= fWt − ηt ¯g(fWt).

By Lemma 2, with an additional application of convexity to average over the conditional distribution of Gt,

DKL 



PfWt+1 |B t Qt+1 |F Qt



≤ η2

> t

2 E



Gt − ¯g(fWt) 2Λt

Bt



+ 12



Tr 



(Σ ref  

> t

)−1Σt



− d + log det Σ ref 

> t

det Σ t



. (133) Taking expectations gives 

E

h

DKL 



PfWt+1 |B t Qt+1 |F Qt

i 

≤ η2

> t

2 E



Gt − ¯g(fWt) 2Λt



+ Ccov  

> t

. (134) 

Step 3: Decompose the gradient discrepancy. Since 

fWt = Wt + ξt,

we have 

¯g(fWt) = ¯ g(Wt + ξt).

Therefore, 

Gt − ¯g(fWt) = ( Gt − ¯g(Wt)) + (¯ g(Wt) − ¯g(Wt + ξt)) . (135) Using 

∥a + b∥2Λt ≤ 2∥a∥2Λt + 2 ∥b∥2Λt ,

we obtain 

E



Gt − ¯g(fWt) 2Λt



≤ 2E

h

∥Gt − ¯g(Wt)∥2Λt

i

+ 2 E

h

∥¯g(Wt + ξt) − ¯g(Wt)∥2Λt

i

= 2 E

h

V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t)

i

, (136) where the last equality follows from (124), (125), and the tower property. 30 Combining (132), (134), and (136) gives the sharper bound 

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

≤ η2 

> t

E

h

V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t)

i

+ Ccov  

> t

.

The stated version with coefficient 2η2 

> t

follows immediately since the gradient-deviation and sensitivity terms are nonnegative. We keep the looser coefficient to match the conventional virtual-perturbation presentation. 

7.7 Concrete Admissibility Certificates 

We now give several concrete covariance families for which admissibility can be verified directly. These certificates replace the earlier black-box assumption that the one-step reference comparison simply holds. 

Proposition 2 (Deterministic covariance schedules are admissible) . Suppose that, for every t,

Σt

is deterministic and positive definite. Choose 

Σref  

> t

= Σ t.

Then Σref  

> t

is S-admissible, 

Ccov  

> t

= 0 , Λt = Σ −1 

> t

,

and the certified one-step comparison (131) holds. 

Proof. A deterministic covariance schedule contains no information about the training sample. Hence the reference kernel may use the same covariance as the actual virtual perturbation. Since Σref  

> t

= Σ t, the Gaussian covariance-mismatch term is zero. Proposition 1 proves the one-step comparison. 

Proposition 3 (Public-predictable covariance schedules are admissible) . Let Ut−1 be public randomness independent of the sample values. Suppose that 

Σt = Ψ t(Ut−1), Σt ≻ 0,

for a measurable map Ψt. Choose 

Σref  

> t

= Σ t.

Then, after conditioning on the public randomness, the synchronized reference kernel is S-admissible, 

Ccov  

> t

= 0 , Λt = Σ −1 

> t

,

and the certified one-step comparison (131) holds. 

Proof. Since Ut−1 is independent of the sample values, conditioning on Ut−1 does not reveal information about S. The reference kernel may therefore use Ψt(Ut−1). Because the actual and reference covariances coincide, the covariance-comparison cost vanishes. The result follows from Proposition 1. 

Proposition 4 (Prefix-observable synchronized covariance) . Suppose that the actual predictable covariance 

Σt admits an FQt -measurable version. Equivalently, suppose there exists a measurable map ψt such that 

Σt = ψt(fW1: t, U) almost surely . (137) 31 Choose 

Σref  

> t

= ψt(fW1: t, U).

Then the synchronized reference covariance is S-admissible, 

Ccov  

> t

= 0 , Λt = Σ −1 

> t

,

and the certified one-step comparison (131) holds. 

Proof. The reference kernel is allowed to condition on FQt = σ(fW1: t)∨U . Hence it can compute ψt(fW1: t, U)

without direct access to S. Since this equals Σt almost surely, the covariance mismatch cost is zero. The claim follows from Proposition 1. 

Proposition 5 (Ghost-adaptive reference covariance) . Let S◦ be an independent ghost sample drawn from the same population distribution as S. Run an independent ghost SGD trajectory 

W ◦ 

> t+1

= W ◦ 

> t

− ηtG◦ 

> t

, G◦ 

> t

= g(W ◦ 

> t

, B ◦ 

> t

),

using randomness independent of the original sample S. Let H◦ 

> t−1

denote the corresponding ghost history. Suppose the actual covariance is generated by a predictable selector 

Σt = Φ t(Ht−1),

and define the reference covariance by 

Σref  

> t

= Φ t(H◦

> t−1

), Σref  

> t

≻ 0.

Then Σref  

> t

is S-admissible. The certified one-step comparison holds with 

Λt = (Σ ref  

> t

)−1

and covariance-comparison cost 

Ccov  

> t, ghost

= 12 E



Tr 



(Σ ref  

> t

)−1Σt



− d + log det Σ ref 

> t

det Σ t



. (138) 

Proof. The ghost trajectory is generated from S◦ and ghost randomness independent of the original sample S.Therefore Σref  

> t

= Φ t(H◦

> t−1

) does not condition on S. It is an admissible reference covariance. The actual and reference covariances need not coincide, so the Gaussian covariance-mismatch cost remains and is exactly 

Ccov 

> t, ghost

. Proposition 1 gives the one-step comparison. 

Remark 3. The ghost-adaptive construction provides a valid reference geometry even when the actual adaptive covariance is not observable from the virtual prefix. The price is the explicit mismatch cost Ccov 

> t, ghost

.If the selector Φt is stable under replacement of the original trajectory by an independent ghost trajectory, this cost can be small. If the selector is highly sample-sensitive, the cost may dominate the bound. 

7.8 Main General Covariance-Comparison Bound 

We can now state the main theorem. The theorem separates three effects: the information accumulated through stochastic gradients, the covariance mismatch between actual and reference smoothing kernels, and the output sensitivity required to transfer the guarantee from the perturbed output fWT back to the original SGD output WT .32 Define the adaptive output-sensitivity functional by 

∆ad Σ1: T (WT , s ) = E [L(WT , s ) − L(WT + ζT , s ) | H T −1] , (139) where 

ζT | H T −1 ∼ N (0 , Σ1: T ), Σ1: T = 

> T−1

X

> t=1

Σt.

The adaptive output-sensitivity penalty is 

Rad ∆ = E

h

∆ad Σ1: T (WT , S ′) − ∆ad Σ1: T (WT , S )

i

. (140) 

Assumption 1 (Basic regularity and certified reference conditions) . The following conditions hold. 1. W1 is independent of S.2. For every fixed w, the random variable ℓ(w, Z ) is R-sub-Gaussian under the population distribution. 3. The loss is differentiable in w, and all gradient-deviation, sensitivity, covariance-comparison, and output-sensitivity quantities appearing below are finite. 4. The actual covariance process is predictable: 

Σt is Ht−1-measurable , Σt ≻ 0 almost surely .

5. The virtual perturbations satisfy 

εt | H t−1 ∼ N (0 , Σt),

and are conditionally independent of the current minibatch randomness and future optimization randomness given the appropriate past history. 6. For each t, a positive definite S-admissible reference covariance Σref  

> t

is chosen, and the reference kernel is the canonical Gaussian kernel 

Qt+1 |F Qt

= N

fWt − ηt ¯g(fWt), Σref 

> t



.

Theorem 1 (General covariance-comparison bound with certified references) . Under Assumption 1, define 

Λt = (Σ ref  

> t

)−1

and 

Ccov  

> t

= 12 E



Tr 



(Σ ref  

> t

)−1Σt



− d + log det Σ ref 

> t

det Σ t



.

Then the expected generalization error of the original SGD output WT satisfies 

|gen( WT , S )| ≤ 

vuut 2R2

n 

> T−1

X

> t=1

 2η2 

> t

E V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t) + Ccov 

> t

 + Rad ∆ . (141) 33 Proof. Let 

ζT | H T −1 ∼ N (0 , Σ1: T )

be an independent fresh final perturbation, and define 

cWT = WT + ζT .

By adding and subtracting losses evaluated at cWT ,

gen( WT , S ) = E L(WT , S ′) − L(WT , S )

= E

h

L(cWT , S ′) − L(cWT , S )

i

+ E

h

∆ad Σ1: T (WT , S ′) − ∆ad Σ1: T (WT , S )

i

. (142) Therefore, 

|gen( WT , S )| ≤ | gen( cWT , S )| + Rad ∆ . (143) Since ζT | H T −1 has the same conditional law as ξT | H T −1, the random variables 

cWT = WT + ζT and fWT = WT + ξT

have the same joint law with S. Hence 

I(cWT ; S) = I(fWT ; S).

The standard information-theoretic generalization inequality gives 

|gen( cWT , S )| ≤ 

r 2R2

n I(cWT ; S) = 

r 2R2

n I(fWT ; S).

By Lemma 3, 

I(fWT ; S) ≤ 

> T−1

X

> t=1

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

.

For each t, Proposition 1 bounds the corresponding one-step KL term by 

2η2 

> t

E

h

V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t)

i

+ Ccov  

> t

.

Substituting into the mutual-information bound and then into the sub-Gaussian generalization inequality proves (141). 

Remark 4. The theorem separates two roles that are often conflated. Predictability of Σt is the causal condi-tion needed to generate the actual virtual Gaussian perturbation. S-admissibility of Σref  

> t

is the information-theoretic condition needed to construct a valid reference kernel. When the two covariances coincide admissi-bly, the covariance-comparison cost is zero. When they differ, Ccov  

> t

is the explicit KL price of using a valid reference geometry. 34 7.9 Readable Corollaries 

The general theorem is stated in terms of the reference precision 

Λt = (Σ ref  

> t

)−1.

This is the correct level of generality for history-adaptive virtual perturbations, because the actual covariance 

Σt and the covariance available to the reference kernel need not coincide. The next corollary gives a more readable form when the reference precision is comparable to the actual precision. 

Corollary 2 (Comparable covariance geometries) . Suppose the conditions of Theorem 1 hold. Assume additionally that there exists κ ≥ 1 such that, for all t = 1 , . . . , T − 1,

(Σ ref  

> t

)−1 ⪯ κ Σ−1 

> t

almost surely . (144) Then 

|gen( WT , S )| ≤ 

vuut 2R2

n

"

2κ 

> T−1

X

> t=1

η2 

> t

E V ad  

> t

+ Γ ad 

> t

 + 

> T−1

X

> t=1

Ccov 

> t

#

+ Rad ∆ . (145) 

Proof. By (144), for every vector x,

∥x∥2Λt = x⊤(Σ ref  

> t

)−1x ≤ κx ⊤Σ−1 

> t

x = κ∥x∥2Σ−1

> t

.

Therefore, 

V ad  

> t

(Λ t) ≤ κV ad  

> t

, Γad  

> t

(Λ t) ≤ κΓad  

> t

.

Substituting these inequalities into Theorem 1 proves the claim. The term V ad  

> t

in Corollary 2 should be interpreted as a conditional mean-square gradient-deviation term. It becomes a genuine conditional variance term only under the conditional unbiasedness condition 

E[Gt | H t−1] = ¯ g(Wt).

Without this condition, V ad  

> t

also contains the squared conditional bias between the conditional mean gradient and the population gradient. 

7.9.1 Admissible Synchronization 

The clean fixed-noise-style form is recovered only when the reference kernel is allowed to use the same covariance as the actual virtual perturbation. This requires more than the algebraic identity 

Σref  

> t

= Σ t.

The reference kernel in the mutual-information decomposition cannot condition directly on the sample S.Hence the actual covariance must be reconstructible from information available to the reference comparison. Recall the reference-visible sigma-field 

FQt = σ(fW1: t) ∨ U ,

where U denotes optional public, auxiliary, or ghost randomness independent of the original training sample 

S.35 Definition 7.2 (Admissible synchronization certificate) . We say that the covariance process admits an S-admissible synchronized reference at time t if there exists an FQt -measurable positive definite random matrix 

Σsync  

> t

such that 

Σsync  

> t

= Σ t almost surely . (146) Equivalently, the actual covariance Σt has a version that can be computed from the virtual prefix fW1: t and admissible sample-independent auxiliary information, without direct access to S.

Proposition 6 (Synchronization certificate implies zero covariance cost) . Suppose Definition 7.2 holds at time t. Choose 

Σref  

> t

= Σ sync  

> t

.

Then Σref  

> t

is S-admissible, 

Σref  

> t

= Σ t almost surely ,

and 

Ccov  

> t

= 0 , Λt = Σ −1 

> t

. (147) 

Proof. By Definition 7.2, Σsync  

> t

is measurable with respect to the information available to the reference kernel. Therefore it can be used without conditioning directly on S. Since Σsync  

> t

= Σ t almost surely, the actual and reference covariances coincide. Substituting Σref  

> t

= Σ t into the covariance-comparison cost gives 

Ccov  

> t

= 12 E



Tr(Σ −1 

> t

Σt) − d + log det Σ t

det Σ t



= 0 .

The identity Λt = Σ −1 

> t

follows immediately. 

Remark 5 (Predictability is not synchronization) . Predictability means 

Σt is Ht−1-measurable .

This is the causal condition needed to generate the actual virtual Gaussian perturbation. Admissible synchro-nization is different: it asks whether the reference kernel can reconstruct the same covariance from 

FQt = σ(fW1: t) ∨ U .

Therefore, a covariance of the form 

Σt = Φ t(Ht−1)

may be predictable but not admissibly synchronized if Ht−1 contains sample-value information that is not recoverable from the virtual prefix or from admissible auxiliary randomness. 

Corollary 3 (Admissibly synchronized covariance bound) . Suppose the conditions of Theorem 1 hold. Assume additionally that, for every t = 1 , . . . , T − 1, the covariance process admits an S-admissible synchronized reference in the sense of Definition 7.2. Then 

Ccov  

> t

= 0 , Λt = Σ −1 

> t

,

and the expected generalization error satisfies 

|gen( WT , S )| ≤ 

vuut 4R2

n 

> T−1

X

> t=1

η2 

> t

E V ad  

> t

+ Γ ad 

> t

 + Rad ∆ . (148) 36 Proof. By Proposition 6, admissible synchronization gives 

Ccov  

> t

= 0 , Λt = Σ −1 

> t

.

Substituting these identities into Theorem 1 gives (148). 

Remark 6 (When the synchronized corollary is automatic) . The admissibly synchronized corollary is automatic in the following cases. 1. Deterministic covariance schedules. If Σt is deterministic, then it is available to both the actual virtual kernel and the reference kernel. 2. Public-random covariance schedules. If 

Σt = Ψ t(Ut−1),

where Ut−1 is independent of the sample values, then synchronization is admissible after conditioning on Ut−1.3. Prefix-observable adaptive covariance. If there exists a measurable map ψt such that 

Σt = ψt(fW1: t, U) almost surely ,

then the reference kernel can recover the actual covariance from the virtual prefix and admissible auxiliary information. 4. Admissible public or ghost augmentation. If the covariance can be reconstructed from fW1: t and auxiliary randomness independent of S, then synchronization is admissible after conditioning on that auxiliary randomness. Outside these cases, one should use the general covariance-comparison theorem rather than the clean synchronized corollary. 

Remark 7 (Interpretation of the readable bounds) . The comparable and admissibly synchronized corollaries have the same algebraic form as fixed-noise virtual perturbation bounds, but their interpretation is more subtle. The term V ad  

> t

is a gradient-deviation term: 

V ad  

> t

= E

h

∥Gt − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

.

It reduces to a conditional variance only when 

E[Gt | H t−1] = ¯ g(Wt).

The term Γad  

> t

measures sensitivity of the population gradient to accumulated virtual perturbations. The penalty Rad ∆ transfers the guarantee from the perturbed output back to the original SGD output. Finally, the synchronized bound is valid only under an admissible synchronization certificate; predictability alone is not enough. 37 8 Special Cases and Recovering Existing Bounds 

The main theorem is stated in a general covariance-comparison form because, for history-adaptive perturba-tions, the actual virtual covariance and the reference covariance used in the mutual-information comparison need not coincide. This section shows how the theorem specializes to familiar fixed-noise settings and to several interpretable adaptive virtual geometries. The key distinction is the following. When the covariance sequence is deterministic, synchronized reference comparisons are automatically admissible, and the clean fixed-noise-style corollary applies directly. When the covariance is history-adaptive and hence generally data-dependent, the general covariance-comparison theorem should be used unless an admissible synchronized comparison is explicitly available. Thus, adaptive examples below should be interpreted as choices of virtual perturbation geometry, not as modifications of the SGD optimizer. Throughout this section, the true SGD update remains unchanged: 

Wt+1 = Wt − ηtGt, Gt = g(Wt, B t), t = 1 , . . . , T − 1. (149) Only the analytical perturbation covariance Σt is varied. 

8.1 Fixed Isotropic Perturbations 

The simplest special case is deterministic isotropic virtual noise. Let 

Σt = σ2 

> t

I, σt > 0, t = 1 , . . . , T − 1, (150) where the sequence {σt}T −1 

> t=1

is fixed before observing the SGD trajectory. Since Σt is deterministic, it is automatically Ht−1-measurable. Moreover, the same covariance can be used in the actual and reference one-step comparisons without depending on S, so the covariance-comparison term vanishes: 

Ccov  

> t

= 0 . (151) The accumulated covariance is deterministic: 

Σ1: t =

> t−1

X

> k=1

Σk = σ21: tI, σ21: t =

> t−1

X

> k=1

σ2

> k

. (152) Consequently, 

∥x∥2Σ−1

> t

= σ−2 

> t

∥x∥2 . (153) Define the Euclidean history-conditioned gradient-deviation numerator 

V Euc  

> t

= E

h

∥Gt − ¯g(Wt)∥2 | H t−1

i

. (154) This is a variance numerator only under the conditional unbiasedness condition 

E[Gt | H t−1] = ¯ g(Wt).

Define the Euclidean sensitivity numerator 

ΓEuc  

> t

= E

h

∥¯g(Wt + ζt) − ¯g(Wt)∥2 | H t−1

i

, ζt | H t−1 ∼ N (0 , σ 21: tI). (155) Then 

V ad  

> t

= 1

σ2

> t

V Euc  

> t

, Γad  

> t

= 1

σ2

> t

ΓEuc  

> t

. (156) 38 Substituting these quantities into the admissibly synchronized covariance corollary yields 

|gen( WT , S )| ≤ 

vuut 4R2

n 

> T−1

X

> t=1

η2

> t

σ2

> t

E V Euc  

> t

+ Γ Euc 

> t

 + R∆,σ . (157) Here R∆,σ is the output-sensitivity penalty corresponding to the deterministic accumulated covariance σ21: T I.Under the usual fixed-noise notation, (157) is the scalar isotropic virtual perturbation form. Thus, the classical fixed isotropic setting is recovered as a special case of the predictable covariance framework. 

8.2 Fixed Geometry-Aware Covariances 

A more general fixed setting allows deterministic anisotropic covariance matrices. Let 

Σt ≻ 0, t = 1 , . . . , T − 1, (158) be deterministic but not necessarily proportional to the identity. Again, each Σt is automatically predictable, and a synchronized reference comparison is admissible. Therefore, 

Ccov  

> t

= 0 , Λt = Σ −1 

> t

. (159) The accumulated covariance is deterministic: 

Σ1: t =

> t−1

X

> k=1

Σk. (160) Define the fixed geometry-aware gradient-deviation and sensitivity terms by 

V hist  

> t, Σt

= E

h

∥Gt − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

, (161) and 

Γhist Σt,Σ1: t = E

h

∥¯g(Wt + ζt) − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

, ζt | H t−1 ∼ N (0 , Σ1: t). (162) The admissibly synchronized covariance corollary gives 

|gen( WT , S )| ≤ 

vuut 4R2

n 

> T−1

X

> t=1

η2 

> t

E

h

V hist  

> t, Σt

+ Γ hist Σt,Σ1: t

i

+ R∆. (163) This is the fixed geometry-aware virtual perturbation bound in history-conditioned notation. Some fixed-noise presentations write the corresponding quantities as functions of Wt alone. That reduction is valid after taking expectations under an additional condition ensuring that the conditional law of the current stochastic gradient given the past depends on the past only through Wt. In more general sampling settings, the history-conditioned notation is the more precise analogue. Compared with isotropic perturbations, deterministic matrix-valued covariances can encode a fixed anisotropic geometry of the parameter space. However, because the covariance is chosen before observing the training trajectory, it still cannot adapt to realized gradient scales, curvature proxies, or time-varying stochasticity. 39 8.3 Adaptive Scalar Perturbations 

The first genuinely history-adaptive case is scalar but time- and path-dependent noise: 

Σt = σ2 

> t

(Ht−1)I, σt(Ht−1) > 0, t = 1 , . . . , T − 1. (164) Here σt may depend on any statistic of the past SGD trajectory, such as moving averages of gradient norms, subbatch fluctuation proxies, gradient-deviation diagnostics, or training-stage indicators. Because σt is 

Ht−1-measurable, the covariance is predictable. The accumulated covariance is 

Σ1: t = σ21: tI, σ21: t =

> t−1

X

> k=1

σ2

> k

(Hk−1). (165) Thus, 

ζt | H t−1 ∼ N (0 , σ 21: tI). (166) Under an admissibly synchronized comparison, the adaptive scalar gradient-deviation and sensitivity terms are 

V ad  

> t

=

E

h

∥Gt − ¯g(Wt)∥2 | H t−1

i

σ2 

> t

(Ht−1) , (167) and 

Γad  

> t

=

E

h

∥¯g(Wt + ζt) − ¯g(Wt)∥2 | H t−1

i

σ2 

> t

(Ht−1) . (168) The term in (167) is a variance only under conditional unbiasedness of Gt for ¯g(Wt). Because σt(Ht−1) is generally data-dependent, the synchronized clean bound is not automatic. The general covariance-comparison theorem applies with a reference scalar covariance 

Σref  

> t

= ( σref  

> t

)2I, (169) which yields 

Λt = (Σ ref  

> t

)−1 = 1(σref  

> t

)2 I. (170) The covariance-comparison cost becomes 

Ccov  

> t

= d

2 E

 σ2

> t

(σref  

> t

)2 − 1 + log (σref  

> t

)2

σ2

> t



. (171) If an admissible synchronized comparison is available, then σref  

> t

= σt, the covariance cost vanishes, and the clean bound uses (167) and (168) . Otherwise, the reference-geometry quantities and the cost (171) must be included. This example makes the basic trade-off explicit. Larger σ2 

> t

reduces inverse-covariance weighted gradient-deviation and sensitivity terms, but it also increases the accumulated covariance and can enlarge the final output-sensitivity penalty. 

8.4 Adaptive Diagonal Covariances 

A richer adaptive family is obtained by choosing coordinate-wise perturbation scales: 

Σt = diag  s2

> t, 1

, . . . , s 2

> t,d

 , st,j > 0, (172) 40 where each st,j is Ht−1-measurable. For any vector x ∈ Rd,

∥x∥2Σ−1

> t

=

> d

X

> j=1

x2

> j

s2

> t,j

. (173) Consequently, under an admissibly synchronized comparison, the adaptive gradient-deviation and sensitivity terms take the coordinate-wise forms 

V ad  

> t

= E



> d

X

> j=1

(Gt,j − ¯gj (Wt)) 2

s2

> t,j

| H t−1

 , (174) and 

Γad  

> t

= E



> d

X

> j=1

(¯ gj (Wt + ζt) − ¯gj (Wt)) 2

s2

> t,j

| H t−1

 , (175) where 

ζt | H t−1 ∼ N (0 , Σ1: t).

The term in (174) is a coordinate-wise variance only under conditional unbiasedness. If the reference covariance is diagonal with entries (sref  

> t,j

)2, then the covariance-comparison cost is 

Ccov  

> t

= 12 E



> d

X

> j=1

s2

> t,j

(sref  

> t,j

)2 − 1 + log (sref  

> t,j

)2

s2

> t,j

! . (176) This expression is the coordinate-wise KL cost of comparing Gaussian smoothing kernels with different diagonal covariances. Diagonal adaptive perturbations allow the analytical geometry to respond to coordinate-wise scale information. Directions assigned larger virtual variance contribute less to the information term, but may contribute more to the accumulated output-sensitivity penalty. Thus, the coordinate-wise covariance exposes an information-sensitivity trade-off rather than a uniformly better choice. 

8.5 Adam-Like Virtual Geometry 

The predictable covariance framework can express virtual perturbation geometries inspired by adaptive optimizers. This does not mean that the SGD update is replaced by Adam. Rather, Adam-like moving statistics are used only to define the covariance of the analytical perturbations. For example, define a past-gradient second-moment proxy by 

vt = βv t−1 + (1 − β)Gt−1 ⊙ Gt−1, 0 < β < 1, (177) with an initialization v1 independent of the current update. Because vt is computed from gradients up to time 

t − 1, it is Ht−1-measurable. Therefore, any positive definite diagonal covariance constructed from vt is predictable. One possible inverse-scale virtual covariance is 

Σinv  

> t

= diag 

 ρ2

> t

√vt + ϵ



+ λ0I, (178) where ρt > 0 is predictable, ϵ > 0 stabilizes the denominator, and λ0 > 0 ensures positive definiteness. This choice assigns smaller virtual perturbation variance to coordinates with larger historical second moment. 41 Table 1: Representative virtual covariance families. These choices affect only the analytical perturbation process, not the SGD update. 

Choice of Σt Predictability Reference comparison 

σ2 

> t

I, deterministic automatic admissibly synchronized deterministic Σt ≻ 0 automatic admissibly synchronized 

σ2 

> t

(Ht−1)I history-adaptive requires certificate or reference covariance 

diag( s2

> t, 1

, . . . , s 2

> t,d

) history-adaptive requires certificate or reference covariance Adam-like diagonal covariance history-adaptive requires certificate or reference covariance Alternatively, one may choose a proportional-scale virtual covariance 

Σprop  

> t

= diag  ρ2 

> t

(√vt + ϵ) + λ0I. (179) This choice assigns larger virtual smoothing to coordinates with larger historical gradient scale. It may reduce the inverse-covariance weighted information contribution of discrepancies in high-fluctuation or high-gradient-deviation directions, while potentially increasing the output-sensitivity penalty along those same directions. Both (178) and (179) are valid choices of actual virtual covariance whenever the defining statistics are predictable and the covariance is positive definite. For the generalization bound, one must still specify the corresponding admissible reference comparison. If the reference covariance matches the actual covariance in an admissible synchronized comparison, the clean corollary applies. Otherwise, the general covariance-comparison theorem applies and includes the appropriate covariance-comparison term. Most importantly, neither (178) nor (179) changes the true SGD recursion: 

Wt+1 = Wt − ηtGt. (180) The Adam-like quantities appear only in the virtual perturbation geometry used for analysis. 

8.6 Summary 

The examples above show that the predictable perturbation theorem unifies several regimes. Fixed deter-ministic covariances recover existing fixed-noise bounds through the admissibly synchronized covariance corollary. In particular, the choice Σt = σ2 

> t

I corresponds to fixed isotropic virtual noise, while a deterministic positive definite covariance Σt ≻ 0 represents a fixed geometry-aware virtual noise model. History-adaptive covariances provide richer analytical geometries, but because they are generally data-dependent, the gen-eral covariance-comparison theorem is the correct default unless synchronized reference comparisons are admissible. For example, σ2 

> t

(Ht−1)I gives an adaptive scalar virtual-noise model, diagonal choices such as 

diag( s2

> t, 1

, . . . , s 2

> t,d

) encode coordinate-wise adaptive geometry, and Adam-like diagonal covariances represent past-gradient virtual geometry. The gain of the framework is analytical flexibility: the proof can use virtual perturbations whose covari-ance reflects past optimization information. This flexibility does not imply that every adaptive covariance yields a tighter bound. Each choice induces a different balance between inverse-covariance information control, covariance-comparison cost, and accumulated output sensitivity. 

# 9 Controlling the Adaptive Sensitivity Penalty 

The main covariance-comparison bound contains two conceptually distinct components. The first is the information term, which is controlled through one-step relative-entropy comparisons and may include the 42 covariance-comparison cost 

Ccov  

> t

.

The second is the adaptive output-sensitivity penalty 

Rad ∆ ,

which accounts for transferring the generalization guarantee from the virtually perturbed output back to the original SGD output WT . This section studies only the latter term. The key point is that the virtual perturbation used in the information-theoretic argument should not move the final output too much in loss. If the loss landscape around WT is insensitive to perturbations drawn from the accumulated adaptive covariance, then the output-sensitivity penalty is small. We first give a global smoothness control, which is safe but often pessimistic, and then give a local curvature refinement that makes the geometry of the accumulated covariance explicit. 

9.1 General Form of the Penalty 

Recall that the accumulated adaptive covariance at the final output is 

Σ1: T = 

> T−1

X

> t=1

Σt, (181) where each Σt is predictable with respect to the real SGD history. The final real history is HT −1, since WT is obtained after T − 1 SGD updates. Let 

ζT | H T −1 ∼ N (0 , Σ1: T ) (182) be an independent final perturbation drawn conditionally on the accumulated adaptive covariance. For a deterministic sample s, define the adaptive output sensitivity by 

∆ad Σ1: T (WT , s ) = E [L(WT , s ) − L(WT + ζT , s ) | H T −1] . (183) The final penalty appearing in the main generalization bound is 

Rad ∆ = E

h

∆ad Σ1: T (WT , S ′) − ∆ad Σ1: T (WT , S )

i

, (184) where S′ is an independent ghost sample drawn from the same population distribution as S.The role of Rad ∆ is to quantify the cost of replacing the original output WT by a locally perturbed output 

WT + ζT . If both the training and ghost losses are insensitive to perturbations drawn from N (0 , Σ1: T ),then this penalty is small. Unlike in fixed-noise analysis, the covariance Σ1: T is generally random and history-dependent. Thus, the penalty depends not only on the local loss landscape around WT , but also on how the adaptive covariance process allocates perturbation mass over the trajectory. This section controls only the output-sensitivity penalty. The covariance-comparison cost in the information term is a separate effect. 

9.2 Control under Global Smoothness 

We first give a simple upper bound under a uniform smoothness assumption. 

Assumption 2 (Uniform smoothness) . For every finite sample s, the empirical risk L(·, s ) is μ-smooth. Equivalently, for all w, u ∈ Rd,

|L(w + u, s ) − L(w, s ) − ⟨∇ L(w, s ), u ⟩| ≤ μ

2 ∥u∥2 . (185) 43 Proposition 7 (Global smoothness control of output sensitivity) . Under Assumption 2, for every deterministic sample s,

∆ad Σ1: T (WT , s ) ≤ μ

2 Tr(Σ 1: T ) almost surely . (186) Consequently, 

Rad ∆ ≤ μ E [Tr(Σ 1: T )] . (187) 

Proof. Condition on HT −1. By Assumption 2, with w = WT and u = ζT ,

|L(WT + ζT , s ) − L(WT , s ) − ⟨∇ L(WT , s ), ζ T ⟩| ≤ μ

2 ∥ζT ∥2 .

Taking conditional expectation and using 

E[ζT | H T −1] = 0 ,

we obtain 

|E[L(WT , s ) − L(WT + ζT , s ) | H T −1]| ≤ μ

2 E[∥ζT ∥2 | H T −1].

Since 

ζT | H T −1 ∼ N (0 , Σ1: T ),

we have 

E[∥ζT ∥2 | H T −1] = Tr(Σ 1: T ),

which proves (186). Applying the triangle inequality to (184) gives 

Rad ∆ ≤ E

h

∆ad Σ1: T (WT , S ′) + ∆ad Σ1: T (WT , S )

i

≤ μE[Tr(Σ 1: T )] .

The global smoothness bound is robust and assumption-light, but it is generally pessimistic in high-dimensional nonconvex problems. It treats every direction as if it had curvature μ, ignoring the anisotropic geometry of the loss around the final iterate. The next subsection gives a sharper local curvature refinement. 

9.3 Local Curvature Refinement 

The global bound depends only on Tr(Σ 1: T ). A more informative description depends on how the accumulated covariance aligns with the local Hessian of the empirical or ghost loss. For a finite sample s, define the local Hessian at the final output by 

Hs(WT ) = ∇2L(WT , s ). (188) 

Assumption 3 (Hessian-Lipschitz remainder control) . For a given sample s, assume that L(·, s ) is twice differentiable in a neighborhood of WT . Assume further that there exists a constant ρs ≥ 0, possibly depending on s and WT , such that for the perturbations considered below the Taylor remainder satisfies 

L(WT + u, s ) − L(WT , s ) − ⟨∇ L(WT , s ), u ⟩ − 12 u⊤Hs(WT )u ≤ ρs

6 ∥u∥3 . (189) A sufficient condition is that the Hessian is globally ρs-Lipschitz in operator norm. If the Hessian-Lipschitz condition is only local, then (189) should be understood on the event where the Gaussian perturbation remains in the local neighborhood, with the complement controlled separately. 44 Proposition 8 (Second-order expansion of adaptive output sensitivity) . Suppose Assumption 3 holds for sample s. Then 

∆ad Σ1: T (WT , s ) = − 12 Tr ( Hs(WT )Σ 1: T ) + Rs(WT , Σ1: T ), (190) where the Taylor remainder satisfies 

|Rs(WT , Σ1: T )| ≤ ρs

6 E

h

∥ζT ∥3 | H T −1

i

. (191) 

Proof. Condition on HT −1. Applying Assumption 3 with u = ζT , we have 

L(WT + ζT , s ) = L(WT , s ) + ⟨∇ L(WT , s ), ζ T ⟩ + 12 ζ⊤ 

> T

Hs(WT )ζT + rs(ζT ), (192) where 

|rs(ζT )| ≤ ρs

6 ∥ζT ∥3 .

Taking conditional expectations and using 

E[ζT | H T −1] = 0 

and 

E[ζ⊤ 

> T

Hs(WT )ζT | H T −1] = Tr( Hs(WT )Σ 1: T ),

we obtain 

E[L(WT + ζT , s ) − L(WT , s ) | H T −1] = 12 Tr( Hs(WT )Σ 1: T ) + E[rs(ζT ) | H T −1].

Using 

∆ad Σ1: T (WT , s ) = E[L(WT , s ) − L(WT + ζT , s ) | H T −1],

we obtain (190) with 

Rs(WT , Σ1: T ) = −E[rs(ζT ) | H T −1].

The bound (191) follows immediately. 

Lemma 5 (Gaussian third-moment control) . If 

ζ ∼ N (0 , Σ) ,

then 

E ∥ζ∥3 ≤ √3 [Tr(Σ)] 3/2 . (193) More generally, one may use sharper dimension- or spectrum-sensitive Gaussian moment bounds depending on the eigenvalues of Σ.

Proof. By Cauchy–Schwarz, 

E ∥ζ∥3 = E

h

∥ζ∥2 ∥ζ∥

i

≤



E ∥ζ∥41/2 

E ∥ζ∥21/2

.

For ζ ∼ N (0 , Σ) ,

E ∥ζ∥2 = Tr(Σ) ,

and 

E ∥ζ∥4 = 2 Tr(Σ 2) + [Tr(Σ)] 2 ≤ 3[Tr(Σ)] 2.

Therefore, 

E ∥ζ∥3 ≤ √3 [Tr(Σ)] 3/2.

45 Combining Proposition 8 and Lemma 5 gives the explicit remainder control 

|Rs(WT , Σ1: T )| ≤ √3ρs

6 [Tr(Σ 1: T )] 3/2 . (194) 

9.4 Curvature-Mismatch Control of Rad ∆

The local expansion reveals that the output-sensitivity penalty depends on the difference between the curvature of the ghost loss and the curvature of the training loss, weighted by the accumulated adaptive covariance. 

Proposition 9 (Local curvature control of the adaptive output penalty) . Suppose the local expansion (190) holds for both S and S′. Then 

Rad ∆ ≤ 12 |E [Tr ([ HS′ (WT ) − HS (WT )] Σ 1: T )] |

+ E [|RS′ (WT , Σ1: T )| + |RS (WT , Σ1: T )|] . (195) In particular, if the local remainder constants for S and S′ are ρS and ρS′ , respectively, then 

Rad ∆ ≤ 12 |E [Tr ([ HS′ (WT ) − HS (WT )] Σ 1: T )] |

+

√36 E

h

(ρS + ρS′ ) [Tr(Σ 1: T )] 3/2i

. (196) 

Proof. Using Proposition 8, 

∆ad Σ1: T (WT , S ′) = − 12 Tr( HS′ (WT )Σ 1: T ) + RS′ (WT , Σ1: T ),

and 

∆ad Σ1: T (WT , S ) = − 12 Tr( HS (WT )Σ 1: T ) + RS (WT , Σ1: T ).

Subtracting gives 

∆ad Σ1: T (WT , S ′) − ∆ad Σ1: T (WT , S )= − 12 Tr ([ HS′ (WT ) − HS (WT )]Σ 1: T ) + RS′ (WT , Σ1: T ) − RS (WT , Σ1: T ).

Taking expectation, absolute values, and applying the triangle inequality proves (195) . The trace-form remainder bound follows from (194). This proposition gives a sharper interpretation than the global smoothness bound. The global bound depends only on the total perturbation mass Tr(Σ 1: T ). The local bound depends on where this mass is placed relative to the curvature mismatch 

HS′ (WT ) − HS (WT ).

If the accumulated covariance places perturbation mass mostly in directions where the training and ghost losses have similar and small curvature, then the output-sensitivity penalty can be small even when Tr(Σ 1: T )

is not tiny. 46 9.5 Positive-Curvature and Flatness Interpretation 

Near a well-trained solution, local Hessians are often interpreted as measuring sharpness or flatness. The local expansion formalizes this connection. 

Corollary 4 (Covariance-weighted local flatness) . Suppose the Taylor remainders in (190) are negligible and the local Hessian Hs(WT ) is positive semidefinite. Then, under the sign convention used in ∆ad Σ1: T ,

∆ad Σ1: T (WT , s ) ≈ − 12 Tr ( Hs(WT )Σ 1: T ) , (197) and therefore 

∆ad Σ1: T (WT , s ) ≈ 12 Tr ( Hs(WT )Σ 1: T ) . (198) Thus, output sensitivity is controlled by a covariance-weighted local curvature rather than by the isotropic trace Tr(Σ 1: T ) alone. This expression clarifies the role of anisotropic adaptive perturbations. Perturbation mass in low-curvature directions contributes little to output sensitivity, while perturbation mass aligned with high-curvature directions can increase the penalty. Therefore, a useful adaptive virtual covariance should not merely be large or small globally; it should allocate perturbation mass in directions that improve information control without producing excessive curvature-weighted output sensitivity. 

9.6 Information–Sensitivity Trade-Off 

The main theorem exposes a trade-off between the information term and the output-sensitivity penalty. Increasing actual or reference covariance in appropriate directions can reduce inverse-covariance weighted information contributions such as 

V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t), Λt = (Σ ref  

> t

)−1.

From the perspective of mutual information, larger virtual smoothing can make conditional transition kernels harder to distinguish. At the same time, increasing the actual adaptive covariance increases the accumulated covariance 

Σ1: T = 

> T−1

X

> t=1

Σt,

which can increase the output-sensitivity penalty. Under global smoothness this is captured by 

Rad ∆ ≤ μE[Tr(Σ 1: T )] .

Under the local curvature refinement, the leading term behaves instead like 

12 |E [Tr ([ HS′ (WT ) − HS (WT )]Σ 1: T )] |

up to higher-order Taylor remainders. Thus, the relevant question is not only how large Σ1: T is, but how it aligns with local curvature and curvature mismatch. Schematically, the adaptive perturbation bound balances information term ∼ 

> T−1

X

> t=1

η2 

> t

E

h

∥·∥ 2Λt

i

, output sensitivity ∼ E [Tr( HT Σ1: T )] , (199) 47 where HT denotes a local Hessian or curvature-mismatch proxy near the final iterate. Larger covariance can improve smoothing for information control, but may worsen perturbation robustness at the output. Smaller covariance can reduce the final sensitivity penalty, but may make the information term larger. The purpose of the history-adaptive framework is not to claim that every adaptive covariance improves the bound. Rather, it provides a language for evaluating covariance choices that respond to the optimization path. A useful adaptive covariance should reduce the information term in directions with large gradient deviation, stochastic fluctuation, or gradient sensitivity, while avoiding excessive accumulated perturbation in high-curvature directions near the final iterate. This is the central information–sensitivity trade-off exposed by predictable virtual perturbations. 

Remark 8 (Scope of this section) . The results in this section control only Rad ∆ , the output-sensitivity penalty. They do not control the covariance-comparison cost Ccov  

> t

, which belongs to the information term and arises when the actual and reference virtual perturbation covariances differ. 

# 10 Discussion: Long-Term Dependence and Predictability 

The central conceptual point of this paper is that virtual perturbation analysis need not restrict the perturbation geometry to be fixed independently of the optimization history. A covariance chosen at update t may depend on the past SGD trajectory, provided that this dependence is causal. In our framework, this causal condition is predictability with respect to the real SGD history. At the same time, predictability should not be confused with a complete solution to the mutual-information comparison. Predictability makes the local conditional Gaussian smoothing step valid; the full information-theoretic bound also requires a valid reference-kernel comparison, and in the history-adaptive case this may introduce an explicit covariance-comparison cost. This section clarifies the role of long-term dependence, the precise meaning of predictability, the limitations of the present analysis, and the directions opened by the framework. 

10.1 Long-Term Dependence in Virtual Perturbation Geometry 

Fixed virtual perturbation analysis avoids long-term dependence by choosing the perturbation covariance sequence before the optimization trajectory is observed. For example, one may set 

Σt = σ2 

> t

I, t = 1 , . . . , T − 1, (200) or choose a deterministic matrix-valued sequence 

Σ1, . . . , ΣT −1. (201) This makes the perturbation process technically convenient because the noise law is independent of the realized SGD path and of the training sample values. However, this independence is restrictive if the purpose of the perturbation is to probe the geometry encountered along training. In realistic stochastic optimization dynamics, many useful scale and geometry estimates are history-dependent. Examples include moving averages of gradient norms, coordinate-wise second-moment estimates, gradient-deviation diagnostics, subbatch fluctuation proxies, curvature surrogates, and preconditioner-like quantities. A natural adaptive virtual perturbation covariance may therefore take the form 

Σt = Φ t(G1, . . . , G t−1), (202) or more generally 

Σt = Φ t(Ht−1), (203) 48 where Ht−1 denotes the real SGD history available before update t.Equations (202) and (203) explicitly introduce long-term dependence into the virtual perturbation process. The covariance at time t may depend on information accumulated over many previous iterations. Thus, the virtual perturbation geometry can reflect the pathwise behavior of SGD rather than a fixed geometry chosen before training. Importantly, this does not change the actual SGD update: 

Wt+1 = Wt − ηtGt, t = 1 , . . . , T − 1. (204) The long-term dependence considered here belongs to the analytical perturbation process, not to the optimizer itself. The true SGD trajectory remains unchanged; only the virtual noise used to analyze it is allowed to adapt to the past. 

10.2 Predictability and Reference Comparisons 

The key local observation is that full independence from the optimization history is stronger than necessary for the conditional Gaussian smoothing step. The conditional relative-entropy argument does not require Σt

to be deterministic. It requires that Σt be known before the current virtual perturbation is sampled. Formally, the predictability condition is 

Σt is Ht−1-measurable . (205) Under this condition, 

εt | H t−1 ∼ N (0 , Σt). (206) Once we condition on Ht−1, the covariance Σt is fixed, even though it may be random before conditioning. This is the structure needed to apply conditional Gaussian smoothing and conditional relative-entropy bounds at a single step. Predictability separates allowed dependence from forbidden dependence. Allowed dependence includes functions of past iterates, past minibatch indices, past minibatches, past gradients, and statistics computed from the past trajectory. Forbidden dependence includes the current virtual perturbation, current unrevealed minibatch randomness, the current stochastic gradient Gt, future gradients, future iterates, or the final output. In this sense, predictability is the causal condition that permits the perturbation geometry to adapt to long-term historical information while preserving the local innovation structure of the proof. This viewpoint is analogous to predictable processes in stochastic analysis and martingale theory. A predictable process may depend on the past but not on the next innovation. Here, Σt plays the role of a predictable process, while εt is the next virtual innovation. The conditional Gaussian law in (206) is therefore the virtual-perturbation analogue of a martingale-style innovation structure. However, predictability alone does not automatically yield the clean fixed-noise-style mutual-information bound. Because Σt may depend on the training data through Ht−1, the reference kernel used in the chain-rule decomposition cannot be assumed to have access to the same covariance without justification. The general theorem therefore compares the actual virtual transition kernel with an admissible reference kernel. If the reference covariance is denoted by Σref  

> t

, then the mean-discrepancy part of the one-step comparison is measured in the reference precision 

Λt = (Σ ref  

> t

)−1, (207) and the covariance-comparison cost is 

Ccov  

> t

= 12 E



Tr((Σ ref  

> t

)−1Σt) − d + log det Σ ref 

> t

det Σ t



. (208) When the comparison is admissibly synchronized, meaning that the reference kernel can reconstruct the same covariance from the reference-visible information 

FQt = σ(fW1: t) ∨ U ,

49 one may choose 

Σref  

> t

= Σ t.

In that case, Ccov  

> t

vanishes and the clean fixed-noise-style corollary is recovered. This synchronization is automatic for deterministic fixed covariances, and may also hold for public or prefix-observable covariance constructions. It is not automatic for arbitrary data-dependent history-adaptive covariances. For fully history-adaptive perturbations, the general covariance-comparison theorem is the safe default. 

10.3 Output Sensitivity and Local Curvature 

The covariance process also affects the output-sensitivity penalty through the accumulated covariance 

Σ1: T = 

> T−1

X

> t=1

Σt.

The penalty 

Rad ∆

measures the cost of transferring the guarantee from a virtually perturbed output back to the original SGD output. Under global smoothness, this term is controlled by a trace bound of the form 

Rad ∆ ≤ μE[Tr(Σ 1: T )] .

This control is robust but often pessimistic because it treats all directions as having worst-case curvature. The local curvature refinement gives a sharper interpretation. Up to higher-order Taylor remainders, the output-sensitivity term is governed by a covariance-weighted curvature mismatch: 

12 |E [Tr ([ HS′ (WT ) − HS (WT )]Σ 1: T )] | .

Thus, the accumulated covariance should not be judged only by its trace. Its alignment with the local curvature of the training and ghost losses matters. Perturbation mass in low-curvature or curvature-matched directions may have a small output-sensitivity cost, while perturbation mass in high-curvature directions may increase the penalty substantially. This observation complements the covariance-comparison discussion above. The information term is influenced by inverse-covariance weighted gradient-deviation and gradient-sensitivity terms, whereas the output penalty is influenced by the accumulated covariance itself and its alignment with local curvature. The framework therefore exposes an information–sensitivity trade-off rather than a universally optimal adaptive covariance rule. 

10.4 What Is Still Not Covered 

The predictable framework is deliberately limited. It relaxes fixed perturbation geometry, but it does not cover all forms of adaptivity. Several important cases remain outside the direct scope of the present theorem. First, if Σt depends on the current minibatch Bt, the current minibatch index Jt, or the current stochastic gradient Gt, then it is generally not Ht−1-measurable under the timing convention of this paper. In that case, the covariance is not fixed before the randomness of the current step is revealed, and the conditional Gaussian comparison used in the proof no longer applies directly. Analyzing such choices would require enlarging the filtration, changing the timing of the update, or introducing additional correction terms. Second, if Σt depends on future iterates, the final output, validation data, or test data, then the causal structure is broken. Such choices may introduce uncontrolled dependence between the perturbation process and quantities that the proof treats as unrevealed. The theorem therefore does not apply to noncausal 50 covariance rules. Third, the current main theorem assumes positive definite covariances. If Σt is singular or genuinely low-rank, then the inverse norm 

∥x∥2Σ−1

> t

(209) is not directly defined. One may handle such cases by adding a ridge term, 

Σt + λI, λ > 0, (210) or by using Moore–Penrose pseudo-inverses together with support conditions. These extensions are natural but require separate technical treatment. Fourth, when the actual and reference covariance processes differ in the one-step comparison, the covariance-comparison term in (208) must be included. Moreover, the corresponding gradient-deviation and sensitivity terms are measured in the reference geometry 

Λt = (Σ ref  

> t

)−1.

Ignoring this distinction would incorrectly treat a data-dependent adaptive covariance as if it were a determin-istic fixed covariance. The covariance-comparison term is not a defect of the framework; it is the correct KL cost of comparing Gaussian kernels with different covariances. Finally, our analysis does not by itself prove generalization bounds for fully adaptive optimizers such as Adam or AdaGrad. Those methods introduce long-term dependence into the actual update rule, not only into the virtual perturbation geometry. The present paper addresses a specific analytical subproblem: how to allow the virtual noise geometry in SGD analysis to depend predictably on the past. Combining this tool with a full analysis of adaptive update dynamics is an important direction for future work. 

10.5 Why This Matters 

The predictable perturbation framework provides a modular way to introduce history dependence into information-theoretic generalization analysis. Although the present paper focuses on SGD with adaptive virtual perturbations, the same principle suggests several future directions. First, adaptive learning-rate analyses may benefit from separating the adaptivity of the true update from the adaptivity of the virtual perturbation geometry. Even when the optimizer uses a fixed update rule, the proof may choose a perturbation covariance that reflects past gradient scales. This offers a bridge between fixed-noise virtual perturbation bounds and analyses of adaptive stochastic dynamics. Second, preconditioned SGD and Adam-like methods naturally generate geometry through accumulated gradient statistics. The predictable covariance framework shows how such statistics can be used analytically without necessarily changing the optimizer. For example, a diagonal covariance based on past second-moment estimates can be used as a virtual geometry, while the actual SGD recursion remains unchanged. A separate analysis would be needed to handle optimizers whose actual updates also use such preconditioners. Third, fractional-memory and history-dependent stochastic methods involve explicit dependence on past gradients. Although the present theorem does not analyze fractional updates directly, it provides a template for handling predictable objects generated from long histories. A natural next step is to combine predictable virtual perturbations with memory-dependent update maps. Fourth, differentially private SGD and related private learning algorithms often calibrate noise to sensitiv-ity, clipping norms, or gradient-deviation and fluctuation estimates. A predictable perturbation viewpoint may help separate the role of actual privacy noise from auxiliary analytical perturbations. This could be useful for studying privacy, stability, and generalization under adaptive noise calibration. Fifth, sharper covariance-comparison bounds may improve the usefulness of history-adaptive perturbation analysis. The term Ccov  

> t

is the exact Gaussian KL cost for comparing covariances, but particular covariance families may allow sharper or more interpretable controls. Similarly, low-rank or structured perturbation 51 geometries may reduce dimensional dependence if the corresponding singular or nearly singular covariance analysis can be developed carefully. The broader message is that fixed perturbation schedules are not the only analytically tractable option. History-adaptive perturbations can be incorporated into virtual perturbation analysis when their dependence on the trajectory is predictable, and when the reference-kernel comparison is handled with the appropri-ate covariance-comparison cost. This opens a controlled path toward information-theoretic analyses of optimization procedures where geometry, scale, and sensitivity evolve over time. At the same time, the framework should not be interpreted as solving all adaptive optimization general-ization problems. It resolves one specific obstacle: allowing virtual perturbation covariances to depend on past SGD history while preserving the local conditional relative-entropy argument and making explicit the additional cost required by the global mutual-information comparison. This modular contribution can be combined with future analyses of adaptive updates, preconditioning, memory, and privacy mechanisms. 

# 11 Numerical Diagnostic Illustration 

This section describes an optional numerical diagnostic framework for visualizing the quantities that appear in the proposed information-theoretic bounds. The purpose is not to empirically prove the theorem; the theorem is a mathematical statement. Rather, the goal is to make the components of the bound visible along a recorded SGD trajectory. Throughout this section, the true training algorithm remains vanilla SGD: 

Wt+1 = Wt − ηtGt, Gt = g(Wt, B t), t = 1 , . . . , T − 1. (211) No virtual perturbation is injected into the update. The perturbation covariances considered below are analytical objects used only to estimate proxy terms for the theoretical quantities. Thus, the numerical quantities in this section should be interpreted as diagnostics, not as certified numerical generalization bounds. 

11.1 Purpose of the Diagnostic Illustration 

The main theorem involves the reference-geometry quantities 

V ad  

> t

(Λ t), Γad  

> t

(Λ t), Ccov  

> t

, Rad ∆ , (212) where 

Λt = (Σ ref  

> t

)−1. (213) In the admissibly synchronized case, 

Σref  

> t

= Σ t, Λt = Σ −1 

> t

, Ccov  

> t

= 0 .

In the general covariance-comparison theorem, the admissible reference covariance Σref  

> t

may differ from the actual predictable covariance Σt, and the covariance-comparison cost must be included. The exact theoretical quantities are generally not computable in finite experiments. They involve population gradients, conditional expectations, Gaussian averages over accumulated adaptive covariances, and admissibility conditions for reference kernels. The goal of this section is therefore to define practical proxies that approximate the behavior of these terms. The diagnostics have four purposes: 1. to visualize gradient-deviation or fluctuation proxies along training; 2. to visualize gradient-sensitivity proxies under accumulated virtual perturbations; 52 3. to compare fixed and predictable adaptive virtual covariance schedules; 4. to display the trade-off between inverse-covariance weighted information proxies, covariance-comparison cost, and output sensitivity. These diagnostics should not be interpreted as evidence that one covariance schedule is universally superior, nor as evidence that the optimizer itself has been improved. The optimizer is unchanged; only the analytical perturbation geometry used in the diagnostic computation changes. 

11.2 Diagnostic Experimental Setup 

A simple setup is sufficient. One may train a small multilayer perceptron or convolutional neural network on a standard dataset such as MNIST or CIFAR-10 using vanilla SGD. During training, record at selected checkpoints: 

Wt, Bt, Gt, ηt, L(Wt, S ), L(Wt, S eval ),

where Seval is an independent validation, test, or ghost sample used only for diagnostics. The virtual covari-ance schedules are evaluated alongside the SGD trajectory but are not used in the optimizer. Representative choices include: fixed isotropic: Σt = σ2I, (214) adaptive scalar: Σt = σ2 

> t

I, σ2 

> t

= σ20 (1 + c bqt−1), (215) adaptive diagonal: Σt = diag( s2

> t, 1

, . . . , s 2

> t,d

), (216) Adam-like virtual geometry: vt = βv t−1 + (1 − β)Gt−1 ⊙ Gt−1. (217) Here bqt−1 denotes any scalar pathwise statistic computed only from the past history Ht−1, such as a moving average of gradient norms or a fluctuation proxy from previous updates. We use bqt−1 rather than bvt−1 to avoid confusing this pathwise scale statistic with the theoretical gradient-deviation term V ad  

> t

.For the Adam-like case, one may use either a proportional covariance 

Σt = diag  ρ2 

> t

(√vt + ϵ) + λ0I, (218) or an inverse-scale covariance 

Σt = diag 

 ρ2

> t

√vt + ϵ



+ λ0I. (219) The ridge term λ0I ensures positive definiteness. All adaptive schedules must be predictable: Σt must be computed only from information available before update t, namely from Ht−1. For example, vt in (217) uses 

Gt−1, not Gt. For a fixed deterministic covariance, the synchronized reference covariance 

Σref  

> t

= Σ t

is admissible. For a data-dependent adaptive covariance, synchronized diagnostics may still be visually informative, but the clean admissibly synchronized theoretical bound should be invoked only when an admissible synchronization certificate is available. Otherwise, one should specify an admissible reference covariance Σref  

> t

and use the general covariance-comparison diagnostic. 53 11.3 Estimating the Bound Proxies 

The quantities appearing in the theoretical bounds are generally not exactly computable in finite experiments. They involve population gradients, conditional expectations, Gaussian averages over accumulated adaptive covariance, and, in the general covariance-comparison theorem, admissible reference geometries. Therefore, the goal of this subsection is not to compute the bound exactly, but to define diagnostics that approximate its main components. A key distinction is important. The theoretical term 

V ad  

> t

= E

h

∥Gt − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

is a conditional mean-square deviation from the population gradient. It is a conditional variance only under the unbiasedness condition 

E[Gt | H t−1] = ¯ g(Wt).

Consequently, numerical proxies based only on subbatch differences estimate conditional stochastic fluctua-tion, not the full gradient-deviation term when conditional bias is present. 

Population-gradient or ghost-batch deviation proxy. If a large evaluation batch or an independent ghost batch 

Beval 

> t

is available, approximate the population gradient by 

b¯gt = g(Wt, B eval  

> t

). (220) Then a synchronized total gradient-deviation proxy is 

bV dev  

> t

= Gt − b¯gt

> 2Σ−1
> t

. (221) If a reference covariance Σref  

> t

is used, define 

Λt = (Σ ref  

> t

)−1,

and use the reference-geometry deviation proxy 

bV dev ,ref  

> t

= Gt − b¯gt 

> 2Λt

. (222) These proxies estimate the total discrepancy from a population-gradient surrogate and therefore capture both stochastic fluctuation and possible bias relative to the evaluation-batch approximation. 

Subbatch fluctuation-only proxy. When an independent evaluation or ghost batch is unavailable, one may use subbatch differences as a fluctuation-only diagnostic. Split the current minibatch into two disjoint subbatches, 

B(1)  

> t

and B(2)  

> t

,

and define 

G(1)  

> t

= g(Wt, B (1)  

> t

), G(2)  

> t

= g(Wt, B (2)  

> t

).

A synchronized fluctuation-only proxy is 

bV fluc  

> t

= 12 G(1)  

> t

− G(2) 

> t
> 2Σ−1
> t

. (223) 54 The factor 1/2 is appropriate when the two subbatch gradients are conditionally independent and identically distributed with the same covariance. For unequal subbatch sizes, the scaling should be adjusted according to the known covariance scaling of the two subbatch estimators. Similarly, the reference-geometry fluctuation-only proxy is 

bV fluc ,ref  

> t

= 12 G(1)  

> t

− G(2) 

> t
> 2Λt

. (224) The labels (223) and (224) are kept for compatibility with earlier references, but the quantities should be interpreted as fluctuation-only proxies rather than full gradient-deviation proxies. With more than two subbatches, one may use the empirical covariance around the subbatch mean. If B(1)  

> t

, . . . , B (K) 

> t

are disjoint subbatches and 

G(k) 

> t

= g(Wt, B (k) 

> t

), ¯Gsub  

> t

= 1

K

> K

X

> k=1

G(k) 

> t

,

then a synchronized empirical fluctuation proxy is 

bV fluc ,K t = 1

K − 1

> K

X

> k=1

G(k) 

> t

− ¯Gsub 

> t
> 2Σ−1
> t

. (225) The corresponding reference-geometry version replaces Σ−1 

> t

by Λt.

Interpretation of deviation versus fluctuation proxies. The direct deviation proxy bV dev  

> t

targets 

∥Gt − ¯g(Wt)∥2Σ−1

> t

through a population-gradient approximation. In contrast, the subbatch proxy bV fluc  

> t

estimates only the conditional fluctuation of minibatch gradients around their conditional mean. It does not estimate the squared conditional bias term 

∥E[Gt | H t−1] − ¯g(Wt)∥2Σ−1

> t

.

Thus, fluctuation proxies are faithful proxies for V ad  

> t

only when conditional unbiasedness is justified or when the conditional bias is negligible. 

Gradient-sensitivity proxy. The adaptive gradient-sensitivity term measures how the population gradient changes under accumulated virtual perturbations. Let 

Σ1: t =

> t−1

X

> k=1

Σk.

At selected checkpoints, draw Monte Carlo perturbations 

ζt,j ∼ N (0 , Σ1: t), j = 1 , . . . , m. (226) Using the same evaluation batch Beval  

> t

to approximate population gradients, define the synchronized sensitivity proxy 

bΓt = 1

m

> m

X

> j=1

g(Wt + ζt,j , B eval  

> t

) − g(Wt, B eval  

> t

) 2Σ−1

> t

. (227) 55 If a reference covariance is used, the reference-geometry sensitivity proxy is 

bΓref  

> t

= 1

m

> m

X

> j=1

g(Wt + ζt,j , B eval  

> t

) − g(Wt, B eval  

> t

) 2Λt

. (228) If no evaluation or ghost batch is available, this sensitivity proxy should not be reported unless a separate held-out minibatch or population-gradient surrogate is specified. 

Covariance-comparison proxy. For the general covariance-comparison theorem, the actual covariance Σt

and the admissible reference covariance Σref  

> t

may differ. The corresponding one-step Gaussian covariance-comparison cost is 

Ccov  

> t

= 12 E



Tr 



(Σ ref  

> t

)−1Σt



− d + log det Σ ref 

> t

det Σ t



.

A pointwise diagnostic at iteration t is 

bCcov  

> t

= 12



Tr 



(Σ ref  

> t

)−1Σt



− d + log det Σ ref 

> t

det Σ t



. (229) When the covariance is admissibly synchronized, 

Σref  

> t

= Σ t,

this proxy is exactly zero. If synchronization is used only as a diagnostic without an admissibility certificate, then bCcov  

> t

= 0 should not be interpreted as a valid theoretical covariance-comparison cost. 

Output-sensitivity proxy. The adaptive output-sensitivity penalty transfers the generalization guarantee from the perturbed output back to the original SGD output. Let 

Σ1: T = 

> T−1

X

> k=1

Σk.

Draw final perturbations 

ζT,j ∼ N (0 , Σ1: T ), j = 1 , . . . , m T .

For a validation or ghost sample Seval , define 

b∆T (Seval ) = 1

mTmTX

> j=1

h

L(WT , S eval ) − L(WT + ζT,j , S eval )

i

. (230) This proxy estimates the local loss sensitivity of the final output under the accumulated adaptive virtual covariance. To approximate the difference between training and population output sensitivity, one may compute the same quantity on both the training sample and an independent evaluation or ghost sample and use the penalty-difference proxy 

bRad ∆ = b∆T (Seval ) − b∆T (S) . (231) If only one evaluation sample is available, then b∆T (Seval ) should be interpreted as an output-sensitivity diagnostic rather than as an estimate of the exact train–ghost penalty difference. 56 Information-sensitivity diagnostic. A practical diagnostic corresponding to the dominant terms in the bound is bB = X

> t∈T

h

2η2

> t

 bV ⋆t + bΓ⋆t



+ bCcov 

> t

i

+ bRad ∆ , (232) where T denotes the set of checkpoints at which the proxies are computed. The symbol bV ⋆t denotes whichever gradient-deviation or fluctuation proxy is used: 

bV ⋆t ∈

n bV dev  

> t

, bV dev ,ref  

> t

, bV fluc  

> t

, bV fluc ,ref 

> t

o

.

Similarly, bΓ⋆t denotes either the synchronized sensitivity proxy bΓt or the reference-geometry sensitivity proxy 

bΓref  

> t

, depending on whether the synchronized or general covariance-comparison diagnostic is being reported. The choice of bV ⋆t , bΓ⋆t , and bRad ∆ should be reported explicitly. In particular, fluctuation-only proxies should not be described as estimating the full gradient-deviation term unless conditional unbiasedness or negligible conditional bias is justified. The diagnostic bB should not be interpreted as a certified numerical generalization bound. It is a visualization tool for comparing the relative behavior of gradient deviation, gradient sensitivity, covariance-comparison cost, and output sensitivity under different virtual perturbation geometries. 

11.4 Fixed versus Adaptive Perturbation Schedules 

The numerical comparison can evaluate several virtual covariance schedules on the same SGD trajectory. This isolates the effect of analytical perturbation geometry from changes in the optimizer. For a fixed isotropic schedule, 

Σt = σ2I, (233) the inverse-covariance weighting is uniform across coordinates and constant across training. This provides a baseline corresponding to fixed-noise analysis. Since the covariance is deterministic, synchronized compari-son is admissible and the clean admissibly synchronized diagnostic is theoretically aligned with the clean corollary. For an adaptive scalar schedule, 

Σt = σ2 

> t

I, σ2 

> t

= σ20 (1 + cbqt−1), (234) the scalar perturbation magnitude changes with a past pathwise statistic bqt−1. This schedule is predictable if 

bqt−1 is computed only from Ht−1. If a synchronized comparison is not theoretically justified, one should also specify an admissible reference scalar covariance and use the general covariance-comparison diagnostic. For an adaptive diagonal schedule, 

Σt = diag( s2

> t, 1

, . . . , s 2

> t,d

), (235) the proxy terms become coordinate-wise weighted: 

∥x∥2Σ−1

> t

=

> d

X

> j=1

x2

> j

s2

> t,j

. (236) This allows one to inspect whether certain coordinates or layers dominate the proxy. Sampling from diagonal covariances and computing inverse-norms can be done without forming dense matrices. For Adam-like virtual geometry, the second-moment estimate 

vt = βv t−1 + (1 − β)Gt−1 ⊙ Gt−1

is predictable because it uses only past gradients. The proportional covariance 

Σt = diag  ρ2 

> t

(√vt + ϵ) + λ0I

57 and inverse-scale covariance 

Σt = diag 

 ρ2

> t

√vt + ϵ



+ λ0I

represent two different analytical interpretations. The proportional form applies more virtual smoothing in historically high-gradient coordinates, whereas the inverse-scale form applies less virtual perturbation in those directions. The comparison is not meant to decide which is universally correct; it shows how the proxy terms respond to different predictable geometries. For low-rank plus isotropic covariance families, sampling and inverse-norm computation can be imple-mented using structured matrix identities. For example, if 

Σt = λ0I + UtDtU ⊤ 

> t

,

then samples can be generated from an isotropic component plus a low-rank component, and inverse products can be computed using the Woodbury identity. 

11.5 Recommended Figures and Interpretation 

A concise numerical diagnostic section may include the following figures. • Perturbation scale over training. Plot σt, Tr(Σ t), or Tr(Σ 1: t) across epochs for fixed and adaptive schedules. This shows how the virtual perturbation geometry evolves. • Gradient-deviation or fluctuation proxy. Plot bV dev  

> t

, bV dev ,ref  

> t

, bV fluc  

> t

, or bV fluc ,ref  

> t

over training. The chosen proxy must be reported explicitly. Fluctuation-only proxies should not be described as estimating the full gradient-deviation term unless conditional unbiasedness or negligible conditional bias is justified. • Gradient-sensitivity proxy. Plot bΓt or bΓref  

> t

over training. This shows how sensitive the population-gradient surrogate is to accumulated virtual perturbations. • Covariance-comparison proxy. Plot bCcov  

> t

or its cumulative sum when actual and reference covariances differ. This visualizes the Gaussian KL cost of using a valid reference geometry different from the actual predictable covariance. • Output sensitivity or flatness proxy. Plot b∆T , or its checkpoint version, as training progresses. This visualizes the cost of transferring the bound from the perturbed output back to the original output. • Curvature-weighted output-sensitivity proxy. If Hessian-vector products or a low-rank Hessian approximation are available, plot bHT = Tr( bHT Σ1: T )

or the ghost-training curvature-mismatch analogue 

Tr 



[ bHSeval (WT ) − bHS (WT )]Σ 1: T



.

This diagnostic estimates the leading local-curvature contribution to the output-sensitivity penalty. It should be reported separately from the global trace proxy Tr(Σ 1: T ).• Diagnostic summary versus empirical train-test gap. Compare bB with the observed train-test loss gap. This comparison is qualitative. The diagnostic summary is not an exact numerical generalization bound. 58 • Information-sensitivity decomposition. Plot the gradient-deviation/fluctuation proxy, gradient-sensitivity proxy, covariance-comparison proxy, and output-sensitivity proxy separately. This makes the trade-off between smoothing, covariance mismatch, and perturbation robustness visible. The expected qualitative behavior is not that adaptive covariance always improves the diagnostic. Rather, the illustration should reveal how different predictable covariance rules redistribute the bound contributions. For example, a schedule with larger virtual covariance may reduce inverse-covariance weighted gradient-deviation and sensitivity proxies while increasing the output perturbation penalty. A data-dependent adaptive schedule may also incur covariance-comparison cost if the reference covariance differs. A diagonal covariance may reduce contributions in some coordinates while amplifying others. The numerical diagnostic is therefore best viewed as a companion to the theory. It provides intuition for the adaptive quantities but does not replace the mathematical assumptions, admissibility conditions, or proofs. The main contribution remains the history-adaptive information-theoretic bound; the diagnostics simply make its components visible. 

# 12 Conclusion 

We developed a history-adaptive extension of virtual perturbation analysis for information-theoretic gen-eralization bounds of stochastic gradient descent. Prior virtual perturbation frameworks typically rely on fixed perturbation schedules or deterministic covariance geometries. While this fixed-noise assumption is analytically convenient, it limits the ability of the theory to represent long-term dependence arising from the optimization history. In contrast, our framework allows the virtual perturbation covariance at each update to depend on the past real SGD trajectory, provided that this dependence is predictable. Thus, the covariance may adapt to past iterates, past minibatches, past gradients, and pathwise statistics, while not depending on the current perturbation, current unrevealed randomness, or future information. The main theoretical contribution is a conditional information-theoretic framework for SGD with pre-dictable history-adaptive virtual perturbations. Predictability is sufficient for the local conditional Gaussian relative-entropy step: after conditioning on the past real SGD history, the covariance is fixed and the one-step Gaussian smoothing argument applies. However, predictability alone is not sufficient for the full mutual-information comparison. Because the adaptive covariance may be data-dependent through the SGD history, the reference perturbation geometry must also be handled carefully. Accordingly, our main theorem is stated as a general covariance-comparison bound. When the actual and reference virtual covariances differ, the bound includes an explicit covariance-comparison cost, and the mean-discrepancy terms are measured in the corresponding reference geometry. The clean fixed-noise-style bound is recovered only under an admissible synchronization certificate. The resulting bounds replace the fixed local gradient-deviation and gradient-sensitivity terms of classical virtual perturbation analysis with conditional adaptive counterparts. In the admissibly synchronized case, the bound is expressed using the actual adaptive perturbation geometry. In the general covariance-comparison case, the bound uses reference-geometry gradient-deviation and gradient-sensitivity terms. The gradient-deviation term should be interpreted as a conditional mean-square deviation from the population gradient; it reduces to a conditional variance only under an appropriate conditional unbiasedness condition. The bounds also include an adaptive output-sensitivity penalty, which accounts for transferring the guarantee from the virtually perturbed output back to the original SGD output. We showed that this penalty can be controlled under global smoothness and further interpreted through local curvature, covariance-weighted flatness, and curvature mismatch between training and ghost losses. The framework recovers fixed isotropic and fixed geometry-aware virtual perturbation bounds as special cases. It also accommodates adaptive scalar, diagonal, Adam-like, curvature-aware, and low-rank virtual covariance geometries, provided the corresponding covariance process is predictable and positive definite, 59 or appropriately regularized. Importantly, these perturbations are purely analytical: the true SGD recursion is not modified. The contribution is therefore not a new optimizer, but a more flexible proof framework for incorporating controlled long-term dependence into virtual perturbation generalization analysis. Several directions remain open. A natural next step is to combine predictable virtual perturbations with analyses of fully adaptive optimizers such as Adam, AdaGrad, and RMSProp, where long-term dependence enters the actual update rule rather than only the virtual perturbation process. Another promising direction is to study fractional-memory stochastic dynamics, where historical gradients play an explicit role. Further work may also investigate adaptive noise calibration in differentially private SGD, sharper covariance-comparison bounds, low-rank or singular perturbation geometries, and empirical diagnostics for the proposed bound components in modern deep learning models. 

# References 

[1] Moritz Hardt, Benjamin Recht, and Yoram Singer. Train faster, generalize better: Stability of stochastic gradient descent. In International Conference on Machine Learning , 2016. [2] Yunwen Lei and Yiming Ying. Fine-grained analysis of stability and generalization for stochastic gradient descent. In International Conference on Machine Learning , 2020. [3] Raef Bassily, Vitaly Feldman, Crist ´obal Guzm ´an, and Kunal Talwar. Stability of stochastic gradient descent on nonsmooth convex losses. In Advances in Neural Information Processing Systems , 2020. [4] Olivier Bousquet, Yegor Klochkov, and Nikita Zhivotovskiy. Sharper bounds for uniformly stable algorithms. In Conference on Learning Theory , 2020. [5] Daniel Russo and James Zou. Controlling bias in adaptive data analysis using information theory. In 

Artificial Intelligence and Statistics , 2016. [6] Aolin Xu and Maxim Raginsky. Information-theoretic analysis of generalization capability of learning algorithms. In Advances in Neural Information Processing Systems , 2017. [7] Ankit Pensia, Varun Jog, and Po-Ling Loh. Generalization error bounds for noisy, iterative algorithms. In IEEE International Symposium on Information Theory , 2018. [8] Jeffrey Negrea, Mahdi Haghifam, Gintare Karolina Dziugaite, Ashish Khisti, and Daniel M. Roy. Information-theoretic generalization bounds for SGLD via data-dependent estimates. In Advances in Neural Information Processing Systems , 2019. [9] Mahdi Haghifam, Jeffrey Negrea, Ashish Khisti, Daniel M. Roy, and Gintare Karolina Dziugaite. Sharpened generalization bounds based on conditional mutual information and an application to noisy, iterative algorithms. In Advances in Neural Information Processing Systems , 2020. [10] Thomas Steinke and Lydia Zakynthinou. Reasoning about generalization via conditional mutual information. In Conference on Learning Theory , 2020. [11] Gergely Neu, Gintare Karolina Dziugaite, Mahdi Haghifam, and Daniel M. Roy. Information-theoretic generalization bounds for stochastic gradient descent. In Proceedings of Machine Learning Research ,2021. [12] John Duchi, Elad Hazan, and Yoram Singer. Adaptive subgradient methods for online learning and stochastic optimization. Journal of Machine Learning Research , 2011. 60 [13] Tijmen Tieleman and Geoffrey Hinton. Lecture 6.5—RMSProp: Divide the gradient by a running average of its recent magnitude. COURSERA: Neural Networks for Machine Learning, 2012. [14] Diederik P. Kingma and Jimmy Ba. Adam: A method for stochastic optimization. In International Conference on Learning Representations , 2015. 61 Algorithm 1 Proxy estimation for adaptive virtual perturbation diagnostics 

Require: SGD trajectory {Wt, G t, B t}Tt=1 , learning rates {ηt}T −1 

> t=1

, actual covariance rule Σt = Φ t(Ht−1), optional admissible reference covariance rule Σref  

> t

, optional evaluation or ghost batches Beval  

> t

, Monte Carlo sample size m

1: Initialize Σ1:1 = 0 

2: for t = 1 , . . . , T − 1 do 

3: Compute predictable actual covariance Σt = Φ t(Ht−1)

4: if using a general reference geometry then 

5: Compute admissible Σref  

> t

and set Λt = (Σ ref  

> t

)−1

6: else 

7: Set Λt = Σ −1

> t

8: end if 

9: if an evaluation or ghost batch Beval  

> t

is available then 

10: Compute b¯gt = g(Wt, B eval  

> t

)

11: Compute total gradient-deviation proxy 

bV dev  

> t

= Gt − b¯gt

> 2Σ−1
> t

12: if using a reference geometry then 

13: Compute reference-geometry total gradient-deviation proxy 

bV dev ,ref  

> t

= Gt − b¯gt

> 2Λt

14: end if 

15: else 

16: Split Bt into B(1)  

> t

, B (2)  

> t

and compute G(1)  

> t

, G (2) 

> t

17: Compute fluctuation-only proxy 

bV fluc  

> t

= 12 G(1)  

> t

− G(2) 

> t
> 2Σ−1
> t

18: if using a reference geometry then 

19: Compute reference-geometry fluctuation-only proxy 

bV fluc ,ref  

> t

= 12 G(1)  

> t

− G(2) 

> t
> 2Λt

20: end if 

21: end if 

22: Draw ζt,j ∼ N (0 , Σ1: t), j = 1 , . . . , m 

23: if an evaluation or ghost batch Beval  

> t

is available then 

24: Estimate synchronized gradient-sensitivity proxy using (227) 

25: if using a reference geometry then 

26: Estimate reference-geometry gradient-sensitivity proxy using (228) 

27: end if 

28: else 

29: Report that the gradient-sensitivity proxy was not computed, or compute it using a separately specified held-out minibatch 

30: end if 

31: if using a general reference geometry then 

32: Compute bCcov  

> t

using (229) 

33: else 

34: Set bCcov  

> t

= 0 only if synchronization is admissible 

35: end if 

36: Update accumulated covariance Σ1: t+1 = Σ 1: t + Σ t

37: end for 

38: Draw ζT,j ∼ N (0 , Σ1: T ), j = 1 , . . . , m 

39: Estimate output-sensitivity proxy b∆T using (230) 

40: If both training and evaluation/ghost samples are available, estimate bRad ∆ using (231) 

41: Compute bB using (232) , reporting whether bV ⋆t is a deviation proxy or a fluctuation-only proxy and whether bΓ⋆t is synchronized or reference-geometry 

62 Appendix 13 Proof of the Conditional Relative-Entropy Lemmas 

This appendix proves the conditional Gaussian relative-entropy inequalities used in the main text. These lemmas provide the local Gaussian comparison step in the analysis of predictable virtual perturbations. They do not by themselves prove the full mutual-information bound. The full proof also requires the reference-kernel mutual-information decomposition, the conditioning-compression argument from rich histories to virtual-prefix conditionings, and the S-admissibility of the reference covariance. The first result treats the common-covariance case. The second result treats the covariance-mismatch case, which is needed when the actual virtual perturbation covariance Σt and the admissible reference covariance 

Σref  

> t

differ. Throughout this appendix, conditional distributions are understood as regular conditional laws. We assume all random variables take values in standard Borel spaces so that the conditional distributions used below exist. All statements hold almost surely with respect to the conditioning sigma-field. 

Lemma 6 (Conditional Gaussian relative-entropy bound) . Let F be a sigma-field. Let Σ ≻ 0 be F-measurable, and let 

ε | F ∼ N (0 , Σ) . (.1) Let X, Y ∈ Rd be random vectors with finite second moments. Assume that ε is conditionally independent of (X, Y ) given F. Then 

DKL 

 PX+ε|F PY +ε|F 

 ≤ 12 E

h

∥X − Y ∥2Σ−1 | F 

i

. (.2) 

Proof. The proof is conditional on F. Fix a realization of F. After conditioning, Σ is a deterministic positive definite matrix and 

ε ∼ N (0 , Σ) 

is independent of the conditional pair (X, Y ). It is therefore enough to prove the deterministic-covariance statement under this conditioning. Let π be any coupling of the conditional laws of X and Y given F. For example, π may be chosen as the actual conditional joint law of (X, Y ) given F. Conditionally on (X, Y ) = ( x, y ), the two smoothed laws are 

N (x, Σ) , N (y, Σ) .

The KL divergence between Gaussians with the same covariance is 

DKL (N (x, Σ) ∥ N (y, Σ)) = 12 (x − y)⊤Σ−1(x − y) = 12 ∥x − y∥2Σ−1 . (.3) Because ε is independent of (X, Y ) after conditioning on F, the conditional laws of X + ε and Y + ε

can be represented as mixtures: 

PX+ε|F =

Z

N (x, Σ) dπ (x, y ), (.4) and 

PY +ε|F =

Z

N (y, Σ) dπ (x, y ). (.5) 63 By joint convexity of relative entropy, 

DKL 

 PX+ε|F PY +ε|F 



= DKL 

Z 

N (x, Σ) dπ (x, y )

Z

N (y, Σ) dπ (x, y )



≤

Z

DKL (N (x, Σ) ∥ N (y, Σ)) dπ (x, y ). (.6) Substituting (.3) into (.6) yields 

DKL 

 PX+ε|F PY +ε|F 

 ≤ 12

Z

∥x − y∥2Σ−1 dπ (x, y ).

Taking π to be the actual conditional joint law of (X, Y ) given F, the right-hand side becomes 

12 E

h

∥X − Y ∥2Σ−1 | F 

i

.

This proves (.2). 

Lemma 7 (Conditional Gaussian comparison with covariance mismatch) . Let F be a sigma-field. Let 

Σ, Σ′ ≻ 0

be F-measurable. Let 

ε | F ∼ N (0 , Σ) , ε′ | F ∼ N (0 , Σ′). (.7) Let X, Y ∈ Rd be random vectors with finite second moments. Assume that, conditionally on F, the Gaussian noises ε, ε ′ are independent of (X, Y ). Then 

DKL 

 PX+ε|F PY +ε′|F 



≤ 12 E

h

∥X − Y ∥2(Σ ′)−1 | F 

i

+ 12



Tr((Σ ′)−1Σ) − d + log det Σ ′

det Σ 



. (.8) 

Proof. Again condition on a realization of F. Then Σ and Σ′ are deterministic positive definite matrices, and the Gaussian noises are independent of the conditional pair (X, Y ).Let π be any coupling of the conditional laws of X and Y given F. Conditionally on (X, Y ) = ( x, y ),the two smoothed laws are 

N (x, Σ) , N (y, Σ′).

The KL divergence between Gaussians with different means and covariances is 

DKL 

 N (x, Σ) N (y, Σ′) = 12



∥x − y∥2(Σ ′)−1 + Tr((Σ ′)−1Σ) − d + log det Σ ′

det Σ 



. (.9) The mean-difference term is weighted by the reference precision (Σ ′)−1.Using the same mixture representation and joint convexity of relative entropy, 

DKL 

 PX+ε|F PY +ε′|F 



≤

Z

DKL 

 N (x, Σ) N (y, Σ′) dπ (x, y ). (.10) 64 Substituting (.9) into (.10) gives 

DKL 

 PX+ε|F PY +ε′|F 



≤ 12

Z

∥x − y∥2(Σ ′)−1 dπ (x, y ) + 12



Tr((Σ ′)−1Σ) − d + log det Σ ′

det Σ 



. (.11) Taking π to be the actual conditional joint law of (X, Y ) given F proves (.8). 

Corollary 5 (Canonical reference-kernel specialization) . Let m and Σref ≻ 0 be F-measurable. Under the conditions of Lemma 7, 

DKL 



PX+ε|F N (m, Σref )



≤ 12 E

h

∥X − m∥2(Σ ref )−1 | F 

i

+ 12



Tr((Σ ref )−1Σ) − d + log det Σ ref 

det Σ 



. (.12) 

Proof. Apply Lemma 7 with 

Y = m, Σ′ = Σ ref ,

where m is F-measurable. Then 

PY +ε′|F = N (m, Σref ).

Remark 9 (Application to the one-step virtual update) . In the main proof, the actual virtual update has the form fWt+1 = fWt − ηtGt + εt, εt | H t−1 ∼ N (0 , Σt).

The canonical reference kernel has mean fWt − ηt ¯g(fWt)

and reference covariance Σref  

> t

. In the notation of Corollary 5, one may take 

X = fWt − ηtGt, m = fWt − ηt ¯g(fWt), Σ = Σ t, Σref = Σ ref  

> t

.

The resulting mean-difference term is weighted by 

Λt = (Σ ref  

> t

)−1,

which is exactly the reference precision used in the main covariance-comparison theorem. 

Remark 10 (Role of predictability and rich conditioning) . Predictability states that 

Σt is Ht−1-measurable .

This ensures that, after conditioning on any sigma-field containing Ht−1, the actual covariance Σt is fixed and the conditional Gaussian comparison above can be applied. In the full proof of the main theorem, the one-step KL term in the mutual-information decomposition is conditioned on the virtual prefix and the sample. The Gaussian comparison is applied under a richer sigma-field, typically of the form 

Bt = At ∨ H t−1,

where 

At = FQt ∨ σ(S), FQt = σ(fW1: t) ∨ U .

The passage from the richer conditioning back to the virtual-prefix KL term is handled by the conditioning-compression lemma in the main text or in the mutual-information appendix. Thus, predictability is the local condition needed for the Gaussian comparison, while S-admissibility of the reference covariance is the global condition needed for a valid reference-kernel comparison. 65 Remark 11 (Synchronized and mismatched comparisons) . When the actual and reference Gaussian kernels use the same covariance, Σ′ = Σ , Lemma 7 reduces to Lemma 6. Indeed, 

Tr(Σ −1Σ) − d + log det Σ det Σ = d − d + 0 = 0 .

In deterministic fixed-covariance settings, such synchronization is automatic. For history-adaptive covariances, however, synchronization is valid in the main theorem only when it is admissible: the reference kernel must be able to construct the same covariance from the virtual prefix and sample-independent auxiliary information. Otherwise, one must use an admissible reference covariance Σref  

> t

and retain the covariance-comparison cost. 

Remark 12 (Coupling and Wasserstein refinement) . The proofs above use the actual conditional joint law of 

(X, Y ) given F. A sharper version may be obtained by minimizing over all conditional couplings of the laws of X and Y given F. In the common-covariance case, this yields a conditional quadratic optimal-transport cost in the norm induced by Σ−1. In the covariance-mismatch case, the corresponding transport cost is measured in the reference precision (Σ ′)−1. For the purposes of the main theorem, the actual conditional coupling is sufficient. 

# 14 Proof of the Mutual Information Decomposition 

This appendix proves the mutual-information decomposition used in the main text. The argument has three components: data processing, the chain rule for mutual information, and a reference-kernel inequality. The decomposition itself is independent of the particular form of the virtual perturbation covariance. The covariance structure enters later when the one-step reference-kernel KL terms are bounded by conditional Gaussian comparison. Compared with a fixed-covariance analysis, we allow the reference construction to use auxiliary public or ghost randomness that is independent of the original training sample. This is important for history-adaptive covariance comparisons, where the reference covariance may be generated by an admissible public or ghost construction. We make this auxiliary randomness explicit below. 

A Setup 

Let S be the training sample. The true SGD trajectory is 

Wt+1 = Wt − ηtGt, Gt = g(Wt, B t), t = 1 , . . . , T − 1. (A.1) The final output is WT . The virtual perturbed trajectory is 

fWt+1 = fWt − ηtGt + εt, t = 1 , . . . , T − 1, (A.2) with fW1 = W1. The perturbation law may be fixed or predictable-history adaptive, depending on the theorem being applied. We write fW1: T = ( fW1, fW2, . . . , fWT ) (A.3) for the full virtual path. The final virtual output is fWT . We assume throughout that the initialization W1 = fW1

is independent of the training sample S.Let U denote optional public, auxiliary, or ghost randomness used by the reference construction. We assume 

U is independent of S. (A.4) 66 If no such auxiliary randomness is used, U is the trivial sigma-field. For each time t, define the reference-visible sigma-field 

FQt = σ(fW1: t) ∨ U . (A.5) The reference kernel at time t is allowed to be measurable with respect to FQt , but it is not allowed to condition directly on the original sample S. Define also 

At = FQt ∨ σ(S). (A.6) 

B Data Processing and Chain Rule 

Since the final virtual iterate fWT is a coordinate projection of the full path fW1: T , data processing gives 

I(fWT ; S) ≤ I(fW1: T ; S). (B.1) Because U is independent of S, we also have 

I(fW1: T ; S) ≤ I(fW1: T , U; S) = I(fW1: T ; S | U ). (B.2) Therefore, 

I(fWT ; S) ≤ I(fW1: T ; S | U ). (B.3) Applying the chain rule for mutual information conditionally on U yields 

I(fW1: T ; S | U ) = I(fW1; S | U ) +  

> T−1

X

> t=1

I(fWt+1 ; S | fW1: t, U). (B.4) Because fW1 = W1 is independent of S and U is also independent of S,

I(fW1; S | U ) = 0 . (B.5) Combining the preceding displays gives 

I(fWT ; S) ≤ 

> T−1

X

> t=1

I(fWt+1 ; S | fW1: t, U). (B.6) For each t, the conditional mutual information can be written as an expected relative entropy: 

I(fWt+1 ; S | fW1: t, U) = E

h

DKL 



PfWt+1 |A t PfWt+1 |F Qt

i 

. (B.7) 

C Reference-Kernel Upper Bound 

The next step replaces the marginal kernel PfWt+1 |F Qt

by an arbitrary admissible reference kernel Qt+1 |F Qt

.The only requirement for the decomposition is that this reference kernel be measurable with respect to FQt .In particular, it may depend on the virtual prefix fW1: t and on auxiliary randomness U, but not directly on the original sample S.

Lemma 8 (Reference-kernel upper bound) . For each t = 1 , . . . , T − 1, let Qt+1 |F Qt

be any probability kernel measurable with respect to FQt . Then 

I(fWt+1 ; S | fW1: t, U) ≤ E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

. (C.1) 67 Proof. Fix a realization of FQt . For notational simplicity, write this realization abstractly as f . Define 

Ps = PfWt+1 |F Qt =f,S =s, ¯P = PfWt+1 |F Qt =f , (C.2) and 

Q = Qt+1 |F Qt =f . (C.3) By definition, 

¯P =

Z

Ps dP S|F Qt =f (s). (C.4) The conditional mutual-information integrand is 

Z

DKL (Ps∥ ¯P ) dP S|F Qt =f (s). (C.5) For any reference distribution Q, the following KL identity holds, with both sides interpreted in the extended-real sense when necessary: 

Z

DKL (Ps∥Q) dP S|F Qt =f (s) = 

Z

DKL (Ps∥ ¯P ) dP S|F Qt =f (s) + DKL ( ¯P ∥Q). (C.6) This follows by expanding 

log dP s

dQ = log dP s

d ¯P + log d ¯PdQ ,

and integrating first with respect to Ps and then over s. Since DKL ( ¯P ∥Q) ≥ 0, we obtain 

Z

DKL (Ps∥ ¯P ) dP S|F Qt =f (s) ≤

Z

DKL (Ps∥Q) dP S|F Qt =f (s).

Finally, integrating over the law of FQt proves (C.1). Combining (B.6) and Lemma 8 yields the desired reference-kernel decomposition: 

I(fWT ; S) ≤ 

> T−1

X

> t=1

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

. (C.7) 

D Conditioning Compression 

The decomposition above produces one-step KL terms conditioned on At, which consists of the reference-visible information together with the sample. The conditional Gaussian comparison is often most naturally proved after conditioning on a richer sigma-field containing the real SGD history. This subsection records the formal bridge. Let 

Bt = At ∨ H t−1. (D.1) Then At ⊆ B t.

Lemma 9 (Conditioning compression for relative entropy) . Let X be a random element and let 

A ⊆ B 

be sigma-fields. Let Q be an A-measurable probability kernel on the state space of X. Then 

E DKL 

 PX|A Q ≤ E DKL 

 PX|B Q . (D.2) 68 Proof. Let 

μA = PX|A , μB = PX|B .

By the tower property for regular conditional distributions, 

μA = E[μB | A ],

where the equality is understood weakly, i.e. after testing against bounded measurable functions. Since Q is A-measurable, it is fixed after conditioning on A. Relative entropy is convex in its first argument. Therefore, conditionally on A,

DKL (μA ∥ Q) ≤ E [DKL (μB ∥ Q) | A ] .

Taking expectations proves the claim. Applying Lemma 9 with 

X = fWt+1 , A = At, B = Bt, Q = Qt+1 |F Qt

,

gives 

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

≤ E

h

DKL 



PfWt+1 |B t Qt+1 |F Qt

i 

. (D.3) This is the formal justification for proving the Gaussian one-step comparison under a richer conditioning sigma-field and then returning to the virtual-prefix KL term. 

E Ghost Reference Kernels and Covariance Comparison 

A natural way to construct a reference kernel is to use an independent ghost sample or other auxiliary randomness independent of S. In the notation above, this ghost randomness is included in U.Let S◦ be independent of S and distributed identically. Given a realized virtual path prefix, one may define a ghost one-step reference kernel as the law of 

ewt − ηtG◦ 

> t

+ ε◦ 

> t

, (E.1) where G◦ 

> t

is a ghost stochastic gradient and ε◦ 

> t

is a ghost or reference perturbation. Such a construction is admissible only when it is measurable with respect to FQt , i.e. the virtual prefix and sample-independent auxiliary randomness, and does not condition directly on the original sample S.The abstract reference-kernel inequality above does not require a ghost construction. It holds for any 

FQt -measurable reference kernel. The ghost construction is simply one useful way to build an admissible reference kernel. In the deterministic fixed-covariance setting, the actual and reference kernels can use the same determin-istic covariance Σt. In this case, the reference comparison is synchronized, and no covariance-comparison cost appears. In the history-adaptive setting, additional care is needed. If the actual covariance is 

Σt = Φ t(Ht−1), (E.2) then it may depend on the original sample S through the real SGD history. A reference kernel cannot automatically use the same covariance unless this synchronized comparison is admissible. If the reference covariance is denoted by Σref  

> t

, then the one-step Gaussian comparison uses the reference precision 

Λt = (Σ ref  

> t

)−1 (E.3) 69 in the mean-discrepancy term and incurs the covariance-comparison cost 

Ccov  

> t

= 12 E



Tr((Σ ref  

> t

)−1Σt) − d + log det Σ ref 

> t

det Σ t



. (E.4) This is the covariance-mismatch term appearing in the general theorem. Thus, for deterministic fixed covariances, the clean synchronized comparison is automatic. For arbitrary data-dependent history-adaptive covariances, synchronization must be justified by an admissible synchroniza-tion certificate. Otherwise, the general covariance-comparison theorem should be used. The decomposition proved in this appendix reduces the problem of controlling 

I(fWT ; S)

to controlling a sum of one-step reference-kernel KL divergences:  

> T−1

X

> t=1

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

.

The conditioning-compression lemma then permits these KL terms to be bounded under the richer condition-ing 

Bt = At ∨ H t−1,

where the predictable covariance Σt and the real SGD history are available for the local Gaussian comparison. The next step in the proof is to apply the conditional Gaussian comparison to the one-step virtual update and decompose the resulting mean discrepancy into adaptive gradient-deviation and gradient-sensitivity terms. In history-adaptive settings, the same mutual-information decomposition remains valid, but the one-step comparisons must account for the admissible reference covariance geometry and any covariance-comparison cost. 

# 15 Proof of the Main Generalization Theorem 

This appendix proves the main covariance-comparison generalization theorem. The proof combines four ingredients: the output perturbation decomposition, the information-theoretic generalization inequality, the reference-kernel mutual-information decomposition, and the certified one-step Gaussian reference compari-son. Unlike the fixed-noise setting, the covariance process here may be data-dependent through the real SGD history. Therefore, the theorem is stated in the general covariance-comparison form. The clean fixed-noise-style bound is recovered only under an admissible synchronization certificate. 

A Setup and Notation 

Let 

S = {Z1, . . . , Z n}

be the training sample, and let S′ be an independent ghost sample drawn from the same distribution. The true SGD dynamics are 

Wt+1 = Wt − ηtGt, Gt = g(Wt, B t), t = 1 , . . . , T − 1, (A.1) where W1 is the initial iterate and WT is the final output. The virtual perturbed trajectory is 

fWt+1 = fWt − ηtGt + εt, t = 1 , . . . , T − 1, (A.2) 70 with fW1 = W1.The predictable covariance process satisfies 

Σt ≻ 0, Σt is Ht−1-measurable , t = 1 , . . . , T − 1. (A.3) The virtual perturbations satisfy 

εt | H t−1 ∼ N (0 , Σt), (A.4) with the conditional independence assumptions stated in the main text. For t = 1 , . . . , T , define the accumulated perturbation and covariance by 

ξt =

> t−1

X

> k=1

εk, ξ1 = 0 , (A.5) and 

Σ1: t =

> t−1

X

> k=1

Σk, Σ1:1 = 0 . (A.6) Then fWt = Wt + ξt, t = 1 , . . . , T. (A.7) Let 

¯g(w) = EZ∼D [g(w, Z )] (A.8) denote the population gradient. Let U denote optional public, auxiliary, or ghost randomness used by the reference construction. We assume U is independent of the original training sample S. If no such auxiliary randomness is used, U is the trivial sigma-field. Define 

FQt = σ(fW1: t) ∨ U , At = FQt ∨ σ(S), Bt = At ∨ H t−1. (A.9) The reference kernel at time t is allowed to be measurable with respect to FQt , but not to condition directly on S.Let Σref  

> t

≻ 0 be an S-admissible reference covariance, and define 

Λt = (Σ ref  

> t

)−1. (A.10) The covariance-comparison cost is 

Ccov  

> t

= 12 E



Tr((Σ ref  

> t

)−1Σt) − d + log det Σ ref 

> t

det Σ t



. (A.11) For reference-geometry quantities, define 

Kt = Ht−1 ∨ U .

The gradient-deviation term is 

V ad  

> t

(Λ t) = E

h

∥Gt − ¯g(Wt)∥2Λt | K t

i

. (A.12) This is a conditional mean-square gradient-deviation term. It becomes a conditional variance only under the corresponding conditional unbiasedness condition. 71 The reference-geometry gradient-sensitivity term is defined in the conditioning-safe form 

Γad  

> t

(Λ t) = E

h

∥¯g(Wt + ξt) − ¯g(Wt)∥2Λt | K t

i

. (A.13) If Λt is Kt-measurable and independent of the realized virtual perturbation beyond Kt, then ξt may equiva-lently be replaced by a fresh conditional Gaussian copy 

ζt | H t−1 ∼ N (0 , Σ1: t).

The adaptive output-sensitivity penalty is 

Rad ∆ = E

h

∆ad Σ1: T (WT , S ′) − ∆ad Σ1: T (WT , S )

i

, (A.14) where 

∆ad Σ1: T (WT , s ) = E [L(WT , s ) − L(WT + ζT , s ) | H T −1] , (A.15) with 

ζT | H T −1 ∼ N (0 , Σ1: T ).

B Certified One-Step Reference Comparison 

We now restate the certified one-step comparison used in the main proof. This replaces the older black-box assumption that the one-step reference comparison simply holds. 

Proposition 10 (Certified one-step Gaussian reference comparison) . Fix t ∈ { 1, . . . , T − 1}. Suppose Σref 

> t

is S-admissible and the reference kernel is the canonical Gaussian kernel 

Qt+1 |F Qt

= N

fWt − ηt ¯g(fWt), Σref 

> t



. (B.1) Then 

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

≤ 2η2 

> t

E

h

V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t)

i

+ Ccov  

> t

. (B.2) 

Proof. By the conditioning-compression lemma, 

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

≤ E

h

DKL 



PfWt+1 |B t Qt+1 |F Qt

i 

. (B.3) Condition on Bt. Under this conditioning, the quantities fWt, Wt, Σt, and Σref  

> t

are fixed for the Gaussian comparison. The actual one-step virtual update has conditional mean 

fWt − ηtGt

and covariance Σt. The reference kernel has mean 

fWt − ηt ¯g(fWt)

72 and covariance Σref  

> t

. Applying the conditional Gaussian covariance-mismatch lemma gives 

E

h

DKL 



PfWt+1 |B t Qt+1 |F Qt

i 

≤ η2

> t

2 E



Gt − ¯g(fWt) 2Λt



+ Ccov  

> t

. (B.4) Since fWt = Wt + ξt,

we have 

Gt − ¯g(fWt) = ( Gt − ¯g(Wt)) + (¯ g(Wt) − ¯g(Wt + ξt)) . (B.5) Using 

∥a + b∥2Λt ≤ 2∥a∥2Λt + 2 ∥b∥2Λt

and the definitions of V ad  

> t

(Λ t) and Γad  

> t

(Λ t), we obtain 

E



Gt − ¯g(fWt) 2Λt



≤ 2E

h

V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t)

i

.

Combining this with (B.3) and (B.4) gives the slightly sharper coefficient η2 

> t

. The stated bound with 2η2

> t

follows immediately and is kept for consistency with the main theorem. 

C Main General Covariance-Comparison Theorem 

Theorem 2 (General covariance-comparison bound) . Assume that for every fixed w, the loss ℓ(w, Z ) is R-sub-Gaussian. Assume the predictable covariance process is positive definite, the required gradient-deviation, sensitivity, covariance-comparison, and output-sensitivity quantities are finite, and the reference covariance 

Σref  

> t

is S-admissible for every t. Then 

|gen( WT , S )| ≤ 

vuut 2R2

n 

> T−1

X

> t=1

 2η2 

> t

E V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t) + Ccov 

> t

 + Rad ∆ . (C.1) 

Proof. Let 

ζT | H T −1 ∼ N (0 , Σ1: T )

be an independent fresh final perturbation, and define 

cWT = WT + ζT .

By adding and subtracting losses evaluated at cWT ,

gen( WT , S ) = E L(WT , S ′) − L(WT , S )

= E

h

L(cWT , S ′) − L(cWT , S )

i

+ E

h

L(WT , S ′) − L(cWT , S ′)

i

+ E

h

L(cWT , S ) − L(WT , S )

i

. (C.2) The first term is gen( cWT , S ). The remaining two terms equal 

E

h

∆ad Σ1: T (WT , S ′) − ∆ad Σ1: T (WT , S )

i

.

73 Hence 

|gen( WT , S )| ≤ | gen( cWT , S )| + Rad ∆ . (C.3) The fresh perturbation ζT has the same conditional law given HT −1 as the accumulated virtual perturba-tion ξT . Therefore, cWT = WT + ζT and fWT = WT + ξT have the same joint law with S. Consequently, 

I(cWT ; S) = I(fWT ; S). (C.4) Applying the information-theoretic generalization inequality to cWT gives 

|gen( cWT , S )| ≤ 

r 2R2

n I(cWT ; S) = 

r 2R2

n I(fWT ; S). (C.5) By the reference-kernel mutual-information decomposition, 

I(fWT ; S) ≤ 

> T−1

X

> t=1

E

h

DKL 



PfWt+1 |A t Qt+1 |F Qt

i 

. (C.6) Using Proposition 10, each one-step term is bounded by 

2η2 

> t

E

h

V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t)

i

+ Ccov  

> t

.

Therefore, 

I(fWT ; S) ≤ 

> T−1

X

> t=1



2η2 

> t

E

h

V ad  

> t

(Λ t) + Γ ad  

> t

(Λ t)

i

+ Ccov 

> t



. (C.7) Substituting (C.7) into (C.5), and then using (C.3), proves (C.1). 

D Comparable and Admissibly Synchronized Covariance Corollaries 

The general theorem uses reference-geometry quantities. The following corollary gives a more readable form when the reference precision is comparable to the actual precision. 

Corollary 6 (Comparable covariance geometries) . Assume the conditions of Theorem 2. Suppose there exists κ ≥ 1 such that, for all t = 1 , . . . , T − 1,

(Σ ref  

> t

)−1 ⪯ κΣ−1 

> t

almost surely . (D.1) Then 

|gen( WT , S )| ≤ 

vuut 2R2

n

"

2κ 

> T−1

X

> t=1

η2 

> t

E V ad  

> t

+ Γ ad 

> t

 + 

> T−1

X

> t=1

Ccov 

> t

#

+ Rad ∆ . (D.2) 

Proof. The precision comparison (D.1) implies 

∥x∥2Λt ≤ κ∥x∥2Σ−1

> t

.

Therefore, 

V ad  

> t

(Λ t) ≤ κV ad  

> t

, Γad  

> t

(Λ t) ≤ κΓad  

> t

.

Substituting these inequalities into Theorem 2 gives the claim. The clean fixed-noise-style result is obtained only when the synchronized reference covariance is admis-sible. 74 Corollary 7 (Admissibly synchronized covariance bound) . Assume the conditions of Theorem 2. Suppose that, for every t = 1 , . . . , T − 1, the covariance process admits an S-admissible synchronized reference, so that the reference covariance can be chosen as 

Σref  

> t

= Σ t

through an admissible reference construction. Then 

|gen( WT , S )| ≤ 

vuut 4R2

n 

> T−1

X

> t=1

η2 

> t

E V ad  

> t

+ Γ ad 

> t

 + Rad ∆ . (D.3) 

Proof. Under admissible synchronization, 

Λt = Σ −1 

> t

.

Thus, 

V ad  

> t

(Λ t) = V ad  

> t

, Γad  

> t

(Λ t) = Γ ad  

> t

.

Moreover, 

Tr(Σ −1 

> t

Σt) − d + log det Σ t

det Σ t

= d − d + 0 = 0 ,

so Ccov  

> t

= 0 . Substituting into Theorem 2 yields (D.3). 

Remark 13. The synchronized covariance condition is automatic for deterministic fixed covariances. For arbitrary history-adaptive covariances, it is not automatic because Σt may depend on S through Ht−1. In that case, synchronization must be justified by an admissible synchronization certificate. Otherwise, the general covariance-comparison theorem should be used. 

# 16 Recovery of the Original Fixed-Noise Bounds 

This appendix shows how the predictable covariance framework recovers the usual fixed-noise virtual pertur-bation bounds when the perturbation covariance sequence is deterministic. The key point is that deterministic covariances are automatically predictable and automatically admit admissibly synchronized reference compar-isons. Therefore, the covariance-comparison cost in the general theorem vanishes. Throughout this appendix, we use the indexing convention of the main text: W1 is the initial iterate, WT is the final output, and there are 

T − 1 updates. 

A Deterministic Covariances Are Predictable 

Suppose that 

Σ1, . . . , ΣT −1 (A.1) are deterministic positive definite matrices. Then each Σt is measurable with respect to every sigma-field, and in particular 

Σt is Ht−1-measurable , t = 1 , . . . , T − 1. (A.2) Thus, the predictable covariance condition is automatically satisfied. The accumulated covariance is also deterministic: 

Σ1: t =

> t−1

X

> k=1

Σk, Σ1:1 = 0 . (A.3) 75 For the final output, 

Σ1: T = 

> T−1

X

> k=1

Σk. (A.4) Because the covariance sequence is deterministic, the actual and reference virtual kernels can use the same covariance without introducing any direct dependence on the training sample S. Thus, the synchronized reference comparison is admissible: 

Σref  

> t

= Σ t, Λt = Σ −1 

> t

, Ccov  

> t

= 0 . (A.5) Therefore, the general covariance-comparison theorem reduces to the clean admissibly synchronized bound 

|gen( WT , S )| ≤ 

vuut 4R2

n 

> T−1

X

> t=1

η2 

> t

E V ad  

> t

+ Γ ad 

> t

 + Rad ∆ . (A.6) The remainder of this appendix identifies the terms in (A.6) with their fixed-covariance counterparts. 

B Reduction of Adaptive Gradient-Deviation and Sensitivity Terms 

The adaptive gradient-deviation term is 

V ad  

> t

= E

h

∥Gt − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

, t = 1 , . . . , T − 1. (B.1) When Σt is deterministic, the inverse covariance geometry is fixed. Thus (B.1) is the history-conditioned fixed-geometry gradient-deviation term. For clarity, define 

V hist  

> t, Σt

= E

h

∥Gt − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

. (B.2) Then 

V ad  

> t

= V hist  

> t, Σt

. (B.3) The notation V is retained because this term reduces to a variance term in the conditionally unbiased case. In full generality, however, V ad  

> t

is a conditional mean-square deviation from the population gradient. If 

E[Gt | H t−1] = ¯ g(Wt), (B.4) then V ad  

> t

becomes the conditional gradient variance measured in the geometry Σ−1 

> t

. Without (B.4) , it also includes the squared conditional bias between the conditional mean gradient and the population gradient. In some fixed-noise presentations, the corresponding quantity is written as a function of Wt, for example 

Vt, Σt (Wt) = E

h

∥Gt − ¯g(Wt)∥2Σ−1

> t

| Wt

i

. (B.5) The history-conditioned quantity V hist  

> t, Σt

is always the more precise object for the present pathwise analysis. It reduces to the Wt-conditioned form after averaging only under an additional conditional Markov-type sampling condition, namely that the conditional law of the current gradient Gt given the available past depends on the past only through Wt. Under this extra condition, 

E[V hist  

> t, Σt

] = E[Vt, Σt (Wt)] . (B.6) For general finite-sample or history-dependent sampling schemes, one should retain the history-conditioned expression. 76 Next consider the adaptive gradient sensitivity. Let 

ζt | H t−1 ∼ N (0 , Σ1: t) (B.7) be a fresh perturbation with the accumulated covariance. Since Σt and Σ1: t are deterministic in the fixed-noise setting, this fresh-copy representation is unambiguous. The adaptive sensitivity is 

Γad  

> t

= E

h

∥¯g(Wt + ζt) − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

. (B.8) When Σt and Σ1: t are deterministic, this is the history-conditioned fixed geometry-aware sensitivity. Define 

Γhist Σt,Σ1: t = E

h

∥¯g(Wt + ζt) − ¯g(Wt)∥2Σ−1

> t

| H t−1

i

, ζt ∼ N (0 , Σ1: t). (B.9) Then 

Γad  

> t

= Γ hist Σt,Σ1: t . (B.10) Equivalently, since the covariance is deterministic, one may write the usual fixed pathwise sensitivity as a function of Wt:

ΓΣt,Σ1: t (Wt) = Eζ∼N (0 ,Σ1: t)

h

∥¯g(Wt + ζ) − ¯g(Wt)∥2Σ−1

> t

i

. (B.11) In this case, 

E[Γ hist Σt,Σ1: t ] = E[Γ Σt,Σ1: t (Wt)] . (B.12) Substituting (B.3) and (B.10) into (A.6) gives the fixed geometry-aware history-conditioned form: 

|gen( WT , S )| ≤ 

vuut 4R2

n 

> T−1

X

> t=1

η2 

> t

E

h

V hist  

> t, Σt

+ Γ hist Σt,Σ1: t

i

+ Rad ∆ . (B.13) Under the additional Markov-type sampling condition discussed above, this can also be expressed using 

Vt, Σt (Wt) and ΓΣt,Σ1: t (Wt) after taking expectations. 

C Reduction of the Output Sensitivity Penalty 

In the adaptive framework, the output sensitivity is 

∆ad Σ1: T (WT , s ) = E [L(WT , s ) − L(WT + ζT , s ) | H T −1] , (C.1) where 

ζT | H T −1 ∼ N (0 , Σ1: T ). (C.2) If Σ1: T is deterministic, the conditional law of ζT is a fixed Gaussian law, and (C.1) reduces to the fixed covariance output sensitivity 

∆Σ1: T (WT , s ) = Eζ∼N (0 ,Σ1: T ) [L(WT , s ) − L(WT + ζ, s )] . (C.3) Consequently, 

Rad ∆ = R∆, (C.4) where 

R∆ = E ∆Σ1: T (WT , S ′) − ∆Σ1: T (WT , S ) . (C.5) 77 Combining (B.13) with (C.4), we obtain 

|gen( WT , S )| ≤ 

vuut 4R2

n 

> T−1

X

> t=1

η2 

> t

E

h

V hist  

> t, Σt

+ Γ hist Σt,Σ1: t

i

+ R∆. (C.6) Under the additional Markov-type sampling convention, this can also be written as 

|gen( WT , S )| ≤ 

vuut 4R2

n 

> T−1

X

> t=1

η2 

> t

E [Vt, Σt (Wt) + Γ Σt,Σ1: t (Wt)] + R∆. (C.7) This is the fixed geometry-aware virtual perturbation bound in the usual Wt-conditioned notation. 

D Fixed Isotropic Specialization 

We now specialize further to deterministic isotropic perturbations: 

Σt = σ2 

> t

I, σt > 0, t = 1 , . . . , T − 1. (D.1) Then 

Σ1: t = σ21: tI, σ21: t =

> t−1

X

> k=1

σ2

> k

. (D.2) The inverse covariance norm becomes 

∥x∥2Σ−1

> t

= 1

σ2

> t

∥x∥2 . (D.3) Define the Euclidean history-conditioned gradient-deviation numerator 

V Euc  

> t

= E

h

∥Gt − ¯g(Wt)∥2 | H t−1

i

. (D.4) This is a variance numerator only under the conditional unbiasedness condition 

E[Gt | H t−1] = ¯ g(Wt).

Define also the Euclidean sensitivity numerator 

Γhist  

> σ1: t

= E

h

∥¯g(Wt + ζt) − ¯g(Wt)∥2 | H t−1

i

, ζt ∼ N (0 , σ 21: tI). (D.5) Then 

V ad  

> t

= 1

σ2

> t

V Euc  

> t

, Γad  

> t

= 1

σ2

> t

Γhist  

> σ1: t

. (D.6) Substituting these expressions into (C.6), we obtain 

|gen( WT , S )| ≤ 

vuut 4R2

n 

> T−1

X

> t=1

η2

> t

σ2

> t

E V Euc  

> t

+ Γ hist 

> σ1: t

 + R∆,σ . (D.7) Here 

R∆,σ = E ∆σ1: T (WT , S ′) − ∆σ1: T (WT , S ) , (D.8) with 

∆σ1: T (WT , s ) = Eζ∼N (0 ,σ 21: T I) [L(WT , s ) − L(WT + ζ, s )] , (D.9) 78 and 

σ21: T = 

> T−1

X

> k=1

σ2

> k

. (D.10) Thus, the deterministic isotropic fixed-noise theorem is recovered as a special case of the predictable covariance framework. 

Remark 14. The recovery above should be interpreted at the level of the expected quantities appearing in the generalization bound. Our adaptive notation conditions on the full real SGD history Ht−1, whereas some fixed-noise presentations write gradient-deviation, variance, and sensitivity terms as functions of Wt alone. Under an additional condition ensuring that the conditional law of the current stochastic gradient depends on the past only through Wt, these formulations agree after taking expectations. In more general sampling settings, the history-conditioned form is the more precise analogue. 

Remark 15. Deterministic fixed covariances remove both sources of additional complication in the history-adaptive theory: they are automatically predictable, and the actual and reference covariances can be synchro-nized without depending on S. For genuinely history-adaptive covariances, the general covariance-comparison theorem should be used unless an admissible synchronized reference comparison is available. 

# 17 Smoothness-Based Sensitivity Control 

This appendix proves that the adaptive output perturbation-sensitivity term can be controlled under smoothness and local curvature assumptions. The key observation is that, for a centered Gaussian perturbation, the first-order term in a Taylor expansion vanishes in expectation. A global smoothness assumption then controls the sensitivity by the trace of the perturbation covariance, whereas a local Hessian expansion gives a sharper curvature-weighted interpretation. This appendix controls only the adaptive output-sensitivity penalty 

Rad ∆ .

It is independent of the covariance-comparison term 

Ccov  

> t

,

which appears in the information part of the main theorem when the actual and reference virtual perturbation covariances differ. 

A Fixed-Covariance Output Sensitivity 

For a deterministic sample s = {z1, . . . , z n}, define the fixed-covariance output sensitivity by 

∆Σ(W, s ) = Eζ∼N (0 ,Σ) [L(W, s ) − L(W + ζ, s )] , (A.1) where ζ is a fresh perturbation independent of all other randomness. If W is random, the expectation in (A.1) is understood conditionally on W , or equivalently over an independent draw of ζ given W .Because of the sign convention 

∆Σ(W, s ) = Eζ [L(W, s ) − L(W + ζ, s )] ,

the sensitivity may be negative near a local minimum, since perturbing W often increases the loss. Therefore, the most useful control is an absolute-value bound. We use the following smoothness assumption. 79 Assumption 4 (Two-sided smoothness remainder) . For every deterministic sample s, the empirical risk 

L(·, s ) is differentiable and satisfies, for all w, u ∈ Rd,

|L(w + u, s ) − L(w, s ) − ⟨∇ L(w, s ), u ⟩| ≤ μ

2 ∥u∥2 . (A.2) Assumption 4 follows, for example, when L(·, s ) has a μ-Lipschitz gradient. Indeed, μ-smoothness gives both upper and lower quadratic Taylor remainder bounds: 

− μ

2 ∥u∥2 ≤ L(w + u, s ) − L(w, s ) − ⟨∇ L(w, s ), u ⟩ ≤ μ

2 ∥u∥2 .

Proposition 11 (Fixed-covariance sensitivity control) . Suppose Assumption 4 holds. Let ζ ∼ N (0 , Σ) ,where Σ ⪰ 0, and assume ζ is independent of W when W is random. Then, for every deterministic sample s,

|∆Σ(W, s )| ≤ μ

2 Tr(Σ) . (A.3) 

Proof. Condition on W . After conditioning, W is fixed and ζ ∼ N (0 , Σ) is centered. Applying Assumption 4 with w = W and u = ζ gives 

|L(W + ζ, s ) − L(W, s ) − ⟨∇ L(W, s ), ζ ⟩| ≤ μ

2 ∥ζ∥2 .

Taking expectation with respect to ζ, the linear term vanishes because 

E[ζ] = 0 .

Therefore, 

|E [L(W + ζ, s ) − L(W, s )] | ≤ μ

2 E[∥ζ∥2].

Since 

E[∥ζ∥2] = Tr(Σ) ,

and 

∆Σ(W, s ) = −E [L(W + ζ, s ) − L(W, s )] ,

the claim follows. 

Remark 16 (Sign convention) . Near a local minimum with positive semidefinite curvature, perturbing the parameter often increases the loss on average. Thus one typically expects 

∆Σ(W, s ) ≤ 0

near such points. The absolute-value bound in Proposition 11 is therefore the more informative stability statement. 

B Adaptive Conditional Output Sensitivity 

We now extend the previous argument to the adaptive setting. Recall that the final accumulated adaptive covariance is 

Σ1: T = 

> T−1

X

> k=1

Σk. (B.1) 80 Since WT is the final output after T − 1 updates, the final real SGD history is HT −1. The adaptive output sensitivity is 

∆ad Σ1: T (WT , s ) = E [L(WT , s ) − L(WT + ζT , s ) | H T −1] , (B.2) where 

ζT | H T −1 ∼ N (0 , Σ1: T ) (B.3) is a fresh final perturbation. 

Proposition 12 (Adaptive conditional sensitivity control) . Suppose Assumption 4 holds. Then, for every deterministic sample s,

∆ad Σ1: T (WT , s ) ≤ μ

2 Tr(Σ 1: T ) almost surely . (B.4) 

Proof. Condition on HT −1. After conditioning, both WT and Σ1: T are fixed, and 

ζT | H T −1 ∼ N (0 , Σ1: T ).

Moreover, 

E[ζT | H T −1] = 0 , E[∥ζT ∥2 | H T −1] = Tr(Σ 1: T ).

Applying the same argument as in Proposition 11, conditionally on HT −1, gives 

|E [L(WT , s ) − L(WT + ζT , s ) | H T −1]| ≤ μ

2 Tr(Σ 1: T ).

This proves (B.4). 

C Final Penalty Bound 

The adaptive final sensitivity penalty is 

Rad ∆ = E

h

∆ad Σ1: T (WT , S ′) − ∆ad Σ1: T (WT , S )

i

. (C.1) 

Proposition 13 (Smoothness control of the final sensitivity penalty) . Suppose Assumption 4 holds and 

E[Tr(Σ 1: T )] < ∞.

Then 

Rad ∆ ≤ μE[Tr(Σ 1: T )] . (C.2) 

Proof. Using the triangle inequality, 

Rad ∆ ≤ E

h

∆ad Σ1: T (WT , S ′)

i

+ E

h

∆ad Σ1: T (WT , S )

i

. (C.3) Applying Proposition 12 to both terms gives 

Rad ∆ ≤ μ

2 E[Tr(Σ 1: T )] + μ

2 E[Tr(Σ 1: T )] = μE[Tr(Σ 1: T )] .

The bound above is robust and easy to use, but it can be pessimistic in high-dimensional nonconvex settings because it uses a global smoothness constant. We next record a local curvature refinement. 81 D Local Curvature Refinement 

For a deterministic sample s, define the local Hessian 

Hs(W ) = ∇2L(W, s ). (D.1) 

Assumption 5 (Local Hessian-Lipschitz condition) . For a deterministic sample s, assume that L(·, s ) is twice differentiable in a neighborhood of W , and that there exists ρs ≥ 0 such that 

∇2L(W + u, s ) − ∇ 2L(W, s ) op ≤ ρs ∥u∥ (D.2) for all perturbations u in the region of interest. If the condition holds globally, the region of interest is all of 

Rd.

Proposition 14 (Second-order expansion of fixed output sensitivity) . Suppose Assumption 5 holds for sample 

s. Let ζ ∼ N (0 , Σ) . Then 

∆Σ(W, s ) = − 12 Tr ( Hs(W )Σ) + Rs(W, Σ) , (D.3) where the remainder satisfies 

|Rs(W, Σ) | ≤ ρs

6 Eζ∼N (0 ,Σ) 

h

∥ζ∥3i

. (D.4) 

Proof. Taylor’s theorem with third-order remainder controlled by the Hessian-Lipschitz condition gives 

L(W + ζ, s ) = L(W, s ) + ⟨∇ L(W, s ), ζ ⟩ + 12 ζ⊤Hs(W )ζ + rs(ζ), (D.5) where 

|rs(ζ)| ≤ ρs

6 ∥ζ∥3 .

Taking expectation over ζ, the linear term vanishes and 

E[ζ⊤Hs(W )ζ] = Tr( Hs(W )Σ) .

Using 

∆Σ(W, s ) = E[L(W, s ) − L(W + ζ, s )] 

gives the expansion with 

Rs(W, Σ) = −E[rs(ζ)] .

The remainder bound follows immediately. 

Proposition 15 (Second-order expansion of adaptive output sensitivity) . Suppose Assumption 5 holds for sample s at W = WT , conditionally on HT −1. Then 

∆ad Σ1: T (WT , s ) = − 12 Tr ( Hs(WT )Σ 1: T ) + Rs(WT , Σ1: T ), (D.6) where 

|Rs(WT , Σ1: T )| ≤ ρs

6 E

h

∥ζT ∥3 | H T −1

i

. (D.7) 

Proof. Condition on HT −1. Then WT and Σ1: T are fixed, and 

ζT | H T −1 ∼ N (0 , Σ1: T ).

Apply Proposition 14 conditionally with W = WT and Σ = Σ 1: T .82 Lemma 10 (Gaussian third-moment control) . If ζ ∼ N (0 , Σ) , then 

E ∥ζ∥3 ≤ 23/2 [Tr(Σ)] 3/2 . (D.8) 

Proof. The bound is a standard Gaussian moment estimate. Since 

E ∥ζ∥2 = Tr(Σ) ,

one may use a Gaussian norm moment comparison to obtain 

E ∥ζ∥3 ≤ 23/2 [Tr(Σ)] 3/2 .

E Curvature-Mismatch Control of the Adaptive Penalty 

The local expansion shows that the output-sensitivity penalty is controlled by the difference between the local curvature of the ghost loss and the local curvature of the training loss, weighted by the accumulated adaptive covariance. 

Proposition 16 (Local curvature control of the adaptive output penalty) . Suppose the adaptive local curvature expansion (D.6) holds for both S and S′. Then 

Rad ∆ ≤ 12 |E [Tr ([ HS′ (WT ) − HS (WT )] Σ 1: T )] |

+ E [|RS′ (WT , Σ1: T )| + |RS (WT , Σ1: T )|] . (E.1) If the local Hessian-Lipschitz constants are ρS′ and ρS , then 

Rad ∆ ≤ 12 |E [Tr ([ HS′ (WT ) − HS (WT )] Σ 1: T )]] + 23/2

6 E

h

(ρS′ + ρS ) [Tr(Σ 1: T )] 3/2i

. (E.2) 

Proof. By Proposition 15, 

∆ad Σ1: T (WT , S ′) = − 12 Tr( HS′ (WT )Σ 1: T ) + RS′ (WT , Σ1: T ),

and 

∆ad Σ1: T (WT , S ) = − 12 Tr( HS (WT )Σ 1: T ) + RS (WT , Σ1: T ).

Subtracting the two expressions, taking expectation and absolute values, and applying the triangle inequality gives (E.1). The trace-form remainder bound follows from Proposition 15 and Lemma 10. 

Remark 17 (Local flatness interpretation) . If the local Hessian Hs(WT ) is positive semidefinite and the Taylor remainder is negligible, then 

∆ad Σ1: T (WT , s ) ≈ 12 Tr( Hs(WT )Σ 1: T ).

Thus, the output sensitivity is governed by covariance-weighted local curvature. Perturbation mass in low-curvature directions contributes little to the penalty, while perturbation mass aligned with high-curvature directions can increase it. 

Remark 18 (Relation to the main theorem) . This appendix controls only the output-sensitivity term Rad ∆ . The covariance-comparison term Ccov  

> t

, which appears in the information part of the general theorem when actual and reference perturbation covariances differ, is controlled separately through Gaussian KL comparison. 83