---
title: Optimal Communication Rate of Secure Aggregation over Ring Networks with Pairwise Keys
title_zh: 具有两两密钥的环形网络上安全聚合的最优通信率
authors: "Xiang Zhang, Han Yu, Zhou Li, Yizhou Zhao, Giuseppe Caire"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01849v2"
tags: ["query:dml-dp"]
score: 8.0
evidence: 去中心化联邦学习的安全聚合
tldr: 本文研究了环形网络下基于两两密钥的信息论拓扑安全聚合（TSA）。针对现有方案依赖中心化密钥服务器的局限性，研究了用户间独立共享密钥的通信效率。通过刻画最小通信速率，发现当用户数K≥5时，由于双重密钥对齐约束，每比特聚合结果需2比特通信量。研究提出了一种线性掩码方案并证明了其最优性，揭示了拓扑稀疏性在安全聚合中的关键作用。
selection_source: fresh_fetch
motivation: 旨在解决传统拓扑安全聚合依赖中心化密钥服务器导致的单点故障问题，通过引入用户间两两独立密钥来提升系统的鲁棒性。
method: 提出了一种线性两两掩码方案，并利用基于密钥依赖结构的熵理论推导了紧致的逆向界限，以证明通信速率的最优性。
result: 确定了环形拓扑下的最小通信速率：K为3或4时为1，K大于等于5时为2，且发现仅需环距离为2的用户对密钥即可实现最优速率。
conclusion: 研究揭示了网络规模和拓扑稀疏性对安全聚合通信效率的深层影响，为去中心化联邦学习中的高效安全计算提供了理论支撑。
---

## 摘要
信息论拓扑安全聚合（TSA）\cite{zhang2026information_regular} 使分布式用户能够在不泄露个人输入的情况下，在任意网络上计算邻域和，同时保持通信效率。它具有广泛的应用，包括去中心化联邦学习（FL）中的安全模型聚合。现有的 TSA 方案依赖于由可信密钥服务器生成的任意相关密钥，这引入了单点故障。在本文中，我们转而研究具有“两两”密钥的 TSA，其中每个用户对 $(i,j)$ 共享一个独立的密钥 $S_{i,j}$。这种密钥可以通过用户间通信建立，从而消除了对密钥服务器的需求并提高了鲁棒性。针对具有 $K$ 个用户的环形拓扑，我们刻画了最小每用户通信率：为了安全地计算所需输入和的一个比特，当 $K=3,4$ 时，每个用户必须向其邻居发送至少 1 个比特；而对于所有 $K\ge 5$，则至少需要 2 个比特。较大网络中较高的通信率是因为每个用户必须同时满足来自其两个邻域的两个独立密钥对齐约束，而在两两密钥独立的情况下，这无法在单个广播符号内解决。我们提出了一种实现这些速率的线性两两掩码方案，并通过利用密钥依赖结构的紧致熵逆界证明了其最优性。值得注意的是，对于所有 $K\ge 4$，在 $\binom{K}{2}$ 个两两密钥中，仅需一个子集——具体而言，是环距离为 2 的用户之间的密钥——就足以实现最优性，这揭示了拓扑稀疏性在安全聚合中的非平凡作用。

## Abstract
Information-theoretic topological secure aggregation (TSA)\cite{zhang2026information_regular} enables distributed users to compute neighborhood sums over arbitrary networks without revealing individual inputs, while remaining communication-efficient. It has broad applications, including secure model aggregation in decentralized federated learning (FL). Existing TSA formulations rely on arbitrarily correlated keys generated by a trusted key server, which introduces a single point of failure. In this paper, we instead study TSA with \tit{pairwise} secret keys, where each user pair $(i,j)$ shares an independent key $S_{i,j}$. Such keys can be established through inter-user communication, eliminating the need for a key server and improving robustness. Focusing on a ring topology with $K$ users, we characterize the minimum per-user communication rate: \tit{to securely compute one bit of the desired input sum, each user must send at least $1$ bit to its neighbors when $K=3,4$, and at least $2$ bits for all $K\ge 5$}. The higher rate in larger networks arises because each user must simultaneously satisfy two independent key-alignment constraints from its two neighborhoods, which cannot be resolved within a single broadcast symbol under pairwise key independence. We propose a linear pairwise-masking scheme that achieves these rates and prove its optimality via tight entropic converse bounds that exploit the dependency structure of the keys. Notably, for all $K\ge 4$, only a subset of the $\binom{K}{2}$ pairwise keys -- specifically, those between users at ring distance $2$ -- is sufficient to achieve optimality, revealing a nontrivial role of topological sparsity in secure aggregation.

---

## 论文详细总结（自动生成）

这篇论文《Optimal Communication Rate of Secure Aggregation over Ring Networks with Pairwise Keys》深入探讨了在去中心化联邦学习（DFL）场景下，如何通过两两密钥（Pairwise Keys）实现高效且安全的数据聚合。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在去中心化网络（如环形拓扑）中，如何在不依赖“可信第三方密钥服务器”的前提下，实现信息论安全聚合（IT-SA），并确定其最小通信开销（最优通信率）。
*   **研究动机**：
    *   **消除单点故障**：传统的拓扑安全聚合（TSA）依赖中心化服务器分发相关密钥，一旦服务器被攻破或失效，系统安全性将崩溃。
    *   **提升鲁棒性**：采用用户间两两独立生成的密钥（Pairwise Keys）可以实现完全去中心化，但这种密钥结构的限制（独立性）是否会增加通信负担尚不明确。
    *   **理论空白**：现有研究多关注全连接网络或假设密钥可任意相关，而对稀疏拓扑（如环形网络）结合两两密钥约束下的通信极限缺乏刻画。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：通过构造一种线性的“两两掩码”方案，利用特定距离的用户间密钥来抵消干扰，同时利用熵理论（Entropic Converse）证明该方案在通信率上的最优性。
*   **关键技术细节**：
    *   **两两密钥模型**：每对用户 $(i, j)$ 共享一个独立随机密钥 $S_{i,j}$。用户 $k$ 的消息 $X_k$ 是其输入 $W_k$ 和所持密钥的函数。
    *   **线性掩码方案**：
        *   对于 $K \ge 5$ 的环形网络，论文提出每个用户发送 2 比特消息来保护 1 比特输入。
        *   **密钥选择策略**：并非使用所有可能的两两密钥，而是仅使用“环距离为 2”的用户对之间的密钥（例如用户 $k$ 与 $k+2$ 共享密钥）。
        *   **构造公式**：用户 $k$ 发送两个分量：$X_k^{(k-1)} = W_k + S_{k, k-2}$ 和 $X_k^{(k+1)} = W_k + S_{k, k+2}$。邻居通过相加抵消密钥，恢复出所需的邻域和。
    *   **逆向证明（Converse）**：利用信息论中的熵不等式，证明当 $K \ge 5$ 时，由于每个用户必须同时满足两个邻居的独立恢复约束，且两两密钥之间缺乏相关性，单比特输入无法在低于 2 比特的通信量下实现安全对齐。

### 3. 实验设计与 Benchmark
*   **研究性质**：本文属于**理论信息论研究**，主要通过数学证明（可实现性方案构造 + 逆向界限证明）来确定最优率，而非基于数据集的实证实验。
*   **场景设定**：具有 $K$ 个节点的环形拓扑网络（Ring Topology），这是 DFL 中最基础且常用的稀疏拓扑。
*   **Benchmark（对比基准）**：
    *   **全连接网络**：已知最优通信率为 1。
    *   **具有相关密钥的环形网络**：已知若密钥由服务器生成且可任意相关，最优通信率为 1。
    *   **本文对比**：展示了在“两两独立密钥”这一更严格、更实用的约束下，通信率如何从 1 跃升至 2。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何 GPU 型号、训练时长或具体算力消耗。
*   **原因**：作为纯理论证明论文，其核心贡献在于数学推导和算法复杂度的理论分析，不涉及大规模模型训练或数值仿真。

### 5. 实验数量与充分性
*   **充分性评价**：
    *   **理论覆盖**：论文完整覆盖了 $K=3, 4$ 以及 $K \ge 5$ 的所有情况，给出了分段函数形式的最优解。
    *   **逻辑严密**：通过构造匹配的上下界（Achievability and Converse），在数学上确立了结论的绝对正确性。
    *   **客观性**：结论基于信息论基本原理，不依赖于特定的启发式算法或随机种子，具有普适性。

### 6. 主要结论与发现
*   **通信率的阶跃**：环形网络下，安全聚合的最优通信率 $R^*$ 存在临界点：
    *   当 $K=3, 4$ 时，$R^* = 1$。
    *   当 $K \ge 5$ 时，$R^* = 2$。
*   **密钥稀疏性发现**：证明了实现最优通信率并不需要 $\binom{K}{2}$ 个全量密钥，仅需环距离为 2 的密钥子集即可。
*   **瓶颈来源**：$K \ge 5$ 时通信率翻倍的根本原因在于“两两密钥的独立性”导致用户无法在单一维度内同时满足两个邻域的密钥对齐要求。

### 7. 优点
*   **理论突破**：首次精确刻画了去中心化两两密钥约束下拓扑安全聚合的通信极限。
*   **实用指导**：揭示了在设计 DFL 协议时，为了去中心化（放弃密钥服务器）必须付出的通信代价（2 倍开销）。
*   **方案简洁**：提出的线性掩码方案计算复杂度极低，易于在资源受限的边缘设备上部署。

### 8. 不足与局限
*   **拓扑局限性**：研究目前仅限于环形拓扑，对于更复杂的正则图或随机图，最优率的通用闭式解仍是有待解决的难题。
*   **静态假设**：未考虑用户掉线（User Dropout）的情况，而在实际联邦学习中，用户掉线是影响安全聚合协议设计的核心挑战。
*   **安全模型**：采用的是半诚实（Honest-but-curious）模型，未讨论恶意攻击者（Active Adversaries）篡改数据的情况。

（完）
