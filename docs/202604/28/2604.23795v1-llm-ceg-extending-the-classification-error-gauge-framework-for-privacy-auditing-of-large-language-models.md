---
title: "LLM-CEG: Extending the Classification Error Gauge Framework for Privacy Auditing of Large Language Models"
authors: Kato Mivule
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23795v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 在大型语言模型中使用 DP-SGD 衡量隐私与效用的权衡
tldr: 扩展了一个使用差分隐私审计大型语言模型隐私与效用权衡的框架。
selection_source: fresh_fetch
motivation: 在大型语言模型中使用 DP-SGD 衡量隐私与效用的权衡。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
This paper extends the Classification Error Gauge (x-CEG) framework, originally developed for measuring the privacy-utility trade-off in tabular datasets, to privacy auditing of Large Language Models (LLMs). We propose LLM-CEG, a systematic framework that employs membership inference attack (MIA) success rates as an empirical privacy gauge and model perplexity as a utility gauge, iteratively adjusting differential privacy parameters until both thresholds are jointly satisfied. A proof-of-concept prototype fine-tunes DistilGPT-2 on a synthetic clinical PII dataset under four privacy regimes using DP-SGD. Results indicate that DP-SGD reduces MIA attacker advantage by 71.5% while simultaneously improving out-of-distribution utility by 47-50% relative to the overfitted baseline, suggesting that differential privacy may act as implicit regularization under narrow fine-tuning conditions. We further extend the SIED engineering framework to the LLM context as LLM-SIED, providing an auditable, regulator-aligned process for privacy-compliant LLM deployment.

---

## 论文详细总结（自动生成）

这篇论文题为《LLM-CEG：扩展分类误差规（CEG）框架用于大型语言模型的隐私审计》，由 Kato Mivule 撰写。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型语言模型（LLM）在训练过程中会隐式记忆敏感信息（如个人身份信息 PII），从而面临成员推理攻击（MIA）的威胁。现有的隐私审计工具零散且缺乏标准化流程。
*   **研究动机**：作者旨在将十年前用于表格数据的 x-CEG 框架扩展到 LLM 领域，建立一套系统化的工程流程，使临床医生、监管机构和决策者能够直观地衡量、管理和沟通隐私与效用之间的权衡（Privacy-Utility Trade-off）。

### 2. 方法论
*   **核心思想**：提出 **LLM-CEG** 框架，将隐私保护视为一个可调节的“压力表”。通过迭代调整差分隐私（DP）参数，直到模型同时满足预设的隐私阈值（通过 MIA 成功率衡量）和效用阈值（通过困惑度 PPL 衡量）。
*   **关键技术细节**：
    *   **LLM-CEG 算法**：一个闭环反馈流程。1. 使用 DP-SGD 进行微调；2. 运行 MIA 计算攻击者优势；3. 评估效用；4. 若未达标则调整隐私预算 $\epsilon$ 并重复。
    *   **LLM-SIED 框架**：将工程过程分为四个阶段：规范（Specifications）、实现（Implementation）、评估（Evaluation）、传播（Dissemination），确保符合欧盟 AI 法案和 NIST AI RMF 等监管要求。
    *   **隐私机制**：采用 **DP-SGD**（差分隐私随机梯度下降），利用 Opacus 库进行梯度裁剪和噪声注入，并使用 Renyi 差分隐私（RDP）会计方法跟踪隐私成本。

### 3. 实验设计
*   **数据集/场景**：使用 Faker 库生成的**合成临床 PII 数据集**（包含姓名、年龄、诊断、药物、薪资、SSN 等）。包含 500 条记录（300 条用于训练，200 条作为非成员集）。
*   **Benchmark 与对比方法**：
    *   **基准模型（Baseline）**：使用标准 SGD 微调的 DistilGPT-2（无隐私保护）。
    *   **对比组**：在三个不同的隐私预算下（$\epsilon = 8.0, 2.0, 0.5$）使用 DP-SGD 微调的模型。
*   **评估指标**：MIA 攻击者优势（Attacker Advantage）、AUROC、困惑度（Perplexity, PPL）。

### 4. 资源与算力
*   **硬件资源**：论文明确指出实验是在**单台消费级 CPU 机器**上完成的（配置为 macOS 25.4.0）。
*   **软件环境**：Python 3.13.12, PyTorch 2.10.0, Hugging Face Transformers 5.2.0, Opacus 1.5.4。
*   **训练时长**：未详细说明具体分钟数，但提到模型仅有 8200 万参数，且在 CPU 上可复现，暗示计算需求极低。

### 5. 实验数量与充分性
*   **实验规模**：共对比了 4 种配置（1 个基准 + 3 个隐私级别），每个模型训练 10 个 Epoch。
*   **充分性评价**：作为**概念验证（Proof-of-Concept）**原型，实验涵盖了从无隐私到高隐私的梯度对比，并引入了帕累托（Pareto）分析。虽然样本量（500 条）和模型规模（DistilGPT-2）较小，但对于验证框架逻辑的正确性是充分且客观的。

### 6. 主要结论与发现
*   **隐私增益**：DP-SGD 将 MIA 攻击者的优势降低了 **71.5%**，将 AUROC 从 0.876 降至接近随机猜测的 0.51 左右。
*   **隐式正则化（核心发现）**：在窄域（Narrow）微调条件下，DP 噪声起到了**隐式正则化**的作用。相比于严重过拟合的基准模型，DP 模型的分布外（OOD）效用提升了 **47-50%**（困惑度从 179.9 降至 120 左右）。
*   **帕累托最优**：实验发现 $\epsilon=8$ 是最优配置，它在提供强力隐私保护的同时保留了最高的模型效用。

### 7. 优点
*   **系统性**：将抽象的数学保证（$\epsilon$）转化为直观的经验指标（MIA 成功率），并提供了完整的工程部署流程（LLM-SIED）。
*   **以人为本**：强调隐私审计的可读性，通过帕累托曲线让非技术利益相关者也能参与决策。
*   **高效性**：证明了在低算力环境下也能进行有效的隐私增强微调和审计。

### 8. 不足与局限
*   **模型规模限制**：实验仅使用了 82M 参数的 DistilGPT-2，尚未在大规模模型（如 Llama 3 或 GPT-4 级别）上验证该结论。
*   **数据局限性**：使用了合成数据而非真实临床数据，且数据集规模较小，可能无法完全模拟真实世界中复杂的数据分布。
*   **审计局限**：MIA 成功率仅是隐私泄露的经验下界，而非理论上的严格上限。

（完）
