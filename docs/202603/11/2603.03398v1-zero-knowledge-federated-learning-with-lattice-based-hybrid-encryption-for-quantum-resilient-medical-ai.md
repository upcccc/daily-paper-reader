---
title: Zero-Knowledge Federated Learning with Lattice-Based Hybrid Encryption for Quantum-Resilient Medical AI
title_zh: 基于格基混合加密的零知识联邦学习，用于抗量子医疗人工智能
authors: Edouard Lansiaux
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03398v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 使用加密协议的隐私保护联邦学习
tldr: 针对医疗AI联邦学习中的隐私泄露、拜占庭攻击及量子计算威胁，本文提出ZKFL-PQ协议。该协议结合了后量子密钥封装（ML-KEM）、格基零知识证明（ZKP）和BFV同态加密，实现了抗量子攻击的隐私保护与梯度完整性验证。实验表明，该方案能完全拦截异常梯度更新，在保证模型精度的同时，其计算开销适用于临床研究周期。
selection_source: fresh_fetch
motivation: 现有的医疗联邦学习面临梯度逆向攻击、恶意客户端投毒以及未来量子计算对加密流量的潜在威胁。
method: 提出ZKFL-PQ协议，集成ML-KEM后量子加密、格基零知识证明验证梯度范数约束以及BFV同态加密实现安全聚合。
result: "实验显示该协议能100%拦截违反范数约束的恶意更新，在维持模型高精度的同时，计算开销约为标准流程的20倍。"
conclusion: ZKFL-PQ为医疗AI提供了一种兼具抗量子安全性与拜占庭鲁棒性的联邦学习框架，适用于对安全性要求极高的临床协作场景。
---

## 摘要
联邦学习 (FL) 使得医院之间能够在不集中患者数据的情况下协作训练医疗人工智能模型。然而，模型更新的交换暴露了关键漏洞：梯度反演攻击可以重建患者信息，拜占庭客户端可以毒害全局模型，而“现在收集，以后解密” (HNDL) 的威胁使得当今的加密流量在未来的量子对手面前变得脆弱。我们引入了 ZKFL-PQ（后量子零知识联邦学习），这是一种三层密码协议，它结合了：(i) 用于抗量子密钥封装的 ML-KEM (FIPS 203)，(ii) 用于可验证的“范数约束”梯度完整性的格基零知识证明，以及 (iii) 用于隐私保护聚合的 BFV 同态加密。我们形式化了安全模型，并在经典随机预言机模型下的 Module-LWE、Ring-LWE 和 SIS 假设下证明了其正确性和零知识性。我们在 5 个联邦客户端、10 轮训练的合成医学影像数据上评估了 ZKFL-PQ。我们的协议实现了对违反范数更新的 100% 拒绝，同时保持模型准确率为 100%，相比之下，标准联邦学习下的准确率灾难性地下降到 23%。计算开销（约 20 倍）经过分析，表明其与每日或每周训练周期的临床研究工作流程相兼容。我们强调，目前的防御措施保证了对大范数恶意更新的拒绝；针对微妙的低范数或定向投毒的鲁棒性仍是未来的研究方向。

## Abstract
Federated Learning (FL) enables collaborative training of medical AI models across hospitals without centralizing patient data. However, the exchange of model updates exposes critical vulnerabilities: gradient inversion attacks can reconstruct patient information, Byzantine clients can poison the global model, and the \emph{Harvest Now, Decrypt Later} (HNDL) threat renders today's encrypted traffic vulnerable to future quantum adversaries.We introduce \textbf{ZKFL-PQ} (\emph{Zero-Knowledge Federated Learning, Post-Quantum}), a three-tiered cryptographic protocol that hybridizes (i) ML-KEM (FIPS~203) for quantum-resistant key encapsulation, (ii) lattice-based Zero-Knowledge Proofs for verifiable \emph{norm-constrained} gradient integrity, and (iii) BFV homomorphic encryption for privacy-preserving aggregation. We formalize the security model and prove correctness and zero-knowledge properties under the Module-LWE, Ring-LWE, and SIS assumptions \emph{in the classical random oracle model}. We evaluate ZKFL-PQ on synthetic medical imaging data across 5 federated clients over 10 training rounds. Our protocol achieves \textbf{100\% rejection of norm-violating updates} while maintaining model accuracy at 100\%, compared to a catastrophic drop to 23\% under standard FL. The computational overhead (factor $\sim$20$\times$) is analyzed and shown to be compatible with clinical research workflows operating on daily or weekly training cycles. We emphasize that the current defense guarantees rejection of large-norm malicious updates; robustness against subtle low-norm or directional poisoning remains future work.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ZKFL-PQ**（后量子零知识联邦学习）的加密协议，旨在解决医疗人工智能（AI）在联邦学习（FL）框架下面临的隐私泄露、恶意攻击及未来量子计算威胁。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的医疗联邦学习虽然避免了数据集中，但仍存在三大安全隐患：
    1.  **梯度反演攻击**：攻击者可通过共享的梯度重建患者原始图像或敏感信息。
    2.  **拜占庭攻击（投毒）**：恶意参与者通过提交异常梯度破坏全局模型的准确性。
    3.  **“现在收集，以后解密”（HNDL）威胁**：现有的加密通信（如 RSA/ECC）无法抵抗未来量子计算机的破解，医疗数据的长期隐私性受损。
*   **研究背景**：医疗数据具有极高的隐私敏感性和长生命周期，迫切需要一种既能防御经典攻击又能抵抗量子威胁的鲁棒联邦学习框架。

### 2. 方法论：ZKFL-PQ 三层加密协议
该协议通过结合三种基于格（Lattice-based）的密码学技术，构建了一个防御体系：
*   **第一层：抗量子传输（ML-KEM）**：采用 FIPS 203 标准的 ML-KEM-768 算法进行密钥封装，确保客户端与服务器之间的通信通道具备抗量子攻击的能力。
*   **第二层：可验证完整性（格基 ZKP）**：
    *   核心思想是利用**零知识证明（ZKP）**，让客户端在不泄露梯度具体值的情况下，证明其梯度的 $\ell_2$ 范数小于预设阈值 $\tau$。
    *   技术细节：基于 SIS（短整数解）假设构建 $\Sigma$-协议，并通过 Fiat-Shamir 启发式将其转换为非交互式证明。如果梯度范数超标，服务器将直接拒绝该更新。
*   **第三层：隐私保护聚合（BFV 同态加密）**：
    *   使用 BFV 方案对梯度进行同态加密。服务器可以在密文状态下对各医院的梯度进行加法聚合，整个过程无法接触到单个医院的明文梯度，仅在聚合后由持有私钥的实体解密平均值。

### 3. 实验设计
*   **数据集与场景**：使用合成医学影像分类任务（4 类诊断：正常、良性、恶性 A、恶性 B）。包含 1000 个样本，每个样本有 784 个特征。
*   **模型架构**：两层隐藏层的多层感知机（MLP），参数量为 108,996 个。
*   **Benchmark（对比方法）**：
    1.  **Standard FL**：使用标准 TLS 加密的联邦学习。
    2.  **FL + ML-KEM**：仅加入后量子传输层，无 ZKP 和同态加密。
    3.  **ZKFL-PQ（本文方法）**：全功能混合协议。
*   **攻击模拟**：在第 4 轮训练时，5 个客户端中的 1 个变为恶意节点，注入放大 50 倍的随机梯度。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Intel Xeon 处理器和 16GB RAM** 的单机上运行。
*   **软件实现**：使用纯 Python 和 NumPy 实现，未调用专门的硬件加速库。
*   **算力说明**：文中**未提到使用 GPU**。由于是纯 CPU 模拟，计算开销主要体现在多项式运算上。单轮训练时间从标准 FL 的 0.149 秒增加到了 ZKFL-PQ 的 2.912 秒。

### 5. 实验数量与充分性
*   **实验规模**：进行了 10 轮联邦训练，设置了 5 个客户端。
*   **消融实验**：
    *   **恶意节点数量**：测试了从 0 到 3 个恶意客户端（占比达 60%）的情况，验证了协议的鲁棒性。
    *   **范数阈值 $\tau$ 的灵敏度**：测试了不同阈值对检测率和误报率（FPR）的影响。
*   **充分性评价**：实验设计较为科学，涵盖了准确率、损失函数收敛性、计算开销和安全性维度。但由于使用的是**合成数据**且**客户端数量较少**，其在真实大规模医疗环境下的表现仍需进一步验证。

### 6. 主要结论与发现
*   **防御效果**：在拜占庭攻击下，标准 FL 的准确率从 100% 暴跌至 23%（接近随机猜测），而 ZKFL-PQ 实现了 **100% 的恶意更新拦截率**，保持了 100% 的模型准确率。
*   **计算开销**：ZKFL-PQ 的计算耗时约为标准 FL 的 **20 倍**。作者认为这对于医疗场景（通常是每日或每周更新一次模型）是完全可以接受的。
*   **通信效率**：虽然引入了加密，但通过部分参数加密等策略，通信量保持在可控范围内。

### 7. 优点与亮点
*   **首创性**：这是首批将 ML-KEM、格基 ZKP 和同态加密集成到统一后量子联邦学习框架中的工作之一。
*   **安全性完备**：同时解决了隐私（HE）、投毒（ZKP）和未来量子威胁（PQ）三个维度的安全问题。
*   **高效的 ZKP 设计**：实验显示 ZKP 的生成和验证耗时极短（占比小于 0.5%），证明了格基 $\Sigma$-协议的高效性。

### 8. 不足与局限
*   **数据真实性**：仅使用了合成数据，缺乏真实 MRI/CT 影像数据集的验证。
*   **部分加密风险**：为了性能，实验中仅对 10 万个参数中的 512 个进行了同态加密。虽然这在实验中有效，但在实际攻击中，未加密的参数可能仍存在泄露风险。
*   **防御范围有限**：目前的 ZKP 仅能拦截“大范数”攻击，对于精心设计的、符合范数约束的“隐蔽投毒”或“后门攻击”缺乏防御能力。
*   **信任假设**：协议假设存在一个可信的解密方持有 BFV 私钥，未来需要引入阈值解密（Threshold Decryption）来消除单点信任风险。

（完）
