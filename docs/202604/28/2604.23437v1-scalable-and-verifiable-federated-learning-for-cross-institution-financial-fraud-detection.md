---
title: Scalable and Verifiable Federated Learning for Cross-Institution Financial Fraud Detection
title_zh: 面向跨机构金融欺诈检测的可扩展且可验证的联邦学习
authors: "Prajwal Panth, Nishant Nigam"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23437v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 用于欺诈检测的可扩展且可验证的联邦学习
tldr: 针对跨机构金融欺诈检测中数据孤岛与隐私保护的矛盾，本文提出动态分片联邦学习（DSFL）框架。该框架通过动态随机分片将通信复杂度从平方级降至线性级，并引入线性完整性标签实现低开销的更新验证。实验表明，DSFL在保持鲁棒性的同时，比传统同态加密方案提速约33倍，为大规模隐私计算提供了高效且可验证的解决方案。
selection_source: fresh_fetch
motivation: 现有的联邦学习协议在处理跨行欺诈检测时，面临计算开销大、通信复杂度高以及缺乏模型更新完整性验证等挑战。
method: 提出DSFL框架，利用动态随机分片优化通信效率，并结合线性完整性标签和主动邻域恢复机制确保数据的安全聚合与系统鲁棒性。
result: 在信用卡欺诈数据集上的实验显示，该方案比基于Paillier加密的安全聚合方案延迟降低了约33倍，且能有效应对参与者掉线。
conclusion: DSFL为金融机构提供了一个可扩展、可验证且兼顾隐私保护的协作式欺诈检测技术基础。
---

## 摘要
全球金融生态系统面临着一个关键的不对称性：虽然欺诈团伙以无国界的分布式网络形式运作，但银行机构仍受限于监管数据孤岛，在 GDPR 等严格隐私法律的约束下，对跨机构威胁模式的可见性有限。尽管联邦学习（FL）支持协作训练，但现有协议在可扩展性、隐私性和完整性之间存在权衡。同态加密方案计算开销巨大，而两两掩码协议需要 O(N^2) 的密钥交换，且缺乏检测畸形更新的机制。现有防御措施在梯度逆向攻击面前也依然脆弱，此类攻击可以重建敏感的交易数据。为了解决这些局限性，我们提出了动态分片联邦学习（DSFL），这是一种用于跨机构金融欺诈检测的可验证安全聚合框架。DSFL 用动态随机分片取代了网状拓扑，将通信复杂度从 O(N^2) 降低到 O(N m)（其中 m 是固定的分片大小），实现了线性可扩展性。为了减轻内部威胁，我们引入了线性完整性标签，这是一种加法同态承诺机制，能够在不产生零知识证明开销的情况下实现对提交更新的概率验证，同时不强制执行语义正确性。此外，主动邻域恢复协议确保了在参与者掉线情况下的稳健聚合。在信用卡欺诈检测数据集（ULB）上的实证评估表明，与基于 Paillier 的安全聚合相比，DSFL 的延迟降低了约 33 倍，同时在模拟故障下保持了强大的韧性。这些结果使 DSFL 成为构建可扩展且隐私保护的协作欺诈检测系统的实用基础。

## Abstract
The global financial ecosystem confronts a critical asymmetry: while fraud syndicates operate as borderless, distributed networks, banking institutions remain constrained by regulatory data silos, limiting visibility into cross-institutional threat patterns under strict privacy laws such as GDPR. Although Federated Learning (FL) enables collaborative training, existing protocols impose a trade-off among scalability, privacy, and integrity. Homomorphic encryption schemes are computationally expensive, while pairwise masking protocols require O(N^2) key exchanges and lack mechanisms to detect malformed updates. Existing defenses also remain vulnerable to gradient inversion attacks that can reconstruct sensitive transaction data.   To address these limitations, we propose Dynamic Sharded Federated Learning (DSFL), a verifiable secure aggregation framework for cross-institution financial fraud detection. DSFL replaces mesh topologies with Dynamic Stochastic Sharding, reducing communication complexity from O(N^2) to O(N m), where m is a fixed shard size, achieving linear scalability. To mitigate insider threats, we introduce Linear Integrity Tags, an additive-homomorphic commitment mechanism that enables probabilistic verification of submitted updates without the overhead of zero-knowledge proofs, while not enforcing semantic correctness. Additionally, the Active Neighborhood Recovery protocol ensures robust aggregation under participant dropouts. Empirical evaluation on the Credit Card Fraud Detection Dataset (ULB) demonstrates an approximately 33x latency reduction compared to Paillier-based secure aggregation, while maintaining strong resilience under simulated failures. These results position DSFL as a practical foundation for scalable and privacy-preserving collaborative fraud detection.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **DSFL (Dynamic Sharded Federated Learning)** 的框架，旨在解决跨机构金融欺诈检测中联邦学习面临的效率与安全挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：金融欺诈（如洗钱、信用卡盗刷）通常跨机构、跨国界发生，但银行受限于隐私法规（如 GDPR），数据处于孤岛状态。
*   **核心矛盾（三律背反）**：现有的联邦学习（FL）协议难以同时满足以下三个指标：
    1.  **可扩展性**：标准安全聚合协议（如 Google SecAgg）的通信复杂度为 $O(N^2)$，在大规模节点下效率极低。
    2.  **隐私性**：梯度信息可能通过“梯度逆向攻击”泄露原始交易数据。
    3.  **完整性**：恶意参与者可能提交伪造梯度（模型投毒），而聚合器难以在不查看明文的情况下验证更新的合法性。
*   **研究目标**：开发一种既能实现线性扩展，又能验证数据完整性，且具备抗掉线能力的隐私保护聚合框架。

### 2. 核心方法论
DSFL 框架由三个关键阶段组成：
*   **动态随机分片 (Dynamic Stochastic Sharding)**：
    *   不再要求所有 $N$ 个节点两两交换密钥，而是利用伪随机函数（PRF）将节点动态划分为大小为 $m$ 的小分片（Shard）。
    *   通信复杂度从 $O(N^2)$ 降至 $O(N \cdot m)$，由于 $m$ 是固定常数，实现了**渐进线性可扩展性**。
*   **分片内掩码与线性完整性标签 (Linear Integrity Tags)**：
    *   **掩码**：在分片内部使用基于 AES-CTR 的零和掩码（Zero-Sum Masking）保护梯度。
    *   **验证**：引入加法同态承诺机制。服务器发布随机挑战向量 $\alpha$，客户端提交更新 $x_u$ 的同时提交标签 $\tau_u = \langle x_u, \alpha \rangle$。服务器通过校验聚合后的标签与梯度之积是否匹配，来检测数据是否被篡改。
*   **主动邻域恢复 (Active Neighborhood Recovery)**：
    *   针对节点掉线问题，服务器仅请求掉线节点与其分片内幸存邻居之间的共享种子，从而在不暴露幸存节点私有掩码的前提下，抵消掉线产生的噪声。

### 3. 实验设计
*   **数据集**：使用 ULB 信用卡欺诈检测数据集（包含约 28.4 万条交易记录）。
*   **场景模拟**：将数据分发给 10 个模拟银行节点，采用分层抽样模拟非独立同分布（Non-IID）的欺诈模式。
*   **基准对比 (Benchmarks)**：
    *   **Paillier 同态加密**：代表高计算开销方案。
    *   **Google SecAgg**：代表标准 $O(N^2)$ 掩码方案。
    *   **本地模型**：作为性能基准。

### 4. 资源与算力
*   **硬件环境**：实验在 Google Colab 环境中运行，使用 Python 3.9 和 NumPy 进行向量化运算。
*   **算力细节**：文中未明确提及具体的 GPU 型号或数量，主要侧重于算法的通信和计算复杂度分析。
*   **扩展性评估**：对于 $N > 10$ 的情况，论文采用了基于经验基准的**解析外推法**来预测大规模网络下的延迟。

### 5. 实验数量与充分性
*   **实验规模**：进行了 5 次独立重复实验并取平均值（附带标准差）。
*   **实验类型**：
    *   **性能实验**：对比不同协议的延迟和带宽。
    *   **鲁棒性实验**：模拟 20% 的节点掉线情况。
    *   **安全性审计**：模拟第 10 轮注入恶意梯度，观察系统的检测能力。
*   **充分性评价**：实验设计较为客观，涵盖了金融场景最关心的延迟、召回率和抗攻击性。但由于大规模节点（如 $N=1000$）的数据是基于公式外推而非真实物理环境测试，其实际网络开销可能被低估。

### 6. 主要结论与发现
*   **效率提升**：在 1000 个节点规模下，DSFL 的延迟比基于 Paillier 的方案降低了约 **33.9 倍**。
*   **检测精度**：协作模型的全局召回率达到 **91.2%**，远高于本地模型平均的 68%，证明了跨机构知识共享的价值。
*   **高鲁棒性**：在 20% 节点掉线的极端情况下，通过主动恢复机制，模型性能（AUPRC）几乎没有损失。
*   **安全性**：线性完整性标签能以极高的概率（接近 $1 - 2^{-64}$）检测出畸形更新。

### 7. 优点
*   **线性扩展**：通过分片技术打破了安全聚合的 $O(N^2)$ 瓶颈，使其具备商用潜力。
*   **低开销验证**：无需复杂的零知识证明（ZKP），利用简单的线性代数性质实现了更新的一致性校验。
*   **针对性强**：方案紧扣金融欺诈检测的实时性需求和监管合规要求。

### 8. 不足与局限
*   **语义验证缺失**：完整性标签只能验证数据是否在传输中被篡改或格式是否异常，**无法识别“语义上”恶意但格式正确的投毒攻击**（例如精心设计的后门攻击）。
*   **外推偏差**：大规模节点的性能数据主要来自数学外推，未充分考虑真实互联网环境中的网络抖动和长尾延迟。
*   **分片安全边界**：虽然分片降低了复杂度，但也降低了合谋攻击的门槛（只需控制一个分片内的 $m-1$ 个节点即可破解该分片隐私）。

（完）
