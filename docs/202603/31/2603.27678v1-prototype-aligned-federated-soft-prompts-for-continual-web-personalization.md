---
title: Prototype-Aligned Federated Soft-Prompts for Continual Web Personalization
title_zh: 原型对齐的联邦软提示用于持续 Web 个性化
authors: "Canran Xiao, Liwei Hou"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27678v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 用于软提示更新的差分隐私联邦聚合
tldr: 针对持续性网络个性化中的非平稳性和隐私限制，本文提出ProtoFed-SP框架。该方法在冻结的骨干模型中注入双时间尺度软提示：快速短提示追踪会话意图，慢速长提示则锚定于通过差分隐私联邦聚合更新的服务器端原型库。实验表明，该方法在多个基准测试中显著提升了推荐性能，并有效平衡了模型的稳定性和塑性，实现了隐私保护下的高效个性化。
selection_source: fresh_fetch
motivation: 现有的个性化方法难以在满足隐私约束的同时，有效平衡用户长期偏好的稳定性与短期意图的快速适应性。
method: 提出ProtoFed-SP框架，通过双时间尺度软提示结合差分隐私联邦聚合的原型库，实现参数高效的持续学习。
result: "在八个基准测试中，ProtoFed-SP在NDCG@10和HR@10指标上均优于最强基线，并显著降低了模型遗忘率。"
conclusion: 该研究提供了一个隐私感知且可控的提示接口，通过原型锚定机制为持续个性化部署提供了稳健的解决方案。
---

## 摘要
持续 Web 个性化对于提升用户参与度至关重要，但现实世界中的非平稳性和隐私约束使得在不遗忘长期偏好的情况下实现快速适应变得十分困难。针对这一差距，我们寻求一种注重隐私且参数高效的接口，在用户/会话层面控制稳定性与可塑性，同时将用户记忆与共享的语义先验相结合。我们提出了 ProtoFed-SP，这是一个基于提示（prompt）的框架，它将双时间尺度的软提示注入到冻结的主干网络中：快速、稀疏的短期提示用于追踪会话意图，而慢速的长期提示则锚定在小型的服务器端原型库上，该原型库通过差分隐私联邦聚合进行持续更新。查询被路由到前 M 个原型，以构建个性化提示。在八个基准测试中，ProtoFed-SP 相比最强的基线模型，在 NDCG@10 上提升了 2.9%，在 HR@10 上提升了 2.0%，其中在 Amazon-Books（相比 INFER 提升 5.0% NDCG）、H&M（相比 Dual-LoRA 提升 2.5%）和淘宝（相比 FedRAP 提升 2.2%）上增幅显著。它还降低了遗忘率（AF）和达到 95% 性能所需的步数（Steps-to-95%），并在实际的差分隐私预算下保持了准确性。我们的贡献在于提出了一种具有原型锚定功能的统一、隐私感知的提示接口，它实现了鲁棒的持续个性化，并提供了一种透明、可控的机制来平衡部署中的稳定性与可塑性。

## Abstract
Continual web personalization is essential for engagement, yet real-world non-stationarity and privacy constraints make it hard to adapt quickly without forgetting long-term preferences. We target this gap by seeking a privacy-conscious, parameter-efficient interface that controls stability-plasticity at the user/session level while tying user memory to a shared semantic prior. We propose ProtoFed-SP, a prompt-based framework that injects dual-timescale soft prompts into a frozen backbone: a fast, sparse short-term prompt tracks session intent, while a slow long-term prompt is anchored to a small server-side prototype library that is continually refreshed via differentially private federated aggregation. Queries are routed to Top-M prototypes to compose a personalized prompt. Across eight benchmarks, ProtoFed-SP improves NDCG@10 by +2.9% and HR@10 by +2.0% over the strongest baselines, with notable gains on Amazon-Books (+5.0% NDCG vs. INFER), H&M (+2.5% vs. Dual-LoRA), and Taobao (+2.2% vs. FedRAP). It also lowers forgetting (AF) and Steps-to-95% and preserves accuracy under practical DP budgets. Our contribution is a unifying, privacy-aware prompting interface with prototype anchoring that delivers robust continual personalization and offers a transparent, controllable mechanism to balance stability and plasticity in deployment.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ProtoFed-SP** 的框架，旨在解决 Web 个性化推荐系统在持续学习环境下的稳定性与可塑性平衡问题，同时兼顾隐私保护。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：Web 环境具有高度的**非平稳性**（用户兴趣、商品供应不断变化）。推荐模型需要在吸收新信号（塑性）的同时，不遗忘长期偏好（稳定性）。
*   **核心挑战**：
    1.  如何在用户层面精准控制稳定性与可塑性的权衡。
    2.  如何在满足隐私保护（如联邦学习、差分隐私）和带宽限制的前提下，实现高效的跨用户知识共享。
    3.  现有的参数高效微调（PEFT）方法缺乏一个共享的语义锚点来连接个体记忆与群体先验。

### 2. 方法论
**ProtoFed-SP (Prototype-Aligned Federated Soft-Prompts)** 的核心思想是在冻结的骨干模型中注入双时间尺度的软提示，并通过联邦原型库进行对齐。

*   **双时间尺度提示 (Dual-Timescale Prompts)**：
    *   **短期提示 ($p_{short}$)**：快速、稀疏更新，用于捕捉会话级别的即时意图。采用 $\ell_1$ 正则化诱导稀疏性。
    *   **长期提示 ($p_{long}$)**：慢速更新，用于存储稳定的长期偏好。
*   **原型路由与组合**：服务器维护一个全局原型库（Prototype Library）。客户端根据当前查询上下文，通过路由器选择 Top-M 个原型，与本地的长短期提示组合成最终的个性化提示。
*   **原型对齐机制**：通过对齐损失（如 Bregman 散度或 InfoNCE），将本地长期提示锚定在全局原型上，防止模型在持续学习中发生漂移。
*   **差分隐私联邦聚合**：
    *   客户端上传经过压缩、剪切并加入高斯噪声的长期提示嵌入。
    *   服务器使用 **DP-FedKMeans** 或鲁棒聚合算法更新原型库，确保个体隐私不被泄露。

### 3. 实验设计
*   **数据集**：涵盖了 8 个主流基准测试，包括 Amazon (Books, Electronics)、MovieLens-20M、Yelp、Gowalla、LastFM-1K、RetailRocket、淘宝（CTR 场景）和 H&M（多模态场景）。
*   **对比方法**：
    *   **传统持续学习**：EWC, SI, MAS, LwF, ER, ReLoop, INFER, CCD。
    *   **参数高效微调**：Prefix-Tuning, Adapters, Dual-LoRA。
    *   **联邦个性化**：FedAvg, FedProx, Ditto, FedRAP。
    *   **大模型推荐**：RecICL, PCL。
*   **评价指标**：NDCG@K, HR@K（准确性）；AF（遗忘率）；Steps-to-95%（适应速度）；以及隐私预算 ($\epsilon, \delta$) 下的性能保持力。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100-80GB GPU**。
*   **软件框架**：基于 PyTorch 2.2 和 Flower/FedML 联邦模拟器。
*   **训练细节**：采用 BF16 精度，每个时间片（Task）在每个客户端上训练 3 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文在 8 个不同领域的任务上进行了广泛测试，模拟了 1000 到 5000 个客户端的联邦环境。
*   **充分性分析**：
    *   **消融实验**：分别验证了长期提示、短期提示、原型对齐和联邦更新的必要性。
    *   **敏感性分析**：测试了原型数量 $K$、路由数量 $M$、稳定性权重 $\lambda_s$ 等关键超参数。
    *   **隐私分析**：详细探讨了不同噪声强度（$\sigma$）对推荐质量的影响。
    *   **用户分层分析**：针对高漂移用户、冷启动用户和活跃用户分别验证了收益。
*   **客观性**：实验设置了统一的候选集和评估协议，并对非提示类基线进行了 PEFT 改造以确保对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：ProtoFed-SP 在所有数据集上均优于最强基线，平均 NDCG@10 提升 2.9%。
*   **有效平衡**：双时间尺度设计显著降低了遗忘率（AF），同时加快了对新意图的适应速度（Steps-to-95% 减少）。
*   **隐私友好**：在实际的隐私预算（如 $\epsilon \approx 3.4$）下，性能损失极小，证明了 DP 聚合原型的可行性。
*   **普适性**：该方法在处理高漂移用户和冷启动场景时表现尤为突出。

### 7. 优点
*   **设计精巧**：通过“原型锚定”为 PEFT 提供了全局语义约束，解决了持续学习中的漂移问题。
*   **参数高效**：仅微调极少量的提示参数（骨干网络冻结），适合端侧部署。
*   **隐私保障**：结合了联邦学习与差分隐私，且不依赖原始交互数据的重放（Replay）。
*   **可解释性**：通过原型路由机制，是个性化过程更加透明可控。

### 8. 不足与局限
*   **骨干网络依赖**：性能上限受限于预训练骨干模型的能力，若骨干模型与目标领域偏差过大，效果可能受限。
*   **模拟环境**：联邦实验是在模拟器中完成的，实际部署中可能面临更复杂的网络波动和系统异构性问题。
*   **超参数调优**：原型库的容量 $K$ 和分离裕度 $\rho$ 等参数仍需根据经验手动设置，缺乏自适应调整机制。

（完）
