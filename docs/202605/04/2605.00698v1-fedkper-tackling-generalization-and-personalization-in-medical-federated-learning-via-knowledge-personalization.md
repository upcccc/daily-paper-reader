---
title: "FedKPer: Tackling Generalization and Personalization in Medical Federated Learning via Knowledge Personalization"
title_zh: FedKPer：通过知识个性化应对医疗联邦学习中的泛化与个性化挑战
authors: "Zoe Fowler, Ghassan AlRegib"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00698v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 针对具有统计异构性的医疗应用联邦学习框架
tldr: 针对医疗联邦学习中由于数据异构性导致的泛化与个性化失衡及遗忘问题，本文提出 FedKPer 框架。该框架在本地训练阶段引入知识个性化以适应特定分布，并在全局聚合时通过强调可靠且标签多样化的更新来提升泛化能力。实验证明，FedKPer 在不牺牲知识保留的前提下，有效优化了泛化与个性化的权衡，提升了模型在医疗场景下的鲁棒性。
selection_source: fresh_fetch
motivation: 医疗机构间的数据异构性导致联邦学习模型难以兼顾全局泛化与本地个性化，并引发了严重的知识遗忘问题。
method: 提出 FedKPer 框架，在本地训练中实施知识个性化，并在全局聚合时优先考虑可靠且标签分布多样的本地更新。
result: 实验结果显示，FedKPer 在多个医疗数据集上显著改善了泛化与个性化之间的权衡，并有效缓解了遗忘现象。
conclusion: 通过结合知识个性化与改进的聚合策略，可以有效解决医疗联邦学习中的统计异构性挑战，实现更好的模型性能。
---

## 摘要
联邦学习（FL）在医疗应用中具有巨大潜力。然而，医疗机构之间的统计异质性为联邦学习带来了重大挑战，因为全局模型既难以泛化到未见的患者群体，也难以适应各家医院独特的数据分布。这种异质性还加剧了全局和局部层面的遗忘，导致模型更新后，先前学习到的患者模式被错误分类。虽然以往的工作大多将泛化和个性化视为独立的挑战，但我们表明，通过与全局模型的选择性对齐以及改进的聚合方案，可以在两者之间取得更好的平衡，从而共同缓解统计异质性的影响。具体而言，我们提出了 FedKPer，在每个本地设备的训练阶段引入知识个性化。随后，通过全局模型聚合过程来考虑泛化，在该过程中，可靠且标签多样化的本地更新会得到重点关注。我们评估了 FedKPer 的性能，并设计了与遗忘常见后果相关的额外指标。总体而言，我们证明了 FedKPer 在不牺牲记忆保留的情况下，改善了泛化与个性化之间的权衡。

## Abstract
Federated learning (FL) holds great potential for medical applications. However, statistical heterogeneity across healthcare institutions poses a major challenge for FL, as the global model struggles both to generalize across unseen patient populations and to adapt to the unique data distributions of individual hospitals. This heterogeneity also exacerbates forgetting at both the global and local level, resulting in previous learned patient patterns to be misclassified after model updates. While prior work has largely treated generalization and personalization as separate challenges, we show that a better balance between the two can be achieved through selective alignment with the global model and a modified aggregation scheme, which together mitigate the effects of statistical heterogeneity. Specifically, we introduce FedKPer, which introduces knowledge personalization into the training stage of each local device. Afterwards, generalization is considered via the global model aggregation process, where local updates that are reliable and label-diverse are emphasized. We evaluate the performance of FedKPer, devising additional metrics that relate to common consequences of forgetting. Overall, we demonstrate FedKPer improves the generalization-personalization trade-off without sacrificing retention.

---

## 论文详细总结（自动生成）

### 论文总结：FedKPer —— 通过知识个性化应对医疗联邦学习中的泛化与个性化挑战

#### 1. 研究动机与核心问题
*   **核心问题**：在医疗联邦学习（FL）中，不同机构间的数据存在严重的**统计异构性**（Non-IID）。这导致全局模型面临双重困境：既难以泛化（Generalization）到未见的患者群体，也难以在特定医院实现良好的个性化（Personalization）适配。
*   **研究动机**：异构数据和部分客户端参与会导致“**遗忘**”现象，即模型更新后会丢失之前学到的患者模式。现有的方法通常将泛化和个性化视为独立问题，且往往通过知识蒸馏（KD）缓解漂移，但若全局模型本身在局部数据上表现不佳，盲目蒸馏反而会误导本地训练。

#### 2. 方法论
FedKPer 的核心思想是**端到端地耦合本地知识个性化与全局聚合正则化**：
*   **本地阶段：知识个性化（Knowledge Personalization）**
    *   引入自适应可靠性权重 $\lambda$。在本地损失函数中，除了交叉熵损失，还加入基于 KL 散度的蒸馏损失。
    *   **关键技术**：$\lambda$ 的大小取决于全局模型在本地数据上的准确度（即 $\lambda = 1 / \ell_{CE}$）。如果全局模型在当前客户端表现差，则降低其指导作用，防止有害的知识迁移。
*   **服务器阶段：基于可靠性与多样性的聚合（Generalization via Aggregation）**
    *   改变传统的按样本量加权的聚合方式。
    *   **关键技术**：服务器根据客户端返回的**可靠性指标**（本地训练准确率）和**标签多样性得分**（基于标签分布熵的计算）来分配权重。这确保了那些表现稳定且类别覆盖广泛的客户端更新在全局模型中占据主导地位，从而提升泛化能力。
*   **遗忘度量指标**：提出了“一致性”（Consistency，基于峰值间遗忘率 IPFR）和“后向迁移”（Backward Transfer, BwT）等新指标，用于量化模型在训练过程中对旧知识的保留能力。

#### 3. 实验设计
*   **数据集**：使用了 MedMNIST v2 中的三个医疗影像数据集：**BloodMNIST**（20个客户端）、**OrganCMNIST**（30个客户端）和 **OrganSMNIST**（50个客户端）。
*   **异构性设置**：采用 Dirichlet 分布（$\alpha = 0.1$）进行数据划分，模拟极高程度的标签分布不均。
*   **对比方法（Baselines）**：涵盖了 11 种主流 FL 算法，包括：
    *   经典 FL：FedAvg, FedProx, FedCurv, MOON, FedNTD。
    *   个性化 FL（pFL）：FedALA, Ditto, FedPer, FedKD, FedBABU, FedAS。

#### 4. 资源与算力
*   **硬件/时长**：论文未明确指出具体的 GPU 型号和数量。
*   **效率分析**：论文提供了一张“准确率 vs. 时间”的对比图（图5），显示在 BloodMNIST 数据集上，达到相同准确率时 FedKPer 比 FedAvg 更节省时间（例如在 500 秒时，FedKPer 的全局准确率比 FedAvg 高出 38.8%）。这表明该方法在计算效率上具有实际应用价值。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同的医疗数据集上进行了测试，每个实验均运行了 3 个随机种子并取平均值，结果包含标准差。
*   **消融实验**：专门对比了“仅使用自适应蒸馏（KD Only）”和“仅使用改进聚合（Agg Only）”的效果，证明了两个组件的互补性（本地蒸馏提升个性化，改进聚合提升泛化）。
*   **公平性**：所有对比方法均在相同的硬件、优化器（SGD, $\eta=0.01$）和通信轮数（100轮）下运行，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先**：FedKPer 在所有三个数据集上的全局准确率均排名第一，尤其在 BloodMNIST 上比次优方法高出 9.8%。
*   **权衡优化**：该方法在“全局-局部平衡”指标上表现最佳，成功缓解了泛化与个性化之间的矛盾。
*   **抗遗忘性**：通过 BwT 和一致性指标验证，FedKPer 有效减少了训练过程中的性能波动和对早期客户端知识的丢失。
*   **鲁棒性**：在“最差客户端准确率”指标上表现优异，说明该方法能有效缩小不同机构间的性能差距。

#### 7. 优点
*   **双向优化**：同时从客户端（自适应蒸馏）和服务器端（加权聚合）入手，解决了 Non-IID 问题的根源。
*   **无需额外数据**：聚合策略仅依赖模型参数和简单的统计量（准确率、熵），不涉及原始数据共享，符合隐私保护要求。
*   **评估全面**：引入了遗忘相关的指标，为联邦学习的鲁棒性评估提供了新视角。

#### 8. 不足与局限
*   **模型复杂度**：实验仅使用了简单的 4 层 CNN，尚未在更深层的网络（如 ResNet 或 Transformer）或更大规模的 3D 医疗影像上验证。
*   **超参数敏感性**：虽然 $\lambda$ 是自适应的，但其上限（Cap=10）和梯度裁剪等参数可能仍需针对不同任务微调。
*   **应用场景限制**：目前仅在分类任务上验证，尚未探讨其在分割或检测等其他医疗影像任务中的表现。

（完）
