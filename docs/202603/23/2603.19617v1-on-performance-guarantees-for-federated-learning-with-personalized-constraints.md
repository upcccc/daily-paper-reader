---
title: On Performance Guarantees for Federated Learning with Personalized Constraints
title_zh: 关于具有个性化约束的联邦学习性能保证的研究
authors: "Mohammadjavad Ebrahimi, Daniel Burbano, Farzad Yousefian"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19617v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 具有私有约束集的联邦优化
tldr: 针对联邦学习中各客户端存在异构私有约束的问题，本文提出了一种名为 PC-FedAvg 的算法。该方法通过多块本地决策向量维护其他代理变量的交叉估计，仅在本地块惩罚不可行性，实现了无需共享约束信息或达成共识的个性化约束优化。理论证明其在次优性上达到 O(ε⁻²) 且在不可行性上达到 O(ε⁻¹) 的通信复杂度，并通过实验验证了其有效性。
selection_source: fresh_fetch
motivation: 传统的联邦学习框架通常处理无约束或全局约束问题，难以应对实际应用中各客户端具有异构资源或私有模型约束的挑战。
method: 提出 PC-FedAvg 算法，通过多块本地决策向量进行交叉估计，使各代理仅在本地惩罚自身约束的不可行性，无需共享约束信息。
result: 理论分析表明该算法在次优性上实现了 O(ε⁻²) 的通信复杂度，在代理端不可行性上实现了 O(ε⁻¹) 的复杂度，并在 MNIST 和 CIFAR-10 数据集上通过实验验证。
conclusion: PC-FedAvg 为具有个性化约束的联邦学习提供了一种高效且保护隐私的优化方案，在保证收敛性的同时兼顾了客户端的异构约束需求。
---

## 摘要
联邦学习 (FL) 已成为一种在多个智能体之间进行分布式学习的高通信效率算法框架。虽然标准的联邦学习公式涵盖了无约束或全局约束的问题，但许多实际场景涉及异构资源或模型约束，从而导致具有智能体特定可行集的优化问题。在本文中，我们研究了一个个性化约束联邦优化问题，其中每个智能体都与一个凸局部目标函数和一个私有约束集相关联。我们提出了 PC-FedAvg，这是一种每个智能体通过多块局部决策向量维护其他智能体变量交叉估计的方法。每个智能体在本地更新所有块，但仅对其自身块的不可行性进行惩罚。此外，交叉估计机制实现了个性化，而无需在智能体之间达成共识或共享约束信息。我们为次优性建立了 \mathcal{O}(ε^{-2}) 的通信复杂度率，并为智能体层面的不可行性建立了 \mathcal{O}(ε^{-1}) 的通信复杂度率。在 MNIST 和 CIFAR-10 数据集上的初步实验验证了我们的理论发现。

## Abstract
Federated learning (FL) has emerged as a communication-efficient algorithmic framework for distributed learning across multiple agents. While standard FL formulations capture unconstrained or globally constrained problems, many practical settings involve heterogeneous resource or model constraints, leading to optimization problems with agent-specific feasible sets. Here, we study a personalized constrained federated optimization problem in which each agent is associated with a convex local objective and a private constraint set. We propose PC-FedAvg, a method in which each agent maintains cross-estimates of the other agents' variables through a multi-block local decision vector. Each agent updates all blocks locally, penalizing infeasibility only in its own block. Moreover, the cross-estimate mechanism enables personalization without requiring consensus or sharing constraint information among agents. We establish communication-complexity rates of $\mathcal{O}(ε^{-2})$ for suboptimality and $\mathcal{O}(ε^{-1})$ for agent-wise infeasibility. Preliminary experiments on the MNIST and CIFAR-10 datasets validate our theoretical findings.

---

## 论文详细总结（自动生成）

这是一份关于论文《On Performance Guarantees for Federated Learning with Personalized Constraints》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的联邦学习（FL）通常假设所有参与者（智能体）共享一个全局模型或遵循相同的全局约束。然而，在实际应用（如协作机器人、分布式传感、能源系统）中，不同智能体往往面临**异构的资源限制**（如计算能力、内存、通信带宽）或**私有的模型约束**（如特定的稀疏度要求）。
*   **核心问题**：如何在不要求智能体之间共享私有约束信息、不强制达成严格全局共识的前提下，解决具有智能体特定可行集的分布式优化问题，并提供严谨的收敛性理论保证。

### 2. 方法论：核心思想与算法流程
*   **核心思想**：提出了一种名为 **PC-FedAvg**（Personalized Constrained Federated Averaging）的算法。其关键在于引入了**多块决策向量（Multi-block decision vector）**和**交叉估计（Cross-estimate）**机制。
*   **关键技术细节**：
    *   **多块结构**：每个智能体 $i$ 维护一个包含所有智能体变量估计的向量。
    *   **本地更新规则**：在本地迭代中，智能体更新所有块。对于代表自身的块，它会根据本地私有约束集进行投影（或通过惩罚项处理）；对于代表其他智能体的块，它仅进行梯度更新而不施加约束。
    *   **软耦合正则化**：通过在目标函数中加入正则项 $\frac{\sigma_i}{2} \|x_i - \bar{x}\|^2$，鼓励智能体变量向群体平均值靠拢，从而在保留个性化约束的同时实现协作。
    *   **块级聚合**：服务器不再简单地覆盖所有本地模型，而是对每个块分别进行聚合，这允许不同智能体的模型状态在每一轮开始时保持差异化，从而支持个性化。
*   **理论保证**：论文证明了该算法在次优性（Suboptimality）上达到 $O(\epsilon^{-2})$ 的通信复杂度，在不可行性（Infeasibility）上达到 $O(\epsilon^{-1})$。

### 3. 实验设计
*   **数据集**：使用了经典的 **MNIST** 和 **CIFAR-10** 图像分类数据集。
*   **实验场景**：采用 Softmax 回归模型，为每个智能体设置了异构的 $\ell_1$ 范数约束（用于促进模型稀疏性），模拟不同智能体对模型复杂度的不同限制。
*   **对比方法（Benchmark）**：
    *   **FedAvg**、**FedProx**、**SCAFFOLD**。
    *   为了公平对比，这些基准方法被修改为“惩罚项版本”（Penalized versions），即在全局目标中加入距离约束集的惩罚项。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **实现细节**：提到了实验设置，如 $m=4$ 个智能体，运行 $R=100$ 轮通信，每轮包含 20 次本地迭代，Batch 大小为 10%。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集上进行了测试，并对比了三种最常用的联邦学习基准算法。
*   **充分性评价**：
    *   **理论验证充分**：实验结果清晰地展示了 PC-FedAvg 在降低“不可行性”方面的显著优势，验证了理论推导。
    *   **消融研究**：论文还考察了本地更新步数（Local Updates）对性能的影响。
    *   **局限性**：实验仅限于线性模型（Softmax 回归），且智能体数量较少（4个），对于大规模深度神经网络或大规模参与者场景的验证尚不充分。

### 6. 主要结论与发现
*   **性能优势**：PC-FedAvg 在保持与基准方法相当的全局损失（Loss）的同时，其**不可行性（约束违反程度）显著低于基准方法**。
*   **个性化能力**：基准方法由于在每轮开始时强制执行服务器的全局平均覆盖，会抹除智能体的个性化约束适应；而 PC-FedAvg 的块级聚合机制成功保留了这种个性化。
*   **收敛一致性**：尽管引入了复杂的个性化约束，PC-FedAvg 仍能保持与标准无约束联邦学习算法同阶的通信复杂度。

### 7. 优点
*   **隐私保护**：智能体无需向服务器或其他智能体透露自己的约束集（如具体的资源预算或模型稀疏度要求）。
*   **理论完备**：为具有个性化约束的非共识优化问题提供了严谨的收敛速度分析，填补了该领域的理论空白。
*   **架构兼容**：保留了标准的“服务器-智能体”通信模式，易于集成到现有的 FL 框架中。

### 8. 不足与局限
*   **存储开销**：每个智能体需要维护所有参与者的变量估计（多块向量），这意味着存储复杂度随智能体数量 $m$ 线性增长，在超大规模联邦学习场景下可能存在扩展性问题。
*   **模型复杂度**：实验部分仅验证了凸优化问题（Softmax 回归），尚未在非凸的深度神经网络（如 ResNet, Transformers）上进行大规模验证。
*   **超参数敏感性**：算法引入了惩罚参数 $\rho$ 和正则化参数 $\sigma_i$，这些参数的调优可能对实际收敛效果有较大影响。

（完）
