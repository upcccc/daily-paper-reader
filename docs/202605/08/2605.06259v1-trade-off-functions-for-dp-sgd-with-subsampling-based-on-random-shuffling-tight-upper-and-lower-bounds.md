---
title: "Trade-off Functions for DP-SGD with Subsampling based on Random Shuffling: Tight Upper and Lower Bounds"
title_zh: 基于随机打乱子采样的 DP-SGD 权衡函数：紧致的上界与下界
authors: "Marten van Dijk, Murat Bilgehan Ertan"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06259v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 对DP-SGD权衡函数的紧致分析
tldr: 本文针对基于随机洗牌（Random Shuffling）子采样的差分隐私随机梯度下降（DP-SGD），在f-DP框架下推导了紧凑的权衡函数分析。研究在特定噪声倍率范围内，利用Berry-Esseen定理给出了透明且可解释的闭式界限，克服了泊松采样分析中公式不透明的问题。此外，通过推广的大数定律，本文还分析了多轮迭代下的隐私组合特性，证明了在大规模数据集上实现强隐私保护的可行性。
selection_source: fresh_fetch
motivation: 旨在为随机洗牌子采样的DP-SGD提供比现有泊松采样更紧凑、透明且具有闭式解的隐私权衡函数分析。
method: 采用f-DP框架，结合Berry-Esseen定理推导单轮界限，并利用推广的大数定律处理多轮迭代的渐近隐私组合。
result: 成功推导出紧凑的上下界，证明了在噪声倍率σ=1且样本量足够大时，随机洗牌能达到接近理想的隐私保护水平。
conclusion: 该研究不仅完善了随机洗牌DP-SGD的理论框架，还通过新证明技术揭示了多轮迭代下隐私预算随轮数平方根增长的渐近特性。
---

## 摘要
我们在 $f$-DP 框架下，对基于随机打乱（Random Shuffling）子采样的差分隐私随机梯度下降（DP-SGD）的权衡函数（trade-off function）进行了紧致分析。我们的分析涵盖了 $σ\geq \sqrt{3/\ln M}$ 的区间，其中 $σ$ 是噪声乘数，$M$ 是单个 epoch 内的迭代轮数。与泊松子采样（Poisson subsampling）的 $f$-DP 分析不同（后者会产生非闭式的隐式公式，虽可由机器计算但不透明），随机打乱允许进行紧致分析，从而得出透明且可解释的闭式界限。我们通过 Berry-Esseen 定理推导出的具体界限在证明框架内达到了常数因子级别的紧致性。我们展示了单 epoch（$E=1$）下的参数设置，其对应的权衡函数 $\geq 1-a-δ$，即仅比理想的随机猜测对角线 $1-a$ 低 $δ$：对于 $δ= 1/100$ 且 $σ= 1$，大约 $M \approx 1.14\times 10^6$ 轮迭代和 $N \approx 1.14\times 10^7$ 个训练样本足以实现有意义的差分隐私。这与近期针对 $σ\leq 1/\sqrt{2 \ln M}$ 区间的负面结果形成对比。我们的具体界限可以在多个 epoch 上进行组合，导致 $δ$ 对 $E$ 具有线性依赖关系，这限制了 $E=O(\sqrt{M})$。为了超越 Berry-Esseen 定理，我们引入了一种基于大数定律推广的新证明技术，该技术产生了一个渐近随机猜测对角线极限结果：如果 $E=c_M^2M$ 且 $c_M\to 0$，则 $E$ 倍组合权衡函数在 $a\in[0,1]$ 上一致满足 $f^{\otimes E}(a)\to 1-a$，且 $δ$ 仅具有 $O(\sqrt{E})$ 的依赖关系。我们将这一渐近区间与相应的泊松子采样渐近情况进行了比较，并强调了显式收敛率的表征仍是一个开放性问题。

## Abstract
We derive a tight analysis of the trade-off function for Differentially Private Stochastic Gradient Descent (DP-SGD) with subsampling based on random shuffling within the $f$-DP framework. Our analysis covers the regime $σ\geq \sqrt{3/\ln M}$, where $σ$ is the noise multiplier and $M$ is the number of rounds within a single epoch. Unlike $f$-DP analyses for Poisson subsampling, which yield non-closed implicit formulas that can be machine computed but are non-transparent, random shuffling admits a tight analysis yielding transparent and interpretable closed-form bounds. Our concrete bounds, derived via the Berry-Esseen theorem, are tight up to constant factors within the proof framework. We demonstrate worked parameter settings for a single epoch ($E=1$) with a corresponding trade-off function $\geq 1-a-δ$, that is, only $δ$ below the ideal random guessing diagonal $1-a$: For $δ= 1/100$ and $σ= 1$, roughly $M \approx 1.14\times 10^6$ rounds and $N \approx 1.14\times 10^7$ training samples suffice to achieve meaningful differential privacy. This is in contrast to recent negative results for the regime $σ\leq 1/\sqrt{2 \ln M}$. Our concrete bounds can be composed over multiple epochs leading to $δ$ having a linear in $E$ dependency, which restricts $E=O(\sqrt{M})$. To go beyond Berry--Esseen, we introduce a new proof technique based on a generalization of the law of large numbers that yields an asymptotic random guessing diagonal-limit result: if $E=c_M^2M$ with $c_M\to 0$, then the $E$-fold composed trade-off function satisfies $f^{\otimes E}(a)\to 1-a$ uniformly in $a\in[0,1]$ with $δ$ having only an $O(\sqrt{E})$ dependency. We compare this asymptotic regime with the corresponding Poisson subsampling asymptotic, and highlight the characterization of explicit convergence rates as an open question.

---

## 论文详细总结（自动生成）

这篇论文针对差分隐私随机梯度下降（DP-SGD）中广泛使用但理论分析较难的“随机打乱”（Random Shuffling）子采样机制，提供了深入的理论分析和紧致的隐私界限。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在差分隐私（DP）领域，DP-SGD 的隐私核算（Privacy Accounting）通常基于**泊松子采样**（Poisson Subsampling）进行分析，因为其数学性质便于处理。然而，实际机器学习框架（如 PyTorch, TensorFlow）普遍采用的是**随机打乱**（Random Shuffling），即每个 epoch 将数据随机排列后划分为固定大小的 mini-batch。
*   **研究动机**：现有的随机打乱隐私分析要么缺乏闭式解（不透明、难以直观理解），要么仅关注隐私泄露的下界（不可能结果）。本文旨在填补这一空白，在 $f$-DP 框架下为随机打乱提供紧致的、闭式的隐私权衡函数（Trade-off Function）上界与下界。

### 2. 论文提出的方法论
*   **核心思想**：将隐私分析转化为一个假设检验问题，通过分析对手区分两个相邻数据集的“假阳性率”与“假阴性率”之间的权衡。
*   **关键技术细节**：
    *   **对抗模型简化**：证明了在最坏情况下，对手的观测可以简化为对一组对数正态分布（Lognormal）随机变量样本均值的阈值检验。
    *   **Berry-Esseen 定理**：在单 epoch 分析中，利用 Berry-Esseen 定理量化了对数正态均值分布与正态分布之间的偏差，从而推导出非渐近的闭式下界。
    *   **Edgeworth 展开与大数定律推广**：为了处理多 epoch 组合，引入了基于 Edgeworth 展开的新证明技术。这超越了传统的 Berry-Esseen 限制，证明了当 epoch 数 $E$ 与轮数 $M$ 满足特定比例时，隐私损失随 $\sqrt{E}$ 增长（而非简单的线性增长）。
    *   **适用范围**：分析主要覆盖高噪声区间，即噪声倍率 $\sigma \geq \sqrt{3/\ln M}$。

### 3. 实验设计
*   **场景与基准**：本文属于理论计算驱动的研究，未进行大规模神经网络训练，而是通过**数值参数反演**来验证理论界限的实用性。
*   **对比对象**：
    *   **泊松子采样**：对比了随机打乱与泊松采样在渐近情况下的隐私保证。
    *   **不可能结果**：与近期关于低噪声区间的隐私泄露下界研究（Ertan & van Dijk, 2026）形成互补。
*   **参数设置**：模拟了联邦学习（Federated Learning）规模的场景，设定 $\delta = 10^{-2}$，噪声 $\sigma = 1$，计算所需的样本量 $N$ 和迭代轮数 $M$。

### 4. 资源与算力
*   **算力说明**：论文中**未提及**使用 GPU 或大规模集群算力。由于其核心贡献在于数学证明和闭式公式推导，相关的数值验证（如 Bisection 方法求解 $M$）仅需普通 CPU 即可在极短时间内完成。

### 5. 实验数量与充分性
*   **实验充分性**：作者通过对不同噪声倍率 $\sigma$（0.5 到 4.0）和隐私参数 $\delta$ 的敏感度分析（见文中图 1 和图 3），展示了闭式公式在各种参数组合下的表现。
*   **客观性与公平性**：研究通过数学证明确保了界限的紧致性，并明确标注了 Berry-Esseen 常数的取值范围。对比随机打乱与泊松采样的渐近系数是基于相同的 $f$-DP 框架，具有高度的客观性。

### 6. 论文的主要结论与发现
*   **闭式界限**：首次为随机打乱提供了紧致的闭式 $f$-DP 下界，使得开发者可以直接通过公式计算隐私预算，无需复杂的数值模拟。
*   **随机打乱的优势**：在渐近低噪声极限下，随机打乱的隐私保护能力比泊松采样强 $\sqrt{2}$ 倍；在高噪声极限下，两者趋于一致。
*   **实用参数建议**：在单 epoch 场景下，对于 $1.14 \times 10^7$ 规模的数据集，设置 $\sigma=1$ 即可获得有意义的隐私保护（$\delta=0.01$）。
*   **组合特性**：多 epoch 下隐私损失的增长率从 $O(E)$ 优化到了 $O(\sqrt{E})$ 的渐近表征。

### 7. 优点
*   **透明度与可解释性**：闭式公式避免了“黑盒”数值工具，便于研究者直观理解 $\sigma, M, E$ 对隐私的影响。
*   **理论完备性**：通过 Berry-Esseen 和 Edgeworth 展开两种路径，兼顾了非渐近的实用性和渐近的理论深度。
*   **直接指导实践**：针对联邦学习等单 epoch 或少 epoch 场景提供了非常具体的参数指导。

### 8. 不足与局限
*   **噪声区间限制**：分析仅适用于 $\sigma \geq \sqrt{3/\ln M}$。在极低噪声（小 $\sigma$）情况下，该理论界限会失效，而这正是某些高性能模型可能涉及的区域。
*   **多 epoch 收敛率**：虽然证明了多 epoch 下的渐近分布，但尚未给出显式的、包含具体常数的收敛率公式（这是一个开放性问题）。
*   **组合损失**：尽管有渐近优化，但在实际非渐近计算中，多 epoch 组合仍会导致隐私参数 $M$ 和 $N$ 的需求呈平方级爆炸，限制了其在超多 epoch 训练中的应用。

（完）
