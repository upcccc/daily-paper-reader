---
title: "FedAFD: Multimodal Federated Learning via Adversarial Fusion and Distillation"
title_zh: FedAFD：基于对抗融合与蒸馏的多模态联邦学习
authors: "Min Tan, Junchao Ma, Yinfu Feng, Jiajun Ding, Wenwen Pan, Tingting Han, Qian Zheng, Zhenzhong Kuang, Zhou Yu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04890v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 具有隐私保护框架的多模态联邦学习
tldr: FedAFD是一个统一的多模态联邦学习框架，旨在解决客户端个性化性能、模态与任务差异以及模型异构性挑战。该框架在客户端引入双层对抗对齐和粒度感知融合模块以优化局部表示，在服务端通过相似度引导的集成蒸馏机制处理异构模型聚合。实验表明，FedAFD在IID和非IID环境下均实现了卓越的性能与效率，为多模态联邦学习提供了新方案。
selection_source: fresh_fetch
motivation: 现有多模态联邦学习方法在处理客户端模型异构性、模态/任务差异以及个性化性能提升方面存在不足。
method: 采用双层对抗对齐与粒度感知融合优化客户端表示，并利用相似度引导的集成蒸馏在服务端聚合异构模型知识。
result: 在多种实验设置下，FedAFD在客户端和服务端的性能及效率表现均优于现有主流多模态联邦学习方法。
conclusion: FedAFD通过对抗对齐与知识蒸馏技术，有效解决了多模态联邦学习中的异构性与对齐难题，提升了全局与个性化模型质量。
---

## 摘要
多模态联邦学习（MFL）使具有异构数据模态的客户端能够在不共享原始数据的情况下协作训练模型，提供了一个利用互补跨模态信息的隐私保护框架。然而，现有方法往往忽视了客户端的个性化性能，且在处理模态/任务差异以及模型异构性方面面临挑战。为了应对这些挑战，我们提出了 FedAFD，这是一个旨在增强客户端和服务器端学习的统一 MFL 框架。在客户端，我们引入了一种双层对抗对齐策略，用于在模态内和模态间对齐局部与全局表示，从而缓解模态和任务差距。我们进一步设计了一个粒度感知融合模块，以自适应地将全局知识整合到个性化特征中。在服务器端，为了处理模型异构性，我们提出了一种相似度引导的集成蒸馏机制，该机制基于特征相似度在共享公共数据上聚合客户端表示，并将融合后的知识蒸馏到全局模型中。在 IID 和非 IID 设置下进行的大量实验表明，FedAFD 在客户端和服务器端均实现了卓越的性能和效率。

## Abstract
Multimodal Federated Learning (MFL) enables clients with heterogeneous data modalities to collaboratively train models without sharing raw data, offering a privacy-preserving framework that leverages complementary cross-modal information. However, existing methods often overlook personalized client performance and struggle with modality/task discrepancies, as well as model heterogeneity. To address these challenges, we propose FedAFD, a unified MFL framework that enhances client and server learning. On the client side, we introduce a bi-level adversarial alignment strategy to align local and global representations within and across modalities, mitigating modality and task gaps. We further design a granularity-aware fusion module to integrate global knowledge into the personalized features adaptively. On the server side, to handle model heterogeneity, we propose a similarity-guided ensemble distillation mechanism that aggregates client representations on shared public data based on feature similarity and distills the fused knowledge into the global model. Extensive experiments conducted under both IID and non-IID settings demonstrate that FedAFD achieves superior performance and efficiency for both the client and the server.

---

## 论文详细总结（自动生成）

这是一份关于论文《FedAFD: Multimodal Federated Learning via Adversarial Fusion and Distillation》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对**多模态联邦学习（MFL）**中普遍存在的三个核心挑战提出了解决方案：
*   **模态与任务差异（Modality and Task Gaps）：** 不同客户端可能持有不同模态的数据（如仅图像、仅文本或图文对）并执行不同任务（分类或检索），导致特征空间不一致。
*   **本地个性化性能被忽视：** 现有方法往往为了提升全局模型性能而牺牲了客户端在本地任务上的表现。
*   **模型异构性（Model Heterogeneity）：** 客户端由于硬件限制，其模型架构（如 ResNet-18 vs. ResNet-101）往往不统一，难以进行传统的参数聚合。

**整体含义：** FedAFD 旨在构建一个统一的框架，通过对抗对齐、特征融合和知识蒸馏技术，在保护隐私的前提下，同时提升异构客户端的本地性能和云端全局模型的泛化能力。

### 2. 方法论：核心思想与关键技术
FedAFD 包含三个关键模块，贯穿客户端训练与服务端聚合的全过程：

*   **双层对抗对齐（Bi-level Adversarial Alignment, BAA）：**
    *   **核心思想：** 将联邦学习视为领域自适应问题，将客户端视为源域，服务端视为目标域。
    *   **技术细节：** 每个客户端配备两个判别器：**模态内判别器**（区分本地与全局同模态特征）和**跨模态判别器**（区分本地特征与全局另一模态特征）。通过对抗损失（Adversarial Loss）强制本地编码器生成与全局表示分布一致的特征，从而缓解模型漂移。
*   **粒度感知特征融合（Granularity-aware Feature Fusion, GFF）：**
    *   **核心思想：** 在样本层面动态平衡本地个性化知识与全局通用语义。
    *   **技术细节：** 引入注意力门控机制，自适应地融合本地编码器提取的特征和全局编码器提取的特征。通过两步注意力精炼，生成最终的融合特征用于本地任务。
*   **相似度引导的集成蒸馏（Similarity-guided Ensemble Distillation, SED）：**
    *   **核心思想：** 在服务端利用公共数据集，根据语义一致性对客户端贡献进行加权聚合。
    *   **技术细节：** 服务端计算客户端上传的公共数据表示与全局表示之间的余弦相似度得分，以此作为权重进行加权聚合，随后通过 $L_2$ 蒸馏损失更新全局模型。

### 3. 实验设计
*   **数据集：**
    *   **本地客户端：** CIFAR-100（图像分类）、AGNEWS（文本分类）、Flickr30k（图文检索）。
    *   **服务端/公共数据：** MS-COCO（图文检索）。
*   **场景设置：** 包含 IID（独立同分布）和 Non-IID（非独立同分布，使用 Dirichlet 分布 $\alpha=0.1$ 模拟标签偏移）。
*   **Benchmark 与对比方法：**
    *   对比了 6 种 SOTA 方法：FedMD, FedGEMS, FedET, CreamFL, FedMKD, FedDFA。
    *   包含 "LOCAL" 基准（即客户端独立训练，不进行联邦协作）。

### 4. 资源与算力
*   **模型架构：** 图像端使用 ResNet-18/ResNet-101，文本端使用 GRU/BERT-base，多模态框架采用 PCME。
*   **超参数：** 通信轮数 40 轮，每轮本地训练 5 个 Epoch；Batch Size 分别为 256（单模态）和 128（多模态）。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但从模型规模（BERT, ResNet-101）和实验轮数来看，该实验需要中等规模的 GPU 集群支持。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 IID 和 Non-IID 两种主流联邦学习环境，并在 4 个不同任务的数据集上进行了验证。
*   **消融实验：** 针对 BAA、GFF、SED 三个核心模块分别进行了剔除实验，验证了各部分的有效性。
*   **其他分析：** 进行了公共数据规模影响分析、通信效率分析（收敛轮数对比）以及基于 t-SNE 的特征可视化分析。
*   **客观性：** 实验设计较为全面，通过可视化手段直观展示了特征对齐的效果，对比方法涵盖了近年来主要的基于蒸馏的联邦学习框架，具有较高的说服力。

### 6. 主要结论与发现
*   **性能卓越：** FedAFD 在所有任务（图像分类、文本分类、跨模态检索）中均优于现有方法，尤其是在 Non-IID 环境下提升显著。
*   **双赢局面：** 与传统方法牺牲本地性能换取全局性能不同，FedAFD 显著提升了客户端的本地准确率（如在 CIFAR-100 上比 CreamFL 高出约 11%）。
*   **收敛更快：** 达到相同性能水平所需的通信轮数更少，证明了该框架的高效性。

### 7. 优点（亮点）
*   **统一框架：** 首次在一个框架内同时解决了跨模态/任务对齐、个性化性能提升和架构无关的模型聚合。
*   **对抗对齐思路新颖：** 将联邦学习中的模态差异转化为领域自适应问题，利用双层判别器实现了深度的语义对齐。
*   **自适应融合：** GFF 模块允许客户端根据样本特性灵活吸收全局知识，有效解决了“灾难性遗忘”或“过度泛化”的问题。

### 8. 不足与局限
*   **公共数据依赖：** 该方法高度依赖于一个高质量且与任务相关的**公共数据集**（MS-COCO），在某些极端隐私或垂直领域，获取此类公共数据可能存在困难。
*   **计算开销：** 客户端需要同时运行本地编码器、全局编码器以及两个判别器，且涉及对抗训练，对边缘设备的计算能力和内存有一定要求。
*   **通信负载：** 虽然论文提到通信效率高，但客户端需要上传公共数据的特征表示，当公共数据集较大时，每轮通信的数据量会随之增加。

（完）
