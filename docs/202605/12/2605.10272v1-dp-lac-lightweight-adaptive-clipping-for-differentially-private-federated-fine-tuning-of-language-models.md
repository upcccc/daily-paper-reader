---
title: "DP-LAC: Lightweight Adaptive Clipping for Differentially Private Federated Fine-tuning of Language Models"
title_zh: DP-LAC：用于语言模型差分隐私联邦微调的轻量级自适应裁剪
authors: "Haaris Mehmood, Jie Xu, Karthikeyan Saravanan, Rogier Van Dalen, Mete Ozay"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10272v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 大模型联邦微调中DP-SGD的自适应裁剪
tldr: "针对联邦学习中大语言模型微调的隐私保护问题，本文提出DP-LAC方法。该方法通过私有直方图估计初始梯度裁剪阈值，并在训练过程中无需额外隐私预算或超参数即可动态调整阈值。实验证明，DP-LAC在保证差分隐私的同时，比现有自适应裁剪方法和标准DP-SGD平均准确率提升了6.6%，有效解决了超参数调优带来的隐私损耗问题。"
selection_source: fresh_fetch
motivation: 现有的差分隐私自适应裁剪技术通常需要复杂的超参数调优，这会消耗额外的隐私预算并降低模型性能。
method: 提出一种轻量级方法，先利用私有直方图估计初始裁剪阈值，再在训练中无需额外开销地动态调整该阈值。
result: "在联邦微调任务中，DP-LAC相比SOTA自适应裁剪方法和标准DP-SGD，平均准确率提升了6.6%。"
conclusion: DP-LAC通过高效的阈值自适应机制，在不增加隐私成本的前提下显著提升了联邦学习下大语言模型的微调效果。
---

## 摘要
联邦学习（FL）能够在保持用户数据留在设备上的同时，跨边缘设备协作训练大规模语言模型（LLM）。然而，FL 仍会通过客户端提供的梯度泄露敏感信息。差分隐私随机梯度下降（DP-SGD）通过将每个客户端的贡献裁剪至阈值 $C$ 并添加与 $C$ 成比例的噪声来缓解这一风险。现有的自适应裁剪技术虽然能动态调整 $C$，但需要繁琐的超参数调优，这可能会消耗隐私预算。在本文中，我们提出了 DP-LAC，该方法首先利用隐私直方图估计在最优值的数量级范围内估计初始裁剪阈值，然后在训练过程中调整该阈值，且不消耗额外的隐私预算或引入新的超参数。实验结果表明，DP-LAC 优于最先进的自适应裁剪方法和原始 DP-SGD，平均准确率提升了 $6.6\%$。

## Abstract
Federated learning (FL) enables the collaborative training of large-scale language models (LLMs) across edge devices while keeping user data on-device. However, FL still exposes sensitive information through client-provided gradients. Differentially private stochastic gradient descent (DP-SGD) mitigates this risk by clipping each client's contribution to a threshold $C$ and adding noise proportional to $C$. Existing adaptive clipping techniques dynamically adjust $C$ but demand tedious hyperparameter tuning, which can erode the privacy budget. In this paper, we introduce DP-LAC, a method that first estimates an initial clipping threshold within an order of magnitude of the optimum using private histogram estimation, and then adapts this threshold during training without consuming additional privacy budget or introducing new hyperparameters. Empirical results show that DP-LAC outperforms both state-of-the-art adaptive clipping methods and vanilla DP-SGD, achieving an average accuracy gain of $6.6\%$.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DP-LAC** 的新方法，旨在解决大语言模型（LLM）在差分隐私联邦学习（DP-FL）微调过程中，梯度裁剪阈值难以设定的核心痛点。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在差分隐私随机梯度下降（DP-SGD）中，**裁剪阈值 $C$** 的选择至关重要。若 $C$ 过大，注入的噪声会淹没信号；若 $C$ 过小，则会引入巨大的梯度偏差。
*   **研究动机**：
    *   现有的自适应裁剪方法通常引入额外的超参数（如裁剪学习率），这些超参数本身需要调优。
    *   **隐私损耗**：在 DP 环境下，任何超参数调优都会消耗隐私预算（Privacy Budget）。根据现有理论，调优过程可能导致总隐私支出增加。
    *   **效率低下**：在大模型场景下，反复实验寻找最优 $C$ 值的计算成本极高。

### 2. 方法论
DP-LAC 的核心思想是**“轻量级且无额外超参数的自适应”**，主要包含两个阶段：
*   **初始阈值估计（Initial $C_0$ Estimation）**：
    *   在第一轮训练中，客户端计算本地伪梯度范数。
    *   客户端在本地模拟不同裁剪阈值下的噪声效果，选择使损失函数最小的候选阈值索引。
    *   通过**差分隐私直方图（Private Histogram）**机制，服务器汇总各客户端的选择，取众数作为初始阈值 $C_0$。这一步仅在第一轮执行，保证了初始值的合理性。
*   **在线自适应更新（Online Adaptation）**：
    *   **核心公式**：$C_t = \min(1, v_{t-1}/v_{t-2}) \cdot C_{t-1}$。
    *   其中 $v$ 是服务器端公共验证集的损失值。其逻辑是：随着模型收敛，验证集损失下降，梯度范数通常也会减小，因此同步缩小 $C$ 可以降低噪声干扰。
*   **变体 DP-CLAC**：若无公共验证集，则分配 1/3 的隐私预算用于私有化聚合客户端的本地训练损失，以此指导 $C$ 的更新。

### 3. 实验设计
*   **数据集与任务**：
    *   **分类任务**：GLUE 基准测试中的 SST-2、QNLI、MNLI。
    *   **生成任务**：SAMSum 摘要提取。
*   **模型**：TinyLlama-1B 和 Qwen3-4B。
*   **对比方法（Benchmarks）**：
    *   标准 DP-FedAvg（固定裁剪）。
    *   梯度归一化（Gradient Normalization）。
    *   基于分位数的自适应裁剪（Andrew et al.）。
    *   噪声倍数衰减和裁剪阈值衰减等 SOTA 方法。
*   **隐私设置**：设置了强（$\epsilon=2$）、中（$\epsilon=4$）、弱（$\epsilon=8$）三种隐私强度。

### 4. 资源与算力
*   **硬件环境**：使用了 **5 台 80GB GPU**（型号未明确，推测为 A100 或 H100）。
*   **并行设置**：采用 20 个并行工作站（Workers）。
*   **训练时长**：单个实验（$\tau$）在不同数据集上耗时 **7.25 到 11 小时**不等。
*   **效率提升**：DP-LAC 在超参数网格搜索速度上比传统方法快 **5-15 倍**，因为它消除了对 DP 相关超参数的调优需求。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个数据集、2 种模型架构、3 种隐私等级。
*   **消融与鲁棒性**：测试了不同的 LoRA Rank（4, 8, 16, 32）对结果的影响。
*   **公平性处理**：在对比实验中，作者遵循了“隐私调优成本”原则，将基线方法的隐私预算缩减为 $\epsilon/3$ 以补偿其调优消耗，而 DP-LAC 使用全量预算，这符合实际部署场景。

### 6. 主要结论与发现
*   **性能优异**：DP-LAC 相比现有最先进方法，平均准确率提升了 **6.6%**。
*   **初始值准确**：通过私有直方图估计的 $C_0$ 与非私有环境下的最优 $C^*$ 在数量级上高度一致。
*   **任务适应性**：在更复杂的任务（如 MNLI 和 SAMSum）以及更严格的隐私限制（$\epsilon=2$）下，DP-LAC 的领先优势更明显。

### 7. 优点
*   **隐私友好**：避免了因调优 DP 超参数而导致的额外隐私泄露。
*   **即插即用**：无需为不同的模型或数据集手动调整裁剪策略，降低了工程门槛。
*   **通信开销极低**：仅需传输一个 one-hot 向量或标量损失值，对带宽几乎无影响。

### 8. 不足与局限
*   **依赖验证集**：主方法依赖于服务器端拥有一个代表性的公共验证集。虽然提出了 DP-CLAC 变体作为补充，但后者会占用一部分隐私预算，导致性能略有下降。
*   **模态局限**：目前仅在 NLP 任务上进行了验证，尚未在计算机视觉或多模态任务中测试。
*   **首轮开销**：第一轮的直方图估计增加了客户端的本地计算负担（需要模拟多次噪声损失）。

（完）
