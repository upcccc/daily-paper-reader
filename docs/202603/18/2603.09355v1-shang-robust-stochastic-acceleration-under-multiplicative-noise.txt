Title: SHANG++: Robust Stochastic Acceleration under Multiplicative Noise

URL Source: https://arxiv.org/pdf/2603.09355v1

Published Time: Wed, 11 Mar 2026 00:50:35 GMT

Number of Pages: 33

Markdown Content:
# SHANG++: Robust Stochastic Acceleration under Multiplicative Noise 

Yaxin Yu a, Long Chen b, ∗, Minfu Feng a

> aSchool of Mathematics, Sichuan University, Chengdu, Sichuan, 610065, China
> bDepartment of Mathematics, University of California, Irvine, Irvine, CA, 92697, USA

Abstract 

Under the multiplicative noise scaling (MNS) condition, original Nesterov acceleration is provably sensitive to noise and may diverge when gradient noise overwhelms the signal. In this paper, we develop two accelerated stochastic gradient descent methods by discretizing the Hessian-driven Nesterov accel-erated gradient flow. We first derive SHANG, a direct Gauss–Seidel-type discretization that already improves stability under MNS. We then introduce SHANG++, which adds a damping correction and achieves faster convergence with stronger noise robustness. We establish convergence guarantees for both convex and strongly convex objectives under MNS, together with explicit parameter choices. In our experiments, SHANG++ performs consistently well across convex problems and applications in deep learning. In a dedicated noise experiment on ResNet-34, a single hyperparameter configuration attains accu-racy within 1% of the noise-free setting. Across all experiments, SHANG++ outperforms existing accelerated methods in robustness and efficiency, with minimal parameter sensitivity. 

Keywords: Stochastic gradient descent, Momentum method, Acceleration, Multiplicative noise scaling, Convergence analysis 

1. Introduction 

Empirical Risk Minimization (ERM) is central to modern large-scale ma-chine learning, including deep neural networks and reinforcement learning [ 1].       

> ∗Corresponding author.
> Email addresses: yaxin_yu@stu.scu.edu.cn (Yaxin Yu), lchen7@uci.edu (Long Chen), fmf@scu.edu.cn (Minfu Feng)
> arXiv:2603.09355v1 [math.OC] 10 Mar 2026

Given a large dataset {(Xi, Y i)}Ni=1 , where Yi denotes the label of data Xi and 

N ≫ 1, the training objective is 

min  

> x

f (x), f (x) = 1

N

> N

X

> i=1

fi(x), (1) where x denotes the network parameters and fi(x) is the loss associated with sample (Xi, Y i). We use x instead of θ for consistency with the optimization formulation. Efficiently computing the minimizer x⋆ = arg min x f (x) is critical for training neural network with large data. Exact gradient evaluation is expensive. At iteration k, Stochastic Gradient Descent (SGD) draws a mini-batch Bk ⊂ { 1, . . . , N } of size M uniformly at random (with or without replacement), independently of the past, and forms 

g(xk) = 1

M

X

> i∈Bk

∇fi(xk). (2) Let Fk := σ(x0, B 0, . . . , B k−1) be the σ-algebra generated by the random variables {x0, B 0, . . . , B k−1}. Then g(xk) is conditionally unbiased: E[g(xk) |Fk] = ∇f (xk), where all randomness is taken with respect to a common underlying probability space. The mini-batch estimator g(x) reduces the cost of computing ∇f (x) but introduces noise. In regimes such as small-batch training or highly over-parameterized models, the variance can scale with and even dominate the signal ∥∇ f (x)∥2. This effect is modeled by the multiplicative-noise scaling (MNS) condition [ 2, 3, 4]. The work in [ 5] further shows that multiplica-tive noise induces geometric distortions in the loss landscape, beyond the smoothing effects of additive noise. 

Definition 1.1 (Multiplicative Noise Scaling (MNS)) . The stochastic gradient estimator g(x) satisfies the MNS condition if there exists σ ≥ 0 such that 

E ∥g(x) − ∇ f (x)∥2 ≤ σ2∥∇ f (x)∥2. (3) SGD slows down when the condition number of f is large. Momentum methods such as Heavy-Ball (HB) [ 6] and Nesterov accelerated gradient (NAG) [ 7] are widely used to accelerate convergence. In training deep neural networks, Adam (Adaptive Moment Estimation) [ 8] is a widely used optimiza-tion algorithm that combines momentum and adaptive step sizes for fast and stable convergence. 2Accelerated variants of SGD have been extensively studied. However, momentum methods are highly sensitive to stochastic noise [ 9, 10 , 11 ], and stability depends critically on parameter choices [ 12 , 13 , 14 , 15 ]. Gupta et al. [ 4] showed that under MNS with σ ≥ 1, NAG fails to converge even in convex and strongly convex settings. In practice, the apparent benefits of momentum largely arise from large mini-batches, which can reduce the variance and make the dynamics closer to the deterministic regime. To address these issues, a series of corrections have been developed. Fol-lowing [ 16 ], many accelerated stochastic algorithms have been proposed [ 13 ,17 , 18 , 19 , 20 , 4, 21 ], aiming to retain acceleration while improving robustness to noise. Vaswani et al. [17] introduced a four-parameter NAG variant with optimal accelerated rates; Liu and Belkin [ 13 ] proposed the Mass method with a three-parameter correction, though acceleration was proved only for over-parameterized linear models; Gupta et al. [ 4] developed AGNES with guarantees matching [ 17 ]; and Hermant et al. [ 21 ] analyzed SNAG, a four-parameter variant in Nesterov’s framework [ 22 ], showing similar rates under mild tuning. From the viewpoint of convex optimization theory, these algorithms are competitive. However, our deep-learning experiments show that they often lose acceleration under high noise and can perform worse than SGD even with recommended hyperparameters. For example, on CIFAR-100 with ResNet-50 and batch size 50 , SGD attains 58 .33% test accuracy whereas AGNES reaches only 42 .82% (see Section 3). With smaller batches, both AGNES and SNAG exhibit strong oscillations and require additional hyperparameter tuning. Motivated by this gap, our goal is not only to design a new accelerated method, but to develop a complementary approach that (i) retains optimal theoretical guarantees, (ii) reduces tuning effort, and (iii) improves stabil-ity. Our contributions emphasize simplicity (fewer parameters), provable acceleration with explicit noise dependence, and robust empirical behavior. 1. We begin with SHANG, a stochastic extension of HNAG method [ 23 ]. Compared with classical NAG, the continuous-time dynamic underlying HNAG features the Hessian-driven term ∇2f (x)x′, which captures curvature-dependent damping and yields a more refined continuous-time model of NAG. SHANG inherits this structure and empirically exhibits improved robustness to stochastic gradient noise. 2. We then refine SHANG into SHANG++ by adding a correction term 

−m(xk+1 − xk) to the x-update, thereby relaxing the coupled stepsize 3scaling in SHANG. This extra degree of freedom helps compensate for the multiplicative-noise–induced rescaling of the effective constants μ and 

L, enabling more flexible parameter choices and improved convergence guarantees under MNS. Empirically, SHANG++ also exhibits improved robustness to stochastic gradient noise. The two “+” signs thus highlight both faster convergence and enhanced noise robustness. 3. We evaluate SHANG/SHANG++ on convex optimization, image classifi-cation, and generative modeling tasks (MNIST, CIFAR-10, CIFAR-100). SHANG++ outperforms existing accelerated methods NAG, SNAG, and AGNES, with clear advantages under high multiplicative noise, and is competitive with Adam. 4. We further examines robustness to multiplicative noise. For realistic noise levels ( σ ≤ 0.5), SHANG++ retains near noise-free accuracy (within 1% degradation), demonstrating that stability can be achieved with fewer parameters and a simpler design than earlier corrections such as AGNES and SNAG. 

Notation. Throughout this paper, we denote by ⟨· , ·⟩ the standard Euclidean inner product in Rd, and by ∥ · ∥ its induced norm. Let x⋆ = arg min x f (x)

denote a global minimizer of f .Let f : Rd → R be differentiable. The Bregman divergence of f between 

x, y ∈ Rd is 

Df (y, x ) := f (y) − f (x) − ⟨∇ f (x), y − x⟩. (4) The function f is convex if Df (y, x ) ≥ 0 for all x, y ∈ Rd. Moreover, f is 

μ-strongly convex with parameter μ ≥ 0 if Df (y, x ) ≥ μ 

> 2

∥y − x∥2.Function f is L-smooth, for some L > 0, if its gradient is L-Lipschitz: 

∥∇ f (y) − ∇ f (x)∥ ≤ L∥y − x∥, ∀x, y ∈ Rd. (5) Let Sμ,L be the class of all differentiable functions that are both μ-strongly convex and L-smooth. For f ∈ S μ,L , the Bregman divergence satisfies 

μ

2 ∥x − y∥2 ≤ Df (x, y ) ≤ L

2 ∥x − y∥2, ∀x, y ∈ Rd. (6) Bregman divergence here is used purely as an analytical tool in the Lyapunov analysis. Parameters μ and L are treated as known hyperparameters for the given problem. Their adaptivity is beyond the scope of this work. 4The remainder of the paper is organized as follows. We present SHANG and SHANG++ in Section 2. Experimental results are reported in Section 3. The complete convergence analysis is given in Section 4. and Section 5 concludes the paper. 

2. Stochastic Hessian-driven Accelerated Nesterov Gradient 

2.1. HNAG Flow 

To accelerate gradient descent, Polyak introduced momentum, which incorporates information from previous iterates and is modeled by the Heavy-Ball (HB) flow [6]: 

x′′ + θx ′ + η∇f (x) = 0 . (7) However, the discrete HB method xk+1 = xk − γ∇f (xk) + β(xk − xk−1) may not converge; see [24, 25] for non-convergent examples. In this work, we build on the second-order dynamical system introduced in [ 26 , 23 ], known as the Hessian-driven Nesterov Accelerated Gradient (HNAG) flow: 

γx ′′ + ( γ + μ)x′ + βγ ∇2f (x)x′ + (1 + μβ )∇f (x) = 0 , (8) where β > 0 is a parameter and γ is a time-scaling function. Compared with the classical HB flow (7) , the additional Hessian-driven term ∇2f (x)x′

captures how the local geometry of f affects the damping strength of the dynamics. As shown in [ 26 ], this curvature aware mechanism provides a more accurate continuous-time description of NAG. The second-order ODE (8) can be equivalently reformulated as the first-order system: 

x′ = v − x − β∇f (x), v′ = μγ (x − v) − 1

γ ∇f (x), γ′ = μ − γ, (9) which removes the explicit dependence on ∇2f (x).

2.2. SHANG 

Applying a Gauss-Seidel-type scheme to discretize the first-order HNAG system (9) and replacing deterministic gradients with unbiased stochastic estimates, we obtain the Stochastic Hessian-driven Nesterov Accelerated Gradient (SHANG) method: 

xk+1 − xk

αk

= vk − xk+1 − βkg(xk) (10a) 5vk+1 − vk

αk

= μγk

(xk+1 − vk+1 ) − 1

γk

g(xk+1 ) (10b) 

γk+1 − γk

αk

≤ μ − γk+1 (10c) with given x0, v0 and γ0. For the time-scaling variable, we use the one-sided schedule condition (10c), which enlarges the admissible choices of {γk}.In the strongly convex case, we fix γk ≡ μ and take a constant step size 

αk ≡ α. In general case, we set μ = 0 and allow both αk and γk to vary; in particular, (10c) implies {γk} is a decreasing sequence. The coupling βk > 0

depends on (αk, γ k) and typically scales as (1 + σ2)αk/γ k. Consequently, SHANG reduces to a one-parameter scheme (α) in the strongly convex regime and a two-parameter scheme (αk, γ k) otherwise. For ease of analysis, we introduce an auxiliary variable 

x+ := x − αβg (x), (11) which is one SGD step at x with step size αβ . With this notation, the update of xk+1 (10a) can be written equivalently as: 

xk+1 − x+

> k

αk

= vk − xk+1 (12) The explicit appearance of x+ 

> k

is only for analysis and does not affect the algorithm itself. As E[g(x)] = ∇f (x), MNS condition (3) implies 

E∥g(x)∥2 ≤ (1 + σ2) ∥∇ f (x)∥2. (13) Under the MNS assumption, this setup of auxiliary variable x+ yields the following descent lemma for smooth objectives. 

Lemma 2.1. Suppose that f is L-smooth and 0 < αβ ≤ 1 

> L(1+ σ2)

. Let g(x) be defined by (2) satisfying the MNS condition (3) and x+ given by (11) . Then we have 

Ef (x+) ≤ f (x) − αβ 

2 ∥∇ f (x)∥2.

Proof. Using (6), we obtain 

f (x+) ≤ f (x) + ⟨∇ f (x), x + − x⟩ + L

2 ∥x+ − x∥2

= f (x) − αβ ⟨∇ f (x), g (x)⟩ + Lα 2β2

2 ∥g(x)∥2.

6Taking expectation and using (13), we have 

Ef (x+) ≤ f (x) − αβ ∥∇ f (x)∥2 + Lα 2β2(1 + σ2)2 ∥∇ f (x)∥2

≤ f (x) − αβ 

2 ∥∇ f (x)∥2,

where the last step follows from 0 < αβ ≤ 1 

> L(1+ σ2)

and Lemma 4.1. Define the augmented variables zk := ( xk, v k), z+ 

> k

:= ( x+ 

> k

, v k), z⋆ := ( x⋆, x ⋆)

and the discrete Lyapunov function 

E(z+ 

> k

; γk) = f (x+ 

> k

) − f (x⋆) + γk

2 ∥vk − x⋆∥2 = DE (z+ 

> k

, z ⋆; γk) (14) The following theorem establishes decay for E E(z+

> k+1

; γk+1 ).

Theorem 2.1. Let f ∈ S μ,L and let g(x) be defined by (2) satisfying the MNS condition (3) . Starting from x0 = v0, generate (xk, v k) by (10) and define x+ 

> k

by (11). Set βk = (1 + σ2)αk/γ k.(1) When 0 < μ < L < ∞, take αk ≡ α and γk ≡ μ. If 0 < α ≤ 11+ σ2

p μL ,then for all k ≥ 0,

E

h

f (x+

> k+1

) − f (x⋆) + μ

2 ∥vk+1 − x⋆∥2

i

≤ (1 + α)−(k+1) E(z+0 ; μ).

(2) When μ = 0 , set αk = 2 

> k+1

and γk = α2

> k

(1 + σ2)2L, then for all k ≥ 0,

E

h

f (x+

> k+1

) − f (x⋆) + γk+1 

2 ∥vk+1 − x⋆∥2

i

≤ 2(k + 2)( k + 3) E(z+0 ; γ0).

We provide a proof outline for Theorem 2.1; full details are deferred to Section 4.1. Given current approximation (xk, v k) and g(xk), the quantities x+

> k

and xk+1 are deterministic, while randomness is introduced through g(xk+1 )

and consequently affects (x+

> k+1

, v k+1 ). In one step, the expectation E is with respect to the randomness in g(xk+1 ).

Proof outline. First of all, we insert the intermediate term E(zk+1 ; γk+1 ) and write E E(z+

> k+1

; γk+1 ) − E (z+ 

> k

; γk) as 

E E(z+

> k+1

; γk+1 ) − E (zk+1 ; γk+1 ) + EE(zk+1 ; γk+1 ) − E (z+ 

> k

; γk) . (15) 7By Lemma 2.1 with αk+1 βk+1 = αkβk = α2  

> k(1+ σ2)
> γk

≤ 1(1+ σ2)L , we have 

E E(z+

> k+1

; γk+1 ) − E (zk+1 ; γk+1 ) ≤ − α2

> k

(1 + σ2)2γk

∥∇ f (xk+1 )∥2. (16) That is, by changing xk+1 to x+

> k+1

, the Lyapunov function has sufficiently decay. Using the definition of Bregman divergence and the choice of γk, we can split the second component in (15) as 

E(zk+1 ; γk+1 ) − E (z+ 

> k

; γk) = DE (zk+1 , z ⋆; γk+1 ) − DE (z+ 

> k

, z ⋆; γk)= DE (zk+1 , z ⋆; γk) − DE (z+ 

> k

, z ⋆; γk) + γk+1 − γk

2 ∥vk+1 − x⋆∥2

≤ ⟨∇E (zk+1 ; γk), z k+1 − z+ 

> k

⟩ − DE (z+ 

> k

, z k+1 ; γk) + αk(μ − γk+1 )2 ∥vk+1 − x⋆∥2

(17) Expanding the first term and using the update rule (10) gives 

⟨∇E (zk+1 ; γk), z k+1 − z+ 

> k

⟩

= − αk⟨∇ f (xk+1 ) − ∇ f (x⋆), x k+1 − x⋆⟩ + αk⟨g(xk+1 ), v k − vk+1 ⟩− αkμ

2 ∥vk+1 − x⋆∥2 − αkμ

2 ∥xk+1 − vk+1 ∥2 + αkμ

2 ∥xk+1 − x⋆∥2

+ αk⟨∇ f (xk+1 ) − g(xk+1 ), v k − x⋆⟩.

(18) After taking the expectation, the last term vanishes, i.e., E[⟨∇ f (xk+1 ) −

g(xk+1 ), v k − x⋆⟩] = 0 . The positive term αk μ 

> 2

∥xk+1 − x⋆∥2 can be canceled by the strong convexity implied by half of the first term −αk⟨∇ f (xk+1 ) −∇f (x⋆), x k+1 − x⋆⟩ = −αk(Df (xk+1 , x ⋆) + Df (x⋆, x k+1 )) .The most difficult term is the expectation of the cross term, as both 

g(xk+1 ) and vk+1 are random variables. We use Cauchy-Schwarz and Young’s inequalities to separate them: 

αk⟨g(xk+1 ), v k − vk+1 ⟩ ≤ α2

> k

2γk

∥g(xk+1 )∥2 + γk

2 ∥vk − vk+1 ∥2 (19) The positive term γk 

> 2

∥vk − vk+1 ∥2 will be canceled by −DE (z+ 

> k

, z ⋆; γk). The positive term 

α2

> k

2γk

E ∥g(xk+1 )∥2 ≤ α2

> k

(1 + σ2)2γk

∥∇ f (xk+1 )∥2,

8will be canceled by the negative term in (16). The remaining negative term can be merged to −αkE(zk+1 ; γk+1 ) which is bounded by −αkE(z+

> k+1

; γk+1 ). Rearranging this term, we get the contraction 

E E(z+

> k+1

; γk+1 ) ≤ 11 + αk

E(z+ 

> k

; γk).

When σ = 0 , SHANG reduces to the deterministic HNAG method analyzed in [ 23 ]. As σ grows, convergence slows but acceleration is preserved. Theorem 2.1 shows that f converges in expectation to the global minimum f (x⋆), i.e., 

lim  

> k→∞

E|f (x+ 

> k

) − f (x⋆)| = 0 ,

which yields an immediate corollary: it not only converges in expectation, but also converges almost surely (a.s.) to f (x⋆). Specifically, as k becomes sufficiently large, the value of f (x+ 

> k

) approaches f (x⋆) with probability 1.Corollary 2.1 follows from Theorem 2.1 by Markov’s inequality and the Borel-Cantelli lemma; see Section 4.3 for details. 

Corollary 2.1. In the setting of Theorem 2.1, f (x+ 

> k

) a.s. 

→ f (x⋆).

2.3. SHANG++ 

In SHANG, the x- and v-updates are advanced with the same stepsize αk,which couples the coefficients in the two equations. While this shared scaling is natural from the underlying dynamics, allowing additional flexibility can be beneficial under MNS, where the effective constants are rescaled. Motivated by this, we use asymmetric stepsize scalings: a reduced effective stepsize ˜αk in the x-update while keeping αk in the v-update; the time-scaling {γk} will be specified later. This yields a faster and more noise-robust Stochastic Hessian-driven Nesterov Accelerated Gradient (SHANG++) method: 

xk+1 − xk

˜αk

= vk − xk+1 − βkg(xk) (20a) 

vk+1 − vk

αk

= μγk

(xk+1 − vk+1 ) − 1

γk

g(xk+1 ) (20b) 9initialized with x0 and v0, where ˜αk := αk 

> 1+ mα k

with parameter m ≥ 0. Equiv-alently, (20a) can be rewritten as 

xk+1 − xk

αk

= vk − xk+1 − m(xk+1 − xk) − βkg(xk), (21) which shows that SHANG++ augments the x-update of SHANG (10a) by an additional correction term −m(xk+1 − xk). The parameter m controls the strength of this correction, and m = 0 recovers SHANG. The notation “++” emphasizes that this extra degree of freedom yields stronger convergence guarantees and, empirically, improved robustness to stochastic gradient noise. 

2.3.1. SHANG++ for Strongly Convex Minimization 

Assume f ∈ S μ,L with 0 < μ < L < ∞. We consider SHANG++ with constant parameters γk ≡ μ and αk ≡ α, and choose m = 1 , so that ˜α = α 

> 1+ α

.Define the auxiliary variable 

x+ 

> k

:= xk − ˜αβg (xk). (22) Then SHANG++ (20) can be rewritten in the following equivalent form: 

xk+1 − x+

> k

˜α = vk − xk+1 (23a) 

vk+1 − vk

α = xk+1 − vk+1 − 1 

> μ

g(xk+1 ) (23b) initialized with x0 and v0.

Theorem 2.2. Let f ∈ S μ,L and let g(x) be defined by (2) satisfying the MNS condition (3) . Starting from x0 = v0, generate (xk, v k) by (23) and define x+ 

> k

by (22) . Choose α = ˜α  

> 1−˜α

and β = ˜αμ/ (1+ σ2) with 0 < ˜α ≤ 11+ σ2

p μL .Then for all k ≥ 0,

E

h

f (x+

> k+1

) − f (x⋆) + μ

2 ∥vk+1 − x⋆∥2

i

≤ (1 − ˜α)k+1 E(z+0 ; μ).

We provide a proof outline of Theorem 2.2 and defer the full proof to Sec-tion 4.2. Theorem 2.2 shows that E[f (x+

> k+1

) − f (x⋆)] contracts linearly at rate 

O



(1 − 11+ σ2

pμ/L )k+1 



, slightly better than O



(1 + 11+ σ2

pμ/L )−(k+1) 



ob-tained in Theorem 2.1. 10 In the deterministic case σ = 0 , choosing m = βμ reduces SHANG++ to the HNAG++ method in [ 27 ]. Note that βμ = ˜α ≤ pμ/L ≪ 1, Theorem 2.1 therefore covers a strictly broader range of correction strengths: beyond the specific choice m = βμ used in HNAG++, it allows larger values of m (up to 

m = 1 in our optimal setting), providing additional flexibility in both tuning and analysis. 

Proof outline. We introduce E(zk+1 ; μ) as an intermediate quantity. Assume 

˜αβ ≤ 1(1+ σ2)L , which is equvialent to ˜α ≤ 1(1+ σ2)

pμ/L . Then Lemma 2.1 implies 

E[E(z+

> k+1

; μ) − E (zk+1 ; μ)] ≤ − ˜α2(1 + σ2)2μ ∥∇ f (xk+1 )∥2 (24) Expanding E(zk+1 ; μ) − E (z+ 

> k

; μ) = ⟨∇E (zk+1 ; μ), z k+1 − z+ 

> k

⟩ − DE (z+ 

> k

, z k+1 ; μ)

and using the update in (23) gives 

− ˜α⟨∇ f (xk+1 ) − ∇ f (x⋆), x k+1 − x⋆⟩ − αμ 

2 ∥vk+1 − x⋆∥2 − αμ 

2 ∥vk+1 − xk+1 ∥2

+ αμ 

2 ∥xk+1 − x⋆∥2 + α⟨g(xk+1 ), v k − vk+1 ⟩ − (α − ˜α)⟨g(xk+1 ), v k − x⋆⟩− Df (x+ 

> k

, x k+1 ) − μ

2 ∥vk − vk+1 ∥2 + ˜ α⟨∇ f (xk+1 ) − g(xk+1 ), v k − x⋆⟩

(25) As before, after taking expectations, the last term vanishes. To obtain a sharper analsysis, we use the identity of squares 

2⟨a, b ⟩ = ∥a∥2 + ∥b∥2 − ∥ a − b∥2

and update rule (23b) to express 

vk − x⋆ = (1 + α)( vk+1 − xk+1 ) + ( xk+1 − x∗) + αμ g(xk+1 ).

Then we expand the second cross term and obtain 

− (α − ˜α)⟨g(xk+1 ), v k − x⋆⟩ = −α ˜α⟨g(xk+1 ), v k − x⋆⟩

= − ˜αμ 

2 ∥vk − xk+1 ∥2 − α2 ˜α

2μ ∥g(xk+1 )∥2 + α(1 + α)μ

2 ∥vk+1 − xk+1 ∥2

− (α − ˜α)⟨g(xk+1 ), x k+1 − x⋆⟩

(26) 11 After taking expectations, the last term can be combined with the first term of (25) . The strong convexity then provides a negative contribution that cancels the ∥xk+1 − x⋆∥2 term. For the cross term α⟨g(xk+1 ), v k − vk+1 ⟩, instead of applying Cauchy-Schwarz, we use the update rule for vk+1 − vk (23b) and the identity of squares to expand 

α⟨g(xk+1 ), v k − vk+1 ⟩ = α2

2μ∥g(xk+1 )∥2 + μ

2 ∥vk − vk+1 ∥2 − α2μ

2 ∥vk+1 − xk+1 ∥2.

(27) Using α2 = α2 ˜α + α ˜α, we decompose 

α2

2μ∥g(xk+1 )∥2 = α2 ˜α

2μ ∥g(xk+1 )∥2 + α ˜α

2μ ∥g(xk+1 )∥2.

The first term cancels with the corresponding negative term in (26) . For the second term, taking expectation and invoking MNS yields α ˜α(1+ σ2)2μ ∥∇ f (xk+1 )∥2,which is then canceled by the negative term in the sufficient decay condition (24) , together with the additional negative term generated by E[−α(f (xk+1 ) −

f (x⋆))] ≤ E[−α(f (x+

> k+1

) − f (x⋆) − ˜αβ  

> 2

∥∇ f (xk+1 )∥2)] . Combining all the above estimates, we obtain the desired result. While Gupta et al. [ 4] interpret noise as inflating smoothness to (1 + σ2)L,our analysis shows it perturbs both smoothness and curvature, giving Lσ =(1 + σ2)L and μσ = μ/ (1 + σ2). Accordingly, the admissible stepsize scaling differs between SHANG and SHANG++: (SHANG) 0 < α ≤

r μσ

Lσ

, (SHANG++) 0 < α ≤ 11 − ˜α

r μσ

Lσ

.

The noise-damping term in SHANG++ further reduces the effective Lipschitz constant from Lσ to (1 − ˜α)Lσ and increases the effective strongly convex constant from μσ to μσ/(1 − ˜α), which helps explain the improved stability and convergence guarantees of SHANG++. 

2.3.2. SHANG++ Method for Convex Minimization 

Assume μ = 0 and m ≥ 0. To streamline the analysis, we introduce an effective time-scaling sequence {˜γk} satisfies γk 

> ˜γk

= αk 

> ˜αk

= 1 + mα k. We choose 

αk = 2

k + 1 , γk = αk ˜αk(1 + σ2)2L, βk = αk(1 + σ2)

γk

.

12 Equivalently, 

˜αk = 2

k + 1 + 2 m, ˜γk = ˜ α2

> k

(1 + σ2)2L, βk = ˜αk(1 + σ2)˜γk

.

With these schedules, SHANG++ (20) can be rewritten as 

xk+1 − xk

˜αk

= vk − xk+1 − βkg(xk) (28a) 

vk+1 − vk

˜αk

= − 1˜γk

g(xk+1 ) (28b) 

˜γk+1 − ˜γk

˜αk

≤ − ˜γk+1 (28c) initialized with x0, v0 and ˜γ0.

Theorem 2.3. Let f ∈ S 0,L and let g(x) be defined by (2) satisfying the MNS condition (3) . Starting from x0 = v0, generate (xk, v k) by (20) and define x+ 

> k

= xk − ˜αkβkg(xk). Assume m ≥ 0, αk = 2 

> k+1

, γk/(1 + σ2) = αk ˜αkLσ

and βk = αk  

> γk/(1+ σ2)

. Then for all k ≥ 0,

E



f (x+

> k+1

) − f (x⋆) + ˜γk+1 

2 ∥vk+1 − x⋆∥2



≤ (1 + 2 m)(2 + 2 m)(k + 2 + 2 m)( k + 3 + 2 m)E0

where E0 = E(z+0 ; ˜ γ0).The system (28) is exactly the SHANG recursion with (αk, γ k) replaced by 

( ˜αk, ˜γk). Consequently, the proof of Theorem 2.3 follows the same argument as Theorem 2.1 verbatim and is omitted; the stated rate is obtained by substituting ˜αk = 2 

> k+1+2 m

into the final bound. 

Corollary 2.2. Under the setting of Theorem 2.2 or 2.3, f (x+ 

> k

) a.s. 

→ f (x⋆).We next compare the stepsize-dependent scaling induced by the two methods in the convex setting: (SHANG) γk

1 + σ2 = α2

> k

Lσ, (SHANG++) γk

1 + σ2 = α2 

> k

· Lσ

1 + mα k

.

Thus SHANG++ effectively replaces Lσ by Lσ/(1+ mα k) in the scaling, which partially counteracts the σ2-dependent amplification in the admissible stepsize. In practice, this added flexibility can improve stability and performance; in our experiments, values of m in the range [0 , 1.5] provide a good trade-off. 13 Batching. Gradient noise can be reduced by increasing the mini-batch size 

M in (2) . If σ21 is the MNS constant for M = 1 , then σ2 

> M

= σ21 /M . Another approach is to average K independent gradient estimators, gK = 1

> K

PKi=1 gi,

which gives an effective MNS constant of σ2/K . Both strategies reduce noise at the cost of higher computation, and a straightforward analysis shows that averaging multiple estimates can accelerate convergence to some extent. 

2.4. SNAG as a Discretization of the HNAG Flow 

Under MNS, one of the most recent first-order stochastic methods designed to overcome the divergence of NAG and accelerate SGD is the Stochastic Nesterov Accelerated Gradient (SNAG) method [ 22 ] [ 21 ]. Its iteration reads: 

xk+1 = ˆ αk+1 xk + (1 − ˆαk+1 )vk+1 − ˆαk+1 s g (xk)

vk+1 = ˆβv k + (1 − ˆβ)xk − ηkg(xk) (29) where g(xk) is a stochastic gradient estimator, and ˆαk+1 , s, ˆβ, and ηk are parameters. By reparameterizing as ˆαk+1 = 11+ αk+1 , s = αk+1 βk+1 , ˆβ = 11+ αk+1 μγk+1 

, and 

ηk = 11+ αk+1 μγk+1  

> αk+1
> γk+1

, the SNAG scheme (29) becomes equivalent to the following update: xk+1 − xk

αk+1 

= vk+1 − xk+1 − βk+1 g(xk)

vk+1 − vk

αk+1 

= μγk+1 

(xk − vk+1 ) − 1

γk+1 

g(xk)

γk+1 − γk

αk+1 

≤ μ − γk+1 

(30) Hence, SNAG can be interpreted as a new discretization of the HNAG flow. By direct calculation, one can show the parameter tuning rules proposed for SNAG in [ 21 ] are compatible with those arising from discretizations of the HNAG flow. In our experiments, SNAG remains comparatively stable as the noise level increases, which is qualitatively consistent with this HNAG-based interpretation. 

3. Numerical Experiments 

The algorithm and convergence analysis in the previous section assume a convex objective. In modern deep learning, the training loss is typically 14 nonconvex, so we use SHANG++ as a practical optimizer rather than a method with guaranteed global rates. For deep learning experiments, we run SHANG++ with three explicit hyperparameters (α, γ, m ), and set μ = 0 together with the coupling β = α/γ ;see Algorithm 1. We implement the update with an index shift so that v is updated first. Fixing β = α/γ reduces tuning to (α, γ, m ) and worked reliably across tasks. While the convex theory suggests β = (1 + σ2)α/γ , in practice the effective noise level σ2 is difficult to estimate and can vary substantially over time; we therefore avoid explicit noise calibration. We also tested simple adaptive proxies for σ, but observed no consistent gains. 

Algorithm 1: SHANG++ for Deep Learning 

Input: Objective function f , initial point x0, step size α, time scaling factor γ, noise-damping m, , iteration horizon T .

k ← 1, v0 ← x0, x1 ← x0, ˜α ← α

> 1+ mα

while k ≤ T do 

gk ← 1

> M

P 

> i∈Bk

∇fi(xk) // stochastic gradient estimate 

vk ← vk−1 − αγ gk

xk+1 ← 11+ ˜ α xk + ˜α 

> 1+ ˜ α

vk − ˜α 

> 1+ ˜ ααγ

gk

k ← k + 1 

end return xT

Throughout this section, NAG denotes the stochastic variant of Nesterov’s accelerated gradient [ 7] (replacing ∇f (x) by g(x)). SNAG refers to the method of [ 21 ]. SHB denotes the stochastic heavy-ball method, i.e., SGD with momentum. 

3.1. Convex optimization 

We first consider the family of objective functions from [4]: 

fd : R → R, fd(x) = 

(|x|d, |x| < 1,

1 + d(|x| − 1) , else ,

for d ≥ 2, with gradient estimators g(x) = (1+ σZ )∇f (x), where Z ∼ N (0 , I d)

is a standard normal random variable. The functions fd belong to S0,L with 

L = d(d − 1) .15 Figure 1: Performance of different algorithms under varying noise levels. 

We compare SHANG and SHANG++ with SGD, NAG, AGNES [ 4], and SNAG [ 21 ] under σ ∈ { 0, 10 , 50 } and d ∈ { 4, 16 }. The parameters used follow their optimal choices for the convex case. All simulations are initialized at 

x0 = 1 , and expectations are averaged over 200 independent runs. In Figure 1, both SHANG and SHANG++ remain stable as the noise level 

σ increases, whereas NAG diverges under large noise. SHANG is generally very competitive, with SHANG++ showing consistently slightly better behavior than the other accelerated stochastic schemes. These results suggest that the proposed methods are reasonably robust to noisy gradients with modest tuning, while maintaining accelerated-like behavior in the high-noise regime. 

3.2. Classification Tasks on MNIST, CIFAR-10 and CIFAR-100 

We benchmark on three training tasks: LeNet-5 on MNIST [ 28 ], ResNet-34 [ 29 ] on CIFAR-10 [ 30 ], and ResNet-50 on CIFAR-100 with standard data augmentation (normalization, random crop, and random flip). For each dataset, we run all algorithms for 50 epochs and report averages over five trials. After 25 epochs, the learning rates for all baseline methods (excluding SHANG and SHANG++) are decayed by a factor of 0.1; AGNES’s correction step size is similarly reduced. For our methods, the time-scaling factor γ is doubled after 25 epochs. This learning-rate schedule follows [ 4]and helps the baselines achieve better performance on deep learning tasks. SHANG and SHANG++ do not use an explicit learning rate; their effective 16 Figure 2: Training, test loss (log scale, running average with decay 0.99 ) on MNIST with LeNet-5 (batch size 50 ). 

learning rate is controlled by the time-scaling parameter γ, with effective learning rate 1/γ (see Algorithm 1). To implement an analogous decay, we increase γ after 25 epochs (thereby reducing the effective step size 1/γ ), so that all methods undergo a comparable mid-training learning-rate reduction. For hyperparameter selection, our two methods were evaluated under three settings: α = 0 .5 with γ ∈ { 1, 1.5, 2} for LeNet-5, γ ∈ { 5, 10 } for ResNet-34 and γ ∈ { 10 , 15 } for ResNet-50. For SHANG++, we fixed m = 1 .5. AGNES employed the default parameter configuration recommended by its authors, 

(η, α, m ) = (0 .01 , 0.001 , 0.99) , which has demonstrated strong performance across various tasks. For SNAG, we adopt the two-parameter variant (η, β ) proposed by the original authors for machine-learning tasks. Hyperparameters are selected via a grid search, learning rate η ∈ { 0.5, 0.1, 0.05 , 0.01 , 0.005 , 0.001 } and momentum β ∈ { 0.7, 0.8, 0.9, 0.99 }. Among these, (η, β ) = (0 .05 , 0.9) yields the best performance, which coincides with the parameter choice recommended by the original authors for training CNNs on the CIFAR dataset. All other baseline algorithms used a fixed learning rate of η = 0 .001 ; for those involving momentum, the momentum coefficient was set to 0.99 .Figures 2, 3, and 4 report the results of SHANG and SHANG++ with 

α = 0 .5, m = 1 .5, and γ = 2 , 5, 10 , respectively. Figure 3 shows the training and test losses of ResNet-34 on CIFAR-10 under different batch sizes, with all algorithmic hyperparameters kept fixed across batch sizes. Batch size strongly affects gradient variance: smaller batches increase noise, larger batches reduce it. At 256 , all methods are stable and gaps narrow; at 50 , NAG, SNAG, and AGNES oscillate with wider bands (AGNES also plateaus higher). At batch 17 Figure 3: Training loss (left) and test loss (right) in log scale (running average with decay 0.99 ) on CIFAR-10 with ResNet-34, for batch sizes 32 (top row), 50 (middle row), and 256 (bottom row). 

18 size 32 , differences among methods become more pronounced. 

Figure 4: Training, test loss (log scale, running average with decay 0.99 ) on CIFAR-100 with ResNet-50 (batch size 50 ). 

Even under extreme noise, SHANG and SHANG++ consistently outper-form other first-order stochastic momentum methods. Notably, when the batch size falls below 50 , AGNES and SNAG lose their acceleration advan-tage over SGD, whereas SHANG, SHANG++, and Adam still offer clear improvements (though Adam is not directly comparable). As also observed by [ 21 ], non–variance-reduced accelerated methods often lose acceleration at very small batch sizes; however, SHANG and SHANG++ appear to remain robust down to relatively smaller thresholds. 

Table 1: Test accuracy of SGD, SHB, NAG, Adam, AGNES, SNAG, SHANG, and SHANG++ on MNIST (LeNet-5), CIFAR-10 (ResNet-34), and CIFAR-100 (ResNet-50). Here b is batch size. Larger values are better. Best is in bold and second best is underlined.                                                                                             

> SGD SHB NAG Adam AGNES SNAG SHANG SHANG++ LeNet-5 91 .07 98 .98 98 .999 .07 98 .88 99 .07 99 .06 99 .11
> (b= 50) ±0.11 ±0.05 ±0.08 ±0.07 ±0.09 ±0.08 ±0.02 ±0.03
> ResNet-34 81 .74 78 .981 .28 86 .99 67 .45 75 .58 84 .585 .36 (b= 32) ±0.38 ±1.67 ±1.58 ±0.14 ±7.7±6.02 ±2±1.42
> ResNet-34 79 .91 84 .59 86 .43 87 .38 70 .49 77 .65 87 .15 87 .4
> (b= 50) ±0.11 ±2.62 ±0.81 ±0.26 ±2.51 ±2.7±0.82 ±0.5
> ResNet-34 68 .49 87 .687 .61 88 .23 77 .84 84 .586 .67 86 .57 (b= 256) ±0.19 ±0.27 ±0.29 ±0.11 ±3.7±0.92 ±0.13 ±0.17
> ResNet-50 58 .31 58 .17 57 .66 59 .87 42 .82 49 .51 63 .31 65 .02
> (b= 50) ±0.51 ±1.99 ±1.44 ±0.61 ±1.24 ±1.56 ±0.93 ±1.25

19 Figure 4 shows ResNet-50 training and test losses on CIFAR-100. SHANG and SHANG++ deliver competitive or superior performance to non-adaptive baselines. Table 1 further summarizes the mean final test accuracy over five independent runs: SHANG and SHANG++ are comparable to Adam, often surpass AGNES and SNAG, and clearly improve over SGD and NAG. The slightly lower absolute accuracies arise because we use intentionally small batch sizes and only 50 training epochs to stress-test optimizer stability rather than to reach full convergence; with standard, longer training schedules, baselines attain their usual performance and the relative ranking of the methods remains essentially unchanged. An interesting observation is that SGD attains the lowest test loss, yet this does not correspond to the best classification accuracy (see Table 1 ). This mismatch is aligned with prior findings: SGD on cross-entropy with hard labels is a likely cause of “confidently wrong” predictions [31]. 

3.3. Robustness to Multiplicative Gradient Noise 

Our theory predicts that time-scale coupling (α, γ ) in SHANG and (α, γ, m )

in SHANG++ mitigates multiplicative gradient noise. To test this, we fix one hyperparameter configuration per optimizer and evaluate across σ ∈{0, 0.05 , 0.1, 0.2, 0.5}. Note that the actual gradient noise level experienced by the optimizer is higher than the nominal σ, because minibatch stochastic gradient descent inherently introduces sampling noise. The multiplicative noise we introduce 

g(xk) = (1 + σN (0 , I d)) ∇f (xk)

is therefore imposed on top of this intrinsic minibatch stochasticity. This one-shot protocol isolates each optimizer’s robustness without re-tuning. All experiments use CIFAR-10 with ResNet-34, batch size 50 , the same settings as subsection 3.2, trained for 100 epochs and averaged over three seeds. Final validation error at epoch 100 is reported. 20 Table 2: Relative change in final classification error compared with σ = 0 (lower is better; negative values indicate improvement). Values are averaged over three seeds.                         

> Method Relative degradation ∆(%) at σ
> 0.05 0.1 0.2 0.5 SHANG −2.5−2.1−1.0−0.2
> SHANG++ +3 .4−0.6−2.1−0.9
> AGNES −14 .4+16 .0+14 .6+13 .5
> SNAG −2.0−2.1−5.00.7
> Figure 5:

Validation error under varying multiplicative noise level σ.Lower is better. 

Figure 5 shows the mean final classification error rate under varying noise levels, and Table 2 reports the relative degradation ∆( σ) = ( E(σ)−E(0)) /E(0) ,where E(σ) denotes the mean classification error rate (averaged over three seeds) at noise level σ.1. At σ = 0 , SHANG and SHANG++ reach 15.9%, outperforming SNAG (17.5%) and AGNES (20.5%). 2. At σ = 0 .1, SHANG slightly improves to 15.6 %, SHANG++ remains stable at 15.9%, SNAG marginally improves to 17.1%, while AGNES degrades to 23.8%. 3. At σ = 0 .5, SHANG and SHANG++ remain near 16%, while SNAG rises to 17.6% and AGNES drifts to 23.2% ( ≈13.5% relative increase). These results align with our Lyapunov analysis: time-scale coupling 

(α, γ, m ) suppresses σ2 amplification, ensuring stable performance without re-tuning. SNAG is stable but less accurate, while AGNES is most sensitive to noise. 

3.4. Image Reconstruction with Small Batch Size 

We further evaluate our algorithms on a generative task of image re-construction with small-batch training, which introduces substantial gra-dient noise. Specifically, we train a lightweight U-Net [ 32 ] (base channels 

32 → 64 → 128 , with bilinear up-sampling and feature concatenation) on CIFAR-10 using batch size 5. We compare SHANG (α = 0 .5, γ = 0 .5) and SHANG++ (α = 0 .5, γ = 0 .5, m = 1) against SNAG, AGNES, NAG, SGD, 21 SHB, and Adam. All other experimental settings follow those in earlier sections.    

> Figure 6: Training and test loss (log scale, running average with decay 0.99 ) on CIFAR-10 using U-Net with batch size 5.

Figure 6 shows training and test losses. Adam achieves the lowest loss due to its adaptive learning rate, but both SHANG and SHANG++ outperform all other non-adaptive methods. In particular, SHANG++ shows stable and efficient training even in this high-noise regime, highlighting its practical robustness. 

4. Convergence Analysis 

In this section, we provide complete proofs of the main theorems. 

4.1. Proof of Theorem 2.1 

Before presenting the proof of Theorem 2.1, we first establish several auxiliary lemmas, beginning with one that relies on conditional expectations under the MNS assumption. 

Lemma 4.1. Let (Ω , F, {F k}k≥0, P) be a complete probability space with filtration {F k}k≥0. Suppose xk is generated by SHANG/SHANG++, g(xk)

denotes the stochastic estimator of ∇f (xk), then the following statements hold 1. E [g(xk) | F k] = ∇f (xk)

2. E [∥g(xk) − ∇ f (xk)∥2] ≤ σ2∥∇ f (xk)∥2

3. E [⟨g(xk), ∇f (xk)⟩] = ∥∇ f (xk)∥2

4. E [∥g(xk)∥2] ≤ (1 + σ2)∥∇ f (xk)∥2

22 Proof of Lemma 4.1. The first two statements follow by an application of Fu-bini’s theorem. For the third result, we observe that since f is a deterministic function, ∇f (xk) is Fk-measurable, then, by the Theorem 8.14 in [ 33 ], we have E [⟨g(xk), ∇f (xk)⟩] = E [E [⟨g(xk), ∇f (xk)⟩ | F k]] = E [⟨E [g(xk) | F k] , ∇f (xk)⟩]= E ∥∇ f (xk)∥2

For the fourth result, using the previous results, we have 

E ∥g(xk)∥2 = E ∥g(xk) − ∇ f (xk)∥2 + 2 ⟨g(xk), ∇f (xk)⟩ − ∥∇ f (xk)∥2

= E ∥g(xk) − ∇ f (xk)∥2 + ∥∇ f (xk)∥2

≤ (1 + σ2)∥∇ f (xk)∥2

The next lemma bounds E E(zk+1 ; γk+1 ) − E (z+ 

> k

; γk).

Lemma 4.2. Let f ∈ S μ,L and let g(x) be defined by (2) satisfying the MNS condition (3) . Starting from x0 = v0, generate (xk, v k) by (10) and define x+

> k

by (11). Then we have 

E [E(zk+1 ; γk+1 )] − E (z+ 

> k

; γk) ≤ E [−αkE(zk+1 ; γk+1 )+α2

> k

(1 + σ2)2γk

∥∇ f (xk+1 )∥2 − αkμ

2 ∥xk+1 − vk+1 ∥2 − Df (x+ 

> k

, x k+1 )



proof of Lemma 4.2. Note that E(z; γ) = DE (z, z ⋆; γ). Applying the three-point identity for the Bregman divergence [34], 

⟨∇ f (y) − ∇ f (x), y − z⟩ = Df (z, y ) + Df (y, x ) − Df (z, x ), ∀ x, y, z ∈ Rd,

(31) and using the update rules (10) , we can expand the Lyapunov difference 23 accordingly. 

E [E(zk+1 ; γk+1 )] − E (z+ 

> k

; γk)= E



E(zk+1 ; γk) − E (z+ 

> k

; γk) + γk+1 − γk

2 ∥vk+1 − x⋆∥2



= E



⟨∇E (zk+1 ; γk), z k+1 − z+ 

> k

⟩ − DE (z+ 

> k

, z k+1 ; γk) + γk+1 − γk

2 ∥vk+1 − x⋆∥2



≤ E [−αk⟨∇ f (xk+1 ) − ∇ f (x⋆), x k+1 − x⋆⟩ + αk⟨g(xk+1 ), v k − vk+1 ⟩

+αkμ⟨vk+1 − x⋆, x k+1 − vk+1 ⟩ + αk⟨∇ f (xk+1 ) − g(xk+1 ), v k − x⋆⟩

+αk(μ − γk+1 )2 ∥vk+1 − x⋆∥2 − DE (z+ 

> k

, z k+1 ; γk)



(32) By the definition of the Bregman divergence and the μ-strong convexity of f

(6), we have 

⟨∇ f (xk+1 ) − ∇ f (x⋆), x k+1 − x⋆⟩ = Df (xk+1 , x ⋆) + Df (x⋆, x k+1 )

≥ Df (xk+1 , x ⋆) + μ

2 ∥xk+1 − x⋆∥2 (33) and 

αkμ⟨vk+1 −x⋆, x k+1 −vk+1 ⟩ = αkμ

2 (∥xk+1 −x⋆∥2−∥ xk+1 −vk+1 ∥2−∥ vk+1 −x⋆∥2)

(34) We denote Fk+1 = σ(x0, B 0, · · · , B k) the σ-algebra. Since f is a deterministic function, vk − x⋆ is Fk+1 -measurable, then 

E [⟨∇ f (xk+1 ) − g(xk+1 ), v k − x⋆⟩] = 0 

For the cross term, using Young Inequality, Cauchy-Schwarz Inequality to obtain 

E [αk⟨g(xk+1 ), v k − vk+1 ⟩] ≤ E

 α2

> k

2γk

∥g(xk+1 )∥2 + γk

2 ∥vk − vk+1 ∥2



≤ E

α2

> k

(1 + σ2)2γk

∥∇ f (xk+1 )∥2 + γk

2 ∥vk − vk+1 ∥2



(35) In addition, using the identity of squares (for v) and Bregman divergence indentity (31) (for x+), we have the component form of 

DE (z+ 

> k

, z k+1 ; γk) = Df (x+ 

> k

, x k+1 ) + γk

2 ∥vk − vk+1 ∥2 (36) 24 Substituting (33)-(36) back into (32), we can obtain 

E [E(zk+1 ; γk+1 )] − E (z+ 

> k

; γk)

≤ E



−αkE(zk+1 ; γk+1 ) + α2

> k

(1 + σ2)2γk

∥∇ f (xk+1 )∥2 − αkμ

2 ∥xk+1 − vk+1 ∥2

−Df (x+ 

> k

, x k+1 )

(37) We proceed to the proof of Theorem 2.1. 

proof of Theorem 2.1. First, we introduce an intermediate term and then 

E E(z+

> k+1

; γk+1 ) − E (z+ 

> k

; γk)= E E(z+

> k+1

; γk+1 ) − E (zk+1 ; γk+1 ) + E E(zk+1 ; γk+1 ) − E (z+ 

> k

; γk) (38) Under the parameter choices in Theorem 2.1, αkβk = αk+1 βk+1 ≤ 1 

> L(1+ σ2)

.Applying Lemma 2.1 yields an upper bound for the first term in (38): 

E E(z+

> k+1

; γk+1 ) − E (zk+1 ; γk+1 ) ≤ E



−αkβk

2 ∥∇ f (xk+1 )∥2



(39) Lemma 4.2 provides an upper bound for the second term in (38) . Combining the two bounds, we obtain: 

E E(z+

> k+1

; γk+1 ) − E (z+ 

> k

; γk)

≤ E



−αkE(zk+1 ; γk+1 ) + ( α2

> k

(1 + σ2)2γk

− αkβk

2 )∥∇ f (xk+1 )∥2



≤ E



−αkE(z+

> k+1

; γk+1 ) + ( α2

> k

(1 + σ2)2γk

− αkβk(1 + αk)2 )∥∇ f (xk+1 )∥2

 (40) The last step applies Lemma 2.1 again to replace −f (xk+1 ) appearing in 

E(zk+1 ; γk+1 ) with −f (x+

> k+1

) − αk βk 

> 2

∥∇ f (xk+1 )∥2. The parameter choices in Theorem 2.1 further ensure that the coefficient of the gradient term is nonpositive. (1). When μ > 0, let γ = μ and α ≤ 11+ σ2

p μL , we have 

E E(z+

> k+1

; μ) ≤ (1 + α)−1E E(z+ 

> k

; μ) ≤ (1 + α)−(k+1) E(z+0 ; μ) (41) 25 (2). When μ = 0 , let αk = 2 

> k+1

, βk = αk (1+ σ2) 

> γk

and γk = α2

> k

(1 + σ2)2L, we have 

E E(z+

> k+1

; γk) ≤ 11 + αk

E E(z+ 

> k

; γk) ≤ Πki=0 

11 + αi

E(z+0 ; γ0) (42) 

4.2. Proof of Theorem 2.2 

For SHANG++ in the strongly convex setting (23) , the next lemma controls the difference between E [E(zk+1 ; μ)] and E E(z+ 

> k

; μ).

Lemma 4.3. Let f ∈ S μ,L and let g(x) be defined by (2) satisfying the MNS condition (3) . Starting from x0 = v0, generate (xk, v k) by (23) and define x+

> k

by (22). Then for all k ≥ 0,

E [E(zk+1 ; μ)] − E (z+ 

> k

; μ)

≤ E



−αE(zk+1 ; μ) − ˜αμ 

2 ∥vk − xk+1 ∥2 − Df (x+ 

> k

, x k+1 )+ ˜αα (1 + σ2)2μ ∥∇ f (xk+1 )∥2



proof of Lemma 4.3. Applying the three-point identity for the Bregman di-vergence (31) and update rules 23, we obtain 

E [E(zk+1 ; μ)] − E (z+ 

> k

; μ) = E ⟨∇E (zk+1 ; μ), z k+1 − z+ 

> k

⟩ − DE (z+ 

> k

, z k+1 ; μ)

= E [−˜α⟨∇ f (xk+1 ) − ∇ f (x⋆), x k+1 − x⋆⟩ + ˜ α⟨∇ f (xk+1 ), v k − v⋆⟩

+μα ⟨vk+1 − x⋆, x k+1 − vk+1 ⟩ − α⟨g(xk+1 ), v k+1 − x⋆⟩ − DE (z+ 

> k

, z k+1 ; μ)

= E [−˜α⟨∇ f (xk+1 ) − ∇ f (x⋆), x k+1 − x⋆⟩ + α⟨g(xk+1 ), v k − vk+1 ⟩−(α − ˜α)⟨g(xk+1 ), v k − x⋆⟩ + μα ⟨vk+1 − x⋆, x k+1 − vk+1 ⟩−DE (z+ 

> k

, z k+1 ; μ) + ˜ α⟨∇ f (xk+1 ) − g(xk+1 ), v k − x⋆⟩

(43) The last term above is zero in expectation. For the first cross term, using 

−⟨ a, b ⟩ = 12 ∥a∥2 + 12 ∥b∥2 − 12 ∥a + b∥2 and the update for vk+1 − vk (23b) yields 

E [α⟨g(xk+1 ), v k − vk+1 ⟩] = E



−α2μ⟨ 1

μg(xk+1 ), vk+1 − vk

α ⟩



= E

 α2

2μ∥g(xk+1 )∥2 + μ

2 ∥vk − vk+1 ∥2 − α2μ

2 ∥xk+1 − vk+1 ∥2

 (44) 26 The coefficient of the second cross term α − ˜α = α − α 

> 1+ α

= α α 

> 1+ α

= α ˜α. Using the updates 

vk − v⋆ = (1 + α)( vk+1 − xk+1 ) + ( xk+1 − x⋆) + αμ g(xk+1 )

and 

vk − xk+1 = (1 + α)( vk+1 − xk+1 ) + αμ g(xk+1 ),

we have 

E [−(α − ˜α)⟨g(xk+1 ), v k − v⋆⟩] = E [−α ˜α⟨g(xk+1 ), v k − v⋆⟩]= E



−˜αμ ⟨αμ g(xk+1 ), (1 + α)( vk+1 − xk+1 )⟩ − α ˜α⟨g(xk+1 ), x k+1 − x⋆⟩−α2 ˜αμ ∥g(xk+1 )∥2



= E



− ˜αμ 

2 ∥vk − xk+1 ∥2 + α2 ˜α

2μ ∥g(xk+1 )∥2 + ˜α(1 + α)2μ

2 ∥vk+1 − xk+1 ∥2

−α ˜α⟨∇ f (xk+1 ), x k+1 − x⋆⟩ − α2 ˜αμ ∥g(xk+1 )∥2



(45) Substituating (44) and (45) back into (43) and collecting coefficients, we have 

E [E(zk+1 ; μ)] − E (z+ 

> k

; μ)= E [−α⟨∇ f (xk+1 ) − ∇ f (x⋆), x k+1 − x⋆⟩ + μα ⟨vk+1 − x⋆, x k+1 − vk+1 ⟩

+α ˜α

2μ ∥g(xk+1 )∥2 + μ

2 ∥vk − vk+1 ∥2 + αμ 

2 ∥xk+1 − vk+1 ∥2

− ˜αμ 

2 ∥vk − xk+1 ∥2 − DE (z+ 

> k

, z k+1 ; μ)



(46) Using (33), (34) and (36), we further bound (43) as 

E [E(zk+1 ; μ)] − E (z+ 

> k

; μ)

≤ E



−αE(zk+1 ; μ) + α ˜α

2μ ∥g(xk+1 )∥2 − ˜αμ 

2 ∥vk − xk+1 ∥2 − Df (x+ 

> k

, x k+1 )



(47) Finally, applying Lemma 4.1 to control the noisy gradient term yields the desired result. 27 We next present the proof of Theorem 2.2. 

proof of Theorem 2.2. Under the parameter choices in Theorem 2.2, ˜αβ = 

> ˜α2(1+ σ2)
> μ

≤ 1 

> L(1+ σ2)

. Applying Lemma 2.1 yields 

E E(z+

> k+1

; μ) − E (z+ 

> k

; μ)= E E(z+

> k+1

; μ) − E (zk+1 ; μ) + E(zk+1 ; μ) − E (z+ 

> k

; μ)

≤ E



E(zk+1 ; μ) − E (z+ 

> k

; μ) − ˜α2(1 + σ2)2μ ∥∇ f (xk+1 )∥2

 (48) Combining the result of Lemma 4.3, we obtain: 

E E(z+

> k+1

; μ) − E (z+ 

> k

; μ)

≤ E



−αE(zk+1 ; μ) − ˜αμ 

2 ∥vk − xk+1 ∥2 − Df (x+ 

> k

, x k+1 )+( ˜αα (1 + σ2)2μ − ˜α2(1 + σ2)2μ )∥∇ f (xk+1 )∥2



≤ E



−αE(z+

> k+1

; μ) − ˜αμ 

2 ∥vk − xk+1 ∥2 − Df (x+ 

> k

, x k+1 )+( ˜αα (1 + σ2)2μ − ˜α2(1 + α)(1 + σ2)2μ )∥∇ f (xk+1 )∥2



(49) In the last inequality, we again use the sufficient descent property of SGD in Lemma 2.1. The identity ˜α2(1 + α) = α ˜α eliminates the gradient term, which yields: 

E E(z+

> k+1

; μ) ≤ (1 + α)−1E E(z+ 

> k

; μ) = (1 − ˜α)E E(z+ 

> k

; μ)

4.3. Proof of Corollary 2.1 and 2.2 

We begin by recalling the definition of almost sure convergence. 

Definition 4.1. Let xk be generated by an algorithm. We say that f (xk)

converges almost surely to f (x⋆) if 

P ({| f (xk) − f (x⋆) |> ε i.o. }) = 0 for every ε > 0

where i.o. stands for infinitely often. 28 For any ε > 0, by Markov’s inequality, P(| f (xk) − f (x⋆) |≥ ε) ≤ 1 

> ε

E[|

f (xk) − f (x⋆) |]. In the strongly convex case, Theorem 2.1 and 2.2 give 

E[| f (xk) − f (x⋆) |] ≤ Cq k for some 0 < q < 1, hence 

> ∞

X

> k=1

P(| f (xk) − f (x⋆) |≥ ε) ≤ Cε

> ∞

X

> k=1

qk < ∞.

In the convex case, Theorem 2.1 and 2.3 give E[| f (xk) − f (x⋆) |] ≤ Ck2 , hence 

> ∞

X

> k=1

P(| f (xk) − f (x⋆) |≥ ε) ≤ Cε

> ∞

X

> k=1

1

k2 < ∞.

Therefore, 

P ({| f (xk) − f (x⋆) |> ε i.o. })= P



lim sup 

> k→∞

| f (xk) − f (x⋆) |≥ ε



= P (∩∞ 

> N=1

∪k≥N | f (xk) − f (x⋆) |≥ ε)= lim   

> N→∞

P (∪k≥N | f (xk) − f (x⋆) |≥ ε)

≤ lim  

> N→∞

X

> k≥N

P (| f (xk) − f (x⋆) |≥ ε)=

> ∞

X

> k=1

P (| f (xk) − f (x⋆) |≥ ε) − lim   

> N→∞
> N−1

X

> k=1

P (| f (xk) − f (x⋆) |≥ ε)= 0 

5. Conclusion 

We proposed SHANG and SHANG++, two stochastic accelerated methods designed for training under multiplicative noise scaling. SHANG++ augments SHANG with a simple damping correction, which improves robustness in noisy regimes while preserving fast convergence. Under the MNS condition, we established accelerated convergence guarantees in both convex and strongly convex settings, together with explicit parameter choices. Empirically, on convex benchmarks as well as image classification and reconstruction tasks, a single hyperparameter configuration performs consistently well and often achieves accuracy close to the noise-free setting (within 1% for σ ≤ 0.5 in our 29 tests). Compared with other stochastic momentum methods, SHANG++ is more stable under small-batch or high-noise conditions and is competitive with widely used optimizers such as Adam. Finally, the strong empirical performance on nonconvex problems suggests that extending the analysis beyond convexity is an interesting direction for future work. 

References 

[1] T. Hastie, R. Tibshirani, J. Friedman, The Elements of Statistical Learn-ing: Data Mining, Inference, and Prediction, Springer, 2009. [2] X. Wu, S. S. Du, R. Ward, Global convergence of adaptive gradient methods for an over-parameterized neural network (2019). arXiv:1902. 07111 .[3] L. Wu, M. Wang, W. Su, The alignment property of sgd noise and how it helps select flat minima: A stability analysis, in: Neural Information Processing Systems, 2022. [4] K. Gupta, J. W. Siegel, S. Wojtowytsch, Nesterov acceleration despite very noisy gradients, in: Proceedings of the 38th International Conference on Neural Information Processing Systems, 2024. [5] L. Hodgkinson, M. W. Mahoney, Multiplicative noise and heavy tails in stochastic optimization (2020). arXiv:2006.06293 .[6] B. Polyak, Some methods of speeding up the convergence of iteration methods, USSR Computational Mathematics and Mathematical Physics 4 (5) (1964) 1–17. doi:https://doi.org/10.1016/0041-5553(64) 90137-5 .[7] Y. Nesterov, A method for solving the convex programming problem with convergence rate O(1 /k 2), Proceedings of the USSR Academy of Sciences 269 (1983) 543–547. [8] D. P. Kingma, J. Ba, Adam: A method for stochastic optimization, in: 3rd International Conference on Learning Representations, ICLR 2015, 2015. 30 [9] O. Devolder, F. Glineur, Y. Nesterov, First-order methods of smooth convex optimization with inexact oracle, Mathematical Programming 146 (2014) 37–75. [10] J.-F. Aujol, C. Dossal, Stability of over-relaxations for the forward-backward algorithm, application to fista, SIAM Journal on Optimization 25 (4) (2015) 2408–2433. doi:10.1137/140994964 .[11] T. Liu, Z. Chen, E. Zhou, T. Zhao, Toward deeper understanding of nonconvex stochastic optimization with momentum using diffusion ap-proximations, ArXiv abs/1802.05155 (2018). [12] R. Kidambi, P. Netrapalli, P. Jain, S. M. Kakade, On the insufficiency of existing momentum schemes for stochastic optimization (2018). arXiv: 1803.05591 .[13] C. Liu, M. Belkin, Accelerating sgd with momentum for over-parameterized learning (2019). arXiv:1810.13395 .[14] M. Assran, M. G. Rabbat, On the convergence of nesterov’s accelerated gradient method in stochastic settings, ArXiv abs/2002.12414 (2020). [15] S. Ganesh, R. Deb, G. Thoppe, A. Budhiraja, Does momentum help in stochastic optimization? A sample complexity analysis., in: Proceedings of the Thirty-Ninth Conference on Uncertainty in Artificial Intelligence, Vol. 216, 2023, pp. 602–612. [16] P. Jain, S. M. Kakade, R. Kidambi, P. Netrapalli, A. Sidford, Accelerating stochastic gradient descent for least squares regression (2018). arXiv: 1704.08227 .[17] S. Vaswani, F. Bach, M. Schmidt, Fast and faster convergence of SGD for over-parameterized models and an accelerated perceptron, in: Inter-national Conference on Artificial Intelligence and Statistics (AISTATS), 2019, pp. 1195–1204. [18] M. Even, R. Berthier, F. Bach, N. Flammarion, P. Gaillard, H. Hendrikx, L. Massoulié, A. Taylor, A continuized view on nesterov acceleration for stochastic gradient descent and randomized gossip, in: Proceedings of the 35th International Conference on Neural Information Processing Systems, 2021. 31 [19] R. Bollapragada, T. Chen, R. Ward, On the fast convergence of minibatch heavy ball momentum, IMA Journal of Numerical Analysis 45 (3) (2024) 1397–1424. doi:10.1093/imanum/drae033 .[20] M. Laborde, A. Oberman, A lyapunov analysis for accelerated gradient methods: from deterministic to stochastic case, in: Proceedings of the Twenty Third International Conference on Artificial Intelligence and Statistics, Vol. 108, 2020, pp. 602–612. [21] J. Hermant, M. Renaud, J.-F. Aujol, C. Dossal, A. Rondepierre, Gradient correlation is a key ingredient to accelerate sgd with momentum (2025). 

arXiv:2410.07870 .[22] Y. Nesterov, Efficiency of coordinate descent methods on huge-scale optimization problems, SIAM Journal on Optimization 22 (2) (2012) 341–362. doi:10.1137/100802001 .[23] L. Chen, H. Luo, A unified convergence analysis of first order convex optimization methods via strong lyapunov functions (2021). arXiv: 2108.00132 .[24] L. Lessard, B. Recht, A. Packard, Analysis and design of optimization algorithms via integral quadratic constraints, SIAM Journal on Opti-mization 26 (1) (2016) 57–95. doi:10.1137/15M1009597 .[25] B. Goujaud, A. Taylor, A. Dieuleveut, Provable non-accelerations of the heavy-ball method (2025). arXiv:2307.11291 .[26] L. Chen, H. Luo, First order optimization methods based on hessian-driven nesterov accelerated gradient flow (2019). arXiv:1912.09276 .[27] L. Chen, Z. Xu, Hnag++: A super-fast accelerated gradient method for strongly convex optimization (2025). arXiv:2510.16680 .[28] Y. LeCun, L. Bottou, Y. Bengio, P. Haffner, Gradient-based learning applied to document recognition, Proceedings of the IEEE 86 (11) (1998) 2278–2324. [29] K. He, X. Zhang, S. Ren, J. Sun, Deep residual learning for image recognition, in: 2016 IEEE Conference on Computer Vision and Pattern Recognition (CVPR), 2016, pp. 770–778. doi:10.1109/CVPR.2016.90 .32 [30] A. Krizhevsky, Learning multiple layers of features from tiny images, 2009. URL https://api.semanticscholar.org/CorpusID:18268744 

[31] S. Thulasidasan, G. Chennupati, J. Bilmes, T. Bhattacharya, S. Michalak, On mixup training: Improved calibration and predictive uncertainty for deep neural networks (2020). arXiv:1905.11001 .[32] O. Ronneberger, P. Fischer, T. Brox, U-net: Convolutional networks for biomedical image segmentation (2015). arXiv:1505.04597 .[33] A. Klenke, Probability Theory: A Comprehensive Course, Springer, 2013. [34] G. Chen, M. Teboulle, Convergence analysis of a proximal-like minimiza-tion algorithm using bregman functions, SIAM Journal on Optimization 3 (3) (1993) 538–543. doi:10.1137/0803026 .33