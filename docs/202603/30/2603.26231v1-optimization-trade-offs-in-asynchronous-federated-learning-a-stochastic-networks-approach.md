---
title: "Optimization Trade-offs in Asynchronous Federated Learning: A Stochastic Networks Approach"
title_zh: 异步联邦学习中的优化权衡：一种随机网络方法
authors: "Abdelkrim Alahyane, Céline Comte, Matthieu Jonckheere"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26231v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 异步联邦学习的随机排队网络框架
tldr: 针对同步联邦学习中的掉队者问题，本文研究了异步联邦学习中的梯度陈旧性和数据偏差挑战。通过引入随机排队网络框架，首次对计算和通信延迟下的更新吞吐量及收敛时间进行了闭式表征。研究揭示了梯度陈旧性、收敛速度与能耗之间的权衡关系，并提出了优化路由与并发的策略，在实验中显著降低了收敛时间和能耗。
selection_source: fresh_fetch
motivation: 现有的异步联邦学习分析忽略了底层的排队动力学，缺乏对更新吞吐量和梯度陈旧性的闭式表征。
method: 开发了一个基于随机排队网络的广义异步SGD框架，联合建模客户端与服务器的计算及通信延迟。
result: 推导出了更新吞吐量和收敛时间的闭式表达式，并揭示了收敛速度与能耗之间的关键权衡。
conclusion: 提出的梯度优化策略能有效平衡并发与路由，在EMNIST数据集上显著提升了收敛效率并降低了能耗。
---

## 摘要
同步联邦学习由于掉队者效应（straggler effect）而难以扩展。异步算法通过在更新到达时立即处理来提高更新吞吐量，但它们引入了两个基本挑战：降低收敛性能的梯度陈旧性（gradient staleness），以及在异构数据分布下对快速客户端的偏置。尽管 AsyncSGD 和广义 AsyncSGD 等算法通过客户端任务队列缓解了这种偏置，但大多数现有分析忽略了底层的排队动力学，且缺乏对更新吞吐量和梯度陈旧性的闭式表征。为了填补这一空白，我们为广义 AsyncSGD 开发了一个随机排队网络框架，该框架联合建模了客户端和中央服务器的随机计算时间，以及随机的上行和下行通信延迟。利用乘积形式网络理论（product-form network theory），我们推导出了更新吞吐量的闭式表达式，以及达到 ε-平稳点所需的通信轮数复杂度和预期挂钟时间（wall-clock time）的闭式上界。这些结果正式刻画了梯度陈旧性与挂钟收敛速度之间的权衡。我们进一步扩展了该框架，以量化随机计时下的能耗，揭示了收敛速度与能量效率之间的额外权衡。基于这些分析结果，我们提出了基于梯度的优化策略，以联合优化路由和并发性。在 EMNIST 上的实验表明，与 AsyncSGD 相比，收敛时间减少了 29%--46%，能耗降低了 36%--49%。

## Abstract
Synchronous federated learning scales poorly due to the straggler effect. Asynchronous algorithms increase the update throughput by processing updates upon arrival, but they introduce two fundamental challenges: gradient staleness, which degrades convergence, and bias toward faster clients under heterogeneous data distributions. Although algorithms such as AsyncSGD and Generalized AsyncSGD mitigate this bias via client-side task queues, most existing analyses neglect the underlying queueing dynamics and lack closed-form characterizations of the update throughput and gradient staleness. To close this gap, we develop a stochastic queueing-network framework for Generalized AsyncSGD that jointly models random computation times at the clients and the central server, as well as random uplink and downlink communication delays. Leveraging product-form network theory, we derive a closed-form expression for the update throughput, alongside closed-form upper bounds for both the communication round complexity and the expected wall-clock time required to reach an $ε$-stationary point. These results formally characterize the trade-off between gradient staleness and wall-clock convergence speed. We further extend the framework to quantify energy consumption under stochastic timing, revealing an additional trade-off between convergence speed and energy efficiency. Building on these analytical results, we propose gradient-based optimization strategies to jointly optimize routing and concurrency. Experiments on EMNIST demonstrate reductions of 29%--46% in convergence time and 36%--49% in energy consumption compared to AsyncSGD.

---

## 论文详细总结（自动生成）

### 异步联邦学习中的优化权衡：一种随机网络方法 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
联邦学习（FL）在异构环境下常受“掉队者效应”影响，导致同步协议效率低下。异步联邦学习（AFL）虽能提高吞吐量，但面临**梯度陈旧性**（降低收敛性）和**快速客户端偏置**（在非独立同分布数据下影响准确性）两大挑战。
*   **研究动机**：现有 AFL 分析多关注“通信轮数”而非实际的“挂钟时间”（Wall-clock time），且往往忽略了底层排队动力学对性能的影响。
*   **核心问题**：如何在考虑随机计算、通信延迟及能耗的情况下，通过数学建模找到梯度陈旧性、更新频率与能量效率之间的最优权衡。

#### 2. 方法论：核心思想与关键技术
论文提出了一种基于**随机排队网络（Jackson Network）**的理论框架，对广义异步 SGD（Generalized AsyncSGD）进行建模。
*   **核心思想**：将联邦学习系统视为一个封闭的随机网络。客户端的计算建模为单服务器 FIFO 队列，上行/下行通信建模为无限服务器队列。
*   **关键技术细节**：
    *   **乘积形式理论（Product-form Theory）**：利用该理论推导出系统稳态分布，进而获得平均相对延迟、更新吞吐量的**闭式表达式**。
    *   **性能指标表征**：首次给出了达到 $\epsilon$-平稳点所需的预期挂钟时间和总能耗的闭式上界。
    *   **联合优化策略**：将路由概率（$p$，决定任务分配）和并发水平（$m$，系统中流通的任务数）作为优化变量。
    *   **偏差修正**：通过在更新规则中引入路由概率的倒数作为缩放因子，确保在异构数据下的无偏学习。

#### 3. 实验设计
*   **数据集**：EMNIST（手写字符）、KMNIST（日文汉字）、CIFAR-100（图像分类）。
*   **数据分布场景**：包括同质（IID）和异构（Non-IID，使用 Dirichlet 分布 $\alpha=0.2$ 模拟）。
*   **服务时间分布**：为了验证理论的鲁棒性，测试了指数分布（理论假设）、确定性分布和对数正态分布。
*   **对比基准（Benchmarks）**：
    1.  **AsyncSGD**：标准基准（均匀路由，$m=n$）。
    2.  **Max-Throughput**：最大化吞吐量策略（仅关注速度，忽略陈旧性）。
    3.  **Round-Optimized**：轮数优化策略（仅关注减少陈旧性，忽略速度）。
    4.  **Time-Optimized / Co-Optimized**：本文提出的时间优化及时间-能量联合优化策略。

#### 4. 资源与算力
*   **硬件设备**：实验使用了 **NVIDIA Tesla P100 GPU**。
*   **软件框架**：基于 PyTorch 实现。
*   **训练时长**：文中未明确给出总的训练小时数，但通过实验图表可见，模拟运行了 400 到 6000 个时间单位（Wall-clock time units），并进行了多次独立重复实验（如 10 次重复以获取均值和标准差）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流数据集，并在每种数据集下对比了四到五种不同的策略。
*   **充分性**：实验不仅验证了收敛速度，还深入探讨了能量消耗的帕累托前沿（Pareto frontier）。通过改变服务时间分布（从随机到确定性）证明了理论框架的普适性。
*   **客观性**：对比了仅优化轮数或仅优化吞吐量的极端情况，客观展示了本文方法在平衡各项指标上的优越性。

#### 6. 主要结论与发现
*   **最优并发水平**：发现存在一个最优的并发数 $m^*$，通常小于客户端总数 $n$。盲目增加并发会因梯度陈旧性过高而适得其反。
*   **路由策略影响**：单纯追求高吞吐量（偏向快速客户端）会导致收敛不稳定；而本文提出的时间优化路由能有效平衡更新频率与梯度质量。
*   **性能提升**：在 EMNIST 实验中，相比标准 AsyncSGD，收敛时间缩短了 **29%--46%**，能耗降低了 **36%--49%**。
*   **能量-延迟权衡**：揭示了能量消耗与训练速度之间的非线性关系，通过调节超参数 $\rho$ 可以灵活导航帕累托前沿。

#### 7. 优点（亮点）
*   **理论严谨性**：利用排队论提供了 AFL 领域少有的闭式解，使性能预测不再依赖于启发式搜索。
*   **现实建模**：联合考虑了计算、上行通信、下行通信以及中央服务器的处理延迟，模型非常贴近真实的边缘计算环境。
*   **多目标优化**：不仅关注算法精度，还兼顾了工业界关心的挂钟时间和电池能耗。

#### 8. 不足与局限
*   **分布假设**：核心理论推导依赖于指数分布假设（虽然实验证明在其他分布下也有效，但数学证明的覆盖面有限）。
*   **客户端动态性**：模型假设客户端集合是固定的，未充分讨论客户端频繁掉线（Churn）或动态加入对排队网络稳态的影响。
*   **计算复杂度**：虽然使用了 Buzen 递归算法优化，但对于超大规模网络（如数百万客户端），计算归一化常数可能仍存在挑战。

（完）
