---
title: Differentially Private Contrastive Learning via Bounding Group-level Contribution
title_zh: 通过限制组级贡献实现差分隐私对比学习
authors: "Kecen Li, Chen Gong, Zinan Lin, Tianhao Wang, Xiaokui Xiao"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26467v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 差分隐私对比学习框架
tldr: 针对差分隐私对比学习中样本间强依赖导致噪声放大、性能下降的问题，本文提出DP-GCL框架。该框架通过将批次划分为不相交的小组并限制负样本范围，从结构上减少梯度依赖并降低敏感度，同时引入组内增强以弥补负样本多样性的损失。实验表明，该方法在多项任务上显著提升了模型效用，在图像分类和图文检索上均刷新了SOTA。
selection_source: fresh_fetch
motivation: 传统对比学习中样本间极强的梯度依赖会放大差分隐私噪声，导致模型在隐私保护下的性能严重下降。
method: 提出DP-GCL框架，通过将批次分组并限制负样本在组内，结合组内增强技术来降低梯度敏感度并维持样本多样性。
result: "在八个数据集上的实验显示，该方法在图像分类和图文检索任务上分别比现有DP对比学习方法提升了5.6%和20.1%。"
conclusion: 通过结构化限制梯度依赖，DP-GCL有效解决了差分隐私对比学习中的效用退化问题，实现了隐私与性能的更好平衡。
---

## 摘要
差分隐私（DP）对比学习旨在从敏感数据中学习通用表示，从而缓解组织在部署或共享基于私有用户内容训练的嵌入模型时的隐私泄露担忧。然而，由于标准对比目标中固有的过强样本间依赖性，现有方法面临严重的效用下降问题，即每个样本的梯度都依赖于批次中的所有其他样本，从而放大了 DP 噪声的影响。在这项工作中，我们认为有效的 DP 对比学习需要显式地减少这种内在的样本间依赖。为此，我们提出了 DP-GCL，这是一个原则性的 DP 对比学习框架，通过限制组级贡献在结构上限制梯度依赖。DP-GCL 将每个批次划分为小的、不相交的组，并将可用的负样本限制在组内样本中，从而使梯度影响局部化并降低敏感度。为了抵消由此导致的负样本多样性损失，我们进一步引入了组内增强，在不增加隐私成本的情况下生成额外的负样本视图。在八个数据集上的广泛实验表明，在实际的隐私预算下，DP-GCL 在单模态和多模态对比学习中均持续推进了最先进水平：与现有的 DP 对比方法相比，它将图像分类准确率提高了 5.6%，图像文本检索准确率提高了 20.1%。

## Abstract
Differentially private (DP) contrastive learning aims to learn general-purpose representations from sensitive data, alleviating the privacy leakage concerns of organizations deploying or sharing embedding models trained on private user content. However, existing approaches suffer from severe utility degradation due to the over-strong inter-sample dependency inherent in standard contrastive objectives, where each sample's gradient depends on all other samples in the batch, amplifying the impact of DP noise. In this work, we argue that effective DP contrastive learning requires explicitly reducing such intrinsic inter-sample reliance. To this end, we propose DP-GCL, a principled DP contrastive learning framework that structurally limits gradient dependency through bounding group-level contribution. DP-GCL partitions each batch into small, disjoint groups and restricts available negative samples to within-group samples, thereby localizing gradient influence and reducing sensitivity. To counteract the resulting loss of negative sample diversity, we further introduce intra-group augmentation, which generates additional negative views without increasing privacy cost. Extensive experiments across eight datasets demonstrate that DP-GCL consistently advances the state of the art in both uni-modal and multi-modal contrastive learning under practical privacy budgets: it improves image classification accuracy by 5.6% and image-text retrieval accuracy by 20.1% over existing DP contrastive methods.

---

## 论文详细总结（自动生成）

这篇论文针对差分隐私（DP）对比学习中的效用退化问题，提出了名为 **DP-GCL** 的新框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在对比学习（如 SimCLR, CLIP）中，常用的 InfoNCE 损失函数具有**强样本间依赖性**。这意味着批次中每个样本的梯度计算都依赖于该批次内的所有其他样本作为负样本。
*   **研究动机**：
    *   直接应用标准 DP-SGD（样本级裁剪）会导致敏感度随批次大小线性增加，引入海量噪声。
    *   现有的批次级裁剪（如 DP-CLIP）虽然降低了敏感度，但无法从大批次训练中获得信号累积收益，导致效用低下。
*   **整体含义**：论文旨在通过在结构上限制梯度依赖性，在保证差分隐私的前提下，提升对比学习在单模态和多模态任务中的特征表示质量。

### 2. 方法论
DP-GCL 框架包含两个核心模块：
*   **限制组级贡献（Bounding Group-level Contribution, BGC）**：
    *   **分组策略**：将大小为 $B$ 的批次随机划分为 $K$ 个不相交的小组，每组大小为 $S$。
    *   **局部对比**：每个样本仅与同组内的其他样本进行对比，忽略组外样本。
    *   **组级裁剪**：对每个小组计算出的梯度进行独立裁剪（Clipping），然后聚合。
    *   **数学优势**：通过这种结构设计，证明了其全局敏感度固定为 $2C$（$C$ 为裁剪阈值），且不随组大小 $S$ 变化，从而在降低噪声的同时允许信号累积。
*   **组内样本增强（Intra-Group Sample Augmentation, ISA）**：
    *   **背景**：分组减少了负样本的数量，可能削弱对比信号。
    *   **技术细节**：在组内对现有的负样本进行多次随机数据增强（如图像裁剪、文本句子交换），在不消耗额外隐私预算的情况下人为增加负样本的多样性。
*   **算法流程**：采样批次 -> 随机分组 -> 组内增强 -> 计算组级 InfoNCE 损失 -> 组级梯度裁剪 -> 注入高斯噪声 -> 更新模型参数。

### 3. 实验设计
*   **数据集**：
    *   **单模态（图像分类）**：Fashion-MNIST, CIFAR-10, EuroSAT, Camelyon, ImageNet。
    *   **多模态（图文检索）**：CUHK-PEDES, RSTPReid, Fashion, ROCO（涵盖通用、行人重识别、时尚、医疗领域）。
*   **Benchmark 与对比方法**：
    *   **Base**：无私有数据训练的基准（随机初始化或预训练模型）。
    *   **DP-SGD**：标准样本级裁剪方法。
    *   **Logit-DP**：一种分解对比损失为成对项并裁剪的方法。
    *   **DP-CLIP**：批次级梯度裁剪的 SOTA 方法。
*   **评估协议**：单模态采用线性探测（Linear Probing）和 k-NN 准确率；多模态采用 Top-10 检索准确率（I→T 和 T→I）。

### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **1 张 NVIDIA GeForce A6000 GPU** 和 512GB 内存的服务器上运行。
*   **训练时长**：
    *   单模态任务（如 CIFAR-10）：DP-GCL 运行时间约为 0.99 小时。
    *   多模态任务（如 PEDES）：DP-GCL 运行时间约为 2.89 小时。
    *   对比：Logit-DP 由于 $O(B^2)$ 的复杂度，在相同任务下需要 60-72 小时，效率远低于 DP-GCL。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 个不同领域的数据集，测试了 $\epsilon \in \{1, 10\}$ 不同的隐私预算。
*   **消融实验**：详细验证了 BGC 和 ISA 两个模块的贡献，并探讨了组大小 $S$ 对信噪比（SNR）的影响。
*   **扩展性实验**：测试了不同批次大小（最高达 8192）、不同 ViT 架构（B/32, B/16, L/14, L/14-H）以及参数高效微调（LoRA）的兼容性。
*   **充分性评价**：实验设计非常充分，不仅对比了效用，还分析了收敛速度、计算开销和抗成员推理攻击（MIA）的能力，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **效用大幅提升**：DP-GCL 在单模态分类上平均提升 5.6%，在多模态检索上平均提升 **20.1%**。
*   **信噪比平衡**：发现组大小 $S$ 存在最优权衡点（通常在 4-16 之间）。过大的组会降低信噪比，而过小的组缺乏对比信号。
*   **大批次友好**：与 DP-CLIP 不同，DP-GCL 能够随着批次大小的增加持续提升性能，展现出良好的扩展性。
*   **收敛更快**：相比基线方法，DP-GCL 在更少的迭代次数内即可达到更高的准确率。

### 7. 优点
*   **理论与实践结合**：通过严谨的数学证明（定理 1-3）确定了敏感度边界，同时解决了对比学习的实际痛点。
*   **计算效率高**：相比 Logit-DP 等方法，计算开销极小，适合大规模部署。
*   **通用性强**：不仅适用于单模态，在更具挑战性的多模态对齐任务上表现尤为突出。
*   **架构无关**：支持多种 Vision Transformer 变体和 LoRA 微调，灵活性高。

### 8. 不足与局限
*   **超大规模预训练缺失**：受限于算力，论文未在十亿级（Billion-scale）数据集上进行预训练验证。
*   **损失函数局限**：目前主要针对 InfoNCE 损失，尚未探讨在 BYOL（无负样本）或 SigLIP（成对 Sigmoid 损失）等其他对比框架下的表现。
*   **隐私-效用差距**：尽管刷新了 SOTA，但与非隐私（Non-private）基准相比，在复杂任务上仍存在明显的性能差距。

（完）
