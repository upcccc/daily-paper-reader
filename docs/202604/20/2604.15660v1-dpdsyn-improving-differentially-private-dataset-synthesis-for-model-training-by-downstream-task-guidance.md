---
title: "DPDSyn: Improving Differentially Private Dataset Synthesis for Model Training by Downstream Task Guidance"
title_zh: DPDSyn：通过下游任务引导改进用于模型训练的差分隐私数据集合成
authors: "Mingxuan Jia, Wen Huang, Weixin Zhao, Xingyi Wang, Jian Peng, Zhishuo Zhang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15660v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 用于AI模型训练的差分隐私数据集合成
tldr: 针对差分隐私数据集合成中低维分布选择困难导致的数据可用性低问题，本文提出 DPDSyn。该方法不再依赖复杂的分布选择，而是直接在原始私有数据上训练满足差分隐私的下游任务模型，并利用该模型指导合成数据的生成。这种方式在确保隐私的同时，有效保留了下游任务的关键信息。实验表明，DPDSyn 在准确率和合成效率上均显著优于现有主流方法，且具备极强的扩展性。
selection_source: fresh_fetch
motivation: 现有差分隐私合成方法难以准确选择能代表原始数据分布的低维特征，导致生成的合成数据在下游任务中表现不佳。
method: 通过在原始数据上训练差分隐私 AI 模型，并利用该模型捕捉下游任务所需的关键特征来指导数据集合成。
result: 实验显示 DPDSyn 在准确率上最高提升 2.40 倍，合成效率提升达 333.73 倍，且在不同数据规模下表现稳定。
conclusion: 利用下游任务引导的差分隐私模型进行数据合成，能显著提升合成数据的实用性与生成效率。
---

## 摘要
如何在实现 AI 模型训练的差分隐私的同时合成数据集，是一个具有重要意义但充满挑战的问题。为了解决这一问题，现有最先进的方法首先从原始私有数据集中选择多个低维分布，这些分布具有高精度逼近原始私有数据集分布的潜力，然后合成一个遵循所有选定低维分布的数据集作为合成数据集。然而，选择合适的低维分布非常困难，这反过来又降低了所得合成数据集的数据效用。为了改进差分隐私数据集合成，我们提出在原始私有数据集上针对下游任务训练一个差分隐私 AI 模型，并利用训练好的模型来合成数据集。具体而言，一方面，该 AI 模型满足差分隐私，因此无论如何使用该模型都不会泄露原始私有数据集的隐私信息；另一方面，该 AI 模型是为完成下游任务而训练的，因此它保留了完成下游任务的关键信息。我们利用该 AI 模型合成数据集，以实现在保护隐私的同时提高数据效用的目标。在四个基准数据集上的实证评估表明，我们提出的 DPDSyn 一贯优于八种最先进的基准方法，准确率最高提升了 2.40 倍，合成效率最高提升了 333.73 倍。进一步的实验还验证了 DPDSyn 在不同数据规模下具有强大的可扩展性。

## Abstract
How to synthesize a dataset while achieving differential privacy for AI model training is a meaningful but challenging problem. To address this problem, state-of-the-art methods first select original private dataset's multiple low-dimensional distributions that have the potential to approximate the distribution of original private dataset with high precision, and then synthesize a dataset obeying all selected low-dimensional distributions as the synthetic dataset. However, it is difficult to select suitable low-dimensional distributions, which in turn degrades the data utility of resulting synthetic dataset. To improve differentially private dataset synthesis, we propose to train a differentially private AI model for downstream tasks on the original private dataset and utilize the trained model to synthesize datasets. In particular, on the one hand, the AI model satisfies differential privacy so no matter how to use the model does not disclose private information of original private dataset. On the other hand, the AI model is trained to complete the downstream task so the AI model preserves critical information for completing downstream tasks. We utilize the AI model to synthesize datasets to achieve the goal of improving data utility while preserving privacy. Empirical evaluations on four benchmark datasets demonstrate that our proposed DPDSyn consistently outperforms eight state-of-the-art baselines with a maximum improvement of 2.40x in accuracy and 333.73x in synthesis efficiency. Further experiments also validate that DPDSyn has strong scalability across varying data scales.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DPDSyn** 的新型差分隐私（Differential Privacy, DP）数据集合成方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保护隐私的前提下，合成高质量的数据集用于 AI 模型训练。
*   **研究背景**：
    *   现有的 DP 合成方法主要分为两类：**选择-测量-生成（Select-Measure-Generate）**方法（如 PrivSyn, AIM）和**基于 GAN** 的方法。
    *   **局限性**：前者依赖于选择合适的低维分布来近似全局分布，但在高维数据下很难选准，导致数据可用性（Utility）受损；后者（GAN）在加入 DP 噪声后训练极不稳定，且生成效率较低。
    *   **动机**：作者发现，合成数据通常是为了特定的下游任务（如分类）。如果能利用下游任务的先验知识来引导合成过程，就能更有效地保留关键特征信息。

### 2. 方法论（DPDSyn）
DPDSyn 的核心思想是：**训练一个满足差分隐私的下游任务模型，并利用该模型作为“标签生成器”来合成数据。** 流程分为四个阶段：
1.  **阶段 1：训练 DP AI 模型**：在原始私有数据集上，使用 **DP-SGD** 算法训练一个针对目标下游任务的模型（如 MLP 或 SVM）。由于模型训练过程满足 $(\epsilon, \delta)$-DP，根据后处理性，该模型的任何输出都不会泄露隐私。
2.  **阶段 2：合成属性（特征）**：为了保留特征的边际分布同时打破记录间的关联，作者对原始数据的每一列（属性）进行**随机洗牌（Shuffling）**。这样生成的合成特征矩阵 $\tilde{X}$ 保留了统计特性，但不包含原始记录的敏感组合。
3.  **阶段 3：合成标签**：将洗牌后的特征 $\tilde{X}$ 输入到阶段 1 训练好的 DP AI 模型中，生成对应的预测标签 $\hat{Y}$。
4.  **阶段 4：组合数据集**：将合成特征 $\tilde{X}$ 与合成标签 $\hat{Y}$ 融合，形成最终的合成数据集。

### 3. 实验设计
*   **数据集**：使用了 4 个基准数据集：Adult（收入预测）、Br2000（人口普查）、LPD（肝病预测）、Smoking（吸烟状态分类）。
*   **下游模型（Benchmark）**：为了验证合成数据的通用性，使用了三种模型进行测试：MLP（多层感知机）、SVM（支持向量机）和 FT-Transformer（深度学习模型）。
*   **对比方法（Baselines）**：共对比了 8 种最先进的方法，包括：
    *   统计方法：ABSyn, PrivSyn, PrivPetal, PrivMRF, AIM, MST, MWEM+PGM。
    *   生成模型：DP-GAN。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号（如 NVIDIA A100 等）或 CPU 硬件配置。
*   **训练时长**：提到了实验设置了 **6 小时**的超时限制。部分基准方法（如 PrivMRF 和 PrivSyn）在处理复杂数据集（如 Smoking）时因超时被排除，而 DPDSyn 在所有任务中均能快速完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 个不同领域的数据集。
    *   对比了 8 种主流基准算法。
    *   使用了 3 种不同类型的下游分类器。
    *   每组实验重复运行 **5 次**取平均值，确保了统计稳定性。
*   **充分性评价**：实验设计非常充分。除了基础的准确率（Accuracy）和 F1 分数对比，还进行了**效率分析**（运行时间）和**可扩展性分析**（将数据规模扩大 2 倍、3 倍后的表现）。对比维度全面，客观反映了该方法在不同场景下的优势。

### 6. 主要结论与发现
*   **效用提升**：DPDSyn 在准确率上最高提升了 **2.40 倍**，F1 分数最高提升了 **15.28 倍**。这证明了“下游任务引导”能比盲目拟合分布更好地保留关键信息。
*   **效率极高**：由于避开了复杂的迭代分布选择，DPDSyn 的合成速度极快，最高实现了 **333.73 倍**的加速。
*   **强扩展性**：随着原始数据规模增加，传统方法（如 PrivMRF）往往会崩溃或超时，而 DPDSyn 的表现依然稳定，且准确率随数据量增加而进一步优化。

### 7. 优点
*   **任务感知（Task-aware）**：巧妙地将合成目标与下游应用对齐，解决了传统方法中“分布拟合得好但预测效果差”的痛点。
*   **架构简洁**：利用 DP-SGD 训练模型替代了复杂的概率图模型推理，大幅降低了计算复杂度。
*   **隐私保证扎实**：基于差分隐私的组合定理和后处理性，提供了严格的数学隐私证明。

### 8. 不足与局限
*   **任务局限性**：DPDSyn 是高度“任务导向”的。如果合成数据集是为了一个任务（如分类 A）生成的，那么它在另一个完全不同的任务（如聚类或分类 B）上的表现可能会显著下降。
*   **特征关联丢失**：通过属性洗牌（Shuffling）生成特征虽然保护了隐私，但也彻底破坏了特征之间的相关性（Correlation）。如果下游任务高度依赖特征间的非线性相互作用，该方法的效果可能会受限。
*   **模型依赖**：合成数据的质量高度依赖于第一阶段训练的 DP 模型。如果隐私预算 $\epsilon$ 过小导致 DP 模型本身欠拟合，合成数据的标签质量也会随之下降。

（完）
