---
title: Distributed Optimization with Coupled Constraints over Time-Varying Digraph
title_zh: 时变有向图上具有耦合约束的分布式优化
authors: "Yeong-Ung Kim, Hyo-Sung Ahn"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10422v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 隐私保护分布式优化算法
tldr: 本文针对具有全局耦合约束的分布式凸优化问题，提出了一种适用于时变有向图的分布式算法。该算法结合了右端项分配分解与原始-对偶方法，能够处理非光滑目标函数，且无需交换原始变量等敏感信息，具有良好的隐私保护特性。在局部函数强凸的条件下，算法实现了O(1/k)的收敛率，适用于电力调度和网络效用最大化等实际场景。
selection_source: fresh_fetch
motivation: 旨在解决具有全局耦合约束且通信拓扑为时变有向图的分布式非光滑优化问题，并兼顾隐私保护需求。
method: 提出一种结合右端项分配分解技术与原始-对偶框架的分布式算法，支持在时变有向图上的全分布式实现。
result: 理论证明了在局部目标函数强凸且不等式次梯度有界的情况下，该算法具有 O(1/k) 的收敛速率。
conclusion: 该算法在处理复杂网络约束优化问题时表现出色，且在保护隐私和应对动态拓扑方面具有显著优势。
---

## 摘要
本文开发了一种分布式算法，用于解决一类分布式凸优化问题，其中局部目标函数可以是一般的非光滑函数，且所有等式和不等式约束在全网范围内耦合。这类问题源于许多领域，如经济调度、网络效用最大化和需求响应。通过结合右端项分配分解法（decomposition by right hand side allocation）和原对偶方法，所提算法能够以完全分布式的方式处理时变有向图网络上的分布式优化问题。出于隐私考虑，该算法不需要通信敏感信息（如原变量）。此外，我们通过对偶分析证明，在局部目标函数强凸但不一定可微且局部不等式的次微分有界的条件下，所提算法在最优性方面保证达到 $O(1/k)$ 的收敛速率。我们对所提算法进行了仿真，以展示其卓越的性能。

## Abstract
In this paper, we develop a distributed algorithm for solving a class of distributed convex optimization problems where the local objective functions can be a general nonsmooth function, and all equalities and inequalities are network-wide coupled. This type of problem arises from many areas, such as economic dispatch, network utility maximization, and demand response. Integrating the decomposition by right hand side allocation and primal-dual methods, the proposed algorithm is able to handle the distributed optimization over networks with time-varying directed graph in fully distributed fashion. This algorithm does not require the communication of sensitive information, such as primal variables, for privacy issues. Further, we show that the proposed algorithm is guaranteed to achieve an $O(1/k)$ rate of convergence in terms of optimality based on duality analysis under the condition that local objective functions are strongly convex but not necessarily differentiable, and the subdifferential of local inequalities is bounded. We simulate the proposed algorithm to demonstrate its remarkable performance.

---

## 论文详细总结（自动生成）

这篇论文《Distributed Optimization with Coupled Constraints over Time-Varying Digraph》（时变有向图上具有耦合约束的分布式优化）由韩国光州科学技术院（GIST）的研究人员撰写。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在多智能体系统（如电力系统经济调度、资源分配、多机器人协作）中，智能体需要在最小化全局目标函数的同时，满足跨网络的**全局耦合约束**（即所有智能体的决策变量共同受限于某些等式或不等式）。
*   **核心挑战**：
    1.  **通信拓扑复杂**：网络是时变的且是有向的（Time-varying Digraph），这比静态无向图更难保证一致性。
    2.  **约束耦合**：约束条件不可分，导致局部决策相互牵制。
    3.  **隐私保护**：在分布式场景下，智能体往往不愿共享原始变量、梯度或局部约束函数等敏感信息。
*   **整体含义**：本文提出了一种新型分布式算法，通过引入辅助变量和原始-对偶框架，在保护隐私的前提下，解决了时变有向图上的非光滑凸优化问题。

### 2. 方法论
*   **核心思想**：结合**右端项分配分解（Right-hand side allocation）**与**增广拉格朗日法（Augmented Lagrangian）**。
*   **关键技术细节**：
    1.  **问题重构**：引入辅助变量 $v_i$ 和 $z_i$，将全局耦合约束转化为局部约束（$A_i x_i - b_i = v_i$ 和 $g_i(x_i) \leq z_i$）以及全局零和约束（$\sum v_i = 0, \sum z_i = 0$）。
    2.  **两层结构**：
        *   **底层（子问题）**：每个智能体利用增广拉格朗日函数求解局部最优解 $x_i$，并更新局部对偶变量（乘子）$u_i$ 和 $y_i$。
        *   **顶层（主问题）**：通过双随机矩阵（Doubly Stochastic Matrix）进行对偶变量的一致性迭代，并根据局部与邻居信息的差异更新辅助变量 $v_i$ 和 $z_i$。
    3.  **隐私机制**：算法仅交换对偶信息（乘子），不交换原始变量 $x_i$、目标函数梯度或局部约束参数。
    4.  **收敛保证**：在局部目标函数强凸、不等式次梯度有界的假设下，理论证明了对偶最优性具有 $O(1/k)$ 的收敛速率。

### 3. 实验设计
*   **实验场景**：模拟了一个包含 **20个智能体** 的网络。
*   **问题设置**：目标函数包含二次项和 $L_1$ 范数（非光滑），约束条件包括随机生成的全局线性等式约束和非线性不等式约束。
*   **Benchmark与对比**：
    *   实验主要展示了该算法在时变有向图下的收敛过程。
    *   文中虽然在引言部分对比了 DIGing、EXTRA、P-EXTRA 等算法的特点，但在数值仿真部分主要侧重于验证所提算法在**最优性误差**、**目标函数值差距**以及**可行性间隙（Feasibility Gap）**三个维度的收敛表现，未直接与其他特定算法进行曲线对比。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提到**具体的硬件配置（如 GPU/CPU 型号）或训练时长。
*   **资源消耗**：由于是分布式优化算法的理论验证，通常在 MATLAB 或 Python 环境下进行数值仿真，对算力要求较低，重点在于迭代次数与收敛精度的关系。

### 5. 实验数量与充分性
*   **实验规模**：进行了一组具有代表性的 20 节点复杂约束优化实验。
*   **充分性评价**：
    *   **理论充分**：作为一篇偏重控制理论与优化的论文，其核心价值在于数学证明（Appendix 中提供了详尽的收敛性证明）。
    *   **仿真验证**：仿真涵盖了原始变量误差、目标函数收敛和约束违反度（等式与不等式），验证了算法的有效性。
    *   **局限性**：缺乏不同网络规模（如百节点、千节点）的扩展性实验，也未进行针对不同时变频率的消融实验。

### 6 主要结论与发现
*   **收敛性**：证明了算法在处理非光滑目标函数和耦合约束时，能够以 $O(1/k)$ 的速率收敛至全局最优解。
*   **鲁棒性**：算法在时变有向图下依然稳健，且辅助变量能有效处理零和约束。
*   **可行性**：通过仿真证明，随着迭代进行，所有智能体的约束违反度均趋于零，实现了可行解的分布式获取。

### 7. 优点
*   **拓扑适应性强**：支持时变有向图，这比大多数要求无向图或静态图的算法更符合实际通信环境。
*   **隐私保护**：通过对偶空间的操作，避免了原始数据的泄露。
*   **处理能力全面**：同时处理非光滑目标函数、等式耦合约束和不等式耦合约束。
*   **固定步长**：使用固定步长即可实现收敛，便于实际工程调参。

### 8. 不足与局限
*   **双随机矩阵依赖**：算法假设存在双随机矩阵 $W^k$。在有向图中，构造双随机矩阵通常需要额外的分布式协议（如 Weight-pushing），这增加了实现复杂度。
*   **强凸性要求**：理论证明依赖于局部目标函数的强凸性假设，对于一般凸函数或非凸问题的适用性有待研究。
*   **同步更新**：算法目前基于同步迭代，未讨论在异步通信或存在通信延迟情况下的表现。
*   **对比实验不足**：数值仿真部分缺乏与同类最新算法（如基于 Push-Sum 的约束优化算法）的性能对比。

（完）
