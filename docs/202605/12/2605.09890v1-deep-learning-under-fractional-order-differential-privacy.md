---
title: Deep Learning under Fractional-Order Differential Privacy
title_zh: 分数阶差分隐私下的深度学习
authors: "Mohammad Partohaghighi, Roummel Marcia"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09890v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 差分隐私随机梯度下降的机制级扩展
tldr: 本文提出分数阶差分隐私随机梯度下降（FO-DP-SGD），旨在解决传统DP-SGD仅利用当前梯度而忽略历史信息的问题。该方法通过分数阶递归查询，将当前裁剪梯度与历史私有输出的幂律加权聚合相结合，为优化过程引入分数阶记忆。实验证明，FO-DP-SGD在保持标准隐私保证的前提下，显著提升了在SVHN和CIFAR等数据集上的分类准确率和隐私-效用权衡。
selection_source: fresh_fetch
motivation: 旨在克服传统DP-SGD仅依赖单步梯度导致的优化效率低下，通过引入历史记忆来改善隐私保护下的模型性能。
method: 通过分数阶递归机制，将当前步的裁剪梯度与经过幂律加权的历史私有输出进行融合，并利用Rényi差分隐私进行会计处理。
result: 在多个标准图像数据集上，FO-DP-SGD的测试准确率均优于DP-SGD及DP-Adam、DP-SAT等多种先进的私有优化基准。
conclusion: 引入分数阶记忆机制能有效平衡灵敏度控制与信号保留，为研究私有优化中的长记忆效应提供了强有力的框架。
---

## 摘要
差分隐私随机梯度下降 (DP-SGD) 是一种基于逐样本裁剪、子采样、高斯扰动和隐私核算的隐私保护学习标准方法。经典的 DP-SGD 发布当前裁剪后的子采样梯度和的噪声版本。我们提出了分数阶差分隐私随机梯度下降 (FO-DP-SGD)，这是一种机制层面的扩展，它在添加高斯噪声之前，将这种仅针对当前的查询替换为一种分数阶递归查询，该查询将当前裁剪后的梯度和与先前发布的隐私和级输出的有限窗口、幂律加权聚合相结合。这在保持标准的“求和-加噪-除法”结构的同时，为发布机制注入了分数阶记忆。在具有泊松子采样的添加/移除相邻关系下，当前步的敏感度分析表明，唯一新增的数据依赖项是缩放后的当前裁剪梯度和。因此，在给定隐私历史的条件下，有效 \(\ell_2\) 敏感度至多为 \(\beta C\)，其中 \(C\) 是裁剪阈值，\(\beta \in (0,1]\) 控制当前步的贡献。因此，FO-DP-SGD 允许通过具有有效噪声敏感度比 \(\sigma/\beta\) 的泊松子采样高斯机制进行标准的逐步 Rényi 差分隐私核算，并组合产生整体的 \((\varepsilon,\delta)\)-差分隐私保证。FO-DP-SGD 为研究隐私优化中的长记忆效应提供了一个框架。分数阶、记忆窗口和混合系数控制了当前步敏感度、信号保留和隐私历史影响之间的权衡。在 SVHN、CIFAR-10 和 CIFAR-100 上的实验表明，与 DP-SGD 以及包括 DP-Adam、DP-IS、SA-DP-SGD、ADP-AdamW、DP-SAT 和 DP-Adam-AC 在内的隐私基准相比，该方法提高了测试准确率和隐私-效用性能。

## Abstract
Differentially private stochastic gradient descent (DP-SGD) is a standard approach to privacy-preserving learning based on per-example clipping, subsampling, Gaussian perturbation, and privacy accounting. Classical DP-SGD releases a noisy version of the current clipped subsampled gradient sum. We propose Fractional-Order Differentially Private Stochastic Gradient Descent (\textbf{FO-DP-SGD}), a mechanism-level extension that replaces this current-only query, before Gaussian noise is added, with a fractional recursive query combining the current clipped sum with a finite-window, power-law-weighted aggregation of previously released private sum-level outputs. This injects fractional memory into the release mechanism while preserving the standard \emph{sum-then-noise-then-divide} structure.   Under add/remove adjacency with Poisson subsampling, the current-step sensitivity analysis shows that the only newly data-dependent term is the scaled current clipped sum. Hence, conditioned on the private history, the effective \(\ell_2\)-sensitivity is at most \(βC\), where \(C\) is the clipping threshold and \(β\in(0,1]\) controls the current-step contribution. Thus, FO-DP-SGD admits standard per-step Rényi differential privacy accounting via a Poisson-subsampled Gaussian mechanism with effective noise-to-sensitivity ratio \(σ/β\), and composes to yield overall \((\varepsilon,δ)\)-differential privacy guarantees.   FO-DP-SGD provides a framework for studying long-memory effects in private optimization. The fractional order, memory window, and mixing coefficient govern the trade-off among current-step sensitivity, signal retention, and private-history influence. Experiments on SVHN, CIFAR-10, and CIFAR-100 show improved test accuracy and privacy--utility performance over DP-SGD and private baselines including DP-Adam, DP-IS, SA-DP-SGD, ADP-AdamW, DP-SAT, and DP-Adam-AC.

---

## 论文详细总结（自动生成）

以下是对论文《Deep Learning under Fractional-Order Differential Privacy》（分数阶差分隐私下的深度学习）的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：差分隐私随机梯度下降（DP-SGD）是隐私保护学习的标准算法，但其面临梯度裁剪带来的偏差和高斯噪声累积导致的方差问题，往往导致模型效用（Utility）大幅下降。
*   **核心挑战**：现有的改进方法大多集中在隐私会计（Accounting）或优化器端的后处理（如自适应学习率），而很少直接改进**私有查询机制（Private Query）**本身。
*   **整体含义**：本文提出了一种名为 **FO-DP-SGD** 的新机制，通过引入分数阶微积分（Fractional Calculus）的思想，在加噪前将当前梯度与历史私有输出进行递归聚合。这使得查询本身具备了“长记忆”特性，从而在不破坏隐私保证的前提下，提高了优化信号的质量。

### 2. 方法论
*   **核心思想**：将传统的“仅针对当前步的查询”扩展为“包含历史信息的递归查询”。
*   **关键技术细节**：
    1.  **分数阶递归查询**：在添加高斯噪声之前，查询函数 $r_t$ 由当前裁剪梯度和（权重为 $\beta$）与历史私有记忆状态 $u_{t-1}$（权重为 $1-\beta$）加权组成。
    2.  **指数移动平均（EMA）趋势跟踪**：利用先前发布的私有输出构建 EMA 趋势，用于衡量历史数据的可靠性。
    3.  **不一致性感知回火（Tempering）**：计算历史发布值与当前 EMA 趋势的差异（不一致性得分），对偏离较大的历史信息进行抑制。
    4.  **置信度感知缩放**：引入置信因子，当私有趋势较弱或噪声过大时，减弱不一致性抑制的强度，以保持稳定性。
    5.  **分数阶内核**：使用幂律权重（由分数阶参数 $\alpha$ 控制）来聚合历史信息，相比简单的指数衰减，能更好地捕捉长程依赖。
*   **隐私保证**：通过理论证明，在给定历史记录的条件下，该递归查询的 $\ell_2$ 敏感度被限制在 $\beta C$（$C$ 为裁剪阈值）。这使得它能无缝兼容标准的 Rényi 差分隐私（RDP）会计框架。

### 3. 实验设计
*   **数据集**：SVHN、CIFAR-10、CIFAR-100。
*   **Benchmark 与对比方法**：
    *   **基础基准**：标准 DP-SGD。
    *   **先进优化器**：DP-Adam、DP-AdamW、DP-IS（重要性采样）、SA-DP-SGD（锐度感知）、ADP-AdamW、DP-SAT、DP-Adam-AC（自适应裁剪）。
*   **实验场景**：在固定的隐私预算 $\varepsilon$ 和 $\delta=10^{-5}$ 下，对比不同算法的测试准确率曲线和最终收敛性能。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练时长**：文中提供了部分运行时间数据（如在 CIFAR-10 上训练 250 个 Epoch 约需 60.1 秒），并指出 FO-DP-SGD 的计算开销极低，仅比标准 DP-SGD 增加约 2% 的运行时间，显著快于 DP-SAM 和 DP-SAT 等复杂优化器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三个主流视觉基准数据集。
    *   每组实验均重复 5 次独立运行，并报告了均值、标准差及 95% 置信区间。
    *   **消融实验**：非常充分。分别针对混合系数 $\beta$、分数阶 $\alpha$、记忆窗口 $K$、裁剪阈值 $C$ 以及“机制级记忆”与“后处理记忆”的优劣进行了详细对比。
*   **客观性**：实验采用了统一的网络架构（全连接层 + Tanh 激活）和相同的预处理流程，确保了对比的公平性。

### 6. 主要结论与发现
*   **效用提升**：FO-DP-SGD 在所有数据集上的表现均优于标准 DP-SGD 和多种自适应 DP 优化器。
*   **隐私-效用权衡**：在相同的隐私预算下，FO-DP-SGD 能达到更高的准确率；或者说，达到相同准确率所需的 $\varepsilon$ 更小。
*   **机制优势**：实验证明，在加噪**前**引入记忆（机制级）比在加噪**后**进行平滑（后处理级）效果更好，因为前者能有效降低单步敏感度。
*   **参数敏感性**：中间值的 $\beta$（如 0.9）和 $\alpha$（如 0.8）通常能提供最佳的平衡。

### 7. 优点
*   **理论与实践结合**：将分数阶微积分这一数学工具巧妙地引入差分隐私领域，且理论证明严谨。
*   **低开销高收益**：算法实现简单，几乎不增加计算负担，但对模型性能的提升显著。
*   **兼容性强**：不改变现有的隐私会计工具，可作为现有 DP 训练流水线的插件式改进。

### 8. 不足与局限
*   **模型复杂度**：实验主要基于较小的全连接网络，尚未在超大规模预训练模型（如大语言模型或大型视觉 Transformer）上验证其扩展性。
*   **噪声继承**：虽然引入了记忆，但历史记忆中也包含了旧的噪声。论文提到未来需要研究更显式的私有记忆去噪或滤波机制（如卡尔曼滤波）。
*   **超参数增加**：相比 DP-SGD，引入了更多需要调节的超参数（如 $\alpha, \beta, \gamma, \zeta$ 等），增加了调参成本。

（完）
