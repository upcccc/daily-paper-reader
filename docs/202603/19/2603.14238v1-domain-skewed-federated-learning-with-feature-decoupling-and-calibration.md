---
title: Domain-Skewed Federated Learning with Feature Decoupling and Calibration
title_zh: 基于特征解耦与校准的领域偏斜联邦学习
authors: "Huan Wang, Jun Shen, Jun Yan, Guansong Pang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14238v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 允许分布式客户端协作训练的联邦学习
tldr: 针对联邦学习中因客户端数据领域偏差（Domain Skew）导致的模型泛化性差问题，本文提出 $F^2$DC 框架。该方法通过领域特征解耦器（DFD）将局部特征分为领域鲁棒和领域相关特征，并利用领域特征校正器（DFC）校正偏差特征以提取更多类别相关信息，最后结合领域感知聚合机制，有效提升了模型在多领域场景下的表征一致性和泛化性能。
selection_source: fresh_fetch
motivation: 客户端间存在的领域偏差会导致局部模型特征坍缩到狭窄的低维子空间，从而阻碍全局模型学习到一致且泛化性强的表征。
method: 提出由领域特征解耦器（DFD）和领域特征校正器（DFC）组成的 $F^2$DC 框架，实现特征的鲁棒性分离与偏差校正，并配合领域感知聚合。
result: 在三个主流多领域数据集上的实验结果表明，$F^2$DC 显著优于现有方法，验证了其解耦和校正模块的有效性。
conclusion: 通过显式解耦和校正领域偏差特征，可以有效缓解联邦学习中的领域偏移挑战，增强全局模型的跨领域一致性。
---

## 摘要
联邦学习（FL）允许分布式客户端以隐私保护的方式协作训练全局模型。然而，一个主要的挑战是领域偏斜（domain skew），即来自不同领域的客户端数据可能会阻碍聚合后的全局模型学习到一致的表示空间，从而导致在多个领域中的泛化能力较差。在本文中，我们认为领域偏斜体现在每个客户端特定于领域的偏置特征中，导致本地模型的表示坍缩到一个狭窄的低维子空间中。随后，我们提出了联邦特征解耦与校准（$F^2$DC），通过校准特定于领域的偏置特征来释放有价值的类别相关信息，从而实现跨领域更一致的表示。$F^2$DC 首先引入了一个新颖的组件——领域特征解耦器（DFD），用于确定每个特征单元的鲁棒性，从而将本地特征分离为领域鲁棒特征和领域相关特征。此外，还提出了领域特征修正器（DFC），通过显式链接判别信号来校准这些领域相关特征，捕捉补充领域鲁棒特征的额外类别相关线索。最后，执行本地模型的领域感知聚合，以促进客户端之间的共识。在三个流行的多领域数据集上的实验结果证明了所提 $F^2$DC 的有效性及其两个模块的贡献。代码可在 https://github.com/mala-lab/F2DC 获取。

## Abstract
Federated learning (FL) allows distributed clients to collaboratively train a global model in a privacy-preserving manner. However, one major challenge is domain skew, where clients' data originating from diverse domains may hinder the aggregated global model from learning a consistent representation space, resulting in poor generalizable ability in multiple domains. In this paper, we argue that the domain skew is reflected in the domain-specific biased features of each client, causing the local model's representations to collapse into a narrow low-dimensional subspace. We then propose Federated Feature Decoupling and Calibration ($F^2$DC), which liberates valuable class-relevant information by calibrating the domain-specific biased features, enabling more consistent representations across domains. A novel component, Domain Feature Decoupler (DFD), is first introduced in $F^2$DC to determine the robustness of each feature unit, thereby separating the local features into domain-robust features and domain-related features. A Domain Feature Corrector (DFC) is further proposed to calibrate these domain-related features by explicitly linking discriminative signals, capturing additional class-relevant clues that complement the domain-robust features. Finally, a domain-aware aggregation of the local models is performed to promote consensus among clients. Empirical results on three popular multi-domain datasets demonstrate the effectiveness of the proposed $F^2$DC and the contributions of its two modules. Code is available at https://github.com/mala-lab/F2DC.

---

## 论文详细总结（自动生成）

这是一份关于论文《Domain-Skewed Federated Learning with Feature Decoupling and Calibration》（基于特征解耦与校准的领域偏斜联邦学习）的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：联邦学习（FL）在处理异构数据时面临巨大挑战。现有研究多关注“标签偏斜”（类别分布不均），而忽略了**“领域偏斜”（Domain Skew）**，即不同客户端的数据虽然类别相同，但来源领域（如照片、草图、卡通等）迥异。
*   **核心问题**：领域偏斜会导致局部模型过度拟合特定领域的特征，产生“维度坍缩”现象（特征仅存在于狭窄的低维子空间），使得聚合后的全局模型在跨领域泛化时表现极差。
*   **研究动机**：作者认为领域偏置特征中其实潜藏着有价值的类别信息。与其像传统方法那样直接剔除这些偏置，不如通过“校准”将其转化为可利用的判别信号。

### 2. 方法论（$F^2$DC 框架）
该方法由三个核心组件构成，旨在实现特征的精准分离与重塑：
*   **领域特征解耦器 (DFD)**：
    *   利用一个轻量级 CNN 模块生成归因图，并通过 **Gumbel Concrete 分布** 生成伪二进制掩码。
    *   将原始特征图解耦为：**领域鲁棒特征**（跨领域一致的判别信息）和**领域相关特征**（包含领域偏置及潜在类别线索）。
    *   通过分离性损失和判别性损失进行优化，确保两类特征有效分离。
*   **领域特征校正器 (DFC)**：
    *   针对解耦出的“领域相关特征”，利用残差连接结构进行校准。
    *   通过显式注入类别标签信号，强制该模块从偏置特征中提取被掩盖的类别相关线索，从而补充领域鲁棒特征。
*   **领域感知聚合 (Domain-Aware Aggregation)**：
    *   在服务端聚合时，不再采用简单的加权平均。
    *   引入**领域差异度 (dk)** 指标（衡量局部领域分布与理想均匀分布的距离），结合数据量大小动态计算聚合权重，以促进客户端间的共识。

### 3. 实验设计
*   **数据集**：
    *   **Digits**：包含 MNIST, USPS, SVHN, SYN 四个领域。
    *   **Office-Caltech**：包含 Caltech, Amazon, Webcam, DSLR 四个领域。
    *   **PACS**：包含 Photo, Art-Painting, Cartoon, Sketch 四个领域。
*   **Benchmark 与对比方法**：
    *   对比了 9 种 SOTA 方法，包括基础方法（FedAvg, FedProx）、对比学习方法（MOON, FedRCL）、原型学习方法（FPL, FedTGP, FedSA）以及专门针对领域偏斜的方法（FedHEAL, FDSE）。
*   **实验设置**：使用 ResNet-10 作为骨干网络，设置 10-20 个客户端，进行 100 轮通信。

### 4. 资源与算力
*   **硬件信息**：论文**未明确说明**具体的 GPU 型号（如 A100 或 RTX 3090）及数量。
*   **训练效率**：文中提供了效率对比表。在 PACS 数据集上，10 个客户端单轮训练时间约为 180.67 秒，FLOPs 为 $16.46 \times 10^{10}$。
*   **开销分析**：相比 FedAvg，$F^2$DC 没有增加额外的通信开销（解耦器和校正器参数保留在本地），计算开销略有增加但处于可接受范围。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流多领域数据集上进行了详尽测试，每组实验均运行 3 次取平均值。
*   **消融实验**：非常充分。分别验证了 DFD、DFC 和领域感知聚合三个模块的独立贡献，并分析了不同特征（$f^+, f^-, f^*$）对最终性能的影响。
*   **超参数分析**：对损失权重（$\lambda_1, \lambda_2$）、平滑系数（$\sigma$）、温度参数（$\tau$）以及聚合系数（$\alpha, \beta$）进行了敏感性测试。
*   **可视化分析**：提供了 T-SNE 聚类图和 Grad-CAM 特征热力图，直观展示了方法在减少特征坍缩和提升关注点一致性方面的效果。

### 6. 主要结论与发现
*   **性能领先**：$F^2$DC 在所有数据集上的平均准确率（AVG）均排名第一，且标准差（STD）最低，证明了其极强的泛化性和公平性。
*   **模块化优势**：将 DFD 和 DFC 插件式地集成到 FedAvg 或 FPL 等现有方法中，均能带来显著的性能提升（如 FedAvg 在 PACS 上提升了 8.94%）。
*   **特征价值**：实验证明，校准后的领域相关特征（$f^*$）比原始偏置特征（$f^-$）具有更强的判别力，验证了“校准胜过剔除”的观点。

### 7. 优点
*   **视角新颖**：不同于以往“消除偏置”的思路，提出了“校准并利用偏置”的新路径。
*   **通信友好**：解耦和校准模块仅在本地运行，不参与全局参数交换，保护隐私的同时不增加带宽负担。
*   **鲁棒性强**：通过领域感知聚合有效缓解了“大领域”主导全局模型的问题。

### 8. 不足与局限
*   **场景假设限制**：实验假设每个客户端仅属于一个单一领域，但在现实中，单个客户端可能拥有混合领域的数据，该场景下的表现尚未验证。
*   **超参数依赖**：框架引入了较多超参数（如 $\lambda, \sigma, \tau, \alpha, \beta$），虽然实验证明了其稳定性，但在全新数据集上的调参成本可能较高。
*   **模型深度**：实验主要基于 ResNet-10 等较浅的网络，在超大规模模型（如 Transformer 或大语言模型）上的适用性有待进一步探讨。

（完）
