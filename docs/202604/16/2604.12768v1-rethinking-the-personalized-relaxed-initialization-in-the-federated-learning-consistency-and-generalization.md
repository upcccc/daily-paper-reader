---
title: "Rethinking the Personalized Relaxed Initialization in the Federated Learning: Consistency and Generalization"
title_zh: 重新审视联邦学习中的个性化松弛初始化：一致性与泛化
authors: "Li Shen, Yan Sun, Dacheng Tao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12768v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 高效的联邦学习算法 FedInit
tldr: 针对联邦学习中异构数据导致的“客户端漂移”问题，本文提出了FedInit算法。该方法在每轮本地训练开始时，通过向先前本地状态的反方向移动来调整全局状态，实现个性化的松弛初始化。理论分析揭示了局部不一致性主要影响泛化误差而非优化误差。实验证明，FedInit在不增加额外通信和计算开销的情况下，显著提升了模型的泛化性能，并可无缝集成至现有算法中。
selection_source: fresh_fetch
motivation: 旨在解决联邦学习中因数据异构导致的客户端漂移问题，并深入探究局部不一致性对模型性能的具体影响机制。
method: 提出FedInit算法，在本地训练开始前，利用当前全局状态与上一轮本地状态的反向偏移进行个性化松弛初始化。
result: 理论证明局部不一致性主要影响泛化误差界，实验显示FedInit在零额外开销下达到了领先的性能水平。
conclusion: 个性化松弛初始化是缓解客户端漂移、提升联邦学习泛化能力的有效手段，具有极高的实用价值和兼容性。
---

## 摘要
联邦学习（FL）是一种分布式范式，通过在异构数据集上进行阶段性的本地训练过程，协调海量本地客户端协作训练全局模型。先前的研究隐式地探讨了联邦学习面临的“客户端漂移”（client-drift）问题，该问题是由本地客户端之间不一致的最优点引起的。然而，到目前为止，仍缺乏可靠的理论分析来解释这种本地不一致性的影响。为了减轻“客户端漂移”的负面影响并探索其在联邦学习中的本质，本文首先提出了一种高效的联邦学习算法 FedInit，该算法允许在每个本地训练阶段开始时采用个性化松弛初始化状态。具体而言，FedInit 通过从当前全局状态向最新本地状态的反方向移动来初始化本地状态。此外，为了进一步理解不一致性如何破坏联邦学习的性能，我们引入了超额风险分析，并研究了散度项以调查联邦学习中的测试误差。我们的研究表明，优化误差对这种本地不一致性并不敏感，而它主要影响泛化误差界。我们进行了大量的实验来验证其效率。所提出的 FedInit 方法在不增加任何额外训练或通信成本的情况下，能够取得与几种先进基准算法相当的结果。同时，阶段性的个性化松弛初始化也可以整合到现有的几种先进算法中，以在联邦学习范式中实现更高的泛化性能。

## Abstract
Federated learning (FL) is a distributed paradigm that coordinates massive local clients to collaboratively train a global model via stage-wise local training processes on the heterogeneous dataset. Previous works have implicitly studied that FL suffers from the ``client-drift'' problem, which is caused by the inconsistent optimum across local clients. However, till now it still lacks solid theoretical analysis to explain the impact of this local inconsistency. To alleviate the negative impact of ``client drift'' and explore its substance in FL, in this paper, we first propose an efficient FL algorithm FedInit, which allows employing the personalized relaxed initialization state at the beginning of each local training stage. Specifically, FedInit initializes the local state by moving away from the current global state towards the reverse direction of the latest local state. Moreover, to further understand how inconsistency disrupts performance in FL, we introduce the excess risk analysis and study the divergence term to investigate the test error in FL. Our studies show that optimization error is not sensitive to this local inconsistency, while it mainly affects the generalization error bound. Extensive experiments are conducted to validate its efficiency. The proposed FedInit method could achieve comparable results compared to several advanced benchmarks without any additional training or communication costs. Meanwhile, the stage-wise personalized relaxed initialization could also be incorporated into several current advanced algorithms to achieve higher generalization performance in the FL paradigm.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking the Personalized Relaxed Initialization in the Federated Learning: Consistency and Generalization》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：联邦学习（FL）在处理非独立同分布（Non-IID）数据时，普遍面临“客户端漂移”（Client-drift）问题，即各客户端本地最优点不一致导致全局模型性能下降。
*   **研究动机**：虽然已有许多方法尝试缓解漂移，但学术界仍缺乏对“局部不一致性”如何具体影响模型性能（尤其是泛化能力）的深入理论解释。
*   **整体含义**：本文旨在通过引入一种简单的“个性化松弛初始化”机制，在不增加通信和计算负担的前提下，提升联邦学习的一致性与泛化性能，并从理论上探讨优化误差与泛化误差的解耦关系。

### 2. 论文提出的方法论：FedInit
*   **核心思想**：在每一轮本地训练开始时，不再直接使用全局模型 $w_t$ 作为起点，而是根据该客户端上一轮的本地状态进行“反向修正”初始化。
*   **关键技术细节**：
    *   **松弛初始化（Relaxed Initialization, RI）**：本地初始化状态定义为 $w_{i,0}^t = w_t + \beta(w_t - w_{i,K}^{t-1})$。其中 $\beta$ 是控制个性化程度的系数。
    *   **逻辑解释**：该公式意味着初始状态是从当前全局模型出发，向着上一轮本地更新方向的**反方向**移动。这种做法增加了本地模型在训练开始时与本地最优点的距离，迫使各客户端在本地训练过程中维持更高的一致性，从而在聚合时减少漂移。
    *   **超额风险分析**：论文引入了超额风险（Excess Risk）框架，将其拆分为**优化误差**（模型在训练集上的收敛程度）和**泛化误差**（训练集与测试集的表现差异）。

### 3. 实验设计
*   **数据集与场景**：
    *   使用经典图像分类数据集 **CIFAR-10** 和 **CIFAR-100**。
    *   通过 Dirichlet 分布（$Dr=0.1$ 和 $0.6$）模拟数据异构性。
    *   额外引入了“客户端偏置”（RGB通道噪声）和“类别偏置”（亮度扰动）来增强异构挑战。
*   **模型架构**：ResNet-18-GN（使用Group Normalization替代BN以适应FL）和 VGG-11。
*   **对比方法（Benchmarks）**：
    *   基础算法：FedAvg, FedAdam。
    *   抗漂移/一致性算法：SCAFFOLD, FedDyn, FedCM。
    *   泛化增强算法：FedSAM。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **效率评估**：作者通过“墙钟时间”（Wall-clock time）和通信轮数来间接评估效率。实验结果显示 FedInit 的单轮耗时与最简单的 FedAvg 几乎持平，远低于 FedSAM 和 SCAFFOLD。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 100 和 200 个客户端规模，参与率分别为 10% 和 5%。
    *   针对不同异构程度（Dirichlet 0.1/0.6）进行了多次重复实验并标注了标准差。
    *   进行了详细的敏感性分析（针对本地迭代次数 $K$ 和松弛系数 $\beta$）。
    *   **插件实验**：将 RI 机制集成到 SCAFFOLD、FedSAM 等算法中，验证了其普适性。
*   **充分性评价**：实验设计较为充分且客观。通过在多种算法上作为“插件”进行对比，有力证明了该初始化机制的独立有效性。

### 6. 论文的主要结论与发现
*   **理论发现**：局部不一致性（一致性项 $\Delta_t$）对**优化误差**的影响相对较小，但它是决定**泛化误差界**的关键因素。
*   **性能结论**：FedInit 在不增加额外通信开销（如 SCAFFOLD 需要双倍通信）和计算开销（如 FedSAM 需要双倍梯度计算）的情况下，达到了 SOTA 性能。
*   **机制发现**：正向的 $\beta$（即远离本地旧状态）能有效缩小客户端间的散度，提升模型稳定性。

### 7. 优点：亮点与创新
*   **极简且高效**：FedInit 仅需一行代码修改初始化逻辑，不改变通信协议，不增加存储负担，非常适合资源受限的边缘设备。
*   **理论深度**：不同于以往只关注收敛性的论文，本文通过超额风险分析深入探讨了泛化性，为理解联邦学习中的一致性提供了新视角。
*   **高兼容性**：作为一种初始化策略，它可以与几乎所有现有的联邦学习优化器（如 FedAdam, SCAFFOLD）无缝结合。

### 8. 不足与局限
*   **超参数敏感性**：虽然 $\beta$ 的搜索范围有限，但最佳 $\beta$ 值可能随数据集和异构程度变化，论文目前主要采用常数设置，缺乏自适应调整策略。
*   **实验覆盖面**：实验主要集中在计算机视觉（CV）任务和有监督学习，未在自然语言处理（NLP）或强化学习等其他 FL 场景中验证。
*   **理论假设**：部分理论推导依赖于 PL-condition 或 Interpolation 假设，这些假设在极度复杂的非凸深度学习模型中可能并不完全成立。

（完）
