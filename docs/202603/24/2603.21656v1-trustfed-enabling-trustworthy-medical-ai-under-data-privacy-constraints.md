---
title: "TrustFed: Enabling Trustworthy Medical AI under Data Privacy Constraints"
title_zh: TrustFed：在数据隐私约束下实现可信医疗人工智能
authors: "Vagish Kumar, Syed Bahauddin Alam, Souvik Chakraborty"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21656v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 联邦学习实现隐私保护的多机构训练
tldr: 针对医疗联邦学习中数据异构、偏置和类别不平衡导致的预测可靠性问题，本文提出TrustFed框架。该框架通过表示感知客户端分配和软最近阈值聚合策略，在不共享原始数据的前提下，实现了具有统计覆盖保证的分布无关不确定性量化。在超过43万张跨6种模态的医学图像上验证，证明了其在复杂医疗场景下的鲁棒性和临床应用潜力。
selection_source: fresh_fetch
motivation: 医疗联邦学习面临严重的数据异构和类别不平衡挑战，导致现有不确定性量化方法在保障预测可靠性方面失效。
method: 提出一种联邦不确定性量化框架，引入表示感知客户端分配机制和软最近阈值聚合策略，实现跨机构的有效校准。
result: 在涵盖6种模态、超过43万张医学图像的大规模实验中，该方法在各种不平衡场景下均实现了稳健的有限样本覆盖保证。
conclusion: TrustFed推动了联邦学习从概念验证向临床部署迈进，证明了统计保证的不确定性是构建下一代医疗AI系统的核心要求。
---

## 摘要
保护患者隐私仍然是跨医疗机构扩展机器学习的基础性障碍，由于伦理、法律和监管约束，集中敏感数据通常是不可行的。联邦学习通过在不共享原始患者数据的情况下实现隐私保护的多机构训练，提供了一种极具前景的替代方案；然而，实际部署面临着来自数据异构性、站点特有偏差和类别不平衡的严峻挑战，这些挑战降低了预测的可靠性，并使现有的不确定性量化方法失效。在此，我们提出了 TrustFed，这是一个联邦不确定性量化框架，它在异构且不平衡的医疗数据下提供无分布、有限样本的覆盖率保证，且无需集中访问。TrustFed 引入了一种表征感知的客户端分配机制，利用内部模型表征来实现跨机构的有效校准，并结合软最近阈值聚合策略，在减少分配不确定性的同时生成紧凑且可靠的预测集。利用涵盖六种临床不同成像模态的超过 430,000 张医学图像，我们对医学影像中的不确定性感知联邦学习进行了最全面的评估之一，证明了在具有不同类别基数和不平衡状态的数据集上具有稳健的覆盖率保证。通过在这种规模和广度上验证 TrustFed，我们的研究将不确定性感知联邦学习从概念验证推向了具有临床意义、模态无关的部署，将具有统计保证的不确定性定位为下一代医疗 AI 系统的核心要求。

## Abstract
Protecting patient privacy remains a fundamental barrier to scaling machine learning across healthcare institutions, where centralizing sensitive data is often infeasible due to ethical, legal, and regulatory constraints. Federated learning offers a promising alternative by enabling privacy-preserving, multi-institutional training without sharing raw patient data; however, real-world deployments face severe challenges from data heterogeneity, site-specific biases, and class imbalance, which degrade predictive reliability and render existing uncertainty quantification methods ineffective. Here, we present TrustFed, a federated uncertainty quantification framework that provides distribution-free, finite-sample coverage guarantees under heterogeneous and imbalanced healthcare data, without requiring centralized access. TrustFed introduces a representation-aware client assignment mechanism that leverages internal model representations to enable effective calibration across institutions, along with a soft-nearest threshold aggregation strategy that mitigates assignment uncertainty while producing compact and reliable prediction sets. Using over 430,000 medical images across six clinically distinct imaging modalities, we conduct one of the most comprehensive evaluations of uncertainty-aware federated learning in medical imaging, demonstrating robust coverage guarantees across datasets with diverse class cardinalities and imbalance regimes. By validating TrustFed at this scale and breadth, our study advances uncertainty-aware federated learning from proof-of-concept toward clinically meaningful, modality-agnostic deployment, positioning statistically guaranteed uncertainty as a core requirement for next-generation healthcare AI systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《TrustFed: Enabling Trustworthy Medical AI under Data Privacy Constraints》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在医疗人工智能领域，由于 HIPAA、GDPR 等严格的隐私法规，集中存储患者数据进行模型训练极具挑战。**联邦学习（FL）**虽能实现“数据不动模型动”，但在实际临床场景中面临**严重的数据异构性（Non-IID）**、**站点特有偏置**和**类别极度不平衡**。
这些问题导致现有模型往往产生“过度自信”或“校准不良”的预测，缺乏可靠的**不确定性量化（UQ）**。论文的核心动机是：如何在保护隐私的前提下，为异构联邦环境下的医疗 AI 提供具有统计学保证（即：预测结果能以预定义的概率包含正确答案）的可靠不确定性估计。

### 2. 提出方法论：TrustFed 框架
TrustFed 是首个在异构医疗数据下提供“无分布、有限样本覆盖保证”的联邦符合性预测（Conformal Prediction）框架。其核心流程如下：
*   **联邦训练阶段**：采用 **CortiNet**（一种基于小波变换的物理感知混合模型）作为骨干网络，通过标准联邦优化算法训练全局模型。
*   **本地校准与特征库构建**：每个客户端利用本地持有的校准集计算“非符合性分数”（通常为 $1 - \hat{p}_y$），并计算本地阈值 $\tau_k$。同时，客户端存储校准样本的特征向量（Embedding）构建“特征库”。
*   **表示感知客户端分配（Representation-aware Assignment）**：在推理时，服务器提取测试样本的特征，各客户端计算该特征与本地特征库的最小距离并返回。服务器据此识别出与该样本最相似的 $N_k$ 个客户端。
*   **软最近阈值聚合（Soft-nearest Threshold Aggregation）**：服务器不直接使用全局统一阈值，而是取这 $N_k$ 个最相关客户端阈值的最大值（$\tau = \max \tau_j$）。这种“保守”策略能有效抵消分配误差，确保在异构环境下的覆盖率。

### 3. 实验设计
*   **数据集**：使用了大规模医学影像基准 **MedMNIST v2**，涵盖 6 种临床模态（血细胞显微镜、视网膜眼底、腹部 CT、皮肤病变、结肠组织病理学、肾脏组织显微镜）。
*   **数据规模**：总计超过 **430,000 张图像**，类别数量从 5 类到 11 类不等。
*   **实验场景**：模拟了两种极端的联邦环境：
    1.  **类别不平衡**：不同客户端拥有的疾病类别比例严重失调。
    2.  **样本量不平衡**：不同客户端拥有的数据总量差异巨大。
*   **对比方法（Benchmark）**：
    *   **FCP (Federated Conformal Prediction)**：基于全局池化分数的现有联邦符合性预测方法。
    *   **Local**：仅依赖本地客户端数据进行校准的方法。
    *   **Proposed (TrustFed)**：本文提出的样本自适应方法。

### 4. 资源与算力
论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）及总训练时长。但作者提到所选用的 **CortiNet** 架构具有参数高效（Parameter-efficient）和低推理延迟的特点，适合在资源受限的分布式环境（如医院服务器）中部署。

### 5. 实验数量与充分性
*   **实验数量**：在 6 个不同任务上分别进行了覆盖率（Coverage）和预测集大小（Efficiency/Cardinality）的测试，并针对超参数 $N_k$（邻居数量）进行了消融实验。
*   **充分性与公平性**：实验涵盖了从微观（显微镜）到宏观（CT）的多种模态，样本量巨大，具有很强的统计说服力。通过对比不同 $\alpha$ 水平（错误率容忍度），验证了算法在各种置信度要求下的稳健性。实验设计考虑了现实中常见的“长尾分布”和“站点差异”，评估较为客观。

### 6. 主要结论与发现
*   **统计有效性**：TrustFed 在所有模态和不平衡场景下，均能严格达到预设的置信水平（如 90% 的覆盖率），而基准方法在异构严重时会出现覆盖不足。
*   **预测集紧凑性**：在保证覆盖率的同时，TrustFed 生成的预测标签集合非常小（通常仅包含 1-2 个候选类别），具有极高的临床实用价值。
*   **表示感知的优势**：基于模型内部特征的客户端匹配远优于基于原始像素的匹配，能更准确地找到数据分布相似的机构。
*   **鲁棒性**：即使在极少数类（罕见病）上，该方法也能提供可靠的预测区间。

### 7. 优点
*   **理论严谨**：提供了有限样本下的数学覆盖保证，而非启发式的置信度评分。
*   **隐私保护**：仅交换标量（距离和阈值），不泄露原始图像、标签或高维特征。
*   **样本自适应**：阈值随测试样本动态调整，能够处理跨机构的分布偏移。
*   **模态无关**：在 6 种完全不同的医学影像任务上表现一致，证明了极强的通用性。

### 8. 不足与局限
*   **任务局限**：目前仅验证了分类任务，尚未扩展到回归（如生理指标预测）或分割（如肿瘤轮廓提取）任务。
*   **超参数依赖**：邻居数量 $N_k$ 的选择目前仍依赖经验设定，缺乏一种自动化的理论最优选择机制。
*   **推理开销**：相比普通 FL，在推理阶段增加了一轮与客户端的标量通信（计算距离和获取阈值），虽然开销极小，但在极端实时场景下需考虑。
*   **多模态缺失**：实验虽跨越多种模态，但每个任务内部是单模态的，未探讨如“影像+电子病历”结合的联邦不确定性量化。

（完）
