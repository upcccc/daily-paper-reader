---
title: "Metric-Normalized Posterior Leakage (mPL): Attacker-Aligned Privacy for Joint Consumption"
title_zh: 度量归一化后验泄漏 (mPL)：面向联合观测的攻击者对齐隐私
authors: "Gaoyi Chen, Minghao Li, Weishi Shi, Yan Huang, Yusheng Wei, Sourabh Yadav, Chenxi Qiu"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.01137v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 联合观测下的度量差分隐私与本地差分隐私
tldr: 针对度量差分隐私（mDP）在联合观测场景下因证据聚合导致隐私泄露的问题，本文提出了度量归一化后验泄露（mPL）指标。该指标通过衡量后验赔率偏移来对齐攻击者视角。为应对联合消费中的隐私风险，研究者进一步提出了概率有界mPL（PBmPL）及其实现框架AmPL，通过“扰动-审计-自适应”流程，在词向量嵌入实验中有效降低了隐私违规频率，同时保持了较高的实用性。
selection_source: fresh_fetch
motivation: 传统的度量差分隐私在多记录联合观测时，由于学习型聚合器会复合证据，导致单条记录的隐私保证在证据聚合下失效。
method: 提出度量归一化后验泄露（mPL）指标，并开发了Adaptive mPL (AmPL) 框架，利用学习型攻击者进行实时审计与参数自适应调整。
result: 在词嵌入案例研究中，AmPL 在联合消费场景下显著降低了神经对抗攻击引发的隐私违规，且效用损失较小。
conclusion: PBmPL 为联合消费环境下的隐私保护提供了一种实用且可验证的防御机制，有效弥补了传统度量差分隐私的不足。
---

## 摘要
度量差分隐私 (mDP) 通过根据语义距离缩放噪声来增强本地差分隐私 (LDP)，但许多机器学习 (ML) 系统是在联合观测下使用的，在这种情况下，与模型无关的逐记录保证可能会忽略证据聚合带来的泄漏。我们引入了度量归一化后验泄漏 (mPL)，这是一种与攻击者对齐、经过距离校准的度量，用于衡量发布所引起的后验赔率偏移，并证明对于单次或独立的发布，一致地限制 mPL 等同于 mDP。然而，在联合观测下，即使满足 mDP，mPL 仍可能很高，因为学习到的聚合器会跨相关项复合证据。为了使控制具有实用性，我们形式化了概率有界 mPL (PBmPL)，它限制了 mPL 超过目标预算的频率，并利用自适应 mPL (AmPL) 将其付诸实践。AmPL 是一个“信任并验证”框架，它进行扰动、使用学习到的攻击者进行审计，并调整参数（带有可选的贝叶斯重映射）以平衡隐私和效用。在词嵌入案例研究中，尽管存在逐记录的 mDP 扰动，神经对抗者在联合观测下仍会违反 mPL，而 AmPL 在效用损失较低的情况下显著降低了此类违规的频率，这表明 PBmPL 是联合观测场景下一种实用且可证明的保护措施。

## Abstract
Metric differential privacy (mDP) strengthens local differential privacy (LDP) by scaling noise to semantic distance, but many machine learning (ML) systems are consumed under joint observation, where model-agnostic, per-record guarantees can miss leakage from evidence aggregation. We introduce metric-normalized posterior leakage (mPL), an attacker-aligned, distance-calibrated measure of posterior-odds shift induced by releases, and show that for single or independent releases, uniformly bounding mPL is equivalent to mDP. Under joint observation, however, satisfying mDP may still leave mPL high because learned aggregators compound evidence across correlated items. To make control practical, we formalize probabilistically bounded mPL (PBmPL), which limits how often mPL may exceed a target budget, and we operationalize it via Adaptive mPL (AmPL), a trust-and-verify framework that perturbs, audits with a learned attacker, and adapts parameters (with optional Bayesian remapping) to balance privacy and utility. In a word-embedding case study, neural adversaries violate mPL under joint consumption despite per-record mDP perturbations, whereas AmPL substantially lowers the frequency of such violations with low utility loss, indicating PBmPL as a practical, certifiable protection for joint-consumption settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Metric-Normalized Posterior Leakage (mPL): Attacker-Aligned Privacy for Joint Consumption》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的**度量差分隐私 (mDP)** 主要针对单条记录提供隐私保证。然而，在实际应用中，攻击者往往能同时观测到多个相关的发布记录（即“联合消费”，如一句话中的多个词、轨迹中的多个点）。
*   **研究动机**：
    *   **证据聚合风险**：即使每条记录都满足 mDP，攻击者仍能通过学习型聚合器（如 RNN、Transformer）利用记录间的相关性来“复合”证据，从而大幅提高推断准确性。
    *   **指标失配**：现有的隐私指标（如 LDP）与攻击者的实际推断能力（后验信念的提升）之间存在脱节。
    *   **语义敏感性**：在连续空间（如词向量嵌入）中，简单的噪声注入无法平衡细粒度的语义保护与实用性。

### 2. 方法论
论文提出了一套从理论定义到工程实现的完整框架：
*   **mPL (Metric-Normalized Posterior Leakage)**：定义为观测到发布结果后，候选秘密对的“后验赔率”相对于“先验赔率”的变化量，并根据秘密间的度量距离进行归一化。
*   **PBmPL (Probabilistic Bounded mPL)**：考虑到最坏情况下的严格限制过于保守，提出概率有界版本，允许 mPL 在极小概率（$\delta$）下超过预算（$\epsilon$）。
*   **AmPL (Adaptive mPL) 框架**：
    1.  **分级扰动**：将数据分为不同敏感级（如 PII 个人身份信息和 PoII 潜在识别信息），应用不同强度的指数机制（Exponential Mechanism）。
    2.  **学习型攻击者审计**：训练神经推断模型（RNN/LSTM/Transformer）作为“代理攻击者”，模拟真实攻击并估算后验分布。
    3.  **反馈驱动调整**：根据攻击者的反馈，迭代优化扰动参数 $\alpha$，平衡隐私违规率与效用损失。
    4.  **贝叶斯重映射 (Bayesian Remapping)**：作为后处理步骤，将扰动后的输出映射回高概率的原始空间以恢复效用，且证明该步骤不增加隐私泄露。

### 3. 实验设计
*   **数据集**：
    *   **AG-News**：新闻分类，包含 12 万训练样本。
    *   **IMDB**：电影评论情感分析，5 万样本。
    *   **Amazon Reviews**：大规模评论数据集。
    *   **Breast Cancer Wisconsin**：用于验证方法在表格数据上的通用性。
*   **Benchmark 与对比方法**：
    *   **基准**：标准的指数机制 (EM)，仅满足逐记录的 mDP。
    *   **对比变体**：AmPL-U（侧重效用）、AmPL-P（侧重隐私）、AmPL-1（仅保护 PII）。
*   **攻击模型**：使用了 RNN、LSTM 和 Transformer 三种不同能力的神经架构作为推断攻击者。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 Adam 优化器，初始学习率为 0.001；在审计阶段，针对不同数据集采样了数千万个（如 AG-News 约 7400 万个）三元组进行违规率评估。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个文本数据集和 1 个表格数据集上进行了测试，涵盖了 3 种攻击者模型和多个隐私预算（$\epsilon$）梯度。
*   **充分性**：
    *   **消融实验**：通过 AmPL-U/P/1 等变体验证了分级保护和反馈机制的有效性。
    *   **统计显著性**：提供了均值和标准差，并利用 Hoeffding 不等式给出了采样审计的置信度保证（Proposition 3.2）。
    *   **客观性**：对比了不同能力的攻击者，证明了即使在强攻击者（Transformer）面前，该方法依然有效。

### 6. 主要结论与发现
*   **mDP 的局限性**：实验证实，即使满足 mDP 的机制，在神经攻击者的联合观测下仍会出现严重的 mPL 违规（违规率可达 10%-20%）。
*   **AmPL 的优越性**：AmPL 平均将隐私违规率降低了约 **29.89%**，同时通过贝叶斯重映射保持了与标准 EM 相当的效用水平。
*   **攻击者能力影响**：Transformer 攻击者通常能发现更多的隐私漏洞，证明了“攻击者在环（Attacker-in-the-loop）”审计的必要性。

### 7. 优点与亮点
*   **攻击者对齐**：将隐私保护从抽象的数学保证转向了防御实际推断攻击。
*   **自适应性**：AmPL 框架能够根据特定数据集的关联特征自动调整噪声，而非使用统一的理论界限。
*   **理论支撑**：证明了 mPL 在独立观测下与 mDP 的等价性，以及后处理不变性。

### 8. 不足与局限
*   **攻击者依赖性**：隐私保证的强度取决于“代理攻击者”的能力。如果实际攻击者拥有比审计模型更强的架构或辅助信息，隐私风险可能被低估。
*   **计算开销**：自适应调整过程需要反复训练攻击模型和进行大规模采样审计，计算成本高于静态扰动方法。
*   **组合性理论**：目前主要针对单次联合发布的审计，对于多次重复发布的通用组合定理（Composition Theorem）仍需进一步探索。

（完）
