---
title: "TADP-RME: A Trust-Adaptive Differential Privacy Framework for Enhancing Reliability of Data-Driven Systems"
title_zh: TADP-RME：一种用于增强数据驱动系统可靠性的信任自适应差分隐私框架
authors: "Labani Halder, Payel Sadhukhan, Sarbani Palit"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08113v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 自适应差分隐私与效用-隐私权衡
tldr: 针对数据驱动系统在对抗环境下的可靠性问题，本文提出TADP-RME框架。该框架通过引入逆信任评分动态调整差分隐私预算，以应对用户信任度的异构性，并结合逆流形嵌入技术，利用非线性变换破坏数据的局部几何结构。该方法在保持差分隐私保证的同时，有效抵御了推理攻击，显著优化了隐私与效用之间的权衡，为可靠学习提供了新方案。
selection_source: fresh_fetch
motivation: 现有差分隐私方案因固定预算导致效用-隐私权衡僵化，且无法应对利用数据几何结构的推理攻击。
method: 引入逆信任评分动态调节隐私预算，并采用逆流形嵌入进行非线性变换以破坏局部几何关系。
result: "在不显著降低效用的前提下，该框架将攻击成功率降低了高达3.1%，表现优于现有防御方法。"
conclusion: TADP-RME提供了一种能够适应不同信任水平的统一框架，增强了数据驱动系统在对抗环境下的可靠性。
---

## 摘要
在对抗性环境下确保可靠性，必须将隐私视为数据驱动系统的基础组成部分。虽然差分隐私和加密协议提供了强有力的保证，但现有方案依赖于固定的隐私预算，导致效用与隐私之间存在僵化的权衡，在用户信任度异构的情况下往往失效。此外，仅依靠噪声的差分隐私会保留几何结构，推理攻击会利用这一点，从而导致隐私泄露。我们提出了 TADP-RME（基于逆流形嵌入的信任自适应差分隐私），这是一个在不同用户信任水平下增强可靠性的框架。它引入了范围在 [0,1] 之间的逆信任评分，以自适应地调节隐私预算，从而实现效用与隐私之间的平滑过渡。此外，逆流形嵌入应用非线性变换来破坏局部几何关系，同时通过后处理保留形式化的差分隐私保证。理论和实证结果表明，该框架改善了隐私与效用之间的权衡，在不显著降低效用的情况下，将攻击成功率降低了高达 3.1%。该框架在对抗推理攻击方面始终优于现有方法，为对抗环境下的可靠学习提供了一种统一的方法。

## Abstract
Ensuring reliability in adversarial settings necessitates treating privacy as a foundational component of data-driven systems. While differential privacy and cryptographic protocols offer strong guarantees, existing schemes rely on a fixed privacy budget, leading to a rigid utility-privacy trade-off that fails under heterogeneous user trust. Moreover, noise-only differential privacy preserves geometric structure, which inference attacks exploit, causing privacy leakage.   We propose TADP-RME (Trust-Adaptive Differential Privacy with Reverse Manifold Embedding), a framework that enhances reliability under varying levels of user trust. It introduces an inverse trust score in the range [0,1] to adaptively modulate the privacy budget, enabling smooth transitions between utility and privacy. Additionally, Reverse Manifold Embedding applies a nonlinear transformation to disrupt local geometric relationships while preserving formal differential privacy guarantees through post-processing.   Theoretical and empirical results demonstrate improved privacy-utility trade-offs, reducing attack success rates by up to 3.1 percent without significant utility degradation. The framework consistently outperforms existing methods against inference attacks, providing a unified approach for reliable learning in adversarial environments.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《TADP-RME: A Trust-Adaptive Differential Privacy Framework for Enhancing Reliability of Data-Driven Systems》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在敏感领域（如医疗、金融）中，数据驱动系统的可靠性高度依赖于隐私保护。现有的差分隐私（DP）方案存在两个主要缺陷：
*   **权衡僵化：** 传统 DP 依赖固定的隐私预算 $\epsilon$，无法根据不同用户或应用场景的信任等级（Trust Levels）动态调整，导致在需要高精度时隐私过度，或在需要高隐私时保护不足。
*   **几何结构泄露：** 仅依靠添加噪声的 DP 机制虽然改变了数值，但往往保留了数据的局部几何结构（如邻域关系、聚类特征）。攻击者可以利用这些残余的几何足迹进行推理攻击（如成员推理、模型逆向）。
**研究动机：** 设计一个既能根据信任度自适应调整隐私强度，又能通过破坏数据几何结构来增强抗攻击能力的统一可靠性框架。

### 2. 论文提出的方法论
论文提出了 **TADP-RME** 框架，包含两个核心组件：

*   **信任自适应差分隐私（TADP）：**
    *   引入逆信任评分 $T \in [0, 1]$，其中 $T=0$ 代表完全信任（高精度需求），$T=1$ 代表完全不信任（高隐私需求）。
    *   **动态预算公式：** $\epsilon_T = \epsilon_{max} - T(\epsilon_{max} - \epsilon_{min})$。
    *   根据 $\epsilon_T$ 校准高斯噪声的方差 $\sigma_T^2$，实现隐私保护强度的平滑过渡。
*   **逆流形嵌入（Reverse Manifold Embedding, RME）：**
    *   这是一种非线性几何变换，旨在将数据映射到更高维的空间并扭曲其邻域关系。
    *   **变换公式：** $\phi(x_i) = (x_i \cos(\alpha x_i), x_i \sin(\alpha x_i))$。
    *   **核心思想：** 借鉴“瑞士卷”等非线性流形，使原始空间中相近的点在变换后变远，增加逆向重构的歧义性。由于 RME 是确定性的后处理步骤，它不会破坏前一步 TADP 提供的形式化 $( \epsilon, \delta )$-DP 保证。

### 3. 实验设计
*   **数据集：** MNIST（手写数字）、Fashion-MNIST（服饰）、CIFAR-10（彩色图像）。
*   **Benchmark（基准方法）：**
    *   **噪声类：** Gaussian DP, Laplace DP, Personalized DP (PDP)。
    *   **投影/几何类：** Random Projection, Reconstruction-Resistant Privacy。
    *   **编码类：** Locality-Sensitive Hashing (LSH), Binary Encoding。
*   **评估指标：**
    *   **效用（Utility）：** 逻辑回归分类准确率、k-NN 邻域重合度（结构保持）、Spearman 秩相关系数（全局距离保持）。
    *   **隐私（Privacy）：** 成员推理攻击（MIA）、属性推理攻击（AIA）、重构攻击（Reconstruction Attack）的成功率。

### 4. 资源与算力
*   论文提到实验环境基于 Python，使用了 Scikit-Learn 和 NumPy 库。
*   **算力说明：** 文中**未明确提及**具体的 GPU 型号、数量或具体的训练时长。考虑到实验主要涉及逻辑回归和基础图像数据集的扁平化处理，推测计算需求处于中等水平，可在常规工作站上完成。

### 5. 实验数量与充分性
*   **实验规模：** 针对 3 个数据集，每个实验均在 5 个独立随机种子下运行并取平均值（含标准差）。
*   **消融实验：** 充分对比了“仅噪声”、“仅嵌入”、“固定预算”与“全框架”的效果，验证了 RME 对隐私提升的边际贡献（提升 1.1% - 3.1%）。
*   **参数敏感性：** 详细分析了 $\epsilon_{min}, \epsilon_{max}$ 以及剪切范数 $C$ 对结果的影响。
*   **客观性：** 实验不仅展示了优点，也诚实地反映了在复杂数据集（如 CIFAR-10）上，高隐私需求会导致效用剧烈下降（准确率跌至 10% 左右）的局限性。

### 6. 主要结论与发现
*   **隐私增强：** TADP-RME 在相同隐私预算下，比纯噪声 DP 机制能更有效地抵御推理攻击，攻击成功率最高降低了 3.1%。
*   **自适应性：** 通过 $T$ 评分，系统能成功在“高精度-低隐私”和“低精度-高隐私”之间平滑切换。
*   **结构破坏：** RME 变换显著降低了 k-NN 重合度和全局距离相关性，证明了其在阻断几何推理路径方面的有效性。
*   **计算效率：** 随着隐私强度增加，由于数据结构被简化，系统的运行时间反而有所下降。

### 7. 优点
*   **理论与实证结合：** 既提供了形式化的 DP 证明，又通过多种攻击模型验证了经验隐私（Empirical Privacy）。
*   **创新的几何视角：** 不同于传统方法只关注数值扰动，RME 引入了非线性流形变换来对抗基于表示学习的现代攻击。
*   **实用性强：** 信任评分机制非常符合现实中多租户、多权限等级的系统架构需求。

### 8. 不足与局限
*   **高维复杂数据挑战：** 在 CIFAR-10 等特征相关性极强的复杂数据集上，隐私保护与效用的冲突极度严重，实用性受限。
*   **维度膨胀：** RME 将特征维度翻倍（$d \to 2d$），虽然增加了攻击者的搜索空间，但也增加了下游任务的存储和计算负担。
*   **模型深度限制：** 实验主要基于线性探针（逻辑回归），未深入探讨该框架在深度神经网络（如 Transformer）内部层级中的表现及兼容性。
*   **逆向映射风险：** 虽然 RME 增加了歧义性，但如果攻击者获知了变换参数 $\alpha$ 和配对逻辑，其组合复杂度的防御效能可能会打折扣。

（完）
