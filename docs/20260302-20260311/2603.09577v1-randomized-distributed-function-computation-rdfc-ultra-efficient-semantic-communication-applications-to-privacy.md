---
title: "Randomized Distributed Function Computation (RDFC): Ultra-Efficient Semantic Communication Applications to Privacy"
title_zh: 随机分布式函数计算 (RDFC)：超高效语义通信在隐私保护中的应用
authors: Onur Günlü
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09577v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 分布式函数计算中的本地差分隐私保证
tldr: 本文提出随机分布式函数计算（RDFC）框架，将其视为一种语义通信形式，旨在通过传输最少信息使接收端生成输入数据的随机化函数。该框架将安全与隐私约束融入模型，利用强协调度量确保局部差分隐私。研究证明了在无共享随机性时仍可实现隐私保证，并给出了通信成本下界。实验表明，RDFC在隐私保护场景下能显著降低通信速率，是能效极高的分布式计算方案。
selection_source: fresh_fetch
motivation: 针对隐私敏感的分布式计算场景，寻求一种能平衡通信效率与隐私保护的语义通信框架。
method: 建立RDFC框架，利用强协调度量和Wyner共同信息理论，分析在有无共享随机性下的通信速率界限。
result: 充足的共享随机性可将通信速率降低两个数量级，且即使无共享随机性，RDFC也远优于无损传输。
conclusion: RDFC为隐私感知型分布式计算系统提供了一种兼顾能效与隐私安全的高效语义通信策略。
---

## 摘要
我们建立了随机分布式函数计算 (RDFC) 框架，在该框架中，发送方仅传输足够的信息，以便接收方生成输入数据的随机化函数。我们将 RDFC 描述为一种语义通信形式，其本质上可以被视为一种广义的远程源编码问题；我们证明了安全和隐私约束能够自然地契合该模型，因为这些约束通常需要一个随机化步骤。利用强协调度量，我们确保了每个输入序列的（本地差分）隐私，并证明了即使在发射机和接收机之间没有共享公共随机性的情况下，也能满足此类保证。本研究提供了 Wyner 公共信息 (WCI) 的下界，即缺乏公共随机性时的通信成本，并提出了数值技术来评估具有无限共享随机性的连续字母表随机变量在 RDFC 速率区域的另一个角点。实验表明，与 WCI 点相比，充足的公共随机性可以将语义通信速率降低多达两个数量级，而没有任何共享随机性的 RDFC 仍然大幅优于无损传输。有限块长分析进一步证实，渐近和非渐近 RDFC 方法之间的隐私参数差距随输入长度呈指数级快速缩小。我们的结果将 RDFC 定位为一种适用于隐私感知分布式计算系统的节能语义通信策略。

## Abstract
We establish the randomized distributed function computation (RDFC) framework, in which a sender transmits just enough information for a receiver to generate a randomized function of the input data. Describing RDFC as a form of semantic communication, which can be essentially seen as a generalized remote-source-coding problem, we show that security and privacy constraints naturally fit this model, as they generally require a randomization step. Using strong coordination metrics, we ensure (local differential) privacy for every input sequence and prove that such guarantees can be met even when no common randomness is shared between the transmitter and receiver.   This work provides lower bounds on Wyner's common information (WCI), which is the communication cost when common randomness is absent, and proposes numerical techniques to evaluate the other corner point of the RDFC rate region for continuous-alphabet random variables with unlimited shared randomness. Experiments illustrate that a sufficient amount of common randomness can reduce the semantic communication rate by up to two orders of magnitude compared to the WCI point, while RDFC without any shared randomness still outperforms lossless transmission by a large margin. A finite blocklength analysis further confirms that the privacy parameter gap between the asymptotic and non-asymptotic RDFC methods closes exponentially fast with input length. Our results position RDFC as an energy-efficient semantic communication strategy for privacy-aware distributed computation systems.

---

## 论文详细总结（自动生成）

### 论文总结：随机分布式函数计算 (RDFC) —— 超高效语义通信在隐私保护中的应用

#### 1. 核心问题与整体含义（研究动机和背景）
本文提出了**随机分布式函数计算 (RDFC)** 框架，旨在解决分布式系统中如何在保证隐私的前提下实现极低功耗通信的问题。
*   **研究动机**：传统的物理层通信关注比特的准确传输，而语义通信关注“含义”的传递。在分布式计算中，接收方往往只需要得到输入数据的某个随机化函数结果（如为了隐私保护而添加噪声后的数据），而非原始数据本身。
*   **核心问题**：如何利用信息论中的“强协调（Strong Coordination）”理论，最小化发送方为了让接收方生成特定随机分布序列而必须传输的信息量。
*   **背景**：该研究将隐私保护机制（如本地差分隐私 LDP）视为一种随机化函数，将其纳入语义通信的远程源编码模型中。

#### 2. 论文提出的方法论
核心思想是将通信过程建模为**分布式信道合成**问题，通过传输索引而非原始数据，使接收端能够模拟出符合目标联合分布 $Q_{\tilde{X}Y}$ 的序列。
*   **关键技术细节**：
    *   **强协调度量**：使用全变分（Total Variation, TV）距离来衡量诱导分布与目标分布的接近程度，确保每一计算实例（而非平均意义上）的隐私性。
    *   **速率区域分析**：识别了协调-随机性速率区域的两个角点：
        1.  **无共享随机性**：通信速率受限于 **Wyner 公共信息 (WCI)**，即 $C(\tilde{X}; Y)$。
        2.  **充足共享随机性**：通信速率可降低至**互信息** $I(\tilde{X}; Y)$。
    *   **具体机制实现**：
        *   **连续变量**：针对截断高斯输入，采用高斯 LDP 机制，推导了 WCI 的下界并给出了互信息的数值计算方法。
        *   **离散变量**：针对对称随机响应机制，利用 BSC（二进制对称信道）混合模型和 Witsenhausen 技术推导速率界限。
    *   **有限块长分析**：通过误差指数分析，证明了在非渐近情况下，实际隐私参数 $\delta_n$ 向目标参数 $\delta$ 收敛的速度随块长 $n$ 呈指数级增长。

#### 3. 实验设计
论文主要通过数值仿真和理论界限对比来验证 RDFC 的效率。
*   **场景/数据集**：
    1.  **连续场景**：模拟截断高斯分布数据，应用 $(\epsilon, \delta)$-LDP 高斯机制。
    2.  **离散场景**：模拟基于对称随机响应的比特序列传输。
*   **Benchmark（基准）**：
    *   **无损传输**：传输原始序列的熵 $H(\tilde{X})$ 或传输随机化后序列的熵 $H(Y)$。
    *   **WCI 点**：作为无共享随机性时的 RDFC 性能基准。
*   **对比方法**：对比了在不同隐私强度（$\epsilon$ 参数）和不同共享随机性水平下的通信速率需求。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**使用了特定的 GPU 型号或大规模集群算力。
*   **性质**：由于本文属于信息论领域的理论研究，其实验部分主要涉及复杂的数学公式数值求解（如数值积分计算互信息）和随机搜索参数扫描，通常在标准的 CPU 工作站上即可完成，不涉及深度学习模型的大规模训练。

#### 5. 实验数量与充分性
*   **实验规模**：论文通过随机搜索扫描了大量参数组合（如表 1 和表 2 所示），涵盖了不同的隐私预算 $\epsilon$、噪声方差和输入分布参数。
*   **充分性与客观性**：
    *   实验涵盖了连续和离散两种典型数据类型，具有较好的代表性。
    *   引入了有限块长分析，弥补了渐近理论在实际应用中的局限性。
    *   对比了 RDFC 与传统无损压缩的巨大差距，客观展示了语义通信在特定任务下的优势。

#### 6. 论文的主要结论与发现
*   **极高的通信效率**：在充足共享随机性的情况下，RDFC 的语义通信速率比 WCI 点（无随机性）可降低多达 **2 个数量级**（例如从 0.0038 降至 $1.8 \times 10^{-5}$）。
*   **优于传统方法**：即使在完全没有共享随机性的极端情况下，RDFC 的通信开销仍远低于传统的无损传输（最高可节省 116 倍以上的带宽）。
*   **隐私保证的可靠性**：非渐近分析证实，通过增加块长，可以快速（指数级）弥补有限长度带来的隐私泄露风险。
*   **能效比**：通信速率的显著降低直接转化为分布式计算系统的能量大幅节省。

#### 7. 优点
*   **理论严密**：将语义通信、强协调理论与隐私保护机制有机结合，建立了坚实的数学框架。
*   **视角新颖**：将隐私保护天然的“随机化”需求转化为降低通信速率的“自由度”，视角独特。
*   **实用潜力**：提出的有限块长界限为实际工程中设计隐私感知系统提供了理论指导。

#### 8. 不足与局限
*   **实现复杂度**：强协调编码器（如似然编码器）在实际大规模数据上的计算复杂度较高，论文虽提到可用自动编码器替代，但未给出具体实现细节。
*   **共享随机性假设**：虽然证明了共享随机性的巨大价值，但在实际大规模分布式系统中，分发和存储大量公共随机密钥本身也存在成本和安全挑战。
*   **实验环境单一**：实验主要基于理想的数学分布（高斯、BSC），在真实世界复杂数据集（如图像、传感器原始数据）上的表现尚待验证。

（完）
