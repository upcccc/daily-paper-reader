---
title: "Concordia: Self-Improving Synthetic Tables for Federated LLMs"
title_zh: Concordia：面向联邦大语言模型的自改进合成表格
authors: "Jimin Huang, Duanyu Feng, Nuo Chen, Xiaoyu Wang, Zhiqiang Zhang, Xueqing Peng, Mingquan Lin, Prayag Tiwari, Guojun Xiong, Alejandro Lopez-Lira, Sophia Ananiadou"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09855v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 使用合成数据进行隐私保护的联邦学习
tldr: 本研究针对联邦学习中 LLM 处理非独立同分布表格数据的挑战，提出了 Concordia 框架。该框架采用三层优化策略：客户端利用 LoRA 在合成数据上训练并学习效用评分器，外部层则通过组相对策略优化（GRPO）改进合成数据生成器。Concordia 实现了合成数据生成与联邦验证效用的对齐，且无需共享原始数据或生成器参数。实验证明，该方法在金融和医疗等隐私敏感领域显著提升了模型性能、稳定性和对分布偏移的鲁棒性。
selection_source: fresh_fetch
motivation: 在联邦学习环境下，现有的静态合成数据生成方法难以适应下游 LLM 优化的动态需求，尤其是在处理异构表格数据时效用受限。
method: 提出一种三层优化框架，结合 LoRA 参数微调、私有验证反馈驱动的效用评分器以及基于 GRPO 的生成器自改进机制。
result: 在金融和医疗等隐私敏感的表格任务中，Concordia 在联邦性能、跨客户端稳定性和抗分布偏移能力上均优于现有基准。
conclusion: 通过将合成数据生成与联邦验证效用紧密对齐，Concordia 为严格隐私限制下的联邦 LLM 适应提供了一种高效且稳健的解决方案。
---

## 摘要
联邦学习 (FL) 能够在不共享原始数据的情况下训练大语言模型 (LLMs)，但在严格的数据隔离和非独立同分布 (non-IID) 的客户端分布下，适配 LLM 在实践中仍然具有挑战性。合成数据为本地训练提供了一种天然的隐私保护替代方案，但现有的联邦流水线通常将合成生成视为静态的或与下游优化松散耦合的，这导致在异构客户端下效用迅速下降。我们研究了在表格任务上对 LLM 进行联邦适配，其中原始记录和验证数据无法共享，且本地训练必须完全依赖合成表格。我们提出了 Concordia，这是一个三层优化框架，尽管存在这些约束，它仍能将合成数据生成与联邦验证效用对齐。在客户端层面，模型通过在合成表格上进行参数高效的 LoRA 训练来完成适配。客户端还从私有验证反馈中学习轻量级效用评分器，以便在本地训练期间对合成样本进行重新加权。在最外层，每个客户端使用组相对策略优化 (GRPO) 来改进其自身的合成表格生成器，并由跨客户端共享的异构评分器集成进行引导，而无需聚合生成器参数或暴露验证数据。在金融和医疗等隐私敏感的表格基准测试上的实验表明，与静态和解耦的合成数据基准相比，Concordia 持续提高了联邦性能、跨客户端稳定性和对分布偏移的鲁棒性。

## Abstract
Federated learning (FL) enables training large language models (LLMs) without sharing raw data, but adapting LLMs under strict data isolation and non-IID client distributions remains challenging in practice. Synthetic data offers a natural privacy-preserving surrogate for local training, yet existing federated pipelines typically treat synthetic generation as static or loosely coupled with downstream optimization, leading to rapidly diminishing utility under heterogeneous clients. We study federated adaptation of LLMs on tabular tasks where raw records and validation data cannot be shared, and local training must rely entirely on synthetic tables. We propose Concordia, a tri-level optimization framework that aligns synthetic data generation with federated validation utility despite these constraints. At the client level, models are adapted via parameter-efficient LoRA training on synthetic tables. Clients additionally learn lightweight utility scorers from private validation feedback to reweight synthetic samples during local training. At the outer level, each client refines its own synthetic table generator using group-relative policy optimization (GRPO), guided by an ensemble of heterogeneous scorers shared across clients, without aggregating generator parameters or exposing validation data. Experiments on privacy-sensitive tabular benchmarks from finance and healthcare demonstrate that Concordia consistently improves federated performance, cross-client stability, and robustness to distribution shift compared to static and decoupled synthetic-data baselines.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Concordia**，一个旨在解决严格隐私约束下联邦大语言模型（LLM）适配表格数据问题的框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在金融和医疗等高监管领域，机构间无法共享原始数据、验证集，甚至无法导出高容量的模型参数（如完整的 LoRA 权重或生成器参数）。在这种“高保证（High-Assurance）”环境下，如何利用联邦学习提升 LLM 对非独立同分布（non-IID）且极度类别不平衡（如保险欺诈率 < 1%）的表格数据的预测能力？
*   **研究动机**：现有的合成数据联邦方法通常将数据生成与下游任务优化脱节，导致合成数据无法有效捕捉对模型性能至关重要的少数类信号。论文旨在建立一种“验证对齐”的合成优化机制，使私有验证集的反馈能在线指导本地合成数据的改进。

### 2. 方法论：Concordia 三层优化框架
Concordia 通过一个三层循环实现合成数据的自改进，而无需交换敏感数据：
*   **内层（本地适配）**：客户端在本地生成器产生的合成表格上，通过参数高效的 **LoRA** 进行微调。
*   **中层（效用评分器学习）**：客户端利用其**私有验证集**的反馈（如 MCC 分数），训练一个轻量级的**效用评分器（Scorer）**。该评分器学习识别哪些合成样本能真正提升模型在真实验证集上的表现，并据此对合成小批量数据进行加权。
*   **外层（联邦生成器精炼）**：
    *   **联邦协作**：客户端仅向服务器上传轻量级的评分器参数。服务器汇总并分发这些评分器形成“评分器池”。
    *   **生成器更新**：客户端使用 **GRPO（组相对策略优化）** 算法，根据评分器池提供的集成奖励信号，在线精炼本地的合成数据生成器。
    *   **合规性保障**：整个过程不交换原始记录、验证集、生成器参数或 LoRA 增量，仅交换评分器参数。

### 3. 实验设计
*   **数据集与场景**：
    *   **金融领域**：German Credit（信用评分）、Lending Club（贷款状态）、Travel Insurance（旅游保险，极度不平衡，正类率约 1%）。
    *   **医疗领域**：Heart Disease 数据集（包含 Cleveland, Hungarian, Switzerland 三个站点参与训练，VA Long Beach 站点用于测试跨站点泛化性）。
*   **基准方法（Benchmark）**：
    *   **Single-node**：预训练模型（Qwen3-4B）及其指令微调版。
    *   **Local-only**：仅在本地进行合成数据生成和适配，不进行联邦评分器共享。
*   **对比维度**：主要衡量 **MCC（马修斯相关系数）**，因为它对类别不平衡更具鲁棒性。

### 4. 资源与算力
*   **模型底座**：使用 **Qwen3-4B** 作为下游预测模型和合成生成器的骨干网络。
*   **训练细节**：每个回合进行 1500 步本地 LoRA 更新，300 步 GRPO 生成器精炼步。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长，但提到了使用 bf16 精度进行传输和计算。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个大领域、7 个不同分布的站点/数据集。
*   **重复性**：所有实验均基于 3 个随机种子运行，并报告了均值和标准差。
*   **充分性**：实验设计包含了跨站点泛化测试（VA Long Beach）和极度不平衡测试（Travel），并进行了消融分析和生成器审计（可视化分析），能够较为客观地证明框架在有限协调回合（1-2 轮）下的有效性。

### 6. 主要结论与发现
*   **性能提升**：Concordia 在所有任务上均优于静态合成基准。在极度不平衡的 Travel 任务中，MCC 从第一轮的 7.07 显著提升至第二轮的 21.78。
*   **稳定性**：通过评分器池的联邦共享，减少了单客户端私有验证集带来的噪声，提升了跨回合的训练稳定性。
*   **机制发现**：生成器审计显示，Concordia 促使生成器不再只是简单模仿格式，而是学会了生成具有更强特征依赖性和临床解释性的“高价值”样本（如在医疗任务中强化了风险特征的关联）。

### 7. 优点
*   **隐私合规性强**：针对极高安全要求的场景设计，最小化了信息交换表面积（仅交换评分器）。
*   **解决长尾问题**：通过效用加权和 GRPO 精炼，有效解决了联邦学习中少数类信号被淹没的难题。
*   **通信效率**：在有限的 1-2 轮协调下即可获得显著收益，适合现实中审批流程复杂的跨机构协作。

### 8. 不足与局限
*   **潜在泄露风险**：虽然不共享数据，但评分器参数在理论上仍可能通过成员推理攻击泄露部分验证集的分布特征。
*   **计算开销**：客户端需要同时运行预测模型适配、评分器训练和生成器 GRPO 优化，对本地算力有一定要求。
*   **回合数限制**：实验仅展示了 2 轮协调的结果，虽然符合现实约束，但未探讨长期收敛性。
*   **任务局限**：目前主要集中在二分类表格任务，尚未验证在多分类或更复杂回归任务中的表现。

（完）
