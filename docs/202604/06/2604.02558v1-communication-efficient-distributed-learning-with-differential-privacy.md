---
title: Communication-Efficient Distributed Learning with Differential Privacy
title_zh: 具有差分隐私的通信高效分布式学习
authors: "Xiaoxing Ren, Yuwen Ma, Nicola Bastianello, Karl H. Johansson, Thomas Parisini, Andreas A. Malikopoulos"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02558v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 具有差分隐私和梯度扰动的分布式学习
tldr: 本研究针对无向网络中的非凸学习问题，提出了一种兼顾通信效率与差分隐私的分布式学习算法。通过局部训练减少通信频率，并利用梯度裁剪和加性噪声保护数据隐私。理论证明了算法能收敛至驻点邻域，并提供严格的隐私保证。实验表明，在相同隐私预算下，该算法在分类任务上的表现优于现有先进方法。
selection_source: fresh_fetch
motivation: 旨在解决分布式学习中通信开销巨大以及参与者敏感数据面临隐私泄露风险的挑战。
method: 结合局部训练机制以降低通信频率，并通过梯度裁剪和添加噪声实现差分隐私保护。
result: 理论证明了算法收敛至驻点邻域，且在相同隐私预算下的分类任务表现优于现有技术。
conclusion: 该研究成功构建了一个在保证严苛隐私需求的同时，兼具高通信效率和良好收敛性能的分布式学习框架。
---

## 摘要
我们研究了无向网络上的非凸学习问题。特别地，我们专注于设计一种既能实现通信高效，又能保证代理数据隐私的算法。第一个目标通过本地训练方法实现，该方法降低了通信频率。第二个目标通过在本地训练期间扰动梯度来实现，具体采用了梯度裁剪和加性噪声。我们证明了所得算法在有界距离内收敛至问题的驻点。此外，我们在差分隐私框架下提供了理论隐私保证，确保无法从网络中共享的训练模型中推断出代理的训练数据。我们展示了该算法在相同隐私预算下的分类任务中，相较于现有最先进方法具有更优越的性能。

## Abstract
We address nonconvex learning problems over undirected networks. In particular, we focus on the challenge of designing an algorithm that is both communication-efficient and that guarantees the privacy of the agents' data. The first goal is achieved through a local training approach, which reduces communication frequency. The second goal is achieved by perturbing gradients during local training, specifically through gradient clipping and additive noise. We prove that the resulting algorithm converges to a stationary point of the problem within a bounded distance. Additionally, we provide theoretical privacy guarantees within a differential privacy framework that ensure agents' training data cannot be inferred from the trained model shared over the network. We show the algorithm's superior performance on a classification task under the same privacy budget, compared with state-of-the-art methods.

---

## 论文详细总结（自动生成）

以下是对论文《Communication-Efficient Distributed Learning with Differential Privacy》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
本文针对**无向网络中的非凸分布式学习问题**，旨在同时解决两个关键挑战：
*   **通信开销**：在分布式环境中，频繁的模型参数交换会消耗大量带宽，限制了算法在大规模网络中的应用。
*   **隐私风险**：共享的模型参数可能泄露参与者的原始敏感数据。
**研究动机**在于现有的分布式学习算法往往只能在通信效率或隐私保护之间二选一，缺乏一个能同时兼顾高效通信、非凸收敛保证以及严格差分隐私（DP）保护的统一框架。

### 2. 论文提出的方法论
论文提出了 **LT-ADMM-DP**（具有差分隐私的局部训练交替方向乘子法）算法。其核心思想和技术细节如下：
*   **局部训练（Local Training）**：代理（Agents）在每次通信前执行 $\tau$ 次局部随机梯度下降（SGD）步骤，显著降低了全局通信频率。
*   **隐私保护机制**：在局部训练过程中，对随机梯度进行两步处理：
    1.  **梯度裁剪（Gradient Clipping）**：通过阈值 $\zeta$ 限制梯度的范数，从而控制单个数据点对模型的影响（即降低敏感度）。
    2.  **加性噪声（Additive Noise）**：向裁剪后的梯度添加高斯噪声，以满足差分隐私要求。
*   **算法流程**：
    *   **局部阶段**：代理利用裁剪且加噪的梯度更新局部变量 $\phi$。
    *   **通信阶段**：代理之间交换桥接变量（Bridge Variables），通过 ADMM 框架实现全局共识。
*   **理论保证**：利用 **Rényi 差分隐私（RDP）** 提供了比传统 DP 更紧致的隐私预算界限，并证明了算法能收敛至非凸问题驻点的有界邻域内。

### 3. 实验设计
*   **实验场景**：带有非凸正则化项的逻辑回归分类任务。
*   **数据集/设置**：
    *   网络拓扑：10个节点的环形网络。
    *   数据规模：每个代理拥有 1000 个样本，特征维度为 5。
*   **Benchmark（对比方法）**：
    *   **PORTER**：一种结合了梯度裁剪和通信压缩的去中心化非凸优化算法。
    *   **PriSMA**：一种针对分布式学习的差分隐私算法。
*   **对比维度**：在相同的隐私预算（$\epsilon$）下，对比各算法的收敛速度（误差随时间的变化）和分类准确率。

### 4. 资源与算力
*   **文中说明**：论文未明确提及具体的硬件设备（如 GPU 型号、数量）或具体的训练时长。
*   **算力评估方式**：作者通过定义逻辑时间成本（局部梯度评估耗时 $t_g$ 与通信耗时 $t_c$ 的加权和）来评估效率，而非直接测量物理时间。

### 5. 实验数量与充分性
*   **实验数量**：主要展示了一组核心的分类对比实验，包括误差收敛曲线和准确率对比。
*   **充分性与客观性**：
    *   **公平性**：实验在**相同的隐私预算**下进行对比，这是评估隐私算法性能的关键前提，体现了公平性。
    *   **局限性**：实验仅限于小规模网络（10节点）和低维特征（5维），且只采用了一种网络拓扑（环形），未测试更大规模或更复杂（如非 IID 数据分布）的场景。

### 6. 论文的主要结论与发现
*   **性能优越性**：在相同的隐私保护水平下，LT-ADMM-DP 的收敛速度明显快于 PORTER 和 PriSMA，且最终达到的分类准确率更高。
*   **通信效率**：通过局部训练机制，该算法在达到相同精度时所需的通信轮数更少。
*   **理论验证**：理论推导出的收敛界限揭示了隐私噪声、梯度方差与稳态误差之间的权衡关系。

### 7. 优点
*   **多目标平衡**：成功将局部训练（提升效率）、ADMM（保证共识）与梯度扰动（保护隐私）结合，解决了非凸环境下的复杂优化问题。
*   **隐私界限更紧**：采用 RDP 分析方法，相比传统 DP 提供了更优的隐私保证，减少了为了保护隐私而引入的过量噪声。
*   **理论完备**：不仅给出了算法，还提供了详尽的收敛性证明和隐私预算推导。

### 8. 不足与局限
*   **实验规模较小**：实验部分的数据维度和网络节点数较低，难以完全体现算法在处理现代深度学习大规模任务时的表现。
*   **超参数敏感性**：算法性能高度依赖于裁剪阈值 $\zeta$、噪声强度 $\sigma_e$ 和局部步数 $\tau$ 的选择，文中未提供自动调优这些参数的策略。
*   **数据异质性探讨不足**：虽然假设中提到了数据异质性，但在实验中未深入探讨在极端非 IID（独立同分布）数据下算法的鲁棒性。

（完）
