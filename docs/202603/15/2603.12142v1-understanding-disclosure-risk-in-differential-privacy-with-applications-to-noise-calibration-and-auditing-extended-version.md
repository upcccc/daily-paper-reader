---
title: Understanding Disclosure Risk in Differential Privacy with Applications to Noise Calibration and Auditing (Extended Version)
title_zh: 理解差分隐私中的披露风险及其在噪声校准与审计中的应用（扩展版）
authors: "Patricia Guerra-Balboa, Annika Sauer, Héber H. Arcolezi, Thorsten Strufe"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12142v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 差分隐私噪声校准与风险度量
tldr: 差分隐私（DP）在数据管理中广泛应用，但如何将噪声水平转化为实际的泄露风险仍具挑战。本文指出传统的重构鲁棒性指标在现实假设下可能失效，并提出了一种统一的风险度量指标——“重构优势”。该指标能一致地衡量成员推理、属性推理和数据重构风险。通过推导紧致边界和最优攻击策略，本文为DP噪声校准和系统审计提供了理论基础，显著提升了数据管理系统中的效用与隐私权衡。
selection_source: fresh_fetch
motivation: 现有的差分隐私风险评估方法在现实场景下可能产生误导性的风险估计，且缺乏统一衡量多种推理攻击的手段。
method: 提出“重构优势”作为统一风险指标，并推导出其与DP噪声之间的紧致理论边界及最优攻击策略。
result: 该指标成功统一了成员推理、属性推理和数据重构的风险评估，并显著提高了DP审计的准确性和覆盖范围。
conclusion: 重构优势为风险驱动的噪声校准和系统化DP审计提供了理论支撑，优化了数据管理系统中的效用与隐私平衡。
---

## 摘要
差分隐私 (DP) 已被广泛应用于数据管理系统，以在提供正式披露保证的前提下实现数据共享。系统面临的一个核心挑战是理解 DP 噪声如何转化为针对推理攻击的有效保护，因为这直接决定了可实现的效用。大多数现有分析仅关注成员推理（仅捕捉一种威胁），或依赖于重构鲁棒性 (ReRo)。然而，我们发现在现实假设下，ReRo 可能会产生误导性的风险估计并违反声称的界限，从而限制了其在原则性 DP 校准和审计中的实用性。本文引入了重构优势 (reconstruction advantage)，这是一种统一的风险指标，能够一致地捕捉成员推理、属性推理和数据重构中的风险。我们推导了将 DP 噪声与对抗优势联系起来的紧致界限，并刻画了针对任意 DP 机制和攻击者知识的最优对抗策略。这些结果实现了风险驱动的噪声校准，并为系统化的 DP 审计奠定了基础。我们表明，重构优势提高了 DP 审计的准确性和范围，并在支持 DP 的数据管理系统中实现了更有效的效用-隐私权衡。

## Abstract
Differential Privacy (DP) is widely adopted in data management systems to enable data sharing with formal disclosure guarantees. A central systems challenge is understanding how DP noise translates into effective protection against inference attacks, since this directly determines achievable utility. Most existing analyses focus only on membership inference -- capturing only a threat -- or rely on reconstruction robustness (ReRo). However, under realistic assumptions, we show that ReRo can yield misleading risk estimates and violate claimed bounds, limiting their usefulness for principled DP calibration and auditing.   This paper introduces reconstruction advantage, a unified risk metric that consistently captures risk across membership inference, attribute inference, and data reconstruction. We derive tight bounds that relate DP noise to adversarial advantage and characterize optimal adversarial strategies for arbitrary DP mechanisms and attacker knowledge. These results enable risk-driven noise calibration and provide a foundation for systematic DP auditing. We show that reconstruction advantage improves the accuracy and scope of DP auditing and enables more effective utility-privacy trade-offs in DP-enabled data management systems.

---

## 论文详细总结（自动生成）

这篇论文对差分隐私（DP）中的披露风险进行了深入探讨，并提出了一种新的度量框架。以下是详细的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何将抽象的差分隐私参数（如 $\epsilon$）转化为直观、可衡量的现实世界攻击风险？
*   **研究动机**：
    *   现有的风险度量指标（如重构鲁棒性 ReRo）存在局限：它们忽略了攻击者的**特定辅助知识**，且无法区分“由于数据参与导致的泄露”与“由于统计推断/归因导致的成功”（即隐私谬误）。
    *   ReRo 在某些现实假设下会给出误导性的高风险估计，导致在噪声校准时为了追求不必要的安全而牺牲过多的数据效用。
    *   缺乏一个能统一衡量成员推理（MIA）、属性推理（AIA）和数据重构（DRA）的通用指标。

### 2. 方法论：重构优势 (RAD)
*   **核心思想**：引入**重构优势 (Reconstruction Advantage, RAD)**。该指标定义为：攻击者在目标记录参与数据集时的成功概率，减去目标记录未参与（仅靠先验分布和辅助知识）时的成功概率。
*   **关键技术细节**：
    *   **统一性**：RAD 通过调整辅助知识 $a(z)$ 的定义，可以无缝切换衡量 MIA、AIA 或全数据重构风险。
    *   **理论边界**：
        *   **最坏情况边界**：证明了 RAD 受限于机制的总体变差（Total Variation），这与具体的辅助知识无关。
        *   **辅助知识相关边界**：推导出了一个普遍紧致（Universally Tight）的边界（定理 4.3），该边界考虑了特定的先验分布和辅助信息。
    *   **最优攻击策略**：论文构建并证明了在给定机制、重构目标和先验知识下的**最优对抗策略（Algorithm 1）**。对于 DP-SGD，进一步简化为基于梯度相似度的最优攻击（Algorithm 2）。
    *   **黑盒边界**：针对无法获知机制内部细节的场景，提供了基于 $( \epsilon, \delta )$-DP 参数的闭式上界。

### 3. 实验设计
*   **数据集**：
    *   **图像分类**：MNIST, Fashion-MNIST（用于评估 DP-SGD 的重构风险）。
    *   **表格/人口统计**：Census, Texas-100X, Adult（用于评估属性归因攻击和聚合查询）。
    *   **移动轨迹**：Porto, Geolife（用于评估本地差分隐私 LDP 的审计）。
*   **对比基准 (Benchmark)**：
    *   **指标对比**：将 RAD 与传统的 ReRo 及其理论边界进行对比。
    *   **审计工具对比**：将基于 RAD 的审计框架与最先进的本地差分隐私审计工具 **LDP Auditor** 进行对比。
*   **评估场景**：涵盖了私有学习（DP-SGD）、聚合查询（Laplace 机制）和本地差分隐私（GRR, OUE, SS 机制）。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 Python 和 TensorFlow 框架进行实验。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。实验主要集中在推理攻击的模拟和理论边界的数值验证上，而非大规模模型的预训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从连续图像数据到离散分类数据的多种模态。
    *   针对不同的隐私预算 $\epsilon$（从 0 到 10+）进行了详尽的扫描。
    *   在 LDP 审计实验中，每组实验重复了 5 次并报告了均值和标准差，样本量达到了 $10^6$ 次运行。
*   **充分性评价**：实验设计非常充分且具有客观性。作者不仅验证了理论边界的紧致性，还通过“归因攻击”实验展示了 RAD 如何避免 ReRo 的误报问题。对比实验覆盖了主流的 DP 机制，证明了该方法的通用性。

### 6. 主要结论与发现
*   **ReRo 的失效**：实验证明，当攻击者拥有辅助知识时，实际风险会超过 ReRo 声称的边界；且 ReRo 会将统计推断误认为隐私泄露。
*   **RAD 的优越性**：RAD 能够准确剔除背景知识的影响，仅衡量因参与数据带来的额外风险。
*   **效用提升**：通过 RAD 进行噪声校准，可以在保持相同实际风险水平的前提下，显著提升数据的效用（例如在 Laplace 机制中减少噪声）。
*   **审计精度**：基于 RAD 的审计框架在估计 LDP 机制的经验 $\epsilon$ 时，比现有的 LDP Auditor 更准确，且能覆盖更高的 $\epsilon$ 范围。

### 7. 优点
*   **理论严密**：提供了数学上的紧致边界证明，并给出了最优攻击策略，使风险评估有了坚实的理论支撑。
*   **高度通用**：一个指标解决了多种攻击类型的度量问题，且适用于中心化 DP 和本地 DP。
*   **实用性强**：为系统开发者提供了直接的噪声校准工具和黑盒审计方法。

### 8. 不足与局限
*   **计算复杂度**：对于连续数据分布，定理 4.3 中的边界涉及复杂的勒贝格积分，可能需要嵌套蒙特卡洛模拟，计算开销较大。
*   **机制依赖性**：最紧致的边界（定理 4.3）需要完全了解机制 $M$ 的输出分布，在完全未知的黑盒审计中，只能退而求其次使用较松的黑盒边界。
*   **高维挑战**：虽然解决了分类数据的维度问题，但在极高维度的复杂生成模型（如大语言模型）中的重构风险应用仍有待进一步验证。

（完）
