---
title: Differentially Private Clustered Federated Learning with Privacy-Preserving Initialization and Normality-Driven Aggregation
title_zh: 具有隐私保护初始化和正态驱动聚合的差分隐私聚类联邦学习
authors: "Jie Xu, Haaris Mehmood, Rogier Van Dalen, Karthikeyan Saravanan, Mete Ozay"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20596v1"
tags: ["query:dml-dp"]
score: 10.0
evidence: 差分隐私聚类联邦学习
tldr: 针对联邦学习中数据异构性与隐私保护难以兼得的问题，本文提出PINA框架。该框架通过两阶段设计，首先利用轻量级LoRA适配器和压缩草图实现隐私保护的聚类中心初始化，随后引入正态驱动的聚合机制提升收敛性与鲁棒性，在保证差分隐私的前提下显著提升了聚类联邦学习的性能，有效应对了DP噪声对聚类过程的干扰。
selection_source: fresh_fetch
motivation: 差分隐私注入的噪声使得聚类联邦学习难以在保护隐私的同时准确初始化聚类中心，且数据异构性限制了传统联邦学习的泛化能力。
method: 提出PINA框架，利用LoRA适配器和压缩草图进行隐私保护的聚类初始化，并引入正态驱动的聚合机制来增强收敛性和鲁棒性。
result: "实验表明，在隐私预算epsilon为2和8时，该方法比现有最先进的DP-FL算法平均准确率提高了2.9%。"
conclusion: PINA通过创新的初始化和聚合策略，在不可信服务器环境下成功平衡了聚类联邦学习的效用与差分隐私的安全性。
---

## 摘要
联邦学习 (FL) 能够在将原始数据保留在终端设备上的同时训练全局模型。尽管如此，FL 已被证明会泄露用户隐私信息，因此在实践中，它通常与差分隐私 (DP) 和安全向量求和等方法结合使用，以为参与者提供正式的隐私保证。在现实的跨设备部署中，数据具有高度异构性，因此原始联邦学习收敛缓慢且泛化能力差。聚类联邦学习 (CFL) 通过将用户划分为不同的簇来缓解这一问题，从而降低了簇内数据的异构性。然而，将 CFL 与 DP 结合仍然具有挑战性：注入的 DP 噪声使得单个客户端的更新噪声过大，且服务器无法利用噪声较小的聚合更新来初始化簇中心。为了应对这一挑战，我们提出了 PINA，这是一个两阶段框架，首先让每个客户端微调一个轻量级的低秩自适应 (LoRA) 适配器，并以隐私保护的方式共享更新的压缩草图 (sketch)。服务器利用这些草图来构建鲁棒的簇中心。在第二阶段，PINA 引入了一种正态驱动的聚合机制，提高了收敛性和鲁棒性。我们的方法在保留聚类联邦学习优势的同时，针对不可信服务器提供了正式的隐私保证。广泛的评估表明，在隐私预算 (epsilon ∈ {2, 8}) 下，我们提出的方法在准确率上平均优于最先进的 DP-FL 算法 2.9%。

## Abstract
Federated learning (FL) enables training of a global model while keeping raw data on end-devices. Despite this, FL has shown to leak private user information and thus in practice, it is often coupled with methods such as differential privacy (DP) and secure vector sum to provide formal privacy guarantees to its participants. In realistic cross-device deployments, the data are highly heterogeneous, so vanilla federated learning converges slowly and generalizes poorly. Clustered federated learning (CFL) mitigates this by segregating users into clusters, leading to lower intra-cluster data heterogeneity. Nevertheless, coupling CFL with DP remains challenging: the injected DP noise makes individual client updates excessively noisy, and the server is unable to initialize cluster centroids with the less noisy aggregated updates. To address this challenge, we propose PINA, a two-stage framework that first lets each client fine-tune a lightweight low-rank adaptation (LoRA) adapter and privately share a compressed sketch of the update. The server leverages these sketches to construct robust cluster centroids. In the second stage, PINA introduces a normality-driven aggregation mechanism that improves convergence and robustness. Our method retains the benefits of clustered FL while providing formal privacy guarantees against an untrusted server. Extensive evaluations show that our proposed method outperforms state-of-the-art DP-FL algorithms by an average of 2.9% in accuracy for privacy budgets (epsilon in {2, 8}).

---

## 论文详细总结（自动生成）

这是一份关于论文《Differentially Private Clustered Federated Learning with Privacy-Preserving Initialization and Normality-Driven Aggregation》（PINA）的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：联邦学习（FL）在实际应用中面临两大挑战：**数据异构性（Non-IID）**导致模型收敛慢、泛化差；**隐私泄露风险**要求引入差分隐私（DP）。
*   **核心问题**：聚类联邦学习（CFL）虽能缓解异构性，但与DP结合时存在冲突：
    1.  **初始化难题**：在不可信服务器环境下，服务器无法直接访问客户端数据或更新来初始化聚类中心。
    2.  **噪声干扰**：DP注入的噪声会破坏原始的聚类结构，导致客户端分配错误。
    3.  **聚合失真**：由于不同簇的客户端参与度不均衡，聚合后的梯度幅度会受到噪声不成比例的影响。

### 2. 方法论：PINA 框架
PINA 提出了一个两阶段的解决方案：

*   **第一阶段：隐私保护的聚类初始化**
    *   **轻量化微调**：客户端使用 LoRA（秩 $r=1$）仅微调预训练模型（如 ViT）极少量的参数（如最后一层注意力层的投影矩阵）。
    *   **压缩草图（Sketching）**：客户端仅挑选更新中绝对值最大的 4 个元素，对其余元素置零，并施加本地差分隐私（LDP）。
    *   **鲁棒聚类**：服务器收集这些极稀疏且带噪的“草图”，利用 K-means 算法构建初始的聚类中心。
*   **第二阶段：聚类模型训练与正态驱动聚合**
    *   **簇识别**：客户端根据本地训练损失将自己分配到最匹配的簇模型。
    *   **安全聚合与 CDP**：利用安全求和协议（Secure Sum）结合中央差分隐私（CDP），确保服务器仅能看到带噪的聚合更新。
    *   **正态驱动聚合（关键创新）**：服务器利用 **Shapiro-Wilk 检验**评估聚合更新的正态性。若更新表现出明显的非正态性（受噪声主导或参与人数极少），则通过缩放机制恢复梯度的 $L_2$ 范数，从而稳定早期训练并增强对不平衡参与的鲁棒性。

### 3. 实验设计
*   **数据集**：
    *   **Rotated CIFAR-10**（2个簇，通过旋转图像构造异构性）。
    *   **Rotated FMNIST**（4个簇，旋转角度分别为0, 90, 180, 270度）。
    *   **FEMNIST**（天然的 Non-IID 数据集，基于不同书写者分类）。
*   **Benchmark 与对比方法**：
    *   基础算法：FedAvg, FedProx, FedNova。
    *   聚类算法：IFCA（最先进的聚类 FL 框架）。
*   **隐私设置**：隐私预算 $\epsilon \in \{2, 8\}$，模拟 10k 规模的客户端群组。

### 4. 资源与算力
*   **模型架构**：使用了拥有 22M 参数的 **ViT-Small** 预训练模型（ImageNet-21k 预训练）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。实验中提到客户端本地训练 10 个 Epoch，Batch Size 为 50。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了测试，涵盖了人工构造的异构场景和真实的天然异构场景。
*   **消融实验**：对比了 PINA 与“随机初始化（Random Initialization）”以及“IFCA + LDP”的效果，验证了初始化机制和聚合缩放的有效性。
*   **客观性**：实验考虑了不同的隐私预算（强隐私 $\epsilon=2$ 和弱隐私 $\epsilon=8$），并提供了多次运行的均值和标准差，结果较为客观。

### 6. 主要结论与发现
*   **性能领先**：在 $\epsilon \in \{2, 8\}$ 的隐私约束下，PINA 的准确率比现有最先进的 DP-FL 算法平均高出 **2.9%**。
*   **初始化优势**：相比随机初始化，PINA 提出的隐私保护初始化能显著加快聚类结构的收敛速度。
*   **鲁棒性**：在天然异构的 FEMNIST 数据集上提升最明显（达 3.2%），证明了该方法处理复杂现实数据的能力。
*   **克服噪声**：传统的 IFCA 在强 DP 噪声下几乎失效（准确率大幅下降），而 PINA 依然能保持较高的聚类准确率。

### 7. 优点
*   **无需辅助数据**：不需要服务器端拥有任何公开的代理数据集来辅助初始化。
*   **隐私与效用平衡**：通过 LoRA 和稀疏草图技术，在极低的隐私泄露风险下实现了高质量的聚类。
*   **创新的聚合策略**：引入统计学中的正态性检验（Shapiro-Wilk）来动态调整梯度，这是一个非常新颖且有效的工程优化思路。

### 8. 不足与局限
*   **架构依赖**：该方法高度依赖于预训练模型和 LoRA 技术，对于从零开始训练的小型模型或非 Transformer 架构的适用性讨论较少。
*   **超参数敏感性**：聚类数量 $C$ 需要预先设定，且正态性检验的阈值（如 0.99）可能需要针对不同任务进行微调。
*   **通信开销**：虽然第一阶段使用了压缩草图，但第二阶段服务器需要下发所有簇的模型供客户端选择，这在簇数量较多时会增加下行带宽压力。
*   **未考虑动态性**：实验未考虑客户端掉线（Dropout）或数据分布随时间漂移的情况。

（完）
