---
title: "MuCALD-SplitFed: Causal-Latent Diffusion for Privacy-Preserving Multi-Task Split-Federated Medical Image Segmentation"
title_zh: MuCALD-SplitFed：用于隐私保护多任务拆分联邦医学图像分割的因果潜扩散模型
authors: "Chamani Shiranthika, Hadi Hadizadeh, Parvaneh Saeedi"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.04108v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 隐私保护的多任务拆分联邦学习
tldr: 本研究针对医疗机构中多任务协作的需求，提出了 MuCALD-SplitFed 框架。该框架将拆分联邦学习与因果表示学习及潜在扩散模型相结合，解决了传统多任务联邦学习在处理异构任务时的不稳定性。MuCALD-SplitFed 不仅在医学图像分割任务中实现了更高的准确率和更好的收敛性，还通过减少拆分点的信息泄露，显著增强了抵御重构和成员推理攻击的能力，性能优于现有的个性化和多任务联邦学习方法。
selection_source: fresh_fetch
motivation: 传统的联邦学习框架难以适应医疗机构中复杂的多任务场景，且面临训练不稳定和隐私安全威胁。
method: 提出一种结合因果表示学习与潜在扩散模型的拆分联邦学习框架，用于处理多任务医学图像分割。
result: 该方法在提升分割性能的同时解决了基准模型的收敛难题，并有效降低了隐私泄露风险。
conclusion: MuCALD-SplitFed 证明了因果学习与扩散模型在增强多任务联邦学习鲁棒性与隐私性方面的有效性。
---

## 摘要
联邦学习通过在不共享原始数据的情况下聚合各客户端的模型更新来实现去中心化训练，而拆分联邦学习则进一步将模型在客户端和服务器之间进行划分，以减少客户端的计算和通信开销。然而，去中心化的医疗机构很少只执行单一的共享任务，这使得标准的联邦学习和拆分联邦学习协作难以与真实的临床工作流对齐。多任务联邦学习通过允许客户端处理不同的任务扩展了这些框架，但往往会引入不稳定性并带来隐私漏洞。本研究提出了 MuCALD-SplitFed，这是一个集成了因果表示学习和潜扩散的多任务拆分联邦学习框架。实验表明，MuCALD-SplitFed 持续提升了分割性能，而基准拆分联邦学习则无法收敛。该方法进一步减少了拆分点处的信息泄露，缓解了基于重构的攻击和成员推理攻击。此外，MuCALD-SplitFed 的表现优于最先进的个性化联邦学习和多任务联邦学习方法。代码仓库位于：https://github.com/ChamaniS/MuCALD_SplitFed。

## Abstract
Federated Learning enables decentralized training by aggregating model updates across clients without sharing raw data, while Split Federated Learning further partitions the model between clients and a server to reduce computation and communication at the client side. However, decentralized medical institutions rarely operate on a single shared task, making standard Federated and SplitFed collaborations poorly aligned with real clinical workflows. Multi-task FL extends these frameworks by allowing clients to handle different tasks, but often introduces instability and privacy vulnerabilities. This study proposes \textbf{MuCALD-SplitFed}, a multi-task SplitFed framework that integrates causal representation learning and latent diffusion. Experiments show MuCALD-SplitFed consistently improves segmentation, while baseline SplitFed fails to converge. The proposed approach further reduces information leakage at split points, mitigating reconstruction-based and membership inference attacks. Additionally, MuCALD SplitFed outperforms state-of-the-art personalized FL and multi-task FL approaches. The code repository is: https://github.com/ChamaniS/MuCALD_SplitFed.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MuCALD-SplitFed**，这是一个针对多任务医学图像分割设计的、结合了因果表示学习与潜扩散模型的拆分联邦学习（Split-Federated Learning）框架。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的联邦学习（FL）和拆分联邦学习（SplitFed）通常假设所有参与方（客户端）执行相同的任务（即 IID 或非 IID 数据分布下的单一目标）。但在实际临床场景中，不同医疗机构往往处理不同的任务（如不同部位、不同模态的分割），这导致了**多任务冲突**、**训练不稳定**以及拆分点处的**隐私泄露**（如重构攻击和成员推理攻击）。
*   **研究动机**：现有的多任务联邦学习在处理异构目标时容易出现梯度冲突，导致模型难以收敛。此外，拆分学习中传输的中间层激活值（Activations）包含大量原始图像信息，存在严重的隐私风险。

### 2. 方法论
MuCALD-SplitFed 框架由三个核心阶段组成：
*   **代理标签构建与因果图发现**：
    *   从五个数据集中提取领域特定的形态学和强度特征（如胚胎透明带厚度、病灶大小等）作为代理标签。
    *   利用 **Notears-MLP** 算法学习这些特征间的因果关系，构建因果图，为弱监督因果学习提供先验。
*   **因果表示与扩散模块 (CRDM)**：
    *   **外生编码器与神经结构因果模型 (Neural-SCM)**：将特征映射为低维外生变量，并学习因果分解，生成具有因果结构的潜变量（$z_{causal}$）。
    *   **潜扩散模型 (LDDM)**：对前端模型输出的特征图进行前向加噪（混淆信息），然后利用以因果潜变量为条件的 LDDM 进行去噪，生成发送至服务器的特征。这一过程能有效解耦任务相关特征与领域特定噪声。
*   **领域对抗因果对齐 (DACA)**：
    *   在服务器端引入梯度反转层（GRL）和领域判别器。
    *   通过对抗训练迫使模型生成领域无关（Domain-agnostic）的表示，抑制任务特定的偏差，增强跨任务泛化能力。
*   **优化目标**：联合最小化分割损失、代理对齐损失、扩散正则化损失、KL 散度以及对抗领域损失。

### 3. 实验设计
*   **数据集**：使用了 5 个异构医学影像数据集，模拟 5 个客户端：
    1.  **Blastocyst**（人胚胎图像）
    2.  **HAM10K**（皮肤病变）
    3.  **FHPsAOPMSB**（胎儿头部超声）
    4.  **MosMed**（肺部 CT）
    5.  **Kvasir-SEG**（内窥镜息肉）
*   **Benchmark 与对比方法**：
    *   **基准模型**：Baseline SplitFed（结合 UNet, UNet3+, SwinUNet）。
    *   **个性化联邦学习 (PFL)**：FedPer, FedRep, FedBN, FedProx, SCAFFOLD。
    *   **多任务联邦学习 (MTFL)**：MOCHA, FedEM。
*   **评估指标**：分割性能（Dice, IoU, HD95 等）和隐私保护性能（MSE, PSNR, SSIM 等重构质量指标）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **训练设置**：提到了使用了 24 轮通信，每轮客户端本地训练 5 个 Epoch，并采用了余弦扩散调度（Cosine diffusion schedule）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个完全不同模态和任务的数据集上进行了验证，涵盖了从超声到 CT 的多种医学影像。
*   **消融实验**：设计了 5 组消融实验，分别验证了 CRDM 模块、DACA 模块、因果建模、扩散机制以及前向加噪对系统性能和隐私的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了分割精度，还通过重构指标量化了隐私保护能力。对比对象涵盖了近年来主流的个性化和多任务联邦学习算法，具有较强的说服力。

### 6. 主要结论与发现
*   **收敛性提升**：在多任务场景下，基准 SplitFed 往往无法收敛（IoU 极低），而 MuCALD-SplitFed 表现出极强的稳定性。
*   **分割精度**：MuCALD-SplitFed 在所有数据集上均显著优于现有的 SoTA 方法（如平均 IoU 提升显著）。
*   **隐私增强**：重构指标（PSNR, SSIM）大幅下降，表明扩散混淆机制使得攻击者几乎无法从拆分点传输的数据中恢复原始图像。
*   **因果的作用**：因果建模有助于区分任务相关的结构信息和虚假相关性，从而在减少信息泄露的同时保持分割精度。

### 7. 优点
*   **创新性结合**：首次将因果表示学习、潜扩散模型与拆分联邦学习结合，解决了多任务环境下的不稳定性。
*   **隐私与性能平衡**：通过扩散模型进行信息混淆，在不牺牲（甚至提升）分割精度的前提下，提供了极强的隐私保护。
*   **鲁棒性**：能够处理跨模态、跨任务的极端非 IID 场景。

### 8. 不足与局限
*   **架构复杂性**：引入了 Neural-SCM、LDDM 和判别器，增加了模型的参数量和计算开销，可能对客户端算力有一定要求。
*   **代理标签依赖**：因果图的发现依赖于手动提取的形态学特征（代理标签），这在某些缺乏先验知识的新医学任务中可能难以实施。
*   **扩展性验证**：实验仅在 5 个客户端上进行，尚未验证在更大规模（如数十或数百个客户端）网络中的通信开销和扩展性。

（完）
