---
title: "HEART-PFL: Stable Personalized Federated Learning under Heterogeneity with Hierarchical Directional Alignment and Adversarial Knowledge Transfer"
title_zh: HEART-PFL：异构环境下基于分层定向对齐与对抗知识迁移的稳定个性化联邦学习
authors: "Minjun Kim, Minje Kim"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24209v1"
tags: ["query:dml-dp"]
score: 8.0
evidence: 异构环境下的个性化联邦学习
tldr: 针对个性化联邦学习（PFL）中数据异构导致的对齐浅层化和蒸馏不稳定性问题，本文提出HEART-PFL框架。该框架通过分层定向对齐（HDA）在不同深度采用不同匹配策略以保留客户端特性，并利用对抗知识转移（AKT）在代理数据上进行对称KL蒸馏以稳定全局更新。实验表明，该方法仅需少量参数即可在多个数据集上达到SOTA性能，显著提升了模型的个性化准确率与鲁棒性。
selection_source: fresh_fetch
motivation: 现有的个性化联邦学习方法在处理异构分布时，存在原型对齐深度不足以及服务器端知识蒸馏过程脆弱且不稳定的缺陷。
method: 提出结合分层定向对齐（HDA）与对抗知识转移（AKT）的双侧框架，利用轻量化适配器实现高效的特征匹配与稳健的全局更新。
result: 在CIFAR-100、Flowers-102和Caltech-101等数据集上取得了领先的个性化准确率，并展现出对域外代理数据的强大鲁棒性。
conclusion: HEART-PFL通过协同增强个性化对齐与全局稳定性，为解决异构环境下的联邦学习挑战提供了一个高效且可扩展的方案。
---

## 摘要
个性化联邦学习 (PFL) 旨在异构分布下提供有效的客户端特定模型，然而现有方法受限于浅层原型对齐和脆弱的服务端蒸馏。我们提出了 HEART-PFL，这是一个双侧框架，它：(i) 执行深度感知的分层定向对齐 (HDA)，在浅层阶段使用余弦相似度，在深层阶段使用 MSE 匹配，以保留客户端特异性；(ii) 通过对抗知识迁移 (AKT) 稳定全局

## Abstract
Personalized Federated Learning (PFL) aims to deliver effective client-specific models under heterogeneous distributions, yet existing methods suffer from shallow prototype alignment and brittle server-side distillation. We propose HEART-PFL, a dual-sided framework that (i) performs depth-aware Hierarchical Directional Alignment (HDA) using cosine similarity in the early stage and MSE matching in the deep stage to preserve client specificity, and (ii) stabilizes global updates through Adversarial Knowledge Transfer (AKT) with symmetric KL distillation on clean and adversarial proxy data. Using lightweight adapters with only 1.46M trainable parameters, HEART-PFL achieves state-of-the-art personalized accuracy on CIFAR-100, Flowers-102, and Caltech-101 (63.42%, 84.23%, and 95.67%, respectively) under Dirichlet non-IID partitions, and remains robust to out-of-domain proxy data. Ablation studies further confirm that HDA and AKT provide complementary gains in alignment, robustness, and optimization stability, offering insights into how the two components mutually reinforce effective personalization. Overall, these results demonstrate that HEART-PFL simultaneously enhances personalization and global stability, highlighting its potential as a strong and scalable solution for PFL(code available at https://github.com/danny0628/HEART-PFL).

---

## 论文详细总结（自动生成）

这是一份关于论文《HEART-PFL: Stable Personalized Federated Learning under Heterogeneity with Hierarchical Directional Alignment and Adversarial Knowledge Transfer》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在个性化联邦学习（PFL）中，如何解决由于客户端数据异构（Non-IID）导致的**原型对齐浅层化**（仅在单层对齐）和**服务端知识蒸馏脆弱性**（容易受教师偏差影响且更新不稳定）的问题。
*   **研究动机**：
    *   现有的基于原型的方法通常只在单一特征层进行对齐，且多为服务端到客户端的单向流动，忽略了深度网络的分层语义。
    *   服务端集成蒸馏在处理异构数据时，往往会将全局模型推向客户端教师的平均值，导致全局更新不稳定。
    *   需要在保证个性化效果的同时，降低通信和计算开销。

### 2. 方法论
HEART-PFL 提出了一个双侧增强框架，核心包含两个关键组件：
*   **分层定向对齐 (HDA, Hierarchical Directional Alignment)**：
    *   **多层原型提取**：在客户端提取不同网络深度的类原型。
    *   **阶段性对齐策略**：在网络**早期阶段**（浅层）使用**余弦相似度**进行定向对齐，引导通用模式的几何一致性；在**深度阶段**（深层）使用**均方误差 (MSE)** 进行语义匹配，保留类特定的具体含义。
*   **对抗知识迁移 (AKT, Adversarial Knowledge Transfer)**：
    *   **对称 KL 蒸馏**：在服务端利用代理数据集，通过双向对称的 KL 散度减少全局适配器与客户端集成之间的表征差异。
    *   **对抗样本增强**：生成对抗性代理数据参与蒸馏，增加特征多样性，增强全局模型对异构输入的鲁棒性。
*   **轻量化实现**：采用适配器（Adapter）架构，仅训练 1.46M 个参数，冻结预训练的主干网络（如 ResNet-18），以提高效率。

### 3. 实验设计
*   **数据集**：CIFAR-100、Flowers-102、Caltech-101。
*   **异构场景**：使用狄利克雷分布（Dirichlet distribution）模拟标签倾斜，设置不同的异构强度参数 $\alpha \in \{0.1, 0.3, 0.5\}$。
*   **对比方法 (Benchmarks)**：
    *   **通用 FL 变体**：FedAvg-per, FedProx-per, FedProto-per。
    *   **经典 PFL 方法**：FedPer, LG-FedAvg, Ditto, FedBABU, FedALA。
    *   **参数高效 PFL**：PerAda（主要竞争对手）。
*   **设置**：20 个客户端，每轮随机选择 8 个，进行 200 轮通信。

### 4. 资源与算力
*   **模型架构**：使用在 ImageNet-1K 上预训练的 ResNet-18 作为主干。
*   **参数量**：可训练参数仅为 1.46M（远低于全模型微调的 11.18M）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了优化器设置（客户端 SGD，服务端 Adam）和批处理大小（客户端 16，服务端 2048）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流数据集、3 种异构梯度、10 种对比算法，实验设计较为全面。
*   **消融实验**：
    *   验证了 HDA 和 AKT 各自的贡献。
    *   分析了 HDA 在不同层级应用不同损失函数的有效性。
    *   探讨了 AKT 中对抗样本和对称 KL 的作用。
*   **鲁棒性测试**：专门测试了当代理数据与客户端数据属于不同领域（Out-of-Domain）时的性能，证明了方法的泛化能力。
*   **可视化分析**：通过 3D 损失平面（Loss Landscape）展示了 AKT 如何使优化空间变得更平滑，增强了结论的说服力。

### 6. 主要结论与发现
*   **性能领先**：HEART-PFL 在所有数据集和异构设置下均取得了 SOTA 性能（如 CIFAR-100 $\alpha=0.1$ 时达到 63.42% 准确率）。
*   **高效性**：在取得最高准确率的同时，其可训练参数量是对比方法中最低的之一。
*   **稳定性**：AKT 显著减少了轮次间的波动，使全局更新更加稳健。
*   **域外鲁棒性**：即使使用完全不同领域的代理数据（如用 Flowers 代理数据训练 CIFAR 任务），性能下降也极小，甚至在某些情况下有所提升。

### 7. 优点
*   **分层对齐思想**：意识到不同深度的特征具有不同属性（通用 vs 语义），并针对性地采用余弦相似度和 MSE，设计精巧。
*   **双向稳定性**：通过对称 KL 和对抗训练解决了服务端蒸馏容易偏离的问题。
*   **极高的参数效率**：非常适合资源受限的联邦学习场景。
*   **开源支持**：提供了代码链接，增加了研究的可信度和可复现性。

### 8. 不足与局限
*   **代理数据依赖**：虽然证明了对域外数据鲁棒，但 AKT 组件仍然依赖于服务端能够获取一定的代理数据集，这在某些极端隐私敏感的场景下可能受限。
*   **任务类型单一**：实验主要集中在图像分类任务上，未在目标检测、自然语言处理等其他领域验证其通用性。
*   **硬件开销未披露**：虽然参数量少，但服务端生成对抗样本和进行大规模批次蒸馏的实际计算耗时未详细列出。

（完）
