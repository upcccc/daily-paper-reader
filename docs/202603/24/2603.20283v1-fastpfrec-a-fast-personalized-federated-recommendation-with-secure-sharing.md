---
title: "FastPFRec: A Fast Personalized Federated Recommendation with Secure Sharing"
title_zh: FastPFRec：一种具有安全共享的快速个性化联邦推荐系统
authors: "Zhenxing Yan, Jidong Yuan, Yongqi Sun, Haiyang Liu, Zhihui Gao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.20283v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 具有隐私感知参数共享的个性化联邦推荐
tldr: 针对基于图神经网络（GNN）的联邦推荐系统在训练效率和隐私保护方面的不足，本文提出FastPFRec框架。该框架通过高效的本地更新策略加速模型收敛，并引入隐私感知参数共享机制以降低泄露风险。在四个真实数据集上的实验表明，FastPFRec在显著减少训练轮数和时间的同时，提升了推荐准确率，为可扩展的联邦推荐提供了高效且安全的解决方案。
selection_source: fresh_fetch
motivation: 现有的基于GNN的联邦推荐系统面临图数据收敛缓慢以及协作过程中隐私泄露的风险。
method: 提出FastPFRec框架，结合高效的本地更新策略加速收敛，并利用隐私感知参数共享机制增强安全性。
result: "在四个真实数据集上，该方法减少了32.0%的训练轮次和34.1%的时间，同时准确率提升了8.1%。"
conclusion: FastPFRec为大规模联邦推荐系统提供了一种兼顾训练效率、预测精度和隐私安全性的有效方案。
---

## 摘要
基于图神经网络（GNN）的联邦推荐系统在保护数据隐私的同时，能够有效捕捉用户与物品之间的关系。然而，现有方法在图数据上往往面临收敛速度慢以及协作过程中隐私泄露风险的问题。为了应对这些挑战，我们提出了 FastPFRec（具有安全共享的快速个性化联邦推荐），这是一个旨在提高训练效率和数据安全性的新型框架。FastPFRec 通过高效的本地更新策略加速模型收敛，并引入了一种隐私感知的参数共享机制以降低泄露风险。在四个真实世界数据集（Yelp、Kindle、Gowalla-100k 和 Gowalla-1m）上的实验表明，与现有基准方法相比，FastPFRec 的训练轮数减少了 32.0%，训练时间缩短了 34.1%，准确率提高了 8.1%。这些结果证明了 FastPFRec 为可扩展的联邦推荐提供了一种高效且隐私保护的解决方案。

## Abstract
Graph neural network (GNN)-based federated recommendation systems effectively capture user-item relationships while preserving data privacy. However, existing methods often face slow convergence on graph data and privacy leakage risks during collaboration. To address these challenges, we propose FastPFRec (Fast Personalized Federated Recommendation with Secure Sharing), a novel framework that enhances both training efficiency and data security. FastPFRec accelerates model convergence through an efficient local update strategy and introduces a privacy-aware parameter sharing mechanism to mitigate leakage risks. Experiments on four real-world datasets (Yelp, Kindle, Gowalla-100k, and Gowalla-1m) show that FastPFRec achieves 32.0% fewer training rounds, 34.1% shorter training time, and 8.1% higher accuracy compared with existing baselines. These results demonstrate that FastPFRec provides an efficient and privacy-preserving solution for scalable federated recommendation.

---

## 论文详细总结（自动生成）

这是一份关于论文《FastPFRec: A Fast Personalized Federated Recommendation with Secure Sharing》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：基于图神经网络（GNN）的推荐系统在捕捉复杂用户-物品关系方面表现优异，但通常依赖中心化数据。联邦学习（FL）的引入解决了隐私合规（如 GDPR）问题，但将 GNN 与 FL 结合面临两大挑战：
    1.  **训练效率低下**：在异构（Non-IID）数据下，GNN 的邻域聚合导致收敛缓慢且通信开销巨大。
    2.  **隐私泄露风险**：标准的客户端-服务器架构中，服务器可能通过梯度逆向追踪用户隐私，且易受恶意客户端的噪声注入攻击。
*   **核心问题**：如何在保证用户隐私和系统安全的前提下，显著提升联邦 GNN 推荐系统的收敛速度和预测精度？

### 2. 方法论：核心思想与技术细节
论文提出了 **FastPFRec** 框架，其核心由三个层次和两项关键技术组成：
*   **三层联邦架构**：引入“受信节点（Trusted Nodes）”作为中间层。客户端将扰动后的参数发给受信节点，由其进行初步聚合和异常检测，再发往中央服务器。这实现了客户端与服务器的解耦，防止了来源追踪。
*   **FastGNN 更新策略**：
    *   **非对称更新**：观察到用户偏好动态变化快而物品特征相对稳定，因此设计用户嵌入每轮更新，而物品嵌入每 $H \times 10$ 轮才更新一次。
    *   **计算优化**：这种稀疏更新机制将物品相关的计算复杂度降低了约一个数量级，同时有效缓解了 GNN 的过平滑（Over-smoothing）问题。
*   **多重隐私保护机制**：
    *   **图扰动**：在本地构建图时，随机添加约 10% 的虚假交互边，模糊真实行为。
    *   **本地差分隐私 (LDP)**：在上传前对模型参数添加拉普拉斯噪声（Laplace Noise）。
    *   **异常检测**：受信节点利用绝对中位差（MAD）检测并剔除恶意客户端的异常更新。

### 3. 实验设计
*   **数据集**：使用了四个真实世界数据集，涵盖不同规模和稀疏度：
    *   **Yelp**（商业评论）、**Kindle**（图书评分）。
    *   **Gowalla-100k** 与 **Gowalla-1m**（地理位置签到，后者极度稀疏，密度仅 0.04%）。
*   **Benchmark（对比方法）**：
    *   **中心化模型**：MF, LightGCN, SGL, XSimGCL（作为性能上限参考）。
    *   **联邦推荐模型**：FedMF, FedNCF, FedGNN, PerFedRec, 以及当时的 SOTA 模型 PerFedRec++。
*   **评价指标**：Hit Rate (HR@10) 和 NDCG@10。

### 4. 资源与算力
*   **硬件环境**：单台工作站，配备 **1 块 NVIDIA Tesla V100 GPU** (32GB RAM)。
*   **软件环境**：Ubuntu 20.04, Python 3.8, PyTorch 2.2.2, CUDA 11.8。
*   **训练时长**：文中提到在 Gowalla-1m 数据集上，对比方法需超过 5 小时，而 FastPFRec 在 100 轮内即可收敛，显著缩短了总时长（具体缩短比例为 34.1%）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 4 个数据集上对比了 9 种基准方法。
    *   **消融实验**：分别验证了 FastGNN 结构、受信节点、异常检测、LDP 噪声以及更新频率对结果的影响。
    *   **鲁棒性实验**：模拟了 30% 恶意客户端攻击场景，以及受信节点故障/被劫持的极端情况。
    *   **超参数分析**：探讨了嵌入维度（Embedding Size）和受信节点数量的影响。
*   **充分性评价**：实验设计非常详尽且客观。通过 Wilcoxon 符号秩检验和 CD 图表证明了统计显著性；同时通过“隐私-效用权衡”分析，量化了引入隐私保护带来的精度损失（约 4.29%），体现了研究的透明度。

### 6. 主要结论与发现
*   **性能提升**：FastPFRec 在准确率上比现有联邦基准平均提高 **8.1%**。
*   **效率飞跃**：相比 PerFedRec++，训练轮数减少了 **32.0%**，训练时间缩短了 **34.1%**。
*   **安全性验证**：三层架构能减少 94.8% 的服务器受损风险，且在 30% 的中间节点失效时仍能保持性能稳定。
*   **收敛特性**：FastGNN 的稀疏更新不仅加速了计算，还通过减少噪声传播使收敛曲线更加平滑。

### 7. 优点（亮点）
*   **创新的更新机制**：打破了 GNN 每一轮必须全量更新的思维定式，利用用户/物品更新频率的差异化实现了效率与精度的双赢。
*   **架构安全性**：三层架构（客户端-受信节点-服务器）在联邦推荐领域具有很强的实用性，有效解决了来源追踪和单点攻击问题。
*   **理论与实践结合**：不仅提供了算法，还给出了非凸设置下的收敛性理论证明。

### 8. 不足与局限
*   **受信节点假设**：系统安全性高度依赖于受信节点“诚实但好奇”的假设。在完全去中心化的临时网络（Ad-hoc）中，寻找可靠的中间基础设施可能存在困难。
*   **高级攻击防御**：虽然能防御噪声注入，但对于更复杂的成员推理攻击（Membership Inference）或模型逆向攻击的防御效果尚未在文中深入探讨。
*   **应用限制**：该方法主要针对二部图推荐场景，对于包含复杂社交关系或多类型实体的异构图推荐，其更新策略可能需要重新调整。

（完）
