---
title: "Beyond Rigid Alignment: Graph Federated Learning via Dual Manifold Calibration"
title_zh: 超越刚性对齐：基于双流形校准的图联邦学习
authors: "Wentao Yu, Bo Han, Jie Yang, Chen Gong"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06260v1"
tags: ["query:dml-dp"]
score: 9.0
evidence: 具有隐私保护的图联邦学习
tldr: 针对图联邦学习中子图语义和结构异构性导致的性能受限问题，本文提出FedGMC框架。该方法摒弃了传统的刚性对齐方式，引入双流形校准机制，通过在服务器端构建等距语义锚点和全局结构模板，引导本地客户端进行流形校准。这种方法在保留全局共性的同时，最大化了本地个性化表示空间，有效解决了传统方法中全局线性假设对局部特征表达的限制。
selection_source: fresh_fetch
motivation: 现有的图联邦学习方法依赖刚性对齐和全局线性假设，严重压缩了客户端的个性化表示空间，难以处理复杂的局部图分布异构性。
method: 提出FedGMC框架，通过等距语义锚点和全局结构模板分别构建语义与结构流形，实现对本地流形的动态校准与聚合。
result: 在11个同质和异质图数据集上的实验证明，FedGMC在平衡全局共性与本地个性化方面显著优于现有的最先进基准方法。
conclusion: 该研究通过流形视角统一处理语义和结构异构性，为图联邦学习提供了一种比传统刚性对齐更具灵活性和表达能力的校准范式。
---

## 摘要
图联邦学习（GFL）能够在保护隐私的同时，实现跨分布式子图的协作表示学习。然而，异质性仍然是一个关键挑战，因为不同客户端的子图通常在语义和结构上都存在显著差异。现有方法通过强制客户端与服务器之间的模型参数或原型进行刚性对齐来应对异质性。然而，这些对齐方式隐含地依赖于一种限制性的全局线性假设，即使用单一且全局一致的表示空间来总结局部数据分布。这严重压缩了客户端的个性化表示空间，且无法保留多样化的局部图分布。为了克服这些局限性，我们提出了联邦图流形校准（FedGMC），这是一种从统一流形视角解决语义异质性和结构异质性的新范式。FedGMC 不再强制执行刚性对齐，而是引入了一种双流形校准机制，在保留全局共性的同时，最大化局部客户端的个性化表示空间。具体而言，针对语义异质性，服务器通过等距语义锚点构建几何最优的语义流形，从而引导局部语义流形的校准。针对结构异质性，服务器通过构建全局结构模板来构建全局结构流形，从而引导局部结构流形的校准。最后，服务器通过聚合局部流形来动态优化全局语义流形和结构流形。在 11 个同质和异质图上的广泛实验表明，FedGMC 有效地平衡了全局共性和局部个性化，从而显著优于最先进的基准方法。

## Abstract
Graph Federated Learning (GFL) enables collaborative representation learning across distributed subgraphs while preserving privacy. However, heterogeneity remains a critical challenge, as subgraphs across clients typically differ significantly in both semantics and structures. Existing methods address heterogeneity by enforcing the rigid alignment of model parameters or prototypes between clients and the server. However, these alignments implicitly rely on a restrictive global linearity assumption that summarizes local data distributions using a single and globally consistent representation space. This severely compresses the personalized representation space of clients and fails to preserve diverse local graph distributions. To overcome these limitations, we propose Federated Graph Manifold Calibration (FedGMC), a novel paradigm that tackles semantic heterogeneity and structural heterogeneity from a unified manifold perspective. Instead of enforcing rigid alignment, FedGMC introduces a dual manifold calibration mechanism that preserves global commonalities while maximizing the personalized representation space of local clients. Specifically, for semantic heterogeneity, the server constructs a geometrically optimal semantic manifold via equidistant semantic anchors, so as to guide the calibration of local semantic manifolds. For structural heterogeneity, the server constructs a global structural manifold by building global structural templates, so as to guide the calibration of local structural manifolds. Finally, the server dynamically refines both global semantic manifolds and structural manifolds by aggregating local manifolds. Extensive experiments on eleven homophilic and heterophilic graphs demonstrate that FedGMC effectively balances global commonality and local personalization, thereby significantly outperforming state-of-the-art baseline methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Rigid Alignment: Graph Federated Learning via Dual Manifold Calibration》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
图联邦学习（GFL）旨在保护隐私的前提下，跨分布式子图进行协作学习。然而，**异构性**（语义异构和结构异构）是其核心挑战。
*   **现有局限：** 传统方法（如 FedProx, FedProto）依赖“刚性对齐”（Rigid Alignment），即强制客户端的参数或原型向全局一致的空间靠拢。
*   **研究动机：** 这种刚性对齐隐含了“全局线性假设”，会严重压缩客户端的个性化表示空间，导致局部独特的图分布特征（如特定的拓扑模式）丢失。
*   **核心目标：** 提出 **FedGMC** 框架，从流形校准（Manifold Calibration）的角度出发，在保留全局共性的同时，最大化本地客户端的个性化表达。

### 2. 方法论：核心思想与关键技术
FedGMC 引入了**双流形校准机制**，分别处理语义和结构异构性：

*   **语义流形校准（Semantic Manifold Calibration）：**
    *   **全局参考：** 服务器利用单纯形等距紧框架（Simplex ETF）构建几何最优的语义锚点，确保类别间距离最大化。
    *   **本地校准：** 客户端提取不含结构的“自我嵌入”（ego-embeddings），通过**正交 Procrustes 变换**（一种等距映射，仅包含旋转和反射）将本地流形对齐到全局锚点，从而在对齐的同时完整保留本地语义的内在几何结构。
*   **结构流形校准（Structural Manifold Calibration）：**
    *   **全局参考：** 服务器维护一组全局结构模板。
    *   **本地校准：** 客户端通过多跳邻居嵌入构建“径向序列矩阵”来表征局部拓扑。利用**最优传输（Optimal Transport）**算法（Sinkhorn-Knopp）将本地结构流形与全局模板进行匹配。
*   **全局流形动态优化：**
    *   服务器根据本地反馈动态调整全局流形。语义上采用难度加权更新并进行超球面投影；结构上通过求解 **Gromov-Wasserstein 重心问题**来融合各客户端的拓扑特征。

### 3. 实验设计
*   **数据集：** 涵盖了 11 个主流基准数据集，包括 6 个同质图（Cora, CiteSeer, PubMed, Amazon-Computer/Photo, ogbn-arxiv）和 5 个异质图（Roman-empire, Amazon-ratings, Minesweeper, Tolokers, Questions）。
*   **实验场景：** 设置了“非重叠”和“重叠”两种子图划分方案，并针对不同客户端数量（5, 10, 20, 30, 50）进行了 66 种场景测试。
*   **对比方法（Benchmark）：** 共 13 种基准方法，包括传统 FL（FedAvg, FedProx, FedPer）、通用 GFL（GCFL, FedGNN, FedSage+）以及最新的个性化 GFL（FED-PUB, FedGTA, AdaFGL, FedTAD, FedIIH, FedICI, FedSSA）。

### 4. 资源与算力
*   **硬件环境：** Linux 服务器，配备 Intel Xeon Gold 6326 CPU (2.90 GHz)，64 GB RAM。
*   **GPU 资源：** 使用了两块 **NVIDIA GeForce RTX 4090 (48 GB 显存)**。
*   **软件框架：** Python 3.8.8, PyTorch 1.12.0, PyTorch Geometric (PyG) 2.5.1。
*   **效率表现：** 论文提到 FedGMC 收敛速度较快（平均约 40 轮），且单轮通信耗时低于 FedIIH 等强基准方法。

### 5. 实验数量与充分性
*   **实验规模：** 进行了大规模的对比实验，每组实验重复 10 次并报告均值和标准差，确保了结果的统计显著性。
*   **维度全面：** 除了主实验外，还包括：
    *   **消融实验：** 验证了语义校准、结构校准和全局优化三个组件的必要性。
    *   **收敛性分析：** 展示了在不同数据集上的快速收敛能力。
    *   **敏感性分析：** 探讨了超参数 $Q, B, \tau, \eta$ 对性能的影响。
    *   **案例研究：** 通过 t-SNE 可视化直观展示了流形校准的效果。
*   **评价：** 实验设计非常充分，覆盖了同质/异质图及多种划分场景，对比方法权威且前沿，结果客观公平。

### 6. 主要结论与发现
*   **性能领先：** FedGMC 在所有 11 个数据集上均显著优于现有最先进方法（SOTA），尤其在处理复杂的异质图时表现突出。
*   **流形校准的优越性：** 证明了相比于“刚性对齐”，通过等距变换进行的“流形校准”能更好地平衡全局共性与本地个性化。
*   **结构保护：** 显式地对拓扑结构进行流形建模和校准，对于提升图神经网络在联邦环境下的泛化能力至关重要。

### 7. 优点（亮点）
*   **视角新颖：** 首次将流形校准范式引入图联邦学习，打破了传统的全局线性假设。
*   **数学严谨：** 利用 Simplex ETF、Procrustes 变换和最优传输理论，为异构性处理提供了坚实的几何数学基础。
*   **统一框架：** 在一个流形视角下同时解决了语义和结构两种不同维度的异构性问题。

### 8. 不足与局限
*   **超参数依赖：** 框架涉及 4 个关键超参数（$Q, B, \tau, \eta$），虽然敏感性分析显示其相对稳健，但在实际应用中仍需一定的人工调优。
*   **计算复杂度：** 尽管单轮耗时可控，但涉及 SVD 分解和最优传输（Sinkhorn/Gromov-Wasserstein）计算，在超大规模图或极多客户端场景下的扩展性仍有待进一步验证。
*   **自适应性：** 目前超参数是静态设置的，未来若能实现超参数的自适应调整将更具实用价值。

（完）
